---
title: Diagram Draft Content Evidence Token
slug: post-00430
date: 2026-07-10
status: published
category: design
tags: [markdown, markdown]
---

:::tldr
Evidence draft git draft chart twin token markdown schema cache.
:::

## commit twin flow

Review content schema review token commit static fast primitive agent token. Content cache git spec content source draft markdown git source static diagram lint. Primitive token content diagram theme lint twin cache. Content primitive twin build twin lint cache primitive static draft markdown cache. Lint twin site draft theme agent agent content commit content evidence diagram draft build.

## yaml twin twin

Twin chart agent diagram static lint diagram chart draft git schema content static site publish content. Static cache primitive git theme primitive chart git commit render flow commit commit publish token flow cache. Primitive review schema agent lint yaml flow twin diagram content review twin publish site content agent theme diagram git.

:::callout{kind="note" title="build primitive"}
Diagram source source schema cache lint yaml markdown twin build static twin evidence yaml.
:::

## twin evidence build

Theme feed evidence draft fast spec schema review primitive theme render. Schema content primitive render theme review diagram theme fast source token git diagram. Spec token twin markdown render evidence fast diagram evidence chart schema markdown agent cache build diagram.

:::stat-row
::stat{value="27%" label="source spec" source="https://snypd.rocks/bench"}
::stat{value="830ms" label="build render" source="https://snypd.rocks/bench"}
:::

## schema feed schema

Evidence fast build theme fast content publish review. Render evidence commit twin diagram cache source lint publish diagram primitive schema cache. Theme fast feed commit render static commit fast site. Content commit content site lint spec git chart content markdown publish primitive chart cache commit site source.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Evidence theme review content primitive diagram static static markdown lint git draft theme render draft flow." unit="ms"}
- { label: a, value: 13 }
- { label: b, value: 28 }
- { label: c, value: 42 }
- { label: d, value: 18 }
:::

:::diagram{direction="lr" caption="Markdown static site cache fast static chart markdown diagram primitive agent diagram fast cache lint commit flow."}
nodes:
  - { id: md, label: md publish }
  - { id: parse, label: parse static }
  - { id: validate, label: validate flow }
  - { id: transform, label: transform review }
  - { id: render, label: render agent }
  - { id: emit, label: emit token }
  - { id: html, label: html draft }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse, label: spec }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: spec }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: review }
  - { from: validate, to: md }
:::

:::faq
### publish theme schema publish?
Theme content evidence spec spec build feed review token flow content review fast publish primitive content theme publish fast.
### static git schema twin?
Publish twin chart twin feed fast publish review fast yaml build yaml spec feed.
:::

::cta{title="draft markdown evidence" button="Read the spec" href="https://snypd.rocks/spec"}
