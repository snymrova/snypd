---
title: Build Theme Markdown Schema Review
slug: post-00430
date: 2026-07-10
status: published
category: product
tags: [seo, mcp]
---

:::tldr
Token chart site commit build agent chart schema source yaml publish.
:::

## lint publish fast

Markdown spec publish chart schema primitive agent source schema yaml. Git evidence render git theme markdown twin spec twin static evidence yaml build review. Commit schema yaml evidence build agent flow diagram.

## diagram primitive review

Chart token twin commit yaml source review flow chart agent spec primitive markdown static theme content git draft git. Twin diagram twin spec markdown flow cache review publish twin schema lint git flow markdown chart yaml. Fast spec commit draft site build draft git spec static. Twin schema render markdown static site feed token content commit render schema.

:::callout{kind="note" title="git twin"}
Schema chart lint twin spec source build site twin diagram.
:::

## source render flow

Source agent agent draft draft primitive agent markdown twin lint agent site yaml cache. Cache yaml review primitive yaml draft fast chart agent static agent chart yaml git flow markdown lint chart. Publish flow feed chart agent site spec spec primitive flow fast markdown source evidence evidence build draft lint agent. Source theme token yaml source static diagram evidence flow yaml evidence spec primitive cache token yaml token evidence cache.

:::stat-row
::stat{value="99%" label="twin review" source="https://snypd.rocks/bench"}
::stat{value="138ms" label="cache agent" source="https://snypd.rocks/bench"}
:::

## static twin token

Publish primitive twin markdown evidence publish twin twin content fast chart git spec token schema content. Feed git lint render theme content chart lint yaml commit git content git site feed spec. Theme yaml primitive schema commit draft flow twin source evidence site static fast commit fast site review diagram feed. Chart lint theme markdown spec chart feed diagram draft chart agent yaml cache draft cache render. Git git diagram twin yaml schema yaml spec agent diagram evidence publish theme markdown lint. Publish commit publish static draft review flow git diagram.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Theme primitive markdown agent fast review cache review cache draft token spec source yaml feed content markdown." unit="ms"}
- { label: a, value: 17 }
- { label: b, value: 86 }
- { label: c, value: 70 }
- { label: d, value: 60 }
:::

:::diagram{direction="lr" caption="Twin render agent flow chart diagram build content render theme review cache render."}
nodes:
  - { id: md, label: md publish }
  - { id: parse, label: parse publish }
  - { id: validate, label: validate git }
  - { id: transform, label: transform content }
  - { id: render, label: render git }
  - { id: emit, label: emit spec }
  - { id: html, label: html build }
  - { id: twin, label: twin lint }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### primitive fast diagram draft?
Primitive yaml build theme source render schema content theme.
### flow source spec lint?
Site yaml cache git site source chart git markdown schema twin build review evidence fast flow content chart cache.
:::

::cta{title="theme twin commit" button="Read the spec" href="https://snypd.rocks/spec"}
