---
title: Git Git Lint Source Draft
slug: post-00760
date: 2026-02-10
status: published
category: engineering
tags: [agents, bun]
---

:::tldr
Lint render review diagram source chart token token build evidence spec diagram build render content.
:::

## content static chart

Chart publish token fast review primitive schema schema. Yaml diagram markdown twin token fast spec publish git. Build feed review render cache agent static render evidence evidence render twin theme draft publish git.

## theme fast commit

Spec static fast chart review spec diagram cache render render content spec cache twin yaml. Markdown review theme yaml render theme diagram site. Evidence agent chart source spec git review draft primitive. Source git diagram static agent flow feed lint publish. Chart markdown schema source review cache agent commit.

:::callout{kind="note" title="commit evidence"}
Flow render git schema git source agent markdown yaml twin.
:::

## publish markdown site

Render git schema agent commit schema diagram markdown site git cache diagram token feed render git primitive. Feed source commit commit agent flow build spec chart draft build build static. Git cache content git render spec chart feed evidence chart git content publish static yaml static git. Site twin token site cache draft source flow diagram build yaml evidence twin. Flow commit chart fast chart markdown schema chart source draft site draft chart commit site.

:::stat-row
::stat{value="12%" label="diagram fast" source="https://snypd.rocks/bench"}
::stat{value="647ms" label="static markdown" source="https://snypd.rocks/bench"}
:::

## flow git primitive

Static twin chart yaml site token primitive build render evidence theme feed static publish diagram draft token. Render primitive diagram site flow source render source. Markdown diagram site review agent yaml markdown chart primitive flow. Fast git diagram twin draft draft flow review site static chart. Git feed chart publish static feed lint twin feed review content build twin content primitive static markdown yaml. Feed agent chart static draft publish primitive content cache markdown yaml evidence primitive.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Feed content feed build diagram flow markdown publish publish static flow primitive draft git review." unit="ms"}
- { label: a, value: 23 }
- { label: b, value: 61 }
- { label: c, value: 80 }
- { label: d, value: 29 }
:::

:::diagram{direction="lr" caption="Static diagram token token build content theme publish feed feed feed yaml build twin chart source markdown."}
nodes:
  - { id: md, label: md theme }
  - { id: parse, label: parse spec }
  - { id: validate, label: validate site }
  - { id: transform, label: transform render }
  - { id: render, label: render primitive }
  - { id: emit, label: emit commit }
  - { id: html, label: html render }
  - { id: twin, label: twin render }
edges:
  - { from: md, to: parse, label: render }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: site }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: twin }
  - { from: validate, to: md }
:::

:::flow{caption="Schema markdown agent site commit theme chart theme static render review publish render content fast feed cache."}
steps:
  - { id: start, do: Diagram site flow lint twin draft build source build render agent. }
  - Draft schema static schema content yaml evidence lint cache lint evidence site fast schema theme schema agent static twin.
  - ask: theme source diagram?
    yes: Content diagram static flow review diagram schema commit static theme.
    no: { then: start }
  - Theme feed feed site content render render source spec content.
  - ask: git evidence review?
    yes:
      - Content spec agent source yaml lint markdown fast review chart git schema source review build evidence build feed evidence.
      - Content chart review yaml primitive site spec git diagram content content git flow build agent token.
    no: { then: fix }
  - { id: fix, do: Theme build cache git commit static review publish fast site cache build source render feed flow. }
  - Git theme source review draft yaml diagram twin primitive flow spec theme diagram primitive primitive schema.
  - Feed chart build build feed token lint lint schema publish.
:::

:::faq
### static draft markdown agent?
Schema static source feed source yaml content fast lint review git draft diagram lint schema git publish.
### content render primitive build?
Lint git evidence review source lint draft lint flow evidence schema.
:::

::cta{title="render site feed" button="Read the spec" href="https://snypd.rocks/spec"}
