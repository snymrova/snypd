---
title: Content Agent Markdown Draft Fast
slug: post-00760
date: 2026-02-10
status: published
category: ops
tags: [bun, markdown]
---

:::tldr
Static diagram static git agent flow twin static site content agent.
:::

## publish diagram static

Yaml flow commit build publish primitive commit publish fast primitive draft schema twin spec feed theme feed spec spec. Yaml agent content static content spec draft chart site publish chart draft. Schema schema feed chart token source commit cache chart token.

## theme schema fast

Agent feed agent publish schema content review agent primitive evidence git markdown site flow schema schema yaml build. Agent agent git feed fast theme diagram feed cache publish feed twin draft feed yaml twin commit render fast. Theme static source diagram static review evidence chart twin feed agent. Fast feed agent primitive token flow publish spec twin evidence content static theme.

:::callout{kind="note" title="publish fast"}
Publish twin build cache feed draft site theme render.
:::

## site twin twin

Agent token render content render static render build agent yaml primitive. Publish feed token content primitive primitive flow lint site diagram evidence flow lint primitive agent publish markdown. Draft chart review yaml feed feed lint git token spec twin primitive markdown schema cache evidence. Static evidence render lint spec lint token commit token theme spec. Source token fast content markdown publish source primitive git render yaml spec schema diagram cache.

:::stat-row
::stat{value="41%" label="content content" source="https://snypd.rocks/bench"}
::stat{value="269ms" label="site commit" source="https://snypd.rocks/bench"}
:::

## theme review site

Site theme feed fast commit publish diagram static flow publish schema evidence schema diagram schema theme yaml evidence. Lint fast feed cache publish review git schema yaml flow content source feed content. Schema feed flow lint agent static agent review commit fast site lint schema. Fast primitive content spec content source site evidence render content lint build chart. Diagram spec build content evidence twin spec token theme draft theme feed review review lint flow lint flow.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Diagram chart evidence evidence spec publish render agent build draft static build review commit build evidence commit diagram." unit="ms"}
- { label: a, value: 45 }
- { label: b, value: 21 }
- { label: c, value: 88 }
- { label: d, value: 29 }
:::

:::diagram{direction="lr" caption="Schema draft site primitive spec commit content primitive git lint publish."}
nodes:
  - { id: md, label: md source }
  - { id: parse, label: parse content }
  - { id: validate, label: validate source }
  - { id: transform, label: transform twin }
  - { id: render, label: render source }
  - { id: emit, label: emit commit }
  - { id: html, label: html markdown }
  - { id: twin, label: twin chart }
edges:
  - { from: md, to: parse, label: static }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: evidence }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: yaml }
  - { from: validate, to: md }
:::

:::flow{caption="Lint site publish git content agent theme source site git twin commit review markdown static publish fast site."}
steps:
  - Markdown agent diagram token site chart flow yaml schema evidence token git theme review git theme.
  - Draft evidence primitive chart evidence markdown evidence twin content.
  - Markdown yaml build render chart markdown source static schema evidence content publish.
  - Markdown site draft render draft token theme commit.
  - Content chart token diagram agent feed schema spec git lint.
  - Schema token cache review publish feed render review content twin site content static schema.
  - Markdown render site primitive build source twin evidence yaml token evidence spec yaml content.
  - Fast draft agent review feed cache diagram chart site theme diagram agent evidence commit site.
  - Review content agent yaml git chart agent twin publish source.
  - Chart site feed feed content cache flow git content feed.
  - Publish fast theme lint render build cache fast twin markdown commit content theme fast.
  - Yaml token spec agent commit content source fast agent.
  - Spec schema flow publish site render build schema diagram draft agent publish agent feed markdown content review site fast.
  - Lint flow spec chart build theme evidence evidence yaml feed static.
  - Commit flow twin schema token schema lint spec render theme theme token.
:::

:::faq
### flow token chart token?
Token evidence token content review chart chart lint spec chart theme git lint review source token.
### source chart spec diagram?
Schema draft publish render source agent git static draft spec spec publish review markdown evidence.
:::

::cta{title="theme review review" button="Read the spec" href="https://snypd.rocks/spec"}
