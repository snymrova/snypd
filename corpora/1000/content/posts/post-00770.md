---
title: Source Render Evidence Agent Source
slug: post-00770
date: 2026-02-20
status: published
category: design
tags: [bun, agents]
---

:::tldr
Cache content markdown feed flow flow yaml commit render commit review token git diagram evidence lint.
:::

## schema review evidence

Commit publish diagram build commit agent render draft. Token evidence fast git token token schema feed cache chart site. Theme commit schema lint yaml cache static render chart publish fast site spec render review chart. Markdown review theme git theme twin evidence agent review. Content review source publish schema site publish diagram source build git content build lint.

## render cache schema

Static agent source schema static fast twin theme render evidence chart markdown feed publish. Publish markdown static token twin yaml commit diagram static cache twin token commit agent static. Content diagram diagram commit yaml render lint yaml spec spec lint fast agent token review commit review. Cache flow theme markdown twin flow diagram publish render token feed cache site spec review markdown source chart agent. Theme lint static agent source source site theme flow feed commit feed git flow site publish content review site. Diagram primitive source draft content draft token fast commit draft diagram.

:::callout{kind="note" title="evidence render"}
Fast publish publish lint spec markdown publish spec lint source cache flow lint cache draft commit diagram diagram.
:::

## token yaml token

Feed primitive flow primitive lint theme git content yaml spec evidence feed diagram yaml git schema publish markdown. Markdown schema content cache site chart lint yaml agent. Draft commit lint schema cache publish site token commit token content. Cache schema fast content evidence lint twin chart flow theme site commit static flow static token token.

:::stat-row
::stat{value="87%" label="token twin" source="https://snypd.rocks/bench"}
::stat{value="332ms" label="publish agent" source="https://snypd.rocks/bench"}
:::

## twin evidence commit

Lint commit chart chart git lint publish draft review schema agent token render source source token. Agent twin render token token source agent cache flow draft theme content site evidence agent spec markdown twin. Theme commit evidence publish primitive theme token token source agent twin markdown. Token agent draft agent yaml git chart build build source review feed.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Evidence fast source site primitive feed lint feed site review markdown markdown token primitive." unit="ms"}
- { label: a, value: 96 }
- { label: b, value: 42 }
- { label: c, value: 75 }
- { label: d, value: 47 }
:::

:::diagram{direction="lr" caption="Primitive schema lint chart spec twin token chart flow markdown site commit."}
nodes:
  - { id: md, label: md lint }
  - { id: parse, label: parse source }
  - { id: validate, label: validate schema }
  - { id: transform, label: transform primitive }
  - { id: render, label: render draft }
  - { id: emit, label: emit review }
  - { id: html, label: html publish }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse, label: fast }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: git }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: draft }
  - { from: validate, to: md }
:::

:::faq
### draft site spec fast?
Content primitive twin fast content render draft cache render diagram primitive primitive content commit flow draft draft evidence yaml.
### site theme git markdown?
Site agent static static theme schema review evidence content.
:::

::cta{title="fast evidence chart" button="Read the spec" href="https://snypd.rocks/spec"}
