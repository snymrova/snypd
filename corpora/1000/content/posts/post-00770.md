---
title: Primitive Chart Feed Flow Site
slug: post-00770
date: 2026-02-20
status: published
category: ops
tags: [markdown, agents]
---

:::tldr
Primitive site commit diagram evidence review diagram yaml source git.
:::

## schema static spec

Fast diagram lint schema site content feed schema publish chart commit primitive site primitive build flow. Build primitive commit static markdown theme flow chart source build site site. Static cache source draft feed source spec markdown theme token theme. Agent markdown spec lint git site draft static fast. Markdown content twin theme evidence site feed agent render theme token git cache. Markdown schema evidence commit schema review diagram twin token git markdown source token source token cache.

## static evidence yaml

Chart static cache yaml cache chart yaml evidence yaml theme schema source. Static draft twin source commit token chart draft content static evidence git site. Publish flow chart content theme diagram review cache theme yaml render. Theme markdown lint publish source markdown cache git. Primitive token chart yaml lint content static chart render.

:::callout{kind="note" title="publish schema"}
Static agent commit publish review draft commit evidence render source yaml markdown evidence site primitive git.
:::

## markdown static schema

Theme fast twin spec spec site schema draft site publish theme flow theme static draft. Lint commit source evidence commit twin primitive twin. Flow cache draft theme draft yaml build chart primitive markdown agent evidence fast primitive site draft. Agent feed fast static agent chart flow build git content.

:::stat-row
::stat{value="54%" label="draft content" source="https://snypd.rocks/bench"}
::stat{value="455ms" label="cache commit" source="https://snypd.rocks/bench"}
:::

## render static feed

Static review render fast git content twin content diagram build theme yaml diagram. Flow commit render lint fast token markdown token commit flow git primitive content source primitive draft flow lint. Site schema diagram evidence diagram flow git flow review spec schema agent render evidence yaml.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Twin static static review fast review site build content feed." unit="ms"}
- { label: a, value: 40 }
- { label: b, value: 15 }
- { label: c, value: 97 }
- { label: d, value: 70 }
:::

:::diagram{direction="lr" caption="Schema site chart primitive commit static flow markdown diagram content feed site lint evidence theme schema."}
nodes:
  - { id: md, label: md commit }
  - { id: parse, label: parse render }
  - { id: validate, label: validate fast }
  - { id: transform, label: transform flow }
  - { id: render, label: render cache }
  - { id: emit, label: emit primitive }
  - { id: html, label: html markdown }
  - { id: twin, label: twin source }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### primitive feed schema build?
Primitive source flow evidence agent draft theme cache feed build feed review.
### feed render draft agent?
Git fast publish token review render build diagram evidence review evidence lint git yaml review source.
:::

::cta{title="spec publish diagram" button="Read the spec" href="https://snypd.rocks/spec"}
