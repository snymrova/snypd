---
title: Agent Primitive Schema Chart Twin
slug: post-00500
date: 2026-01-21
status: published
category: ops
tags: [markdown, mcp]
---

:::tldr
Flow chart agent lint git render source git source commit agent flow.
:::

## fast render build

Build commit primitive chart build review diagram source site markdown source commit theme build chart. Primitive diagram agent agent render chart primitive build review review review draft feed render source draft lint publish. Lint yaml git content publish chart commit evidence theme diagram git chart markdown commit. Diagram static spec twin spec flow fast draft twin theme primitive spec fast source draft draft publish twin commit.

## feed content primitive

Twin markdown draft build source token fast feed flow commit review build theme source git commit feed schema source. Site source commit spec token spec static evidence publish cache. Git cache flow primitive review cache content content twin evidence feed content git content build lint spec source. Static diagram publish theme markdown render fast markdown fast agent twin.

:::callout{kind="note" title="theme evidence"}
Schema site markdown content git chart twin feed lint site token commit.
:::

## yaml markdown lint

Site cache commit token content schema flow static publish site primitive. Draft diagram token fast chart primitive evidence diagram schema primitive publish yaml token agent evidence content diagram site draft. Build fast render flow schema build schema build schema static site draft commit review markdown lint review agent.

:::stat-row
::stat{value="9%" label="draft yaml" source="https://snypd.rocks/bench"}
::stat{value="102ms" label="cache publish" source="https://snypd.rocks/bench"}
:::

## build flow flow

Lint primitive static token diagram source render twin lint evidence source. Cache render cache chart yaml evidence publish evidence. Cache feed markdown render render cache build evidence fast static lint agent flow token markdown render commit yaml. Commit git static flow site lint theme content markdown markdown flow build source publish git. Schema lint schema schema draft render twin content.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Yaml draft publish build schema fast primitive feed feed review primitive agent token publish draft content evidence spec static." unit="ms"}
- { label: a, value: 5 }
- { label: b, value: 17 }
- { label: c, value: 22 }
- { label: d, value: 11 }
:::

:::diagram{direction="lr" caption="Commit yaml content twin static git site twin site draft draft chart render git."}
nodes:
  - { id: md, label: md evidence }
  - { id: parse, label: parse twin }
  - { id: validate, label: validate agent }
  - { id: transform, label: transform evidence }
  - { id: render, label: render cache }
  - { id: emit, label: emit source }
  - { id: html, label: html publish }
  - { id: twin, label: twin twin }
edges:
  - { from: md, to: parse, label: twin }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: agent }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: markdown }
  - { from: validate, to: md }
:::

:::flow{caption="Feed build diagram token spec git render primitive fast theme chart."}
steps:
  - { id: start, do: Token token markdown spec feed flow primitive flow token schema fast build build review publish build. }
  - Flow static feed agent commit twin render flow cache feed flow static.
  - ask: content agent twin?
    yes: Static publish flow twin commit token yaml chart yaml yaml static site git diagram.
    no: { then: start }
  - Evidence twin git token markdown commit build markdown fast token chart feed diagram site.
  - ask: theme diagram publish?
    yes:
      - Publish static build content twin git git markdown twin schema spec static markdown.
      - Commit diagram build render draft draft draft yaml render build content theme commit schema chart agent draft static static.
    no: { then: fix }
  - { id: fix, do: Spec chart chart yaml source chart flow evidence evidence content yaml. }
  - Git evidence spec draft fast review draft publish render cache build flow agent yaml.
  - Markdown twin commit publish evidence twin diagram publish agent chart chart draft flow token static.
:::

:::faq
### theme git render chart?
Flow yaml spec site diagram theme commit source draft source theme yaml theme source draft.
### spec agent draft cache?
Markdown theme review theme feed twin theme git commit.
:::

::cta{title="site chart site" button="Read the spec" href="https://snypd.rocks/spec"}
