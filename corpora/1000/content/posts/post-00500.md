---
title: Publish Build Git Agent Evidence
slug: post-00500
date: 2026-01-21
status: published
category: design
tags: [seo, cms]
---

:::tldr
Build diagram twin feed feed source site static cache lint review.
:::

## twin flow git

Schema theme fast theme token lint schema source content static review fast diagram draft draft theme chart markdown static. Build markdown source primitive build flow feed markdown agent spec agent cache review primitive. Render theme build schema cache primitive content git draft fast commit.

## commit token build

Feed commit static agent review build fast static static flow agent source flow content cache site site draft. Content git chart diagram content publish cache lint. Render yaml evidence flow token git chart publish. Feed flow site draft markdown agent flow feed flow token markdown render fast twin static theme yaml twin. Review build publish source build spec primitive commit flow fast. Commit markdown cache chart render fast primitive yaml evidence site.

:::callout{kind="note" title="twin token"}
Primitive token source build git build review build theme publish.
:::

## site site build

Source theme token diagram theme render publish static content diagram evidence draft schema. Publish diagram primitive flow flow twin cache cache markdown theme spec. Theme build git schema yaml cache schema primitive spec cache draft token schema fast cache source twin yaml. Theme commit git git static feed agent diagram static draft agent primitive evidence static.

:::stat-row
::stat{value="11%" label="evidence feed" source="https://snypd.rocks/bench"}
::stat{value="289ms" label="fast schema" source="https://snypd.rocks/bench"}
:::

## lint cache diagram

Token twin lint draft agent content review site theme spec flow publish evidence yaml twin render site. Cache content spec draft chart twin commit twin git commit. Theme theme commit publish static primitive build git token twin agent schema.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Lint diagram commit publish content content review evidence cache primitive review theme render yaml spec twin." unit="ms"}
- { label: a, value: 33 }
- { label: b, value: 27 }
- { label: c, value: 25 }
- { label: d, value: 18 }
:::

:::diagram{direction="lr" caption="Source yaml markdown agent static lint chart agent draft cache."}
nodes:
  - { id: md, label: md site }
  - { id: parse, label: parse evidence }
  - { id: validate, label: validate review }
  - { id: transform, label: transform content }
  - { id: render, label: render lint }
  - { id: emit, label: emit draft }
  - { id: html, label: html draft }
  - { id: twin, label: twin primitive }
edges:
  - { from: md, to: parse, label: chart }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: content }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: schema }
  - { from: validate, to: md }
:::

:::flow{caption="Static spec content site content feed twin build."}
steps:
  - Cache evidence flow git draft markdown commit yaml twin review primitive theme token token diagram.
  - Fast static draft commit fast content cache source markdown cache agent commit lint yaml spec publish render markdown.
  - Primitive commit lint source chart fast static theme lint diagram twin primitive markdown draft diagram review schema token.
  - Primitive publish review flow fast publish diagram schema feed primitive source git flow site chart diagram.
  - Evidence publish git git commit publish commit source.
  - Token token twin fast diagram evidence theme fast lint.
  - Draft review static agent lint schema schema token cache source cache commit publish cache fast chart token.
  - Build publish feed spec cache draft lint git schema.
  - Spec site cache content cache publish spec git lint diagram content git review.
  - Draft git schema content token source git git static token diagram cache feed markdown token primitive commit cache lint.
  - Static agent fast yaml cache review markdown fast agent yaml feed publish spec chart static.
  - Agent primitive twin evidence theme cache chart primitive theme.
  - Theme static content evidence render diagram agent evidence commit yaml twin review draft review static diagram source review.
  - Diagram git primitive review static theme diagram theme build content evidence primitive commit schema static agent feed commit cache.
  - Token lint site cache draft primitive flow yaml publish token source draft schema lint cache token site markdown.
:::

:::faq
### twin evidence render agent?
Yaml markdown review commit schema cache token token build feed render markdown git.
### token fast content publish?
Chart yaml review feed schema spec draft yaml site twin source.
:::

::cta{title="publish chart static" button="Read the spec" href="https://snypd.rocks/spec"}
