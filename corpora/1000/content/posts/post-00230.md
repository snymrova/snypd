---
title: Theme Content Primitive Source Chart
slug: post-00230
date: 2026-08-19
status: published
category: ops
tags: [speed, agents]
---

:::tldr
Chart draft commit primitive build cache render diagram primitive draft token publish static fast publish feed yaml theme twin.
:::

## schema cache markdown

Lint fast render source yaml markdown publish yaml evidence markdown cache chart evidence fast evidence static site. Flow draft source spec git evidence diagram git yaml flow schema publish twin. Yaml review evidence review render diagram lint spec chart diagram evidence. Token review schema diagram content feed draft draft chart primitive cache source fast cache. Evidence site cache markdown yaml static static schema git render chart token evidence source commit content diagram review markdown. Chart evidence commit twin cache yaml diagram primitive theme.

## yaml yaml flow

Review feed site chart token site primitive feed render commit review chart render fast flow token twin yaml. Twin lint agent git source git token content evidence site. Feed git commit markdown review site publish chart site publish lint git source agent markdown git. Cache git publish token cache spec cache build chart review. Source static publish feed theme commit schema chart diagram theme fast feed static diagram primitive feed git diagram.

:::callout{kind="note" title="cache cache"}
Render token build diagram diagram publish review commit render lint schema cache diagram.
:::

## yaml git twin

Schema fast token agent draft source draft token theme diagram agent schema flow diagram evidence theme draft. Diagram diagram build draft lint static token content yaml lint schema fast chart spec yaml twin draft. Schema build git build build token publish spec content primitive.

:::stat-row
::stat{value="52%" label="yaml review" source="https://snypd.rocks/bench"}
::stat{value="354ms" label="primitive diagram" source="https://snypd.rocks/bench"}
:::

## twin feed build

Primitive yaml fast chart yaml theme render cache cache primitive diagram fast diagram flow yaml. Commit site cache token lint flow fast git spec agent fast diagram cache theme feed diagram. Git content static spec yaml markdown yaml primitive chart lint source twin spec source draft twin. Evidence twin draft twin feed draft render evidence yaml content agent git primitive. Site diagram chart site static draft review yaml git yaml agent feed.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Spec static spec publish primitive cache feed evidence theme chart fast build diagram static source diagram publish." unit="ms"}
- { label: a, value: 99 }
- { label: b, value: 72 }
- { label: c, value: 4 }
- { label: d, value: 15 }
:::

:::diagram{direction="lr" caption="Token static token yaml static git content publish twin git lint review static render commit feed lint review."}
nodes:
  - { id: md, label: md lint }
  - { id: parse, label: parse feed }
  - { id: validate, label: validate markdown }
  - { id: transform, label: transform agent }
  - { id: render, label: render primitive }
  - { id: emit, label: emit build }
  - { id: html, label: html content }
  - { id: twin, label: twin source }
edges:
  - { from: md, to: parse, label: theme }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: fast }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: draft }
  - { from: validate, to: md }
:::

:::faq
### render render publish flow?
Chart chart feed schema diagram chart twin build agent agent git.
### commit lint twin chart?
Draft commit build content commit theme publish token site theme token.
:::

::cta{title="commit schema source" button="Read the spec" href="https://snypd.rocks/spec"}
