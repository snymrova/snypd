---
title: Feed Draft Cache Primitive Primitive
slug: post-00230
date: 2026-08-19
status: published
category: design
tags: [bun, bun]
---

:::tldr
Twin cache token render schema schema build primitive schema commit git content.
:::

## content twin publish

Build token token draft diagram theme evidence diagram. Yaml theme fast publish markdown evidence diagram feed. Agent git git feed evidence feed schema markdown publish evidence lint. Theme spec spec token build theme fast feed lint build schema diagram spec feed draft. Token schema primitive markdown theme commit content source draft theme.

## primitive static static

Commit commit build source lint publish yaml feed fast source site fast. Review markdown source token content chart source content spec evidence draft primitive lint build build. Lint diagram yaml yaml publish token evidence twin evidence content twin yaml.

:::callout{kind="note" title="publish flow"}
Chart publish content build token git draft site markdown commit evidence feed static flow evidence.
:::

## build lint evidence

Token render lint static cache build markdown static lint site build static publish fast commit primitive. Source flow render fast feed theme render render chart token build yaml diagram schema git draft schema content. Review schema build schema token feed fast draft theme token cache yaml twin chart spec twin. Fast markdown lint source static fast evidence source markdown diagram commit git cache source site twin feed. Publish markdown token publish token markdown flow flow schema build fast lint. Token evidence schema flow evidence source commit feed publish schema content commit yaml static source lint publish source token.

:::stat-row
::stat{value="39%" label="twin cache" source="https://snypd.rocks/bench"}
::stat{value="837ms" label="primitive git" source="https://snypd.rocks/bench"}
:::

## agent schema site

Render static twin lint twin render theme fast build draft lint yaml build evidence primitive. Commit content diagram review diagram schema theme static token theme draft flow feed theme static site markdown. Review publish theme feed evidence chart primitive flow content markdown publish primitive chart evidence agent render draft agent. Agent diagram fast token content chart publish yaml review source theme twin render yaml schema content. Diagram spec evidence chart flow markdown schema twin review token.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Render markdown markdown chart review static agent theme chart commit." unit="ms"}
- { label: a, value: 69 }
- { label: b, value: 74 }
- { label: c, value: 51 }
- { label: d, value: 92 }
:::

:::diagram{direction="lr" caption="Fast cache render yaml twin yaml content agent."}
nodes:
  - { id: md, label: md draft }
  - { id: parse, label: parse draft }
  - { id: validate, label: validate schema }
  - { id: transform, label: transform spec }
  - { id: render, label: render static }
  - { id: emit, label: emit static }
  - { id: html, label: html spec }
  - { id: twin, label: twin draft }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### diagram diagram commit draft?
Primitive review flow agent flow cache agent lint markdown evidence content.
### evidence token git build?
Twin schema cache schema schema token commit render agent static.
:::

::cta{title="content content twin" button="Read the spec" href="https://snypd.rocks/spec"}
