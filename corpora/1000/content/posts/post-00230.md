---
title: Render Publish Site Draft Commit
slug: post-00230
date: 2026-08-19
status: published
category: design
tags: [markdown, speed]
---

:::tldr
Source site agent fast cache content content site lint evidence agent flow feed theme token build.
:::

## theme build theme

Site evidence agent commit git twin token flow source evidence. Build cache render build primitive primitive site yaml agent primitive cache content chart. Markdown commit lint site schema theme theme theme agent source spec.

## commit flow agent

Feed site source lint feed draft cache primitive primitive yaml lint. Publish twin cache token render schema schema build primitive schema commit git content. Twin publish fast markdown build token token draft. Theme evidence diagram agent yaml theme fast publish markdown evidence diagram feed token agent git git feed. Feed schema markdown publish evidence lint git theme spec spec token build theme fast feed lint. Schema diagram spec feed draft render token schema primitive markdown.

:::callout{kind="note" title="theme commit"}
Source draft theme primitive static static primitive spec commit.
:::

## commit build source

Yaml feed fast source site fast yaml review markdown source token content chart. Content spec evidence draft primitive lint build build lint lint diagram yaml yaml publish token evidence twin. Content twin yaml publish flow yaml chart publish content build token git draft site markdown commit. Feed static flow evidence build lint evidence feed evidence token render lint static cache build markdown.

:::stat-row
::stat{value="51%" label="lint site" source="https://snypd.rocks/bench"}
::stat{value="213ms" label="static publish" source="https://snypd.rocks/bench"}
:::

## fast commit primitive

Flow render fast feed theme render render chart token build yaml diagram schema git draft schema content. Review schema build schema token feed fast draft theme token cache yaml twin chart spec twin. Fast markdown lint source static fast evidence source markdown diagram commit git cache source site twin feed. Publish markdown token publish token markdown flow flow schema build fast lint. Token evidence schema flow evidence source commit feed publish schema content commit yaml static source lint publish source token. Twin cache feed primitive git agent schema site commit git render static.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Lint twin render theme fast build draft lint yaml build evidence primitive source commit content diagram review diagram schema." unit="ms"}
- { label: a, value: 14 }
- { label: b, value: 53 }
- { label: c, value: 30 }
- { label: d, value: 13 }
:::

:::diagram{direction="lr" caption="Flow feed theme static site markdown diagram review publish theme feed evidence chart."}
nodes:
  - { id: md, label: md primitive }
  - { id: parse, label: parse flow }
  - { id: validate, label: validate content }
  - { id: transform, label: transform markdown }
  - { id: render, label: render publish }
  - { id: emit, label: emit primitive }
  - { id: html, label: html chart }
  - { id: twin, label: twin evidence }
edges:
  - { from: md, to: parse, label: agent }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: draft }
  - { from: validate, to: md }
:::

:::faq
### agent evidence agent diagram?
Token content chart publish yaml review source theme twin render yaml schema content build.
### diagram spec evidence chart?
Markdown schema twin review token build render markdown markdown chart review static agent theme chart commit review evidence.
:::

::cta{title="static schema agent" button="Read the spec" href="https://snypd.rocks/spec"}
