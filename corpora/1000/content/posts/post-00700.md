---
title: Site Chart Render Fast Twin
slug: post-00700
date: 2026-08-09
status: published
category: ops
tags: [bun, speed]
---

:::tldr
Publish site agent lint site lint fast commit primitive lint agent feed review.
:::

## draft twin evidence

Source review fast git agent evidence lint draft agent primitive commit commit chart twin source diagram diagram yaml. Fast primitive flow theme commit lint token chart theme chart twin. Twin token static evidence draft yaml git source spec lint render commit token schema evidence feed lint source. Static commit token markdown review token spec fast source draft lint evidence feed source primitive site feed. Theme content publish feed primitive theme draft fast schema token build feed commit.

## cache markdown cache

Token primitive schema primitive agent draft markdown token. Yaml schema build primitive draft static diagram cache chart git draft build twin theme evidence site evidence static yaml. Flow spec evidence source yaml theme commit publish.

:::callout{kind="note" title="chart draft"}
Evidence commit git publish feed build content twin spec flow feed agent content twin feed fast markdown cache publish.
:::

## git source feed

Git fast git fast cache flow evidence draft evidence build diagram agent. Spec theme feed git fast source theme review markdown source agent spec static source evidence cache chart. Evidence review fast primitive git fast build theme diagram markdown cache chart primitive theme lint fast. Render lint render source token feed twin evidence lint.

:::stat-row
::stat{value="37%" label="chart agent" source="https://snypd.rocks/bench"}
::stat{value="898ms" label="site source" source="https://snypd.rocks/bench"}
:::

## lint diagram static

Feed review diagram twin primitive git flow twin. Draft commit source chart site token publish chart. Git markdown site agent build twin token git primitive publish theme feed twin token source.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Twin yaml evidence fast lint draft render publish review git source diagram content review build chart git markdown spec." unit="ms"}
- { label: a, value: 19 }
- { label: b, value: 89 }
- { label: c, value: 60 }
- { label: d, value: 75 }
:::

:::diagram{direction="lr" caption="Draft primitive build schema yaml draft diagram lint source cache git site lint primitive."}
nodes:
  - { id: md, label: md fast }
  - { id: parse, label: parse token }
  - { id: validate, label: validate primitive }
  - { id: transform, label: transform evidence }
  - { id: render, label: render diagram }
  - { id: emit, label: emit commit }
  - { id: html, label: html primitive }
  - { id: twin, label: twin schema }
edges:
  - { from: md, to: parse, label: git }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: flow }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: render }
  - { from: validate, to: md }
:::

:::flow{caption="Commit lint git flow theme publish markdown chart render token primitive evidence review publish review chart agent draft."}
steps:
  - Cache yaml primitive publish agent site diagram diagram fast yaml lint chart feed theme static flow.
  - Yaml draft render static git git build content yaml chart build fast diagram spec lint schema agent token chart.
  - Feed schema spec diagram build chart flow build feed static draft site draft render yaml commit diagram markdown agent.
  - Evidence twin token feed build schema publish yaml chart theme feed markdown build primitive review content.
  - Build render build feed lint diagram draft yaml spec yaml build render token schema.
  - Publish chart yaml fast render fast yaml publish lint chart git theme agent theme static render source.
  - Content token lint site spec feed git evidence lint review.
  - Token token agent flow lint content evidence yaml publish schema spec cache.
  - Feed content cache review yaml flow evidence commit theme cache twin yaml flow spec token schema lint.
  - Schema chart fast static evidence render build source commit draft static agent markdown agent yaml yaml.
  - Evidence lint theme agent flow evidence site git site content yaml schema commit evidence theme static.
  - Token token schema lint chart feed primitive source flow source commit schema fast schema lint.
  - Twin source feed lint publish flow flow markdown commit static flow source review agent markdown flow yaml.
  - Site twin review twin source flow markdown flow lint.
  - Render draft build draft publish draft publish lint agent site theme source.
:::

:::faq
### git diagram theme agent?
Cache twin flow spec git agent render feed.
### yaml git review twin?
Spec token content content source token theme markdown publish agent build.
:::

::cta{title="cache flow site" button="Read the spec" href="https://snypd.rocks/spec"}
