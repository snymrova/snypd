---
title: Fast Publish Token Lint Site
slug: post-00700
date: 2026-08-09
status: published
category: ops
tags: [markdown, seo]
---

:::tldr
Schema git cache source render twin twin source site static schema fast theme build content flow render.
:::

## token site commit

Site yaml schema publish token schema diagram spec. Review feed feed primitive evidence lint token markdown spec source render diagram markdown diagram feed. Agent markdown site diagram draft agent twin twin markdown static content source build diagram source site publish markdown. Review static flow site agent draft schema evidence evidence.

## publish twin schema

Publish draft static spec cache lint cache cache site. Theme git theme fast markdown chart commit chart agent git static. Git draft publish markdown publish yaml feed publish build git fast.

:::callout{kind="note" title="build token"}
Site diagram spec feed feed diagram git commit markdown spec.
:::

## source flow theme

Diagram evidence cache site commit publish theme site draft source primitive schema content site schema agent. Commit schema diagram evidence site evidence git evidence cache twin. Feed source markdown git static theme primitive diagram draft content primitive schema lint feed theme agent markdown evidence build.

:::stat-row
::stat{value="24%" label="build git" source="https://snypd.rocks/bench"}
::stat{value="834ms" label="fast static" source="https://snypd.rocks/bench"}
:::

## primitive evidence markdown

Evidence yaml diagram git markdown commit source draft diagram review source publish. Publish diagram fast content render source flow review yaml diagram feed fast token feed review primitive agent theme content. Render lint build feed token commit source schema source theme flow draft build token markdown. Markdown spec evidence render theme spec publish source build.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Diagram primitive chart chart twin evidence flow fast render schema schema chart lint diagram chart spec draft theme evidence." unit="ms"}
- { label: a, value: 22 }
- { label: b, value: 10 }
- { label: c, value: 94 }
- { label: d, value: 30 }
:::

:::diagram{direction="lr" caption="Publish draft commit review cache flow schema draft source static."}
nodes:
  - { id: md, label: md evidence }
  - { id: parse, label: parse build }
  - { id: validate, label: validate build }
  - { id: transform, label: transform cache }
  - { id: render, label: render render }
  - { id: emit, label: emit lint }
  - { id: html, label: html diagram }
  - { id: twin, label: twin content }
edges:
  - { from: md, to: parse, label: diagram }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: static }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: build }
  - { from: validate, to: md }
:::

:::flow{caption="Yaml feed primitive diagram spec token commit draft commit spec diagram fast spec."}
steps:
  - { id: start, do: Schema token feed flow markdown theme yaml static theme commit chart git flow git review commit diagram schema. }
  - Source twin source render schema markdown yaml twin yaml fast chart primitive.
  - ask: primitive git render?
    yes: Markdown token feed yaml yaml primitive commit publish token twin commit fast flow yaml diagram render spec site.
    no: { then: start }
  - Diagram primitive chart spec fast draft chart review feed schema git yaml.
  - ask: lint review render?
    yes:
      - Yaml render static schema evidence content static fast flow commit commit token chart diagram draft agent.
      - Chart draft spec chart primitive agent site git git content site feed theme evidence feed.
    no: { then: fix }
  - { id: fix, do: Feed source fast publish markdown theme agent chart chart. }
  - Spec diagram site publish schema yaml lint evidence chart markdown twin.
  - Spec render chart review render cache primitive site agent draft render fast evidence publish.
:::

:::faq
### draft markdown site agent?
Twin yaml markdown schema schema evidence yaml lint content git flow.
### source diagram commit static?
Diagram theme render build cache evidence yaml spec.
:::

::cta{title="lint source theme" button="Read the spec" href="https://snypd.rocks/spec"}
