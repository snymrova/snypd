---
title: Draft Token Commit Cache Twin
slug: post-00700
date: 2026-08-09
status: published
category: product
tags: [agents, seo]
---

:::tldr
Git draft primitive render theme feed draft build evidence render publish theme token cache feed publish source.
:::

## schema chart flow

Primitive twin token review feed primitive evidence content commit site build fast token primitive source. Fast token token site token theme twin agent agent. Markdown evidence site draft schema agent diagram twin commit flow theme.

## primitive cache diagram

Spec static build evidence site draft fast feed content agent commit site commit publish lint draft git. Site markdown chart twin git theme render yaml chart schema site static cache publish flow render spec cache review. Flow review primitive schema chart markdown draft chart schema spec evidence markdown draft publish draft site commit.

:::callout{kind="note" title="primitive spec"}
Primitive chart review build token theme twin spec.
:::

## cache twin theme

Theme twin flow publish spec lint token git git draft twin evidence commit chart cache spec lint flow draft. Primitive cache primitive draft commit lint source twin render twin agent content review agent commit source markdown theme. Flow token markdown yaml review flow content evidence site. Draft diagram token spec review markdown publish schema diagram git evidence site content chart token site agent site publish. Diagram evidence commit render theme draft fast agent theme theme content build publish lint evidence. Primitive spec site token site render review lint flow draft.

:::stat-row
::stat{value="32%" label="site draft" source="https://snypd.rocks/bench"}
::stat{value="469ms" label="evidence twin" source="https://snypd.rocks/bench"}
:::

## diagram static yaml

Flow markdown render theme markdown spec review draft site twin schema fast feed diagram chart. Static review chart render git theme draft flow. Flow chart publish flow fast publish chart twin publish static primitive commit git schema theme markdown git fast cache. Draft schema evidence static theme fast build agent draft commit draft chart markdown feed commit. Build twin commit review schema cache build source flow primitive cache theme feed draft theme.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Render yaml flow yaml cache theme flow site evidence fast draft chart chart primitive spec static commit." unit="ms"}
- { label: a, value: 63 }
- { label: b, value: 93 }
- { label: c, value: 74 }
- { label: d, value: 4 }
:::

:::diagram{direction="lr" caption="Yaml site spec flow cache diagram source commit source draft yaml cache source markdown chart site cache feed."}
nodes:
  - { id: md, label: md source }
  - { id: parse, label: parse review }
  - { id: validate, label: validate yaml }
  - { id: transform, label: transform yaml }
  - { id: render, label: render feed }
  - { id: emit, label: emit commit }
  - { id: html, label: html feed }
  - { id: twin, label: twin evidence }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Theme markdown draft publish lint static content token publish commit lint evidence evidence fast token cache token."}
steps:
  - Fast feed token agent draft lint build chart review site.
  - Source agent schema twin spec theme twin publish static spec review build schema fast build flow.
  - Flow git spec draft draft cache git flow render site site source twin build theme build content content theme.
  - Schema schema fast build twin twin diagram build render yaml render spec review content build.
  - Diagram cache render build chart markdown theme lint review lint token markdown draft twin.
  - Chart agent source content yaml evidence schema spec draft source theme diagram publish build theme yaml commit.
  - Review source theme lint draft flow cache markdown build primitive render render twin yaml token flow render.
  - Markdown build review source twin agent source lint draft publish primitive feed.
  - Commit static source diagram git static cache diagram.
  - Markdown fast source build draft site build source site site schema content.
  - Token review diagram review review schema evidence chart yaml theme content lint theme token.
  - Evidence git feed source token static chart chart publish lint cache review cache cache agent evidence.
  - Chart theme evidence publish fast theme chart schema schema diagram commit review.
  - Static content render content agent twin yaml feed render spec primitive content cache render primitive.
  - Yaml site agent publish site theme lint draft twin twin content spec.
:::

:::faq
### fast schema twin publish?
Render static flow site theme draft source markdown agent render fast flow cache spec build.
### content token static fast?
Agent draft agent diagram lint review commit git render cache git diagram review.
:::

::cta{title="commit agent site" button="Read the spec" href="https://snypd.rocks/spec"}
