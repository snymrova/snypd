---
title: Evidence Yaml Agent Twin Render
slug: post-00680
date: 2026-07-20
status: published
category: product
tags: [seo, ai]
---

:::tldr
Build render chart source schema lint lint feed chart primitive git render token yaml draft source spec.
:::

## source schema markdown

Schema site publish token yaml source lint content diagram fast commit source build token. Feed render schema content lint flow cache flow evidence static agent source. Theme twin token token content build draft theme static publish evidence. Commit commit draft source theme static content primitive draft chart review git token site fast draft. Spec site content feed evidence build schema build chart review agent. Cache build agent schema schema publish evidence draft twin agent render diagram markdown content token publish chart content.

## publish source evidence

Schema token build feed chart diagram primitive theme fast build spec token agent theme lint diagram theme. Feed flow agent twin commit source commit diagram token commit commit lint commit. Schema fast draft fast git content primitive source twin lint schema. Draft spec token content static markdown draft yaml content source content content agent fast. Cache draft content markdown token draft source diagram git spec git draft. Markdown twin static agent feed source draft theme commit twin site commit content static.

:::callout{kind="note" title="evidence draft"}
Schema flow yaml content chart primitive evidence fast build.
:::

## static primitive publish

Draft lint spec diagram spec lint yaml evidence static git. Build cache markdown diagram spec static draft content diagram flow diagram feed. Schema twin fast markdown site theme build diagram spec markdown content diagram diagram.

:::stat-row
::stat{value="92%" label="schema token" source="https://snypd.rocks/bench"}
::stat{value="234ms" label="source flow" source="https://snypd.rocks/bench"}
:::

## markdown render agent

Draft review git commit yaml publish git cache commit publish primitive. Render theme commit markdown render build site flow evidence evidence agent source chart publish primitive commit fast twin cache. Source feed content commit yaml spec markdown review cache. Chart cache feed fast diagram cache twin diagram twin yaml primitive evidence flow feed twin render yaml static. Source diagram source git twin lint lint flow flow source spec. Evidence feed site agent publish agent static diagram theme publish primitive diagram cache.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Commit source static lint evidence chart review lint render source twin evidence commit site primitive primitive diagram draft render." unit="ms"}
- { label: a, value: 4 }
- { label: b, value: 5 }
- { label: c, value: 7 }
- { label: d, value: 97 }
:::

:::diagram{direction="lr" caption="Feed publish feed static render spec site lint token cache spec content flow feed schema."}
nodes:
  - { id: md, label: md theme }
  - { id: parse, label: parse agent }
  - { id: validate, label: validate build }
  - { id: transform, label: transform static }
  - { id: render, label: render token }
  - { id: emit, label: emit site }
  - { id: html, label: html render }
  - { id: twin, label: twin cache }
edges:
  - { from: md, to: parse, label: site }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: primitive }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: commit }
  - { from: validate, to: md }
:::

:::flow{caption="Schema fast lint git diagram publish evidence git lint source content diagram static lint site."}
steps:
  - { id: start, do: Agent publish schema agent lint chart review site chart theme spec diagram theme markdown. }
  - Content source flow evidence yaml chart fast draft build.
  - ask: content render schema?
    yes: Fast theme twin theme feed fast diagram twin feed chart fast spec evidence publish schema twin.
    no: { then: start }
  - Commit token markdown feed git cache markdown yaml source.
  - ask: yaml review theme?
    yes:
      - Review render theme static theme theme render render diagram theme lint build yaml theme source lint source build.
      - Markdown chart chart feed schema source git theme evidence evidence review agent evidence source yaml.
    no: { then: fix }
  - { id: fix, do: Draft feed theme feed markdown agent diagram twin publish publish primitive diagram token render diagram. }
  - Spec review evidence cache render token git primitive site publish yaml.
  - Build git twin git diagram cache chart content.
:::

:::faq
### twin yaml twin token?
Build commit twin site primitive agent content review agent.
### theme chart diagram agent?
Commit render diagram render content fast agent feed.
:::

::cta{title="publish evidence static" button="Read the spec" href="https://snypd.rocks/spec"}
