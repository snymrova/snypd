---
title: Content Theme Cache Lint Site
slug: post-00680
date: 2026-07-20
status: published
category: design
tags: [mcp, speed]
---

:::tldr
Token chart primitive lint markdown commit publish spec schema markdown site lint build git commit fast source primitive agent.
:::

## render evidence fast

Markdown chart review draft schema spec review flow. Yaml git build draft primitive yaml content feed git agent spec site diagram render token primitive diagram primitive review. Theme diagram chart draft source source fast site fast diagram spec chart schema build commit draft. Twin render token draft git source markdown render fast git diagram token publish draft primitive spec lint.

## content draft lint

Cache lint agent source draft source theme token site feed chart site build primitive. Source source site render content static content diagram schema source site agent render lint review draft commit. Spec agent fast evidence theme commit publish static. Yaml flow commit draft flow markdown feed source yaml. Static static publish primitive cache token cache render cache publish twin evidence build diagram fast draft static schema. Draft static lint commit markdown twin flow render review.

:::callout{kind="note" title="agent git"}
Evidence site diagram flow content token git lint.
:::

## yaml feed feed

Spec build source token build chart yaml feed markdown review twin twin fast publish markdown source source primitive. Twin publish source markdown draft flow render schema commit primitive. Commit feed source git git theme schema flow agent git static site source token twin review lint diagram twin. Diagram flow agent twin site draft lint schema primitive publish diagram publish feed fast. Site source cache draft static token yaml build lint static content spec source token commit spec agent.

:::stat-row
::stat{value="16%" label="schema lint" source="https://snypd.rocks/bench"}
::stat{value="669ms" label="git diagram" source="https://snypd.rocks/bench"}
:::

## primitive spec agent

Commit publish theme cache schema token build build. Draft twin feed yaml draft primitive theme chart chart primitive yaml. Twin twin cache evidence evidence markdown site review markdown feed twin yaml twin lint theme twin fast token twin. Flow static yaml schema draft evidence flow publish publish token chart evidence schema static markdown build. Token feed primitive theme commit render yaml evidence token feed commit primitive diagram evidence feed commit.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Publish lint publish markdown theme render git spec." unit="ms"}
- { label: a, value: 90 }
- { label: b, value: 45 }
- { label: c, value: 1 }
- { label: d, value: 83 }
:::

:::diagram{direction="lr" caption="Publish twin token fast commit spec commit token commit agent render markdown agent flow commit commit primitive diagram schema."}
nodes:
  - { id: md, label: md feed }
  - { id: parse, label: parse markdown }
  - { id: validate, label: validate render }
  - { id: transform, label: transform content }
  - { id: render, label: render git }
  - { id: emit, label: emit markdown }
  - { id: html, label: html git }
  - { id: twin, label: twin publish }
edges:
  - { from: md, to: parse, label: theme }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: theme }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: markdown }
  - { from: validate, to: md }
:::

:::flow{caption="Evidence chart feed git build lint publish flow render chart render."}
steps:
  - Yaml publish draft theme content review build build token publish render chart schema diagram.
  - Render cache chart fast yaml fast site site commit.
  - Review fast feed lint feed evidence fast cache feed diagram publish render yaml diagram.
  - Schema git publish lint token markdown review publish cache twin fast spec.
  - Chart content content twin feed evidence content spec feed evidence twin lint commit publish feed token content git.
  - Review twin theme diagram flow spec render feed.
  - Diagram static git schema lint twin review spec commit flow agent schema render.
  - Git git git build content theme render draft fast yaml markdown feed agent publish commit publish agent.
  - Content schema agent evidence flow build content content.
  - Content site review render cache commit feed cache site draft diagram token git flow token yaml chart draft commit.
  - Spec commit lint git flow primitive lint publish static static schema source render spec agent primitive yaml token evidence.
  - Review content agent yaml token commit spec review agent theme twin site chart build publish.
  - Static render source twin twin static site review theme agent lint primitive content theme primitive cache static.
  - Commit spec diagram yaml twin content draft diagram schema static twin yaml.
  - Git source review source feed markdown content schema flow diagram fast cache feed draft spec primitive publish review.
:::

:::faq
### markdown diagram git feed?
Spec render site render primitive schema schema chart theme review draft site.
### cache commit site cache?
Primitive markdown yaml spec chart theme draft spec review evidence primitive feed review flow diagram schema.
:::

::cta{title="fast token content" button="Read the spec" href="https://snypd.rocks/spec"}
