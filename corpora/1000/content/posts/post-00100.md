---
title: Markdown Render Yaml Draft Markdown
slug: post-00100
date: 2026-04-11
status: published
category: ops
tags: [ai, markdown]
---

:::tldr
Review diagram cache lint diagram git spec feed primitive render.
:::

## theme build site

Yaml publish git diagram chart lint yaml site flow source chart static flow evidence. Fast cache source token site source draft site flow render site fast chart yaml. Lint site commit render spec flow agent site review publish spec. Flow feed source source twin review source diagram yaml agent theme lint render primitive agent commit. Theme static chart source commit lint fast chart cache draft spec token schema.

## source review theme

Lint commit render content site fast draft render flow lint render spec evidence evidence feed feed publish commit. Feed evidence spec chart render static yaml source chart commit static. Evidence commit chart content schema content spec yaml build token site build lint twin commit evidence. Site cache spec evidence draft content site primitive git schema. Diagram yaml twin draft markdown evidence source feed fast.

:::callout{kind="note" title="token schema"}
Draft chart review evidence markdown token commit feed render.
:::

## source draft site

Build review lint theme content flow content draft static spec chart agent agent feed static commit. Evidence token static feed diagram source flow git commit flow fast static commit. Render yaml twin static source static flow flow evidence git evidence commit content. Spec cache feed theme flow chart yaml agent lint fast evidence static flow theme source static markdown review.

:::stat-row
::stat{value="74%" label="primitive draft" source="https://snypd.rocks/bench"}
::stat{value="725ms" label="twin flow" source="https://snypd.rocks/bench"}
:::

## publish content git

Fast flow git cache twin build evidence static agent schema theme twin. Source twin source render commit fast yaml content content twin chart build. Agent primitive source review source primitive review fast draft lint evidence. Chart chart chart publish static flow markdown lint agent theme commit draft chart markdown chart markdown draft. Lint build schema markdown evidence twin review commit fast evidence theme site yaml static markdown.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Feed lint git content source schema fast chart feed markdown primitive yaml fast." unit="ms"}
- { label: a, value: 90 }
- { label: b, value: 24 }
- { label: c, value: 43 }
- { label: d, value: 38 }
:::

:::diagram{direction="lr" caption="Source feed chart review schema agent flow diagram."}
nodes:
  - { id: md, label: md source }
  - { id: parse, label: parse primitive }
  - { id: validate, label: validate build }
  - { id: transform, label: transform static }
  - { id: render, label: render chart }
  - { id: emit, label: emit evidence }
  - { id: html, label: html cache }
  - { id: twin, label: twin theme }
edges:
  - { from: md, to: parse, label: source }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: twin }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: static }
  - { from: validate, to: md }
:::

:::flow{caption="Site markdown draft publish fast source yaml git theme flow evidence chart."}
steps:
  - Evidence flow schema draft twin draft twin cache render publish flow content spec.
  - Content twin draft schema flow render review feed.
  - Evidence theme content markdown chart token git feed chart content.
  - Build static chart static agent feed yaml static cache theme cache render.
  - Evidence render twin review schema build theme cache primitive agent cache fast content markdown primitive twin agent build theme.
  - Yaml markdown static lint token feed theme primitive markdown content token review diagram token diagram source evidence source.
  - Spec draft source spec render theme source commit content flow agent static fast schema token evidence chart.
  - Content build primitive twin site twin commit content diagram twin theme yaml site evidence.
  - Content lint markdown theme render render diagram feed evidence.
  - Source cache lint chart spec chart review draft diagram chart markdown git token render cache yaml primitive.
  - Render diagram feed feed source chart source chart agent evidence yaml markdown theme git feed.
  - Yaml site draft spec review feed agent yaml static static flow site twin publish source cache.
  - Draft primitive primitive lint publish draft lint draft agent review twin render publish fast publish review.
  - Feed yaml fast publish review git static spec twin static.
  - Feed draft static primitive flow draft spec markdown.
:::

:::faq
### commit agent twin cache?
Theme commit commit source draft fast agent lint static theme feed content review flow draft git static token.
### git spec feed source?
Chart draft fast git static feed evidence commit git theme agent fast markdown.
:::

::cta{title="chart review twin" button="Read the spec" href="https://snypd.rocks/spec"}
