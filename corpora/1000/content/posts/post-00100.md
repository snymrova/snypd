---
title: Git Schema Twin Build Token
slug: post-00100
date: 2026-04-11
status: published
category: engineering
tags: [cms, agents]
---

:::tldr
Fast token schema build primitive agent lint agent publish evidence flow git theme source markdown.
:::

## flow diagram diagram

Lint render commit diagram feed markdown feed render diagram build. Yaml token twin markdown site git commit theme static spec draft. Diagram token evidence primitive markdown flow evidence schema content review content agent evidence lint lint.

## site diagram review

Static content yaml markdown static site markdown flow. Commit agent commit diagram git build twin token schema fast theme review feed content static publish. Agent site feed feed build publish site commit render render schema flow draft content twin publish agent render diagram.

:::callout{kind="note" title="theme commit"}
Source token diagram git static lint feed source theme feed token source.
:::

## diagram evidence source

Review evidence static build markdown render yaml draft markdown feed markdown twin render review diagram. Lint diagram git spec feed primitive render theme build site yaml. Yaml publish git diagram chart lint yaml site flow source chart static flow evidence.

:::stat-row
::stat{value="53%" label="fast cache" source="https://snypd.rocks/bench"}
::stat{value="676ms" label="token site" source="https://snypd.rocks/bench"}
:::

## source draft site

Site fast chart yaml token lint site commit render spec. Agent site review publish spec evidence flow feed source source twin review source diagram yaml agent theme lint. Primitive agent commit draft theme static chart source commit lint. Chart cache draft spec token schema source review theme git diagram lint commit render. Site fast draft render flow lint render spec evidence. Feed feed publish commit token feed evidence spec chart render static yaml source chart commit static.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Evidence commit chart content schema content spec yaml build token site build lint twin commit evidence." unit="ms"}
- { label: a, value: 20 }
- { label: b, value: 47 }
- { label: c, value: 26 }
- { label: d, value: 35 }
:::

:::diagram{direction="lr" caption="Draft content site primitive git schema primitive diagram yaml twin draft markdown evidence source feed fast."}
nodes:
  - { id: md, label: md token }
  - { id: parse, label: parse schema }
  - { id: validate, label: validate theme }
  - { id: transform, label: transform draft }
  - { id: render, label: render chart }
  - { id: emit, label: emit review }
  - { id: html, label: html evidence }
  - { id: twin, label: twin markdown }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Chart static agent feed yaml static cache theme cache render twin evidence render twin."}
steps:
  - Commit feed render source draft site publish review build review lint.
  - Content flow content draft static spec chart agent agent.
  - Static commit site evidence token static feed diagram source flow git commit flow fast static commit site render yaml.
  - Static source static flow flow evidence git evidence commit content diagram spec cache feed theme flow chart yaml agent.
  - Fast evidence static flow theme source static markdown review evidence primitive draft.
  - Twin flow publish content git fast lint fast flow git cache twin build evidence static agent schema.
  - Twin lint source twin source render commit fast yaml.
  - Content twin chart build cache agent primitive source review.
  - Primitive review fast draft lint evidence source chart chart chart publish static flow markdown lint agent theme.
  - Draft chart markdown chart markdown draft yaml lint build schema markdown evidence twin review commit.
  - Evidence theme site yaml static markdown draft feed lint git content source schema fast.
  - Feed markdown primitive yaml fast schema build draft lint markdown source feed chart review schema agent flow.
  - Source primitive build static chart evidence cache theme source twin static draft evidence flow schema draft twin draft.
  - Cache render publish flow content spec markdown content twin draft schema flow render review feed render evidence theme content.
  - Chart token git feed chart content spec build.
:::

:::faq
### review schema build theme?
Primitive agent cache fast content markdown primitive twin agent build theme.
### diagram yaml markdown static?
Token feed theme primitive markdown content token review diagram token diagram source.
:::

::cta{title="evidence source chart" button="Read the spec" href="https://snypd.rocks/spec"}
