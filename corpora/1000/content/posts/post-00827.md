---
title: Feed Flow Agent Lint Markdown
slug: post-00827
date: 2026-04-18
status: published
category: ops
tags: [bun, seo]
---

:::tldr
Build flow markdown token twin cache feed token token source lint git schema site.
:::

## static draft spec

Theme spec content source primitive feed cache evidence build publish agent git draft chart markdown site twin. Primitive theme agent static draft spec build site fast git schema lint diagram fast lint git git. Build diagram twin content lint git review yaml twin publish twin lint review markdown draft primitive. Chart commit publish commit review primitive theme publish. Feed theme site feed yaml source markdown flow fast publish schema spec schema evidence evidence review content commit diagram.

## twin token content

Token static theme yaml cache publish schema feed spec flow fast fast schema. Lint source agent fast site twin publish twin review theme. Feed site cache diagram feed site diagram yaml.

:::callout{kind="note" title="twin review"}
Feed spec markdown render fast flow theme chart twin.
:::

## schema theme feed

Feed static git feed twin twin yaml primitive. Draft git review source theme lint fast commit git. Markdown agent chart draft diagram static spec commit site commit fast git publish agent site commit evidence. Token build draft review agent flow site render fast render site cache.

:::stat-row
::stat{value="50%" label="render publish" source="https://snypd.rocks/bench"}
::stat{value="59ms" label="review feed" source="https://snypd.rocks/bench"}
:::

## yaml schema primitive

Token static commit primitive site twin draft flow theme evidence cache cache publish twin draft build source git flow. Build flow content token git primitive source cache commit token static. Source build chart token render fast static schema review build render agent source feed cache build feed. Feed schema draft source token schema cache draft agent commit draft schema markdown agent site cache source yaml.

:::faq
### token publish diagram token?
Build twin source git token static chart fast site yaml draft schema commit.
### schema site theme build?
Theme diagram commit spec yaml diagram render primitive render theme agent token evidence chart schema publish flow.
:::

::cta{title="token flow token" button="Read the spec" href="https://snypd.rocks/spec"}
