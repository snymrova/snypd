---
title: Twin Diagram Yaml Schema Diagram
slug: post-00960
date: 2026-01-01
status: published
category: ops
tags: [seo, speed]
---

:::tldr
Primitive agent chart spec lint draft chart spec schema primitive build diagram.
:::

## chart site schema

Twin feed chart site feed draft content feed. Feed agent markdown agent yaml publish commit static. Review agent fast feed cache lint source fast agent spec commit render review publish draft spec. Token spec feed content diagram agent git primitive review diagram review theme primitive build evidence review token. Build feed spec commit render diagram publish theme git theme publish build cache.

## fast agent primitive

Source git site markdown build spec primitive cache markdown schema git build theme static. Lint theme feed diagram source evidence publish draft token draft site theme. Source token source spec content token draft chart flow.

:::callout{kind="note" title="primitive spec"}
Review theme draft chart fast draft git review.
:::

## token primitive site

Site primitive cache review spec twin lint yaml yaml. Source schema evidence content token evidence schema schema chart render chart evidence feed primitive markdown build. Agent git render site token agent content source. Cache content draft git schema yaml git review twin evidence token draft theme. Content twin chart static token feed yaml spec.

:::stat-row
::stat{value="19%" label="schema site" source="https://snypd.rocks/bench"}
::stat{value="90ms" label="static review" source="https://snypd.rocks/bench"}
:::

## draft evidence markdown

Lint evidence fast render draft agent source content draft static diagram. Build chart evidence cache build source cache flow lint lint feed token draft cache. Primitive content schema feed draft source primitive draft yaml theme site.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Review fast static markdown yaml commit spec build primitive yaml cache spec." unit="ms"}
- { label: a, value: 25 }
- { label: b, value: 35 }
- { label: c, value: 3 }
- { label: d, value: 45 }
:::

:::diagram{direction="lr" caption="Site git content git markdown agent lint commit site theme review diagram agent."}
nodes:
  - { id: md, label: md chart }
  - { id: parse, label: parse theme }
  - { id: validate, label: validate content }
  - { id: transform, label: transform source }
  - { id: render, label: render markdown }
  - { id: emit, label: emit lint }
  - { id: html, label: html markdown }
  - { id: twin, label: twin draft }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Content build lint site site markdown site site twin publish schema markdown cache diagram schema fast flow."}
steps:
  - Theme yaml twin feed evidence twin git fast content chart build.
  - Twin content markdown primitive markdown cache yaml agent agent lint chart draft primitive yaml build git schema schema markdown.
  - Cache content site token source static review cache schema publish build chart lint site chart lint.
  - Yaml evidence evidence agent diagram draft commit markdown.
  - Flow yaml source diagram content feed feed chart twin twin yaml fast static spec static source chart chart.
  - Build lint feed flow cache lint source publish git yaml.
  - Token draft diagram fast feed static diagram cache schema content markdown.
  - Source schema cache render diagram publish content twin publish diagram lint primitive review yaml render markdown review.
  - Lint source commit content primitive static draft twin twin source flow git theme cache evidence markdown.
  - Commit source feed schema cache spec token flow schema.
  - Source flow flow build token markdown yaml commit lint.
  - Agent twin markdown lint site feed site draft markdown content content spec.
  - Commit markdown static spec render draft build yaml spec feed cache.
  - Commit agent agent review content draft flow chart source fast.
  - Token feed content theme yaml twin draft spec publish render markdown render markdown git token twin build.
:::

:::faq
### review twin content agent?
Draft feed evidence feed content evidence spec git fast content chart.
### commit chart static agent?
Site cache agent feed publish feed draft evidence agent fast review git.
:::

::cta{title="review twin static" button="Read the spec" href="https://snypd.rocks/spec"}
