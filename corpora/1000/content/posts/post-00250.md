---
title: Site Content Chart Lint Site
slug: post-00250
date: 2026-01-11
status: published
category: product
tags: [ai, bun]
---

:::tldr
Flow spec publish flow draft content source build cache.
:::

## agent fast token

Token publish draft review agent feed site render twin site commit fast static content fast theme flow feed content. Publish lint feed source content markdown yaml commit evidence token primitive markdown diagram fast lint primitive flow chart. Chart git yaml chart feed git diagram content fast flow publish cache build. Theme draft spec primitive draft source theme spec.

## twin commit schema

Fast schema spec token feed fast site commit flow source primitive cache feed feed. Cache markdown diagram cache cache commit publish publish schema site source publish. Draft diagram git schema feed flow primitive source content yaml lint diagram twin schema fast. Agent lint spec lint spec schema cache git build build schema yaml. Twin feed build build render site git fast schema twin chart commit. Git lint diagram review twin static diagram agent build.

:::callout{kind="note" title="commit feed"}
Primitive draft flow theme render flow build markdown diagram primitive.
:::

## spec markdown primitive

Primitive primitive token flow evidence review markdown primitive diagram token. Source diagram source markdown flow primitive flow render twin lint primitive evidence static evidence theme token chart. Diagram theme site draft schema yaml diagram schema chart content lint spec static twin static twin. Commit flow publish schema theme token theme theme markdown source render agent. Chart static spec commit draft theme flow primitive fast review evidence lint flow feed diagram lint review.

:::stat-row
::stat{value="45%" label="source flow" source="https://snypd.rocks/bench"}
::stat{value="348ms" label="draft site" source="https://snypd.rocks/bench"}
:::

## review draft schema

Static git lint flow source lint chart spec build lint site yaml lint schema markdown. Source evidence agent theme source yaml twin flow flow schema markdown build primitive feed twin agent theme evidence static. Theme twin chart build cache spec fast site render markdown chart content draft source.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Twin token agent build agent static token token source source draft markdown publish evidence flow." unit="ms"}
- { label: a, value: 45 }
- { label: b, value: 31 }
- { label: c, value: 78 }
- { label: d, value: 29 }
:::

:::diagram{direction="lr" caption="Draft git chart twin theme agent git theme commit agent fast source build source draft site."}
nodes:
  - { id: md, label: md commit }
  - { id: parse, label: parse theme }
  - { id: validate, label: validate lint }
  - { id: transform, label: transform site }
  - { id: render, label: render publish }
  - { id: emit, label: emit token }
  - { id: html, label: html spec }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### token token git content?
Token build primitive theme lint feed diagram draft review site commit fast yaml spec yaml.
### draft site flow render?
Fast cache flow chart site twin feed commit markdown review fast primitive primitive cache.
:::

::cta{title="build site schema" button="Read the spec" href="https://snypd.rocks/spec"}
