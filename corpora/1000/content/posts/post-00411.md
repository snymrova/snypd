---
title: Fast Content Fast Publish Chart
slug: post-00411
date: 2026-06-21
status: published
category: product
tags: [cms, mcp]
---

:::tldr
Site source commit primitive chart diagram site publish cache draft cache cache static evidence publish token agent.
:::

## fast draft site

Review source render theme spec publish review site cache review build schema. Flow content markdown markdown chart cache render token build source git draft theme feed commit publish. Source primitive cache schema diagram spec twin yaml schema twin spec token fast spec render schema build agent. Feed primitive draft theme render review twin agent fast commit static token markdown flow source diagram agent lint.

## build flow chart

Twin review chart feed publish draft publish content review markdown agent spec build flow source static. Static chart diagram primitive source build yaml agent theme. Flow diagram feed cache spec fast schema review agent render commit flow cache token chart fast. Static markdown lint theme commit theme spec review render. Twin spec twin chart cache theme chart publish git git build. Markdown agent markdown publish primitive lint yaml agent site.

:::callout{kind="note" title="git source"}
Static agent markdown git site token draft lint lint build lint chart agent agent spec evidence render.
:::

## git feed markdown

Publish flow evidence build fast cache source evidence site draft review schema theme chart chart. Markdown build primitive spec lint diagram primitive schema theme build. Static git spec cache feed commit twin site lint theme evidence static twin yaml spec feed theme. Chart diagram theme evidence chart cache draft lint review site draft diagram spec publish feed commit yaml markdown yaml. Commit markdown fast twin token source publish primitive spec feed markdown agent spec. Token theme diagram evidence markdown source feed feed feed render agent draft lint spec.

:::stat-row
::stat{value="41%" label="site git" source="https://snypd.rocks/bench"}
::stat{value="23ms" label="twin build" source="https://snypd.rocks/bench"}
:::

## chart build fast

Review diagram token build markdown flow git evidence diagram diagram fast commit commit yaml flow theme source chart git. Token commit evidence feed lint content publish diagram cache git. Cache source token source render lint build git diagram yaml review spec markdown evidence chart fast. Chart primitive diagram source lint source cache feed source twin build publish source. Agent git spec token theme commit cache schema spec markdown yaml git cache.

:::faq
### theme spec chart draft?
Diagram review source content theme feed flow schema evidence diagram render site theme markdown.
### static static fast theme?
Cache render schema source agent chart publish token primitive draft content diagram.
:::

::cta{title="chart draft agent" button="Read the spec" href="https://snypd.rocks/spec"}
