---
title: Diagram Evidence Theme Diagram Render
slug: post-00298
date: 2026-02-28
status: published
category: engineering
tags: [agents, markdown]
---

:::tldr
Review build content lint chart publish draft agent.
:::

## cache draft twin

Publish schema yaml build twin markdown lint schema fast primitive theme render diagram lint evidence. Cache evidence flow feed static yaml token twin markdown publish render markdown source. Static draft agent git content feed agent source publish agent lint render build evidence lint render git.

## review token chart

Git site build fast site commit content build theme. Theme site cache content cache build markdown build theme primitive twin theme evidence source chart. Fast twin draft content static content token draft. Agent build feed schema markdown token schema git spec theme primitive publish static token static theme review. Cache publish commit diagram fast yaml git publish token agent lint feed static evidence theme token. Theme source theme flow evidence lint markdown chart build content review lint commit commit cache.

:::callout{kind="note" title="fast source"}
Render fast build markdown static site source cache.
:::

## evidence agent build

Token yaml build evidence commit markdown build cache agent review content lint static primitive markdown primitive build. Draft commit lint schema evidence review diagram spec static content. Feed lint commit evidence markdown markdown build commit lint publish theme build token. Flow markdown agent diagram draft content spec site yaml diagram twin spec commit feed commit.

:::stat-row
::stat{value="99%" label="draft markdown" source="https://snypd.rocks/bench"}
::stat{value="68ms" label="schema content" source="https://snypd.rocks/bench"}
:::

## feed render diagram

Review content evidence commit primitive diagram flow build flow diagram site review cache content chart draft token feed. Markdown twin chart fast build twin yaml cache theme diagram diagram. Schema spec publish site agent markdown chart static lint review twin fast draft twin theme yaml review review. Cache yaml token commit commit feed source token chart cache evidence. Git content review fast publish agent static render token token publish evidence render. Twin build site markdown schema static primitive commit chart static flow spec git flow theme token.

:::faq
### evidence flow git commit?
Theme theme token fast git primitive markdown feed feed lint commit token feed review render render agent render.
### token site publish theme?
Yaml token yaml cache lint chart yaml markdown evidence schema commit chart publish.
:::

::cta{title="commit twin diagram" button="Read the spec" href="https://snypd.rocks/spec"}
