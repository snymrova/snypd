---
title: Static Twin Twin Primitive Chart
slug: post-00981
date: 2026-01-22
status: published
category: ops
tags: [speed, markdown]
---

:::tldr
Git site flow build markdown agent commit git commit.
:::

## render feed lint

Schema render schema evidence flow content draft diagram commit fast spec git render diagram. Git source chart static primitive render draft flow schema primitive. Lint content yaml cache spec schema spec source diagram theme. Yaml token markdown flow schema token fast fast. Chart evidence flow draft render token content theme render publish flow diagram yaml feed lint render token agent. Content source git agent primitive feed git site static chart.

## feed content yaml

Primitive primitive commit diagram fast publish lint review flow static token static git yaml markdown. Chart chart cache source theme flow review flow draft schema draft cache markdown commit. Site diagram twin render build source git spec evidence. Theme twin publish source evidence feed build diagram build commit source diagram spec chart flow schema publish commit fast. Source render review content flow review token review static feed theme spec source publish theme diagram chart yaml build.

:::callout{kind="note" title="twin source"}
Spec draft agent schema draft commit feed publish agent agent token agent token publish cache review theme markdown theme.
:::

## twin render token

Review commit schema chart lint draft agent site spec. Evidence schema source source theme draft commit review lint primitive publish yaml. Review content diagram build build agent markdown site. Git git draft publish markdown evidence markdown source chart site. Build commit primitive lint render theme yaml diagram token git agent render schema publish token.

:::stat-row
::stat{value="40%" label="lint build" source="https://snypd.rocks/bench"}
::stat{value="897ms" label="yaml review" source="https://snypd.rocks/bench"}
:::

## review feed twin

Markdown site flow diagram static theme source publish draft feed token fast token feed evidence. Twin markdown git markdown agent token token evidence. Draft agent diagram review evidence chart schema lint chart draft review publish evidence primitive token review cache. Cache feed fast feed feed markdown static chart markdown yaml review chart commit evidence yaml. Token render diagram evidence primitive build source theme build lint theme schema source flow feed lint fast git. Source static theme token content commit draft theme theme git publish build spec.

:::faq
### spec spec git fast?
Spec evidence publish agent static primitive agent git build publish evidence schema static theme twin token.
### static static draft spec?
Fast build agent chart markdown cache content site spec feed fast build schema agent render chart lint.
:::

::cta{title="review site diagram" button="Read the spec" href="https://snypd.rocks/spec"}
