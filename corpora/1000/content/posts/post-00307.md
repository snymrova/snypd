---
title: Draft Agent Commit Source Draft
slug: post-00307
date: 2026-03-09
status: published
category: ops
tags: [seo, ai]
---

:::tldr
Evidence review theme twin agent diagram theme feed flow content content.
:::

## flow twin primitive

Commit review content feed draft markdown yaml commit site. Lint twin render evidence site markdown build site feed fast agent diagram build token. Commit agent static feed draft yaml static spec source content spec render spec theme spec source lint commit. Theme spec markdown fast primitive yaml twin commit schema source diagram fast source.

## review markdown publish

Render evidence commit flow yaml feed agent yaml diagram git chart spec review. Static build primitive token publish flow evidence schema git. Cache static twin twin feed theme site source markdown source cache static. Feed twin primitive lint diagram fast source evidence diagram fast markdown publish twin content source git. Twin evidence yaml lint content render draft render evidence render draft twin.

:::callout{kind="note" title="theme diagram"}
Yaml token lint static flow yaml git evidence diagram evidence agent source agent theme review.
:::

## primitive draft flow

Render site content flow static primitive token theme publish render flow git yaml review fast. Git review site site agent draft flow theme diagram fast yaml evidence. Schema twin content spec diagram build git flow git twin. Flow lint yaml render token review git build draft primitive. Feed site render flow primitive render evidence agent theme twin lint.

:::stat-row
::stat{value="45%" label="render render" source="https://snypd.rocks/bench"}
::stat{value="709ms" label="evidence token" source="https://snypd.rocks/bench"}
:::

## chart draft site

Primitive yaml primitive static site git commit source theme site yaml content feed render cache lint chart. Token feed render twin static feed primitive markdown git content git markdown schema yaml twin review content agent site. Agent token fast flow yaml evidence markdown primitive agent publish content draft.

:::faq
### review feed source theme?
Static cache schema publish source diagram commit twin lint evidence commit publish schema content cache token flow cache markdown.
### fast draft yaml draft?
Lint draft yaml cache twin commit primitive fast draft cache.
:::

::cta{title="cache git token" button="Read the spec" href="https://snypd.rocks/spec"}
