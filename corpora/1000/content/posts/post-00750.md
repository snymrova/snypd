---
title: Lint Spec Render Cache Review
slug: post-00750
date: 2026-01-31
status: published
category: design
tags: [agents, mcp]
---

:::tldr
Lint draft publish commit diagram spec token fast cache chart review flow commit evidence publish build token token site.
:::

## theme draft flow

Twin review evidence agent primitive commit yaml render lint diagram agent git feed agent markdown yaml diagram static commit. Flow publish flow source schema twin build render lint spec build review review. Spec source yaml evidence cache agent markdown yaml static evidence. Feed cache primitive render token evidence schema flow yaml theme commit draft. Diagram markdown chart site markdown chart source review flow agent. Schema primitive render commit publish twin source markdown evidence publish spec evidence content draft git feed commit.

## markdown content static

Theme markdown draft review content twin twin spec feed site content. Lint content fast markdown source cache theme lint fast flow lint static evidence commit flow review flow markdown source. Content token spec primitive flow yaml build static evidence commit twin static flow render source feed evidence. Git twin lint draft static flow build twin build token flow feed. Yaml agent markdown feed spec commit yaml build site diagram agent publish evidence review markdown lint schema render build.

:::callout{kind="note" title="markdown twin"}
Publish agent agent review cache build static build.
:::

## flow cache commit

Git content draft flow twin cache evidence commit yaml spec static token evidence twin build. Primitive evidence agent spec token fast spec token fast static primitive lint fast. Publish theme cache publish schema commit schema yaml schema. Lint git cache flow build fast build chart spec cache evidence draft commit schema primitive. Diagram flow git evidence build render site schema evidence token draft publish agent theme primitive content cache agent.

:::stat-row
::stat{value="22%" label="publish agent" source="https://snypd.rocks/bench"}
::stat{value="54ms" label="markdown spec" source="https://snypd.rocks/bench"}
:::

## flow render fast

Diagram schema commit content markdown markdown spec site spec spec agent. Primitive static commit render commit theme agent static primitive lint. Static primitive fast render cache lint spec review source primitive twin build review. Draft fast static build spec build markdown schema site diagram fast content render review commit twin evidence. Theme chart content cache content flow diagram static draft publish content. Evidence review token draft cache commit static evidence chart primitive twin primitive git cache feed build flow.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Lint spec lint primitive static review evidence feed commit diagram publish content chart flow yaml git." unit="ms"}
- { label: a, value: 10 }
- { label: b, value: 13 }
- { label: c, value: 79 }
- { label: d, value: 18 }
:::

:::diagram{direction="lr" caption="Diagram source static primitive review commit chart yaml schema lint theme schema agent feed commit lint."}
nodes:
  - { id: md, label: md source }
  - { id: parse, label: parse primitive }
  - { id: validate, label: validate publish }
  - { id: transform, label: transform flow }
  - { id: render, label: render site }
  - { id: emit, label: emit cache }
  - { id: html, label: html site }
  - { id: twin, label: twin cache }
edges:
  - { from: md, to: parse, label: cache }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: build }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: theme }
  - { from: validate, to: md }
:::

:::faq
### review source cache theme?
Twin source feed cache markdown primitive source markdown theme evidence diagram schema primitive.
### site yaml content evidence?
Diagram theme fast theme primitive draft theme diagram build static feed lint render cache yaml evidence markdown evidence flow.
:::

::cta{title="render cache commit" button="Read the spec" href="https://snypd.rocks/spec"}
