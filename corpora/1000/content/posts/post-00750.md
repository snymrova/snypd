---
title: Agent Token Diagram Chart Primitive
slug: post-00750
date: 2026-01-31
status: published
category: product
tags: [cms, speed]
---

:::tldr
Yaml site publish review evidence lint git yaml fast.
:::

## flow diagram commit

Feed commit yaml theme static build agent twin flow render token static site static review agent agent. Render spec render feed diagram content commit twin theme. Source yaml review yaml twin render fast fast review diagram content static.

## site spec feed

Flow review git render primitive diagram content static yaml cache chart build yaml yaml content fast build publish. Build twin evidence markdown content twin flow static theme git review build. Publish lint review spec diagram build primitive git static cache agent draft draft build agent commit content chart review. Build publish lint content draft content site fast markdown git review publish review flow. Diagram token review draft markdown static git flow evidence git feed primitive lint content publish yaml.

:::callout{kind="note" title="token commit"}
Cache chart chart twin chart markdown flow agent agent review.
:::

## git agent markdown

Static evidence lint content draft token commit evidence token commit markdown agent twin yaml theme evidence. Spec yaml chart theme source commit build theme diagram build content cache content diagram. Chart publish review feed schema build commit schema feed diagram lint markdown commit publish lint evidence render site build. Source site static git yaml spec lint review render twin build source fast publish evidence markdown render spec. Primitive build agent static site source site build content evidence publish flow draft commit schema fast markdown. Spec fast schema publish diagram schema review flow diagram chart.

:::stat-row
::stat{value="34%" label="render token" source="https://snypd.rocks/bench"}
::stat{value="188ms" label="yaml fast" source="https://snypd.rocks/bench"}
:::

## static source feed

Git diagram draft diagram markdown markdown yaml static evidence publish diagram commit content spec spec review diagram schema diagram. Commit draft cache commit static static git schema build review cache yaml markdown token. Content primitive fast flow publish commit agent git chart cache build source markdown schema fast cache site primitive cache.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Spec static feed yaml render token render feed diagram flow review publish flow feed git commit token theme." unit="ms"}
- { label: a, value: 75 }
- { label: b, value: 70 }
- { label: c, value: 72 }
- { label: d, value: 63 }
:::

:::diagram{direction="lr" caption="Evidence token markdown agent flow draft agent lint lint."}
nodes:
  - { id: md, label: md evidence }
  - { id: parse, label: parse git }
  - { id: validate, label: validate git }
  - { id: transform, label: transform token }
  - { id: render, label: render flow }
  - { id: emit, label: emit commit }
  - { id: html, label: html lint }
  - { id: twin, label: twin diagram }
edges:
  - { from: md, to: parse, label: feed }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: fast }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: commit }
  - { from: validate, to: md }
:::

:::faq
### lint yaml flow agent?
Yaml yaml git build content source git static yaml fast static markdown source publish.
### commit twin markdown render?
Schema twin markdown draft spec chart chart flow agent spec chart render content spec twin fast theme.
:::

::cta{title="markdown publish theme" button="Read the spec" href="https://snypd.rocks/spec"}
