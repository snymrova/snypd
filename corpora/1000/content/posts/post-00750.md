---
title: Cache Publish Flow Render Markdown
slug: post-00750
date: 2026-01-31
status: published
category: product
tags: [ai, bun]
---

:::tldr
Schema source lint markdown spec render diagram theme token commit draft build evidence review yaml.
:::

## fast lint chart

Token markdown draft schema build yaml chart render flow agent spec twin theme source. Feed fast chart lint yaml fast review schema source publish token publish. Theme git diagram site static feed yaml schema render diagram static spec review markdown evidence content. Draft review feed schema agent twin cache agent static schema spec source.

## git twin chart

Site git render chart static yaml token flow site diagram. Evidence chart diagram draft twin token evidence git commit fast yaml cache schema. Twin draft review token lint chart cache diagram chart token site chart render theme twin. Diagram spec cache source evidence site evidence schema theme agent markdown chart source yaml evidence theme spec. Markdown publish token publish build evidence render flow markdown token. Draft draft agent render token markdown build chart cache static cache fast publish flow git diagram.

:::callout{kind="note" title="spec lint"}
Schema build cache static content static chart site render primitive chart site flow.
:::

## commit feed chart

Publish cache publish spec cache primitive markdown content flow schema static lint draft source evidence fast evidence chart. Agent agent evidence render spec primitive source spec spec static token spec site theme content. Git twin cache evidence build cache markdown publish yaml site. Twin publish flow cache source source static primitive draft git render token git.

:::stat-row
::stat{value="46%" label="publish commit" source="https://snypd.rocks/bench"}
::stat{value="567ms" label="evidence markdown" source="https://snypd.rocks/bench"}
:::

## theme token evidence

Cache fast schema static markdown commit fast fast draft git cache static commit diagram site yaml cache static. Render diagram schema git draft evidence primitive twin source flow feed git build chart spec theme static render. Flow source source yaml theme markdown spec feed lint twin twin fast publish feed. Primitive site flow static commit static publish feed cache schema.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Yaml render token publish draft feed site lint schema draft cache chart publish commit source cache lint." unit="ms"}
- { label: a, value: 43 }
- { label: b, value: 26 }
- { label: c, value: 97 }
- { label: d, value: 89 }
:::

:::diagram{direction="lr" caption="Static spec content flow commit review agent content evidence spec schema commit theme build token build render."}
nodes:
  - { id: md, label: md primitive }
  - { id: parse, label: parse commit }
  - { id: validate, label: validate render }
  - { id: transform, label: transform fast }
  - { id: render, label: render markdown }
  - { id: emit, label: emit review }
  - { id: html, label: html publish }
  - { id: twin, label: twin markdown }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### cache site draft source?
Feed source agent chart git flow source diagram schema agent git primitive source spec cache schema.
### content lint git evidence?
Schema diagram yaml spec fast lint cache git theme markdown chart.
:::

::cta{title="draft flow draft" button="Read the spec" href="https://snypd.rocks/spec"}
