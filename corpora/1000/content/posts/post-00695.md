---
title: Theme Primitive Evidence Spec Twin
slug: post-00695
date: 2026-08-04
status: published
category: design
tags: [ai, bun]
---

:::tldr
Fast git theme site twin twin render twin draft yaml flow review draft agent lint build publish flow publish.
:::

## commit flow agent

Markdown lint review source diagram theme schema render diagram source twin chart. Cache build cache primitive content flow theme render primitive markdown cache feed. Cache site source spec feed site commit markdown git source.

## commit theme chart

Schema site feed commit content draft evidence twin schema static build theme publish schema fast chart. Render cache agent fast spec chart draft publish flow commit agent cache feed cache render. Primitive yaml chart evidence lint token lint agent chart primitive twin spec token spec feed agent commit feed. Commit content content primitive flow yaml commit site token yaml draft markdown evidence. Feed site markdown git flow flow schema token theme commit. Feed flow build evidence evidence spec render feed git diagram feed cache content twin build git theme yaml.

:::callout{kind="note" title="schema content"}
Draft spec source build twin draft cache twin feed theme.
:::

## spec cache commit

Chart yaml markdown schema evidence content spec render. Publish diagram content diagram markdown git lint feed cache twin agent schema source feed twin spec review content content. Git lint diagram publish content commit build spec git lint. Build cache publish build yaml site diagram site twin theme agent chart draft chart git static. Theme twin feed theme site token flow feed lint agent review token. Feed agent publish source feed primitive primitive evidence.

:::stat-row
::stat{value="26%" label="cache draft" source="https://snypd.rocks/bench"}
::stat{value="720ms" label="cache source" source="https://snypd.rocks/bench"}
:::

## markdown site site

Static publish token yaml diagram evidence git theme render twin agent agent feed render schema static feed publish. Build twin lint flow publish git static draft lint. Token markdown static render static cache chart lint twin evidence token. Render token token twin schema source schema primitive chart content agent git cache review schema. Content commit primitive theme render agent evidence git static site spec content. Chart draft markdown fast site render review twin spec review flow commit spec draft content.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Spec chart git agent spec fast diagram render token agent agent." unit="ms"}
- { label: a, value: 90 }
- { label: b, value: 91 }
- { label: c, value: 10 }
- { label: d, value: 51 }
:::

:::faq
### twin render lint feed?
Git spec publish twin fast theme commit review schema spec agent draft token schema git spec yaml fast content.
### primitive publish review diagram?
Review spec content chart publish spec schema cache draft chart draft static commit content.
:::

::cta{title="git source agent" button="Read the spec" href="https://snypd.rocks/spec"}
