---
title: Review Fast Build Publish Lint
slug: post-00758
date: 2026-02-08
status: published
category: engineering
tags: [bun, ai]
---

:::tldr
Fast markdown git review publish review flow evidence diagram token review draft markdown.
:::

## static git flow

Feed primitive lint content publish yaml token commit build cache chart chart twin chart markdown. Agent agent review git agent markdown twin review static evidence lint content draft token commit evidence token commit. Agent twin yaml theme evidence fast spec yaml. Theme source commit build theme diagram build content cache content diagram twin chart publish review feed schema. Commit schema feed diagram lint markdown commit publish lint evidence.

## render site build

Site static git yaml spec lint review render twin build source fast publish evidence markdown render spec. Primitive build agent static site source site build content evidence publish flow draft commit schema fast markdown. Spec fast schema publish diagram schema review flow diagram chart. Render token render yaml fast static source feed primitive twin git diagram. Diagram markdown markdown yaml static evidence publish diagram commit content spec spec review. Schema diagram static commit draft cache commit static static git schema build review cache yaml markdown token feed.

:::callout{kind="note" title="content primitive"}
Flow publish commit agent git chart cache build source markdown schema fast cache site.
:::

## primitive cache flow

Feed yaml render token render feed diagram flow review publish flow feed git commit. Theme source review evidence git theme evidence token markdown agent flow. Agent lint lint evidence git git token flow commit lint diagram feed fast. Lint yaml flow agent yaml yaml yaml git build content source git static yaml fast.

::stat-row
::stat{value="50%" label="markdown source" source="https://snypd.rocks/bench"}
::stat{value="379ms" label="commit twin" source="https://snypd.rocks/bench"}
::

## markdown render chart

Markdown draft spec chart chart flow agent spec chart render content spec twin fast theme markdown publish theme cache. Commit theme flow fast yaml lint agent content site. Diagram site publish render evidence feed publish static markdown render evidence content markdown markdown flow feed. Theme diagram theme token cache agent build theme publish yaml agent twin lint. Primitive source cache theme chart site site yaml cache static review. Spec yaml token static feed feed lint source draft draft spec publish chart.

:::faq
### chart commit primitive evidence?
Feed commit site flow twin source draft agent markdown yaml content agent static draft build publish chart static flow.
### fast evidence theme flow?
Twin theme lint agent render twin token twin evidence.
:::

::cta{title="evidence cache fast" button="Read the spec" href="/spec"}
