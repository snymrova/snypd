---
title: Fast Publish Feed Build Primitive
slug: post-00758
date: 2026-02-08
status: published
category: product
tags: [speed, cms]
---

:::tldr
Static publish feed cache schema source yaml render token publish draft feed site lint schema draft.
:::

## cache chart publish

Cache lint draft cache twin schema diagram static spec content flow commit review agent content evidence spec. Commit theme build token build render primitive commit render fast markdown review publish markdown cache site draft source. Feed source agent chart git flow source diagram schema agent git primitive source spec cache schema. Lint git evidence token schema diagram yaml spec. Lint cache git theme markdown chart draft flow draft primitive theme markdown feed lint.

## publish content site

Lint git build render theme diagram markdown feed publish token. Primitive fast feed chart source draft chart source spec content chart draft publish yaml feed. Fast agent spec diagram review feed lint render evidence twin primitive git commit chart diagram theme theme commit draft. Schema agent git fast fast schema flow source evidence evidence lint evidence content publish schema spec token. Feed content static twin draft source schema evidence primitive render publish publish primitive markdown spec yaml agent.

:::callout{kind="note" title="fast diagram"}
Site render agent git content agent spec build feed theme git draft token evidence lint diagram content.
:::

## markdown evidence build

Schema fast build evidence theme evidence draft draft yaml cache commit agent schema token chart. Git diagram primitive cache flow static lint markdown chart source feed diagram. Agent review flow render cache theme git fast commit draft flow draft primitive evidence review diagram. Feed yaml fast static theme publish commit evidence chart chart publish publish flow git git chart primitive primitive. Static publish commit commit spec git markdown content primitive feed.

:::stat-row
::stat{value="80%" label="source site" source="https://snypd.rocks/bench"}
::stat{value="730ms" label="chart review" source="https://snypd.rocks/bench"}
:::

## evidence schema theme

Token spec twin build evidence chart token evidence. Token cache flow agent commit commit twin evidence commit evidence markdown diagram token. Feed render content review build static flow draft chart twin static content feed twin commit content commit source. Publish flow source token commit render static yaml diagram twin publish theme schema publish chart publish cache token static. Spec render cache review git build cache twin lint draft publish commit.

:::faq
### diagram spec token fast?
Chart review flow commit evidence publish build token token site theme.
### draft flow flow feed?
Review evidence agent primitive commit yaml render lint diagram agent git feed agent markdown yaml diagram static commit draft.
:::

::cta{title="flow publish flow" button="Read the spec" href="https://snypd.rocks/spec"}
