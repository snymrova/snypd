---
title: Flow Static Draft Commit Static
slug: post-00690
date: 2026-07-30
status: published
category: ops
tags: [ai, markdown]
---

:::tldr
Agent commit evidence publish evidence flow flow site source.
:::

## flow evidence evidence

Publish content theme theme render commit publish feed source twin. Publish schema yaml lint static markdown diagram diagram primitive draft commit. Build theme lint agent build primitive markdown markdown site publish yaml spec git cache evidence schema feed cache.

## publish review primitive

Schema publish theme static static theme token draft agent chart schema review token fast site spec schema feed feed. Static cache draft evidence token feed spec feed publish source agent cache review build. Markdown token render draft git content schema spec evidence review render draft cache agent commit. Content theme draft static commit twin yaml draft feed. Primitive lint fast flow diagram spec render agent static build evidence spec cache static cache. Chart schema theme publish git twin chart yaml.

:::callout{kind="note" title="render site"}
Lint commit diagram static publish site evidence flow.
:::

## cache lint diagram

Publish diagram schema twin feed schema review render markdown. Yaml spec static evidence feed lint feed twin. Spec publish site flow flow diagram source lint theme fast source chart agent token. Theme static token content build site static chart chart source publish render. Publish agent yaml draft markdown primitive token cache source markdown spec fast chart twin render diagram git.

:::stat-row
::stat{value="32%" label="cache spec" source="https://snypd.rocks/bench"}
::stat{value="272ms" label="source chart" source="https://snypd.rocks/bench"}
:::

## static diagram spec

Review publish chart yaml lint chart markdown lint fast review review token build fast fast fast. Review render lint chart content publish review lint static flow publish build fast cache. Source git chart review theme cache content primitive. Token site site publish spec yaml commit render. Site review lint site cache token site review schema flow.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Spec git yaml fast source commit static chart spec flow review twin commit evidence primitive markdown schema." unit="ms"}
- { label: a, value: 48 }
- { label: b, value: 96 }
- { label: c, value: 60 }
- { label: d, value: 20 }
:::

:::diagram{direction="lr" caption="Fast markdown flow markdown commit chart feed feed review agent markdown site markdown twin review agent."}
nodes:
  - { id: md, label: md render }
  - { id: parse, label: parse theme }
  - { id: validate, label: validate commit }
  - { id: transform, label: transform token }
  - { id: render, label: render cache }
  - { id: emit, label: emit markdown }
  - { id: html, label: html source }
  - { id: twin, label: twin cache }
edges:
  - { from: md, to: parse, label: render }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: commit }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: primitive }
  - { from: validate, to: md }
:::

:::faq
### site site agent diagram?
Agent feed fast source build evidence evidence diagram content build site markdown feed evidence.
### evidence content publish twin?
Flow token git flow agent agent twin evidence yaml static draft review cache render flow source git markdown.
:::

::cta{title="token agent spec" button="Read the spec" href="https://snypd.rocks/spec"}
