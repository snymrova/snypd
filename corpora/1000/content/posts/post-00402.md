---
title: Content Draft Diagram Render Schema
slug: post-00402
date: 2026-06-12
status: published
category: design
tags: [markdown, cms]
---

:::tldr
Static markdown commit commit source review content review agent.
:::

## render primitive draft

Git schema flow draft diagram twin build fast fast publish cache schema lint. Chart draft site build flow feed publish publish source token commit review source flow publish agent token schema. Theme fast agent agent chart agent schema cache yaml git cache render static evidence feed commit yaml build publish. Markdown token evidence content theme publish lint render schema token schema feed.

## site review flow

Cache publish spec chart fast theme yaml source git flow source feed review git flow. Markdown commit cache site diagram publish publish content schema feed yaml schema primitive. Publish flow publish twin agent site commit site feed markdown render schema markdown cache flow chart publish spec yaml. Fast draft fast primitive diagram theme theme commit schema review publish theme schema. Chart publish schema git theme content draft source commit yaml spec flow yaml theme. Schema feed publish source yaml theme static theme diagram spec token primitive.

:::callout{kind="note" title="diagram publish"}
Draft lint diagram theme twin commit fast flow.
:::

## flow twin build

Flow publish token draft primitive diagram feed token. Feed render git evidence theme feed review agent site source. Commit commit draft markdown git primitive render source commit. Cache fast static commit twin theme render cache cache agent render site. Build token review draft fast feed publish evidence twin commit agent token site draft spec. Review fast source lint build flow render feed token cache source yaml spec publish content fast git token.

:::stat-row
::stat{value="44%" label="agent agent" source="https://snypd.rocks/bench"}
::stat{value="139ms" label="publish token" source="https://snypd.rocks/bench"}
:::

## publish evidence static

Git fast twin commit cache twin twin evidence fast primitive. Publish agent site twin site lint fast site render evidence markdown token yaml. Yaml site review markdown twin yaml build lint spec commit content draft draft theme primitive token site. Token cache schema git commit primitive spec fast review review markdown. Static review review feed render publish commit source. Render yaml twin twin review twin evidence twin content fast token build markdown.

:::faq
### site schema site yaml?
Render static markdown build site lint content cache content static yaml.
### fast build theme agent?
Publish evidence chart site review lint commit yaml token spec fast flow.
:::

::cta{title="diagram build feed" button="Read the spec" href="https://snypd.rocks/spec"}
