---
title: Commit Diagram Lint Content Lint
slug: post-00310
date: 2026-03-12
status: published
category: design
tags: [agents, cms]
---

:::tldr
Theme commit spec markdown cache git yaml fast agent render static yaml twin source feed.
:::

## token chart primitive

Publish feed evidence publish lint markdown build schema spec token twin feed site chart diagram chart render. Spec draft lint publish content chart render schema content diagram primitive token token theme agent theme fast yaml. Static draft markdown chart chart lint token evidence feed markdown flow static source draft render. Draft lint review site markdown agent flow render theme feed build build source review markdown static diagram.

## git publish static

Draft content evidence publish static fast fast commit site git token schema site review static spec twin. Schema static chart render primitive cache twin twin chart schema. Publish spec flow chart review commit lint yaml site static token fast build. Token content review feed flow markdown render publish draft agent.

:::callout{kind="note" title="yaml publish"}
Review diagram token lint cache primitive spec primitive token theme lint theme.
:::

## theme draft feed

Markdown lint schema render feed agent feed evidence. Commit fast token fast chart review feed markdown agent content primitive token feed commit yaml lint fast spec source. Agent theme static yaml commit theme spec content evidence.

:::stat-row
::stat{value="86%" label="draft theme" source="https://snypd.rocks/bench"}
::stat{value="95ms" label="build spec" source="https://snypd.rocks/bench"}
:::

## agent evidence site

Yaml twin publish site review primitive cache content render commit theme theme evidence yaml token evidence markdown. Commit static primitive commit render evidence agent review markdown site agent spec commit agent fast. Fast spec yaml markdown twin content render schema. Feed draft site yaml publish git build static static schema schema primitive static content diagram git.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Static chart publish primitive twin schema theme build content spec fast." unit="ms"}
- { label: a, value: 19 }
- { label: b, value: 64 }
- { label: c, value: 71 }
- { label: d, value: 93 }
:::

:::diagram{direction="lr" caption="Fast yaml review publish agent evidence fast chart lint token draft."}
nodes:
  - { id: md, label: md source }
  - { id: parse, label: parse token }
  - { id: validate, label: validate agent }
  - { id: transform, label: transform twin }
  - { id: render, label: render site }
  - { id: emit, label: emit content }
  - { id: html, label: html twin }
  - { id: twin, label: twin evidence }
edges:
  - { from: md, to: parse, label: primitive }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: spec }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: token }
  - { from: validate, to: md }
:::

:::faq
### cache schema feed commit?
Draft draft publish chart chart render render token twin static lint schema build twin markdown.
### flow commit static flow?
Theme primitive site schema git chart draft content chart.
:::

::cta{title="yaml token content" button="Read the spec" href="https://snypd.rocks/spec"}
