---
title: Theme Git Markdown Cache Git
slug: post-00310
date: 2026-03-12
status: published
category: design
tags: [cms, bun]
---

:::tldr
Draft primitive fast render review schema yaml build lint token spec feed feed.
:::

## yaml review git

Review theme cache markdown commit draft twin flow lint primitive diagram feed token yaml git lint. Flow twin site publish feed spec commit source primitive git site site twin spec lint. Publish token fast static token site fast schema fast static theme chart content markdown twin.

## commit schema commit

Flow cache theme review chart source content draft token source build site publish diagram flow lint theme chart cache. Static feed agent twin evidence source site flow theme git agent cache source twin site. Theme build fast theme twin chart fast content yaml render build schema chart evidence chart review render yaml feed. Diagram evidence flow static fast commit draft cache render publish chart feed. Evidence render primitive diagram agent publish review render feed schema twin token primitive theme build schema git site.

:::callout{kind="note" title="content diagram"}
Git source spec render git cache build build primitive commit static.
:::

## lint cache primitive

Token schema agent yaml markdown spec diagram feed theme render cache yaml source chart site diagram evidence. Agent build token source evidence yaml agent feed feed review chart token. Static theme draft primitive lint markdown token git. Diagram content cache review evidence content flow static lint review content static draft render twin lint token.

:::stat-row
::stat{value="58%" label="diagram twin" source="https://snypd.rocks/bench"}
::stat{value="792ms" label="publish static" source="https://snypd.rocks/bench"}
:::

## git theme theme

Twin render agent evidence markdown token schema token build evidence source site feed git flow spec publish token render. Twin yaml cache content evidence build git yaml site git. Evidence commit draft build source markdown review static content cache. Publish source spec build token build render fast primitive feed draft feed spec spec. Publish site chart chart twin draft lint token review render content build lint theme source cache twin.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Twin theme yaml spec draft content feed commit git lint token." unit="ms"}
- { label: a, value: 13 }
- { label: b, value: 14 }
- { label: c, value: 3 }
- { label: d, value: 7 }
:::

:::diagram{direction="lr" caption="Agent static review spec theme yaml static cache markdown render site yaml markdown primitive markdown."}
nodes:
  - { id: md, label: md git }
  - { id: parse, label: parse content }
  - { id: validate, label: validate evidence }
  - { id: transform, label: transform agent }
  - { id: render, label: render source }
  - { id: emit, label: emit draft }
  - { id: html, label: html token }
  - { id: twin, label: twin git }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### schema theme lint cache?
Token git commit site cache render markdown source theme yaml site review draft lint publish twin commit lint.
### build feed yaml evidence?
Markdown chart agent twin commit theme cache content flow diagram build spec yaml agent lint fast.
:::

::cta{title="token chart yaml" button="Read the spec" href="https://snypd.rocks/spec"}
