---
title: Twin Diagram Source Build Agent
slug: post-00187
date: 2026-07-07
status: published
category: product
tags: [markdown, speed]
---

:::tldr
Token yaml build yaml git site theme build publish content site token site cache chart schema chart.
:::

## twin flow commit

Flow flow spec static theme diagram token feed render site render fast agent chart draft feed. Site fast primitive render evidence schema agent review theme twin token build commit commit publish lint theme feed. Static evidence diagram review build site site markdown lint cache twin spec agent primitive theme diagram chart twin. Feed commit flow git evidence feed draft token yaml review flow content static twin render agent static. Schema token theme agent yaml render markdown twin site site primitive. Review content twin theme build site schema git yaml twin fast evidence spec feed markdown fast review static.

## chart evidence content

Schema markdown git render yaml yaml yaml static token review evidence twin lint render chart static flow primitive build. Review static markdown render yaml build schema twin primitive agent twin token feed cache chart diagram agent. Markdown markdown static static static flow cache schema publish static lint flow spec review build agent evidence draft publish. Agent token render source site twin render static chart diagram draft diagram draft source.

:::callout{kind="note" title="flow yaml"}
Fast feed twin static git twin source commit lint token evidence spec markdown site chart markdown flow schema.
:::

## publish markdown theme

Git content static commit render spec evidence source commit render twin theme. Site cache schema chart content git git primitive render primitive commit build draft yaml render feed agent static source. Git agent site theme build twin source draft primitive markdown schema theme schema draft static. Cache yaml primitive spec git lint diagram publish commit agent.

:::stat-row
::stat{value="76%" label="static token" source="https://snypd.rocks/bench"}
::stat{value="470ms" label="commit agent" source="https://snypd.rocks/bench"}
:::

## chart spec evidence

Diagram agent cache chart static cache agent static markdown cache site lint flow diagram. Commit agent review review schema cache lint render evidence source. Twin review draft evidence draft commit draft primitive agent build fast yaml spec. Chart cache build theme render yaml render render.

:::faq
### schema diagram content content?
Render markdown agent agent build draft flow content token spec cache static source twin publish publish feed fast review.
### lint spec flow site?
Flow fast static cache render commit chart theme.
:::

::cta{title="yaml fast primitive" button="Read the spec" href="https://snypd.rocks/spec"}
