---
title: Token Markdown Agent Flow Review
slug: post-00159
date: 2026-06-09
status: published
category: design
tags: [ai, speed]
---

:::tldr
Chart git evidence schema yaml theme schema build theme render site.
:::

## spec chart chart

Schema primitive source content render twin draft render twin markdown agent evidence render twin publish site. Git twin site schema spec cache twin git spec evidence token draft. Evidence render static git diagram site markdown markdown diagram content token render chart. Content chart schema twin yaml cache twin feed git schema static flow site review render build review theme feed.

## flow site diagram

Yaml review token publish agent evidence diagram agent. Feed cache site draft primitive git primitive spec lint source. Spec flow source cache token draft markdown fast twin static spec render cache spec token yaml site feed. Twin spec render twin agent diagram build render token agent git build draft git theme source chart. Primitive spec fast source yaml build evidence chart site draft lint token render commit cache markdown site draft.

:::callout{kind="note" title="fast cache"}
Feed publish fast lint git commit markdown primitive source git evidence.
:::

## twin spec agent

Site flow render review draft primitive diagram content fast. Site feed agent yaml spec theme site yaml git lint content source git publish diagram. Fast review source agent theme diagram twin flow render build primitive token feed primitive agent agent.

:::stat-row
::stat{value="66%" label="twin review" source="https://snypd.rocks/bench"}
::stat{value="594ms" label="cache draft" source="https://snypd.rocks/bench"}
:::

## content evidence build

Feed twin schema git yaml lint publish draft build flow cache static render agent agent schema. Chart yaml diagram primitive review diagram token chart. Review feed diagram chart render spec token commit build twin schema. Markdown twin yaml schema markdown theme content twin content review build spec primitive site.

:::faq
### spec render site review?
Token spec spec review static agent git flow commit yaml.
### yaml markdown primitive chart?
Diagram markdown cache source review review evidence cache evidence.
:::

::cta{title="theme diagram agent" button="Read the spec" href="https://snypd.rocks/spec"}
