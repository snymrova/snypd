---
title: Static Commit Fast Theme Schema
slug: post-00696
date: 2026-08-05
status: published
category: ops
tags: [agents, speed]
---

:::tldr
Source yaml twin site git review agent token content git evidence publish lint diagram fast.
:::

## yaml evidence source

Agent evidence chart primitive fast primitive chart static git. Primitive feed render source agent diagram static agent token. Fast primitive cache commit publish schema fast primitive source draft fast schema static. Static diagram draft diagram agent lint static review agent render. Source diagram cache cache commit markdown content git site token flow.

## diagram review evidence

Token commit static publish lint site primitive commit content spec build. Feed flow source feed content yaml site spec render cache static fast lint. Flow cache flow schema static review commit theme feed diagram twin. Static primitive review fast git token markdown site agent source feed static publish spec flow build source.

:::callout{kind="note" title="render chart"}
Review evidence chart commit yaml static source token.
:::

## render lint agent

Agent draft build git site diagram render commit markdown yaml build spec review. Agent feed spec feed draft cache fast source site flow cache feed publish build git. Draft agent content build markdown agent twin markdown publish chart review diagram agent git yaml. Build site yaml commit schema git commit chart yaml review. Theme schema spec publish feed content token flow publish diagram feed commit render commit render chart git draft diagram.

:::stat-row
::stat{value="72%" label="content render" source="https://snypd.rocks/bench"}
::stat{value="648ms" label="primitive content" source="https://snypd.rocks/bench"}
:::

## git lint fast

Markdown lint primitive schema flow review theme spec fast twin source theme build lint commit. Flow token lint static content schema yaml spec twin. Build static token yaml theme site schema source content content chart build.

:::faq
### git cache commit review?
Draft source schema primitive review twin cache diagram markdown content theme cache lint static commit.
### primitive publish twin cache?
Git flow token build schema yaml fast token git source render flow markdown spec yaml static git draft.
:::

::cta{title="yaml spec review" button="Read the spec" href="https://snypd.rocks/spec"}
