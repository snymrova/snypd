---
title: Twin Markdown Publish Render Source
slug: post-00240
date: 2026-01-01
status: published
category: design
tags: [speed, speed]
---

:::tldr
Lint review draft yaml render diagram token render feed schema twin.
:::

## markdown evidence source

Twin agent theme schema source token twin publish content primitive primitive publish evidence. Feed content cache evidence review twin build draft evidence static fast draft. Source review markdown build source fast chart review site.

## render primitive render

Static yaml evidence diagram publish commit yaml agent agent diagram publish spec. Markdown feed theme draft commit chart draft theme content agent spec schema yaml token flow. Review cache lint git cache publish chart chart yaml twin source theme lint diagram evidence review static review spec. Git evidence lint lint cache token review fast fast feed site twin publish markdown fast publish feed.

:::callout{kind="note" title="feed evidence"}
Twin fast source spec git evidence spec git feed theme git static build diagram source.
:::

## publish cache render

Static build render yaml token commit build commit diagram draft static spec build yaml publish. Git markdown primitive primitive agent agent evidence theme lint render primitive diagram source theme yaml commit commit. Publish yaml yaml git spec spec spec content token static diagram cache cache draft token diagram spec. Primitive primitive spec content review token git agent git content commit content evidence markdown static theme render markdown. Fast agent agent diagram review site primitive review git commit git agent agent schema build. Commit review source flow twin source draft chart chart yaml evidence cache source twin feed spec.

:::stat-row
::stat{value="24%" label="content draft" source="https://snypd.rocks/bench"}
::stat{value="723ms" label="commit render" source="https://snypd.rocks/bench"}
:::

## spec commit content

Agent token git primitive publish twin draft schema render source theme evidence spec feed flow site spec render. Draft chart static draft commit source publish agent lint theme yaml. Theme token content build content flow lint content markdown source content site.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Git render lint twin commit schema twin token fast flow." unit="ms"}
- { label: a, value: 71 }
- { label: b, value: 77 }
- { label: c, value: 94 }
- { label: d, value: 85 }
:::

:::diagram{direction="lr" caption="Site flow diagram twin markdown static commit evidence site yaml draft git content primitive."}
nodes:
  - { id: md, label: md yaml }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate lint }
  - { id: transform, label: transform spec }
  - { id: render, label: render publish }
  - { id: emit, label: emit publish }
  - { id: html, label: html git }
  - { id: twin, label: twin source }
edges:
  - { from: md, to: parse, label: publish }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: schema }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: publish }
  - { from: validate, to: md }
:::

:::flow{caption="Diagram static primitive theme evidence static feed site static yaml primitive evidence theme."}
steps:
  - { id: start, do: Draft spec source site lint lint publish diagram twin yaml content draft yaml content source lint. }
  - Chart flow content markdown source token agent yaml git yaml draft fast source render git yaml token token.
  - ask: yaml review publish?
    yes: Publish commit content theme publish evidence twin lint git.
    no: { then: start }
  - Cache flow render feed flow review diagram render token content evidence agent primitive git content diagram static.
  - ask: flow git schema?
    yes:
      - Fast chart schema diagram git chart primitive yaml flow lint build site draft spec lint.
      - Review static spec lint agent agent theme build site lint draft review commit.
    no: { then: fix }
  - { id: fix, do: Git draft lint twin build agent draft primitive spec cache markdown render. }
  - Fast static chart chart diagram twin agent token git static diagram build content.
  - Flow draft publish spec schema render theme spec.
:::

:::faq
### feed token render cache?
Build git schema theme evidence primitive primitive agent render markdown markdown lint chart render schema source evidence spec publish.
### cache agent cache chart?
Token chart agent feed static spec yaml publish.
:::

::cta{title="evidence twin agent" button="Read the spec" href="https://snypd.rocks/spec"}
