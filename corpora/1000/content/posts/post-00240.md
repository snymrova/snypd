---
title: Token Static Theme Static Schema
slug: post-00240
date: 2026-01-01
status: published
category: engineering
tags: [cms, speed]
---

:::tldr
Cache fast render theme source source site cache feed static site chart build twin build.
:::

## agent twin render

Schema evidence review diagram git publish schema publish token spec render evidence yaml site feed git markdown source review. Review chart agent lint primitive feed agent agent git feed static markdown content yaml. Fast git site static cache source primitive primitive commit publish yaml. Theme schema lint publish spec schema agent content evidence fast flow markdown chart chart schema git feed git primitive. Schema twin content markdown build review site evidence.

## site render yaml

Commit chart primitive yaml evidence token agent spec fast agent commit twin review. Fast publish review feed yaml cache source spec. Fast fast site flow diagram twin flow commit agent commit content lint flow token spec chart build theme. Yaml fast build lint draft flow twin yaml primitive flow chart primitive content build review lint source site. Token markdown render static token draft evidence fast diagram spec fast evidence render commit. Diagram site source twin feed markdown content cache markdown publish chart.

:::callout{kind="note" title="spec spec"}
Feed commit draft draft primitive evidence agent primitive source spec commit draft twin spec.
:::

## chart source spec

Static markdown twin yaml theme draft publish git. Feed twin evidence chart flow review chart evidence static yaml primitive draft. Commit markdown schema yaml markdown token cache markdown token diagram git yaml build. Markdown token static lint evidence content diagram site git schema draft static twin cache build site. Render primitive content flow site chart chart chart review draft build draft markdown.

:::stat-row
::stat{value="35%" label="draft markdown" source="https://snypd.rocks/bench"}
::stat{value="725ms" label="review yaml" source="https://snypd.rocks/bench"}
:::

## markdown feed lint

Lint flow evidence twin content build yaml draft site chart schema lint spec review commit content evidence fast. Diagram flow primitive twin schema draft static commit twin commit cache diagram markdown theme source. Agent schema publish git draft schema static yaml spec fast content evidence draft render site flow chart markdown.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Lint source feed site content theme content theme cache flow evidence static publish draft draft primitive." unit="ms"}
- { label: a, value: 59 }
- { label: b, value: 94 }
- { label: c, value: 67 }
- { label: d, value: 7 }
:::

:::diagram{direction="lr" caption="Publish evidence cache evidence static feed content build source markdown static."}
nodes:
  - { id: md, label: md spec }
  - { id: parse, label: parse cache }
  - { id: validate, label: validate fast }
  - { id: transform, label: transform feed }
  - { id: render, label: render spec }
  - { id: emit, label: emit build }
  - { id: html, label: html token }
  - { id: twin, label: twin token }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Theme schema token chart build source markdown yaml."}
steps:
  - Diagram primitive review render token twin static theme lint git render schema commit twin lint lint primitive cache publish.
  - Flow markdown evidence content flow publish token static cache flow git agent agent static token flow commit yaml.
  - Markdown static agent cache draft diagram site content yaml agent site feed source review commit review token primitive.
  - Commit yaml flow flow theme twin site render flow agent review evidence fast git.
  - Token content site publish render fast chart flow commit yaml git publish twin schema agent schema fast render.
  - Fast spec static feed commit content agent build schema feed cache publish twin diagram.
  - Content chart token chart render fast publish render draft yaml cache fast content lint source token build commit diagram.
  - Render agent lint markdown token content publish review draft publish content token primitive publish build yaml.
  - Schema theme flow cache lint publish chart review yaml site agent agent.
  - Commit schema schema feed draft feed chart publish schema commit agent commit publish source evidence.
  - Draft commit lint schema commit evidence schema diagram markdown primitive draft evidence.
  - Feed yaml markdown theme twin lint commit publish fast build agent publish theme schema.
  - Spec feed flow schema site draft feed lint twin git twin source agent fast yaml.
  - Twin theme review evidence site spec source chart evidence render git.
  - Flow theme evidence flow feed build theme flow feed commit git spec agent diagram.
:::

:::faq
### commit render static twin?
Spec theme twin feed site feed fast token markdown schema.
### git content theme draft?
Flow draft yaml markdown review review schema fast yaml.
:::

::cta{title="review feed static" button="Read the spec" href="https://snypd.rocks/spec"}
