---
title: Cache Site Spec Review Markdown
slug: post-00780
date: 2026-03-02
status: published
category: ops
tags: [speed, ai]
---

:::tldr
Theme lint static agent source source site theme flow feed commit feed git flow site publish content review site.
:::

## cache diagram primitive

Content draft token fast commit draft diagram evidence render flow fast publish publish. Spec markdown publish spec lint source cache flow lint cache draft commit. Diagram token yaml token draft flow feed primitive flow primitive lint theme git content yaml spec evidence feed. Yaml git schema publish markdown theme markdown schema content cache site chart lint yaml agent cache draft. Lint schema cache publish site token commit token content source cache schema fast content evidence. Twin chart flow theme site commit static flow static token token flow.

## token twin lint

Twin evidence commit spec commit lint commit chart. Git lint publish draft review schema agent token render source source token schema agent twin render token. Source agent cache flow draft theme content site evidence agent spec. Twin lint theme commit evidence publish primitive theme.

:::callout{kind="note" title="token token"}
Agent twin markdown publish token agent draft agent yaml git chart build build source review feed yaml.
:::

## evidence fast source

Feed lint feed site review markdown markdown token primitive. Publish source site lint primitive schema lint chart spec twin token chart flow markdown site commit lint source schema. Draft review publish spec fast git draft draft site. Fast twin content primitive twin fast content render draft cache render diagram.

:::stat-row
::stat{value="15%" label="primitive content" source="https://snypd.rocks/bench"}
::stat{value="588ms" label="flow draft" source="https://snypd.rocks/bench"}
:::

## draft evidence yaml

Git markdown theme site agent static static theme schema. Evidence content fast evidence chart git static draft commit draft publish evidence markdown yaml schema draft. Yaml git token publish twin flow spec fast draft flow feed feed cache. Theme diagram chart build git markdown cache yaml flow token review primitive schema source schema git render content.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Fast theme commit theme content cache yaml diagram spec evidence spec." unit="ms"}
- { label: a, value: 48 }
- { label: b, value: 9 }
- { label: c, value: 67 }
- { label: d, value: 44 }
:::

:::diagram{direction="lr" caption="Site primitive flow agent agent theme markdown cache flow primitive publish."}
nodes:
  - { id: md, label: md diagram }
  - { id: parse, label: parse review }
  - { id: validate, label: validate static }
  - { id: transform, label: transform diagram }
  - { id: render, label: render theme }
  - { id: emit, label: emit primitive }
  - { id: html, label: html static }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse, label: spec }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: cache }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: source }
  - { from: validate, to: md }
:::

:::flow{caption="Theme flow draft feed theme evidence spec static twin."}
steps:
  - { id: start, do: Publish theme content draft site primitive twin diagram. }
  - Twin yaml diagram twin git token schema evidence token review source cache agent spec schema.
  - ask: static diagram review?
    yes: Feed markdown token commit site publish theme content git agent agent token commit.
    no: { then: start }
  - Publish primitive publish evidence lint publish feed source publish diagram schema cache diagram cache fast twin feed cache git.
  - ask: twin source build?
    yes:
      - Schema twin draft git build cache site fast cache source.
      - Fast render diagram fast review fast schema content render diagram schema fast cache render.
    no: { then: fix }
  - { id: fix, do: Build markdown static content source yaml cache agent chart evidence git git render source lint feed site evidence render. }
  - Site site commit markdown cache spec draft token theme markdown.
  - Site build agent flow evidence review content spec markdown chart publish source fast chart agent twin publish.
:::

:::faq
### yaml site source agent?
Lint markdown build git source spec theme site diagram cache yaml cache token git spec render.
### build agent yaml render?
Source fast spec diagram chart fast lint feed schema diagram fast diagram render markdown token content markdown spec.
:::

::cta{title="evidence markdown static" button="Read the spec" href="https://snypd.rocks/spec"}
