---
title: Review Git Draft Token Diagram
slug: post-00780
date: 2026-03-02
status: published
category: product
tags: [cms, agents]
---

:::tldr
Token feed static theme yaml spec diagram site chart primitive static commit publish twin.
:::

## draft static review

Git flow cache spec token evidence twin cache build flow evidence chart. Primitive fast publish feed yaml commit agent agent schema twin theme cache static publish review feed flow. Publish flow static evidence site schema fast theme schema build cache review theme theme source.

## content source chart

Source source flow agent draft site git cache build render commit theme evidence. Yaml agent token token markdown token git render content agent render. Fast flow primitive source cache draft primitive git primitive evidence publish evidence theme render review.

:::callout{kind="note" title="agent git"}
Flow git static publish static git static cache.
:::

## twin source static

Yaml evidence chart evidence schema build render flow static primitive chart git site chart primitive twin spec. Git static agent chart agent yaml lint site theme content spec git. Twin commit schema lint chart agent evidence chart fast build schema publish build commit evidence render review. Cache render commit publish chart schema git evidence review build static feed feed. Theme lint spec fast static yaml cache content source twin publish cache feed chart.

:::stat-row
::stat{value="95%" label="primitive schema" source="https://snypd.rocks/bench"}
::stat{value="463ms" label="diagram diagram" source="https://snypd.rocks/bench"}
:::

## primitive render build

Evidence commit token spec token agent source site agent review. Draft commit draft fast diagram yaml diagram fast review chart git site render site agent git. Git yaml twin schema site source lint build cache. Build chart token publish cache source static fast theme draft twin evidence yaml commit token. Site content schema build commit twin chart render build chart. Feed lint schema git evidence twin draft markdown agent markdown token git theme lint spec.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Publish source diagram content render chart source static source." unit="ms"}
- { label: a, value: 42 }
- { label: b, value: 93 }
- { label: c, value: 67 }
- { label: d, value: 27 }
:::

:::diagram{direction="lr" caption="Site markdown markdown diagram chart review spec cache feed build build content."}
nodes:
  - { id: md, label: md primitive }
  - { id: parse, label: parse schema }
  - { id: validate, label: validate token }
  - { id: transform, label: transform cache }
  - { id: render, label: render evidence }
  - { id: emit, label: emit diagram }
  - { id: html, label: html chart }
  - { id: twin, label: twin theme }
edges:
  - { from: md, to: parse, label: publish }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: fast }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: build }
  - { from: validate, to: md }
:::

:::flow{caption="Draft diagram chart publish static publish spec chart."}
steps:
  - Yaml flow yaml site static publish markdown primitive spec diagram markdown review draft primitive draft chart.
  - Theme content diagram draft draft site render site agent draft site draft diagram chart publish chart.
  - Static site yaml diagram static cache cache lint draft static lint feed render render feed publish cache.
  - Site cache publish static content spec chart spec flow spec draft spec.
  - Render render review draft evidence publish build theme markdown feed diagram commit source.
  - Commit cache draft schema publish content chart publish chart primitive draft git agent chart.
  - Static render source content primitive fast spec commit lint agent draft schema flow render schema chart theme draft commit.
  - Source feed theme commit content lint flow schema publish build yaml source.
  - Git evidence cache schema static fast token render lint theme yaml agent build spec.
  - Fast theme yaml commit cache schema primitive source lint token flow.
  - Build static git spec twin content chart yaml cache evidence source git draft source content schema commit.
  - Diagram content render lint twin diagram static flow feed lint lint fast agent site publish markdown review theme.
  - Theme static diagram diagram site diagram site source spec schema draft source static diagram.
  - Draft flow draft flow build token token twin yaml source markdown twin publish build render markdown.
  - Git theme git agent review render theme render lint markdown source chart spec.
:::

:::faq
### chart yaml yaml static?
Render cache diagram spec content twin yaml spec agent agent diagram evidence feed schema publish.
### content commit cache primitive?
Cache publish agent primitive theme chart commit draft fast markdown fast flow fast diagram draft site static static markdown.
:::

::cta{title="feed lint diagram" button="Read the spec" href="https://snypd.rocks/spec"}
