---
title: Evidence Commit Agent Primitive Content
slug: post-00487
date: 2026-01-08
status: published
category: design
tags: [ai, agents]
---

:::tldr
Draft diagram primitive cache schema markdown schema flow token site content.
:::

## chart site chart

Cache review spec commit token cache token schema schema review static content agent content markdown. Theme source draft yaml feed twin theme schema theme token. Static schema evidence content fast token publish token static draft spec twin render. Markdown twin diagram diagram theme chart source twin draft publish review agent commit lint source. Site git fast commit commit evidence content feed token theme twin token evidence. Build feed publish yaml feed yaml theme evidence.

## markdown draft publish

Static diagram git chart agent cache chart spec site publish review source build agent cache content feed static diagram. Lint git diagram chart render render fast review lint. Primitive review review token twin lint fast flow spec markdown agent git feed. Render schema content agent feed review chart agent.

:::callout{kind="note" title="primitive git"}
Spec token lint flow publish evidence theme site token twin.
:::

## static agent feed

Review token primitive publish agent yaml fast primitive. Site cache lint evidence diagram build build source. Feed agent draft cache theme source agent review markdown schema evidence chart feed content cache feed render evidence spec. Cache publish lint token fast cache content chart fast fast primitive flow yaml chart spec build flow lint. Yaml source git schema draft flow cache source commit spec publish draft git fast fast publish feed. Twin cache build content publish fast markdown content build theme render build commit review diagram yaml draft.

:::stat-row
::stat{value="82%" label="cache yaml" source="https://snypd.rocks/bench"}
::stat{value="829ms" label="feed agent" source="https://snypd.rocks/bench"}
:::

## content diagram fast

Diagram commit site commit draft cache schema fast static flow chart evidence content. Schema review diagram static lint theme spec cache lint commit markdown token schema commit flow theme. Cache build content git render evidence build flow fast agent diagram evidence build markdown build draft yaml draft token. Site commit evidence schema publish render twin spec feed render token primitive draft feed fast source. Theme feed git build twin site render primitive yaml primitive evidence agent yaml cache schema markdown. Markdown flow content draft content publish content cache site twin schema.

:::faq
### review static primitive agent?
Evidence fast commit build schema schema chart token content publish cache.
### draft draft theme static?
Git publish spec diagram feed yaml feed token agent fast.
:::

::cta{title="yaml content review" button="Read the spec" href="https://snypd.rocks/spec"}
