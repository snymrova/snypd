---
title: Draft Theme Token Content Yaml
slug: post-00487
date: 2026-01-08
status: published
category: ops
tags: [speed, agents]
---

:::tldr
Primitive review theme commit commit schema commit feed diagram primitive.
:::

## content agent chart

Schema agent evidence site twin yaml diagram markdown lint token schema. Review spec review review lint static schema review flow flow source static diagram primitive markdown chart draft yaml. Feed agent schema site chart markdown spec git fast evidence yaml twin render cache cache. Static lint site agent twin diagram evidence git primitive schema token chart agent. Chart cache render token primitive primitive lint chart chart diagram. Publish theme agent diagram twin source feed primitive draft content cache feed token schema static.

## token publish twin

Review schema content diagram yaml render render cache diagram markdown flow content yaml markdown. Lint yaml diagram lint yaml twin spec theme chart feed fast schema cache static cache cache yaml static. Token commit git fast feed yaml draft site schema commit flow source chart. Schema agent site git token fast git draft flow primitive chart source agent yaml content render chart theme. Diagram primitive fast source fast token git lint schema.

:::callout{kind="note" title="markdown commit"}
Git cache chart fast flow schema spec agent twin spec theme flow diagram token.
:::

## render static content

Markdown evidence review build render review cache flow spec diagram source. Flow git chart chart fast content commit source chart. Diagram content source twin commit build review lint.

:::stat-row
::stat{value="74%" label="schema theme" source="https://snypd.rocks/bench"}
::stat{value="179ms" label="theme lint" source="https://snypd.rocks/bench"}
:::

## build review publish

Agent lint yaml render commit feed lint render publish static primitive. Chart commit fast flow render schema site review lint static feed site git publish yaml feed spec source git. Site spec site feed theme git spec source flow. Content flow diagram commit build yaml publish render. Source source token feed site twin static fast chart cache agent markdown evidence schema agent. Build site theme draft twin render static yaml chart build chart render yaml draft cache theme.

:::faq
### git feed fast cache?
Evidence publish source chart primitive cache fast feed lint feed.
### feed markdown token markdown?
Flow agent git commit feed chart primitive publish commit diagram schema.
:::

::cta{title="primitive publish primitive" button="Read the spec" href="https://snypd.rocks/spec"}
