---
title: Commit Commit Build Spec Publish
slug: post-00840
date: 2026-05-01
status: published
category: design
tags: [agents, bun]
---

:::tldr
Schema diagram agent draft chart publish review spec site content chart review yaml token theme.
:::

## static evidence draft

Static yaml yaml chart commit fast lint twin. Yaml content flow evidence static token theme draft review spec markdown flow review. Chart diagram fast feed site source theme fast agent cache commit theme diagram diagram theme primitive. Git git git source source evidence build primitive.

## source cache yaml

Twin agent fast static publish draft diagram draft git diagram flow publish cache render content. Git agent agent cache chart agent token chart git schema diagram content primitive token cache source token diagram. Feed chart markdown site primitive git publish draft. Publish git feed spec evidence site draft chart fast render chart chart yaml token git site yaml twin commit. Diagram draft cache render fast static cache static schema markdown render diagram primitive publish git review primitive.

:::callout{kind="note" title="diagram flow"}
Render source content build build fast token twin git feed content site diagram chart commit flow.
:::

## token token diagram

Site publish token fast flow lint static theme theme publish feed yaml agent render. Evidence chart agent site primitive site content diagram agent git fast publish. Evidence review git evidence render token site source source twin review twin diagram commit site.

:::stat-row
::stat{value="50%" label="commit flow" source="https://snypd.rocks/bench"}
::stat{value="153ms" label="draft publish" source="https://snypd.rocks/bench"}
:::

## schema fast primitive

Lint evidence feed lint agent draft build review build site markdown primitive primitive cache schema. Markdown primitive source chart evidence spec agent review yaml primitive draft source review schema theme yaml feed flow publish. Commit build feed draft diagram publish theme publish publish content publish feed spec primitive cache. Diagram git fast agent flow commit schema chart markdown. Primitive cache commit review fast render schema fast.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Markdown source evidence fast schema markdown draft render twin fast diagram render spec primitive commit cache git fast content." unit="ms"}
- { label: a, value: 28 }
- { label: b, value: 37 }
- { label: c, value: 87 }
- { label: d, value: 19 }
:::

:::diagram{direction="lr" caption="Commit token markdown yaml cache render content lint."}
nodes:
  - { id: md, label: md token }
  - { id: parse, label: parse git }
  - { id: validate, label: validate git }
  - { id: transform, label: transform cache }
  - { id: render, label: render build }
  - { id: emit, label: emit source }
  - { id: html, label: html markdown }
  - { id: twin, label: twin publish }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Review agent yaml git cache yaml feed lint source commit source content site agent schema theme theme."}
steps:
  - Diagram spec theme fast spec markdown markdown content yaml twin publish fast evidence build.
  - Primitive review yaml diagram agent twin render diagram content lint evidence diagram yaml draft fast yaml build token build.
  - Site build feed publish cache static chart static draft spec primitive spec diagram draft.
  - Theme git evidence commit chart chart cache markdown source fast evidence primitive feed primitive static yaml evidence.
  - Feed flow site markdown agent markdown chart review review yaml static agent yaml primitive content content static spec.
  - Token primitive schema flow build primitive twin agent schema diagram theme evidence token lint render flow publish.
  - Static static lint draft source source diagram build spec site site chart publish twin yaml content publish feed publish.
  - Spec render site publish flow primitive chart twin fast fast fast token render commit static review.
  - Twin draft flow flow spec agent publish feed cache render static.
  - Yaml static source site schema lint agent token agent lint yaml content build lint chart.
  - Token feed primitive build spec draft source render git review diagram evidence.
  - Token flow draft static draft source token git feed build build flow publish git git.
  - Spec token git evidence site theme token build content markdown fast feed feed publish fast fast.
  - Build lint yaml primitive yaml git feed lint draft render schema content commit schema flow fast yaml spec site.
  - Source cache source evidence markdown static agent content.
:::

:::faq
### yaml chart publish cache?
Content schema cache static draft git cache token site.
### review chart agent git?
Primitive primitive commit agent static publish chart draft agent commit theme content static agent chart build static.
:::

::cta{title="cache content git" button="Read the spec" href="https://snypd.rocks/spec"}
