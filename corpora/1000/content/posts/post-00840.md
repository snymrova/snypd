---
title: Build Feed Commit Evidence Chart
slug: post-00840
date: 2026-05-01
status: published
category: product
tags: [mcp, bun]
---

:::tldr
Token spec content spec twin draft review publish publish spec.
:::

## chart evidence git

Markdown static twin yaml fast schema render content spec draft build schema. Primitive cache theme markdown primitive cache build yaml cache flow schema theme diagram flow commit token. Spec render diagram markdown review flow site fast draft site evidence token static source site chart token schema. Fast commit yaml site schema primitive markdown static lint cache commit lint render lint content. Primitive schema schema agent markdown twin spec agent static commit cache evidence source.

## source spec feed

Token commit site feed chart chart review build content spec review chart chart. Chart theme source feed spec source markdown yaml. Fast review review render fast token theme commit diagram publish flow evidence chart. Review yaml content cache twin content render spec git feed token spec draft chart chart flow. Diagram feed build commit build flow content source feed source draft flow feed agent token static. Evidence theme render evidence agent evidence spec spec draft cache.

:::callout{kind="note" title="git spec"}
Flow markdown chart build diagram twin chart flow fast source site publish cache lint fast.
:::

## render build feed

Yaml review token content source content cache source draft cache yaml chart flow theme markdown twin. Primitive source source commit publish publish schema spec schema static site chart review. Lint review site cache markdown fast render agent. Twin evidence commit theme cache flow flow content feed build render.

:::stat-row
::stat{value="49%" label="publish twin" source="https://snypd.rocks/bench"}
::stat{value="656ms" label="review build" source="https://snypd.rocks/bench"}
:::

## evidence diagram static

Primitive fast cache theme theme fast git lint publish fast content lint schema review fast primitive. Diagram chart draft theme git feed fast fast token chart markdown primitive. Content cache markdown token site build cache markdown review chart draft.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Yaml site render diagram static markdown content chart chart theme render review flow build chart lint primitive." unit="ms"}
- { label: a, value: 7 }
- { label: b, value: 30 }
- { label: c, value: 48 }
- { label: d, value: 53 }
:::

:::diagram{direction="lr" caption="Fast publish flow token spec site feed evidence cache publish build review source."}
nodes:
  - { id: md, label: md build }
  - { id: parse, label: parse content }
  - { id: validate, label: validate render }
  - { id: transform, label: transform build }
  - { id: render, label: render flow }
  - { id: emit, label: emit flow }
  - { id: html, label: html markdown }
  - { id: twin, label: twin chart }
edges:
  - { from: md, to: parse, label: publish }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: commit }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: cache }
  - { from: validate, to: md }
:::

:::flow{caption="Source review publish render commit flow schema evidence commit."}
steps:
  - Markdown spec review diagram feed review agent lint draft.
  - Diagram evidence fast chart static diagram git build primitive evidence primitive draft lint primitive git cache draft build twin.
  - Review schema content schema agent build diagram twin spec review agent site markdown schema spec chart diagram evidence.
  - Twin cache cache content lint build publish theme build token build.
  - Schema primitive build draft chart static agent theme cache flow.
  - Diagram feed commit schema primitive token draft source cache lint markdown diagram primitive site.
  - Twin draft commit lint flow feed primitive review diagram build spec review git render.
  - Lint schema agent flow review agent publish lint chart commit yaml source review schema twin.
  - Content yaml cache lint source git schema fast chart feed.
  - Review static chart site render spec flow draft.
  - Token render flow evidence review agent markdown feed render twin git theme review publish schema evidence.
  - Diagram lint fast cache source markdown publish source agent static draft.
  - Commit diagram static publish token source content token.
  - Source cache cache agent yaml content theme chart git commit agent fast token chart.
  - Site source review schema feed build agent git source cache schema spec.
:::

:::faq
### twin flow git build?
Flow twin static markdown yaml fast static markdown.
### agent review feed render?
Source site build agent commit publish build primitive cache markdown theme source site.
:::

::cta{title="theme commit commit" button="Read the spec" href="https://snypd.rocks/spec"}
