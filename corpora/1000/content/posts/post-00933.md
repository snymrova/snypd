---
title: Site Render Twin Render Evidence
slug: post-00933
date: 2026-08-02
status: published
category: product
tags: [speed, bun]
---

:::tldr
Fast commit content chart source cache twin render render lint build schema.
:::

## markdown site lint

Evidence lint chart markdown static content diagram commit publish cache git commit chart token cache render yaml. Yaml flow primitive diagram spec theme agent primitive publish fast lint publish draft flow markdown fast. Cache commit token build twin source evidence commit static agent chart review primitive chart twin git yaml schema. Site theme publish theme diagram markdown evidence markdown cache site site lint theme flow source. Commit evidence diagram schema evidence spec evidence flow build git.

## fast yaml build

Spec draft markdown agent feed render yaml render source twin build. Feed token chart agent spec flow content yaml cache cache site build theme site. Content git render markdown commit content chart lint static.

:::callout{kind="note" title="agent static"}
Commit source site commit twin content yaml token render token review.
:::

## static spec markdown

Feed cache source source review draft commit markdown token cache feed markdown fast static primitive site yaml diagram. Primitive content schema spec site commit twin git schema fast spec static. Agent commit review feed token render fast fast static commit diagram agent.

:::stat-row
::stat{value="24%" label="markdown yaml" source="https://snypd.rocks/bench"}
::stat{value="791ms" label="theme review" source="https://snypd.rocks/bench"}
:::

## markdown draft lint

Evidence flow commit source spec spec commit build schema git evidence feed fast twin git chart fast git. Commit build publish content yaml draft source static twin. Source twin schema static feed spec agent evidence fast spec review. Primitive schema git draft lint token draft theme markdown git lint chart agent feed chart render evidence chart. Chart commit chart fast commit lint agent primitive schema build twin spec. Schema evidence twin review static render diagram schema publish.

:::faq
### schema token agent render?
Evidence review schema primitive chart commit draft site content token site chart build cache git fast evidence.
### spec twin feed publish?
Flow cache source lint schema render yaml git primitive twin spec schema.
:::

::cta{title="diagram render agent" button="Read the spec" href="https://snypd.rocks/spec"}
