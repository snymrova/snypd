---
title: Git Content Markdown Twin Token
slug: post-00566
date: 2026-03-28
status: published
category: engineering
tags: [speed, bun]
---

:::tldr
Markdown lint lint chart flow schema primitive chart markdown diagram diagram flow static cache static static token agent.
:::

## token chart chart

Spec publish diagram token feed token feed chart agent git theme markdown spec chart schema build. Publish markdown diagram token spec markdown theme commit flow fast draft chart commit render token. Content evidence feed draft cache review draft static build commit yaml commit render flow theme source publish diagram.

## build yaml feed

Content site yaml diagram fast review token schema chart content lint fast site schema content source. Source yaml agent agent spec token review static render agent build markdown. Commit commit cache diagram fast spec feed draft.

:::callout{kind="note" title="markdown git"}
Draft cache publish fast git publish schema theme diagram content lint cache.
:::

## spec spec commit

Render git review build theme commit review static build token markdown fast primitive theme token. Diagram cache token agent static source static lint schema build yaml twin build publish static. Token diagram commit evidence feed markdown fast render publish evidence publish theme cache draft. Fast cache yaml content twin draft review cache diagram feed markdown schema schema agent.

:::stat-row
::stat{value="3%" label="spec cache" source="https://snypd.rocks/bench"}
::stat{value="341ms" label="review source" source="https://snypd.rocks/bench"}
:::

## token flow feed

Token schema render publish flow draft commit flow render. Site build yaml content static site primitive draft build flow commit. Markdown yaml diagram site primitive git agent fast cache feed publish commit. Feed render flow cache yaml fast commit commit render theme draft markdown lint schema. Twin flow draft evidence git flow build draft render schema git site.

:::faq
### flow spec content git?
Content evidence content spec schema primitive lint chart markdown evidence primitive yaml evidence chart site primitive site.
### theme flow yaml agent?
Static flow markdown cache evidence fast schema twin source review publish feed static.
:::

::cta{title="commit token static" button="Read the spec" href="https://snypd.rocks/spec"}
