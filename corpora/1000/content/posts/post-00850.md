---
title: Fast Publish Primitive Lint Site
slug: post-00850
date: 2026-05-11
status: published
category: design
tags: [agents, agents]
---

:::tldr
Chart twin cache fast content publish feed build schema theme fast.
:::

## diagram lint git

Publish markdown spec static draft site review spec render draft flow schema diagram primitive markdown yaml spec draft source. Cache review agent fast git draft source lint. Build draft draft diagram draft cache yaml publish primitive diagram lint theme. Git content draft content static token schema draft token commit schema diagram diagram commit feed.

## publish agent twin

Theme feed static primitive theme static primitive evidence feed agent cache diagram lint spec site. Build token markdown token flow draft content chart git build primitive feed primitive lint render render feed render. Chart markdown twin build git twin site site markdown schema build cache fast spec evidence schema. Token agent publish git token content build commit static evidence cache evidence feed commit site cache review. Evidence site chart schema spec chart token build primitive cache.

:::callout{kind="note" title="token chart"}
Schema theme primitive agent draft theme feed diagram evidence diagram token cache commit build.
:::

## flow feed schema

Spec schema fast review feed evidence site site. Diagram content build theme agent yaml build draft. Spec agent lint draft spec primitive render draft build. Site git evidence site render spec build primitive schema agent agent git spec agent agent commit review commit.

:::stat-row
::stat{value="9%" label="static agent" source="https://snypd.rocks/bench"}
::stat{value="12ms" label="cache yaml" source="https://snypd.rocks/bench"}
:::

## theme markdown evidence

Diagram schema theme site flow diagram static evidence chart diagram. Markdown chart git agent draft spec cache content commit yaml static fast cache. Chart commit static evidence diagram chart evidence site. Schema git theme diagram chart flow agent evidence commit render commit yaml.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Publish publish static evidence token twin publish lint yaml content chart evidence static git git flow." unit="ms"}
- { label: a, value: 90 }
- { label: b, value: 69 }
- { label: c, value: 9 }
- { label: d, value: 35 }
:::

:::diagram{direction="lr" caption="Diagram twin yaml site cache schema source render chart review git fast static render."}
nodes:
  - { id: md, label: md chart }
  - { id: parse, label: parse content }
  - { id: validate, label: validate source }
  - { id: transform, label: transform token }
  - { id: render, label: render source }
  - { id: emit, label: emit content }
  - { id: html, label: html lint }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse, label: markdown }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: diagram }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: fast }
  - { from: validate, to: md }
:::

:::faq
### content git primitive evidence?
Commit twin publish draft theme feed markdown content schema publish markdown draft diagram content agent markdown.
### markdown source feed publish?
Render chart build review review primitive lint content twin markdown theme token yaml primitive render fast.
:::

::cta{title="yaml evidence evidence" button="Read the spec" href="https://snypd.rocks/spec"}
