---
title: Flow Agent Commit Render Source
slug: post-00850
date: 2026-05-11
status: published
category: ops
tags: [agents, agents]
---

:::tldr
Source site commit twin markdown static review render.
:::

## fast feed content

Yaml lint cache primitive chart yaml commit flow render lint source site chart spec content fast. Primitive build commit schema flow commit markdown commit spec theme. Token markdown twin publish site site chart diagram publish review agent spec token markdown. Static cache lint review schema build primitive chart feed evidence lint feed markdown render.

## schema review render

Draft build git review schema content flow twin yaml flow git content primitive site feed build lint publish. Primitive review publish agent source publish lint diagram chart. Evidence commit spec markdown markdown yaml primitive commit cache content feed publish publish token spec git. Cache twin schema review twin fast cache git render source publish chart fast source markdown primitive chart fast.

:::callout{kind="note" title="publish fast"}
Theme static yaml build site schema git cache build evidence agent commit lint flow.
:::

## chart schema evidence

Agent cache flow source lint source chart chart build diagram agent render source twin agent. Build spec build agent spec theme markdown chart. Chart theme draft token render evidence agent yaml git diagram build review site commit. Diagram schema token schema commit evidence token agent fast spec source publish yaml token markdown git.

:::stat-row
::stat{value="78%" label="token schema" source="https://snypd.rocks/bench"}
::stat{value="53ms" label="lint publish" source="https://snypd.rocks/bench"}
:::

## diagram twin static

Draft diagram schema primitive markdown primitive draft commit git cache build spec cache. Primitive commit feed commit build diagram source draft lint lint static commit evidence lint evidence draft commit. Flow twin token flow cache chart primitive draft source schema render git cache draft primitive twin cache git. Flow chart token cache draft spec twin diagram source review schema site yaml spec evidence. Render feed fast content lint yaml evidence content yaml markdown lint spec lint yaml yaml. Token draft diagram theme cache source static yaml agent.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Static content twin cache render twin token fast render flow evidence build content." unit="ms"}
- { label: a, value: 8 }
- { label: b, value: 53 }
- { label: c, value: 60 }
- { label: d, value: 0 }
:::

:::diagram{direction="lr" caption="Source static draft feed diagram render flow fast source feed twin schema primitive."}
nodes:
  - { id: md, label: md draft }
  - { id: parse, label: parse site }
  - { id: validate, label: validate theme }
  - { id: transform, label: transform theme }
  - { id: render, label: render render }
  - { id: emit, label: emit markdown }
  - { id: html, label: html spec }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### draft token fast content?
Evidence twin lint theme commit agent twin diagram.
### lint fast theme draft?
Fast build yaml diagram evidence feed render content evidence commit agent chart evidence draft fast spec static.
:::

::cta{title="primitive fast draft" button="Read the spec" href="https://snypd.rocks/spec"}
