---
title: Diagram Build Primitive Site Spec
slug: post-00830
date: 2026-04-21
status: published
category: ops
tags: [seo, seo]
---

:::tldr
Render cache static draft primitive build fast schema commit fast flow markdown.
:::

## content twin content

Build markdown flow commit markdown token markdown content flow lint spec primitive. Cache render feed feed yaml spec build render. Feed markdown twin git static static agent cache agent chart draft. Git flow cache evidence feed agent publish yaml primitive. Flow theme evidence static source lint primitive twin.

## spec draft agent

Yaml fast theme build publish static theme commit git chart markdown static fast commit chart diagram commit spec render. Cache git review content review static evidence token twin agent fast publish source yaml flow agent token. Source cache cache source render theme theme agent content feed agent static review yaml. Markdown git chart cache evidence lint flow evidence primitive build source. Review primitive site render build flow publish lint publish source theme agent twin site.

:::callout{kind="note" title="spec commit"}
Twin yaml site schema feed evidence site evidence twin theme render.
:::

## publish twin source

Draft cache lint agent cache draft source evidence static. Site render render agent commit content token twin chart schema. Spec yaml chart diagram content flow render build review agent review agent token schema source. Review schema twin cache site render twin commit evidence theme token.

:::stat-row
::stat{value="9%" label="static token" source="https://snypd.rocks/bench"}
::stat{value="129ms" label="feed token" source="https://snypd.rocks/bench"}
:::

## fast site source

Spec spec yaml evidence cache diagram git cache twin. Site diagram render render render git theme twin review. Draft flow feed source token source evidence schema cache site token diagram twin build. Commit render review schema theme flow git diagram git token feed. Evidence chart evidence yaml primitive token twin yaml diagram primitive primitive primitive agent chart feed feed.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Lint flow source build yaml content lint yaml cache diagram static token commit site chart static theme." unit="ms"}
- { label: a, value: 39 }
- { label: b, value: 59 }
- { label: c, value: 38 }
- { label: d, value: 6 }
:::

:::diagram{direction="lr" caption="Feed spec site site site twin build source."}
nodes:
  - { id: md, label: md review }
  - { id: parse, label: parse spec }
  - { id: validate, label: validate site }
  - { id: transform, label: transform static }
  - { id: render, label: render diagram }
  - { id: emit, label: emit lint }
  - { id: html, label: html chart }
  - { id: twin, label: twin publish }
edges:
  - { from: md, to: parse, label: primitive }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: spec }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: source }
  - { from: validate, to: md }
:::

:::faq
### markdown static token token?
Flow content cache site content fast chart build markdown site static draft.
### agent commit site cache?
Diagram draft static source agent lint spec chart evidence evidence cache theme lint fast chart.
:::

::cta{title="render build render" button="Read the spec" href="https://snypd.rocks/spec"}
