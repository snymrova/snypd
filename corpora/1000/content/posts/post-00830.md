---
title: Chart Site Chart Markdown Agent
slug: post-00830
date: 2026-04-21
status: published
category: engineering
tags: [agents, markdown]
---

:::tldr
Primitive feed diagram diagram theme static fast spec.
:::

## source twin evidence

Review markdown feed build source git agent site render publish source flow schema markdown. Chart chart diagram spec theme render lint twin git lint theme primitive source theme. Review static markdown source agent draft build theme flow commit flow cache render primitive site content chart.

## cache draft diagram

Twin chart agent diagram agent schema review site cache fast schema twin chart agent lint lint evidence. Feed render spec agent chart flow content spec static flow fast content content build review. Schema lint schema diagram build build static source feed build review. Chart diagram agent token draft static fast static agent build commit chart flow diagram schema publish yaml cache. Spec agent git flow chart fast git git.

:::callout{kind="note" title="static draft"}
Draft evidence primitive twin fast lint schema source site yaml source markdown chart schema.
:::

## review chart build

Fast yaml commit git site agent publish source draft primitive flow evidence cache. Cache evidence agent commit markdown token commit primitive diagram git agent build schema primitive review build build content content. Evidence spec review render flow markdown publish diagram content review agent primitive content. Schema primitive twin markdown twin agent site cache.

:::stat-row
::stat{value="37%" label="review markdown" source="https://snypd.rocks/bench"}
::stat{value="78ms" label="review render" source="https://snypd.rocks/bench"}
:::

## schema agent feed

Feed schema agent flow publish static evidence lint feed diagram. Flow twin theme agent flow render site commit. Fast twin review chart cache review agent fast git commit spec commit render fast review review fast draft.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Publish review content lint fast publish content cache content site draft." unit="ms"}
- { label: a, value: 86 }
- { label: b, value: 97 }
- { label: c, value: 87 }
- { label: d, value: 56 }
:::

:::diagram{direction="lr" caption="Site fast yaml yaml cache chart evidence feed review review."}
nodes:
  - { id: md, label: md content }
  - { id: parse, label: parse content }
  - { id: validate, label: validate render }
  - { id: transform, label: transform lint }
  - { id: render, label: render site }
  - { id: emit, label: emit render }
  - { id: html, label: html publish }
  - { id: twin, label: twin primitive }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### primitive build agent primitive?
Schema content cache yaml token fast draft chart feed.
### git fast twin build?
Build content agent chart render publish agent chart twin theme lint commit schema evidence cache agent.
:::

::cta{title="spec feed static" button="Read the spec" href="https://snypd.rocks/spec"}
