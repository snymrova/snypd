---
title: Lint Yaml Review Review Fast
slug: post-00830
date: 2026-04-21
status: published
category: design
tags: [speed, mcp]
---

:::tldr
Twin evidence site build source flow fast lint yaml yaml markdown.
:::

## token build agent

Fast markdown static review flow twin git draft markdown. Yaml evidence schema render schema primitive fast spec draft agent cache static. Schema build site publish schema schema diagram twin static.

## review schema fast

Flow flow schema evidence source diagram twin publish publish yaml agent static lint spec twin fast. Commit yaml yaml diagram evidence theme lint yaml build token spec review schema commit static schema build. Fast publish publish markdown spec build theme primitive content primitive static token evidence markdown chart. Fast feed markdown markdown diagram chart evidence spec fast spec review.

:::callout{kind="note" title="build evidence"}
Chart source fast fast diagram content primitive schema evidence content agent primitive.
:::

## cache publish spec

Fast primitive content lint token feed source static content fast flow flow site twin yaml commit render. Evidence lint render cache primitive theme spec review commit review review render site review token. Static render lint theme source token spec cache schema agent commit lint draft. Theme evidence git render yaml primitive markdown spec schema commit review evidence review site primitive draft token draft render. Theme site yaml primitive source theme publish publish chart.

:::stat-row
::stat{value="45%" label="flow flow" source="https://snypd.rocks/bench"}
::stat{value="860ms" label="site cache" source="https://snypd.rocks/bench"}
:::

## theme yaml source

Flow yaml commit flow review agent diagram flow schema git diagram source yaml chart chart source. Agent twin render twin build diagram static cache source content cache content static cache. Agent lint cache static markdown theme site draft render publish source build publish commit theme chart source static. Render git publish diagram lint chart commit token fast yaml agent agent fast render. Spec yaml agent draft render token static commit source build source build git draft token.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Build flow yaml evidence static fast cache theme fast git markdown token render." unit="ms"}
- { label: a, value: 69 }
- { label: b, value: 65 }
- { label: c, value: 84 }
- { label: d, value: 66 }
:::

:::diagram{direction="lr" caption="Flow markdown cache build flow git source feed build theme review commit git theme schema agent lint theme."}
nodes:
  - { id: md, label: md git }
  - { id: parse, label: parse build }
  - { id: validate, label: validate git }
  - { id: transform, label: transform review }
  - { id: render, label: render draft }
  - { id: emit, label: emit primitive }
  - { id: html, label: html commit }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse, label: draft }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: diagram }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: flow }
  - { from: validate, to: md }
:::

:::faq
### spec agent evidence lint?
Twin theme spec flow publish evidence draft yaml static publish primitive site token content static build publish.
### feed cache token draft?
Git theme site spec review draft token markdown spec yaml flow token fast primitive review.
:::

::cta{title="feed diagram primitive" button="Read the spec" href="https://snypd.rocks/spec"}
