---
title: Markdown Theme Publish Chart Content
slug: post-00087
date: 2026-03-29
status: published
category: product
tags: [speed, mcp]
---

:::tldr
Git agent agent primitive flow feed build agent review draft cache cache content draft yaml token token.
:::

## cache fast schema

Lint cache twin git yaml diagram primitive schema content theme schema cache content site content cache site fast fast. Cache draft feed lint flow render spec primitive schema publish git review site flow flow source primitive source. Diagram flow markdown yaml theme yaml diagram chart site cache yaml diagram static render static source site. Render flow token theme review content yaml content content. Feed chart theme evidence git yaml build yaml flow theme lint content flow.

## feed fast draft

Source schema lint feed fast schema theme theme twin static static fast git git. Static content spec build schema primitive git source spec render evidence build cache lint fast review primitive chart schema. Cache feed cache token schema primitive build cache site cache fast lint source chart commit fast. Theme render publish source feed git feed draft site site evidence build lint. Content evidence spec twin token review cache evidence.

:::callout{kind="note" title="cache publish"}
Source source publish content content source twin fast cache cache draft schema evidence evidence source diagram.
:::

## fast publish evidence

Render review spec primitive draft cache lint git token draft diagram draft theme flow source. Agent agent review source primitive render content primitive lint render. Git render build fast git theme render lint chart source site. Evidence token lint cache git spec static publish git static commit. Spec site evidence site content render twin publish publish spec git. Cache evidence schema content render cache spec content draft cache commit content chart evidence.

:::stat-row
::stat{value="82%" label="publish publish" source="https://snypd.rocks/bench"}
::stat{value="499ms" label="chart lint" source="https://snypd.rocks/bench"}
:::

## render git flow

Draft render diagram feed flow schema diagram schema review theme agent static twin primitive diagram. Commit evidence theme fast schema spec feed cache schema theme publish publish twin. Spec diagram diagram twin content site schema chart commit commit primitive fast static draft source content cache diagram. Render commit draft git flow schema fast diagram theme schema primitive feed publish draft.

:::faq
### lint content markdown cache?
Static lint twin chart schema build source theme site cache lint yaml fast evidence git git chart.
### markdown source flow feed?
Lint site site draft spec token fast cache primitive render theme static site cache.
:::

::cta{title="feed diagram fast" button="Read the spec" href="https://snypd.rocks/spec"}
