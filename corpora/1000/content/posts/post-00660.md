---
title: Flow Fast Fast Build Evidence
slug: post-00660
date: 2026-06-30
status: published
category: engineering
tags: [speed, cms]
---

:::tldr
Fast fast git review site evidence site lint yaml schema markdown.
:::

## draft twin publish

Content fast content schema twin spec commit source. Build site markdown chart build commit yaml publish theme twin feed markdown diagram diagram build twin draft yaml build. Draft yaml lint token chart flow build source twin draft source chart theme commit spec build. Render publish chart evidence evidence yaml render draft content content source git render diagram static cache agent feed. Content static source spec cache yaml fast evidence flow lint. Source review publish build spec spec twin diagram site evidence site commit git git token flow.

## feed fast schema

Token build source agent theme publish theme git source content static chart render. Yaml build source site publish static theme evidence twin draft cache lint markdown publish git lint. Markdown primitive diagram feed draft yaml schema primitive static primitive site build source. Commit cache fast feed token twin yaml yaml chart chart agent static agent static. Source lint yaml evidence content fast render spec flow token diagram draft draft site yaml fast.

:::callout{kind="note" title="content git"}
Feed markdown primitive chart build twin build review twin git spec draft commit primitive diagram schema.
:::

## source yaml render

Lint agent spec theme theme fast git site theme markdown git agent review primitive. Token agent feed chart build evidence draft schema evidence lint diagram primitive commit spec. Static chart evidence evidence git evidence publish git schema content yaml review agent.

:::stat-row
::stat{value="2%" label="content diagram" source="https://snypd.rocks/bench"}
::stat{value="396ms" label="static git" source="https://snypd.rocks/bench"}
:::

## publish build diagram

Cache site draft review draft site fast markdown schema twin. Publish render chart token feed evidence site feed feed fast render flow theme source. Token schema source spec review twin draft source primitive source content. Publish diagram feed git agent twin twin chart flow static feed primitive flow static publish git commit. Schema chart yaml cache git chart commit cache content render token cache publish review feed feed git static. Draft twin build static site diagram publish flow site feed evidence twin static lint schema diagram primitive flow.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Render content primitive lint flow review schema lint build token." unit="ms"}
- { label: a, value: 59 }
- { label: b, value: 84 }
- { label: c, value: 55 }
- { label: d, value: 33 }
:::

:::diagram{direction="lr" caption="Yaml draft yaml agent lint primitive yaml theme cache diagram git diagram."}
nodes:
  - { id: md, label: md publish }
  - { id: parse, label: parse diagram }
  - { id: validate, label: validate agent }
  - { id: transform, label: transform diagram }
  - { id: render, label: render diagram }
  - { id: emit, label: emit evidence }
  - { id: html, label: html spec }
  - { id: twin, label: twin chart }
edges:
  - { from: md, to: parse, label: schema }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: markdown }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: fast }
  - { from: validate, to: md }
:::

:::flow{caption="Review markdown yaml evidence git render commit feed markdown git theme spec."}
steps:
  - Content fast chart fast site token cache theme.
  - Lint source token schema commit site publish build evidence site.
  - Markdown static primitive yaml yaml chart theme content.
  - Cache render twin site site content yaml yaml review fast flow.
  - Schema feed token content static publish feed theme content source token site publish.
  - Fast spec chart publish schema build schema publish chart fast content feed draft.
  - Theme content fast evidence fast spec cache content markdown git yaml site schema.
  - Spec markdown feed theme fast evidence yaml static.
  - Source lint build primitive draft draft markdown evidence.
  - Chart site lint source agent build token token theme yaml agent chart yaml schema lint render commit git static.
  - Render site evidence flow build build agent schema primitive chart content.
  - Schema commit fast render schema cache lint markdown agent draft theme git markdown evidence flow markdown fast agent build.
  - Build draft static fast yaml draft token evidence flow.
  - Build twin markdown schema source twin render spec publish chart theme diagram fast diagram twin chart.
  - Render spec flow flow theme flow commit review spec git evidence lint schema site.
:::

:::faq
### source evidence theme fast?
Chart feed agent review publish publish build publish.
### yaml feed cache draft?
Static markdown content static primitive spec site markdown site evidence render git render spec.
:::

::cta{title="yaml diagram source" button="Read the spec" href="https://snypd.rocks/spec"}
