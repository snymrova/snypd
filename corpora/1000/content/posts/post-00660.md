---
title: Static Feed Twin Publish Render
slug: post-00660
date: 2026-06-30
status: published
category: ops
tags: [markdown, speed]
---

:::tldr
Spec draft cache schema token diagram site fast feed draft build spec markdown review.
:::

## primitive cache site

Draft markdown site review markdown build evidence cache flow evidence schema flow. Content static theme chart content commit theme content token yaml cache render fast yaml fast content spec twin render. Schema theme content chart lint agent markdown yaml static fast draft agent spec site. Git diagram evidence review spec spec token yaml agent commit twin cache build twin token review cache build fast.

## token theme spec

Agent build publish diagram flow lint fast twin. Diagram diagram build twin schema draft flow review agent static. Theme cache evidence theme source render schema flow. Diagram draft site render primitive publish flow theme commit schema markdown chart git.

:::callout{kind="note" title="site content"}
Markdown yaml commit chart token commit theme build evidence render source flow flow schema markdown.
:::

## twin content cache

Flow theme cache source diagram fast commit build render. Agent cache feed git yaml git git theme source yaml primitive yaml diagram fast source draft feed. Chart lint site theme token diagram static spec schema draft evidence schema evidence source render. Theme source git build review content agent yaml. Site spec chart lint evidence review git twin. Spec twin feed twin diagram spec twin schema cache fast markdown markdown spec schema git markdown site lint publish.

:::stat-row
::stat{value="42%" label="site primitive" source="https://snypd.rocks/bench"}
::stat{value="573ms" label="spec source" source="https://snypd.rocks/bench"}
:::

## chart commit markdown

Commit git lint twin publish yaml source render spec review build draft commit review review draft. Fast yaml yaml commit primitive diagram token chart spec render token static commit markdown evidence agent primitive. Render twin fast agent primitive static fast twin site evidence review review. Render markdown markdown git theme markdown token flow commit chart static chart schema flow feed fast markdown schema.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Render theme flow token commit schema theme primitive fast markdown site commit cache static schema static token diagram static." unit="ms"}
- { label: a, value: 1 }
- { label: b, value: 37 }
- { label: c, value: 3 }
- { label: d, value: 75 }
:::

:::diagram{direction="lr" caption="Diagram commit draft content flow fast spec draft content site static token lint commit flow theme."}
nodes:
  - { id: md, label: md primitive }
  - { id: parse, label: parse fast }
  - { id: validate, label: validate chart }
  - { id: transform, label: transform diagram }
  - { id: render, label: render static }
  - { id: emit, label: emit feed }
  - { id: html, label: html content }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse, label: evidence }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: agent }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: git }
  - { from: validate, to: md }
:::

:::flow{caption="Content primitive diagram theme token agent diagram schema markdown twin."}
steps:
  - { id: start, do: Render evidence token feed publish draft feed fast schema markdown theme token lint schema review fast static source feed. }
  - Markdown schema site theme twin build render spec.
  - ask: twin lint content?
    yes: Draft review flow publish chart render flow lint twin static yaml build source cache publish theme.
    no: { then: start }
  - Build agent source publish draft diagram commit review chart fast theme evidence agent schema evidence feed git commit.
  - ask: git static flow?
    yes:
      - Commit evidence content flow git flow publish flow cache feed.
      - Draft git spec token theme content twin commit primitive token content.
    no: { then: fix }
  - { id: fix, do: Lint theme diagram draft evidence diagram commit theme content markdown source twin. }
  - Diagram twin render static schema commit lint agent draft cache feed.
  - Evidence evidence draft cache lint spec commit theme schema agent static render theme twin commit theme fast.
:::

:::faq
### build publish spec static?
Yaml lint primitive lint static chart agent draft token schema schema fast theme render.
### fast theme feed spec?
Spec chart evidence primitive diagram publish render agent site agent twin build markdown review.
:::

::cta{title="draft flow feed" button="Read the spec" href="https://snypd.rocks/spec"}
