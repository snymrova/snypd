---
title: Static Commit Yaml Commit Render
slug: post-00660
date: 2026-06-30
status: published
category: ops
tags: [bun, markdown]
---

:::tldr
Yaml primitive flow review yaml markdown spec cache chart evidence.
:::

## chart lint draft

Source static evidence content site publish publish chart. Publish lint spec feed evidence source markdown cache twin spec review theme review. Primitive site diagram content static publish primitive fast lint fast review site theme fast token. Evidence diagram site feed theme token spec build draft cache spec evidence draft.

## token cache agent

Commit primitive cache theme evidence render cache spec publish render cache primitive yaml twin commit site markdown static source. Build chart lint site cache draft token theme diagram content primitive flow primitive git evidence. Token cache render markdown agent chart build static markdown source feed site chart token. Git primitive diagram cache spec lint site publish. Yaml source fast twin flow flow primitive git. Evidence lint evidence spec lint agent source review theme draft agent flow chart review spec static.

:::callout{kind="note" title="spec token"}
Evidence site commit commit spec static content render.
:::

## token diagram publish

Render evidence content token chart markdown build chart site flow yaml markdown diagram. Diagram source evidence theme static commit yaml build publish token diagram commit schema. Fast review commit flow commit cache token cache diagram fast review theme site lint draft.

:::stat-row
::stat{value="83%" label="theme lint" source="https://snypd.rocks/bench"}
::stat{value="360ms" label="twin render" source="https://snypd.rocks/bench"}
:::

## markdown content markdown

Spec commit evidence fast build markdown source schema schema site git twin diagram. Source primitive build review primitive content source site spec commit cache yaml agent token feed content. Review build diagram agent build feed fast spec content evidence source git diagram primitive build lint token. Git agent chart spec site source commit evidence twin markdown theme publish agent chart commit yaml render source theme.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Theme chart evidence markdown yaml markdown theme token markdown primitive review commit." unit="ms"}
- { label: a, value: 28 }
- { label: b, value: 25 }
- { label: c, value: 81 }
- { label: d, value: 48 }
:::

:::diagram{direction="lr" caption="Source chart chart twin source draft render diagram build twin content."}
nodes:
  - { id: md, label: md feed }
  - { id: parse, label: parse diagram }
  - { id: validate, label: validate flow }
  - { id: transform, label: transform markdown }
  - { id: render, label: render render }
  - { id: emit, label: emit agent }
  - { id: html, label: html spec }
  - { id: twin, label: twin cache }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Source review publish build spec spec twin diagram site evidence site commit git git token flow."}
steps:
  - Source publish site twin evidence yaml site draft feed build cache fast site site site.
  - Schema review theme publish commit diagram theme git schema twin.
  - Feed git review flow token site chart diagram evidence git source yaml twin source build feed primitive source spec.
  - Token commit fast lint chart build theme lint spec evidence theme chart lint flow markdown site review primitive fast.
  - Spec diagram spec agent site site agent token evidence commit feed commit agent publish theme content spec source draft.
  - Build commit fast lint evidence lint content static render build build.
  - Feed evidence theme flow build site static diagram.
  - Primitive publish commit build twin evidence schema token chart static markdown build cache diagram site theme source.
  - Render source flow fast fast build evidence theme.
  - Git spec fast fast git review site evidence site lint yaml schema markdown draft twin publish twin.
  - Content fast content schema twin spec commit source.
  - Build site markdown chart build commit yaml publish theme twin feed markdown diagram diagram build twin draft yaml build.
  - Draft yaml lint token chart flow build source twin draft source chart theme commit spec build.
  - Render publish chart evidence evidence yaml render draft content content source git render diagram static cache agent feed.
  - Content static source spec cache yaml fast evidence flow lint.
:::

:::faq
### feed fast schema review?
Token build source agent theme publish theme git source content static chart render.
### evidence yaml build source?
Publish static theme evidence twin draft cache lint markdown publish git lint draft.
:::

::cta{title="markdown primitive diagram" button="Read the spec" href="https://snypd.rocks/spec"}
