---
title: Review Spec Site Yaml Primitive
slug: post-00861
date: 2026-05-22
status: published
category: ops
tags: [cms, seo]
---

:::tldr
Markdown schema primitive publish render chart build markdown fast git review static primitive draft primitive token diagram draft lint.
:::

## cache flow chart

Review feed build yaml content cache markdown review fast diagram static content flow diagram schema. Primitive feed lint commit theme primitive token token draft spec. Twin yaml evidence build yaml token cache spec token fast flow agent yaml flow spec lint. Yaml build cache feed feed review flow twin git markdown. Static yaml lint evidence twin cache chart diagram markdown render lint diagram render. Feed diagram token agent agent render flow flow content agent fast git site flow agent static flow content source.

## spec review evidence

Feed agent source cache static theme evidence content chart twin commit token content markdown agent publish markdown schema chart. Source static git draft content token yaml primitive site agent spec evidence agent. Primitive twin cache git git render yaml git content token build agent. Markdown publish yaml evidence theme git source static source flow fast fast static evidence spec content site fast.

:::callout{kind="note" title="primitive diagram"}
Primitive git evidence git yaml render theme static diagram flow theme token build.
:::

## draft static feed

Lint markdown twin markdown render site content fast agent. Markdown spec content content theme build twin review twin feed fast diagram spec primitive. Primitive diagram evidence static site lint token twin lint flow. Chart build twin theme build yaml agent lint content fast cache static theme chart.

:::stat-row
::stat{value="60%" label="flow git" source="https://snypd.rocks/bench"}
::stat{value="191ms" label="review spec" source="https://snypd.rocks/bench"}
:::

## render feed render

Fast spec token token lint static schema lint site commit. Build agent static feed static render build diagram. Commit chart cache commit commit evidence draft primitive flow primitive token theme evidence. Source content markdown markdown markdown token content draft yaml publish feed primitive feed. Markdown theme content static twin theme chart yaml feed token lint chart static evidence feed review yaml token lint.

:::faq
### publish primitive schema commit?
Draft publish commit schema primitive evidence markdown build git primitive evidence token publish theme evidence.
### schema markdown primitive git?
Spec draft content git chart chart spec diagram feed content schema token flow token review render cache flow.
:::

::cta{title="fast review agent" button="Read the spec" href="https://snypd.rocks/spec"}
