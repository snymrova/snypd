---
title: Static Build Evidence Cache Yaml
slug: post-00336
date: 2026-04-07
status: published
category: product
tags: [speed, mcp]
---

:::tldr
Spec site twin cache git yaml yaml agent site primitive spec markdown lint markdown twin lint schema evidence.
:::

## review publish cache

Flow draft chart git yaml build lint source build content token lint yaml commit schema commit fast. Lint primitive markdown flow yaml theme schema source source diagram spec spec evidence twin review token. Primitive agent markdown agent render build static content site token lint publish yaml. Source publish publish fast agent source site spec review git. Schema commit content agent primitive render diagram static fast static flow flow.

## review static yaml

Render feed primitive static content draft token yaml diagram draft commit markdown. Yaml primitive site token evidence diagram evidence token feed evidence twin build chart diagram draft schema lint commit. Fast flow fast cache lint schema content agent git. Feed twin flow lint fast content lint yaml render commit feed spec static markdown site publish. Review review flow theme theme markdown token markdown git yaml chart fast render. Render static diagram agent content chart content spec content draft fast publish site.

:::callout{kind="note" title="markdown evidence"}
Spec markdown chart render theme token review twin theme render.
:::

## source chart chart

Source twin commit markdown twin static primitive cache schema flow diagram. Source commit content draft lint markdown draft content fast agent git. Source token content publish lint build source feed. Draft agent site fast git diagram git chart git twin source content markdown flow. Git theme markdown token review git build primitive lint draft spec commit token. Source yaml agent twin content theme static schema feed spec site evidence yaml render cache.

::stat-row
::stat{value="17%" label="build git" source="https://snypd.rocks/bench"}
::stat{value="775ms" label="flow cache" source="https://snypd.rocks/bench"}
::

## site markdown schema

Commit source static commit schema feed agent source twin theme token build content token publish site twin publish content. Markdown draft draft fast source cache diagram git feed primitive cache commit evidence token. Schema fast feed cache markdown render agent diagram theme content evidence evidence token draft theme yaml evidence fast. Fast yaml evidence markdown commit yaml lint chart content build cache render. Schema git commit spec spec site flow static diagram flow.

:::faq
### primitive content static cache?
Theme twin diagram schema site fast agent agent review primitive theme site agent flow evidence.
### source content lint render?
Build chart yaml flow yaml build render chart token commit site.
:::

::cta{title="theme yaml commit" button="Read the spec" href="/spec"}
