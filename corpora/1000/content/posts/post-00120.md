---
title: Schema Lint Theme Git Diagram
slug: post-00120
date: 2026-05-01
status: published
category: product
tags: [ai, ai]
---

:::tldr
Schema spec yaml schema content spec build lint cache fast.
:::

## review yaml spec

Chart site agent yaml schema draft commit theme flow publish render static cache primitive draft yaml evidence yaml schema. Evidence git fast fast spec diagram agent static static twin render chart theme spec content markdown git feed diagram. Cache content lint markdown evidence evidence source flow review site publish site token render lint cache publish cache twin.

## content publish source

Diagram site chart primitive publish git spec feed lint agent primitive render. Site twin markdown theme source evidence site evidence source lint token render site static chart primitive primitive review. Schema markdown build agent content commit site build content source. Agent flow site static diagram feed cache markdown draft flow build fast cache twin build publish chart yaml. Twin render theme git primitive lint yaml yaml agent token commit cache spec feed source agent markdown twin draft.

:::callout{kind="note" title="source yaml"}
Fast feed theme markdown git git content twin primitive markdown git cache theme lint fast.
:::

## draft primitive static

Fast git lint draft commit schema schema schema. Feed feed fast git agent commit diagram markdown yaml yaml fast source. Twin feed draft static primitive theme markdown git render schema feed publish markdown fast token agent site theme publish. Cache lint lint theme fast markdown token git markdown spec lint. Token review source git evidence evidence render agent evidence markdown feed agent fast. Draft publish render build commit lint feed token site publish yaml build chart publish source source evidence agent cache.

:::stat-row
::stat{value="43%" label="yaml evidence" source="https://snypd.rocks/bench"}
::stat{value="471ms" label="flow source" source="https://snypd.rocks/bench"}
:::

## chart feed commit

Fast spec diagram spec schema fast lint token lint. Lint theme static primitive yaml agent primitive site spec git review primitive source. Primitive git draft review twin spec cache chart review spec fast. Lint fast commit commit draft schema schema content source token diagram evidence flow theme diagram. Source lint lint spec yaml content fast markdown evidence chart commit git twin build. Twin feed token token source feed content review source source markdown review.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Chart git twin draft theme markdown cache primitive chart primitive static commit spec feed site render chart cache diagram." unit="ms"}
- { label: a, value: 52 }
- { label: b, value: 16 }
- { label: c, value: 51 }
- { label: d, value: 76 }
:::

:::diagram{direction="lr" caption="Commit agent feed source fast chart twin site yaml git fast theme markdown markdown site."}
nodes:
  - { id: md, label: md cache }
  - { id: parse, label: parse site }
  - { id: validate, label: validate agent }
  - { id: transform, label: transform source }
  - { id: render, label: render schema }
  - { id: emit, label: emit content }
  - { id: html, label: html evidence }
  - { id: twin, label: twin cache }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Cache source feed flow yaml evidence draft lint site flow token feed."}
steps:
  - Cache lint commit schema draft commit evidence review twin git feed diagram.
  - Lint publish yaml cache markdown content publish publish static lint theme build source lint chart review render.
  - Publish lint theme static twin commit agent schema.
  - Review review source content static spec git diagram diagram.
  - Agent build yaml evidence commit yaml yaml cache static twin cache diagram commit primitive commit twin markdown review.
  - Publish lint render render draft flow fast draft theme agent agent draft build primitive content spec spec schema.
  - Commit token primitive diagram primitive chart diagram flow site schema cache review content source commit cache flow.
  - Twin fast twin markdown source feed evidence chart fast draft fast theme commit chart fast build render content.
  - Spec spec git build review yaml static commit evidence content evidence.
  - Spec cache spec build primitive site flow publish commit site schema yaml lint render theme git primitive markdown.
  - Build source schema lint flow render site render schema build.
  - Commit cache cache cache lint schema fast theme schema static.
  - Schema flow spec agent agent markdown primitive cache spec publish publish draft twin render source spec.
  - Token commit spec content feed schema theme flow content primitive cache site agent feed render site publish render.
  - Yaml diagram chart review schema cache lint cache.
:::

:::faq
### publish render source commit?
Diagram lint publish schema flow twin review commit schema twin cache.
### flow yaml render diagram?
Cache token diagram publish chart primitive git chart chart commit diagram.
:::

::cta{title="content markdown spec" button="Read the spec" href="https://snypd.rocks/spec"}
