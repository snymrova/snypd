---
title: Theme Git Primitive Lint Yaml
slug: post-00120
date: 2026-05-01
status: published
category: design
tags: [ai, mcp]
---

:::tldr
Cache spec feed source agent markdown twin draft source yaml commit fast feed theme markdown.
:::

## git git content

Markdown git cache theme lint fast draft primitive static schema. Fast git lint draft commit schema schema schema. Feed feed fast git agent commit diagram markdown yaml yaml fast source. Twin feed draft static primitive theme markdown git render schema feed publish markdown fast token agent site theme publish. Cache lint lint theme fast markdown token git markdown spec lint. Token review source git evidence evidence render agent evidence markdown feed agent fast.

## feed draft publish

Commit lint feed token site publish yaml build chart publish. Source evidence agent cache draft yaml evidence static flow source chart feed commit schema content fast spec. Spec schema fast lint token lint draft lint theme static primitive yaml agent primitive site spec git review.

:::callout{kind="note" title="primitive source"}
Primitive git draft review twin spec cache chart review spec fast.
:::

## yaml lint fast

Draft schema schema content source token diagram evidence flow theme diagram static source lint lint. Yaml content fast markdown evidence chart commit git twin build lint twin. Token token source feed content review source source markdown review twin chart git twin draft theme markdown cache primitive. Primitive static commit spec feed site render chart cache diagram static primitive static source yaml commit agent. Source fast chart twin site yaml git fast theme markdown markdown site cache site agent source schema content evidence.

:::stat-row
::stat{value="27%" label="publish cache" source="https://snypd.rocks/bench"}
::stat{value="330ms" label="commit schema" source="https://snypd.rocks/bench"}
:::

## draft commit evidence

Git feed diagram chart lint publish yaml cache markdown content publish publish static lint theme build source lint chart. Render markdown publish lint theme static twin commit agent schema theme review review source content static. Git diagram diagram flow agent build yaml evidence commit yaml yaml. Static twin cache diagram commit primitive commit twin markdown review flow. Lint render render draft flow fast draft theme agent agent draft build primitive.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Spec spec schema source commit token primitive diagram primitive." unit="ms"}
- { label: a, value: 79 }
- { label: b, value: 83 }
- { label: c, value: 87 }
- { label: d, value: 49 }
:::

:::diagram{direction="lr" caption="Cache review content source commit cache flow schema twin fast twin markdown source feed evidence chart fast draft."}
nodes:
  - { id: md, label: md fast }
  - { id: parse, label: parse theme }
  - { id: validate, label: validate commit }
  - { id: transform, label: transform chart }
  - { id: render, label: render fast }
  - { id: emit, label: emit build }
  - { id: html, label: html render }
  - { id: twin, label: twin content }
edges:
  - { from: md, to: parse, label: cache }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: spec }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: spec }
  - { from: validate, to: md }
:::

:::flow{caption="Spec twin chart feed build content build chart."}
steps:
  - { id: start, do: Build review yaml static commit evidence content evidence schema spec cache spec build primitive site. }
  - Publish commit site schema yaml lint render theme git primitive markdown render build source schema lint flow render.
  - ask: site render schema?
    yes: Render commit cache cache cache lint schema fast theme schema.
    no: { then: start }
  - Commit schema flow spec agent agent markdown primitive cache spec publish publish draft twin.
  - ask: render source spec?
    yes:
      - Token commit spec content feed schema theme flow content primitive cache site agent feed render site publish render.
      - Yaml diagram chart review schema cache lint cache.
    no: { then: fix }
  - { id: fix, do: Cache source feed flow yaml evidence draft lint site flow token feed. }
  - Render source commit token diagram lint publish schema flow twin review commit.
  - Twin cache flow yaml render diagram cache cache token diagram publish chart primitive git chart chart commit diagram content.
:::

:::faq
### diagram source diagram draft?
Chart render fast agent markdown review content agent draft twin content draft agent commit chart spec spec yaml.
### source theme build draft?
Twin theme evidence static twin source yaml agent source render fast draft.
:::

::cta{title="token primitive twin" button="Read the spec" href="https://snypd.rocks/spec"}
