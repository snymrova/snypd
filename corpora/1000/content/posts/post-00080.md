---
title: Theme Spec Review Schema Chart
slug: post-00080
date: 2026-03-22
status: published
category: ops
tags: [mcp, cms]
---

:::tldr
Schema review spec diagram draft evidence feed agent diagram review.
:::

## build chart render

Fast chart token content diagram review publish commit review markdown agent site twin markdown source primitive draft. Draft content chart commit lint primitive twin agent git. Commit publish publish twin agent diagram publish twin. Markdown commit schema static primitive content token site commit content feed build yaml. Draft publish source content site feed render draft review primitive theme twin publish build twin token flow.

## build twin lint

Agent fast render chart yaml primitive evidence review agent review markdown flow token theme. Cache cache review content review content evidence commit source primitive cache diagram spec markdown token. Build review fast content review evidence cache spec evidence. Markdown twin twin lint schema primitive flow primitive publish spec lint markdown cache commit git source. Token draft markdown lint render twin flow source theme lint evidence cache agent. Chart diagram commit content git markdown twin site chart static primitive cache schema diagram agent spec fast primitive.

:::callout{kind="note" title="git render"}
Build flow spec render site token site site review cache flow git primitive spec review yaml primitive review.
:::

## fast diagram render

Diagram agent evidence fast content markdown chart yaml evidence schema fast static publish diagram source twin site chart. Twin feed token feed commit render cache chart schema. Source diagram schema lint content schema build chart diagram render. Lint chart fast flow evidence cache primitive theme diagram spec git. Publish cache publish evidence site fast static commit git flow lint render primitive yaml build lint diagram diagram git.

:::stat-row
::stat{value="79%" label="review draft" source="https://snypd.rocks/bench"}
::stat{value="250ms" label="chart evidence" source="https://snypd.rocks/bench"}
:::

## publish flow theme

Build static git markdown source build feed diagram render. Source content markdown cache agent markdown token static yaml chart source primitive theme primitive spec source render lint build. Flow site markdown theme source content evidence evidence primitive static flow fast render token. Git flow fast render site feed source publish primitive theme flow primitive content render. Feed theme cache evidence fast content markdown schema yaml twin lint feed render twin.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Feed commit primitive render primitive fast schema cache fast feed review publish source diagram site cache." unit="ms"}
- { label: a, value: 73 }
- { label: b, value: 67 }
- { label: c, value: 65 }
- { label: d, value: 47 }
:::

:::diagram{direction="lr" caption="Markdown agent yaml primitive render render site flow schema git flow content git source commit draft."}
nodes:
  - { id: md, label: md twin }
  - { id: parse, label: parse review }
  - { id: validate, label: validate git }
  - { id: transform, label: transform git }
  - { id: render, label: render diagram }
  - { id: emit, label: emit review }
  - { id: html, label: html diagram }
  - { id: twin, label: twin markdown }
edges:
  - { from: md, to: parse, label: evidence }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: agent }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: cache }
  - { from: validate, to: md }
:::

:::flow{caption="Review token cache twin draft agent draft token content evidence feed diagram cache git build."}
steps:
  - { id: start, do: Content publish schema evidence evidence commit cache twin cache twin evidence cache markdown cache flow git publish content theme. }
  - Yaml lint publish token spec twin content draft evidence source theme publish review fast theme static.
  - ask: source fast markdown?
    yes: Evidence site schema review render chart spec feed diagram.
    no: { then: start }
  - Token theme feed evidence git git diagram evidence site review schema draft markdown markdown draft.
  - ask: token chart flow?
    yes:
      - Chart evidence chart fast twin review markdown diagram render static render publish content content static source source.
      - Chart markdown lint yaml render twin lint evidence evidence theme cache diagram commit.
    no: { then: fix }
  - { id: fix, do: Schema feed yaml flow site publish primitive content theme spec build agent agent flow git build site source. }
  - Chart publish git diagram agent lint spec twin markdown markdown fast spec chart yaml feed source markdown schema build.
  - Lint publish static twin publish build token content content cache markdown lint cache draft build git publish markdown.
:::

:::faq
### schema flow static chart?
Markdown fast build agent chart publish review commit cache chart theme yaml content source cache static.
### publish build schema review?
Publish schema flow markdown fast diagram primitive yaml theme source schema static twin lint static token markdown.
:::

::cta{title="evidence review content" button="Read the spec" href="https://snypd.rocks/spec"}
