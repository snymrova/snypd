---
title: Evidence Evidence Primitive Static Flow
slug: post-00080
date: 2026-03-22
status: published
category: design
tags: [agents, mcp]
---

:::tldr
Git flow fast render site feed source publish primitive theme flow primitive content render.
:::

## fast feed theme

Fast content markdown schema yaml twin lint feed render twin evidence feed commit primitive render primitive. Schema cache fast feed review publish source diagram site cache evidence review commit site. Markdown agent yaml primitive render render site flow schema git flow content git source commit draft. Review git git diagram review diagram markdown evidence agent cache twin content publish schema evidence evidence commit cache twin.

## cache twin evidence

Cache flow git publish content theme commit yaml. Publish token spec twin content draft evidence source theme publish review fast. Static source fast markdown theme evidence site schema review. Chart spec feed diagram yaml token theme feed evidence git.

:::callout{kind="note" title="git diagram"}
Site review schema draft markdown markdown draft token chart flow diagram chart evidence chart fast twin.
:::

## review markdown diagram

Render publish content content static source source draft chart markdown lint yaml render twin. Evidence evidence theme cache diagram commit flow schema feed yaml flow site. Primitive content theme spec build agent agent flow git build site source.

:::stat-row
::stat{value="95%" label="chart publish" source="https://snypd.rocks/bench"}
::stat{value="576ms" label="diagram agent" source="https://snypd.rocks/bench"}
:::

## lint spec twin

Fast spec chart yaml feed source markdown schema. Flow lint publish static twin publish build token content content. Markdown lint cache draft build git publish markdown git review token.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Twin draft agent draft token content evidence feed diagram cache git." unit="ms"}
- { label: a, value: 21 }
- { label: b, value: 91 }
- { label: c, value: 87 }
- { label: d, value: 50 }
:::

:::diagram{direction="lr" caption="Evidence markdown fast build agent chart publish review commit cache chart theme yaml content source cache static."}
nodes:
  - { id: md, label: md publish }
  - { id: parse, label: parse build }
  - { id: validate, label: validate schema }
  - { id: transform, label: transform review }
  - { id: render, label: render diagram }
  - { id: emit, label: emit publish }
  - { id: html, label: html schema }
  - { id: twin, label: twin flow }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Content agent evidence git cache static source cache agent feed static flow static spec static."}
steps:
  - Fast diagram primitive yaml theme source schema static.
  - Lint static token markdown evidence review content content agent content cache yaml markdown spec agent site yaml twin primitive.
  - Commit markdown flow agent render review lint primitive agent yaml twin.
  - Source twin source source diagram fast source fast git evidence source lint twin cache.
  - Lint lint markdown cache schema token yaml diagram.
  - Content source lint lint fast source static content theme git chart static spec theme.
  - Render fast build token git draft render evidence commit chart.
  - Token flow source twin primitive yaml git build chart static.
  - Site fast schema git diagram agent primitive publish content git agent theme evidence.
  - Git static chart feed draft spec source lint flow spec diagram twin render.
  - Evidence theme render diagram theme feed cache evidence lint markdown site commit site lint token build fast.
  - Yaml primitive evidence cache chart markdown twin evidence commit yaml git static feed static.
  - Flow git spec theme schema fast schema source markdown.
  - Token static evidence review feed diagram commit agent static markdown git evidence static spec.
  - Token lint diagram agent commit fast git chart draft evidence yaml content render site.
:::

:::faq
### evidence content git diagram?
Fast agent markdown git render feed content content flow yaml spec spec commit diagram.
### commit primitive flow feed?
Source lint markdown source flow fast feed static site render content chart twin flow schema publish.
:::

::cta{title="token static static" button="Read the spec" href="https://snypd.rocks/spec"}
