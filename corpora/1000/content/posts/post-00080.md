---
title: Twin Review Git Git Diagram
slug: post-00080
date: 2026-03-22
status: published
category: design
tags: [speed, ai]
---

:::tldr
Agent cache twin content publish schema evidence evidence commit cache twin cache twin evidence cache markdown.
:::

## cache flow git

Theme commit yaml lint publish token spec twin. Draft evidence source theme publish review fast theme. Source fast markdown theme evidence site schema review render chart spec feed diagram yaml. Theme feed evidence git git diagram evidence site review schema draft.

## markdown markdown draft

Flow diagram chart evidence chart fast twin review markdown diagram render static render publish content content static. Source draft chart markdown lint yaml render twin lint evidence evidence theme cache diagram commit flow schema. Yaml flow site publish primitive content theme spec build agent agent flow git build site source feed chart publish. Diagram agent lint spec twin markdown markdown fast spec chart yaml feed source markdown schema.

:::callout{kind="note" title="build flow"}
Publish static twin publish build token content content cache markdown lint cache.
:::

## draft build git

Git review token cache twin draft agent draft. Content evidence feed diagram cache git build schema flow static chart. Markdown fast build agent chart publish review commit cache chart theme yaml content source cache static. Build schema review diagram publish schema flow markdown fast diagram primitive yaml.

:::stat-row
::stat{value="11%" label="source schema" source="https://snypd.rocks/bench"}
::stat{value="469ms" label="twin lint" source="https://snypd.rocks/bench"}
:::

## static token markdown

Content content agent content cache yaml markdown spec agent site yaml twin primitive cache commit markdown. Agent render review lint primitive agent yaml twin static source twin source source diagram fast source fast git. Source lint twin cache agent lint lint markdown cache schema token yaml diagram static content source. Lint fast source static content theme git chart static spec theme render. Fast build token git draft render evidence commit chart primitive.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Flow source twin primitive yaml git build chart static draft site." unit="ms"}
- { label: a, value: 56 }
- { label: b, value: 92 }
- { label: c, value: 62 }
- { label: d, value: 82 }
:::

:::diagram{direction="lr" caption="Primitive publish content git agent theme evidence site."}
nodes:
  - { id: md, label: md git }
  - { id: parse, label: parse static }
  - { id: validate, label: validate chart }
  - { id: transform, label: transform feed }
  - { id: render, label: render draft }
  - { id: emit, label: emit spec }
  - { id: html, label: html source }
  - { id: twin, label: twin lint }
edges:
  - { from: md, to: parse, label: flow }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: spec }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: diagram }
  - { from: validate, to: md }
:::

:::flow{caption="Primitive token diagram publish review twin render content source schema site flow lint commit yaml."}
steps:
  - Render chart evidence theme render diagram theme feed cache evidence lint markdown site commit site lint token build fast.
  - Yaml primitive evidence cache chart markdown twin evidence commit yaml git static feed static.
  - Flow git spec theme schema fast schema source markdown.
  - Token static evidence review feed diagram commit agent static markdown git evidence static spec.
  - Token lint diagram agent commit fast git chart draft evidence yaml content render site.
  - Content agent evidence git cache static source cache agent feed static flow static spec static.
  - Content git diagram static fast agent markdown git render feed content content flow yaml spec spec.
  - Diagram commit primitive flow feed review source lint markdown source flow fast feed static site.
  - Content chart twin flow schema publish token static static primitive.
  - Build source primitive token markdown site feed commit primitive content lint theme lint draft flow diagram token.
  - Diagram primitive markdown lint diagram spec flow build evidence schema twin token commit.
  - Publish agent feed publish source draft site primitive draft content fast flow.
  - Content build publish static content schema schema git cache cache.
  - Render build spec primitive publish primitive chart git lint publish theme yaml markdown draft content schema yaml.
  - Markdown token theme build twin agent render spec fast cache evidence.
:::

:::faq
### primitive content primitive flow?
Draft lint schema flow content chart diagram source publish flow twin theme twin feed.
### content commit cache diagram?
Markdown render spec build site agent commit commit publish content lint feed flow build.
:::

::cta{title="git diagram draft" button="Read the spec" href="https://snypd.rocks/spec"}
