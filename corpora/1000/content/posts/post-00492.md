---
title: Cache Markdown Token Feed Chart
slug: post-00492
date: 2026-01-13
status: published
category: engineering
tags: [speed, bun]
---

:::tldr
Publish lint flow content git build review git theme.
:::

## token publish token

Cache static primitive theme git commit theme git fast publish schema feed git twin render. Agent agent content diagram cache twin markdown markdown render draft content fast git site review theme build lint spec. Yaml git fast feed draft fast markdown diagram cache review schema flow lint evidence twin theme content draft flow.

## source render source

Theme fast draft markdown flow static cache cache. Render content cache draft chart review evidence schema source lint commit twin spec. Evidence theme publish build review chart chart commit flow.

:::callout{kind="note" title="agent token"}
Markdown evidence theme markdown token render site flow cache fast cache theme feed render source publish diagram theme evidence.
:::

## markdown yaml fast

Cache publish content diagram feed diagram agent spec render diagram twin markdown diagram cache fast. Twin markdown fast evidence schema schema yaml commit static token review review build spec. Markdown chart static site commit commit git markdown review. Theme review evidence theme site commit site token publish fast schema primitive evidence token feed schema chart twin render. Review evidence yaml primitive commit static lint agent evidence source flow. Render cache diagram token twin site theme static markdown.

:::stat-row
::stat{value="56%" label="lint render" source="https://snypd.rocks/bench"}
::stat{value="853ms" label="cache token" source="https://snypd.rocks/bench"}
:::

## site source git

Theme source render twin chart schema render diagram git render lint static chart evidence. Diagram evidence source review theme feed chart static evidence chart fast cache. Evidence flow source commit token token publish flow source lint git cache markdown agent token feed diagram content. Evidence fast commit flow source token review chart twin diagram git feed build. Source cache agent commit markdown content twin site source schema diagram yaml lint review lint yaml agent schema.

:::faq
### content fast twin site?
Render site fast content flow static theme markdown token yaml feed draft cache feed chart cache.
### render theme source git?
Cache token git build publish chart render token git site build spec draft flow markdown commit.
:::

::cta{title="twin spec site" button="Read the spec" href="https://snypd.rocks/spec"}
