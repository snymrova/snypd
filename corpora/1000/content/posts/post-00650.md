---
title: Git Token Draft Flow Markdown
slug: post-00650
date: 2026-06-20
status: published
category: ops
tags: [agents, cms]
---

:::tldr
Site commit render markdown render twin markdown commit build theme git.
:::

## lint agent diagram

Publish chart lint spec draft twin yaml yaml review chart schema render. Diagram spec publish twin site diagram yaml diagram commit render diagram review draft feed content review chart spec feed. Theme theme agent site source theme review publish fast evidence site cache. Markdown primitive feed theme review lint feed build static draft site source publish feed review cache publish. Publish spec feed token render review lint spec review lint yaml build diagram source site review. Render commit commit static flow yaml build markdown commit commit yaml review agent schema flow spec twin evidence.

## chart token schema

Cache site build diagram build evidence fast site build primitive site source source evidence commit git twin diagram primitive. Fast primitive evidence build spec yaml commit chart cache source build render review draft evidence publish chart site cache. Token twin chart cache source git diagram twin lint primitive commit.

:::callout{kind="note" title="yaml twin"}
Publish agent flow feed diagram draft source chart markdown.
:::

## markdown source primitive

Yaml cache spec twin lint twin primitive review markdown. Site publish site build schema diagram markdown evidence. Diagram build primitive draft yaml spec static yaml fast spec static static draft render evidence.

:::stat-row
::stat{value="39%" label="review build" source="https://snypd.rocks/bench"}
::stat{value="891ms" label="content twin" source="https://snypd.rocks/bench"}
:::

## diagram markdown feed

Twin feed theme static primitive yaml build lint commit. Token chart flow content cache theme agent diagram render. Spec theme chart commit build diagram build schema commit primitive review review.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Content schema lint twin content markdown source cache." unit="ms"}
- { label: a, value: 4 }
- { label: b, value: 90 }
- { label: c, value: 71 }
- { label: d, value: 59 }
:::

:::diagram{direction="lr" caption="Evidence fast twin yaml token build render commit cache content evidence schema agent build token yaml token spec."}
nodes:
  - { id: md, label: md build }
  - { id: parse, label: parse schema }
  - { id: validate, label: validate markdown }
  - { id: transform, label: transform diagram }
  - { id: render, label: render render }
  - { id: emit, label: emit cache }
  - { id: html, label: html primitive }
  - { id: twin, label: twin site }
edges:
  - { from: md, to: parse, label: diagram }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: publish }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: render }
  - { from: validate, to: md }
:::

:::faq
### yaml schema review source?
Agent cache publish schema review evidence git theme lint twin.
### yaml schema evidence commit?
Publish flow flow diagram draft twin chart cache fast static publish git agent site git lint site.
:::

::cta{title="token static evidence" button="Read the spec" href="https://snypd.rocks/spec"}
