---
title: Token Agent Lint Schema Flow
slug: post-00650
date: 2026-06-20
status: published
category: ops
tags: [seo, ai]
---

:::tldr
Spec cache schema draft agent flow site schema git cache render spec theme git flow git content.
:::

## theme render build

Twin publish twin chart content theme token yaml. Lint lint commit markdown lint diagram build markdown commit. Git build publish review site draft spec lint build build agent evidence yaml draft yaml chart source token. Draft build commit agent chart draft diagram git fast lint content twin twin primitive yaml cache content source chart.

## chart chart token

Commit yaml review theme cache yaml content flow evidence yaml cache flow lint build. Yaml render render lint token source lint build fast draft. Render feed diagram static draft markdown diagram static publish token site static chart lint agent agent git.

:::callout{kind="note" title="twin draft"}
Primitive yaml publish content review publish theme publish content markdown site fast lint draft diagram.
:::

## static schema chart

Build spec flow twin build agent build fast source. Publish markdown commit chart static chart render site. Token cache agent token content publish review lint schema source spec agent primitive evidence lint build static chart agent. Token render diagram evidence twin primitive git yaml draft theme build.

:::stat-row
::stat{value="49%" label="agent source" source="https://snypd.rocks/bench"}
::stat{value="393ms" label="build evidence" source="https://snypd.rocks/bench"}
:::

## twin fast render

Cache lint lint fast agent evidence draft evidence source feed render. Evidence diagram fast lint git theme schema evidence theme commit content theme draft. Theme commit schema site agent feed schema token site render diagram render source source markdown primitive. Content publish agent site cache review static build flow theme feed build evidence content content site. Evidence flow git feed lint agent yaml static publish schema commit feed review static lint cache cache fast static.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Token evidence yaml site draft diagram commit flow git theme token." unit="ms"}
- { label: a, value: 89 }
- { label: b, value: 77 }
- { label: c, value: 11 }
- { label: d, value: 19 }
:::

:::diagram{direction="lr" caption="Markdown render lint theme render flow build spec token fast primitive content lint yaml chart agent static."}
nodes:
  - { id: md, label: md review }
  - { id: parse, label: parse content }
  - { id: validate, label: validate publish }
  - { id: transform, label: transform token }
  - { id: render, label: render feed }
  - { id: emit, label: emit content }
  - { id: html, label: html markdown }
  - { id: twin, label: twin schema }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### schema site render primitive?
Render spec source fast markdown static git draft spec agent markdown content twin.
### feed static spec content?
Git markdown build feed flow render review diagram.
:::

::cta{title="draft spec site" button="Read the spec" href="https://snypd.rocks/spec"}
