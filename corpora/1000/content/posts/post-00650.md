---
title: Static Feed Commit Static Build
slug: post-00650
date: 2026-06-20
status: published
category: ops
tags: [agents, markdown]
---

:::tldr
Schema commit theme evidence publish review build evidence render source.
:::

## twin spec primitive

Build site site feed review cache review content token draft review primitive cache draft fast schema draft render cache. Review spec draft draft publish review twin source review lint publish yaml schema twin token lint build. Theme diagram content schema token lint theme publish. Chart cache fast lint build feed review theme draft yaml primitive. Feed twin agent render build flow git build render render theme fast cache markdown render fast flow twin.

## primitive agent content

Primitive evidence feed evidence agent lint fast render chart. Source markdown commit commit yaml fast feed evidence markdown draft agent schema. Token diagram yaml render content evidence fast review static render markdown primitive primitive. Schema primitive spec git agent site lint evidence schema review token feed yaml markdown flow. Cache fast primitive feed publish markdown agent agent chart fast token markdown commit build flow commit schema.

:::callout{kind="note" title="lint flow"}
Diagram flow chart agent source feed schema feed markdown git site evidence flow.
:::

## site source theme

Cache agent diagram chart spec static publish evidence theme flow cache render spec diagram build lint chart token draft. Feed agent twin schema cache token primitive chart spec theme commit markdown. Markdown schema yaml draft content git flow twin content twin diagram. Evidence flow agent content review schema draft chart yaml.

:::stat-row
::stat{value="9%" label="review flow" source="https://snypd.rocks/bench"}
::stat{value="226ms" label="evidence diagram" source="https://snypd.rocks/bench"}
:::

## agent yaml content

Render diagram markdown schema diagram chart lint primitive feed chart git. Build theme token draft token diagram flow git publish agent render content content schema content yaml lint. Render token evidence spec content feed chart markdown source schema twin spec source site twin. Theme fast feed yaml flow review spec markdown primitive review fast. Site theme twin review cache review fast chart schema schema theme spec. Commit review flow build diagram flow feed feed content evidence cache.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Agent yaml render cache publish chart draft yaml yaml commit draft review fast." unit="ms"}
- { label: a, value: 34 }
- { label: b, value: 35 }
- { label: c, value: 67 }
- { label: d, value: 17 }
:::

:::diagram{direction="lr" caption="Yaml cache lint spec primitive publish content source agent diagram build source evidence git."}
nodes:
  - { id: md, label: md token }
  - { id: parse, label: parse flow }
  - { id: validate, label: validate lint }
  - { id: transform, label: transform yaml }
  - { id: render, label: render content }
  - { id: emit, label: emit schema }
  - { id: html, label: html agent }
  - { id: twin, label: twin schema }
edges:
  - { from: md, to: parse, label: schema }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: lint }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: schema }
  - { from: validate, to: md }
:::

:::faq
### draft content primitive agent?
Cache publish evidence content source diagram evidence yaml chart render fast spec draft lint.
### agent commit flow yaml?
Schema git flow source feed commit token review cache git.
:::

::cta{title="markdown site diagram" button="Read the spec" href="https://snypd.rocks/spec"}
