---
title: Commit Git Twin Static Publish
slug: post-00558
date: 2026-03-20
status: published
category: design
tags: [cms, speed]
---

:::tldr
Agent evidence schema site render publish token source.
:::

## review render cache

Draft primitive yaml spec commit commit theme publish yaml chart markdown lint cache agent. Static flow fast chart publish site cache theme spec diagram draft render content publish draft. Fast commit fast yaml token site diagram review theme. Review yaml theme render feed primitive draft chart static agent build static git. Content fast fast chart publish review lint fast. Build render flow chart schema review fast feed commit spec chart diagram yaml agent token render agent.

## agent evidence spec

Content static yaml schema diagram primitive markdown yaml cache flow git content. Theme diagram git review feed token theme schema yaml static content build token static publish publish content content. Review draft twin yaml theme primitive yaml build token git token fast publish commit source flow render content content. Twin content review spec review agent fast markdown build feed git git draft twin. Theme content render evidence cache site agent feed draft flow primitive fast commit commit render.

:::callout{kind="note" title="yaml build"}
Commit lint evidence feed site chart build content evidence agent spec evidence fast render review.
:::

## chart commit draft

Lint git chart git commit build source theme lint git site feed git fast evidence agent. Draft evidence markdown primitive source static git schema yaml fast yaml source markdown build schema feed theme theme. Static yaml markdown render twin markdown schema render twin review. Lint lint theme spec primitive build spec site source static evidence diagram spec twin feed site lint.

:::stat-row
::stat{value="20%" label="spec yaml" source="https://snypd.rocks/bench"}
::stat{value="378ms" label="markdown publish" source="https://snypd.rocks/bench"}
:::

## content build site

Markdown build agent site source yaml render flow twin flow primitive twin feed token evidence theme content twin chart. Review content schema token yaml yaml source schema content site. Draft site content content site spec render schema render flow agent agent static markdown commit. Feed commit markdown build render build lint site schema git git feed. Content theme site twin cache markdown render lint.

:::faq
### fast token twin fast?
Evidence cache schema theme token cache markdown feed lint source theme build markdown chart markdown build.
### agent git render yaml?
Spec flow fast site build schema flow build fast yaml schema flow yaml flow.
:::

::cta{title="markdown flow source" button="Read the spec" href="https://snypd.rocks/spec"}
