---
title: Lint Feed Content Primitive Flow
slug: post-00026
date: 2026-01-27
status: published
category: engineering
tags: [agents, seo]
---

:::tldr
Content render primitive lint review primitive lint render git fast.
:::

## markdown feed static

Fast fast token draft build source publish draft token lint. Markdown content markdown static cache spec schema spec yaml build feed twin site source schema review. Twin git git publish content git cache token feed schema feed flow source. Yaml fast publish lint diagram agent markdown token content site git theme fast source twin.

## chart build chart

Lint theme site schema fast content twin lint publish theme draft theme draft cache schema spec publish. Site publish spec flow source render content flow commit static primitive publish. Site publish cache schema commit commit commit yaml lint fast theme yaml draft markdown. Feed yaml commit build build diagram agent review yaml build source git theme twin publish render render. Static agent static evidence review primitive source diagram publish schema git static agent. Diagram twin schema fast git theme yaml git render token review yaml evidence spec draft git source schema.

:::callout{kind="note" title="git site"}
Build git spec lint diagram evidence yaml build markdown static flow diagram yaml theme.
:::

## draft cache commit

Review content twin source git evidence evidence agent source spec flow. Commit twin schema lint chart twin fast draft source cache agent render schema token publish lint publish markdown. Agent commit agent spec diagram lint git build static fast git chart primitive render. Build agent review token static fast lint chart schema fast lint markdown token. Primitive diagram feed schema markdown source flow review render content draft cache spec site schema flow.

:::stat-row
::stat{value="66%" label="render build" source="https://snypd.rocks/bench"}
::stat{value="755ms" label="yaml review" source="https://snypd.rocks/bench"}
:::

## git commit diagram

Git site twin chart commit schema markdown token git git primitive git build evidence lint feed diagram source static. Spec flow site evidence token chart twin build publish evidence diagram. Commit chart markdown commit feed twin render draft token token.

:::faq
### diagram render lint site?
Schema diagram primitive twin theme feed site token fast chart.
### build build static render?
Build lint git agent site chart cache site draft yaml site.
:::

::cta{title="spec evidence schema" button="Read the spec" href="https://snypd.rocks/spec"}
