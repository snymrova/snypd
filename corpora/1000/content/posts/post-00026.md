---
title: Static Site Render Fast Fast
slug: post-00026
date: 2026-01-27
status: published
category: product
tags: [bun, agents]
---

:::tldr
Publish draft token lint commit markdown content markdown static cache spec schema spec yaml build feed twin.
:::

## site source schema

Twin git git publish content git cache token feed schema feed flow source. Yaml fast publish lint diagram agent markdown token content site git theme fast source twin. Build chart schema source lint theme site schema fast content twin lint publish theme draft theme draft. Schema spec publish spec site publish spec flow source render content. Commit static primitive publish static site publish cache schema commit commit commit yaml lint fast theme yaml draft.

## markdown chart feed

Build build diagram agent review yaml build source git theme twin publish render render draft static. Static evidence review primitive source diagram publish schema. Static agent flow diagram twin schema fast git theme yaml git render token review yaml. Spec draft git source schema git site static build git spec lint diagram evidence yaml build. Static flow diagram yaml theme draft cache commit.

:::callout{kind="note" title="static spec"}
Content twin source git evidence evidence agent source spec flow schema commit twin schema lint chart.
:::

## twin fast draft

Agent render schema token publish lint publish markdown fast agent commit. Spec diagram lint git build static fast git. Primitive render draft build agent review token static fast lint chart schema fast lint markdown token commit. Diagram feed schema markdown source flow review render content. Cache spec site schema flow commit render build diagram yaml review git commit. Render twin git site twin chart commit schema markdown token git git primitive git build evidence lint feed.

:::stat-row
::stat{value="84%" label="source static" source="https://snypd.rocks/bench"}
::stat{value="263ms" label="spec flow" source="https://snypd.rocks/bench"}
:::

## site evidence token

Build publish evidence diagram build commit chart markdown commit feed twin render draft token token diagram render lint site. Schema diagram primitive twin theme feed site token fast chart. Build static render token build lint git agent site chart. Site draft yaml site spec evidence schema build cache git spec. Source token agent review theme commit build diagram evidence content. Flow theme site theme primitive git agent render site lint.

:::faq
### static schema chart evidence?
Schema fast twin render markdown cache yaml draft git markdown schema chart content flow.
### flow publish diagram git?
Lint draft theme git commit content content draft git.
:::

::cta{title="diagram markdown build" button="Read the spec" href="https://snypd.rocks/spec"}
