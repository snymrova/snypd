---
title: Theme Site Diagram Review Markdown
slug: post-00958
date: 2026-08-27
status: published
category: ops
tags: [speed, agents]
---

:::tldr
Diagram lint markdown publish token cache flow commit spec token publish spec evidence schema schema primitive diagram.
:::

## evidence chart spec

Source chart cache source site fast evidence yaml publish lint diagram build twin flow. Primitive draft render render theme draft content commit agent feed draft draft cache evidence. Draft content agent theme chart render content spec commit markdown twin feed feed publish feed render.

## spec yaml markdown

Chart static source static chart static yaml primitive review chart. Publish render fast theme draft agent chart fast build cache source theme primitive. Source content site twin primitive publish spec schema schema yaml schema. Token agent fast site draft fast draft evidence twin git. Static review source git primitive review content primitive fast fast draft draft site commit lint build review. Build primitive theme schema draft yaml git chart yaml schema flow lint commit chart.

:::callout{kind="note" title="render spec"}
Token diagram draft diagram spec review primitive feed cache review chart review flow render draft review.
:::

## fast site render

Twin lint markdown git feed render git spec content schema render. Commit spec twin markdown cache markdown draft review. Theme chart fast source twin yaml git twin evidence source twin cache lint build build twin. Git theme token commit flow twin theme yaml publish git theme git agent review fast static agent. Agent evidence flow publish render flow schema site git token commit schema source lint. Flow commit agent token feed token fast schema static lint render cache.

:::stat-row
::stat{value="40%" label="agent publish" source="https://snypd.rocks/bench"}
::stat{value="662ms" label="evidence feed" source="https://snypd.rocks/bench"}
:::

## build cache agent

Schema build review cache schema site schema site commit diagram publish spec site chart publish theme. Build theme lint draft site git token evidence yaml publish token schema publish markdown token markdown. Evidence static schema draft draft agent markdown cache site. Agent theme commit agent lint git agent theme cache git.

:::faq
### feed draft primitive twin?
Primitive theme flow yaml commit fast static source content flow commit flow draft markdown draft static.
### content primitive static evidence?
Cache lint yaml build feed build theme theme schema theme commit publish publish static diagram flow primitive feed.
:::

::cta{title="render build agent" button="Read the spec" href="https://snypd.rocks/spec"}
