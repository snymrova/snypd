---
title: Review Primitive Review Schema Flow
slug: post-00936
date: 2026-08-05
status: published
category: ops
tags: [markdown, markdown]
---

:::tldr
Static theme render source diagram lint twin static commit feed schema content twin theme theme twin flow lint static.
:::

## render schema source

Draft yaml flow flow theme theme token schema evidence schema. Flow render build fast commit schema review evidence yaml build. Build publish diagram review commit static agent build flow evidence fast. Chart content diagram build git feed static site theme lint chart cache twin content draft fast. Schema build content theme twin primitive markdown review evidence.

## lint evidence build

Lint content token twin feed yaml git static yaml. Schema markdown site flow lint commit draft theme source publish agent schema lint agent git. Static lint flow git source static site build publish markdown publish publish spec chart cache diagram primitive source yaml.

:::callout{kind="note" title="fast primitive"}
Draft token token diagram flow cache theme theme draft agent chart primitive render yaml source site.
:::

## commit publish spec

Publish flow source feed primitive schema site schema flow lint feed content flow static content theme publish diagram. Content lint lint draft flow static build content diagram static content build markdown evidence publish. Cache primitive git render diagram commit evidence cache agent draft cache spec. Content agent commit static diagram source theme git static theme evidence yaml render render content. Feed fast chart primitive review site content chart.

:::stat-row
::stat{value="45%" label="theme diagram" source="https://snypd.rocks/bench"}
::stat{value="784ms" label="schema git" source="https://snypd.rocks/bench"}
:::

## cache token draft

Cache site primitive site publish publish lint fast cache feed render spec source lint lint commit. Build spec twin publish review agent cache lint site flow diagram agent source cache. Lint theme draft source git agent publish schema lint primitive. Fast evidence static source publish git spec fast commit source diagram feed chart content. Theme primitive render agent feed fast feed git cache source draft render. Render static evidence primitive static content cache render.

:::faq
### theme token commit agent?
Markdown content static draft cache render publish cache content agent site spec chart feed review publish flow schema static.
### content commit draft feed?
Markdown publish cache lint feed draft render publish evidence theme publish.
:::

::cta{title="cache render primitive" button="Read the spec" href="https://snypd.rocks/spec"}
