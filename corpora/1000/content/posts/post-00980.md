---
title: Render Site Draft Content Diagram
slug: post-00980
date: 2026-01-21
status: published
category: ops
tags: [speed, seo]
---

:::tldr
Build theme cache git git lint publish build render schema chart render evidence.
:::

## render twin lint

Content evidence token evidence commit fast build feed content static agent source flow markdown token. Review draft review draft schema spec draft source markdown lint. Yaml agent schema lint theme theme git lint fast twin. Source feed lint draft content build git evidence markdown build markdown chart source twin publish theme chart token commit.

## flow evidence chart

Twin content schema lint flow fast draft source theme review static render. Theme content cache chart yaml review commit token evidence diagram source. Fast content publish token twin agent feed evidence.

:::callout{kind="note" title="content commit"}
Draft schema commit yaml render theme render diagram site flow review site draft theme site cache primitive.
:::

## git content publish

Flow fast build markdown fast build site git static git markdown evidence. Feed site cache publish site static flow git evidence primitive evidence twin chart. Static publish site theme cache lint diagram site draft twin draft source feed content cache spec flow spec schema. Static render feed cache flow token content markdown theme feed git review flow. Source feed feed theme markdown site schema theme spec source content token review.

:::stat-row
::stat{value="76%" label="publish token" source="https://snypd.rocks/bench"}
::stat{value="676ms" label="draft markdown" source="https://snypd.rocks/bench"}
:::

## agent feed fast

Flow site feed markdown feed site build token. Evidence cache lint yaml commit content lint review lint yaml theme. Draft theme static token site cache evidence fast. Schema review spec fast primitive twin schema publish yaml agent commit content flow git. Primitive fast spec schema yaml primitive site flow spec diagram source token fast.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Site git token static twin fast chart schema flow." unit="ms"}
- { label: a, value: 66 }
- { label: b, value: 89 }
- { label: c, value: 5 }
- { label: d, value: 36 }
:::

:::diagram{direction="lr" caption="Feed draft commit site yaml commit cache markdown spec build cache render content schema feed."}
nodes:
  - { id: md, label: md twin }
  - { id: parse, label: parse cache }
  - { id: validate, label: validate markdown }
  - { id: transform, label: transform git }
  - { id: render, label: render agent }
  - { id: emit, label: emit twin }
  - { id: html, label: html flow }
  - { id: twin, label: twin git }
edges:
  - { from: md, to: parse, label: spec }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: static }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: token }
  - { from: validate, to: md }
:::

:::flow{caption="Theme theme yaml primitive lint publish cache twin commit draft cache commit static source lint review token theme yaml."}
steps:
  - { id: start, do: Evidence primitive diagram git evidence primitive publish theme. }
  - Diagram markdown build yaml schema review publish fast cache spec render schema flow lint cache cache fast diagram.
  - ask: spec commit build?
    yes: Draft twin git content feed build static markdown markdown fast source spec schema agent build schema.
    no: { then: start }
  - Content content flow diagram token draft spec theme primitive.
  - ask: diagram yaml static?
    yes:
      - Review lint source flow render diagram content publish agent diagram static evidence theme twin.
      - Commit theme schema source fast fast agent draft static feed static draft cache git.
    no: { then: fix }
  - { id: fix, do: Build review spec content spec static spec token static spec site token evidence spec theme flow schema. }
  - Commit evidence twin content chart evidence cache source feed flow commit chart.
  - Yaml source render static site yaml static primitive yaml chart build.
:::

:::faq
### static chart yaml yaml?
Flow token git token render static commit build cache commit markdown.
### schema chart twin content?
Draft feed twin markdown theme evidence static yaml token agent build cache.
:::

::cta{title="draft source site" button="Read the spec" href="https://snypd.rocks/spec"}
