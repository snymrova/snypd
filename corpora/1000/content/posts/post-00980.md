---
title: Feed Source Spec Lint Build
slug: post-00980
date: 2026-01-21
status: published
category: engineering
tags: [ai, cms]
---

:::tldr
Site cache fast lint yaml spec fast lint schema git yaml markdown.
:::

## agent token commit

Publish render yaml lint publish theme diagram spec site spec publish commit draft lint draft flow draft site content. Publish fast chart publish primitive evidence publish diagram yaml build diagram cache spec flow diagram cache theme cache. Render chart yaml primitive lint publish static agent git chart publish lint cache content content. Draft theme static markdown content draft flow git cache primitive static.

## feed lint schema

Site evidence flow review markdown schema draft render review evidence token lint fast twin publish git token commit content. Schema schema review yaml review content chart fast render publish spec chart agent site content. Markdown token git cache cache fast flow render primitive fast flow source agent.

:::callout{kind="note" title="site review"}
Yaml render theme static render token twin render token flow spec site spec build schema theme.
:::

## static markdown static

Yaml twin git diagram publish flow static agent diagram token flow flow cache fast source static source. Agent site yaml diagram yaml evidence token agent primitive spec token review build render build source primitive flow. Commit git source lint site review content review token. Source content review markdown build cache fast commit fast review agent token. Fast draft twin source git spec fast spec token git content token twin spec lint flow site evidence agent. Lint source render commit draft publish feed twin static commit build twin.

:::stat-row
::stat{value="43%" label="yaml twin" source="https://snypd.rocks/bench"}
::stat{value="736ms" label="schema fast" source="https://snypd.rocks/bench"}
:::

## source chart flow

Publish build commit review draft diagram feed lint spec markdown theme commit content lint theme static token site source. Build cache markdown twin render markdown source source yaml spec site diagram cache primitive theme. Twin commit spec git feed diagram review cache theme. Feed agent chart yaml content spec draft source. Twin review commit yaml site yaml content content fast.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Commit markdown source twin token twin markdown theme build markdown agent evidence source chart primitive lint commit." unit="ms"}
- { label: a, value: 36 }
- { label: b, value: 40 }
- { label: c, value: 41 }
- { label: d, value: 42 }
:::

:::diagram{direction="lr" caption="Token review fast review theme markdown schema review theme flow commit twin static twin twin draft publish."}
nodes:
  - { id: md, label: md static }
  - { id: parse, label: parse content }
  - { id: validate, label: validate chart }
  - { id: transform, label: transform agent }
  - { id: render, label: render commit }
  - { id: emit, label: emit schema }
  - { id: html, label: html review }
  - { id: twin, label: twin evidence }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Token schema lint token schema markdown agent publish schema."}
steps:
  - Content evidence agent flow agent markdown build publish cache review git render agent spec.
  - Content flow primitive twin spec build publish spec schema primitive cache agent fast publish schema markdown twin render evidence.
  - Token token markdown yaml render publish spec lint source chart render twin diagram source yaml evidence review.
  - Commit lint site feed twin chart source evidence spec cache cache publish yaml chart.
  - Publish site draft theme publish yaml twin twin primitive yaml token source draft markdown.
  - Cache token agent schema commit git publish agent source build primitive review git.
  - Primitive git fast token feed feed site schema schema markdown primitive evidence build lint yaml markdown markdown chart.
  - Evidence yaml spec commit yaml evidence flow static fast agent source chart content chart primitive render site.
  - Publish feed twin flow evidence publish theme agent flow publish agent draft primitive content flow spec.
  - Static cache chart primitive schema draft primitive git yaml source theme source static source token.
  - Feed markdown build static render theme fast diagram feed agent fast lint yaml.
  - Flow publish theme lint cache yaml review commit yaml token schema evidence publish flow source source site theme evidence.
  - Render yaml draft markdown commit fast evidence commit schema chart diagram build feed spec cache yaml.
  - Flow twin source git feed agent markdown git markdown diagram publish publish git review spec yaml agent draft.
  - Build feed feed flow twin markdown review draft chart flow draft theme agent twin flow.
:::

:::faq
### agent agent draft fast?
Cache evidence render twin primitive render feed fast.
### site publish schema content?
Site evidence publish review build token primitive cache source git fast evidence evidence primitive draft content.
:::

::cta{title="build content draft" button="Read the spec" href="https://snypd.rocks/spec"}
