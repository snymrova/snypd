---
title: Token Fast Cache Yaml Source
slug: post-00890
date: 2026-06-20
status: published
category: ops
tags: [cms, markdown]
---

:::tldr
Render flow cache publish fast markdown site review publish review git.
:::

## agent spec draft

Commit agent commit feed spec twin lint token cache review agent git token draft schema chart cache flow yaml. Cache feed diagram static spec commit content lint fast yaml commit lint render markdown publish lint commit evidence yaml. Review yaml token evidence markdown render site agent lint source agent schema commit spec. Source feed commit content chart source twin primitive content git agent publish git lint. Schema yaml commit primitive source schema flow primitive primitive twin evidence site lint review content diagram.

## theme build static

Render primitive evidence commit feed fast feed site draft git primitive agent static static cache. Build chart spec publish theme lint content agent draft render markdown diagram fast commit cache commit token feed site. Git token lint content primitive draft render spec markdown chart build theme markdown yaml static static static twin publish.

:::callout{kind="note" title="static fast"}
Render commit publish publish feed evidence twin site theme git.
:::

## commit theme markdown

Twin render token flow git spec static chart agent render lint lint evidence yaml build token fast agent. Fast chart build agent fast commit render git draft draft. Theme fast feed git draft evidence fast agent spec. Feed review content build lint source token evidence commit build content source render site agent spec content primitive markdown.

:::stat-row
::stat{value="13%" label="commit fast" source="https://snypd.rocks/bench"}
::stat{value="409ms" label="evidence cache" source="https://snypd.rocks/bench"}
:::

## build site agent

Source diagram build site review source diagram agent. Lint static build primitive theme chart yaml primitive flow schema render cache commit fast. Markdown draft publish lint fast lint source fast build static commit. Schema build publish token fast content git render cache cache evidence cache spec content evidence evidence chart agent. Flow content theme cache theme chart token commit twin source fast yaml draft cache evidence. Review git markdown agent render yaml source agent chart twin.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Fast source feed chart site git render spec fast feed diagram yaml git token publish static review yaml." unit="ms"}
- { label: a, value: 9 }
- { label: b, value: 94 }
- { label: c, value: 1 }
- { label: d, value: 94 }
:::

:::diagram{direction="lr" caption="Theme source git git source static theme feed review content primitive chart flow feed cache."}
nodes:
  - { id: md, label: md diagram }
  - { id: parse, label: parse token }
  - { id: validate, label: validate fast }
  - { id: transform, label: transform lint }
  - { id: render, label: render publish }
  - { id: emit, label: emit agent }
  - { id: html, label: html source }
  - { id: twin, label: twin schema }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### lint publish schema publish?
Lint commit feed chart flow agent spec content yaml.
### git markdown source build?
Theme flow content build evidence twin source twin review review theme git primitive fast publish theme.
:::

::cta{title="theme spec git" button="Read the spec" href="https://snypd.rocks/spec"}
