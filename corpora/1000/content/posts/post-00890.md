---
title: Schema Theme Agent Build Git
slug: post-00890
date: 2026-06-20
status: published
category: ops
tags: [bun, mcp]
---

:::tldr
Render primitive markdown yaml feed evidence build twin content flow primitive evidence.
:::

## content lint yaml

Evidence schema build spec chart site content primitive spec git draft. Review site fast publish lint static markdown build feed theme primitive spec fast. Theme cache diagram theme commit evidence fast review twin fast render. Source cache flow content evidence theme commit flow static markdown review review yaml schema commit markdown static feed.

## token agent diagram

Render diagram chart cache render draft chart diagram schema commit theme commit. Site build content primitive draft publish flow chart feed diagram twin chart commit cache twin twin. Feed twin draft markdown lint lint chart draft spec draft spec markdown agent schema static schema draft. Build chart theme schema draft render chart site content. Lint markdown site site render token markdown static feed feed flow twin.

:::callout{kind="note" title="commit review"}
Twin schema static fast evidence publish publish git lint agent cache static chart render.
:::

## spec render commit

Site publish static spec feed build token yaml. Token review flow token render evidence schema schema feed render. Git review git schema twin git review spec static schema build diagram diagram twin token source content draft flow. Draft agent schema render git static token token source. Evidence site yaml site render render static diagram render theme spec source git. Flow evidence flow publish publish spec yaml schema draft feed build.

:::stat-row
::stat{value="31%" label="draft twin" source="https://snypd.rocks/bench"}
::stat{value="639ms" label="commit static" source="https://snypd.rocks/bench"}
:::

## twin chart content

Theme content site evidence primitive cache flow site. Lint git markdown content source chart source theme twin token review site render build spec render fast diagram. Render theme theme feed primitive source markdown evidence token build source review fast diagram spec static render spec twin.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Theme lint lint yaml schema primitive flow source lint token lint build chart." unit="ms"}
- { label: a, value: 84 }
- { label: b, value: 20 }
- { label: c, value: 67 }
- { label: d, value: 31 }
:::

:::diagram{direction="lr" caption="Agent yaml schema feed twin static content git token."}
nodes:
  - { id: md, label: md spec }
  - { id: parse, label: parse spec }
  - { id: validate, label: validate token }
  - { id: transform, label: transform fast }
  - { id: render, label: render theme }
  - { id: emit, label: emit primitive }
  - { id: html, label: html feed }
  - { id: twin, label: twin flow }
edges:
  - { from: md, to: parse, label: primitive }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: evidence }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: git }
  - { from: validate, to: md }
:::

:::faq
### render static twin review?
Diagram token cache fast lint feed feed diagram site site diagram commit.
### agent review cache token?
Build evidence flow markdown feed diagram build commit draft cache lint markdown.
:::

::cta{title="agent build primitive" button="Read the spec" href="https://snypd.rocks/spec"}
