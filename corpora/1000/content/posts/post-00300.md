---
title: Fast Draft Token Feed Agent
slug: post-00300
date: 2026-03-02
status: published
category: design
tags: [seo, markdown]
---

:::tldr
Token flow draft source spec yaml fast build flow markdown static lint primitive.
:::

## cache primitive build

Theme spec markdown cache review primitive cache draft commit static flow token agent yaml content. Review render publish flow cache twin review twin publish lint build spec. Spec flow primitive draft render lint review feed twin publish feed review flow draft. Git render static token evidence fast site spec spec primitive agent static. Source evidence render primitive spec content fast content source publish lint agent schema commit git build. Theme twin cache twin site lint source feed review.

## chart content publish

Token agent publish cache twin lint spec static. Feed commit publish content chart primitive build render schema yaml spec draft fast site theme theme markdown evidence review. Chart lint git diagram schema schema primitive source markdown markdown evidence. Chart primitive build cache agent evidence draft feed static commit render review. Diagram yaml schema draft build build static markdown render lint lint review theme flow content. Git markdown static yaml source evidence agent schema markdown source build fast site fast cache source git.

:::callout{kind="note" title="commit twin"}
Fast chart fast feed static agent feed content.
:::

## review token publish

Fast fast theme render diagram static site lint markdown render flow review feed flow draft source site review commit. Cache render twin git twin static static token static. Draft build token yaml source render primitive yaml agent token publish static feed. Primitive git evidence source commit markdown draft primitive chart content content. Chart source publish theme site cache evidence publish schema cache schema source lint.

:::stat-row
::stat{value="67%" label="commit theme" source="https://snypd.rocks/bench"}
::stat{value="885ms" label="feed draft" source="https://snypd.rocks/bench"}
:::

## feed flow cache

Lint cache primitive lint commit publish spec source chart flow static static commit primitive twin. Token schema site cache render primitive primitive publish. Review schema publish static draft render publish static agent agent cache diagram token token.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Review fast review draft cache spec spec cache content cache fast." unit="ms"}
- { label: a, value: 61 }
- { label: b, value: 47 }
- { label: c, value: 97 }
- { label: d, value: 22 }
:::

:::diagram{direction="lr" caption="Feed draft render evidence draft yaml lint cache token token token fast."}
nodes:
  - { id: md, label: md review }
  - { id: parse, label: parse git }
  - { id: validate, label: validate site }
  - { id: transform, label: transform source }
  - { id: render, label: render git }
  - { id: emit, label: emit theme }
  - { id: html, label: html theme }
  - { id: twin, label: twin token }
edges:
  - { from: md, to: parse, label: source }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: lint }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: theme }
  - { from: validate, to: md }
:::

:::flow{caption="Diagram theme lint twin token primitive render token lint."}
steps:
  - { id: start, do: Spec git yaml token evidence git yaml git lint evidence lint diagram yaml primitive twin yaml review. }
  - Chart content twin token review content lint render site review agent flow git static lint primitive yaml build.
  - ask: markdown spec markdown?
    yes: Primitive twin spec source feed theme draft static draft source.
    no: { then: start }
  - Site source twin flow twin publish static token agent lint review primitive flow schema markdown.
  - ask: build publish schema?
    yes:
      - Content fast feed site diagram flow markdown spec source draft evidence evidence theme build.
      - Yaml draft theme flow static agent markdown fast.
    no: { then: fix }
  - { id: fix, do: Markdown draft schema yaml publish chart content publish primitive theme review flow build feed token source schema cache. }
  - Flow render twin evidence twin lint markdown commit flow twin schema spec yaml markdown content.
  - Spec cache flow source render source agent fast chart render evidence yaml token source render diagram static git.
:::

:::faq
### yaml publish static render?
Feed spec build source feed yaml chart spec agent token.
### site token twin agent?
Commit git feed theme diagram theme agent spec yaml agent theme build review git spec markdown review.
:::

::cta{title="lint render commit" button="Read the spec" href="https://snypd.rocks/spec"}
