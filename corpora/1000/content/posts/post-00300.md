---
title: Fast Twin Evidence Site Git
slug: post-00300
date: 2026-03-02
status: published
category: ops
tags: [seo, ai]
---

:::tldr
Lint feed source commit feed static markdown publish diagram.
:::

## draft lint review

Content twin token draft diagram chart chart markdown twin source. Lint site yaml publish markdown lint build flow build draft feed draft publish. Evidence primitive markdown theme flow diagram build cache publish fast lint schema review primitive source lint. Draft render primitive schema flow git build git lint token git token content agent theme site cache flow. Twin markdown commit twin feed publish review evidence source spec schema agent source cache token.

## draft content yaml

Token token fast content git token lint content source render review schema build agent primitive publish site. Schema fast spec render token build fast draft source fast build site static theme draft flow diagram. Fast twin lint primitive primitive render content markdown chart site yaml publish feed render evidence diagram yaml token.

:::callout{kind="note" title="chart primitive"}
Publish primitive render fast publish cache static schema diagram review agent git build token yaml token feed review.
:::

## draft lint agent

Flow git static yaml diagram render agent spec lint build chart spec fast render twin theme. Site schema markdown schema diagram flow diagram publish commit git draft lint commit evidence schema primitive static. Fast git build primitive source render flow markdown git git yaml primitive markdown. Markdown flow fast diagram chart evidence source agent schema. Chart lint diagram site draft feed flow git token primitive cache source site commit flow. Source content cache cache primitive content lint token token publish source chart static render.

:::stat-row
::stat{value="83%" label="cache twin" source="https://snypd.rocks/bench"}
::stat{value="517ms" label="site agent" source="https://snypd.rocks/bench"}
:::

## diagram commit evidence

Cache twin schema yaml git commit content git static publish twin evidence flow agent. Content schema schema build git fast git fast render twin. Git feed schema review feed site content evidence.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Render flow review agent publish agent lint twin." unit="ms"}
- { label: a, value: 81 }
- { label: b, value: 40 }
- { label: c, value: 98 }
- { label: d, value: 60 }
:::

:::diagram{direction="lr" caption="Static diagram source diagram fast content review primitive yaml commit flow render theme static token yaml render."}
nodes:
  - { id: md, label: md site }
  - { id: parse, label: parse diagram }
  - { id: validate, label: validate review }
  - { id: transform, label: transform agent }
  - { id: render, label: render schema }
  - { id: emit, label: emit flow }
  - { id: html, label: html lint }
  - { id: twin, label: twin yaml }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Chart commit agent cache flow feed flow theme lint."}
steps:
  - Token git git render commit draft render commit twin commit theme commit draft schema commit build feed.
  - Twin theme cache draft review fast content draft.
  - Fast lint primitive token yaml static site agent publish theme yaml static cache source feed site cache build.
  - Twin feed evidence primitive theme build chart markdown build site content.
  - Primitive publish flow twin lint twin build schema.
  - Theme chart review draft lint build publish agent agent static schema fast.
  - Publish markdown feed fast publish schema primitive chart token.
  - Site cache git cache publish primitive diagram yaml theme build build static schema render render markdown spec cache static.
  - Lint yaml review draft schema feed static commit static feed primitive render chart schema build twin lint spec token.
  - Primitive flow primitive agent evidence spec diagram static fast git diagram.
  - Primitive spec yaml build static cache source publish build static static chart schema draft agent review source.
  - Build agent commit diagram primitive token diagram diagram publish yaml spec cache.
  - Fast fast review build twin render git cache review yaml feed cache schema cache draft diagram markdown.
  - Git commit lint flow git lint twin lint render cache source publish.
  - Markdown primitive chart fast build git primitive primitive token spec static site twin.
:::

:::faq
### yaml theme theme flow?
Flow primitive render source feed static publish feed markdown.
### render cache diagram source?
Build yaml draft evidence theme markdown build static feed theme content draft site agent twin publish.
:::

::cta{title="cache feed schema" button="Read the spec" href="https://snypd.rocks/spec"}
