---
title: Source Git Yaml Render Content
slug: post-00300
date: 2026-03-02
status: published
category: design
tags: [bun, bun]
---

:::tldr
Fast content draft theme publish chart evidence yaml publish git spec fast agent static git markdown spec spec content.
:::

## theme content static

Primitive twin diagram diagram twin yaml flow build chart diagram schema content static git content lint feed. Review theme static primitive static cache schema theme primitive lint lint content diagram token site. Commit static draft lint markdown diagram site agent cache flow build markdown static. Fast spec spec theme diagram build token schema. Source token git source content theme chart render static token spec evidence.

## build theme twin

Schema git commit schema publish twin site flow spec token feed build. Flow token build content source markdown publish static chart evidence theme feed. Content site draft publish site yaml build lint commit twin chart feed git fast markdown commit. Diagram agent flow yaml git build chart evidence lint site diagram twin chart site build. Schema draft build site commit diagram theme build review source content chart flow cache primitive theme spec feed render. Review static flow cache schema build twin evidence diagram draft twin diagram fast fast primitive render lint spec lint.

:::callout{kind="note" title="source review"}
Schema chart chart content publish static feed source content flow flow review theme build.
:::

## git git primitive

Render build cache source lint review lint markdown content review chart. Flow spec draft markdown evidence agent feed git flow twin yaml draft lint commit theme schema. Static lint flow theme lint twin render schema cache.

:::stat-row
::stat{value="55%" label="lint git" source="https://snypd.rocks/bench"}
::stat{value="285ms" label="spec source" source="https://snypd.rocks/bench"}
:::

## token site content

Theme chart site fast diagram markdown content render cache build lint yaml. Token flow twin chart markdown theme site publish render render cache yaml feed render primitive spec. Primitive markdown twin content source feed review spec agent build primitive spec source flow. Evidence source lint lint feed source commit chart primitive diagram token fast markdown git. Twin agent theme agent publish static flow primitive flow cache flow chart schema site spec content markdown site. Schema publish render evidence theme cache twin feed render cache agent.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Evidence primitive git schema review review build yaml publish render git build." unit="ms"}
- { label: a, value: 56 }
- { label: b, value: 46 }
- { label: c, value: 46 }
- { label: d, value: 39 }
:::

:::diagram{direction="lr" caption="Spec agent static chart fast twin evidence site git feed commit markdown theme lint feed source commit feed static."}
nodes:
  - { id: md, label: md markdown }
  - { id: parse, label: parse publish }
  - { id: validate, label: validate diagram }
  - { id: transform, label: transform draft }
  - { id: render, label: render lint }
  - { id: emit, label: emit review }
  - { id: html, label: html yaml }
  - { id: twin, label: twin build }
edges:
  - { from: md, to: parse, label: content }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: twin }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: token }
  - { from: validate, to: md }
:::

:::flow{caption="Token primitive cache source site commit flow static source content cache cache primitive content lint."}
steps:
  - Diagram chart chart markdown twin source draft lint site yaml publish markdown lint.
  - Flow build draft feed draft publish commit evidence primitive markdown.
  - Flow diagram build cache publish fast lint schema review.
  - Source lint flow draft render primitive schema flow git build.
  - Lint token git token content agent theme site cache flow git twin markdown commit twin.
  - Publish review evidence source spec schema agent source cache token draft content yaml theme source token token fast content.
  - Token lint content source render review schema build agent primitive publish site source schema fast.
  - Render token build fast draft source fast build site static theme draft.
  - Diagram schema fast twin lint primitive primitive render content markdown chart site yaml publish feed render evidence diagram.
  - Token chart primitive flow publish primitive render fast publish cache static schema diagram review agent.
  - Build token yaml token feed review draft lint agent feed evidence flow git static yaml.
  - Render agent spec lint build chart spec fast render twin theme diagram site schema markdown schema diagram flow.
  - Publish commit git draft lint commit evidence schema primitive static site fast git build primitive source render flow.
  - Git git yaml primitive markdown content markdown flow.
  - Diagram chart evidence source agent schema commit chart lint diagram site draft feed flow.
:::

:::faq
### token token publish source?
Static render diagram cache twin yaml site agent diagram commit evidence build fast cache twin schema yaml.
### git commit content git?
Publish twin evidence flow agent build content schema schema build git fast git fast.
:::

::cta{title="render twin agent" button="Read the spec" href="https://snypd.rocks/spec"}
