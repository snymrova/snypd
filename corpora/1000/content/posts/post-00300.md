---
title: Token Flow Draft Chart Static
slug: post-00300
date: 2026-03-02
status: published
category: product
tags: [cms, bun]
---

:::tldr
Spec render publish git render feed agent lint.
:::

## cache flow spec

Flow lint draft theme theme draft schema chart. Agent commit theme static token spec token fast agent primitive render fast content. Twin diagram primitive review primitive diagram schema chart diagram.

## token diagram publish

Source content fast content lint evidence feed token lint feed chart draft markdown review token. Diagram twin token fast primitive git schema git static twin primitive diagram flow publish site token review review source. Publish agent review source diagram render token schema. Theme content yaml draft site source schema publish chart site publish build. Content theme spec build content chart commit evidence flow diagram. Spec feed diagram token flow diagram git schema yaml source schema token feed build publish.

:::callout{kind="note" title="primitive static"}
Evidence theme agent token twin source agent site.
:::

## feed source render

Static source evidence static content git diagram theme theme chart twin markdown. Git draft static render lint static fast cache chart publish chart lint git yaml commit. Markdown static spec draft spec commit content fast primitive review static schema build draft primitive build.

::stat-row
::stat{value="53%" label="primitive review" source="https://snypd.rocks/bench"}
::stat{value="798ms" label="evidence yaml" source="https://snypd.rocks/bench"}
::

## evidence source primitive

Yaml evidence primitive chart markdown twin flow agent draft twin evidence agent theme git git content render build. Theme publish schema token static token build review render chart token feed twin flow twin lint git yaml. Review spec site git commit git publish commit. Evidence theme token publish static content render feed feed chart yaml token token draft. Agent primitive publish commit chart evidence primitive static build yaml markdown commit flow spec cache.

::chart{type="bar" source="https://snypd.rocks/bench" caption="yaml commit diagram"}
  data: { a: 75, b: 74, c: 75 }

:::diagram{direction="lr"}
parse -> validate -> transform -> render -> emit
:::

:::flow
1. Build render review twin review evidence content markdown static markdown schema git theme feed content review content.
2. Markdown chart render lint schema publish content fast.
3. Primitive primitive chart diagram evidence theme diagram render theme build twin markdown review build content.
4. Chart publish draft agent cache draft twin build git publish schema yaml.
5. Twin markdown lint schema fast primitive theme render diagram lint.
6. Draft cache evidence flow feed static yaml token twin markdown publish render markdown source source static.
7. Agent git content feed agent source publish agent lint render build evidence lint.
8. Git review token chart diagram theme git site build fast.
9. Commit content build theme yaml theme site cache content cache build markdown build.
10. Primitive twin theme evidence source chart markdown fast twin.
11. Content static content token draft chart agent build feed schema markdown token schema.
12. Spec theme primitive publish static token static theme review commit cache publish commit diagram fast.
13. Git publish token agent lint feed static evidence theme token yaml theme source theme.
14. Evidence lint markdown chart build content review lint commit commit cache fast source agent render fast build markdown.
15. Site source cache evidence agent build publish source token yaml build evidence commit markdown.
:::

:::faq
### build cache agent review?
Lint static primitive markdown primitive build render draft.
### commit lint schema evidence?
Diagram spec static content draft feed lint commit evidence markdown markdown build commit lint publish theme.
:::

::cta{title="build token commit" button="Read the spec" href="/spec"}
