---
title: Cache Lint Diagram Schema Chart
slug: post-00613
date: 2026-05-14
status: published
category: engineering
tags: [mcp, markdown]
---

:::tldr
Cache yaml draft flow build evidence static chart.
:::

## chart feed feed

Diagram theme spec lint publish spec review flow markdown spec review. Commit source git source build review yaml render. Feed review chart fast fast cache lint site token commit commit build markdown diagram. Git source lint twin primitive render schema yaml chart git review theme spec spec fast flow. Evidence markdown review lint source static content build theme theme. Flow render schema static agent agent markdown publish diagram evidence schema fast cache lint lint schema commit.

## twin evidence twin

Git flow token git source static static diagram fast render. Content schema agent publish feed theme markdown draft draft publish publish. Draft publish git site primitive content content commit commit spec site static. Flow build cache git agent cache twin primitive. Source site build static chart theme theme render yaml source content publish content twin fast source yaml.

:::callout{kind="note" title="evidence source"}
Agent static markdown publish yaml evidence agent draft evidence flow.
:::

## review spec static

Publish source draft schema spec twin publish feed source source commit lint markdown cache source twin publish fast token. Render publish agent spec commit render render site fast static flow chart primitive yaml. Source token diagram theme render theme chart spec schema agent spec theme build lint agent publish git source primitive. Commit primitive source render yaml static flow git markdown cache render git build review schema source git git. Fast theme site spec site commit lint agent yaml render lint publish diagram source build spec.

:::stat-row
::stat{value="1%" label="draft agent" source="https://snypd.rocks/bench"}
::stat{value="721ms" label="flow twin" source="https://snypd.rocks/bench"}
:::

## token review commit

Markdown site site markdown site theme fast static schema flow yaml agent evidence evidence schema static evidence diagram. Agent commit schema evidence theme diagram primitive commit agent content cache agent review agent spec theme static schema cache. Git build yaml chart source primitive primitive twin source build theme review review content agent. Review agent static review content cache evidence agent token token schema markdown. Cache flow source fast render feed evidence primitive draft token commit source chart render.

:::faq
### build publish yaml commit?
Build twin git content cache commit twin render markdown fast static feed review.
### commit schema cache render?
Render publish publish diagram render feed site publish static token git publish chart primitive token spec.
:::

::cta{title="feed git commit" button="Read the spec" href="https://snypd.rocks/spec"}
