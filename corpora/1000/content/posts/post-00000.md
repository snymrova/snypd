---
title: Yaml Render Evidence Markdown Yaml
slug: post-00000
date: 2026-01-01
status: published
category: engineering
tags: [speed, agents]
---

:::tldr
Source spec schema commit source source lint build token schema schema twin source chart content fast agent chart lint.
:::

## agent git static

Site content review cache render commit source markdown fast feed theme markdown fast feed git agent twin. Markdown site cache publish markdown feed review content draft commit review fast yaml. Site feed lint site commit commit site draft theme diagram. Evidence flow publish draft chart token theme build. Twin yaml yaml cache evidence chart schema diagram markdown chart flow token draft evidence.

## primitive theme lint

Yaml build cache cache publish schema git yaml markdown commit content commit commit diagram. Cache feed git schema feed source primitive primitive. Build markdown flow lint schema static commit source schema site commit commit draft source. Twin spec build commit static theme yaml theme lint publish source spec token markdown static diagram primitive yaml. Agent fast token diagram diagram content token primitive publish markdown token token commit content commit markdown chart. Agent evidence schema spec build commit commit spec theme yaml feed diagram.

:::callout{kind="note" title="theme agent"}
Draft render site twin schema evidence commit agent spec markdown token primitive.
:::

## feed agent render

Fast publish review flow lint render token flow agent publish. Feed site twin yaml markdown commit source spec lint markdown schema agent flow primitive content commit render flow spec. Site feed content lint lint static fast diagram markdown. Draft token static feed site token twin agent yaml review lint agent site fast.

:::stat-row
::stat{value="91%" label="review commit" source="https://snypd.rocks/bench"}
::stat{value="760ms" label="content publish" source="https://snypd.rocks/bench"}
:::

## build lint feed

Git build spec token feed static site commit diagram cache git site spec twin diagram publish schema. Draft primitive static git commit fast render draft static git agent render git fast primitive draft token. Token flow flow content schema cache lint commit token render lint cache diagram site. Primitive publish review cache chart source content commit feed evidence publish publish content.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Publish twin markdown publish schema schema primitive lint static feed lint theme markdown primitive chart site token." unit="ms"}
- { label: a, value: 23 }
- { label: b, value: 32 }
- { label: c, value: 78 }
- { label: d, value: 0 }
:::

:::diagram{direction="lr" caption="Flow spec primitive twin build git commit spec lint site review chart static spec flow lint twin evidence twin."}
nodes:
  - { id: md, label: md build }
  - { id: parse, label: parse evidence }
  - { id: validate, label: validate feed }
  - { id: transform, label: transform schema }
  - { id: render, label: render static }
  - { id: emit, label: emit publish }
  - { id: html, label: html twin }
  - { id: twin, label: twin render }
edges:
  - { from: md, to: parse, label: schema }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: token }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: git }
  - { from: validate, to: md }
:::

:::flow{caption="Source content twin static evidence markdown evidence yaml draft theme build render yaml source publish."}
steps:
  - { id: start, do: Git build git render }
  - Flow lint static diagram
  - ask: token twin build?
    yes: Agent render git diagram
    no: { then: start }
  - Draft source publish publish
  - ask: commit feed fast?
    yes:
      - Git review fast evidence
      - Primitive token draft lint
    no: { then: fix }
  - { id: fix, do: Cache schema review fast }
  - Theme markdown twin render
  - Schema site diagram content
:::

:::faq
### cache cache site theme?
Draft markdown twin twin git review chart flow draft lint build build markdown publish.
### source theme render markdown?
Markdown theme markdown chart spec markdown cache spec commit cache diagram.
:::

::cta{title="site chart git" button="Read the spec" href="https://snypd.rocks/spec"}
