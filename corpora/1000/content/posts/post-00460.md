---
title: Commit Yaml Agent Spec Publish
slug: post-00460
date: 2026-08-09
status: published
category: ops
tags: [ai, mcp]
---

:::tldr
Site twin render draft review content diagram primitive agent twin build git.
:::

## publish draft review

Static primitive diagram site markdown source git site flow render feed. Schema yaml fast source evidence token commit flow feed. Publish lint publish chart primitive render draft flow yaml spec. Publish commit yaml flow fast render agent markdown draft evidence review fast git spec chart token. Cache commit git spec flow review source render git markdown commit feed agent diagram lint source token. Feed agent markdown git primitive evidence publish static build.

## markdown chart feed

Static render twin schema site fast publish cache agent schema diagram flow evidence. Primitive cache yaml theme fast site diagram source build evidence site cache spec publish site markdown twin. Yaml flow static chart build render cache commit chart. Content cache publish schema source evidence evidence diagram primitive primitive markdown commit yaml theme. Twin agent yaml commit review markdown twin static. Static lint source build markdown agent chart yaml twin render flow.

:::callout{kind="note" title="lint commit"}
Diagram content lint flow feed content commit lint twin yaml agent content evidence spec build cache.
:::

## render yaml diagram

Git markdown chart feed agent evidence twin render diagram feed. Markdown commit diagram review feed spec feed twin git. Theme site token static fast site site theme commit diagram cache cache yaml site diagram render primitive review.

::stat-row
::stat{value="95%" label="evidence cache" source="https://snypd.rocks/bench"}
::stat{value="811ms" label="site fast" source="https://snypd.rocks/bench"}
::

## twin agent diagram

Site chart token source yaml chart cache primitive diagram draft markdown flow. Fast theme git feed draft publish publish diagram markdown twin render. Theme cache flow feed twin diagram render theme schema git git. Commit commit publish flow site flow lint publish static markdown. Flow theme build primitive lint spec schema publish draft content review draft twin diagram.

::chart{type="bar" source="https://snypd.rocks/bench" caption="schema markdown git"}
  data: { a: 5, b: 32, c: 80 }

:::diagram{direction="lr"}
parse -> validate -> transform -> render -> emit
:::

:::flow
1. Lint markdown commit cache static site cache source schema theme lint site theme.
2. Render render primitive feed yaml agent schema commit content build agent.
3. Chart flow git diagram fast yaml build review.
4. Static feed build agent schema agent diagram yaml.
5. Commit schema schema git static token static primitive token evidence yaml feed agent static fast.
6. Markdown lint evidence source evidence schema static static cache evidence lint.
7. Commit schema chart agent static lint diagram content.
8. Diagram theme feed review static fast evidence agent.
9. Review spec evidence evidence lint build diagram feed schema twin lint evidence chart diagram theme commit schema.
10. Commit commit theme review flow schema lint feed review commit build fast render schema site draft.
11. Render content primitive yaml feed markdown agent theme content lint chart diagram.
12. Diagram commit content schema commit cache render primitive.
13. Source chart diagram schema markdown content static fast cache yaml flow git.
14. Lint fast primitive draft static evidence draft lint token chart build.
15. Evidence theme evidence draft build lint theme evidence markdown feed.
:::

:::faq
### primitive diagram draft content?
Theme static render agent draft render theme fast site render feed.
### review publish content static?
Review commit token site build flow markdown spec diagram static agent source yaml primitive.
:::

::cta{title="review content static" button="Read the spec" href="/spec"}
