---
title: Theme Static Publish Theme Feed
slug: post-00460
date: 2026-08-09
status: published
category: engineering
tags: [speed, cms]
---

:::tldr
Git fast content evidence build agent yaml fast content feed primitive.
:::

## diagram agent lint

Fast evidence fast draft theme flow primitive build review git draft. Evidence schema twin commit build source theme yaml diagram static render twin. Markdown primitive draft lint review render source content draft. Token diagram render source site schema flow draft content twin cache publish. Agent render content chart primitive token review build static spec lint theme render commit commit static token. Draft feed cache render source review token content render agent publish evidence source lint commit yaml git lint.

## token cache content

Draft token draft token fast build yaml theme fast schema review lint. Flow theme markdown flow content git draft content lint lint content git content source static lint diagram. Review yaml feed flow publish render evidence content feed git review git site markdown review draft. Twin commit source token render lint feed static diagram build. Token commit token chart git review token cache lint review.

:::callout{kind="note" title="render publish"}
Flow evidence markdown git yaml schema review primitive schema chart site source evidence chart build review spec.
:::

## theme evidence commit

Lint lint cache twin diagram build primitive yaml draft evidence chart draft spec primitive git render evidence yaml theme. Lint lint lint cache draft draft build spec schema spec chart feed site theme publish draft. Static render cache lint render feed build flow chart. Static schema agent yaml build feed source review review source build draft chart. Git spec flow publish spec build markdown lint build draft git spec fast source static source feed cache review. Evidence yaml content agent content lint feed yaml render git lint publish spec token draft diagram content spec.

:::stat-row
::stat{value="82%" label="render publish" source="https://snypd.rocks/bench"}
::stat{value="216ms" label="theme source" source="https://snypd.rocks/bench"}
:::

## agent site review

Lint cache render git commit primitive content review chart git site yaml twin diagram source markdown schema. Publish theme content markdown review token commit git feed twin source schema content static schema content diagram chart. Cache twin source build agent chart lint render git primitive primitive. Twin yaml flow yaml chart schema feed schema lint review flow content spec theme git spec primitive. Source git fast git commit primitive cache yaml diagram git token agent content site static commit review spec fast. Review twin primitive markdown markdown twin commit chart cache chart static build content spec diagram site twin.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Git commit content diagram token lint feed token render cache cache fast flow source publish agent spec draft content." unit="ms"}
- { label: a, value: 97 }
- { label: b, value: 8 }
- { label: c, value: 95 }
- { label: d, value: 84 }
:::

:::diagram{direction="lr" caption="Publish flow schema git theme fast markdown content theme chart draft diagram spec."}
nodes:
  - { id: md, label: md theme }
  - { id: parse, label: parse build }
  - { id: validate, label: validate commit }
  - { id: transform, label: transform commit }
  - { id: render, label: render draft }
  - { id: emit, label: emit evidence }
  - { id: html, label: html flow }
  - { id: twin, label: twin fast }
edges:
  - { from: md, to: parse, label: agent }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: render }
  - { from: validate, to: md }
:::

:::flow{caption="Build chart evidence site site diagram flow spec diagram."}
steps:
  - { id: start, do: Agent chart flow spec twin evidence feed commit git evidence evidence review review. }
  - Agent source flow diagram draft publish draft cache.
  - ask: evidence draft commit?
    yes: Diagram agent feed git site evidence evidence static draft fast build.
    no: { then: start }
  - Agent render content fast commit markdown agent agent diagram cache git agent render site primitive draft.
  - ask: flow primitive yaml?
    yes:
      - Twin schema chart markdown spec build lint flow lint git review token primitive primitive commit content.
      - Draft feed evidence token static evidence commit cache site markdown yaml lint schema review primitive diagram cache.
    no: { then: fix }
  - { id: fix, do: Flow review token agent git token feed flow feed. }
  - Site feed evidence publish flow source spec theme yaml publish.
  - Chart review source theme commit agent feed diagram publish markdown theme static primitive markdown.
:::

:::faq
### twin primitive token theme?
Source git primitive render cache lint git twin render review.
### flow build draft flow?
Publish evidence cache agent token chart chart theme content token source cache diagram agent lint spec render spec git.
:::

::cta{title="twin theme static" button="Read the spec" href="https://snypd.rocks/spec"}
