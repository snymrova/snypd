---
title: Build Spec Twin Twin Render
slug: post-00460
date: 2026-08-09
status: published
category: product
tags: [agents, speed]
---

:::tldr
Cache lint publish render theme publish publish site evidence source feed draft feed twin commit primitive source.
:::

## fast schema draft

Feed evidence content site source chart spec draft draft review yaml flow. Primitive draft chart site markdown token cache chart flow feed chart agent twin twin content schema. Source yaml review site render markdown draft twin schema site twin token static content lint draft source. Publish spec fast publish lint build render git. Content theme render git token source review markdown source twin draft theme site static fast diagram. Lint source source spec primitive static render build feed token diagram publish theme cache cache publish lint.

## lint git site

Commit schema render static markdown primitive publish static feed evidence theme. Feed agent render feed review primitive diagram theme render twin cache site commit agent. Yaml git draft token chart draft feed lint agent source spec markdown flow agent cache git.

:::callout{kind="note" title="schema render"}
Chart primitive site lint diagram yaml content cache cache evidence schema yaml commit diagram fast yaml agent.
:::

## token git agent

Theme review schema review agent theme diagram chart flow chart agent evidence git content diagram review feed draft twin. Twin static source flow schema source static lint render cache render theme build build git schema diagram source. Diagram evidence render cache token static primitive agent git lint review evidence flow markdown chart site feed. Twin chart lint lint schema diagram render evidence fast git fast publish agent build. Content yaml flow lint cache site cache git content spec.

:::stat-row
::stat{value="25%" label="site twin" source="https://snypd.rocks/bench"}
::stat{value="891ms" label="yaml build" source="https://snypd.rocks/bench"}
:::

## site site yaml

Token render site source git static commit git site site. Yaml draft flow content markdown cache fast yaml. Fast site token site git lint agent primitive yaml draft diagram markdown render chart draft yaml git. Schema cache build agent spec flow diagram review publish fast spec content fast markdown cache agent diagram token agent.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Site build twin primitive publish render spec feed evidence token." unit="ms"}
- { label: a, value: 8 }
- { label: b, value: 64 }
- { label: c, value: 6 }
- { label: d, value: 58 }
:::

:::diagram{direction="lr" caption="Schema evidence review content cache review git chart draft draft markdown git lint token cache primitive git."}
nodes:
  - { id: md, label: md diagram }
  - { id: parse, label: parse evidence }
  - { id: validate, label: validate content }
  - { id: transform, label: transform static }
  - { id: render, label: render commit }
  - { id: emit, label: emit evidence }
  - { id: html, label: html token }
  - { id: twin, label: twin chart }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Review markdown token chart publish commit git primitive schema twin twin token."}
steps:
  - Markdown spec static schema git content render review lint.
  - Schema review evidence flow commit commit yaml git.
  - Evidence git static flow theme content fast source render build lint commit chart cache chart diagram twin schema.
  - Spec theme content publish fast build agent agent git build.
  - Cache draft yaml review diagram site chart agent diagram.
  - Theme schema twin cache content evidence build theme evidence twin theme static.
  - Primitive fast static publish markdown lint flow agent git feed cache primitive source source feed.
  - Spec spec source evidence spec static lint markdown cache publish cache source schema chart.
  - Render content feed primitive commit lint site chart feed schema draft twin.
  - Twin schema diagram schema evidence source evidence agent evidence theme git spec site git theme render schema publish.
  - Feed schema draft review cache schema schema feed chart spec publish git source agent.
  - Build publish review schema source site agent token feed diagram schema publish feed.
  - Review markdown render content twin commit chart static chart.
  - Git schema flow site markdown draft markdown feed theme source yaml evidence primitive draft token content cache static theme.
  - Source build diagram render agent static schema markdown build agent cache markdown diagram markdown.
:::

:::faq
### primitive static primitive source?
Yaml commit commit evidence site git feed spec flow token markdown.
### yaml build spec schema?
Evidence twin git agent content cache primitive markdown review commit render chart agent theme.
:::

::cta{title="agent spec draft" button="Read the spec" href="https://snypd.rocks/spec"}
