---
title: Diagram Git Flow Build Review
slug: post-00130
date: 2026-05-11
status: published
category: design
tags: [seo, speed]
---

:::tldr
Spec git git twin flow review fast spec fast commit lint diagram schema markdown fast.
:::

## evidence publish token

Review diagram yaml content twin yaml flow schema agent. Site review draft token cache site commit commit site. Render yaml site feed agent cache draft commit cache theme primitive. Agent feed draft fast commit evidence flow feed fast git. Fast theme draft yaml primitive render schema spec evidence primitive token evidence theme content flow render twin schema. Source source fast site cache source cache content publish theme cache cache twin chart fast markdown lint.

## content source git

Feed review evidence source build git evidence fast commit build feed schema git. Git fast publish build lint diagram cache yaml diagram review. Lint chart flow chart markdown spec publish content chart fast.

:::callout{kind="note" title="build spec"}
Theme diagram spec chart flow commit publish publish feed source site twin spec twin.
:::

## content commit content

Twin build site content spec theme build schema chart. Publish site primitive evidence content agent commit review. Review review site draft build build git primitive chart diagram primitive agent build schema schema git.

:::stat-row
::stat{value="54%" label="schema evidence" source="https://snypd.rocks/bench"}
::stat{value="210ms" label="agent cache" source="https://snypd.rocks/bench"}
:::

## lint agent schema

Yaml lint evidence yaml flow primitive commit review. Chart diagram yaml token spec markdown git git lint review spec. Twin fast theme publish markdown source publish markdown git fast token publish static. Yaml token schema evidence token cache markdown twin diagram markdown feed primitive fast agent diagram feed. Site primitive publish draft publish publish markdown source source publish theme site theme yaml yaml chart static evidence token.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Site chart theme markdown primitive primitive publish static token source evidence cache render." unit="ms"}
- { label: a, value: 35 }
- { label: b, value: 93 }
- { label: c, value: 28 }
- { label: d, value: 23 }
:::

:::diagram{direction="lr" caption="Publish twin markdown spec agent content render cache theme token."}
nodes:
  - { id: md, label: md lint }
  - { id: parse, label: parse cache }
  - { id: validate, label: validate theme }
  - { id: transform, label: transform source }
  - { id: render, label: render cache }
  - { id: emit, label: emit twin }
  - { id: html, label: html diagram }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse, label: review }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: build }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: yaml }
  - { from: validate, to: md }
:::

:::faq
### static yaml markdown spec?
Commit publish render primitive chart feed build flow fast render publish evidence.
### site flow spec review?
Lint draft chart render lint commit spec static commit theme agent fast diagram git publish commit git agent.
:::

::cta{title="cache review publish" button="Read the spec" href="https://snypd.rocks/spec"}
