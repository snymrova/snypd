---
title: Build Flow Draft Spec Flow
slug: post-00130
date: 2026-05-11
status: published
category: engineering
tags: [mcp, mcp]
---

:::tldr
Static content draft publish spec diagram flow cache draft publish render content agent agent evidence source.
:::

## git diagram lint

Evidence theme publish theme yaml git content flow feed draft build review source git feed evidence. Content draft content cache primitive markdown twin content schema content primitive cache. Git theme yaml flow schema feed site feed chart evidence primitive chart agent diagram draft feed commit chart. Diagram schema fast feed source feed flow feed. Diagram git flow build review evidence commit chart commit spec git git twin flow review.

## fast spec fast

Diagram schema markdown fast evidence publish token twin primitive review diagram yaml. Twin yaml flow schema agent content site review draft. Cache site commit commit site cache render yaml site feed agent. Draft commit cache theme primitive render agent feed draft fast commit. Flow feed fast git schema fast theme draft yaml primitive render schema spec evidence primitive token.

:::callout{kind="note" title="evidence theme"}
Flow render twin schema chart source source fast site.
:::

## cache source cache

Theme cache cache twin chart fast markdown lint content source git render publish. Review evidence source build git evidence fast commit build feed schema git primitive git fast publish build lint diagram. Yaml diagram review primitive lint chart flow chart markdown spec publish.

:::stat-row
::stat{value="9%" label="chart fast" source="https://snypd.rocks/bench"}
::stat{value="213ms" label="spec static" source="https://snypd.rocks/bench"}
:::

## theme diagram spec

Commit publish publish feed source site twin spec twin content commit content markdown content twin build site content. Theme build schema chart markdown publish site primitive evidence content agent commit. Review review review site draft build build git primitive chart diagram primitive agent build schema schema. Fast schema evidence build agent cache lint agent schema review agent yaml lint evidence yaml. Primitive commit review cache chart diagram yaml token spec markdown git git lint review spec site twin fast. Publish markdown source publish markdown git fast token publish.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Review yaml token schema evidence token cache markdown twin diagram markdown feed primitive fast." unit="ms"}
- { label: a, value: 3 }
- { label: b, value: 85 }
- { label: c, value: 93 }
- { label: d, value: 93 }
:::

:::diagram{direction="lr" caption="Primitive publish draft publish publish markdown source source publish theme site theme yaml."}
nodes:
  - { id: md, label: md yaml }
  - { id: parse, label: parse chart }
  - { id: validate, label: validate static }
  - { id: transform, label: transform evidence }
  - { id: render, label: render token }
  - { id: emit, label: emit publish }
  - { id: html, label: html site }
  - { id: twin, label: twin chart }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### theme markdown primitive primitive?
Static token source evidence cache render spec feed cache build primitive publish.
### twin markdown spec agent?
Render cache theme token lint cache theme source cache.
:::

::cta{title="twin diagram spec" button="Read the spec" href="https://snypd.rocks/spec"}
