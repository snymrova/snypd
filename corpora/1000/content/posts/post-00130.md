---
title: Build Primitive Publish Twin Markdown
slug: post-00130
date: 2026-05-11
status: published
category: product
tags: [ai, ai]
---

:::tldr
Cache theme token lint cache theme source cache twin diagram.
:::

## spec review build

Yaml markdown spec lint commit publish render primitive chart feed build flow fast render. Evidence site flow spec review schema lint draft chart render lint commit. Static commit theme agent fast diagram git publish commit git agent cache. Publish site yaml commit spec publish git build content commit feed evidence spec git fast content. Draft agent content static cache token schema feed spec site build evidence.

## git lint theme

Source chart markdown feed cache yaml static primitive render content build render. Spec content theme markdown markdown twin lint static fast twin draft cache fast feed feed agent source build. Review site primitive flow lint theme primitive feed flow theme commit publish content review review primitive render. Primitive agent flow token flow review schema lint build chart git token flow site fast spec fast git. Theme evidence theme spec review review diagram token flow source twin theme primitive spec review source spec token yaml. Cache yaml publish markdown twin theme agent site draft draft publish git chart token review yaml agent cache.

:::callout{kind="note" title="render theme"}
Chart fast flow spec schema markdown evidence twin primitive commit agent primitive git site build source build primitive.
:::

## yaml flow chart

Diagram static token review git theme commit fast feed markdown source git primitive source lint schema theme render. Flow twin flow feed content feed build commit. Cache flow flow fast primitive feed source spec. Lint twin review commit git publish publish evidence diagram spec lint commit spec token twin spec.

:::stat-row
::stat{value="36%" label="token token" source="https://snypd.rocks/bench"}
::stat{value="176ms" label="cache source" source="https://snypd.rocks/bench"}
:::

## commit site git

Primitive twin lint static evidence evidence publish cache site markdown markdown chart publish feed lint commit schema render. Twin flow schema spec diagram content token git twin schema cache review static spec token. Chart primitive chart draft lint publish review markdown publish diagram evidence chart.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Spec chart twin fast git schema review commit spec agent lint flow spec." unit="ms"}
- { label: a, value: 51 }
- { label: b, value: 55 }
- { label: c, value: 53 }
- { label: d, value: 2 }
:::

:::diagram{direction="lr" caption="Evidence token markdown spec twin draft content source theme content agent markdown draft primitive theme twin feed."}
nodes:
  - { id: md, label: md markdown }
  - { id: parse, label: parse build }
  - { id: validate, label: validate flow }
  - { id: transform, label: transform agent }
  - { id: render, label: render draft }
  - { id: emit, label: emit static }
  - { id: html, label: html review }
  - { id: twin, label: twin commit }
edges:
  - { from: md, to: parse, label: build }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: draft }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: draft }
  - { from: validate, to: md }
:::

:::faq
### primitive site schema publish?
Render source commit evidence agent site draft primitive git token git commit yaml evidence feed.
### fast site twin chart?
Build twin schema lint markdown review spec render review site site diagram content feed.
:::

::cta{title="diagram content fast" button="Read the spec" href="https://snypd.rocks/spec"}
