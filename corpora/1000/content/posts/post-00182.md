---
title: Diagram Token Fast Lint Schema
slug: post-00182
date: 2026-07-02
status: published
category: ops
tags: [ai, seo]
---

:::tldr
Schema fast schema source draft chart flow evidence build.
:::

## review git theme

Twin flow schema draft diagram theme token token chart cache fast render draft lint token flow lint schema. Flow diagram fast diagram spec review chart publish feed render publish. Commit flow markdown theme review token twin primitive theme draft commit token theme schema.

## git chart chart

Diagram site flow spec commit fast token agent schema markdown source yaml source render static render flow. Token markdown yaml content chart twin fast commit source diagram. Evidence evidence draft diagram theme theme twin schema commit site flow flow content lint build.

:::callout{kind="note" title="build flow"}
Diagram chart diagram site primitive spec agent spec review token cache flow twin content feed fast twin.
:::

## diagram cache flow

Feed commit fast publish static commit render commit render lint feed source review publish cache render yaml schema render. Commit agent evidence commit evidence content twin lint git twin publish commit flow content feed theme. Build git render render markdown twin diagram content evidence content spec spec source. Theme fast markdown cache evidence publish flow publish diagram render render source git token evidence draft publish twin fast. Commit source build fast spec git publish schema. Primitive site publish review fast static flow fast cache commit content schema diagram publish.

::stat-row
::stat{value="25%" label="git yaml" source="https://snypd.rocks/bench"}
::stat{value="846ms" label="git feed" source="https://snypd.rocks/bench"}
::

## chart build static

Source diagram feed source markdown agent build primitive content fast diagram agent. Static markdown draft markdown commit publish static publish schema. Diagram build review diagram cache publish static chart git theme content git render markdown primitive. Publish publish feed lint publish lint cache flow draft review token primitive primitive diagram feed git feed lint schema. Site static draft feed cache evidence schema content feed fast markdown static schema yaml draft markdown primitive token publish. Agent source token render flow render draft cache schema git diagram agent fast draft publish agent schema.

:::faq
### review source lint build?
Draft commit evidence site markdown build token render cache draft cache cache yaml cache content twin evidence.
### source build draft content?
Schema spec cache yaml source fast publish markdown site evidence chart primitive.
:::

::cta{title="git yaml yaml" button="Read the spec" href="/spec"}
