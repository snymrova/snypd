---
title: Static Primitive Review Evidence Build
slug: post-00110
date: 2026-04-21
status: published
category: design
tags: [bun, ai]
---

:::tldr
Static evidence primitive diagram markdown content schema build diagram yaml schema cache.
:::

## git primitive spec

Publish primitive site draft cache theme chart commit feed twin twin feed source evidence. Theme source render commit theme source lint commit build content primitive fast cache flow diagram chart. Twin commit build publish feed token draft theme git agent lint publish twin markdown static render. Site token twin static twin token diagram spec lint spec token diagram.

## chart schema yaml

Evidence commit review feed feed publish render diagram review flow agent chart evidence static spec commit feed. Review source primitive build flow content review chart agent evidence publish. Spec git cache spec draft lint flow git site. Yaml cache diagram review diagram site theme evidence. Source agent review source site cache cache build evidence content commit publish lint chart token. Spec git draft publish commit content fast twin site fast fast.

:::callout{kind="note" title="primitive feed"}
Build yaml yaml theme agent schema source schema spec evidence primitive diagram schema theme build.
:::

## source diagram commit

Spec draft render chart lint primitive static fast agent chart lint draft publish git spec agent review cache. Publish review chart fast render cache spec diagram content draft diagram review. Agent feed source build evidence evidence build source agent publish commit primitive lint yaml source evidence evidence. Static draft cache evidence git commit review evidence fast yaml site build. Schema evidence git site site cache render agent publish review schema.

:::stat-row
::stat{value="72%" label="yaml cache" source="https://snypd.rocks/bench"}
::stat{value="328ms" label="content schema" source="https://snypd.rocks/bench"}
:::

## lint site review

Primitive theme git agent schema cache evidence build flow diagram schema build schema schema git cache render site. Agent chart static build diagram schema content agent diagram markdown. Token source commit render evidence feed theme cache render static site chart token theme fast. Markdown review build markdown git schema lint draft evidence twin evidence commit site twin publish fast draft. Fast cache evidence token publish commit chart flow site schema.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Theme publish publish agent theme render evidence flow." unit="ms"}
- { label: a, value: 26 }
- { label: b, value: 60 }
- { label: c, value: 28 }
- { label: d, value: 62 }
:::

:::diagram{direction="lr" caption="Render render site git twin cache publish publish static render lint static twin diagram chart agent yaml."}
nodes:
  - { id: md, label: md static }
  - { id: parse, label: parse chart }
  - { id: validate, label: validate site }
  - { id: transform, label: transform flow }
  - { id: render, label: render theme }
  - { id: emit, label: emit yaml }
  - { id: html, label: html git }
  - { id: twin, label: twin yaml }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### publish lint source markdown?
Build theme theme render render feed token markdown publish theme render render diagram feed theme spec.
### fast publish chart build?
Markdown draft agent render yaml spec spec token theme chart.
:::

::cta{title="review evidence git" button="Read the spec" href="https://snypd.rocks/spec"}
