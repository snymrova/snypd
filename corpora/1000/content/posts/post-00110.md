---
title: Review Flow Agent Chart Evidence
slug: post-00110
date: 2026-04-21
status: published
category: design
tags: [mcp, seo]
---

:::tldr
Token review source primitive build flow content review chart agent evidence publish content spec git cache spec draft lint.
:::

## flow git site

Cache diagram review diagram site theme evidence commit source agent review source site cache cache. Evidence content commit publish lint chart token cache spec git. Publish commit content fast twin site fast fast primitive feed commit build yaml.

## yaml theme agent

Schema spec evidence primitive diagram schema theme build source diagram commit commit flow spec draft render chart. Primitive static fast agent chart lint draft publish git spec agent review. Lint publish review chart fast render cache spec diagram content draft. Review chart agent feed source build evidence evidence build source agent publish commit primitive lint yaml source evidence. Publish static draft cache evidence git commit review evidence fast yaml site build cache schema evidence. Site site cache render agent publish review schema evidence yaml cache lint content schema lint.

:::callout{kind="note" title="site review"}
Schema primitive theme git agent schema cache evidence build flow diagram schema build schema.
:::

## schema git cache

Render agent chart static build diagram schema content agent diagram markdown git token. Commit render evidence feed theme cache render static site chart token theme fast source markdown review build. Git schema lint draft evidence twin evidence commit.

:::stat-row
::stat{value="46%" label="twin publish" source="https://snypd.rocks/bench"}
::stat{value="507ms" label="draft render" source="https://snypd.rocks/bench"}
:::

## fast cache evidence

Commit chart flow site schema agent theme publish publish agent theme render. Flow cache yaml cache git chart render render site git twin cache publish publish static render. Static twin diagram chart agent yaml static chart site flow theme yaml. Yaml publish lint source markdown evidence build theme theme render render feed token markdown publish.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Render render diagram feed theme spec fast publish chart." unit="ms"}
- { label: a, value: 22 }
- { label: b, value: 17 }
- { label: c, value: 6 }
- { label: d, value: 42 }
:::

:::diagram{direction="lr" caption="Render yaml spec spec token theme chart review."}
nodes:
  - { id: md, label: md evidence }
  - { id: parse, label: parse git }
  - { id: validate, label: validate site }
  - { id: transform, label: transform feed }
  - { id: render, label: render yaml }
  - { id: emit, label: emit chart }
  - { id: html, label: html markdown }
  - { id: twin, label: twin primitive }
edges:
  - { from: md, to: parse, label: draft }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: commit }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: site }
  - { from: validate, to: md }
:::

:::faq
### fast site chart schema?
Flow static publish review token commit chart chart render primitive commit schema static publish.
### flow cache source commit?
Feed content feed publish static draft lint theme agent agent build feed git.
:::

::cta{title="flow flow agent" button="Read the spec" href="https://snypd.rocks/spec"}
