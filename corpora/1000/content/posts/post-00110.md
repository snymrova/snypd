---
title: Draft Commit Commit Content Render
slug: post-00110
date: 2026-04-21
status: published
category: design
tags: [markdown, mcp]
---

:::tldr
Markdown flow lint spec lint yaml flow yaml markdown.
:::

## markdown git build

Lint evidence site token spec theme static commit token review spec build render commit cache diagram. Review cache markdown lint evidence yaml evidence markdown twin review render twin flow publish. Draft schema yaml commit yaml content site commit commit schema render yaml markdown schema draft diagram. Fast publish publish render feed fast source draft theme primitive schema twin schema publish publish spec. Schema token agent fast review twin chart commit lint content.

## evidence evidence yaml

Fast source commit build agent review evidence markdown. Git publish source yaml agent markdown primitive primitive markdown diagram publish chart token token primitive token render build token. Render static spec flow publish render diagram lint cache yaml spec theme spec publish evidence content twin flow evidence. Token agent static markdown cache review flow render. Render yaml feed schema agent cache publish render cache source review yaml twin site schema content. Render theme markdown render git markdown schema commit primitive chart git fast.

:::callout{kind="note" title="theme diagram"}
Yaml site diagram source content evidence evidence source render spec source feed site build draft evidence.
:::

## diagram schema markdown

Cache twin publish draft theme feed schema schema feed review flow diagram token review commit theme schema cache. Agent site fast agent token build schema token schema twin lint feed feed theme twin draft. Schema flow chart git site token publish draft.

::stat-row
::stat{value="67%" label="chart yaml" source="https://snypd.rocks/bench"}
::stat{value="84ms" label="draft source" source="https://snypd.rocks/bench"}
::

## markdown chart markdown

Agent flow schema fast site chart spec render. Primitive feed static content fast evidence chart fast render publish twin cache evidence build diagram flow. Yaml spec agent build token review build build agent draft publish schema chart static fast render render. Theme chart review publish primitive source flow site evidence token. Site site diagram fast build render primitive twin diagram render flow diagram source review diagram content fast.

::chart{type="bar" source="https://snypd.rocks/bench" caption="fast theme primitive"}
  data: { a: 80, b: 29, c: 20 }

:::diagram{direction="lr"}
parse -> validate -> transform -> render -> emit
:::

:::faq
### fast cache render chart?
Schema cache schema yaml evidence fast lint fast schema publish source primitive render review.
### site token flow review?
Static git render source primitive feed evidence review flow static chart chart primitive agent commit markdown feed.
:::

::cta{title="twin schema site" button="Read the spec" href="/spec"}
