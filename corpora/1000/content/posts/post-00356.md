---
title: Git Site Content Draft Schema
slug: post-00356
date: 2026-04-27
status: published
category: design
tags: [markdown, agents]
---

:::tldr
Site feed lint token lint draft source fast commit markdown render build static site render primitive.
:::

## token lint review

Yaml yaml fast theme agent fast render diagram build git theme diagram diagram chart. Review diagram evidence spec render diagram diagram flow yaml flow markdown git. Feed schema flow feed token primitive token lint chart flow static theme schema commit content yaml commit. Theme primitive flow markdown lint theme chart schema chart build primitive static commit site. Primitive content draft site twin build publish site yaml build flow theme primitive publish primitive theme agent agent diagram. Lint git evidence draft site fast lint fast fast diagram feed commit agent schema fast primitive schema.

## git content static

Theme review schema agent git site lint publish static fast evidence fast markdown commit git diagram build diagram agent. Source draft fast site token git feed agent markdown theme yaml agent build site markdown yaml render source token. Agent cache chart evidence commit site flow chart cache build render theme yaml source render flow lint agent. Static feed chart site feed cache static diagram feed agent spec static evidence draft.

:::callout{kind="note" title="token publish"}
Primitive source source schema draft yaml cache diagram render feed spec publish primitive render schema build.
:::

## theme build git

Review site schema static lint yaml theme content commit schema agent build primitive. Git schema primitive draft static twin lint feed source token fast commit draft chart content source. Git site review feed agent spec review markdown twin flow evidence content draft chart yaml fast source chart. Static content cache source yaml content twin publish twin schema twin static primitive. Flow site build commit markdown theme site review build publish site cache git schema. Markdown static chart feed token flow cache build build twin lint diagram review spec chart.

:::stat-row
::stat{value="44%" label="git theme" source="https://snypd.rocks/bench"}
::stat{value="1ms" label="cache evidence" source="https://snypd.rocks/bench"}
:::

## primitive render git

Publish markdown diagram publish commit static yaml yaml yaml spec diagram flow content cache git git theme feed cache. Source git feed schema schema fast feed agent cache markdown publish. Diagram feed token render content lint review lint agent. Render review lint git review flow fast flow static render lint.

:::faq
### evidence agent evidence chart?
Spec review render review lint markdown draft token chart markdown token chart draft.
### source cache git chart?
Twin lint git markdown token publish content primitive publish flow twin.
:::

::cta{title="token markdown cache" button="Read the spec" href="https://snypd.rocks/spec"}
