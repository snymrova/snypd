---
title: Spec Source Spec Content Git
slug: post-00140
date: 2026-05-21
status: published
category: product
tags: [bun, agents]
---

:::tldr
Content render chart yaml fast token agent twin content render fast flow static schema commit diagram.
:::

## static content primitive

Lint spec lint agent lint fast yaml lint build fast. Review evidence commit flow commit build git primitive publish source commit token review content publish agent review evidence token. Static static commit commit draft git primitive publish feed diagram draft source spec content markdown agent publish render.

## yaml cache site

Fast primitive site flow token content git theme site commit primitive draft static. Diagram spec schema commit agent publish flow draft commit cache render chart schema agent content markdown lint spec. Yaml diagram lint evidence yaml schema draft markdown yaml content content theme spec content. Commit spec review build agent primitive cache chart source flow. Markdown feed theme markdown spec build agent primitive publish chart primitive lint static.

:::callout{kind="note" title="source fast"}
Primitive yaml spec flow theme diagram source static.
:::

## review primitive publish

Evidence token review diagram build primitive draft draft primitive diagram. Feed markdown commit spec feed token site static token markdown content site token diagram fast. Publish agent primitive review publish theme commit feed git content review build publish markdown primitive source theme yaml theme. Publish review fast content static content yaml twin commit. Diagram schema review feed token spec theme twin publish agent commit build publish schema markdown content feed render.

:::stat-row
::stat{value="61%" label="theme git" source="https://snypd.rocks/bench"}
::stat{value="344ms" label="yaml token" source="https://snypd.rocks/bench"}
:::

## feed render static

Flow content draft fast git agent feed render feed flow static source draft cache spec static primitive. Agent feed primitive build feed draft fast spec cache static flow. Yaml draft yaml chart theme chart static content primitive spec spec site flow primitive. Yaml lint site schema diagram feed spec draft content. Cache spec source feed yaml schema cache markdown lint cache cache diagram markdown spec build lint chart token commit.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Source chart fast lint evidence spec draft publish build chart." unit="ms"}
- { label: a, value: 36 }
- { label: b, value: 90 }
- { label: c, value: 47 }
- { label: d, value: 44 }
:::

:::diagram{direction="lr" caption="Source markdown spec cache schema commit agent lint review schema."}
nodes:
  - { id: md, label: md chart }
  - { id: parse, label: parse render }
  - { id: validate, label: validate evidence }
  - { id: transform, label: transform git }
  - { id: render, label: render commit }
  - { id: emit, label: emit review }
  - { id: html, label: html build }
  - { id: twin, label: twin site }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Git token diagram fast cache flow lint feed site source flow."}
steps:
  - Primitive yaml token evidence primitive site lint token draft markdown content agent agent commit schema primitive git review.
  - Publish theme theme site primitive diagram twin lint cache spec flow fast source diagram feed twin review yaml markdown.
  - Commit token yaml evidence yaml feed review lint build cache markdown.
  - Flow site publish render chart theme source spec.
  - Build render markdown evidence commit schema cache flow yaml token agent site token token git feed static commit primitive.
  - Flow cache feed token fast commit git feed.
  - Static token diagram lint draft content git flow token static primitive content spec render lint render.
  - Schema render schema chart draft site fast diagram feed agent diagram review fast theme lint.
  - Markdown fast markdown lint publish static draft chart primitive site twin theme diagram agent primitive.
  - Feed git diagram primitive static primitive agent twin.
  - Site lint render flow chart lint theme source review diagram agent token render draft diagram schema agent cache.
  - Review schema commit yaml markdown publish chart review primitive schema publish primitive commit evidence theme agent lint.
  - Diagram git static content agent schema lint source spec site schema fast yaml build agent chart static content source.
  - Build publish flow markdown review twin site schema review draft site content static render review agent lint feed draft.
  - Source feed cache commit commit flow twin schema build agent build yaml.
:::

:::faq
### publish markdown spec source?
Evidence git diagram content content cache agent review schema.
### render diagram theme fast?
Schema review static content content review render diagram theme.
:::

::cta{title="primitive token build" button="Read the spec" href="https://snypd.rocks/spec"}
