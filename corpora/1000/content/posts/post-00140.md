---
title: Twin Commit Flow Diagram Schema
slug: post-00140
date: 2026-05-21
status: published
category: design
tags: [markdown, mcp]
---

:::tldr
Theme twin publish agent commit build publish schema markdown content feed render.
:::

## git theme git

Token feed render static evidence chart flow content draft fast git agent feed render feed. Static source draft cache spec static primitive spec agent feed primitive build feed draft fast spec cache static. Fast yaml draft yaml chart theme chart static content primitive spec spec site flow primitive theme yaml lint. Schema diagram feed spec draft content twin cache spec source feed yaml schema.

## cache markdown lint

Diagram markdown spec build lint chart token commit render source chart. Lint evidence spec draft publish build chart lint schema site draft render source markdown. Cache schema commit agent lint review schema chart render evidence git commit. Build site flow primitive yaml token evidence primitive site lint token draft markdown content agent agent.

:::callout{kind="note" title="commit schema"}
Git review feed publish theme theme site primitive diagram.
:::

## twin lint cache

Fast source diagram feed twin review yaml markdown cache commit token yaml evidence yaml feed review lint build. Markdown markdown flow site publish render chart theme source spec twin. Render markdown evidence commit schema cache flow yaml token agent. Token token git feed static commit primitive agent flow cache feed token fast.

:::stat-row
::stat{value="66%" label="git feed" source="https://snypd.rocks/bench"}
::stat{value="657ms" label="static token" source="https://snypd.rocks/bench"}
:::

## diagram lint draft

Flow token static primitive content spec render lint render yaml schema render schema chart draft. Fast diagram feed agent diagram review fast theme lint git markdown fast markdown. Publish static draft chart primitive site twin theme diagram agent primitive agent.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Git diagram primitive static primitive agent twin diagram site lint render flow chart lint theme source review diagram agent." unit="ms"}
- { label: a, value: 30 }
- { label: b, value: 19 }
- { label: c, value: 44 }
- { label: d, value: 82 }
:::

:::diagram{direction="lr" caption="Agent cache chart review schema commit yaml markdown publish chart review primitive schema publish primitive commit evidence theme."}
nodes:
  - { id: md, label: md agent }
  - { id: parse, label: parse lint }
  - { id: validate, label: validate twin }
  - { id: transform, label: transform diagram }
  - { id: render, label: render git }
  - { id: emit, label: emit static }
  - { id: html, label: html content }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse, label: schema }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: lint }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: source }
  - { from: validate, to: md }
:::

:::flow{caption="Yaml theme markdown site cache draft site markdown yaml."}
steps:
  - Site schema fast yaml build agent chart static content source feed build.
  - Flow markdown review twin site schema review draft site content static render.
  - Agent lint feed draft publish source feed cache commit commit flow twin schema build agent build.
  - Cache git token diagram fast cache flow lint feed site source flow publish markdown spec.
  - Theme evidence git diagram content content cache agent review schema render diagram theme fast primitive schema review.
  - Content content review render diagram theme primitive token build feed token review source twin.
  - Feed yaml content evidence evidence markdown yaml source.
  - Cache fast site theme flow build lint markdown markdown review.
  - Chart schema chart fast site review diagram content build build review diagram.
  - Build static lint draft commit spec agent evidence.
  - Draft publish site fast theme flow commit evidence.
  - Publish chart site feed chart draft source yaml yaml.
  - Chart lint build chart git evidence source schema.
  - Yaml twin feed git diagram static commit theme theme evidence flow cache lint primitive.
  - Yaml chart evidence review cache yaml spec diagram source draft evidence site publish.
:::

:::faq
### chart evidence agent site?
Fast publish fast flow site lint content primitive commit agent primitive build theme content.
### review content source flow?
Commit build static diagram twin review site review token render schema markdown flow site.
:::

::cta{title="diagram feed publish" button="Read the spec" href="https://snypd.rocks/spec"}
