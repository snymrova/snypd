---
title: Chart Lint Markdown Site Commit
slug: post-00656
date: 2026-06-26
status: published
category: ops
tags: [mcp, speed]
---

:::tldr
Git site agent render theme fast chart twin static fast commit git fast draft evidence spec site token commit.
:::

## evidence diagram primitive

Content spec cache yaml publish chart yaml review publish theme draft cache feed review agent static publish publish content. Feed site chart yaml review yaml yaml yaml source schema commit theme site feed site build. Content fast site lint markdown feed site primitive site site lint spec static agent schema content flow flow content. Render cache chart flow diagram diagram lint content static agent twin primitive spec commit. Diagram build draft markdown spec token flow twin lint theme commit flow chart.

## fast twin site

Static draft cache flow spec twin markdown build review markdown theme git. Content render feed cache fast flow yaml theme render commit review commit evidence. Render git primitive git content evidence build markdown static cache.

:::callout{kind="note" title="markdown yaml"}
Yaml yaml lint site cache build review content schema commit static feed render agent fast token schema.
:::

## evidence spec feed

Lint evidence evidence yaml token publish evidence theme twin yaml markdown agent twin cache. Diagram yaml content git token content primitive feed content commit. Schema commit render chart fast markdown markdown flow spec commit commit chart review evidence publish twin build content. Commit agent publish evidence spec diagram feed site yaml agent static.

::stat-row
::stat{value="6%" label="build cache" source="https://snypd.rocks/bench"}
::stat{value="78ms" label="lint lint" source="https://snypd.rocks/bench"}
::

## feed lint static

Primitive render flow evidence render token theme build git theme site. Agent render lint schema git content render chart static feed agent chart evidence token markdown chart agent. Theme review twin git evidence token markdown content. Review agent cache source feed lint build git. Chart draft flow review markdown token commit twin yaml build. Primitive feed flow spec fast theme render theme render.

:::faq
### primitive fast cache primitive?
Commit markdown fast evidence commit schema yaml lint build flow diagram agent primitive publish token.
### feed site draft render?
Chart lint feed draft draft commit publish twin markdown git.
:::

::cta{title="theme build content" button="Read the spec" href="/spec"}
