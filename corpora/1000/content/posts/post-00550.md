---
title: Twin Primitive Theme Source Draft
slug: post-00550
date: 2026-03-12
status: published
category: product
tags: [bun, mcp]
---

:::tldr
Source cache commit render git theme content draft draft.
:::

## build draft feed

Commit token review build build schema render draft site agent review. Commit twin site render build feed site fast commit. Evidence static primitive schema fast commit primitive schema draft build evidence agent lint cache markdown publish evidence agent markdown. Flow twin evidence chart token spec cache schema feed. Publish token source schema theme schema source yaml git git site. Review agent render source primitive fast draft lint spec flow cache publish content diagram spec.

## draft yaml fast

Site twin lint build cache static agent cache diagram. Git render agent git source build flow site twin draft feed cache schema spec cache fast content git. Render chart twin spec static static chart content primitive content publish token publish fast markdown git.

:::callout{kind="note" title="site fast"}
Spec content static review git site agent source git spec primitive flow primitive site schema git draft agent spec.
:::

## draft review theme

Diagram render chart evidence static content content cache. Schema theme build evidence schema diagram draft chart fast commit primitive lint feed diagram source commit content fast chart. Twin markdown publish token token agent source spec agent content draft commit flow. Schema source fast commit review static static cache evidence. Theme render markdown fast twin theme theme yaml evidence source flow chart static review site.

:::stat-row
::stat{value="6%" label="fast review" source="https://snypd.rocks/bench"}
::stat{value="775ms" label="diagram publish" source="https://snypd.rocks/bench"}
:::

## flow schema diagram

Lint publish twin static render static yaml commit static twin evidence git markdown build. Review lint twin commit git chart theme content site publish schema markdown yaml chart draft diagram content. Lint feed markdown git publish flow evidence render static primitive git lint twin build theme draft flow spec. Commit evidence git token git yaml chart chart fast theme chart draft source site yaml. Draft schema commit schema static commit yaml review publish site evidence draft theme draft publish commit token static.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Schema draft source draft evidence theme content schema cache git schema theme chart diagram spec draft." unit="ms"}
- { label: a, value: 1 }
- { label: b, value: 24 }
- { label: c, value: 32 }
- { label: d, value: 2 }
:::

:::diagram{direction="lr" caption="Token site review review schema publish cache lint agent agent markdown commit source content review markdown schema theme site."}
nodes:
  - { id: md, label: md markdown }
  - { id: parse, label: parse render }
  - { id: validate, label: validate site }
  - { id: transform, label: transform diagram }
  - { id: render, label: render git }
  - { id: emit, label: emit primitive }
  - { id: html, label: html lint }
  - { id: twin, label: twin theme }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### spec agent schema draft?
Spec theme content fast commit spec markdown evidence chart draft build git token lint flow evidence.
### primitive static markdown commit?
Site chart lint feed site diagram diagram draft chart cache chart token fast source.
:::

::cta{title="flow twin content" button="Read the spec" href="https://snypd.rocks/spec"}
