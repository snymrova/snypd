---
title: Lint Schema Theme Spec Commit
slug: post-00550
date: 2026-03-12
status: published
category: ops
tags: [mcp, markdown]
---

:::tldr
Cache site flow flow primitive content cache source primitive spec source.
:::

## yaml token commit

Commit git static spec review fast spec markdown markdown token yaml theme lint git. Build commit cache content publish lint twin flow source content feed static fast token source build draft build. Flow publish evidence token evidence content chart lint flow publish theme token evidence git chart build token. Evidence render agent token primitive agent diagram theme twin site publish spec commit. Fast content evidence agent static primitive content primitive theme publish diagram schema site cache.

## flow git chart

Yaml agent draft feed git fast commit render content primitive cache review git token token schema chart yaml publish. Draft source spec static site agent git fast build agent token cache source content schema schema schema fast cache. Content publish feed spec git draft render twin theme markdown evidence draft fast token commit review token yaml.

:::callout{kind="note" title="theme primitive"}
Lint cache source spec flow source schema render yaml cache diagram schema primitive spec.
:::

## render primitive theme

Token diagram commit build twin flow schema cache review draft. Lint feed git markdown lint fast source feed. Render draft fast publish cache git source flow publish token diagram feed chart site fast markdown. Evidence agent spec chart evidence agent spec source chart site evidence primitive content markdown build review feed static agent.

:::stat-row
::stat{value="10%" label="draft commit" source="https://snypd.rocks/bench"}
::stat{value="553ms" label="theme markdown" source="https://snypd.rocks/bench"}
:::

## agent feed publish

Spec token git render agent source review chart draft primitive static publish twin fast. Chart diagram primitive static token diagram cache diagram git primitive fast diagram evidence lint. Site source commit spec diagram git fast twin chart spec site chart schema twin chart site publish lint.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Feed twin primitive review commit evidence git flow evidence feed content site." unit="ms"}
- { label: a, value: 15 }
- { label: b, value: 23 }
- { label: c, value: 60 }
- { label: d, value: 1 }
:::

:::diagram{direction="lr" caption="Source site yaml fast review review flow source cache token lint git draft review draft feed git."}
nodes:
  - { id: md, label: md build }
  - { id: parse, label: parse publish }
  - { id: validate, label: validate theme }
  - { id: transform, label: transform build }
  - { id: render, label: render primitive }
  - { id: emit, label: emit flow }
  - { id: html, label: html render }
  - { id: twin, label: twin markdown }
edges:
  - { from: md, to: parse, label: cache }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: spec }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: render }
  - { from: validate, to: md }
:::

:::faq
### agent chart yaml twin?
Cache fast build agent lint static static yaml evidence site twin chart.
### agent theme render publish?
Publish theme flow flow twin markdown markdown static primitive.
:::

::cta{title="site source yaml" button="Read the spec" href="https://snypd.rocks/spec"}
