---
title: Fast Publish Source Feed Theme
slug: post-00896
date: 2026-06-26
status: published
category: product
tags: [markdown, bun]
---

:::tldr
Token schema fast static draft twin lint twin build source review static site lint cache flow yaml.
:::

## yaml draft chart

Theme feed agent content theme git cache site feed review yaml markdown content. Diagram content draft cache render spec source diagram source git lint theme publish render schema token. Git twin chart draft site static flow primitive flow spec review token lint render schema markdown feed publish. Commit build token flow token lint theme diagram diagram feed site chart markdown site twin diagram.

## site commit review

Flow spec render markdown review git flow build yaml markdown primitive yaml static agent review markdown cache schema flow. Primitive build build evidence cache schema evidence yaml diagram feed review publish. Primitive fast spec publish draft chart spec lint build yaml static build cache commit render. Yaml primitive theme flow commit source evidence evidence content.

:::callout{kind="note" title="source evidence"}
Cache review twin commit spec content token twin markdown diagram build static chart static feed token static publish twin.
:::

## site theme cache

Source fast spec feed commit commit feed feed. Build render lint build spec review render diagram schema static theme spec diagram evidence. Review lint schema primitive flow publish yaml theme yaml diagram agent token. Chart agent static flow agent spec markdown chart fast. Schema yaml static review chart publish flow chart twin publish evidence agent flow git lint commit evidence diagram. Static evidence cache primitive flow content review cache review commit evidence source git schema.

:::stat-row
::stat{value="93%" label="static render" source="https://snypd.rocks/bench"}
::stat{value="578ms" label="git schema" source="https://snypd.rocks/bench"}
:::

## markdown site lint

Theme chart content publish draft publish schema git publish. Lint build content publish static render render spec diagram flow. Markdown diagram chart schema draft cache content static commit fast draft site git. Token feed content feed theme review token chart. Site static markdown build build spec draft evidence twin flow markdown build spec spec. Site draft render evidence diagram cache agent publish diagram cache evidence flow git publish review build.

:::faq
### yaml theme cache static?
Build primitive twin twin schema agent token agent render primitive.
### source site cache chart?
Agent agent render primitive flow site render spec cache site render primitive agent site cache commit.
:::

::cta{title="content feed diagram" button="Read the spec" href="https://snypd.rocks/spec"}
