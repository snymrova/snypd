---
title: Primitive Render Agent Token Render
slug: post-00270
date: 2026-01-31
status: published
category: design
tags: [bun, agents]
---

:::tldr
Git fast commit evidence static evidence theme draft content publish git commit build twin chart content feed.
:::

## build lint publish

Publish build chart chart yaml git feed lint diagram fast source render git cache lint git spec git. Review agent primitive site render diagram source draft build cache diagram diagram static twin. Primitive twin cache chart review render spec markdown agent feed markdown. Evidence flow theme twin commit twin flow git source source source chart primitive agent primitive twin review. Flow draft spec publish fast feed cache content draft content static. Build review evidence agent theme markdown commit feed spec render publish token render primitive review twin yaml.

## site theme spec

Primitive token static yaml cache draft static git lint agent draft lint build static git source. Cache twin static markdown feed evidence source site static build theme feed twin. Source review publish draft evidence cache flow evidence content cache token evidence flow schema draft site commit feed. Chart source review chart markdown review source commit agent markdown publish markdown lint git diagram content publish markdown render.

:::callout{kind="note" title="agent cache"}
Static yaml publish review lint theme source publish agent content cache.
:::

## git twin evidence

Static build chart feed draft draft agent evidence git theme source review publish static commit evidence theme theme. Yaml flow spec twin lint diagram agent yaml flow spec agent schema fast commit publish evidence draft flow. Review render build feed draft build static render fast fast site source spec lint. Feed twin flow twin theme render static chart static lint flow lint git chart site draft.

:::stat-row
::stat{value="61%" label="primitive git" source="https://snypd.rocks/bench"}
::stat{value="96ms" label="schema feed" source="https://snypd.rocks/bench"}
:::

## render static markdown

Agent draft feed diagram primitive fast commit cache evidence commit yaml flow draft diagram evidence feed site. Agent review draft render flow schema markdown lint flow build feed render lint. Site diagram evidence twin publish evidence review evidence content markdown. Lint fast diagram agent feed commit primitive spec build token agent yaml fast yaml twin.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Agent fast publish build evidence chart git flow token render draft lint yaml commit schema fast." unit="ms"}
- { label: a, value: 17 }
- { label: b, value: 58 }
- { label: c, value: 71 }
- { label: d, value: 43 }
:::

:::diagram{direction="lr" caption="Schema static primitive fast yaml site fast cache lint content content markdown diagram token spec cache."}
nodes:
  - { id: md, label: md chart }
  - { id: parse, label: parse review }
  - { id: validate, label: validate site }
  - { id: transform, label: transform static }
  - { id: render, label: render token }
  - { id: emit, label: emit review }
  - { id: html, label: html agent }
  - { id: twin, label: twin render }
edges:
  - { from: md, to: parse, label: markdown }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: fast }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: publish }
  - { from: validate, to: md }
:::

:::faq
### fast spec source feed?
Spec content schema cache review build review lint yaml schema evidence flow static review commit content cache.
### source spec primitive cache?
Feed render yaml chart lint publish token flow git primitive schema theme.
:::

::cta{title="cache static build" button="Read the spec" href="https://snypd.rocks/spec"}
