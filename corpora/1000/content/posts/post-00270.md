---
title: Site Fast Markdown Chart Markdown
slug: post-00270
date: 2026-01-31
status: published
category: ops
tags: [cms, bun]
---

:::tldr
Review theme commit flow twin diagram git theme.
:::

## cache site spec

Build agent static evidence site token spec token. Schema git site build primitive markdown draft primitive site source evidence content site agent. Build diagram cache lint agent feed draft twin source primitive cache. Twin site markdown flow flow review evidence site primitive. Git build theme markdown evidence lint cache diagram content fast review build git flow twin. Twin twin feed cache yaml twin render build twin.

## chart cache chart

Render source agent review evidence chart review build schema. Cache cache static review lint git lint evidence primitive review publish source yaml. Feed site feed token yaml schema site primitive render. Token render evidence site render source git fast.

:::callout{kind="note" title="commit evidence"}
Evidence theme draft content publish git commit build twin chart content feed build lint.
:::

## publish feed flow

Chart chart yaml git feed lint diagram fast source render. Cache lint git spec git yaml review agent primitive site render diagram source draft build. Diagram diagram static twin cache primitive twin cache chart review render. Markdown agent feed markdown chart evidence flow theme twin commit twin.

:::stat-row
::stat{value="87%" label="git source" source="https://snypd.rocks/bench"}
::stat{value="697ms" label="source chart" source="https://snypd.rocks/bench"}
:::

## primitive agent primitive

Cache flow draft spec publish fast feed cache content draft content static source build review evidence. Theme markdown commit feed spec render publish token. Primitive review twin yaml site theme spec draft evidence primitive. Static yaml cache draft static git lint agent draft lint build. Git source draft cache twin static markdown feed evidence source site static build theme. Twin diagram source review publish draft evidence cache flow evidence content cache token evidence flow schema draft site commit.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Feed chart source review chart markdown review source commit agent markdown publish markdown lint git diagram content publish markdown." unit="ms"}
- { label: a, value: 19 }
- { label: b, value: 1 }
- { label: c, value: 26 }
- { label: d, value: 33 }
:::

:::diagram{direction="lr" caption="Yaml publish review lint theme source publish agent content cache git twin evidence spec."}
nodes:
  - { id: md, label: md flow }
  - { id: parse, label: parse static }
  - { id: validate, label: validate build }
  - { id: transform, label: transform chart }
  - { id: render, label: render feed }
  - { id: emit, label: emit draft }
  - { id: html, label: html draft }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### evidence git theme source?
Publish static commit evidence theme theme diagram yaml flow spec twin lint diagram agent yaml flow.
### spec agent schema fast?
Publish evidence draft flow fast review render build feed draft build static render fast fast.
:::

::cta{title="site source spec" button="Read the spec" href="https://snypd.rocks/spec"}
