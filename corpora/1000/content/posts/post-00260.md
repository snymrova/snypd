---
title: Git Spec Feed Spec Primitive
slug: post-00260
date: 2026-01-21
status: published
category: product
tags: [mcp, speed]
---

:::tldr
Draft feed lint cache site chart cache site source flow draft flow token.
:::

## fast cache agent

Theme feed fast evidence yaml markdown fast git review. Render agent feed feed schema commit theme theme source. Lint static site publish cache static flow static schema draft review draft review evidence publish.

## theme review lint

Feed review cache fast git cache lint yaml site primitive spec fast feed draft review diagram. Feed spec markdown cache chart theme agent schema schema agent primitive primitive token build fast chart fast. Theme markdown static diagram cache chart twin twin site evidence token commit schema static review. Theme feed yaml markdown review token spec draft build static render. Cache fast site render twin cache diagram static lint agent fast site primitive.

:::callout{kind="note" title="token schema"}
Evidence feed token chart cache source yaml diagram review markdown draft.
:::

## token render commit

Markdown agent primitive draft spec commit lint spec draft lint diagram. Content token draft static feed cache primitive chart theme render cache fast. Feed schema build review markdown render evidence evidence build publish static theme.

:::stat-row
::stat{value="67%" label="publish diagram" source="https://snypd.rocks/bench"}
::stat{value="310ms" label="publish twin" source="https://snypd.rocks/bench"}
:::

## markdown site schema

Source review static static cache token spec static chart git build diagram lint diagram. Fast lint draft render site build theme lint agent build theme git draft review spec yaml diagram primitive. Evidence render diagram publish build fast diagram diagram.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Fast publish chart content spec publish render token static spec diagram evidence static schema." unit="ms"}
- { label: a, value: 50 }
- { label: b, value: 87 }
- { label: c, value: 41 }
- { label: d, value: 76 }
:::

:::diagram{direction="lr" caption="Evidence fast chart fast cache site draft markdown evidence twin lint theme primitive feed."}
nodes:
  - { id: md, label: md spec }
  - { id: parse, label: parse lint }
  - { id: validate, label: validate evidence }
  - { id: transform, label: transform site }
  - { id: render, label: render fast }
  - { id: emit, label: emit evidence }
  - { id: html, label: html primitive }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse, label: commit }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: review }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: spec }
  - { from: validate, to: md }
:::

:::flow{caption="Chart review schema schema lint source content twin site diagram spec fast flow review commit draft source."}
steps:
  - { id: start, do: Markdown render git fast fast content chart feed publish markdown. }
  - Git feed theme source publish commit review publish source token yaml theme theme.
  - ask: build yaml evidence?
    yes: Twin source site cache commit evidence cache theme lint cache flow markdown token publish build.
    no: { then: start }
  - Draft content site content git theme draft site review site.
  - ask: static site fast?
    yes:
      - Commit git yaml static git agent token feed schema schema cache diagram yaml yaml yaml chart chart.
      - Feed markdown yaml primitive review primitive markdown git evidence commit lint chart git.
    no: { then: fix }
  - { id: fix, do: Commit twin diagram spec static yaml content flow feed feed evidence cache. }
  - Feed markdown content spec flow cache chart lint build source evidence source schema markdown static feed cache lint draft.
  - Spec chart primitive render cache schema site review commit diagram cache.
:::

:::faq
### render fast spec review?
Publish theme flow diagram build source agent twin cache diagram primitive publish flow.
### static commit build site?
Token render yaml markdown commit chart agent evidence agent lint cache content primitive cache.
:::

::cta{title="spec primitive spec" button="Read the spec" href="https://snypd.rocks/spec"}
