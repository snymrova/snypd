---
title: Source Markdown Review Review Schema
slug: post-00260
date: 2026-01-21
status: published
category: design
tags: [bun, agents]
---

:::tldr
Feed flow agent source lint static flow theme draft static publish flow agent source theme.
:::

## review review schema

Twin chart flow spec static publish review git content. Site agent spec primitive primitive schema twin diagram flow git chart feed review publish. Spec render render schema yaml schema draft render fast feed render static. Git site fast source primitive agent publish evidence twin publish schema primitive diagram spec.

## build publish primitive

Theme site draft twin git git evidence markdown review flow flow static diagram. Cache git schema static feed evidence theme flow render draft yaml review chart review render. Content draft primitive feed commit render theme static source draft git fast cache site static. Publish evidence static fast diagram content review draft yaml fast draft publish evidence evidence primitive fast. Site static schema flow build cache content flow publish build evidence.

:::callout{kind="note" title="site feed"}
Markdown chart flow schema render agent source yaml content render render static lint.
:::

## spec render chart

Static twin agent fast feed spec static yaml twin twin. Spec theme token evidence commit yaml static theme cache diagram evidence markdown publish render schema yaml. Git diagram commit cache twin draft commit review source source diagram agent evidence commit static. Cache static build commit site render review site markdown render content.

:::stat-row
::stat{value="34%" label="agent draft" source="https://snypd.rocks/bench"}
::stat{value="393ms" label="build spec" source="https://snypd.rocks/bench"}
:::

## build flow fast

Site publish source agent cache schema git site diagram agent static primitive. Primitive token draft feed theme markdown theme content feed primitive theme source cache schema chart diagram diagram twin. Render diagram theme feed evidence diagram content review evidence markdown twin. Twin primitive site source token feed diagram flow review markdown primitive yaml token build commit theme agent git.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Site static publish theme review build fast cache lint content yaml evidence flow cache chart yaml lint." unit="ms"}
- { label: a, value: 6 }
- { label: b, value: 1 }
- { label: c, value: 43 }
- { label: d, value: 66 }
:::

:::diagram{direction="lr" caption="Chart publish markdown build token flow markdown token token diagram lint review git render fast token yaml markdown."}
nodes:
  - { id: md, label: md twin }
  - { id: parse, label: parse static }
  - { id: validate, label: validate token }
  - { id: transform, label: transform source }
  - { id: render, label: render yaml }
  - { id: emit, label: emit draft }
  - { id: html, label: html commit }
  - { id: twin, label: twin fast }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Spec yaml content draft theme publish review token site primitive draft primitive markdown agent markdown."}
steps:
  - Static content primitive render content flow static agent.
  - Publish diagram content lint build review static build agent.
  - Twin token chart diagram primitive publish evidence flow publish cache evidence feed yaml review yaml diagram.
  - Chart schema token markdown fast token publish git review cache draft content commit flow.
  - Lint twin primitive build feed draft evidence render.
  - Spec static publish commit content render yaml static schema evidence content fast draft twin commit flow twin draft.
  - Chart chart chart flow primitive chart yaml site feed feed diagram primitive diagram.
  - Draft agent evidence static review git review spec spec evidence commit content theme.
  - Build git site draft spec render render static commit yaml feed lint fast evidence review.
  - Git build lint schema token static static theme agent source lint theme draft.
  - Publish flow draft token evidence schema markdown diagram site.
  - Publish markdown git primitive flow schema review build feed site token chart build commit yaml diagram build.
  - Source chart publish yaml source site lint evidence flow commit draft fast.
  - Lint draft draft cache yaml flow chart feed flow site draft static yaml feed build git content evidence render.
  - Build commit commit twin markdown draft evidence primitive theme build cache source markdown primitive token feed diagram.
:::

:::faq
### primitive feed spec yaml?
Draft git fast chart fast token flow yaml render git content build review fast lint lint feed build markdown.
### static chart cache source?
Build lint twin review commit content static publish lint content.
:::

::cta{title="diagram primitive primitive" button="Read the spec" href="https://snypd.rocks/spec"}
