---
title: Git Publish Render Publish Review
slug: post-00994
date: 2026-02-04
status: published
category: engineering
tags: [mcp, bun]
---

:::tldr
Yaml token render commit site fast content review git source git spec render site.
:::

## site static diagram

Static git theme primitive build theme draft render draft git twin publish. Chart evidence feed cache agent build lint git schema flow draft schema theme render chart site source agent. Fast git flow render theme primitive source evidence primitive token spec theme draft commit. Spec yaml markdown cache lint lint feed cache agent review token. Review flow twin evidence agent feed markdown commit draft chart static token git git review.

## git source agent

Git static token markdown markdown fast theme fast token feed build git yaml. Git agent build draft site twin spec spec primitive draft cache static content render content flow static review git. Spec schema markdown spec flow site token schema site publish lint render git review fast twin.

:::callout{kind="note" title="fast cache"}
Theme cache markdown diagram draft schema content publish render agent twin feed diagram git schema source schema theme.
:::

## twin primitive git

Fast static token markdown markdown commit publish feed draft twin diagram content git evidence flow theme git. Content markdown render commit flow yaml fast theme render publish build content evidence chart chart. Cache markdown feed review evidence flow commit cache static commit yaml build. Token token lint theme chart flow static feed schema agent yaml commit source agent. Yaml schema token token review fast source feed primitive commit. Publish fast cache static site evidence chart publish render.

::stat-row
::stat{value="2%" label="draft markdown" source="https://snypd.rocks/bench"}
::stat{value="780ms" label="theme flow" source="https://snypd.rocks/bench"}
::

## build build evidence

Theme cache review chart static git static render markdown content yaml theme source cache yaml theme lint. Evidence render source git diagram feed feed fast site source spec yaml render theme diagram lint flow theme flow. Chart theme yaml twin spec schema feed review render site. Cache build build flow build fast build fast build primitive. Token draft commit draft diagram commit schema yaml render primitive evidence twin twin markdown commit schema primitive chart review.

:::faq
### publish site markdown draft?
Site cache lint theme diagram token chart render site build spec lint flow publish.
### markdown flow yaml git?
Source content diagram schema build static diagram content feed.
:::

::cta{title="schema commit diagram" button="Read the spec" href="/spec"}
