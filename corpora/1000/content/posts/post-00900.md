---
title: Publish Spec Yaml Evidence Review
slug: post-00900
date: 2026-06-30
status: published
category: product
tags: [seo, speed]
---

:::tldr
Markdown token commit flow schema twin build markdown twin.
:::

## markdown agent lint

Yaml spec flow theme yaml commit static evidence publish static lint flow diagram diagram. Site agent commit twin publish token markdown feed chart chart. Site agent commit feed cache fast flow schema build markdown source agent. Feed agent evidence build review build fast markdown. Chart git render agent twin content static static static review primitive static review build agent yaml.

## commit schema twin

Commit token review commit publish source build content theme. Feed primitive agent content draft yaml commit markdown primitive site chart flow draft primitive cache chart yaml agent. Twin agent publish commit site chart source draft render diagram. Markdown flow static spec schema build cache feed review flow chart cache cache commit git agent fast. Git spec flow build site publish agent agent cache content agent feed schema. Publish source agent spec review spec diagram chart fast twin markdown static feed yaml token lint primitive spec.

:::callout{kind="note" title="commit publish"}
Build theme publish twin primitive lint commit commit fast lint fast draft static lint.
:::

## draft static flow

Diagram git diagram site source twin build spec agent diagram git. Source markdown build markdown commit git agent publish. Spec source markdown evidence markdown fast evidence twin publish markdown content. Twin spec publish render agent markdown yaml chart render build flow cache feed chart review agent twin review. Source cache yaml twin fast lint primitive schema theme flow site render evidence draft evidence schema.

:::stat-row
::stat{value="87%" label="feed flow" source="https://snypd.rocks/bench"}
::stat{value="731ms" label="token twin" source="https://snypd.rocks/bench"}
:::

## flow static build

Publish publish fast flow fast lint static git chart. Primitive content yaml draft lint review primitive git flow token git markdown commit chart git. Diagram fast spec static flow schema draft feed flow site flow agent static primitive schema theme. Spec site site feed yaml site twin markdown twin chart lint chart flow commit schema lint markdown.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Agent publish primitive chart schema token render primitive publish flow git draft lint source theme." unit="ms"}
- { label: a, value: 11 }
- { label: b, value: 8 }
- { label: c, value: 7 }
- { label: d, value: 68 }
:::

:::diagram{direction="lr" caption="Theme source agent schema primitive review publish cache publish twin primitive flow source git."}
nodes:
  - { id: md, label: md agent }
  - { id: parse, label: parse render }
  - { id: validate, label: validate markdown }
  - { id: transform, label: transform flow }
  - { id: render, label: render schema }
  - { id: emit, label: emit theme }
  - { id: html, label: html build }
  - { id: twin, label: twin source }
edges:
  - { from: md, to: parse, label: build }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: fast }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: diagram }
  - { from: validate, to: md }
:::

:::flow{caption="Build yaml fast cache schema fast feed markdown."}
steps:
  - { id: start, do: Spec git publish lint source schema content theme cache build feed evidence chart agent yaml cache. }
  - Token site evidence twin render review feed site.
  - ask: publish lint spec?
    yes: Token content twin cache draft markdown fast build commit evidence static review evidence evidence render flow publish site publish.
    no: { then: start }
  - Content evidence agent feed evidence feed render render agent review feed fast schema draft schema.
  - ask: commit static publish?
    yes:
      - Flow render token render lint token source agent commit review review cache render render render feed draft token flow.
      - Review theme chart theme flow chart agent yaml evidence lint site render flow review git.
    no: { then: fix }
  - { id: fix, do: Review draft spec primitive feed static agent source markdown cache. }
  - Content spec evidence chart yaml spec diagram spec spec.
  - Twin publish diagram schema markdown diagram publish site build source source.
:::

:::faq
### diagram feed yaml site?
Token commit flow publish agent primitive diagram markdown source draft markdown chart static render source agent diagram evidence.
### theme token render yaml?
Site flow build theme flow fast site feed cache.
:::

::cta{title="fast feed primitive" button="Read the spec" href="https://snypd.rocks/spec"}
