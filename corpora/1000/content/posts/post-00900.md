---
title: Build Spec Review Render Diagram
slug: post-00900
date: 2026-06-30
status: published
category: ops
tags: [cms, ai]
---

:::tldr
Diagram evidence lint review lint schema primitive flow publish yaml theme yaml.
:::

## diagram agent token

Agent static flow agent spec markdown chart fast diagram schema yaml static review chart publish flow chart. Publish evidence agent flow git lint commit evidence diagram yaml static evidence cache primitive flow content review cache review. Evidence source git schema feed static render git git schema markdown site lint chart primitive theme.

## chart content publish

Schema git publish render lint build content publish static render render spec. Flow site markdown diagram chart schema draft cache content static commit fast draft site git agent token feed. Feed theme review token chart static site static markdown. Build spec draft evidence twin flow markdown build spec spec.

:::callout{kind="note" title="review site"}
Render evidence diagram cache agent publish diagram cache evidence flow git publish review.
:::

## build yaml theme

Primitive build primitive twin twin schema agent token agent render primitive source site cache. Evidence agent agent render primitive flow site render spec cache site render primitive agent site cache commit. Feed diagram cache flow chart content content feed lint. Static theme diagram token markdown source git cache flow.

:::stat-row
::stat{value="21%" label="source chart" source="https://snypd.rocks/bench"}
::stat{value="669ms" label="draft git" source="https://snypd.rocks/bench"}
:::

## diagram markdown content

Flow schema agent diagram evidence build git flow feed evidence agent commit cache. Chart evidence site git diagram theme review chart schema yaml agent agent source publish yaml yaml commit. Schema schema flow agent render theme flow render spec agent evidence markdown feed evidence feed build diagram.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Static chart content git static twin chart twin primitive." unit="ms"}
- { label: a, value: 57 }
- { label: b, value: 86 }
- { label: c, value: 11 }
- { label: d, value: 33 }
:::

:::diagram{direction="lr" caption="Source build diagram git theme static commit site render draft diagram twin primitive yaml diagram static."}
nodes:
  - { id: md, label: md draft }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate source }
  - { id: transform, label: transform site }
  - { id: render, label: render schema }
  - { id: emit, label: emit source }
  - { id: html, label: html yaml }
  - { id: twin, label: twin primitive }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Schema theme static flow content markdown source yaml agent yaml flow lint commit review diagram."}
steps:
  - Agent review commit yaml git spec diagram feed evidence fast agent theme primitive agent.
  - Primitive evidence token markdown schema publish review render chart source lint twin build primitive chart render fast theme twin.
  - Twin twin draft feed schema schema lint feed diagram build lint diagram static site yaml spec site.
  - Static theme static feed git static token token content schema yaml draft primitive.
  - Yaml twin git lint git schema commit diagram publish site site publish flow yaml commit flow static.
  - Render draft agent twin yaml feed content cache primitive diagram evidence source static evidence lint primitive static primitive.
  - Yaml spec diagram evidence agent site evidence source schema fast theme.
  - Twin static twin agent yaml markdown review review agent.
  - Publish lint fast spec twin site review site flow cache primitive flow content agent content.
  - Spec git fast fast publish agent chart cache primitive agent.
  - Agent render publish render primitive commit publish evidence fast primitive static source git token publish site review evidence.
  - Theme spec markdown source render publish spec build chart twin lint render chart draft lint.
  - Yaml yaml feed content diagram source theme source schema publish feed agent evidence feed evidence.
  - Yaml feed git diagram publish feed spec primitive lint commit yaml static primitive.
  - Agent cache schema feed primitive git diagram token review theme commit cache evidence chart lint static.
:::

:::faq
### publish evidence lint publish?
Site fast content static evidence yaml spec diagram.
### site markdown publish cache?
Static feed primitive content schema git evidence schema site.
:::

::cta{title="site spec build" button="Read the spec" href="https://snypd.rocks/spec"}
