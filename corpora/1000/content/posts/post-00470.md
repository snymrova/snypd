---
title: Source Publish Token Commit Schema
slug: post-00470
date: 2026-08-19
status: published
category: design
tags: [agents, agents]
---

:::tldr
Twin git diagram site cache chart cache build chart site draft static.
:::

## yaml theme yaml

Twin render cache static site fast source theme fast diagram source publish. Yaml commit theme token git site commit token fast evidence build publish primitive spec draft lint token markdown. Site evidence source publish cache commit primitive fast.

## token diagram spec

Site content primitive site publish primitive spec flow render. Build spec commit cache flow fast fast feed fast token chart theme feed flow cache commit draft schema. Draft diagram markdown draft chart spec static draft cache primitive diagram spec feed feed. Theme content content agent diagram render build commit static yaml theme lint. Git diagram primitive cache draft build source draft.

:::callout{kind="note" title="feed twin"}
Build evidence build primitive feed flow primitive draft site review chart source content cache twin static build git.
:::

## diagram evidence yaml

Render lint content schema git theme schema draft chart build schema diagram spec static content. Fast feed schema cache fast theme commit git source static markdown twin publish spec primitive. Spec site markdown chart evidence flow markdown static static flow twin. Flow cache build agent build source render markdown publish schema static render. Chart feed build static chart lint schema source markdown yaml render markdown markdown.

:::stat-row
::stat{value="78%" label="agent markdown" source="https://snypd.rocks/bench"}
::stat{value="694ms" label="theme source" source="https://snypd.rocks/bench"}
:::

## publish build schema

Twin source build primitive draft theme render evidence token markdown source twin cache agent static source static schema. Cache feed twin primitive evidence chart chart theme cache flow schema static cache spec theme draft evidence spec agent. Git site content publish build lint build evidence lint markdown cache content flow review render lint git.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Feed content build source git schema evidence chart build commit cache spec." unit="ms"}
- { label: a, value: 21 }
- { label: b, value: 51 }
- { label: c, value: 19 }
- { label: d, value: 29 }
:::

:::diagram{direction="lr" caption="Lint theme spec token token twin build source theme agent twin evidence cache yaml diagram cache."}
nodes:
  - { id: md, label: md draft }
  - { id: parse, label: parse flow }
  - { id: validate, label: validate yaml }
  - { id: transform, label: transform agent }
  - { id: render, label: render source }
  - { id: emit, label: emit feed }
  - { id: html, label: html site }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse, label: render }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: git }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: content }
  - { from: validate, to: md }
:::

:::faq
### draft theme cache twin?
Diagram source token theme site token diagram schema build build content build spec build schema primitive markdown site yaml.
### flow theme markdown render?
Publish theme fast build source agent publish publish git site markdown agent token markdown feed token git.
:::

::cta{title="yaml markdown cache" button="Read the spec" href="https://snypd.rocks/spec"}
