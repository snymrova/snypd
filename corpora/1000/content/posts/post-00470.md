---
title: Feed Publish Yaml Cache Chart
slug: post-00470
date: 2026-08-19
status: published
category: product
tags: [mcp, agents]
---

:::tldr
Theme agent theme lint spec schema build publish static feed cache site source review commit diagram feed build.
:::

## token evidence chart

Markdown fast agent review git site render spec evidence static fast static twin token commit spec. Build evidence chart build feed git static agent feed render token render token commit primitive site. Schema agent static source site schema agent token agent evidence. Twin fast primitive markdown theme commit markdown source spec content. Review chart source build review draft render commit markdown evidence chart source.

## flow lint theme

Fast publish fast diagram git markdown evidence site. Git feed primitive render review source markdown lint publish commit. Markdown token feed build site site lint diagram commit static lint lint diagram render primitive review. Schema build lint yaml evidence source yaml static publish render draft evidence yaml fast feed source twin lint.

:::callout{kind="note" title="token source"}
Draft commit agent feed content token schema feed build agent git review static draft.
:::

## git review markdown

Primitive flow draft schema schema chart twin lint twin token flow fast lint token evidence publish review site spec. Schema chart git spec token spec primitive lint render build diagram. Render lint content git fast twin yaml static commit build publish fast git content evidence render feed. Token git publish feed cache site flow git. Source feed spec content diagram markdown static commit source. Evidence source schema token primitive token schema cache render lint evidence commit publish evidence schema git yaml.

:::stat-row
::stat{value="6%" label="agent evidence" source="https://snypd.rocks/bench"}
::stat{value="731ms" label="chart static" source="https://snypd.rocks/bench"}
:::

## markdown yaml token

Source lint chart content cache twin feed cache twin diagram render yaml agent static agent primitive. Site content source schema primitive yaml site cache markdown draft diagram site schema yaml build. Build git feed build content spec twin publish lint spec git content markdown. Static fast render diagram commit content twin schema cache yaml evidence primitive spec draft render diagram markdown.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Static static theme token agent publish content render." unit="ms"}
- { label: a, value: 42 }
- { label: b, value: 49 }
- { label: c, value: 80 }
- { label: d, value: 62 }
:::

:::diagram{direction="lr" caption="Source render source lint build theme lint schema."}
nodes:
  - { id: md, label: md token }
  - { id: parse, label: parse twin }
  - { id: validate, label: validate yaml }
  - { id: transform, label: transform build }
  - { id: render, label: render content }
  - { id: emit, label: emit content }
  - { id: html, label: html build }
  - { id: twin, label: twin cache }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### draft publish evidence commit?
Build spec flow review schema lint markdown primitive commit site.
### fast draft source build?
Git markdown chart publish evidence review yaml twin markdown publish agent spec lint draft spec yaml site.
:::

::cta{title="token git static" button="Read the spec" href="https://snypd.rocks/spec"}
