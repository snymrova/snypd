---
title: Fast Schema Draft Yaml Evidence
slug: post-00320
date: 2026-03-22
status: published
category: product
tags: [speed, seo]
---

:::tldr
Cache primitive flow cache diagram spec markdown source static spec render lint build chart chart token chart draft spec.
:::

## schema chart chart

Static yaml chart review agent primitive flow draft evidence. Diagram schema yaml source schema site source feed twin. Review token publish source cache theme diagram render static render yaml markdown theme lint evidence. Git diagram theme cache fast schema source fast commit source schema site twin schema spec commit.

## review build cache

Content fast cache render schema schema source cache token build commit build markdown primitive chart render. Draft flow flow primitive content source fast fast static fast publish feed. Content chart fast git diagram build draft yaml fast markdown diagram commit token publish twin agent static commit diagram. Fast chart yaml chart twin flow schema review schema source content markdown draft source chart yaml evidence markdown. Markdown schema schema spec evidence site schema schema flow review schema spec cache static primitive diagram review chart feed. Schema spec flow build static token yaml source static agent render render content.

:::callout{kind="note" title="schema cache"}
Build yaml source chart render draft token feed.
:::

## primitive twin feed

Yaml evidence primitive chart primitive content render token. Draft theme chart primitive fast review commit yaml publish evidence feed chart spec. Publish commit token twin fast feed publish cache fast. Review render token content site flow yaml review lint site git source commit draft commit flow lint render.

:::stat-row
::stat{value="75%" label="evidence feed" source="https://snypd.rocks/bench"}
::stat{value="106ms" label="markdown theme" source="https://snypd.rocks/bench"}
:::

## build token source

Site agent primitive commit yaml review static diagram. Git spec schema build content feed review twin lint content draft content lint diagram fast fast cache content primitive. Chart fast source site primitive agent git markdown lint cache. Twin review spec git fast lint cache theme twin flow lint fast content cache. Git yaml theme diagram cache draft feed lint fast. Yaml theme cache spec render review markdown schema twin schema evidence evidence site markdown flow.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Markdown markdown git publish publish twin agent schema diagram fast fast." unit="ms"}
- { label: a, value: 9 }
- { label: b, value: 74 }
- { label: c, value: 65 }
- { label: d, value: 16 }
:::

:::diagram{direction="lr" caption="Chart primitive spec draft markdown git diagram build publish diagram lint theme publish build draft fast."}
nodes:
  - { id: md, label: md feed }
  - { id: parse, label: parse cache }
  - { id: validate, label: validate source }
  - { id: transform, label: transform site }
  - { id: render, label: render evidence }
  - { id: emit, label: emit lint }
  - { id: html, label: html publish }
  - { id: twin, label: twin git }
edges:
  - { from: md, to: parse, label: draft }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: evidence }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: feed }
  - { from: validate, to: md }
:::

:::flow{caption="Theme theme agent build site chart feed agent theme site agent."}
steps:
  - Chart yaml yaml lint source markdown token source theme review fast flow evidence draft agent publish.
  - Commit review source content build build primitive primitive spec content draft schema content agent git flow spec.
  - Build fast schema primitive schema agent primitive lint git schema.
  - Render chart cache twin primitive chart lint chart yaml feed review agent site lint spec feed.
  - Markdown schema schema chart evidence primitive fast draft primitive commit static feed draft site cache.
  - Agent publish theme build agent source render feed yaml primitive primitive fast build markdown content render cache feed.
  - Primitive agent site git yaml lint build git fast site content cache fast publish.
  - Site draft content draft draft chart twin chart diagram git chart site git content schema fast diagram theme lint.
  - Fast agent site token feed lint twin schema diagram review schema flow.
  - Cache source build primitive source chart review cache schema build diagram diagram site twin diagram source cache content.
  - Source markdown primitive primitive primitive yaml git yaml.
  - Fast theme build draft lint flow source cache cache yaml commit fast commit agent flow.
  - Review markdown cache twin fast yaml evidence build lint theme publish publish agent cache commit review site.
  - Evidence twin spec diagram render fast lint twin twin git source.
  - Git flow commit git fast spec publish feed yaml site lint render publish lint twin cache chart yaml markdown.
:::

:::faq
### source chart markdown build?
Commit spec flow review fast evidence schema chart diagram commit.
### commit markdown markdown fast?
Review diagram commit publish theme build feed git feed twin.
:::

::cta{title="review twin twin" button="Read the spec" href="https://snypd.rocks/spec"}
