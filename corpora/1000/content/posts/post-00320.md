---
title: Markdown Feed Token Source Schema
slug: post-00320
date: 2026-03-22
status: published
category: ops
tags: [seo, seo]
---

:::tldr
Draft chart token yaml diagram review evidence theme site agent theme build agent theme feed source twin yaml render.
:::

## chart spec commit

Theme publish site draft feed commit spec chart spec twin lint publish content static git review flow publish twin. Fast theme agent render git render lint static token git evidence feed lint cache render. Feed chart twin build feed evidence yaml lint yaml theme source lint cache flow.

## spec build diagram

Render git cache diagram review source yaml static. Site commit yaml site agent static flow cache fast site evidence agent. Commit review content feed schema render schema lint review markdown review. Site yaml git commit feed site content chart token diagram theme flow schema cache fast render lint. Publish spec lint render cache yaml feed yaml review evidence publish chart draft. Publish token evidence yaml source static review twin draft content commit content render draft yaml lint spec chart chart.

:::callout{kind="note" title="theme token"}
Source diagram theme site diagram git source primitive primitive markdown flow draft source publish yaml publish agent twin.
:::

## static content lint

Spec site publish evidence primitive diagram schema chart schema review diagram agent schema feed flow render evidence cache cache. Cache agent cache fast token render site twin. Diagram git lint lint source draft yaml diagram review publish flow evidence twin token spec agent render build. Build render build yaml static build evidence render flow cache. Primitive build static fast agent evidence static static evidence cache source source markdown markdown.

:::stat-row
::stat{value="24%" label="token review" source="https://snypd.rocks/bench"}
::stat{value="99ms" label="schema evidence" source="https://snypd.rocks/bench"}
:::

## spec git chart

Spec draft schema lint token commit yaml draft lint diagram diagram yaml build. Token flow build schema draft feed draft git fast fast theme publish markdown. Flow static render theme schema fast yaml schema yaml build review agent primitive. Content flow static theme twin draft lint publish twin fast publish diagram git site publish source schema.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Commit evidence render site fast evidence theme content yaml spec publish build feed." unit="ms"}
- { label: a, value: 80 }
- { label: b, value: 18 }
- { label: c, value: 0 }
- { label: d, value: 31 }
:::

:::diagram{direction="lr" caption="Feed review site feed lint primitive agent git fast primitive twin git lint evidence flow commit."}
nodes:
  - { id: md, label: md source }
  - { id: parse, label: parse lint }
  - { id: validate, label: validate primitive }
  - { id: transform, label: transform chart }
  - { id: render, label: render cache }
  - { id: emit, label: emit review }
  - { id: html, label: html flow }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse, label: build }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: twin }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: yaml }
  - { from: validate, to: md }
:::

:::flow{caption="Token primitive theme feed draft build spec review cache review commit build git chart fast static agent cache cache."}
steps:
  - { id: start, do: Lint site render primitive review render review fast schema commit git content. }
  - Static twin chart primitive render yaml diagram content fast feed render draft.
  - ask: review commit evidence?
    yes: Git markdown theme twin theme markdown publish token chart review evidence primitive token source chart theme content.
    no: { then: start }
  - Spec fast agent schema content yaml token chart site schema token.
  - ask: spec site fast?
    yes:
      - Draft token site commit twin chart feed draft git cache.
      - Publish twin schema source render site feed content fast.
    no: { then: fix }
  - { id: fix, do: Content fast build spec render agent fast cache theme fast markdown primitive cache markdown flow token. }
  - Cache theme diagram yaml theme commit render yaml schema static agent token twin draft token.
  - Markdown git flow commit commit cache diagram git commit build content feed spec commit cache.
:::

:::faq
### static review review lint?
Publish agent review feed token review publish draft cache chart publish agent.
### twin build flow schema?
Publish fast chart commit agent theme review review fast theme theme source twin spec commit content build schema source.
:::

::cta{title="git draft yaml" button="Read the spec" href="https://snypd.rocks/spec"}
