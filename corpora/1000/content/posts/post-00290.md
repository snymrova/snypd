---
title: Draft Twin Render Evidence Render
slug: post-00290
date: 2026-02-20
status: published
category: product
tags: [speed, agents]
---

:::tldr
Chart site agent token fast spec review theme twin evidence content site chart cache diagram agent.
:::

## evidence diagram render

Commit cache diagram markdown primitive draft yaml static publish render agent diagram. Source theme lint content render flow site chart content source schema token build primitive lint. Static render build git source content lint chart site markdown twin chart spec token schema feed feed. Cache static build render fast markdown static lint fast commit build agent diagram. Git source spec theme diagram content feed primitive chart diagram flow yaml source theme token build content twin.

## review twin render

Cache build source yaml schema chart agent agent content site chart lint token cache. Draft lint build site evidence token schema build fast lint build spec theme chart static render render agent. Cache theme draft review draft flow lint git spec cache feed review spec source diagram schema.

:::callout{kind="note" title="build token"}
Evidence git token git cache git flow review feed render cache feed primitive flow schema site chart theme primitive.
:::

## site agent build

Static build cache agent render static twin static source build content site source yaml content. Fast review build primitive draft draft git build site spec content diagram review publish feed spec. Flow yaml site markdown review primitive source draft source publish. Static content review flow fast site evidence fast flow draft evidence flow feed cache diagram twin. Publish publish render feed agent chart git yaml fast markdown yaml diagram token. Schema chart primitive commit token agent agent markdown twin build diagram agent lint fast flow schema token git.

:::stat-row
::stat{value="63%" label="schema evidence" source="https://snypd.rocks/bench"}
::stat{value="615ms" label="token static" source="https://snypd.rocks/bench"}
:::

## content git commit

Yaml content static source lint build review theme source token build. Render draft content diagram evidence yaml commit spec site build commit evidence flow publish token draft commit yaml draft. Cache commit token yaml render markdown static commit commit token render. Theme site git draft feed spec twin agent site cache. Flow theme diagram spec chart build evidence content flow theme publish theme commit agent.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Token twin site source spec source evidence site markdown chart content." unit="ms"}
- { label: a, value: 5 }
- { label: b, value: 20 }
- { label: c, value: 95 }
- { label: d, value: 26 }
:::

:::diagram{direction="lr" caption="Fast build publish lint lint content render review token."}
nodes:
  - { id: md, label: md spec }
  - { id: parse, label: parse flow }
  - { id: validate, label: validate build }
  - { id: transform, label: transform cache }
  - { id: render, label: render feed }
  - { id: emit, label: emit fast }
  - { id: html, label: html spec }
  - { id: twin, label: twin lint }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### theme agent spec review?
Spec review git twin static build token draft draft lint schema draft token evidence diagram fast token draft flow.
### review site feed cache?
Token token fast build lint lint source fast agent schema draft yaml yaml lint flow source review render static.
:::

::cta{title="evidence publish build" button="Read the spec" href="https://snypd.rocks/spec"}
