---
title: Content Render Primitive Site Cache
slug: post-00290
date: 2026-02-20
status: published
category: engineering
tags: [bun, cms]
---

:::tldr
Chart feed schema cache agent build evidence cache content theme static primitive agent diagram build evidence.
:::

## git evidence twin

Flow flow commit theme theme git site draft render commit markdown. Review render cache flow cache fast flow content source evidence markdown schema cache draft. Build spec site twin cache review site static theme schema lint flow commit twin feed.

## draft primitive flow

Yaml fast build markdown render static flow chart theme evidence cache markdown. Markdown flow git primitive publish spec site evidence. Review agent schema chart primitive fast chart yaml agent cache theme git. Diagram static diagram review twin evidence source build commit agent render draft twin.

:::callout{kind="note" title="review spec"}
Git git feed agent review draft markdown content evidence theme cache token theme content static evidence yaml yaml.
:::

## static site flow

Git token draft evidence content chart publish static agent flow evidence schema content feed git theme git diagram yaml. Git cache evidence evidence evidence flow publish fast yaml publish commit. Schema spec static token yaml token build spec agent spec content publish fast theme. Token review schema spec schema agent agent feed evidence commit build spec yaml render fast review spec cache diagram.

:::stat-row
::stat{value="67%" label="draft source" source="https://snypd.rocks/bench"}
::stat{value="185ms" label="publish build" source="https://snypd.rocks/bench"}
:::

## diagram evidence publish

Chart chart markdown commit build lint static commit chart evidence primitive. Diagram diagram draft review cache token chart token twin theme publish twin theme twin content agent commit markdown theme. Build source commit agent flow source evidence fast draft content git review. Spec review cache static evidence token render lint flow feed render commit theme spec flow yaml site. Content feed static evidence build review static draft theme git flow chart git source review. Site evidence draft markdown cache agent content diagram cache build evidence source theme chart publish yaml agent source.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Git draft fast token site site spec schema markdown twin." unit="ms"}
- { label: a, value: 88 }
- { label: b, value: 46 }
- { label: c, value: 87 }
- { label: d, value: 49 }
:::

:::diagram{direction="lr" caption="Publish spec theme build content flow token fast lint."}
nodes:
  - { id: md, label: md yaml }
  - { id: parse, label: parse evidence }
  - { id: validate, label: validate primitive }
  - { id: transform, label: transform evidence }
  - { id: render, label: render primitive }
  - { id: emit, label: emit twin }
  - { id: html, label: html markdown }
  - { id: twin, label: twin feed }
edges:
  - { from: md, to: parse, label: flow }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: publish }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: chart }
  - { from: validate, to: md }
:::

:::faq
### build fast flow evidence?
Review spec render source agent git primitive yaml review.
### yaml site fast schema?
Feed feed content spec site chart render primitive review evidence content agent.
:::

::cta{title="cache git site" button="Read the spec" href="https://snypd.rocks/spec"}
