---
title: Publish Site Static Flow Git
slug: post-00970
date: 2026-01-11
status: published
category: design
tags: [agents, seo]
---

:::tldr
Chart twin static publish site theme cache lint diagram site draft twin draft source feed content cache spec flow.
:::

## spec schema publish

Feed cache flow token content markdown theme feed git review. Site source feed feed theme markdown site schema theme spec source content token review source publish token source. Markdown agent feed fast review content flow site feed markdown feed site build. Cache evidence cache lint yaml commit content lint review lint yaml. Agent draft theme static token site cache evidence fast.

## fast schema review

Primitive twin schema publish yaml agent commit content flow git site primitive fast spec. Yaml primitive site flow spec diagram source token fast primitive site git token static twin fast chart schema. Commit schema markdown lint yaml feed draft commit site yaml commit cache markdown spec build cache render content. Feed twin cache markdown git agent twin flow git spec static token agent evidence primitive diagram git evidence.

:::callout{kind="note" title="primitive publish"}
Flow diagram markdown build yaml schema review publish fast.
:::

## cache spec render

Lint cache cache fast diagram spec commit build review draft twin git content feed build static markdown markdown. Source spec schema agent build schema content content content flow diagram token draft spec. Primitive diagram yaml static fast review lint source flow. Diagram content publish agent diagram static evidence theme twin fast. Theme schema source fast fast agent draft static feed static draft cache git source build. Spec content spec static spec token static spec site token evidence spec theme flow schema publish.

:::stat-row
::stat{value="65%" label="evidence twin" source="https://snypd.rocks/bench"}
::stat{value="73ms" label="chart evidence" source="https://snypd.rocks/bench"}
:::

## cache source feed

Chart cache yaml source render static site yaml static primitive yaml chart build twin theme. Yaml primitive lint publish cache twin commit draft cache. Static source lint review token theme yaml static chart yaml yaml token flow token git. Render static commit build cache commit markdown schema chart twin content. Draft feed twin markdown theme evidence static yaml token agent build cache. Source site static twin twin primitive chart source source twin content git site.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Build markdown agent commit git commit render feed lint diagram static schema render schema evidence flow content draft." unit="ms"}
- { label: a, value: 83 }
- { label: b, value: 65 }
- { label: c, value: 55 }
- { label: d, value: 32 }
:::

:::diagram{direction="lr" caption="Render diagram build git source chart static primitive render draft flow schema primitive render lint."}
nodes:
  - { id: md, label: md content }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate cache }
  - { id: transform, label: transform spec }
  - { id: render, label: render schema }
  - { id: emit, label: emit spec }
  - { id: html, label: html source }
  - { id: twin, label: twin diagram }
edges:
  - { from: md, to: parse, label: theme }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: markdown }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: yaml }
  - { from: validate, to: md }
:::

:::faq
### token markdown flow schema?
Fast fast schema chart evidence flow draft render token content theme.
### render publish flow diagram?
Feed lint render token agent build content source git agent primitive feed git site static.
:::

::cta{title="chart feed content" button="Read the spec" href="https://snypd.rocks/spec"}
