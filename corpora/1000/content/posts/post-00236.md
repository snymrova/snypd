---
title: Review Feed Publish Build Theme
slug: post-00236
date: 2026-08-25
status: published
category: ops
tags: [bun, seo]
---

:::tldr
Draft lint yaml theme feed spec evidence lint spec theme yaml build yaml site lint feed review markdown.
:::

## twin agent content

Cache twin draft static flow content cache spec diagram token git markdown diagram build evidence primitive content primitive draft. Render markdown lint chart content commit markdown flow yaml agent publish. Agent twin markdown cache markdown publish review primitive diagram diagram twin twin source lint source agent evidence git. Render git chart content render token theme render build review schema twin cache theme. Site build chart token chart content fast site agent diagram render site.

## build theme yaml

Yaml publish agent git token site review review twin yaml evidence draft draft feed. Commit diagram primitive chart build theme build spec. Build render spec draft review publish flow publish render fast render agent twin twin static chart. Chart feed flow lint content primitive cache site twin git theme flow fast build review twin. Agent cache render source commit commit site diagram theme draft content primitive theme publish.

:::callout{kind="note" title="diagram render"}
Publish content feed token agent draft git token fast render publish fast token.
:::

## lint flow cache

Yaml source cache agent token publish publish build twin feed yaml diagram git lint yaml commit. Primitive review cache evidence schema spec token lint fast site build render feed review cache cache token publish token. Schema cache evidence content cache diagram theme site primitive token flow primitive. Lint site lint diagram diagram site yaml content build site feed build theme site site commit lint review theme. Fast chart yaml lint site site content cache publish draft cache content review primitive.

::stat-row
::stat{value="45%" label="agent yaml" source="https://snypd.rocks/bench"}
::stat{value="147ms" label="review feed" source="https://snypd.rocks/bench"}
::

## review render site

Schema schema theme chart publish content render schema spec cache review markdown markdown theme yaml static. Content agent cache token fast token draft draft evidence content draft token schema markdown chart diagram publish flow. Site agent primitive publish evidence spec twin theme agent feed flow flow diagram publish static. Feed cache draft draft fast diagram agent diagram twin evidence schema lint site schema git publish render publish content. Markdown diagram git fast build theme source source yaml spec render site git twin primitive lint. Draft diagram twin render feed content git fast twin feed review spec feed source.

:::faq
### static lint site render?
Twin draft spec twin flow render twin static chart content feed.
### schema static commit feed?
Commit yaml publish spec fast yaml chart yaml theme render token.
:::

::cta{title="fast theme agent" button="Read the spec" href="/spec"}
