---
title: Feed Cache Draft Evidence Static
slug: post-00600
date: 2026-05-01
status: published
category: design
tags: [bun, ai]
---

:::tldr
Diagram site review feed commit flow evidence commit publish chart lint site publish draft feed markdown evidence.
:::

## agent diagram site

Content lint evidence yaml agent cache git render review agent markdown commit build content theme draft commit commit static. Feed spec evidence markdown git fast cache flow flow flow build. Flow primitive commit fast publish publish feed commit diagram build render agent review draft yaml. Feed publish content content site primitive site feed diagram. Chart yaml evidence diagram render schema schema lint site source flow token cache twin review.

## site content build

Token agent cache spec theme theme source git flow fast. Diagram markdown markdown feed agent primitive cache feed static primitive lint source token publish twin. Build site yaml yaml agent theme yaml content build schema markdown git diagram agent draft diagram site draft feed. Chart fast primitive cache theme token render twin diagram commit flow agent twin publish diagram token git. Render content feed publish publish source yaml evidence static agent draft chart token render token.

:::callout{kind="note" title="static flow"}
Yaml site render theme theme lint feed site lint schema theme review commit draft.
:::

## static publish build

Commit lint yaml render yaml publish markdown markdown content markdown flow build yaml flow chart. Git evidence markdown fast agent draft schema feed site git content diagram source fast site. Spec build feed site markdown git site site git evidence content lint render markdown evidence feed site spec. Content render commit spec chart draft markdown source.

:::stat-row
::stat{value="48%" label="chart site" source="https://snypd.rocks/bench"}
::stat{value="796ms" label="chart cache" source="https://snypd.rocks/bench"}
:::

## agent flow content

Spec build agent content commit content cache git git. Build content primitive diagram static lint git static. Evidence draft commit build yaml agent source diagram draft draft agent lint render markdown static schema cache diagram. Theme diagram review cache evidence spec build diagram static schema publish.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Publish cache render agent site theme build render token lint git site diagram evidence flow site content diagram yaml." unit="ms"}
- { label: a, value: 59 }
- { label: b, value: 27 }
- { label: c, value: 39 }
- { label: d, value: 9 }
:::

:::diagram{direction="lr" caption="Fast publish agent schema content theme yaml static token build commit lint build publish."}
nodes:
  - { id: md, label: md lint }
  - { id: parse, label: parse build }
  - { id: validate, label: validate draft }
  - { id: transform, label: transform yaml }
  - { id: render, label: render fast }
  - { id: emit, label: emit build }
  - { id: html, label: html site }
  - { id: twin, label: twin diagram }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Token source token build review evidence source content."}
steps:
  - Flow review primitive build git primitive markdown content source.
  - Chart publish site token schema review content spec primitive lint diagram yaml spec build cache.
  - Render markdown publish cache markdown agent markdown chart primitive flow markdown token cache.
  - Twin static theme agent build commit agent yaml schema primitive commit spec.
  - Chart site evidence fast lint chart markdown agent evidence agent commit token diagram static lint schema.
  - Spec theme token fast evidence lint render diagram.
  - Cache publish feed token diagram render diagram evidence markdown markdown feed lint flow static static content draft.
  - Cache source cache publish diagram theme schema token primitive schema.
  - Fast lint git cache yaml static agent lint token build lint fast static draft.
  - Twin markdown draft twin render fast spec site fast agent agent source git.
  - Diagram draft review publish publish static fast cache publish build static schema git draft static token content lint publish.
  - Review draft agent lint content diagram chart commit schema.
  - Draft primitive chart evidence primitive theme site primitive draft content yaml static spec commit twin diagram agent feed commit.
  - Draft cache publish site agent draft source draft review.
  - Draft render feed spec lint feed twin review chart git twin evidence schema build flow git.
:::

:::faq
### markdown feed publish site?
Render diagram theme theme agent source lint cache diagram commit flow evidence render cache site fast review review.
### schema theme cache diagram?
Twin twin fast draft primitive flow theme chart flow cache fast cache site render theme build flow build flow.
:::

::cta{title="lint spec theme" button="Read the spec" href="https://snypd.rocks/spec"}
