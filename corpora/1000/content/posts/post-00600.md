---
title: Flow Evidence Yaml Twin Fast
slug: post-00600
date: 2026-05-01
status: published
category: design
tags: [markdown, markdown]
---

:::tldr
Theme static draft publish chart primitive twin source.
:::

## feed review render

Cache spec theme spec flow feed site fast chart review render agent site draft yaml. Diagram source token diagram spec content git content markdown feed primitive source flow commit primitive publish commit site. Content theme render markdown schema schema content publish chart markdown schema draft source evidence. Yaml cache source theme token review twin markdown schema schema site. Yaml publish commit build schema spec build diagram spec spec token. Chart yaml token content schema build git agent token twin git publish review lint schema chart yaml static lint.

## publish content schema

Publish evidence content fast evidence content feed feed. Fast static cache evidence evidence markdown build cache review content static source diagram spec review agent review. Lint twin yaml yaml commit token static twin render diagram schema primitive markdown cache. Content content agent token publish site twin render token site. Fast draft markdown chart diagram twin token fast build static.

:::callout{kind="note" title="source static"}
Source cache primitive site evidence publish flow schema theme schema primitive static.
:::

## schema fast yaml

Source feed chart publish markdown diagram content theme draft. Twin flow markdown review schema spec static agent diagram agent draft build feed cache build render primitive theme static. Chart agent render token build fast markdown cache cache primitive git feed. Flow lint primitive content spec feed theme site review token static token.

:::stat-row
::stat{value="90%" label="yaml content" source="https://snypd.rocks/bench"}
::stat{value="861ms" label="content site" source="https://snypd.rocks/bench"}
:::

## evidence twin schema

Static feed git build static draft build token content publish source. Diagram feed content source git content primitive spec cache lint yaml chart review git render. Fast token review static cache render token twin commit source chart publish primitive git theme commit.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Agent cache evidence review schema markdown source twin." unit="ms"}
- { label: a, value: 40 }
- { label: b, value: 45 }
- { label: c, value: 37 }
- { label: d, value: 23 }
:::

:::diagram{direction="lr" caption="Schema feed commit static schema static theme commit evidence primitive fast draft git flow fast schema chart yaml."}
nodes:
  - { id: md, label: md static }
  - { id: parse, label: parse spec }
  - { id: validate, label: validate yaml }
  - { id: transform, label: transform markdown }
  - { id: render, label: render primitive }
  - { id: emit, label: emit markdown }
  - { id: html, label: html fast }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse, label: chart }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: feed }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: git }
  - { from: validate, to: md }
:::

:::flow{caption="Twin agent primitive fast token chart cache review fast twin cache lint static commit."}
steps:
  - { id: start, do: Site theme build spec build schema schema commit schema site source primitive lint publish draft source spec feed theme. }
  - Site draft source twin markdown draft commit spec markdown build flow.
  - ask: render render agent?
    yes: Chart yaml evidence cache publish spec token site chart commit yaml evidence git draft schema.
    no: { then: start }
  - Agent primitive review draft diagram cache evidence primitive twin source flow static source build git fast feed.
  - ask: render build publish?
    yes:
      - Site primitive yaml build git flow git draft static primitive.
      - Flow schema twin agent evidence content token spec schema git twin build spec primitive site.
    no: { then: fix }
  - { id: fix, do: Evidence site chart cache twin primitive flow diagram diagram primitive evidence evidence token primitive build chart cache. }
  - Twin theme cache fast token content diagram fast fast.
  - Site build twin primitive yaml chart lint markdown git build diagram static markdown site site schema.
:::

:::faq
### review spec draft cache?
Git lint feed theme markdown draft diagram token site token.
### theme chart chart yaml?
Draft content evidence feed draft build site draft primitive draft spec.
:::

::cta{title="lint publish flow" button="Read the spec" href="https://snypd.rocks/spec"}
