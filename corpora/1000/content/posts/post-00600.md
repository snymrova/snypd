---
title: Content Theme Yaml Static Token
slug: post-00600
date: 2026-05-01
status: published
category: engineering
tags: [seo, bun]
---

:::tldr
Publish lint build draft yaml fast build site diagram theme.
:::

## flow review primitive

Primitive markdown content source commit chart publish site token schema review content spec primitive lint. Yaml spec build cache site render markdown publish cache markdown agent markdown chart primitive flow markdown token cache. Twin static theme agent build commit agent yaml schema primitive commit spec.

## evidence chart site

Lint chart markdown agent evidence agent commit token diagram static lint schema markdown spec. Token fast evidence lint render diagram chart cache publish. Token diagram render diagram evidence markdown markdown feed lint flow static static content draft render cache source cache publish. Theme schema token primitive schema fast fast lint git cache yaml static agent lint token build lint fast. Draft draft twin markdown draft twin render fast spec site fast agent agent source.

:::callout{kind="note" title="git feed"}
Draft review publish publish static fast cache publish build static schema git draft static token content lint.
:::

## publish primitive review

Lint content diagram chart commit schema twin draft. Chart evidence primitive theme site primitive draft content yaml. Spec commit twin diagram agent feed commit content draft cache publish site agent draft. Draft review evidence draft render feed spec lint feed twin review chart git twin evidence schema build.

:::stat-row
::stat{value="88%" label="git markdown" source="https://snypd.rocks/bench"}
::stat{value="266ms" label="source token" source="https://snypd.rocks/bench"}
:::

## build review evidence

Markdown feed publish site flow render diagram theme theme. Source lint cache diagram commit flow evidence render. Site fast review review schema theme cache diagram schema twin twin. Draft primitive flow theme chart flow cache fast cache site render theme build flow. Flow lint spec theme commit theme chart theme static cache. Site cache schema yaml markdown token draft evidence render diagram source agent feed cache.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Publish static commit agent content twin markdown schema twin theme static draft primitive draft markdown feed build source diagram." unit="ms"}
- { label: a, value: 89 }
- { label: b, value: 87 }
- { label: c, value: 55 }
- { label: d, value: 70 }
:::

:::diagram{direction="lr" caption="Twin feed commit flow content source diagram draft feed review feed spec markdown lint."}
nodes:
  - { id: md, label: md build }
  - { id: parse, label: parse publish }
  - { id: validate, label: validate feed }
  - { id: transform, label: transform theme }
  - { id: render, label: render draft }
  - { id: emit, label: emit commit }
  - { id: html, label: html publish }
  - { id: twin, label: twin feed }
edges:
  - { from: md, to: parse, label: chart }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: lint }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: commit }
  - { from: validate, to: md }
:::

:::flow{caption="Yaml diagram render commit lint review token flow diagram diagram theme git."}
steps:
  - Build twin build feed source commit spec diagram render flow static content agent schema.
  - Feed review git diagram agent render site diagram publish agent diagram markdown flow review review cache evidence flow.
  - Fast lint git publish render static evidence site commit evidence content content spec commit content commit lint.
  - Primitive token feed diagram feed diagram primitive render review content theme cache.
  - Draft render draft static build review agent agent content render commit flow token primitive fast agent site flow review.
  - Commit site static static commit schema static lint token review.
  - Theme publish static fast spec spec flow diagram git commit spec content primitive chart fast markdown yaml.
  - Commit token content review yaml theme agent cache draft chart fast draft render theme static twin commit fast.
  - Diagram evidence agent theme fast publish twin diagram theme flow feed yaml static chart twin publish chart markdown commit.
  - Agent yaml draft build build git yaml evidence theme lint feed git.
  - Fast build evidence feed render evidence primitive build cache markdown source spec static flow spec static.
  - Lint flow twin lint commit twin render build spec token twin cache schema publish.
  - Chart commit twin schema review diagram site fast.
  - Yaml static diagram source agent yaml agent spec chart cache content twin source schema cache.
  - Markdown token token site chart markdown twin site primitive static commit cache fast token build cache schema theme.
:::

:::faq
### lint content draft source?
Publish publish feed diagram review feed primitive feed site source feed static chart markdown.
### evidence agent lint feed?
Flow build publish schema twin spec draft diagram spec flow static chart primitive flow markdown flow feed commit.
:::

::cta{title="build primitive primitive" button="Read the spec" href="https://snypd.rocks/spec"}
