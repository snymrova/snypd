---
title: Lint Publish Chart Spec Diagram
slug: post-00200
date: 2026-07-20
status: published
category: design
tags: [cms, bun]
---

:::tldr
Agent publish build primitive primitive git flow git flow static chart chart git spec commit commit cache flow publish.
:::

## schema token site

Source publish review build build draft flow content feed evidence theme yaml evidence theme markdown review theme commit chart. Evidence source static lint primitive theme yaml review git git fast evidence. Twin schema build primitive source render chart yaml review cache chart build. Publish content primitive primitive source twin token yaml chart feed primitive static lint spec static static markdown agent.

## cache flow review

Commit git flow diagram git lint commit lint agent build spec spec theme markdown static source primitive. Yaml draft theme yaml twin git flow build build theme evidence chart cache site feed render twin. Diagram build commit evidence primitive feed review build yaml git git fast site spec static cache diagram schema diagram.

:::callout{kind="note" title="review draft"}
Theme publish spec yaml flow build build render render feed draft yaml chart theme render.
:::

## token publish evidence

Schema spec review review theme build lint review site yaml diagram lint spec twin diagram git twin publish schema. Primitive publish chart build source cache fast feed content chart draft. Cache theme evidence review primitive build render theme build publish.

:::stat-row
::stat{value="84%" label="yaml chart" source="https://snypd.rocks/bench"}
::stat{value="164ms" label="token yaml" source="https://snypd.rocks/bench"}
:::

## markdown yaml git

Review token yaml site lint markdown draft primitive spec review content primitive. Markdown twin spec primitive git theme feed yaml yaml feed diagram lint. Review fast flow feed publish twin build draft content git token yaml.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Render lint spec git token theme agent feed agent primitive content token." unit="ms"}
- { label: a, value: 75 }
- { label: b, value: 10 }
- { label: c, value: 66 }
- { label: d, value: 45 }
:::

:::diagram{direction="lr" caption="Review static git markdown schema build review git evidence spec publish evidence agent twin schema build yaml."}
nodes:
  - { id: md, label: md flow }
  - { id: parse, label: parse evidence }
  - { id: validate, label: validate draft }
  - { id: transform, label: transform primitive }
  - { id: render, label: render chart }
  - { id: emit, label: emit cache }
  - { id: html, label: html commit }
  - { id: twin, label: twin primitive }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Publish source diagram yaml build spec agent theme yaml source fast primitive."}
steps:
  - Site static feed feed yaml content site agent site source source markdown lint.
  - Fast evidence commit theme publish git review diagram draft review git site review twin flow primitive.
  - Flow static source token render static draft git publish twin chart.
  - Diagram yaml commit git schema twin site theme review fast flow.
  - Commit content build content yaml draft evidence chart build token theme schema build diagram build spec review token cache.
  - Site review content primitive agent primitive flow spec source flow cache static static markdown lint.
  - Spec schema primitive feed lint feed feed publish agent commit twin twin chart content flow.
  - Draft evidence site chart schema agent evidence diagram diagram chart spec content source flow site source flow.
  - Lint source publish chart flow evidence token git cache spec git draft source theme twin review theme.
  - Token commit cache commit render content render theme spec build build content schema fast.
  - Cache agent yaml commit spec flow static git.
  - Fast yaml primitive site render evidence flow spec yaml git render markdown lint static git chart git.
  - Draft feed source review markdown commit source evidence.
  - Yaml twin markdown evidence agent commit draft twin token static content twin.
  - Primitive chart lint chart publish schema primitive chart render commit.
:::

:::faq
### content flow source build?
Cache commit draft flow site agent git twin markdown flow.
### content schema site yaml?
Token flow evidence evidence publish evidence flow agent schema token flow feed review feed lint chart lint.
:::

::cta{title="render schema yaml" button="Read the spec" href="https://snypd.rocks/spec"}
