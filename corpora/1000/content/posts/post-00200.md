---
title: Agent Diagram Fast Publish Token
slug: post-00200
date: 2026-07-20
status: published
category: design
tags: [cms, seo]
---

:::tldr
Cache token lint agent render publish schema feed schema.
:::

## flow theme lint

Schema yaml build agent content chart feed lint build markdown draft draft. Lint publish chart spec diagram commit static lint schema agent publish build primitive primitive. Flow git flow static chart chart git spec commit commit cache flow publish schema token.

## site spec twin

Review build build draft flow content feed evidence theme yaml evidence theme markdown. Theme commit chart spec evidence source static lint primitive theme yaml review git git fast evidence. Twin schema build primitive source render chart yaml review cache chart build. Publish content primitive primitive source twin token yaml chart feed primitive static lint spec static static markdown agent. Flow review markdown source commit git flow diagram git lint commit. Agent build spec spec theme markdown static source primitive chart yaml draft.

:::callout{kind="note" title="theme yaml"}
Git flow build build theme evidence chart cache site feed render twin feed diagram build commit evidence primitive feed.
:::

## review build yaml

Fast site spec static cache diagram schema diagram review draft git theme publish spec yaml. Build build render render feed draft yaml chart theme render token publish evidence render feed schema spec review. Theme build lint review site yaml diagram lint spec twin diagram git twin publish schema token. Publish chart build source cache fast feed content chart. Build cache theme evidence review primitive build render theme build publish diagram yaml.

:::stat-row
::stat{value="79%" label="render token" source="https://snypd.rocks/bench"}
::stat{value="515ms" label="markdown yaml" source="https://snypd.rocks/bench"}
:::

## git primitive publish

Yaml site lint markdown draft primitive spec review content primitive lint. Twin spec primitive git theme feed yaml yaml. Diagram lint publish review fast flow feed publish twin build draft content git token yaml lint render lint spec. Token theme agent feed agent primitive content token source theme commit draft diagram review static. Markdown schema build review git evidence spec publish evidence agent twin schema build yaml flow.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Draft primitive chart cache commit primitive draft site static feed feed yaml content site agent site." unit="ms"}
- { label: a, value: 75 }
- { label: b, value: 77 }
- { label: c, value: 4 }
- { label: d, value: 38 }
:::

:::diagram{direction="lr" caption="Fast evidence commit theme publish git review diagram draft review git site review twin flow primitive."}
nodes:
  - { id: md, label: md token }
  - { id: parse, label: parse flow }
  - { id: validate, label: validate static }
  - { id: transform, label: transform source }
  - { id: render, label: render token }
  - { id: emit, label: emit render }
  - { id: html, label: html static }
  - { id: twin, label: twin draft }
edges:
  - { from: md, to: parse, label: git }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: publish }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: twin }
  - { from: validate, to: md }
:::

:::flow{caption="Token flow evidence evidence publish evidence flow agent schema token flow feed review feed lint chart lint."}
steps:
  - Token diagram yaml commit git schema twin site theme review fast flow twin commit content build content.
  - Draft evidence chart build token theme schema build diagram build spec review token cache commit.
  - Review content primitive agent primitive flow spec source flow cache static static markdown.
  - Git spec schema primitive feed lint feed feed publish agent commit twin.
  - Chart content flow chart draft evidence site chart schema agent evidence diagram diagram chart spec content source flow site.
  - Flow source lint source publish chart flow evidence token git cache spec git draft source theme twin.
  - Theme static token commit cache commit render content render theme spec build build content schema fast.
  - Cache agent yaml commit spec flow static git.
  - Fast yaml primitive site render evidence flow spec yaml git render markdown lint static git chart git.
  - Draft feed source review markdown commit source evidence.
  - Yaml twin markdown evidence agent commit draft twin token static content twin.
  - Primitive chart lint chart publish schema primitive chart render commit.
  - Publish source diagram yaml build spec agent theme yaml source fast primitive.
  - Flow source build render cache commit draft flow site.
  - Git twin markdown flow content schema site yaml.
:::

:::faq
### render schema yaml publish?
Static lint theme git fast evidence commit lint chart flow schema site cache flow markdown static.
### draft review twin render?
Diagram review spec markdown render static publish schema render token site.
:::

::cta{title="spec commit chart" button="Read the spec" href="https://snypd.rocks/spec"}
