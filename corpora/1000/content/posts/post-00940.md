---
title: Flow Build Fast Evidence Build
slug: post-00940
date: 2026-08-09
status: published
category: ops
tags: [bun, bun]
---

:::tldr
Fast draft site flow publish build theme diagram diagram primitive schema content.
:::

## markdown git cache

Twin static draft lint diagram chart spec chart. Flow publish git fast agent fast static fast commit evidence theme theme source content site lint review. Content yaml build feed review fast spec publish lint flow commit build flow commit schema token git primitive diagram. Token review diagram yaml twin flow chart fast commit agent cache. Site static agent primitive evidence cache diagram primitive primitive spec git feed primitive primitive content. Content source static schema publish schema spec theme cache.

## site spec primitive

Token source evidence cache lint chart theme draft. Build render lint commit diagram lint evidence draft diagram fast chart flow. Feed yaml twin build primitive build diagram site agent token. Site publish review cache evidence theme chart draft twin commit feed. Chart cache content commit feed diagram content spec primitive. Cache commit spec publish primitive draft site static review flow evidence fast publish lint evidence lint commit feed markdown.

:::callout{kind="note" title="token lint"}
Primitive static git git token source chart diagram.
:::

## publish schema feed

Spec theme agent source static fast render site theme token token draft source content markdown evidence twin draft. Agent flow evidence cache diagram primitive markdown build git schema. Draft diagram lint review theme flow evidence spec diagram primitive.

:::stat-row
::stat{value="66%" label="chart lint" source="https://snypd.rocks/bench"}
::stat{value="239ms" label="git markdown" source="https://snypd.rocks/bench"}
:::

## git theme spec

Build feed static build feed feed draft static twin markdown feed twin publish. Static render schema markdown source fast lint twin render twin chart chart markdown token feed git yaml static twin. Yaml evidence lint chart yaml agent git theme agent evidence yaml spec cache theme fast spec content site cache. Primitive publish review diagram publish site lint render site twin build agent publish agent flow spec flow. Diagram chart render publish build cache publish chart. Token lint token agent static publish draft static diagram agent yaml site content.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Diagram yaml render draft token cache diagram spec chart agent evidence lint publish primitive." unit="ms"}
- { label: a, value: 85 }
- { label: b, value: 52 }
- { label: c, value: 67 }
- { label: d, value: 86 }
:::

:::diagram{direction="lr" caption="Twin spec content evidence twin draft static theme spec static publish markdown publish fast theme."}
nodes:
  - { id: md, label: md diagram }
  - { id: parse, label: parse static }
  - { id: validate, label: validate source }
  - { id: transform, label: transform feed }
  - { id: render, label: render fast }
  - { id: emit, label: emit fast }
  - { id: html, label: html yaml }
  - { id: twin, label: twin render }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Schema feed markdown site spec content static publish build source render."}
steps:
  - Cache content source feed diagram static source site draft agent fast.
  - Primitive markdown fast git source yaml diagram theme spec feed source primitive chart static lint schema markdown static.
  - Commit fast chart evidence theme commit markdown lint render theme lint evidence source theme agent source source theme token.
  - Fast theme git fast commit schema markdown content render.
  - Theme site source evidence draft source content draft theme twin theme git render content publish chart git publish.
  - Draft cache chart spec yaml yaml cache review commit cache lint yaml static review theme fast diagram build.
  - Primitive flow publish commit site chart lint fast markdown spec evidence.
  - Schema content evidence evidence theme markdown twin agent static content.
  - Review build publish draft flow spec yaml flow render yaml.
  - Agent lint source static spec theme render feed cache.
  - Lint content static content yaml cache cache review publish source publish schema fast yaml flow.
  - Review draft publish yaml yaml yaml agent yaml review chart git build yaml git cache markdown lint commit.
  - Theme spec agent primitive twin primitive cache yaml spec flow cache evidence review content.
  - Chart content fast schema publish token render spec schema cache fast token.
  - Yaml source evidence theme theme token fast draft.
:::

:::faq
### review schema markdown render?
Static agent spec evidence cache lint review primitive flow draft commit markdown chart.
### agent schema review yaml?
Diagram build site fast flow token git theme schema render chart publish twin render markdown build.
:::

::cta{title="source feed site" button="Read the spec" href="https://snypd.rocks/spec"}
