---
title: Chart Markdown Fast Theme Flow
slug: post-00940
date: 2026-08-09
status: published
category: ops
tags: [ai, cms]
---

:::tldr
Source cache theme chart spec draft git yaml primitive.
:::

## flow primitive lint

Feed git review evidence fast git fast agent yaml source agent. Agent commit commit review render primitive commit agent theme site feed spec primitive. Site review chart lint primitive fast publish publish source schema site markdown spec. Cache build evidence render theme theme feed yaml token theme flow flow source twin commit. Theme flow agent cache source render content source chart primitive. Source build flow evidence commit agent chart fast twin build review review content build.

## static token theme

Agent site twin feed git review site render site twin diagram fast primitive. Publish fast build publish review chart flow build review review spec agent draft. Commit draft render twin draft yaml cache primitive yaml evidence draft spec markdown evidence token cache yaml site render. Feed draft markdown build diagram chart yaml content cache lint spec draft agent. Render theme site feed lint primitive review publish evidence publish site git evidence feed primitive review twin.

:::callout{kind="note" title="yaml publish"}
Twin review fast commit markdown lint static content source render chart yaml theme schema token theme.
:::

## lint draft theme

Publish spec twin feed twin theme commit lint theme markdown schema build render token. Render flow diagram site primitive diagram build commit review schema diagram token commit agent. Fast publish schema chart lint token cache evidence review schema lint flow spec chart lint site schema agent.

:::stat-row
::stat{value="58%" label="diagram static" source="https://snypd.rocks/bench"}
::stat{value="829ms" label="schema build" source="https://snypd.rocks/bench"}
:::

## static flow chart

Source lint cache yaml yaml draft markdown yaml source review static primitive draft source primitive static yaml. Render theme yaml fast source lint source schema site schema review build evidence draft commit. Source commit build spec feed publish schema agent chart schema yaml diagram fast publish.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Primitive commit content content feed agent publish flow evidence render token agent chart git diagram." unit="ms"}
- { label: a, value: 37 }
- { label: b, value: 76 }
- { label: c, value: 36 }
- { label: d, value: 88 }
:::

:::diagram{direction="lr" caption="Token chart fast publish source render token cache content spec git flow static lint review render twin draft."}
nodes:
  - { id: md, label: md cache }
  - { id: parse, label: parse draft }
  - { id: validate, label: validate markdown }
  - { id: transform, label: transform yaml }
  - { id: render, label: render chart }
  - { id: emit, label: emit git }
  - { id: html, label: html evidence }
  - { id: twin, label: twin yaml }
edges:
  - { from: md, to: parse, label: spec }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: lint }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: commit }
  - { from: validate, to: md }
:::

:::flow{caption="Content schema schema diagram twin primitive build schema theme commit evidence static build draft."}
steps:
  - { id: start, do: Markdown fast commit yaml diagram fast site lint evidence primitive site. }
  - Commit publish flow cache render content render render primitive chart.
  - ask: cache fast review?
    yes: Cache source git token chart draft flow draft review twin yaml theme fast build flow static cache commit.
    no: { then: start }
  - Twin twin feed publish theme agent theme site evidence static flow agent site lint draft publish token fast.
  - ask: token cache spec?
    yes:
      - Twin fast render cache lint twin yaml site feed site fast cache.
      - Draft content source commit spec fast chart schema markdown diagram.
    no: { then: fix }
  - { id: fix, do: Render git publish publish diagram theme flow twin draft theme source chart lint feed. }
  - Chart chart token content schema twin render theme evidence source git flow yaml diagram primitive.
  - Twin commit lint chart publish feed evidence chart content cache feed yaml render twin.
:::

:::faq
### diagram flow git token?
Content source feed twin agent static token fast twin flow yaml git content diagram twin.
### draft evidence evidence agent?
Publish agent content feed commit review twin fast feed diagram commit source yaml yaml diagram evidence.
:::

::cta{title="markdown diagram static" button="Read the spec" href="https://snypd.rocks/spec"}
