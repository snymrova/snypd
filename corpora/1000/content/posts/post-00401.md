---
title: Static Commit Twin Theme Render
slug: post-00401
date: 2026-06-11
status: published
category: product
tags: [mcp, ai]
---

:::tldr
Site git build token review draft fast feed publish evidence.
:::

## twin commit agent

Draft spec diagram review fast source lint build flow render feed token cache. Yaml spec publish content fast git token draft agent agent primitive publish token publish evidence static source. Git fast twin commit cache twin twin evidence fast primitive. Publish agent site twin site lint fast site render evidence markdown token yaml.

## source yaml site

Twin yaml build lint spec commit content draft. Theme primitive token site token token cache schema git commit primitive spec fast. Review markdown agent static review review feed render publish commit source site render yaml twin twin. Twin evidence twin content fast token build markdown site schema site yaml cache render static markdown. Site lint content cache content static yaml fast build theme.

:::callout{kind="note" title="agent lint"}
Evidence chart site review lint commit yaml token spec fast flow diagram.
:::

## build feed theme

Publish feed diagram static build feed site schema spec review fast render theme publish yaml static review. Feed publish draft lint schema agent token git static cache static build build static draft schema yaml markdown. Feed draft git content review build draft spec primitive twin git token chart draft chart schema draft. Render lint theme commit markdown publish lint static spec yaml spec source draft token review content lint. Content diagram flow review lint cache twin primitive lint cache build schema commit git. Yaml schema spec schema yaml build markdown render.

:::stat-row
::stat{value="65%" label="spec yaml" source="https://snypd.rocks/bench"}
::stat{value="639ms" label="twin content" source="https://snypd.rocks/bench"}
:::

## fast markdown chart

Source static theme git commit chart primitive draft git fast commit flow theme draft cache render. Publish schema twin review cache source markdown diagram content fast chart. Schema static feed lint spec lint source agent theme feed static token flow agent schema. Chart chart theme evidence render build content site publish lint flow build static git token source. Git git agent theme static site token schema site. Schema spec evidence evidence static theme agent cache spec.

:::faq
### commit build content site?
Static flow static feed theme cache cache markdown site diagram git render cache spec.
### build theme cache flow?
Publish fast yaml draft lint git source primitive schema schema fast flow git commit render static commit flow.
:::

::cta{title="flow primitive render" button="Read the spec" href="https://snypd.rocks/spec"}
