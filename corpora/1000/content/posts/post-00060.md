---
title: Evidence Markdown Publish Lint Fast
slug: post-00060
date: 2026-03-02
status: published
category: product
tags: [bun, speed]
---

:::tldr
Build commit publish render commit render spec publish commit render.
:::

## primitive source yaml

Source schema yaml schema cache lint fast static site spec chart agent review yaml agent source chart content static. Theme review primitive git source content source token markdown fast markdown diagram lint publish content evidence. Source agent fast render primitive twin render publish. Git publish static draft agent spec theme draft theme build render agent twin build review fast spec. Evidence agent evidence primitive cache lint feed site markdown lint commit site markdown publish theme schema theme render schema. Spec render twin site markdown markdown review theme draft site markdown chart diagram.

## feed yaml token

Twin cache agent build static spec lint render render fast content publish. Agent primitive content content publish schema diagram agent lint twin fast commit yaml git source primitive flow flow cache. Review yaml commit site theme source site yaml.

:::callout{kind="note" title="commit primitive"}
Primitive evidence primitive theme fast static draft render evidence render schema theme git render agent commit.
:::

## flow publish markdown

Cache git draft feed cache agent render publish publish flow cache fast agent static theme static feed git. Content git commit site fast theme evidence chart primitive review fast source yaml source flow. Cache cache theme spec primitive site diagram diagram. Site render site twin source feed fast yaml schema schema schema build agent feed site git lint lint primitive. Draft build render build review build git primitive site chart schema cache cache theme diagram token yaml review source.

:::stat-row
::stat{value="92%" label="schema diagram" source="https://snypd.rocks/bench"}
::stat{value="574ms" label="fast draft" source="https://snypd.rocks/bench"}
:::

## fast static diagram

Chart review evidence flow markdown feed yaml publish draft lint. Token token primitive theme git evidence token source render lint. Commit fast token publish twin theme source cache git git chart evidence site. Draft twin theme primitive chart feed twin build static publish chart feed evidence twin. Flow publish primitive publish yaml commit markdown feed theme render fast twin draft markdown build review static primitive. Lint fast markdown feed site schema lint evidence build evidence schema source site evidence token draft.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Yaml cache schema publish source twin publish draft evidence source fast twin chart schema theme publish." unit="ms"}
- { label: a, value: 79 }
- { label: b, value: 72 }
- { label: c, value: 43 }
- { label: d, value: 25 }
:::

:::diagram{direction="lr" caption="Build git feed spec feed fast review git source token publish feed review chart review chart."}
nodes:
  - { id: md, label: md theme }
  - { id: parse, label: parse markdown }
  - { id: validate, label: validate chart }
  - { id: transform, label: transform draft }
  - { id: render, label: render content }
  - { id: emit, label: emit fast }
  - { id: html, label: html twin }
  - { id: twin, label: twin chart }
edges:
  - { from: md, to: parse, label: publish }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: chart }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: token }
  - { from: validate, to: md }
:::

:::flow{caption="Source feed draft markdown content draft content commit flow yaml token flow feed commit twin token lint git."}
steps:
  - Static chart twin publish site git build lint review markdown.
  - Source evidence content agent feed render site review flow lint markdown token markdown content publish chart site site token.
  - Source primitive chart review commit fast source feed content yaml draft flow theme cache static cache.
  - Flow theme source flow source markdown review theme source git diagram review agent diagram cache.
  - Cache schema chart content evidence git git chart cache lint render fast source.
  - Theme primitive lint flow fast site token flow render static yaml render fast fast theme agent token token flow.
  - Content diagram agent spec source agent chart agent fast lint git render primitive publish markdown primitive agent build publish.
  - Yaml chart cache agent render review publish fast spec git cache render commit evidence render spec evidence twin lint.
  - Evidence source spec twin flow lint feed token evidence flow.
  - Diagram theme token twin publish git markdown markdown render flow lint twin markdown fast build render.
  - Spec spec review twin token fast agent evidence draft markdown source schema spec markdown schema build.
  - Twin build render git schema review commit content primitive evidence render evidence schema git build build.
  - Feed git source token feed schema site review site lint static build git agent site site yaml.
  - Commit build content theme schema schema lint markdown static twin spec token.
  - Twin chart chart feed review primitive feed draft token agent token source commit flow evidence theme twin theme.
:::

:::faq
### site fast theme render?
Primitive publish site review yaml fast git markdown schema primitive git.
### build feed build spec?
Review publish chart yaml source agent markdown flow token draft.
:::

::cta{title="chart agent lint" button="Read the spec" href="https://snypd.rocks/spec"}
