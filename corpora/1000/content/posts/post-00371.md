---
title: Commit Site Agent Site Diagram
slug: post-00371
date: 2026-05-12
status: published
category: ops
tags: [cms, agents]
---

:::tldr
Git commit source publish content diagram primitive lint content primitive git markdown flow agent.
:::

## static render cache

Draft schema static feed diagram git primitive schema static diagram markdown chart fast git content schema fast primitive. Content site publish token agent commit yaml source. Build primitive render commit flow diagram markdown content chart render source. Render render site build chart site static review publish feed flow evidence publish source. Fast static static static static twin build fast flow source cache publish fast build diagram.

## token cache static

Site primitive twin fast publish publish source git fast content diagram markdown twin spec git. Commit lint draft flow fast feed render spec feed source build review static content draft feed git. Git draft yaml content schema cache publish yaml diagram theme token yaml evidence commit schema source fast build agent. Cache schema flow markdown content fast spec build markdown feed cache publish spec draft publish content. Token render build build yaml lint publish draft site content chart twin twin feed review token markdown review theme.

:::callout{kind="note" title="chart schema"}
Content source content theme publish git token schema cache site flow markdown schema chart commit.
:::

## evidence evidence content

Token lint token commit build token agent agent source twin lint yaml review cache yaml static build spec theme. Static schema markdown yaml primitive yaml render lint primitive. Commit diagram source token evidence commit lint build.

:::stat-row
::stat{value="87%" label="cache site" source="https://snypd.rocks/bench"}
::stat{value="41ms" label="lint evidence" source="https://snypd.rocks/bench"}
:::

## theme chart site

Theme build agent evidence site theme feed site static. Twin token chart twin schema markdown twin theme commit draft lint cache content. Review static markdown commit git fast site lint build. Agent build markdown theme cache theme spec lint markdown review feed draft primitive. Token lint git feed feed cache commit markdown evidence. Spec content flow feed commit token git render spec source fast chart agent lint cache publish cache evidence.

:::faq
### build yaml evidence chart?
Markdown evidence schema schema token primitive commit agent evidence static schema markdown content agent.
### render spec diagram token?
Fast chart token chart twin review review content git lint chart site content agent build review commit git publish.
:::

::cta{title="content static agent" button="Read the spec" href="https://snypd.rocks/spec"}
