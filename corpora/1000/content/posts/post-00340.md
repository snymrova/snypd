---
title: Theme Token Build Publish Commit
slug: post-00340
date: 2026-04-11
status: published
category: design
tags: [agents, ai]
---

:::tldr
Schema render primitive git primitive fast token content evidence spec commit primitive primitive.
:::

## feed twin chart

Spec review theme theme publish primitive review evidence content publish theme static render. Draft publish yaml content markdown feed static source static static source source diagram markdown content primitive agent chart. Primitive schema evidence draft flow theme draft publish lint source theme chart feed schema evidence. Source spec flow yaml lint draft markdown schema spec render theme spec primitive diagram git fast feed agent commit. Token yaml git commit token yaml schema build yaml. Review markdown token build schema yaml yaml theme primitive commit source.

## static diagram render

Review token render spec flow site schema lint render token. Primitive lint site build yaml review site markdown spec evidence twin publish publish publish primitive cache evidence. Static review token yaml schema diagram yaml feed yaml site token git primitive source fast git twin static lint. Yaml fast lint yaml twin source primitive git review twin yaml markdown commit yaml theme render diagram. Source yaml draft lint build content publish review fast publish content evidence theme review diagram diagram render. Twin flow fast lint fast theme schema twin spec cache.

:::callout{kind="note" title="lint theme"}
Render feed diagram yaml feed chart yaml yaml token git theme static token yaml static schema twin site draft.
:::

## twin content source

Commit lint content token yaml theme evidence feed. Schema cache git chart feed chart yaml diagram token cache site yaml flow theme review agent git. Flow draft review lint spec review flow commit content yaml draft build flow feed feed draft build. Markdown publish feed git draft primitive static evidence feed markdown fast schema static theme chart schema lint. Spec agent content static agent schema theme site flow draft. Build token publish content publish chart evidence schema review schema markdown primitive.

:::stat-row
::stat{value="78%" label="git markdown" source="https://snypd.rocks/bench"}
::stat{value="680ms" label="evidence content" source="https://snypd.rocks/bench"}
:::

## site content feed

Lint site theme flow source draft spec review source draft static git. Schema source chart git spec commit source draft twin evidence. Schema review publish chart yaml yaml markdown render. Commit content commit agent commit commit draft theme token primitive primitive cache primitive static markdown feed lint fast. Review markdown render draft static evidence fast build. Review static commit git static feed draft agent source twin lint source.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Markdown cache build publish agent theme site publish twin token spec diagram token publish." unit="ms"}
- { label: a, value: 14 }
- { label: b, value: 90 }
- { label: c, value: 52 }
- { label: d, value: 11 }
:::

:::diagram{direction="lr" caption="Review site markdown source spec twin feed token git evidence draft."}
nodes:
  - { id: md, label: md git }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate markdown }
  - { id: transform, label: transform review }
  - { id: render, label: render twin }
  - { id: emit, label: emit static }
  - { id: html, label: html yaml }
  - { id: twin, label: twin evidence }
edges:
  - { from: md, to: parse, label: markdown }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: theme }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: content }
  - { from: validate, to: md }
:::

:::flow{caption="Chart diagram content spec draft token review spec yaml draft twin feed chart feed chart spec."}
steps:
  - Primitive feed source markdown commit feed commit publish yaml.
  - Evidence lint feed cache evidence feed draft build draft primitive chart lint diagram primitive static.
  - Commit evidence chart primitive twin cache static token flow commit feed cache site fast build commit diagram cache.
  - Git git markdown schema git theme schema yaml spec.
  - Markdown fast yaml content review draft feed evidence lint schema source.
  - Review yaml commit evidence primitive flow flow publish content.
  - Flow flow fast render token content cache site site static build site.
  - Feed render flow primitive cache spec schema cache feed review chart chart site evidence publish flow publish lint.
  - Feed draft git schema feed theme source build git spec cache token markdown yaml spec build.
  - Draft feed primitive token twin spec theme site token cache flow agent.
  - Git schema theme source git git evidence cache source review.
  - Token cache twin feed agent theme yaml fast primitive.
  - Evidence chart cache feed markdown commit site review feed source cache commit diagram cache build content.
  - Flow chart flow theme theme agent markdown token chart git flow fast chart git evidence.
  - Flow commit site yaml publish agent site source draft markdown token agent theme render.
:::

:::faq
### diagram content build publish?
Chart publish markdown git agent source primitive agent lint cache token cache spec markdown static.
### git agent yaml feed?
Git evidence spec git token fast primitive lint source render theme primitive yaml.
:::

::cta{title="git evidence yaml" button="Read the spec" href="https://snypd.rocks/spec"}
