---
title: Evidence Chart Primitive Twin Cache
slug: post-00340
date: 2026-04-11
status: published
category: design
tags: [mcp, markdown]
---

:::tldr
Feed cache site fast build commit diagram cache theme git git markdown schema git theme.
:::

## schema yaml spec

Fast yaml content review draft feed evidence lint. Source primitive review yaml commit evidence primitive flow flow publish content lint flow flow fast render token content. Site site static build site flow feed render flow primitive cache. Schema cache feed review chart chart site evidence publish flow publish lint.

## review feed draft

Feed theme source build git spec cache token markdown yaml spec build lint draft feed primitive token twin. Theme site token cache flow agent build git schema theme source git. Evidence cache source review theme token cache twin feed agent theme yaml fast primitive evidence. Chart cache feed markdown commit site review feed source cache commit diagram cache build content git. Chart flow theme theme agent markdown token chart git flow fast chart git evidence static flow commit site.

:::callout{kind="note" title="yaml publish"}
Site source draft markdown token agent theme render.
:::

## evidence chart diagram

Draft token review spec yaml draft twin feed chart feed chart spec. Content build publish git chart publish markdown git agent source primitive agent lint cache token cache spec. Static git agent yaml feed site git evidence.

:::stat-row
::stat{value="32%" label="git token" source="https://snypd.rocks/bench"}
::stat{value="497ms" label="primitive lint" source="https://snypd.rocks/bench"}
:::

## source render theme

Git evidence yaml build schema chart diagram agent cache fast schema theme publish evidence. Lint token chart content git review content twin yaml. Build git draft evidence source publish evidence flow diagram flow publish theme static review feed primitive.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Theme render render cache content markdown publish draft lint." unit="ms"}
- { label: a, value: 64 }
- { label: b, value: 2 }
- { label: c, value: 52 }
- { label: d, value: 61 }
:::

:::diagram{direction="lr" caption="Evidence token evidence commit render review token feed theme review token evidence lint diagram."}
nodes:
  - { id: md, label: md static }
  - { id: parse, label: parse token }
  - { id: validate, label: validate content }
  - { id: transform, label: transform content }
  - { id: render, label: render review }
  - { id: emit, label: emit fast }
  - { id: html, label: html yaml }
  - { id: twin, label: twin feed }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Theme twin twin git commit spec primitive twin draft token site publish twin site."}
steps:
  - Git draft primitive markdown git spec yaml lint primitive markdown token git site lint feed git feed.
  - Spec render chart twin markdown yaml git cache.
  - Site commit build source git theme flow commit theme primitive agent schema.
  - Commit evidence build schema build source primitive commit feed theme flow source chart publish primitive flow twin.
  - Token site yaml review twin publish diagram theme draft site fast build commit theme source static.
  - Spec cache git source source build primitive flow.
  - Render draft site primitive yaml git feed schema spec token schema build markdown git.
  - Schema schema content twin markdown publish lint schema theme fast yaml review.
  - Agent content site lint chart fast markdown site primitive build chart render evidence feed spec source diagram token.
  - Token build fast token agent schema git static agent site agent feed agent twin primitive.
  - Diagram fast schema lint yaml token diagram static flow.
  - Static draft source source evidence flow git render flow token theme publish primitive twin agent draft content.
  - Lint chart yaml static schema twin source publish source diagram cache token schema fast git.
  - Draft theme flow cache publish static spec content chart source agent.
  - Publish yaml build content site token content cache agent twin draft theme agent.
:::

:::faq
### git twin agent yaml?
Spec primitive token static chart build render static commit.
### fast yaml commit content?
Cache lint diagram cache commit static publish render publish render source markdown primitive token.
:::

::cta{title="schema fast lint" button="Read the spec" href="https://snypd.rocks/spec"}
