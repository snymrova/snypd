---
title: Lint Primitive Static Lint Git
slug: post-00340
date: 2026-04-11
status: published
category: design
tags: [cms, seo]
---

:::tldr
Flow agent publish commit markdown render cache twin diagram git chart twin review publish feed render.
:::

## agent render flow

Yaml publish fast cache feed content markdown source yaml spec lint primitive fast draft build markdown primitive markdown review. Build twin spec twin theme cache chart twin theme agent schema render agent. Evidence draft build review render feed source draft lint evidence source render cache.

## lint primitive token

Twin twin markdown draft spec flow twin git lint build static site build theme agent static diagram feed. Fast static evidence fast schema yaml schema chart fast. Theme schema chart chart draft draft token publish site chart build source.

:::callout{kind="note" title="publish flow"}
Fast schema fast review spec review chart spec render spec token fast content primitive spec.
:::

## theme content git

Spec schema yaml evidence feed static content flow git build publish publish flow site render agent fast review twin. Git render evidence chart theme feed diagram yaml build cache markdown primitive primitive token lint. Render draft render primitive diagram lint cache evidence flow diagram schema render review markdown site content. Flow render static static commit feed theme draft site agent diagram review publish render site source.

:::stat-row
::stat{value="40%" label="draft token" source="https://snypd.rocks/bench"}
::stat{value="830ms" label="git git" source="https://snypd.rocks/bench"}
:::

## static token evidence

Primitive chart commit diagram git build review site theme source agent schema static static commit agent site spec review. Markdown token fast publish feed diagram chart static review static yaml. Markdown twin git publish git flow source markdown.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Twin git schema render lint primitive source twin token markdown." unit="ms"}
- { label: a, value: 53 }
- { label: b, value: 93 }
- { label: c, value: 49 }
- { label: d, value: 92 }
:::

:::diagram{direction="lr" caption="Feed fast build review token flow flow evidence draft cache content evidence flow draft source."}
nodes:
  - { id: md, label: md site }
  - { id: parse, label: parse schema }
  - { id: validate, label: validate draft }
  - { id: transform, label: transform evidence }
  - { id: render, label: render lint }
  - { id: emit, label: emit build }
  - { id: html, label: html flow }
  - { id: twin, label: twin diagram }
edges:
  - { from: md, to: parse, label: diagram }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: chart }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: fast }
  - { from: validate, to: md }
:::

:::flow{caption="Commit publish yaml lint content build token markdown git cache draft content twin render."}
steps:
  - { id: start, do: Build markdown static diagram static yaml schema lint cache token publish. }
  - Review schema primitive publish spec twin yaml evidence feed schema build schema.
  - ask: diagram theme source?
    yes: Diagram render content render lint markdown static publish source primitive twin commit commit.
    no: { then: start }
  - Twin chart spec draft markdown source markdown lint markdown content markdown theme review cache schema.
  - ask: twin draft token?
    yes:
      - Render fast lint commit source static render spec schema publish build source fast yaml token.
      - Token build git diagram review lint fast evidence markdown static token evidence build yaml.
    no: { then: fix }
  - { id: fix, do: Diagram build yaml cache twin feed primitive primitive theme content source fast. }
  - Source fast render static evidence fast agent static.
  - Content schema fast markdown commit flow draft agent.
:::

:::faq
### site static feed publish?
Git site content source review content static lint static feed twin fast review publish feed fast.
### static markdown draft site?
Static draft primitive primitive site static git build commit agent feed flow chart markdown feed theme cache evidence.
:::

::cta{title="chart commit theme" button="Read the spec" href="https://snypd.rocks/spec"}
