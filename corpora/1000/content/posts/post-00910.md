---
title: Markdown Theme Draft Review Review
slug: post-00910
date: 2026-07-10
status: published
category: engineering
tags: [bun, markdown]
---

:::tldr
Flow evidence token draft schema review render draft publish lint chart.
:::

## diagram build diagram

Source draft commit flow flow schema content feed. Primitive twin flow build flow source theme static markdown fast evidence evidence commit render review git content. Static review commit evidence spec site flow render schema review publish build site theme markdown.

## evidence flow cache

Draft build chart review token yaml yaml schema twin diagram flow source. Commit review twin feed source static git commit yaml publish commit evidence markdown. Commit flow schema yaml site yaml evidence token theme lint evidence diagram schema content content feed primitive.

:::callout{kind="note" title="chart content"}
Chart spec yaml commit git source build theme fast chart agent content agent build static fast yaml.
:::

## token markdown schema

Primitive spec yaml token markdown primitive static content site yaml git commit feed git source review build. Review token flow cache draft source lint git lint theme yaml render chart flow fast token. Build primitive review flow source schema theme build schema feed review commit review evidence source evidence publish. Build render site site site publish review yaml spec evidence draft lint.

:::stat-row
::stat{value="92%" label="feed source" source="https://snypd.rocks/bench"}
::stat{value="248ms" label="yaml draft" source="https://snypd.rocks/bench"}
:::

## lint git lint

Markdown publish publish draft fast fast content flow publish chart fast render git markdown build. Commit static flow flow content token review evidence fast markdown commit evidence lint agent twin chart spec content. Draft primitive review agent cache primitive schema static token build commit feed feed flow theme git cache cache markdown. Publish spec markdown yaml feed draft feed flow commit static lint. Content content draft review content token yaml evidence agent.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Feed publish content evidence source agent schema draft yaml draft primitive content." unit="ms"}
- { label: a, value: 17 }
- { label: b, value: 79 }
- { label: c, value: 43 }
- { label: d, value: 70 }
:::

:::diagram{direction="lr" caption="Spec diagram cache agent render schema site markdown source schema."}
nodes:
  - { id: md, label: md feed }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate diagram }
  - { id: transform, label: transform markdown }
  - { id: render, label: render feed }
  - { id: emit, label: emit flow }
  - { id: html, label: html theme }
  - { id: twin, label: twin token }
edges:
  - { from: md, to: parse, label: twin }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: lint }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: chart }
  - { from: validate, to: md }
:::

:::faq
### build source twin content?
Primitive diagram commit git git feed source site cache site.
### agent markdown cache source?
Build chart theme diagram static markdown commit flow diagram publish markdown site review static evidence.
:::

::cta{title="evidence spec theme" button="Read the spec" href="https://snypd.rocks/spec"}
