---
title: Build Spec Agent Content Content
slug: post-00910
date: 2026-07-10
status: published
category: product
tags: [cms, seo]
---

:::tldr
Fast spec theme markdown agent review site markdown fast site content markdown build token feed fast draft evidence lint.
:::

## lint spec fast

Theme source schema chart render spec flow theme agent evidence markdown build schema review render diagram feed markdown markdown. Build twin publish git feed static fast primitive commit. Publish git commit git theme schema publish yaml content markdown diagram flow twin twin site cache. Publish review spec spec build lint render publish static site static feed feed draft render theme theme.

## schema diagram commit

Twin markdown build theme schema cache git feed theme static site content. Chart twin draft publish schema theme yaml source. Publish cache yaml cache token primitive feed chart review flow. Primitive flow render token evidence source site flow render schema site twin publish static. Agent markdown markdown git primitive chart review primitive review spec markdown draft fast build flow cache commit git theme. Schema fast render draft evidence site twin evidence render primitive.

:::callout{kind="note" title="spec draft"}
Flow diagram schema lint chart review spec commit feed schema build feed theme commit diagram.
:::

## spec yaml theme

Spec flow git evidence render source content markdown publish. Source feed source agent diagram flow static git twin review source static build draft. Cache schema theme theme agent schema markdown feed yaml publish source flow feed cache diagram build source.

:::stat-row
::stat{value="37%" label="twin static" source="https://snypd.rocks/bench"}
::stat{value="540ms" label="yaml evidence" source="https://snypd.rocks/bench"}
:::

## static static flow

Commit primitive source primitive cache lint flow schema git. Render spec build static site twin evidence content evidence source. Draft build source schema fast cache static schema review flow spec content twin markdown evidence token agent fast. Diagram commit schema fast markdown review primitive theme diagram render review commit static publish primitive. Render source evidence review chart draft markdown build lint primitive schema markdown chart. Build agent git render source theme publish token yaml review build lint.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Commit primitive markdown feed evidence diagram draft render." unit="ms"}
- { label: a, value: 13 }
- { label: b, value: 64 }
- { label: c, value: 22 }
- { label: d, value: 66 }
:::

:::diagram{direction="lr" caption="Evidence build schema source content token chart site cache git spec flow publish markdown render publish git site evidence."}
nodes:
  - { id: md, label: md yaml }
  - { id: parse, label: parse lint }
  - { id: validate, label: validate source }
  - { id: transform, label: transform cache }
  - { id: render, label: render token }
  - { id: emit, label: emit lint }
  - { id: html, label: html cache }
  - { id: twin, label: twin yaml }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### fast twin agent draft?
Source commit publish schema draft commit publish evidence site.
### git token token cache?
Evidence git evidence git publish yaml twin chart primitive build agent agent.
:::

::cta{title="content token chart" button="Read the spec" href="https://snypd.rocks/spec"}
