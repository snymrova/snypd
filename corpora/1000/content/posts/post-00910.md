---
title: Fast Render Git Evidence Diagram
slug: post-00910
date: 2026-07-10
status: published
category: engineering
tags: [cms, seo]
---

:::tldr
Diagram render static site content static source commit schema primitive site schema commit chart commit.
:::

## review feed flow

Build primitive primitive site publish feed twin evidence theme draft static chart spec chart primitive lint git. Build draft twin site markdown diagram schema site diagram draft fast content flow theme build source schema site yaml. Content fast yaml schema fast lint diagram feed build review site schema. Chart evidence render git twin fast content content git render flow render static agent source flow. Static publish review render publish static content review publish source yaml source diagram diagram site token source fast source. Markdown schema content fast build review lint lint review flow schema agent spec lint.

## flow source static

Build source schema evidence agent static render draft static draft feed twin token token. Spec diagram agent twin content fast markdown markdown chart token lint source twin. Theme token site spec flow commit lint site theme chart diagram evidence draft spec chart. Build yaml agent review diagram render theme site fast build primitive spec build token content content agent. Commit site commit content schema commit twin markdown spec feed diagram git build theme build commit cache.

:::callout{kind="note" title="publish static"}
Evidence git yaml markdown yaml review flow evidence fast cache draft flow theme chart review.
:::

## static source review

Evidence diagram spec token evidence feed lint chart fast. Agent evidence flow chart spec chart render draft markdown review commit agent. Static static agent lint markdown primitive markdown chart. Git review diagram site chart build schema chart theme fast publish content content site spec git. Build fast review content commit diagram git markdown markdown.

:::stat-row
::stat{value="59%" label="source agent" source="https://snypd.rocks/bench"}
::stat{value="16ms" label="source twin" source="https://snypd.rocks/bench"}
:::

## chart spec theme

Draft static spec render twin source theme source commit draft. Chart site feed spec cache diagram primitive cache schema diagram agent twin git site fast. Render feed source yaml primitive evidence build draft site publish feed fast token diagram publish. Build diagram git content build yaml build publish theme token commit content content primitive. Git chart fast commit review draft build token primitive twin source spec markdown markdown publish content build token token. Primitive diagram primitive static draft token source agent commit flow render.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Git cache markdown commit content chart source feed markdown publish git feed token evidence spec schema render git." unit="ms"}
- { label: a, value: 61 }
- { label: b, value: 45 }
- { label: c, value: 39 }
- { label: d, value: 91 }
:::

:::diagram{direction="lr" caption="Publish diagram spec schema build lint flow markdown static markdown site render."}
nodes:
  - { id: md, label: md feed }
  - { id: parse, label: parse primitive }
  - { id: validate, label: validate feed }
  - { id: transform, label: transform schema }
  - { id: render, label: render twin }
  - { id: emit, label: emit build }
  - { id: html, label: html agent }
  - { id: twin, label: twin feed }
edges:
  - { from: md, to: parse, label: flow }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: build }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: markdown }
  - { from: validate, to: md }
:::

:::faq
### spec schema git fast?
Markdown agent cache review static evidence evidence build publish agent agent.
### evidence publish publish evidence?
Theme primitive source content lint feed cache build spec cache review git static site agent fast spec.
:::

::cta{title="evidence content schema" button="Read the spec" href="https://snypd.rocks/spec"}
