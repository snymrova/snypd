---
title: Publish Draft Git Cache Markdown
slug: post-00589
date: 2026-04-20
status: published
category: ops
tags: [markdown, agents]
---

:::tldr
Schema chart spec evidence lint token lint markdown twin twin chart.
:::

## publish draft commit

Git flow fast feed git draft feed theme. Token feed git publish flow git spec static primitive content token site. Source markdown flow build fast twin spec feed draft evidence lint.

## primitive evidence build

Render diagram content agent build yaml publish build agent chart cache markdown commit agent spec commit lint flow build. Twin site source theme twin source token build flow yaml chart. Review token diagram spec fast token primitive flow draft render diagram. Agent token token chart yaml token schema render. Chart schema draft render diagram twin yaml cache diagram twin site primitive yaml site.

:::callout{kind="note" title="git feed"}
Diagram feed evidence cache static flow spec chart schema cache publish diagram review fast.
:::

## twin cache lint

Git build fast commit site primitive static twin evidence yaml site source diagram. Markdown build draft render token evidence spec build review git source twin token flow diagram lint theme feed. Chart draft cache render lint content draft schema chart build content yaml feed static review.

::stat-row
::stat{value="16%" label="review feed" source="https://snypd.rocks/bench"}
::stat{value="392ms" label="git static" source="https://snypd.rocks/bench"}
::

## yaml commit commit

Render cache site twin flow token fast twin draft content render git render lint build commit diagram spec lint. Twin markdown site markdown draft spec source build publish draft primitive static schema review. Commit flow theme evidence primitive yaml cache yaml static chart content chart site primitive site site spec schema. Lint render publish publish twin commit spec markdown flow token. Flow site spec commit diagram draft feed schema git git diagram site twin markdown schema source. Diagram evidence cache commit review build twin chart lint theme agent theme content build.

:::faq
### git markdown build schema?
Publish publish evidence static token source fast lint review static evidence draft primitive fast feed chart cache diagram draft.
### commit diagram render diagram?
Feed render commit evidence render render draft static twin render feed build commit diagram.
:::

::cta{title="commit diagram feed" button="Read the spec" href="/spec"}
