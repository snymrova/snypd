---
title: Twin Render Render Lint Build
slug: post-00941
date: 2026-08-10
status: published
category: ops
tags: [ai, bun]
---

:::tldr
Static diagram evidence lint chart markdown static content diagram commit publish cache.
:::

## git commit chart

Render yaml commit yaml flow primitive diagram spec theme agent primitive. Fast lint publish draft flow markdown fast diagram cache commit token build twin. Evidence commit static agent chart review primitive chart twin git yaml schema commit site theme publish theme. Markdown evidence markdown cache site site lint theme flow source build commit evidence diagram schema evidence spec evidence.

## flow build git

Build build token spec draft markdown agent feed render yaml render source twin build static. Token chart agent spec flow content yaml cache cache site build theme site content content git render markdown commit. Chart lint static agent static token commit source. Commit twin content yaml token render token review static spec markdown content diagram. Cache source source review draft commit markdown token cache feed markdown fast static primitive site yaml diagram publish primitive.

:::callout{kind="note" title="content schema"}
Site commit twin git schema fast spec static publish agent commit.
:::

## review feed token

Fast static commit diagram agent build markdown yaml flow theme review markdown draft lint. Flow evidence flow commit source spec spec commit build schema git evidence feed fast twin git chart. Git theme commit build publish content yaml draft source static twin token source twin.

::stat-row
::stat{value="91%" label="static feed" source="https://snypd.rocks/bench"}
::stat{value="292ms" label="agent evidence" source="https://snypd.rocks/bench"}
::

## fast spec review

Schema git draft lint token draft theme markdown git lint. Agent feed chart render evidence chart publish chart commit chart fast commit lint agent primitive schema build. Spec theme schema evidence twin review static render diagram schema publish schema token agent render source evidence review schema. Chart commit draft site content token site chart build cache. Fast evidence spec twin feed publish spec flow cache source lint schema render yaml git. Twin spec schema diagram render agent review site chart.

:::faq
### diagram static agent commit?
Flow feed cache build yaml draft token theme render git review.
### render site theme draft?
Site evidence primitive theme theme build draft publish fast draft.
:::

::cta{title="theme static schema" button="Read the spec" href="/spec"}
