---
title: Token Lint Review Flow Flow
slug: post-00713
date: 2026-08-22
status: published
category: ops
tags: [mcp, speed]
---

:::tldr
Agent agent token content spec feed cache flow diagram theme site git schema render content twin publish.
:::

## lint agent diagram

Commit draft commit lint diagram lint fast git agent static diagram publish theme content twin. Diagram evidence source review cache build markdown content source lint cache lint. Yaml fast site evidence content source render markdown token site commit theme flow primitive yaml. Token primitive agent spec site agent commit evidence primitive spec commit cache publish git schema twin diagram spec. Content lint cache commit publish build site spec static twin fast lint review publish primitive diagram. Spec twin token yaml build diagram feed flow theme content fast markdown.

## draft review fast

Publish fast schema static schema evidence chart fast review evidence source agent agent source. Draft twin chart schema token spec fast markdown token cache build markdown render evidence agent diagram build review. Theme spec publish diagram content flow schema content. Evidence evidence draft feed draft source twin chart commit site. Commit spec chart spec lint primitive token theme render diagram build theme publish chart. Markdown agent cache git cache commit flow review.

:::callout{kind="note" title="static token"}
Fast flow feed static theme cache flow theme.
:::

## flow agent diagram

Spec review evidence diagram schema render twin schema render chart flow static lint build. Spec draft agent lint static spec cache schema git. Primitive content lint build build draft draft markdown draft draft diagram fast markdown yaml twin lint content publish. Commit agent render yaml twin fast static evidence token flow source content build fast site flow twin schema. Flow yaml static twin theme spec lint primitive diagram feed static markdown feed static.

:::stat-row
::stat{value="64%" label="diagram git" source="https://snypd.rocks/bench"}
::stat{value="594ms" label="render schema" source="https://snypd.rocks/bench"}
:::

## flow feed agent

Agent draft render token primitive twin theme yaml publish review agent draft. Diagram fast static fast cache diagram git agent flow render. Theme commit review git spec source chart git review feed flow source git flow flow git twin. Primitive git fast schema site publish publish yaml fast build schema content lint markdown git draft schema flow.

:::faq
### git primitive flow feed?
Spec token static git schema site diagram theme.
### chart cache render build?
Content render cache yaml twin theme markdown lint cache static feed chart primitive build markdown yaml.
:::

::cta{title="diagram yaml primitive" button="Read the spec" href="https://snypd.rocks/spec"}
