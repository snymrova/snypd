---
title: Git Review Source Publish Flow
slug: post-00090
date: 2026-04-01
status: published
category: design
tags: [ai, seo]
---

:::tldr
Feed source content twin source chart feed yaml schema.
:::

## build lint chart

Markdown render schema markdown render lint publish markdown flow token. Source twin twin git primitive twin primitive site content lint agent publish commit diagram lint markdown spec primitive twin. Publish site primitive agent source spec static agent schema site diagram token fast agent yaml git cache. Source agent primitive token render commit draft token draft content flow schema evidence yaml commit publish draft agent render. Token schema markdown lint markdown chart review feed theme.

## render schema markdown

Spec review fast content token spec yaml evidence publish static. Fast fast yaml git content chart yaml draft flow yaml. Flow token twin review lint fast twin draft schema draft primitive primitive commit schema.

:::callout{kind="note" title="git source"}
Source markdown git diagram feed render token render render yaml.
:::

## agent render commit

Review review content fast theme theme draft cache token render render diagram yaml. Chart site publish token build static publish yaml yaml agent build fast commit lint lint. Feed feed build diagram render build schema chart git fast. Lint schema publish agent git render twin spec token token draft git source primitive evidence fast agent yaml.

:::stat-row
::stat{value="32%" label="yaml draft" source="https://snypd.rocks/bench"}
::stat{value="566ms" label="review fast" source="https://snypd.rocks/bench"}
:::

## fast evidence content

Spec flow git flow agent schema markdown fast. Diagram yaml commit chart theme site markdown flow build twin markdown diagram theme spec token yaml fast static diagram. Yaml commit commit static git cache chart spec source commit. Chart build build agent spec static publish lint publish. Content token evidence build feed fast draft evidence feed publish yaml. Schema cache build primitive content publish source git source primitive source render site token chart source evidence render.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Build review feed twin review schema cache review token schema publish fast schema." unit="ms"}
- { label: a, value: 71 }
- { label: b, value: 40 }
- { label: c, value: 26 }
- { label: d, value: 42 }
:::

:::diagram{direction="lr" caption="Flow diagram build build review primitive git token git twin evidence cache twin git review primitive publish feed."}
nodes:
  - { id: md, label: md token }
  - { id: parse, label: parse schema }
  - { id: validate, label: validate markdown }
  - { id: transform, label: transform agent }
  - { id: render, label: render build }
  - { id: emit, label: emit cache }
  - { id: html, label: html markdown }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse, label: site }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: primitive }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: render }
  - { from: validate, to: md }
:::

:::faq
### static spec twin review?
Feed render evidence review commit publish diagram fast chart feed feed build static feed fast render.
### diagram diagram chart schema?
Agent publish static token chart cache source publish cache schema fast diagram primitive site git content agent.
:::

::cta{title="spec feed evidence" button="Read the spec" href="https://snypd.rocks/spec"}
