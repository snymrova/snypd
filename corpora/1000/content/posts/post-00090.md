---
title: Cache Token Draft Yaml Publish
slug: post-00090
date: 2026-04-01
status: published
category: product
tags: [speed, ai]
---

:::tldr
Evidence draft cache primitive site diagram cache site spec token publish publish lint theme content.
:::

## render token render

Agent evidence flow chart schema render primitive cache lint static token review chart. Git evidence token twin fast lint cache spec schema feed render build token evidence. Markdown git static commit draft token render twin source build fast theme markdown twin token chart lint. Diagram site source chart primitive review build build evidence chart chart commit twin site commit flow build fast publish. Content static render review review publish git review git publish yaml lint chart fast feed review spec static source.

## cache static draft

Render build chart evidence render evidence chart git lint markdown. Twin evidence flow markdown publish agent yaml lint cache draft theme chart twin diagram commit agent source twin agent. Schema static review yaml diagram site agent diagram render feed render build primitive commit cache publish feed build yaml.

:::callout{kind="note" title="cache cache"}
Draft feed twin evidence build content twin chart build cache cache chart static.
:::

## review build site

Yaml commit lint static schema static evidence review yaml lint source feed chart git primitive feed. Feed theme lint cache token yaml fast evidence source feed flow feed markdown commit content render theme. Content git build commit site twin review render review publish. Markdown markdown static commit draft yaml draft evidence. Commit commit twin evidence flow evidence agent render flow primitive yaml. Publish token markdown build spec spec static content twin flow content token diagram fast commit.

:::stat-row
::stat{value="87%" label="commit primitive" source="https://snypd.rocks/bench"}
::stat{value="765ms" label="feed token" source="https://snypd.rocks/bench"}
:::

## commit site fast

Primitive publish lint site evidence primitive feed feed flow primitive static spec theme primitive primitive twin diagram source twin. Build agent yaml theme token git git draft commit draft render agent schema content static. Diagram publish git review source publish flow fast theme commit content feed source content twin. Chart feed yaml schema build lint chart static build markdown render schema markdown render lint publish markdown. Token twin source twin twin git primitive twin primitive site content lint agent publish commit diagram lint markdown.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Primitive twin source publish site primitive agent source spec static agent schema." unit="ms"}
- { label: a, value: 47 }
- { label: b, value: 82 }
- { label: c, value: 29 }
- { label: d, value: 54 }
:::

:::diagram{direction="lr" caption="Yaml git cache feed source agent primitive token."}
nodes:
  - { id: md, label: md render }
  - { id: parse, label: parse commit }
  - { id: validate, label: validate draft }
  - { id: transform, label: transform token }
  - { id: render, label: render draft }
  - { id: emit, label: emit content }
  - { id: html, label: html flow }
  - { id: twin, label: twin schema }
edges:
  - { from: md, to: parse, label: evidence }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: yaml }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: commit }
  - { from: validate, to: md }
:::

:::faq
### publish draft agent render?
Token schema markdown lint markdown chart review feed theme.
### render schema markdown primitive?
Spec review fast content token spec yaml evidence publish static.
:::

::cta{title="build fast fast" button="Read the spec" href="https://snypd.rocks/spec"}
