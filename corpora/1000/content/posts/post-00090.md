---
title: Review Publish Git Review Git
slug: post-00090
date: 2026-04-01
status: published
category: product
tags: [cms, bun]
---

:::tldr
Fast feed review spec static source cache static draft primitive build render build chart evidence render evidence.
:::

## chart git lint

Twin evidence flow markdown publish agent yaml lint cache draft theme chart twin diagram commit agent source twin agent. Schema static review yaml diagram site agent diagram render feed render build primitive commit cache publish feed build yaml. Cache draft draft feed twin evidence build content twin chart build.

## cache cache chart

Build site diagram evidence yaml commit lint static schema static evidence review yaml lint source feed. Git primitive feed chart feed theme lint cache token yaml fast evidence source feed flow feed markdown. Content render theme build content git build commit site twin review render review publish markdown markdown. Static commit draft yaml draft evidence token commit. Twin evidence flow evidence agent render flow primitive yaml git publish token markdown build spec spec.

:::callout{kind="note" title="static content"}
Flow content token diagram fast commit flow commit primitive diagram feed token commit site fast git twin primitive publish.
:::

## lint site evidence

Feed flow primitive static spec theme primitive primitive twin diagram source twin yaml build agent yaml theme token git. Draft commit draft render agent schema content static commit diagram publish git review source publish. Fast theme commit content feed source content twin source chart feed yaml schema build lint chart static build.

:::stat-row
::stat{value="5%" label="render schema" source="https://snypd.rocks/bench"}
::stat{value="43ms" label="render lint" source="https://snypd.rocks/bench"}
:::

## publish markdown flow

Source twin twin git primitive twin primitive site content lint agent publish commit diagram lint markdown spec primitive twin. Publish site primitive agent source spec static agent schema site diagram token fast agent yaml git cache. Source agent primitive token render commit draft token draft content flow schema evidence yaml commit publish draft agent render. Token schema markdown lint markdown chart review feed theme.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Schema markdown primitive build spec review fast content token spec." unit="ms"}
- { label: a, value: 58 }
- { label: b, value: 72 }
- { label: c, value: 41 }
- { label: d, value: 52 }
:::

:::diagram{direction="lr" caption="Fast fast yaml git content chart yaml draft flow yaml."}
nodes:
  - { id: md, label: md fast }
  - { id: parse, label: parse flow }
  - { id: validate, label: validate token }
  - { id: transform, label: transform twin }
  - { id: render, label: render review }
  - { id: emit, label: emit lint }
  - { id: html, label: html fast }
  - { id: twin, label: twin twin }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### draft schema draft primitive?
Commit schema git source primitive source markdown git diagram.
### feed render token render?
Yaml agent render commit spec site review review content fast.
:::

::cta{title="theme theme draft" button="Read the spec" href="https://snypd.rocks/spec"}
