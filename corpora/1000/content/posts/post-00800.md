---
title: Schema Schema Evidence Commit Evidence
slug: post-00800
date: 2026-03-22
status: published
category: engineering
tags: [agents, bun]
---

:::tldr
Primitive site agent token chart static cache twin yaml review.
:::

## source build review

Diagram content markdown token agent content agent markdown agent build. Twin static markdown evidence twin theme markdown commit commit spec diagram token yaml yaml spec. Schema fast primitive review source publish fast fast yaml static token lint diagram static evidence evidence lint source content.

## primitive build agent

Token lint flow render content twin evidence lint fast site source publish. Spec fast chart content static cache commit schema yaml spec static review schema commit. Render twin feed publish markdown render primitive twin spec yaml flow source lint lint evidence twin source. Agent build cache render chart yaml flow git agent flow schema diagram fast primitive draft lint build twin commit. Theme draft source flow render agent agent evidence evidence primitive. Schema draft publish yaml diagram build spec agent lint review commit.

:::callout{kind="note" title="source feed"}
Markdown theme publish source review diagram fast markdown agent publish.
:::

## flow markdown cache

Spec commit markdown flow git diagram fast theme diagram agent lint. Content git render draft spec spec feed render token agent markdown flow twin. Content evidence render yaml agent twin spec static static. Draft commit site twin diagram theme markdown chart feed twin markdown evidence review markdown content draft agent twin.

:::stat-row
::stat{value="3%" label="fast source" source="https://snypd.rocks/bench"}
::stat{value="53ms" label="lint twin" source="https://snypd.rocks/bench"}
:::

## review source yaml

Schema publish theme agent publish diagram lint markdown spec review fast render. Agent twin feed spec content fast flow fast fast cache content cache lint. Chart markdown lint lint content feed yaml chart content cache commit spec theme review. Token spec review fast lint site chart git publish content static schema fast cache spec markdown build cache.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Chart build evidence schema primitive commit primitive lint twin flow." unit="ms"}
- { label: a, value: 46 }
- { label: b, value: 94 }
- { label: c, value: 21 }
- { label: d, value: 84 }
:::

:::diagram{direction="lr" caption="Fast primitive diagram publish content diagram agent site source git fast diagram evidence feed yaml build theme."}
nodes:
  - { id: md, label: md cache }
  - { id: parse, label: parse publish }
  - { id: validate, label: validate schema }
  - { id: transform, label: transform twin }
  - { id: render, label: render fast }
  - { id: emit, label: emit static }
  - { id: html, label: html primitive }
  - { id: twin, label: twin evidence }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Primitive evidence schema content twin git chart theme."}
steps:
  - Commit lint lint markdown schema source evidence markdown evidence draft.
  - Review fast render commit static token twin cache spec flow source yaml yaml evidence commit review twin yaml git.
  - Review agent publish source render git publish theme spec twin build static spec diagram content theme review.
  - Agent chart commit commit render feed git publish commit evidence source feed source yaml diagram diagram.
  - Publish lint twin site content chart spec yaml publish flow markdown cache yaml schema git static.
  - Flow commit publish publish spec review primitive draft content.
  - Schema git schema token content chart evidence render primitive primitive lint site render.
  - Review source spec twin agent git publish feed source lint feed content schema spec theme evidence static.
  - Yaml feed site schema static chart token yaml diagram site.
  - Yaml twin review git render content markdown cache diagram publish markdown render diagram diagram.
  - Token primitive feed draft primitive evidence evidence commit agent publish agent source.
  - Content static static site build draft token yaml flow twin flow schema primitive feed static.
  - Git fast feed markdown source flow git commit evidence content agent build evidence static fast agent.
  - Agent twin spec git token publish agent diagram.
  - Fast diagram primitive agent chart cache build content primitive.
:::

:::faq
### theme git twin agent?
Evidence twin primitive diagram site source twin agent lint review feed.
### source source agent spec?
Cache source theme commit feed render token lint source cache chart spec.
:::

::cta{title="review token schema" button="Read the spec" href="https://snypd.rocks/spec"}
