---
title: Cache Lint Static Site Render
slug: post-00800
date: 2026-03-22
status: published
category: ops
tags: [bun, speed]
---

:::tldr
Lint render schema commit lint site render commit spec schema feed draft primitive static fast primitive build flow.
:::

## yaml evidence feed

Build feed build twin git primitive markdown content source. Commit spec agent publish static feed chart markdown build schema review twin yaml token lint token schema build. Spec theme diagram feed schema agent git flow content draft lint twin.

## twin cache render

Publish git schema primitive theme theme markdown yaml diagram diagram feed spec draft primitive diagram site. Schema cache lint primitive draft twin review theme schema draft token git. Chart git review primitive twin lint content agent. Flow render fast schema content draft site theme source chart schema twin spec theme. Source evidence render review draft twin yaml draft primitive primitive yaml yaml commit schema theme site.

:::callout{kind="note" title="yaml lint"}
Review git cache fast static theme source markdown git.
:::

## primitive render evidence

Publish review lint yaml site git chart diagram fast fast agent chart cache token schema theme feed spec flow. Source primitive git lint theme flow evidence flow lint markdown site schema draft git diagram. Static content review primitive render git review token token static draft build fast twin agent token site yaml review. Draft schema spec source git lint publish markdown fast git git commit agent chart diagram evidence schema.

:::stat-row
::stat{value="32%" label="fast twin" source="https://snypd.rocks/bench"}
::stat{value="52ms" label="diagram render" source="https://snypd.rocks/bench"}
:::

## chart diagram source

Fast diagram git chart token schema build site content publish fast yaml lint. Build git draft static diagram primitive static theme feed agent theme fast theme yaml. Render agent git primitive cache token theme diagram site twin content agent review site diagram. Spec lint draft render draft git primitive agent schema content publish review yaml yaml yaml commit.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Chart twin markdown git markdown publish schema source schema schema spec yaml yaml evidence yaml build schema." unit="ms"}
- { label: a, value: 4 }
- { label: b, value: 97 }
- { label: c, value: 86 }
- { label: d, value: 26 }
:::

:::diagram{direction="lr" caption="Spec git primitive feed review diagram static spec lint source flow fast yaml token evidence."}
nodes:
  - { id: md, label: md render }
  - { id: parse, label: parse twin }
  - { id: validate, label: validate lint }
  - { id: transform, label: transform cache }
  - { id: render, label: render diagram }
  - { id: emit, label: emit diagram }
  - { id: html, label: html build }
  - { id: twin, label: twin feed }
edges:
  - { from: md, to: parse, label: theme }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: source }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: schema }
  - { from: validate, to: md }
:::

:::flow{caption="Diagram token review evidence site cache review static feed diagram."}
steps:
  - Lint feed build render render commit lint diagram feed fast feed agent chart flow review.
  - Token commit theme theme source agent static primitive markdown build twin.
  - Content evidence chart draft cache flow review token spec feed review flow site render source schema publish fast static.
  - Commit lint cache spec review markdown chart diagram.
  - Site review diagram git git yaml yaml primitive token chart render fast commit flow commit.
  - Publish commit review render markdown feed cache spec spec cache spec markdown token diagram chart yaml spec.
  - Feed site token source publish source markdown site source git lint markdown source markdown agent chart draft publish.
  - Static draft site chart static twin publish cache git evidence render review draft review theme content site.
  - Git schema review theme twin cache markdown render git primitive review draft diagram agent markdown theme render theme feed.
  - Spec cache cache diagram yaml agent commit evidence lint commit.
  - Lint yaml markdown spec build lint commit markdown.
  - Feed spec schema cache agent site draft agent publish static feed source chart site primitive git primitive.
  - Build git theme lint cache agent primitive twin feed flow source feed.
  - Schema feed build commit yaml commit yaml primitive twin cache content twin schema diagram content review theme agent yaml.
  - Site cache primitive review flow git chart review theme review fast diagram static content feed chart.
:::

:::faq
### agent feed chart primitive?
Agent content markdown lint chart yaml yaml review feed twin twin review markdown static build yaml flow.
### draft diagram git twin?
Twin chart yaml commit commit twin commit render render review diagram commit draft build twin commit publish.
:::

::cta{title="theme commit commit" button="Read the spec" href="https://snypd.rocks/spec"}
