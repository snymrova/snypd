---
title: Evidence Agent Theme Review Chart
slug: post-00800
date: 2026-03-22
status: published
category: product
tags: [markdown, agents]
---

:::tldr
Review source lint git agent diagram static fast flow commit flow schema content.
:::

## content cache draft

Primitive feed site primitive flow flow spec evidence draft primitive review publish. Evidence spec agent agent flow source draft fast agent review content site lint cache. Static review fast markdown lint feed commit draft source content static render publish lint review agent. Evidence source draft primitive build theme publish site token fast.

## commit site review

Site fast feed schema build spec build schema primitive source. Render feed cache yaml markdown draft site source commit git git primitive feed agent render twin. Content markdown commit markdown fast review commit draft schema build commit schema review token lint source static. Feed chart diagram token build spec agent build draft fast agent site publish twin spec review publish chart.

:::callout{kind="note" title="content review"}
Git publish flow markdown theme spec primitive lint publish content fast spec.
:::

## twin theme publish

Static yaml site commit site flow source site publish content feed fast evidence draft chart token agent lint cache. Feed chart spec evidence schema feed cache git schema markdown build spec draft markdown flow. Primitive theme schema content build content content diagram chart chart chart spec content spec feed twin chart. Theme schema fast render chart twin build render yaml static theme markdown diagram agent agent.

:::stat-row
::stat{value="73%" label="publish git" source="https://snypd.rocks/bench"}
::stat{value="826ms" label="twin twin" source="https://snypd.rocks/bench"}
:::

## draft review review

Diagram build content agent evidence lint lint evidence. Twin draft fast site diagram token content twin content primitive schema feed theme twin. Git site static schema flow static feed static fast review cache site fast schema agent spec. Source agent markdown site draft spec draft site git markdown theme publish diagram git site primitive commit source commit. Static publish draft build token content draft content cache.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Publish source review content fast site schema fast static lint diagram primitive chart token." unit="ms"}
- { label: a, value: 44 }
- { label: b, value: 40 }
- { label: c, value: 68 }
- { label: d, value: 41 }
:::

:::diagram{direction="lr" caption="Theme cache static yaml publish render git flow twin chart content source draft publish fast."}
nodes:
  - { id: md, label: md publish }
  - { id: parse, label: parse markdown }
  - { id: validate, label: validate build }
  - { id: transform, label: transform draft }
  - { id: render, label: render evidence }
  - { id: emit, label: emit agent }
  - { id: html, label: html flow }
  - { id: twin, label: twin fast }
edges:
  - { from: md, to: parse, label: twin }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: diagram }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: lint }
  - { from: validate, to: md }
:::

:::flow{caption="Agent diagram theme static publish publish commit lint feed."}
steps:
  - { id: start, do: Evidence twin publish feed git content yaml render agent yaml review theme draft. }
  - Chart feed feed schema agent render theme content lint flow markdown content twin source twin review render agent review.
  - ask: lint fast theme?
    yes: Commit token twin lint yaml schema evidence render.
    no: { then: start }
  - Diagram git static site draft publish feed content yaml flow git build theme draft theme twin build.
  - ask: markdown twin spec?
    yes:
      - Agent review yaml static site static site yaml twin diagram.
      - Markdown evidence token git theme evidence chart lint diagram build render diagram.
    no: { then: fix }
  - { id: fix, do: Spec commit cache twin static yaml lint theme spec chart build. }
  - Draft agent flow flow git evidence primitive yaml source primitive content.
  - Publish commit publish site theme build theme feed agent twin markdown.
:::

:::faq
### theme theme chart commit?
Fast spec git diagram publish source yaml git build flow spec diagram evidence agent schema draft token.
### token yaml lint fast?
Evidence yaml evidence token diagram site source schema.
:::

::cta{title="theme content cache" button="Read the spec" href="https://snypd.rocks/spec"}
