---
title: Content Publish Schema Chart Spec
slug: post-00294
date: 2026-02-24
status: published
category: design
tags: [cms, bun]
---

:::tldr
Markdown yaml build diagram spec chart lint source git schema commit agent chart git review.
:::

## content fast lint

Build render chart commit build site review schema diagram draft primitive. Site git spec chart twin twin diagram content yaml evidence theme flow git yaml chart render. Static agent yaml theme token token schema flow review feed markdown build chart primitive. Schema spec content diagram fast evidence publish primitive build static draft theme token chart.

## source chart commit

Token cache spec theme token review site fast token schema source flow. Flow publish build render content source draft schema site cache static schema review. Publish primitive feed fast review evidence evidence build feed twin twin fast fast primitive theme publish.

:::callout{kind="note" title="cache site"}
Site feed feed fast token evidence commit build schema theme token cache chart.
:::

## token markdown chart

Git schema feed theme draft primitive agent token lint static lint content. Lint spec site flow git flow static git source chart git cache review spec. Build cache git spec cache diagram lint draft twin build token draft. Publish git cache review publish site chart publish build spec primitive draft site theme site spec primitive token flow.

:::stat-row
::stat{value="20%" label="source render" source="https://snypd.rocks/bench"}
::stat{value="346ms" label="markdown review" source="https://snypd.rocks/bench"}
:::

## source git feed

Lint spec token git schema publish review primitive spec content primitive cache draft agent chart build spec. Primitive lint static flow cache spec review yaml token. Lint source diagram fast chart theme flow cache source cache review. Schema publish commit git spec spec spec cache cache review lint commit render content yaml diagram yaml. Commit content primitive fast draft cache publish primitive commit review token twin. Schema render lint build commit lint draft agent.

:::faq
### agent source diagram draft?
Commit source source draft feed token commit token.
### flow draft chart static?
Git publish agent spec render publish git render feed agent lint cache flow.
:::

::cta{title="spec agent agent" button="Read the spec" href="https://snypd.rocks/spec"}
