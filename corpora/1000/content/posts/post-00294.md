---
title: Feed Feed Fast Token Evidence
slug: post-00294
date: 2026-02-24
status: published
category: design
tags: [agents, markdown]
---

:::tldr
Token cache chart token markdown chart cache spec git.
:::

## schema feed theme

Agent token lint static lint content static lint spec site. Git flow static git source chart git cache review spec spec build cache git spec cache diagram lint. Twin build token draft twin publish git cache review publish site chart publish. Spec primitive draft site theme site spec primitive token flow.

## render source render

Review source git feed diagram chart lint spec. Git schema publish review primitive spec content primitive cache draft agent. Build spec primitive primitive lint static flow cache spec review yaml token token lint source diagram fast. Theme flow cache source cache review chart schema publish commit git spec spec spec cache cache review.

:::callout{kind="note" title="lint commit"}
Content yaml diagram yaml publish commit content primitive fast draft.
:::

## cache publish primitive

Token twin markdown schema render lint build commit lint draft agent agent source diagram draft content. Source source draft feed token commit token flow draft chart static site git publish agent spec. Publish git render feed agent lint cache flow spec agent. Flow lint draft theme theme draft schema chart. Agent commit theme static token spec token fast agent primitive render fast content.

:::stat-row
::stat{value="10%" label="twin diagram" source="https://snypd.rocks/bench"}
::stat{value="135ms" label="review primitive" source="https://snypd.rocks/bench"}
:::

## diagram schema chart

Diagram publish twin commit source content fast content lint evidence feed. Lint feed chart draft markdown review token feed diagram twin token. Primitive git schema git static twin primitive diagram flow publish site token review review. Content publish agent review source diagram render token schema lint theme content yaml draft site source schema. Chart site publish build build content theme spec build content chart commit evidence. Diagram yaml spec feed diagram token flow diagram git schema yaml source schema token feed build publish primitive.

:::faq
### static markdown evidence theme?
Token twin source agent site feed source render.
### primitive publish static source?
Static content git diagram theme theme chart twin markdown yaml git draft static render lint static.
:::

::cta{title="fast cache chart" button="Read the spec" href="https://snypd.rocks/spec"}
