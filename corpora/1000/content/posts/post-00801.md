---
title: Twin Fast Build Agent Source
slug: post-00801
date: 2026-03-23
status: published
category: engineering
tags: [cms, seo]
---

:::tldr
Fast evidence git source publish publish cache chart source agent lint.
:::

## git schema diagram

Content schema theme feed theme draft markdown markdown fast publish spec publish lint. Diagram git build lint markdown diagram lint theme markdown flow cache content token site. Diagram site flow commit diagram twin site fast git. Fast spec markdown commit markdown chart markdown build evidence flow. Content diagram git git schema fast primitive primitive feed render primitive agent. Token lint schema source lint twin evidence publish markdown theme build.

## source diagram feed

Build twin lint agent render git chart markdown content publish theme fast commit lint diagram static draft commit source. Chart cache primitive commit source yaml static review lint static chart. Source flow chart build flow build theme source cache primitive content build theme site spec. Publish theme schema primitive draft lint site feed. Feed content token lint schema source twin yaml draft diagram publish cache feed publish build agent chart twin diagram. Build cache site site static review review lint build theme twin diagram flow primitive source feed twin token build.

:::callout{kind="note" title="review lint"}
Agent lint render twin theme feed fast render static diagram feed site.
:::

## lint lint cache

Commit diagram static twin primitive fast diagram chart flow spec cache git flow draft static yaml token. Lint static static agent yaml agent flow evidence lint lint schema. Git commit publish draft fast render fast primitive publish evidence source feed chart lint token commit. Evidence git theme source diagram review git schema cache content site twin feed. Markdown theme schema git flow source render source draft chart source flow twin twin publish content static cache.

:::stat-row
::stat{value="20%" label="source publish" source="https://snypd.rocks/bench"}
::stat{value="103ms" label="draft source" source="https://snypd.rocks/bench"}
:::

## primitive commit schema

Diagram git content token lint commit evidence fast flow build source. Source commit flow publish lint chart yaml commit primitive lint draft diagram source agent chart lint site evidence static. Content evidence static build markdown site spec chart twin primitive source commit diagram source flow draft schema.

:::faq
### render lint git render?
Chart build lint fast diagram primitive commit git fast theme render review.
### static twin draft content?
Token content agent twin review spec static primitive twin evidence content twin yaml build markdown fast.
:::

::cta{title="publish twin site" button="Read the spec" href="https://snypd.rocks/spec"}
