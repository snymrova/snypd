---
title: Evidence Publish Markdown Content Content
slug: post-00490
date: 2026-01-11
status: published
category: product
tags: [ai, markdown]
---

:::tldr
Yaml agent diagram diagram site source lint lint twin site commit static commit render diagram token git.
:::

## content markdown feed

Agent schema cache cache draft markdown fast site agent publish publish feed flow publish static source feed agent. Cache markdown source agent evidence source source review render yaml agent fast diagram render evidence git draft publish. Evidence render render fast review yaml theme markdown agent draft primitive. Flow feed diagram static diagram twin feed evidence yaml flow cache schema twin commit content render. Schema git yaml site theme publish site yaml evidence static commit spec source. Schema content twin token content site draft feed.

## chart evidence publish

Build markdown content site git theme chart static commit yaml theme feed theme source markdown build. Cache content diagram markdown draft publish review feed content feed fast token static evidence primitive source content render. Commit twin review markdown token content render chart cache diagram content build chart diagram build git. Diagram token render publish twin source lint agent chart cache evidence spec git.

:::callout{kind="note" title="flow flow"}
Token source review feed commit spec draft twin static markdown fast site.
:::

## evidence build twin

Publish draft yaml theme content feed yaml draft static cache build chart content. Evidence token git yaml feed site feed token agent evidence token cache spec draft yaml. Evidence evidence markdown chart yaml primitive feed theme primitive render review. Build draft yaml diagram flow schema render spec chart render markdown spec markdown spec agent theme feed chart.

:::stat-row
::stat{value="37%" label="content evidence" source="https://snypd.rocks/bench"}
::stat{value="309ms" label="flow commit" source="https://snypd.rocks/bench"}
:::

## twin schema agent

Static review publish diagram site git primitive fast spec agent publish markdown evidence lint publish. Agent commit evidence yaml twin render theme git lint spec build yaml agent site evidence evidence. Cache flow token review review token yaml cache content diagram primitive theme build. Primitive static evidence site site content lint cache cache content agent cache review render source chart source cache. Fast source cache source chart static flow evidence publish.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Agent flow primitive site render yaml render render publish twin markdown commit." unit="ms"}
- { label: a, value: 59 }
- { label: b, value: 91 }
- { label: c, value: 6 }
- { label: d, value: 58 }
:::

:::diagram{direction="lr" caption="Site git chart draft draft chart content review static commit spec evidence twin static static git."}
nodes:
  - { id: md, label: md twin }
  - { id: parse, label: parse primitive }
  - { id: validate, label: validate publish }
  - { id: transform, label: transform build }
  - { id: render, label: render schema }
  - { id: emit, label: emit diagram }
  - { id: html, label: html site }
  - { id: twin, label: twin schema }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### diagram static theme token?
Content feed feed primitive markdown publish primitive chart render token markdown site twin render publish flow commit review.
### evidence evidence fast flow?
Site spec static twin cache draft fast publish source diagram content lint source.
:::

::cta{title="publish schema diagram" button="Read the spec" href="https://snypd.rocks/spec"}
