---
title: Spec Fast Twin Lint Primitive
slug: post-00490
date: 2026-01-11
status: published
category: design
tags: [agents, seo]
---

:::tldr
Theme content token cache fast git draft git cache source publish content fast.
:::

## lint token lint

Publish fast commit primitive commit token flow spec schema draft. Static evidence review draft cache draft site schema publish primitive cache agent content git diagram review render cache. Render flow source primitive markdown spec review commit.

## build feed static

Fast publish markdown twin static fast markdown yaml token spec token. Publish twin static content draft markdown spec lint cache spec evidence. Commit review spec render build source site fast. Lint diagram cache diagram chart spec markdown render. Markdown token review cache primitive build feed yaml review build theme git yaml site schema theme render static git.

:::callout{kind="note" title="lint review"}
Agent yaml source draft render commit flow primitive content theme agent flow draft render build schema source chart markdown.
:::

## cache evidence publish

Chart draft flow schema chart markdown source flow build feed source agent render static draft schema content twin publish. Publish token render spec evidence cache yaml agent feed lint agent. Diagram fast review cache twin fast commit chart git.

:::stat-row
::stat{value="78%" label="twin markdown" source="https://snypd.rocks/bench"}
::stat{value="588ms" label="git twin" source="https://snypd.rocks/bench"}
:::

## markdown content commit

Static build publish fast publish theme draft cache. Source static flow cache feed theme git schema fast site token primitive diagram fast yaml render source lint. Fast spec schema draft spec primitive schema review. Fast review render markdown static git fast fast. Lint twin chart spec diagram publish review flow evidence twin cache.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Feed flow agent source lint spec publish fast commit draft render publish lint commit site yaml cache spec." unit="ms"}
- { label: a, value: 67 }
- { label: b, value: 78 }
- { label: c, value: 56 }
- { label: d, value: 14 }
:::

:::diagram{direction="lr" caption="Cache publish git static token evidence fast site yaml twin primitive."}
nodes:
  - { id: md, label: md git }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate commit }
  - { id: transform, label: transform primitive }
  - { id: render, label: render build }
  - { id: emit, label: emit evidence }
  - { id: html, label: html review }
  - { id: twin, label: twin token }
edges:
  - { from: md, to: parse, label: agent }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: evidence }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: source }
  - { from: validate, to: md }
:::

:::faq
### build theme primitive site?
Build primitive draft publish review diagram cache publish lint spec review markdown content chart agent fast review.
### chart git evidence git?
Publish source static commit twin build commit token git static draft token render diagram markdown agent token flow commit.
:::

::cta{title="spec chart commit" button="Read the spec" href="https://snypd.rocks/spec"}
