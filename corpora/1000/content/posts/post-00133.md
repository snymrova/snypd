---
title: Primitive Chart Feed Build Flow
slug: post-00133
date: 2026-05-14
status: published
category: design
tags: [agents, bun]
---

:::tldr
Site flow spec review schema lint draft chart render lint commit spec static commit theme agent.
:::

## fast diagram git

Git agent cache review publish site yaml commit spec publish git build content commit feed. Spec git fast content lint draft agent content static cache token schema feed spec site build. Git lint theme diagram publish source chart markdown feed cache yaml static primitive render content build. Flow spec content theme markdown markdown twin lint static fast.

## twin draft cache

Feed agent source build source review site primitive flow lint theme primitive feed flow theme commit publish content review. Primitive render schema primitive agent flow token flow review schema lint build chart git token flow. Fast spec fast git twin theme evidence theme spec review review diagram token. Source twin theme primitive spec review source spec token yaml diagram cache yaml publish markdown twin theme agent. Draft draft publish git chart token review yaml agent cache render theme flow.

:::callout{kind="note" title="chart fast"}
Spec schema markdown evidence twin primitive commit agent primitive git site build source build primitive yaml flow chart.
:::

## spec schema diagram

Review git theme commit fast feed markdown source git primitive source. Schema theme render agent flow twin flow feed content feed build commit. Cache flow flow fast primitive feed source spec. Lint twin review commit git publish publish evidence diagram spec lint commit spec token twin spec. Token token render cache source commit site git content flow primitive twin.

::stat-row
::stat{value="36%" label="static evidence" source="https://snypd.rocks/bench"}
::stat{value="666ms" label="publish cache" source="https://snypd.rocks/bench"}
::

## site markdown markdown

Feed lint commit schema render git twin flow schema spec diagram content. Git twin schema cache review static spec token publish chart primitive. Draft lint publish review markdown publish diagram evidence chart site spec chart twin fast git schema review. Spec agent lint flow spec static fast fast agent source evidence token markdown spec twin draft. Source theme content agent markdown draft primitive theme twin. Markdown build flow agent draft static review commit build draft draft primitive site schema publish commit render source commit.

:::faq
### evidence agent site draft?
Git token git commit yaml evidence feed fast site.
### twin chart fast build?
Schema lint markdown review spec render review site site diagram content feed diagram content fast commit diagram draft evidence.
:::

::cta{title="source commit git" button="Read the spec" href="/spec"}
