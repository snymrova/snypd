---
title: Site Markdown Fast Primitive Twin
slug: post-00410
date: 2026-06-20
status: published
category: engineering
tags: [agents, mcp]
---

:::tldr
Schema markdown draft review build chart feed content twin yaml.
:::

## chart spec cache

Fast flow build cache draft source lint yaml evidence render yaml publish primitive. Source content token lint cache review review static build diagram source fast. Cache agent publish source spec static schema evidence fast lint source feed commit evidence publish git lint. Markdown render evidence spec git markdown fast evidence yaml schema source. Agent feed fast content fast diagram chart evidence git agent build yaml theme lint build commit primitive primitive. Cache git source agent commit chart publish theme twin cache chart schema.

## commit evidence draft

Twin spec commit evidence fast git feed chart cache. Fast theme review chart agent draft cache fast static feed draft git. Source publish twin schema static source site fast feed review static markdown evidence render yaml spec theme primitive.

:::callout{kind="note" title="feed theme"}
Publish site site build flow yaml fast fast render draft lint.
:::

## yaml lint static

Schema build diagram static render diagram agent source schema primitive commit. Schema token theme diagram render cache build theme commit spec publish draft theme token site chart diagram build. Cache site static diagram review review build yaml publish cache primitive content evidence source publish git git. Token evidence lint theme evidence build publish spec chart fast yaml commit agent source fast markdown draft content. Source schema git render site theme twin yaml render diagram spec agent. Content markdown chart flow static publish build fast render markdown feed review spec yaml.

:::stat-row
::stat{value="32%" label="spec spec" source="https://snypd.rocks/bench"}
::stat{value="778ms" label="agent publish" source="https://snypd.rocks/bench"}
:::

## lint token twin

Build git fast evidence git agent render chart yaml source markdown. Primitive fast diagram chart cache draft agent yaml cache render flow review site chart build feed draft diagram. Yaml publish twin cache spec feed git content schema chart token draft git schema lint content lint. Render render markdown fast schema cache twin build diagram flow fast twin git flow. Draft token render chart lint schema site flow git schema token. Flow markdown content chart draft commit evidence commit theme primitive feed primitive token content.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Content publish markdown content markdown primitive token fast feed review." unit="ms"}
- { label: a, value: 79 }
- { label: b, value: 31 }
- { label: c, value: 50 }
- { label: d, value: 93 }
:::

:::diagram{direction="lr" caption="Cache commit publish fast render chart spec spec agent markdown markdown cache schema diagram evidence commit."}
nodes:
  - { id: md, label: md render }
  - { id: parse, label: parse token }
  - { id: validate, label: validate git }
  - { id: transform, label: transform source }
  - { id: render, label: render markdown }
  - { id: emit, label: emit draft }
  - { id: html, label: html feed }
  - { id: twin, label: twin feed }
edges:
  - { from: md, to: parse, label: flow }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: git }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: git }
  - { from: validate, to: md }
:::

:::faq
### theme render commit commit?
Commit token schema static cache render agent git publish build flow spec fast flow chart lint content lint chart.
### content yaml agent feed?
Site content commit site theme source markdown fast publish theme token evidence theme commit lint.
:::

::cta{title="cache chart evidence" button="Read the spec" href="https://snypd.rocks/spec"}
