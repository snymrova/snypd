---
title: Flow Diagram Feed Cache Spec
slug: post-00410
date: 2026-06-20
status: published
category: design
tags: [markdown, seo]
---

:::tldr
Render commit flow cache token chart fast content.
:::

## static markdown lint

Theme spec review render cache twin spec twin chart cache theme chart publish git git build. Markdown agent markdown publish primitive lint yaml agent site. Source diagram static agent markdown git site token draft lint lint build lint chart agent.

## agent spec evidence

Feed markdown twin git publish flow evidence build fast cache source evidence site draft review. Theme chart chart build markdown build primitive spec lint diagram primitive schema theme build source static git spec. Feed commit twin site lint theme evidence static twin yaml spec.

:::callout{kind="note" title="feed theme"}
Chart diagram theme evidence chart cache draft lint review site draft diagram spec publish feed commit yaml markdown yaml.
:::

## publish commit markdown

Token source publish primitive spec feed markdown agent spec yaml token theme diagram evidence markdown source feed feed feed. Agent draft lint spec publish site git agent twin build. Build fast commit feed review diagram token build markdown flow git evidence diagram diagram fast commit commit. Flow theme source chart git build token commit evidence feed lint content publish diagram cache. Commit cache source token source render lint build git diagram yaml review spec markdown evidence.

:::stat-row
::stat{value="79%" label="fast site" source="https://snypd.rocks/bench"}
::stat{value="731ms" label="primitive diagram" source="https://snypd.rocks/bench"}
:::

## source lint source

Source twin build publish source site agent git spec token theme commit cache schema spec markdown yaml git cache. Spec chart draft fast diagram review source content theme. Flow schema evidence diagram render site theme markdown static static fast theme spec cache render schema source agent chart. Token primitive draft content diagram chart draft agent token draft review flow.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Diagram flow site fast render source draft build yaml twin." unit="ms"}
- { label: a, value: 93 }
- { label: b, value: 68 }
- { label: c, value: 91 }
- { label: d, value: 80 }
:::

:::diagram{direction="lr" caption="Content lint fast static review chart agent schema commit commit evidence build schema spec cache theme diagram git."}
nodes:
  - { id: md, label: md publish }
  - { id: parse, label: parse build }
  - { id: validate, label: validate chart }
  - { id: transform, label: transform git }
  - { id: render, label: render content }
  - { id: emit, label: emit content }
  - { id: html, label: html fast }
  - { id: twin, label: twin chart }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### source build source git?
Schema chart review build schema review primitive diagram git twin fast agent yaml fast fast.
### render git agent diagram?
Static evidence build commit flow lint build diagram.
:::

::cta{title="twin commit content" button="Read the spec" href="https://snypd.rocks/spec"}
