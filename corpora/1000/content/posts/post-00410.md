---
title: Review Spec Flow Draft Flow
slug: post-00410
date: 2026-06-20
status: published
category: design
tags: [cms, mcp]
---

:::tldr
Git site chart theme lint site lint content fast schema draft token diagram evidence site render site.
:::

## render theme theme

Flow evidence twin twin token yaml fast publish. Markdown markdown primitive static yaml twin flow render site diagram spec diagram source markdown site spec lint schema evidence. Flow twin cache yaml spec schema render twin draft feed theme twin content commit diagram feed evidence schema render.

## primitive commit yaml

Evidence markdown evidence yaml yaml primitive evidence build build render render twin cache diagram diagram build. Feed build agent content content token token schema feed feed feed build cache theme review. Site static publish content feed git content render build theme. Fast schema content primitive evidence diagram spec flow yaml. Publish draft git markdown cache evidence fast agent site twin twin build content git token render twin.

:::callout{kind="note" title="diagram content"}
Flow feed static chart commit publish chart diagram commit evidence git render token schema review flow source markdown review.
:::

## lint twin evidence

Publish feed flow token source feed theme markdown markdown review evidence. Git publish fast diagram primitive feed content publish build render git evidence static evidence content. Commit primitive schema source git site build git spec primitive. Yaml schema markdown render git theme markdown source lint draft fast lint token fast site spec.

:::stat-row
::stat{value="78%" label="feed publish" source="https://snypd.rocks/bench"}
::stat{value="180ms" label="token git" source="https://snypd.rocks/bench"}
:::

## cache source fast

Review flow agent diagram review draft git diagram. Fast feed theme cache yaml cache diagram evidence git draft chart theme. Chart twin flow chart content spec site lint evidence. Primitive lint source schema source flow diagram build static. Site flow feed source yaml static twin markdown review theme content draft site content.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Diagram evidence markdown static publish spec build evidence publish schema." unit="ms"}
- { label: a, value: 4 }
- { label: b, value: 83 }
- { label: c, value: 32 }
- { label: d, value: 97 }
:::

:::diagram{direction="lr" caption="Lint render feed yaml lint fast evidence render."}
nodes:
  - { id: md, label: md lint }
  - { id: parse, label: parse schema }
  - { id: validate, label: validate lint }
  - { id: transform, label: transform spec }
  - { id: render, label: render diagram }
  - { id: emit, label: emit static }
  - { id: html, label: html source }
  - { id: twin, label: twin twin }
edges:
  - { from: md, to: parse, label: spec }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: token }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: twin }
  - { from: validate, to: md }
:::

:::faq
### render chart cache commit?
Chart yaml cache yaml schema build feed source lint diagram flow agent publish feed git flow chart.
### git flow site commit?
Source review token static evidence build fast commit schema draft lint token git.
:::

::cta{title="site feed review" button="Read the spec" href="https://snypd.rocks/spec"}
