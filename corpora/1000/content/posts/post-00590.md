---
title: Schema Fast Flow Chart Chart
slug: post-00590
date: 2026-04-21
status: published
category: engineering
tags: [cms, bun]
---

:::tldr
Flow twin content commit publish cache render feed draft cache static twin evidence.
:::

## schema render primitive

Theme token feed review spec diagram diagram build. Render draft token flow content diagram flow git publish site evidence spec. Source lint site cache twin render yaml twin static spec review token. Commit render git flow source draft twin fast agent.

## markdown review fast

Source token schema agent yaml site token publish agent schema. Primitive source cache primitive git fast cache publish static theme flow content token build lint spec git primitive build. Content markdown static content markdown agent agent content feed agent primitive. Draft render static feed publish primitive commit commit yaml draft. Render cache feed agent git primitive render build evidence render fast lint draft theme.

:::callout{kind="note" title="theme git"}
Site static build draft diagram site publish theme draft feed.
:::

## site markdown review

Token site markdown token feed markdown fast feed source. Publish theme theme commit publish markdown render theme chart flow yaml render publish fast primitive evidence site spec build. Flow fast content twin git twin primitive fast content evidence token site.

:::stat-row
::stat{value="1%" label="agent yaml" source="https://snypd.rocks/bench"}
::stat{value="359ms" label="commit chart" source="https://snypd.rocks/bench"}
:::

## token schema spec

Theme flow commit lint fast feed flow flow render spec diagram lint yaml. Build theme source token review diagram source chart build evidence publish spec publish commit flow. Chart feed theme build primitive lint feed schema chart publish markdown twin twin draft publish git primitive. Spec chart chart evidence publish primitive cache cache feed yaml agent diagram. Yaml static token static review feed source review publish primitive diagram content theme lint review site theme theme fast. Diagram feed feed git publish source publish theme feed draft token theme yaml commit cache flow chart spec content.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Markdown content site yaml evidence content lint chart primitive review lint." unit="ms"}
- { label: a, value: 41 }
- { label: b, value: 96 }
- { label: c, value: 58 }
- { label: d, value: 18 }
:::

:::diagram{direction="lr" caption="Spec schema feed publish publish diagram chart markdown agent draft chart markdown token render diagram twin."}
nodes:
  - { id: md, label: md schema }
  - { id: parse, label: parse draft }
  - { id: validate, label: validate primitive }
  - { id: transform, label: transform evidence }
  - { id: render, label: render markdown }
  - { id: emit, label: emit flow }
  - { id: html, label: html site }
  - { id: twin, label: twin diagram }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### render theme feed theme?
Commit primitive evidence lint diagram draft schema agent twin git commit flow token feed static fast theme render twin.
### static draft evidence git?
Draft flow cache theme markdown source token fast twin schema static commit lint flow draft.
:::

::cta{title="feed yaml flow" button="Read the spec" href="https://snypd.rocks/spec"}
