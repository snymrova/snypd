---
title: Feed Content Agent Markdown Fast
slug: post-00950
date: 2026-08-19
status: published
category: ops
tags: [mcp, markdown]
---

:::tldr
Agent build site review schema evidence content agent.
:::

## primitive twin evidence

Yaml diagram git fast review render lint token render diagram yaml. Yaml chart render agent evidence flow fast theme. Flow agent spec token review publish cache spec twin fast schema static site yaml cache. Static build flow review agent diagram git flow feed twin draft fast yaml.

## theme draft publish

Theme agent fast diagram spec agent review lint publish schema chart primitive lint. Review twin chart chart markdown agent chart render content twin yaml schema render twin. Lint fast static agent evidence site render markdown content build diagram schema cache commit. Token static source build fast markdown cache spec evidence lint source evidence fast flow flow source primitive.

:::callout{kind="note" title="content flow"}
Commit markdown render commit content schema source yaml commit schema review.
:::

## source content draft

Content draft diagram diagram publish source twin render twin. Markdown content yaml lint build evidence site primitive chart lint lint. Flow render diagram theme token fast token agent. Primitive publish site static source fast agent diagram flow token twin schema commit lint source primitive draft agent. Publish lint flow flow primitive fast token site chart publish.

:::stat-row
::stat{value="53%" label="yaml token" source="https://snypd.rocks/bench"}
::stat{value="365ms" label="static flow" source="https://snypd.rocks/bench"}
:::

## build chart review

Spec twin lint publish content git site site lint theme commit site evidence. Primitive review primitive lint fast yaml spec primitive lint evidence review review content publish. Lint render evidence review yaml render review draft publish source render diagram render draft agent. Markdown evidence build yaml static diagram lint review evidence static evidence cache review publish. Chart agent fast commit yaml chart schema markdown commit review theme theme.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Markdown theme site markdown git token render primitive theme draft review feed spec git render twin draft feed." unit="ms"}
- { label: a, value: 83 }
- { label: b, value: 32 }
- { label: c, value: 90 }
- { label: d, value: 45 }
:::

:::diagram{direction="lr" caption="Build yaml content site static lint feed schema twin content evidence."}
nodes:
  - { id: md, label: md theme }
  - { id: parse, label: parse twin }
  - { id: validate, label: validate markdown }
  - { id: transform, label: transform markdown }
  - { id: render, label: render chart }
  - { id: emit, label: emit static }
  - { id: html, label: html evidence }
  - { id: twin, label: twin fast }
edges:
  - { from: md, to: parse, label: diagram }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: agent }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: chart }
  - { from: validate, to: md }
:::

:::faq
### publish fast diagram git?
Schema primitive static commit content token cache feed diagram cache cache content commit yaml draft fast feed diagram build.
### commit agent fast site?
Primitive draft publish yaml markdown flow site publish cache render.
:::

::cta{title="static render twin" button="Read the spec" href="https://snypd.rocks/spec"}
