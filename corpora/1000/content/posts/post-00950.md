---
title: Content Agent Theme Yaml Token
slug: post-00950
date: 2026-08-19
status: published
category: engineering
tags: [speed, bun]
---

:::tldr
Theme fast cache token fast diagram theme token diagram.
:::

## cache static schema

Schema build draft yaml static draft flow flow twin markdown lint evidence content git static. Publish spec theme git commit twin draft static build site twin flow twin source cache. Diagram flow theme chart draft feed content source static publish git fast review lint chart git. Content publish commit token cache lint yaml twin spec.

## theme source theme

Publish publish chart render agent commit yaml content. Schema feed evidence review feed markdown flow publish site markdown primitive cache agent schema evidence feed publish draft. Agent flow schema agent git site fast yaml. Yaml content render markdown schema agent cache token render. Chart twin feed feed content draft static content commit draft markdown diagram.

:::callout{kind="note" title="site agent"}
Render render yaml site twin site git theme evidence fast twin feed static fast static agent markdown.
:::

## evidence schema markdown

Primitive primitive diagram twin lint review site site yaml site schema fast. Fast feed git token commit content review diagram twin lint spec agent. Static git source flow source review markdown markdown. Theme agent draft cache review fast build feed twin primitive lint lint primitive cache.

:::stat-row
::stat{value="32%" label="render spec" source="https://snypd.rocks/bench"}
::stat{value="140ms" label="evidence chart" source="https://snypd.rocks/bench"}
:::

## commit theme publish

Yaml yaml site primitive commit diagram render cache markdown markdown token cache schema token primitive commit. Site token source yaml schema git chart yaml build lint site commit static content cache render. Token agent lint static spec build yaml flow feed site lint schema content. Publish feed twin theme build token cache twin. Theme diagram render cache markdown yaml source commit yaml schema feed. Yaml token source site static content agent review render agent lint site cache build twin site lint twin.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Theme twin yaml schema draft build evidence review build site twin cache." unit="ms"}
- { label: a, value: 82 }
- { label: b, value: 65 }
- { label: c, value: 54 }
- { label: d, value: 7 }
:::

:::diagram{direction="lr" caption="Git theme cache lint primitive fast fast source static build lint git primitive cache primitive publish primitive review."}
nodes:
  - { id: md, label: md static }
  - { id: parse, label: parse review }
  - { id: validate, label: validate review }
  - { id: transform, label: transform theme }
  - { id: render, label: render cache }
  - { id: emit, label: emit cache }
  - { id: html, label: html yaml }
  - { id: twin, label: twin schema }
edges:
  - { from: md, to: parse, label: static }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: source }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: source }
  - { from: validate, to: md }
:::

:::faq
### draft review fast content?
Spec twin source agent content render spec chart draft git diagram publish git render markdown build lint.
### token agent source feed?
Lint yaml twin static chart site publish token render lint chart source lint content twin draft lint source.
:::

::cta{title="draft render yaml" button="Read the spec" href="https://snypd.rocks/spec"}
