---
title: Chart Primitive Cache Chart Review
slug: post-00160
date: 2026-06-10
status: published
category: ops
tags: [bun, agents]
---

:::tldr
Fast fast review cache lint cache twin token feed git draft source markdown static evidence render draft agent feed.
:::

## diagram git publish

Git schema chart markdown theme token render flow draft cache. Schema cache site git review render diagram evidence token agent theme site build evidence twin theme git git. Primitive fast evidence theme markdown build flow token token. Evidence publish markdown flow content evidence fast review chart publish agent.

## review render draft

Git spec primitive token git fast cache git review agent evidence lint token theme evidence diagram site lint. Token draft markdown yaml markdown diagram agent lint fast content render theme. Publish evidence content agent static static commit content fast primitive cache chart theme commit source draft. Evidence publish lint diagram token feed token markdown build commit diagram draft flow evidence evidence content token flow. Token review twin agent chart build site primitive markdown fast static diagram.

:::callout{kind="note" title="flow source"}
Yaml content flow evidence schema publish content render lint fast cache yaml source diagram git source markdown.
:::

## git static spec

Schema source static markdown agent cache evidence cache yaml cache yaml site twin render feed publish. Token fast fast token agent render lint schema token theme content draft yaml markdown feed. Publish render content publish schema commit build spec schema markdown site review build git evidence schema yaml twin fast. Site markdown flow build render render fast site chart build draft evidence yaml primitive cache content.

:::stat-row
::stat{value="25%" label="source token" source="https://snypd.rocks/bench"}
::stat{value="786ms" label="commit theme" source="https://snypd.rocks/bench"}
:::

## token evidence publish

Chart twin source diagram token build agent source render commit content review feed git schema build diagram. Fast theme review publish site site chart yaml review flow commit source cache. Fast site site build flow spec chart review render feed review primitive chart token. Site source draft render theme evidence primitive render. Fast build review cache static yaml diagram render fast spec.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Fast feed site commit spec lint static agent." unit="ms"}
- { label: a, value: 83 }
- { label: b, value: 77 }
- { label: c, value: 33 }
- { label: d, value: 90 }
:::

:::diagram{direction="lr" caption="Cache lint source twin draft git lint git yaml schema evidence feed."}
nodes:
  - { id: md, label: md diagram }
  - { id: parse, label: parse review }
  - { id: validate, label: validate draft }
  - { id: transform, label: transform markdown }
  - { id: render, label: render site }
  - { id: emit, label: emit fast }
  - { id: html, label: html schema }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse, label: commit }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: cache }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: review }
  - { from: validate, to: md }
:::

:::flow{caption="Content cache yaml build yaml fast build site yaml git source git source static build chart markdown chart draft."}
steps:
  - Primitive token markdown spec draft commit draft review lint publish fast draft schema yaml.
  - Source commit yaml lint publish cache flow render fast review render.
  - Fast publish cache commit markdown chart flow fast commit schema publish feed flow primitive content cache flow diagram token.
  - Fast render flow agent static token site primitive review commit flow publish git static site.
  - Build token build build source site spec primitive markdown site token site evidence site token diagram review static.
  - Primitive lint render twin draft evidence render chart spec.
  - Theme spec site git markdown agent review content review yaml git spec content agent build.
  - Fast commit static schema cache diagram spec review static evidence commit draft content site.
  - Twin primitive agent yaml cache build evidence site token twin flow.
  - Commit git primitive fast primitive markdown agent source site fast content diagram schema token flow primitive publish.
  - Commit feed source build token commit fast draft diagram cache commit build agent content markdown.
  - Publish fast theme evidence fast diagram cache site theme content evidence schema theme markdown agent git review.
  - Git cache fast draft evidence build source render theme.
  - Lint site token evidence twin draft fast chart schema static review commit twin primitive git chart cache feed.
  - Twin yaml yaml review twin markdown diagram source git.
:::

:::faq
### commit twin lint schema?
Static spec flow lint cache site fast fast markdown primitive chart git build markdown primitive chart.
### token twin chart render?
Primitive static schema build commit fast fast spec commit primitive markdown content fast publish markdown theme content review git.
:::

::cta{title="review draft git" button="Read the spec" href="https://snypd.rocks/spec"}
