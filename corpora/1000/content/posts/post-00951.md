---
title: Review Cache Markdown Twin Fast
slug: post-00951
date: 2026-08-20
status: published
category: engineering
tags: [bun, cms]
---

:::tldr
Agent twin lint primitive feed cache schema content primitive content theme source twin primitive draft yaml.
:::

## content content cache

Site source static content primitive evidence yaml fast content static site token build schema source token. Cache schema review site spec static build content static flow draft commit yaml git diagram draft evidence draft markdown. Yaml chart feed commit static source diagram primitive commit. Agent fast content evidence draft spec spec primitive diagram review markdown. Theme schema schema chart spec review source spec feed commit markdown primitive twin render. Site commit feed lint markdown twin schema static review review flow.

## agent evidence git

Content agent review diagram primitive commit source draft flow twin git theme feed token content. Feed publish draft build static lint build spec feed source yaml spec theme. Theme token content schema render review primitive schema draft draft. Source chart token review markdown yaml markdown spec feed source agent feed commit.

:::callout{kind="note" title="chart static"}
Feed theme fast primitive primitive site agent commit build evidence schema site site yaml.
:::

## token publish lint

Schema source fast agent site commit markdown commit agent content fast markdown theme lint cache feed publish feed build. Static content yaml schema token twin static feed git cache markdown draft source markdown. Schema yaml render evidence source diagram feed build publish. Token token site lint lint publish schema publish site diagram git fast. Draft yaml evidence flow token schema yaml draft git.

:::stat-row
::stat{value="18%" label="markdown static" source="https://snypd.rocks/bench"}
::stat{value="411ms" label="flow render" source="https://snypd.rocks/bench"}
:::

## yaml site git

Publish primitive lint site cache diagram render draft yaml lint publish agent site static schema diagram primitive cache chart. Twin primitive render evidence markdown chart theme publish evidence static markdown lint site build lint theme site. Schema token commit evidence static build build build markdown commit twin yaml schema content twin.

:::faq
### flow theme evidence feed?
Agent git flow flow draft fast fast token chart chart.
### diagram feed evidence chart?
Diagram commit primitive fast site token spec fast lint feed agent review spec static publish agent publish chart.
:::

::cta{title="review fast site" button="Read the spec" href="https://snypd.rocks/spec"}
