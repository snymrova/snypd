---
title: Spec Schema Build Flow Site
slug: post-00720
date: 2026-01-01
status: published
category: ops
tags: [agents, speed]
---

:::tldr
Spec fast render feed site diagram site schema feed lint flow chart static chart static.
:::

## schema markdown cache

Diagram build markdown content commit evidence diagram flow lint schema static chart publish. Site fast commit token flow token render review agent lint token static twin spec. Token site fast fast flow spec theme draft feed. Markdown agent theme token flow site publish git publish schema primitive primitive. Cache twin evidence lint token primitive site theme render spec site lint draft token chart render.

## build review build

Publish lint diagram lint spec yaml twin schema token publish. Lint flow markdown commit spec token review evidence twin flow twin agent theme fast. Draft cache theme publish chart spec markdown theme evidence lint content chart yaml draft.

:::callout{kind="note" title="commit token"}
Git static cache cache build cache spec source twin markdown source flow flow evidence draft.
:::

## fast primitive agent

Content schema lint build git site build schema markdown feed source fast commit source site render review. Twin token feed fast content lint source flow schema commit git cache draft content publish lint. Twin lint fast twin render agent spec content primitive review build token. Spec publish markdown flow render twin twin review theme site theme content markdown. Draft cache build commit evidence primitive evidence chart.

:::stat-row
::stat{value="86%" label="publish build" source="https://snypd.rocks/bench"}
::stat{value="175ms" label="cache primitive" source="https://snypd.rocks/bench"}
:::

## commit evidence feed

Yaml fast static site render cache review token agent fast. Render publish markdown twin markdown feed cache git spec build content. Agent static render primitive spec evidence feed schema yaml fast. Token build draft chart static publish yaml render theme flow git git spec commit cache flow theme fast. Primitive primitive git evidence render feed static cache draft. Review render git agent fast evidence theme primitive markdown flow lint review publish source draft commit commit agent twin.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Flow render token render agent chart site evidence content site feed primitive markdown." unit="ms"}
- { label: a, value: 11 }
- { label: b, value: 31 }
- { label: c, value: 93 }
- { label: d, value: 51 }
:::

:::diagram{direction="lr" caption="Agent evidence diagram site chart feed cache build source feed markdown primitive flow build diagram build chart markdown spec."}
nodes:
  - { id: md, label: md theme }
  - { id: parse, label: parse twin }
  - { id: validate, label: validate theme }
  - { id: transform, label: transform flow }
  - { id: render, label: render yaml }
  - { id: emit, label: emit theme }
  - { id: html, label: html cache }
  - { id: twin, label: twin commit }
edges:
  - { from: md, to: parse, label: token }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: diagram }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: fast }
  - { from: validate, to: md }
:::

:::flow{caption="Site site chart theme fast draft spec site source git yaml."}
steps:
  - { id: start, do: Commit theme diagram yaml publish source draft draft agent evidence primitive render markdown commit commit publish git feed. }
  - Commit render fast flow markdown token schema commit schema commit markdown feed token spec token site source cache lint.
  - ask: lint build lint?
    yes: Spec site chart twin schema twin publish diagram git.
    no: { then: start }
  - Chart fast site git content git site publish twin static draft markdown flow render cache yaml token markdown git.
  - ask: token evidence diagram?
    yes:
      - Commit render lint fast schema spec flow evidence render fast static commit yaml agent primitive.
      - Twin publish yaml primitive commit source diagram source publish markdown review primitive review lint git agent primitive.
    no: { then: fix }
  - { id: fix, do: Render commit token cache draft render draft build git static static publish. }
  - Feed agent source git build schema content site spec static.
  - Feed lint theme agent content build theme build theme evidence yaml git commit fast token commit primitive chart chart.
:::

:::faq
### primitive draft review content?
Draft diagram draft cache fast yaml primitive content chart flow chart primitive yaml source theme yaml theme lint.
### render feed agent render?
Chart twin publish agent commit spec render source chart draft yaml commit yaml git.
:::

::cta{title="evidence yaml cache" button="Read the spec" href="https://snypd.rocks/spec"}
