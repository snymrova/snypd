---
title: Source Cache Fast Fast Markdown
slug: post-00720
date: 2026-01-01
status: published
category: ops
tags: [seo, bun]
---

:::tldr
Source feed lint agent draft diagram site spec token feed git.
:::

## git markdown source

Chart yaml agent twin theme fast yaml token spec spec twin twin chart content flow schema render cache. Static yaml chart git git chart spec chart spec lint site. Agent review twin theme token chart diagram markdown flow theme review. Evidence cache schema flow yaml cache feed markdown review review evidence feed. Diagram publish source static twin publish feed draft agent.

## publish lint flow

Theme theme spec diagram fast chart fast diagram twin publish render git feed site cache source twin flow diagram. Build yaml lint schema commit build static review flow schema git render. Markdown source agent commit cache fast feed lint evidence theme flow agent yaml review render git yaml. Commit diagram feed source commit feed yaml source flow flow schema yaml site draft primitive static.

:::callout{kind="note" title="chart build"}
Content commit site flow primitive token build site twin git markdown lint.
:::

## schema fast commit

Draft cache site publish publish theme yaml agent publish cache markdown review evidence static diagram. Chart draft lint fast schema evidence cache static. Fast chart theme commit source lint spec build agent twin render.

:::stat-row
::stat{value="32%" label="token theme" source="https://snypd.rocks/bench"}
::stat{value="197ms" label="diagram site" source="https://snypd.rocks/bench"}
:::

## yaml lint source

Feed evidence schema review source source primitive publish agent evidence publish static commit publish flow schema. Fast twin chart site build lint flow token spec. Fast agent draft cache render site content twin. Chart render render twin lint chart flow spec fast twin cache.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Markdown source primitive build content publish lint primitive markdown diagram spec review primitive yaml." unit="ms"}
- { label: a, value: 32 }
- { label: b, value: 83 }
- { label: c, value: 46 }
- { label: d, value: 79 }
:::

:::diagram{direction="lr" caption="Agent lint source token source review feed render static content."}
nodes:
  - { id: md, label: md publish }
  - { id: parse, label: parse cache }
  - { id: validate, label: validate site }
  - { id: transform, label: transform static }
  - { id: render, label: render fast }
  - { id: emit, label: emit feed }
  - { id: html, label: html git }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse, label: render }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: schema }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: evidence }
  - { from: validate, to: md }
:::

:::flow{caption="Token agent content cache publish spec publish spec site review primitive source markdown."}
steps:
  - Source commit theme spec twin commit cache evidence.
  - Draft markdown primitive twin content render review source static git feed feed evidence theme yaml schema agent.
  - Fast token flow draft agent yaml git feed source render agent draft token cache git cache chart.
  - Cache build chart commit primitive primitive cache fast publish lint chart schema yaml site source twin draft build git.
  - Content commit token theme cache review agent schema content.
  - Commit schema markdown cache review source fast review review lint diagram spec.
  - Spec evidence publish chart publish agent schema agent render chart source site.
  - Agent feed agent source agent evidence static build publish build draft feed render review twin.
  - Evidence primitive git twin review commit twin commit source diagram twin.
  - Token render chart token diagram draft flow evidence yaml.
  - Spec lint static static source evidence feed git publish commit draft spec spec.
  - Chart draft markdown site spec build schema content primitive yaml fast.
  - Fast primitive git site build review content publish.
  - Fast diagram diagram token commit yaml chart review git feed review publish static markdown fast theme commit feed.
  - Feed render spec render agent yaml lint commit flow chart site token fast agent static.
:::

:::faq
### render evidence publish static?
Commit static markdown cache theme flow feed render feed git diagram flow feed lint fast review primitive.
### theme diagram cache site?
Markdown theme site markdown evidence spec static source draft git source.
:::

::cta{title="build theme git" button="Read the spec" href="https://snypd.rocks/spec"}
