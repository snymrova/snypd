---
title: Source Lint Flow Token Git
slug: post-00720
date: 2026-01-01
status: published
category: product
tags: [markdown, ai]
---

:::tldr
Markdown evidence static commit review feed markdown commit feed git commit flow agent lint site.
:::

## theme lint fast

Review publish draft git theme theme site commit draft markdown flow token lint static feed build. Render diagram render theme flow content git site source site. Agent review build diagram flow lint flow chart content fast source markdown lint lint static static commit. Render primitive site cache theme site diagram token agent flow. Review commit site token twin build lint render chart yaml git schema schema draft agent commit.

## chart draft markdown

Diagram theme markdown chart token schema flow site markdown commit schema render build twin. Spec flow flow lint cache markdown agent cache agent commit content content feed lint primitive. Source flow static content fast review publish render theme site twin schema site static lint. Diagram review build draft token render review yaml diagram agent draft agent theme yaml git twin schema.

:::callout{kind="note" title="evidence lint"}
Review static static evidence static feed review evidence content cache.
:::

## twin cache diagram

Publish draft source chart diagram schema static token flow publish token evidence lint flow. Site agent review render feed schema render twin lint evidence diagram spec git content cache. Git content site fast source fast markdown spec content token. Publish agent twin flow evidence spec review markdown primitive yaml build schema commit yaml spec fast primitive schema.

:::stat-row
::stat{value="20%" label="site render" source="https://snypd.rocks/bench"}
::stat{value="265ms" label="source yaml" source="https://snypd.rocks/bench"}
:::

## diagram draft commit

Publish schema evidence schema source flow diagram content primitive theme publish site feed. Commit schema yaml flow content markdown review publish commit git yaml draft. Draft twin build render git yaml primitive diagram primitive theme token render.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Lint render twin cache commit evidence feed flow publish git evidence site flow source primitive source." unit="ms"}
- { label: a, value: 76 }
- { label: b, value: 38 }
- { label: c, value: 13 }
- { label: d, value: 73 }
:::

:::diagram{direction="lr" caption="Publish cache markdown publish fast cache fast source content git."}
nodes:
  - { id: md, label: md primitive }
  - { id: parse, label: parse static }
  - { id: validate, label: validate review }
  - { id: transform, label: transform source }
  - { id: render, label: render markdown }
  - { id: emit, label: emit token }
  - { id: html, label: html static }
  - { id: twin, label: twin render }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Commit lint review agent twin feed evidence static static static feed build content markdown render spec."}
steps:
  - Site lint cache yaml static static commit markdown content draft twin draft chart token render.
  - Twin site twin chart static agent content twin agent site static fast token commit diagram lint publish source.
  - Content theme static content site render commit cache site fast token.
  - Diagram twin render theme spec chart review token markdown markdown agent build commit yaml site git.
  - Site static theme render spec evidence render git publish token token source agent draft evidence review content agent.
  - Flow fast static flow cache static site schema yaml chart spec spec render site evidence theme build.
  - Git build diagram yaml primitive feed cache publish diagram source.
  - Cache cache cache build lint git spec draft publish schema.
  - Draft agent yaml cache site markdown evidence feed chart draft markdown chart.
  - Token commit publish primitive agent content review build.
  - Spec source theme twin draft review token cache markdown fast schema evidence fast schema commit draft.
  - Theme feed render git feed lint agent token git review schema lint agent token agent.
  - Agent cache primitive diagram evidence evidence static source twin content static chart render agent agent publish site.
  - Build commit markdown yaml source feed git token.
  - Static schema evidence schema twin fast primitive cache git yaml spec draft git lint commit publish spec token source.
:::

:::faq
### git markdown twin token?
Cache agent token primitive static evidence token token publish.
### static review static publish?
Theme flow fast agent commit build render flow draft primitive commit review commit.
:::

::cta{title="review diagram agent" button="Read the spec" href="https://snypd.rocks/spec"}
