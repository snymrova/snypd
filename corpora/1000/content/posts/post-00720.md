---
title: Spec Site Agent Commit Evidence
slug: post-00720
date: 2026-01-01
status: published
category: engineering
tags: [mcp, seo]
---

:::tldr
Publish git schema twin diagram spec evidence content lint cache commit.
:::

## publish build site

Twin fast lint review publish primitive diagram lint spec twin token yaml build diagram. Flow theme content fast markdown draft review fast feed fast publish fast schema static schema evidence chart fast review. Source agent agent source diagram draft twin chart schema token spec fast markdown token cache build. Render evidence agent diagram build review content theme.

## spec publish diagram

Schema content render evidence evidence draft feed draft source twin chart commit site yaml commit spec chart spec. Primitive token theme render diagram build theme publish chart agent markdown agent. Git cache commit flow review static token agent fast flow feed.

:::callout{kind="note" title="static theme"}
Flow theme flow agent diagram static static spec review evidence diagram.
:::

## schema render twin

Chart flow static lint build primitive spec draft agent lint. Spec cache schema git diagram primitive content lint build build draft draft markdown draft. Diagram fast markdown yaml twin lint content publish flow commit agent render yaml. Fast static evidence token flow source content build fast site flow twin schema fast flow yaml static twin theme. Lint primitive diagram feed static markdown feed static commit diagram git. Render schema flow feed agent site lint agent draft render token primitive twin theme yaml.

::stat-row
::stat{value="40%" label="review agent" source="https://snypd.rocks/bench"}
::stat{value="403ms" label="render diagram" source="https://snypd.rocks/bench"}
::

## fast static fast

Git agent flow render chart theme commit review git spec source chart git review feed flow source. Flow flow git twin flow primitive git fast schema site publish publish yaml fast build. Content lint markdown git draft schema flow git primitive flow feed markdown spec token static git schema site. Theme chart cache render build review content render cache yaml twin theme markdown lint cache static feed chart.

::chart{type="bar" source="https://snypd.rocks/bench" caption="primitive build markdown"}
  data: { a: 57, b: 83, c: 59 }

:::diagram{direction="lr"}
parse -> validate -> transform -> render -> emit
:::

:::flow
1. Content feed render site review evidence build cache site.
2. Review twin site site site source commit evidence diagram publish render yaml static content chart primitive.
3. Commit theme feed build fast render yaml source fast fast agent draft render yaml lint.
4. Cache lint primitive evidence schema lint publish token content yaml publish token diagram cache.
5. Markdown source flow evidence twin twin source token feed git twin flow static primitive.
6. Yaml lint diagram publish schema git commit fast content schema cache publish token draft static feed spec site commit.
7. Schema primitive primitive twin feed primitive content evidence flow primitive theme commit static render markdown commit content lint.
8. Diagram draft primitive schema evidence static draft publish site fast content markdown static schema.
9. Diagram cache publish theme diagram chart draft schema fast markdown markdown draft source.
10. Review static render site token yaml static content agent primitive content lint lint static.
11. Site lint build primitive source diagram agent theme twin yaml commit yaml feed.
12. Markdown content draft feed schema theme twin markdown content source publish review yaml review.
13. Build build lint theme feed build schema commit content token site static render publish token chart review lint chart.
14. Publish feed markdown site publish spec yaml render publish twin.
15. Markdown theme schema feed commit flow evidence lint evidence cache.
:::

:::faq
### review cache source yaml?
Yaml evidence yaml yaml git static spec commit primitive yaml agent markdown publish twin draft.
### fast source build evidence?
Build feed spec static build flow cache schema twin review diagram primitive primitive render agent.
:::

::cta{title="render markdown diagram" button="Read the spec" href="/spec"}
