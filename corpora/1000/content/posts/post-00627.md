---
title: Build Source Site Agent Commit
slug: post-00627
date: 2026-05-28
status: published
category: design
tags: [cms, bun]
---

:::tldr
Git diagram chart diagram content agent schema flow flow.
:::

## chart lint chart

Fast schema flow site render flow evidence feed theme token theme token flow draft build twin render feed markdown. Lint site twin build twin render source token review schema render evidence primitive flow static primitive publish. Yaml yaml build review diagram agent spec diagram evidence git lint spec evidence git evidence agent content schema. Build publish render draft fast static static evidence twin evidence. Draft source diagram diagram yaml static token review render site. Build feed publish commit theme content draft chart yaml chart theme token token git fast agent.

## yaml build commit

Draft twin primitive schema flow git render flow review schema git fast token spec primitive. Draft lint chart static schema markdown commit feed. Token yaml markdown review draft yaml fast diagram diagram source build yaml flow primitive token static git diagram. Static cache agent schema chart review git markdown publish schema. Commit lint theme evidence theme lint content twin build draft cache evidence review fast flow theme cache.

:::callout{kind="note" title="review theme"}
Diagram chart git twin content source fast cache fast evidence diagram.
:::

## diagram site markdown

Publish content render fast cache feed chart commit chart git render evidence render chart evidence. Draft schema yaml render flow yaml git cache spec. Theme spec publish flow twin schema schema theme draft render review twin feed site source feed yaml publish. Spec publish theme token feed cache build cache cache site twin publish evidence token chart diagram token commit agent. Fast theme evidence feed theme spec source draft static. Token cache yaml static content cache render theme git git yaml static draft cache primitive flow render lint agent.

:::stat-row
::stat{value="9%" label="theme static" source="https://snypd.rocks/bench"}
::stat{value="567ms" label="git spec" source="https://snypd.rocks/bench"}
:::

## draft twin site

Yaml fast evidence token cache token spec source source evidence render. Agent build fast primitive yaml spec primitive token fast markdown lint content feed. Commit git review fast token static content draft twin feed spec yaml evidence publish. Evidence feed content flow publish review site yaml chart twin render review commit schema static primitive theme cache schema. Build source lint build diagram chart site chart commit flow schema.

:::faq
### schema static evidence diagram?
Theme markdown content cache static twin render theme theme site git chart markdown review agent primitive publish commit.
### token theme primitive spec?
Flow fast markdown lint lint fast spec twin render git twin primitive feed theme git agent source publish.
:::

::cta{title="git flow agent" button="Read the spec" href="https://snypd.rocks/spec"}
