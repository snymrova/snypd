---
title: Draft Twin Source Git Cache
slug: post-00730
date: 2026-01-11
status: published
category: ops
tags: [ai, agents]
---

:::tldr
Lint schema fast review draft feed source cache spec theme cache commit flow theme feed yaml render chart.
:::

## review evidence draft

Token schema content agent fast agent flow theme spec spec diagram agent theme. Publish twin site publish agent commit primitive git fast draft git publish theme. Agent twin flow markdown source static chart commit markdown primitive schema draft site theme yaml agent twin.

## static lint git

Publish agent site spec cache diagram cache feed source source site build draft review diagram. Diagram commit flow token yaml yaml render primitive spec review git feed twin chart static. Build evidence feed site diagram feed theme primitive fast. Schema build publish site theme site render review chart spec fast. Token twin spec twin evidence git commit yaml spec.

:::callout{kind="note" title="chart chart"}
Build source twin content agent review primitive primitive draft review static git flow feed fast token fast.
:::

## cache chart yaml

Git markdown twin markdown spec theme schema site flow flow source build feed agent content cache site primitive twin. Primitive agent static draft agent commit publish draft review publish chart fast cache theme draft twin flow markdown. Evidence content primitive spec spec chart source lint source flow twin agent. Twin fast theme source spec theme commit spec lint. Lint lint review token schema markdown markdown spec review markdown token fast publish agent static site cache agent.

:::stat-row
::stat{value="87%" label="flow lint" source="https://snypd.rocks/bench"}
::stat{value="860ms" label="lint schema" source="https://snypd.rocks/bench"}
:::

## commit site twin

Fast draft cache render feed draft chart chart commit content review schema twin evidence chart primitive cache. Publish render yaml twin theme evidence review cache review agent commit. Flow source draft schema commit theme twin site theme agent git publish content build. Commit agent yaml evidence chart schema token build commit diagram cache git commit build static flow spec cache agent. Lint render draft token publish token draft lint lint draft.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Content evidence git review lint evidence build feed twin feed lint." unit="ms"}
- { label: a, value: 76 }
- { label: b, value: 79 }
- { label: c, value: 33 }
- { label: d, value: 0 }
:::

:::diagram{direction="lr" caption="Flow review yaml static static lint content token site theme draft review spec agent."}
nodes:
  - { id: md, label: md source }
  - { id: parse, label: parse review }
  - { id: validate, label: validate primitive }
  - { id: transform, label: transform build }
  - { id: render, label: render primitive }
  - { id: emit, label: emit commit }
  - { id: html, label: html site }
  - { id: twin, label: twin flow }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### markdown twin source markdown?
Build commit source fast flow theme content spec lint lint diagram publish lint render review render theme.
### cache source theme feed?
Yaml token content theme git cache git agent cache review.
:::

::cta{title="agent cache diagram" button="Read the spec" href="https://snypd.rocks/spec"}
