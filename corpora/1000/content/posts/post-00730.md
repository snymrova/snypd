---
title: Site Publish Fast Twin Diagram
slug: post-00730
date: 2026-01-11
status: published
category: product
tags: [seo, ai]
---

:::tldr
Schema render cache review static schema build publish yaml schema lint primitive lint feed flow evidence.
:::

## theme source fast

Agent theme spec fast schema commit feed evidence commit spec static flow draft source git flow yaml draft review. Publish review content feed commit flow twin commit source. Agent primitive content spec cache evidence diagram agent site review token feed.

## yaml content lint

Agent git chart diagram theme commit flow cache twin site content draft. Content theme content yaml token source render publish schema site static. Cache spec static flow agent fast static review.

:::callout{kind="note" title="schema theme"}
Build chart markdown markdown git git agent render diagram draft source.
:::

## commit diagram static

Lint build build content commit flow fast spec render agent evidence git content. Token schema site commit primitive publish schema agent commit twin chart markdown. Markdown git token flow review flow flow spec lint markdown commit draft evidence spec. Draft primitive flow review evidence twin fast primitive markdown cache chart token twin. Lint review draft evidence review cache static chart evidence lint evidence schema theme. Publish theme static git diagram yaml markdown commit source draft review.

:::stat-row
::stat{value="38%" label="markdown flow" source="https://snypd.rocks/bench"}
::stat{value="511ms" label="cache evidence" source="https://snypd.rocks/bench"}
:::

## static chart chart

Content diagram markdown commit theme chart content commit publish. Primitive chart fast agent git site cache draft. Spec source theme static render evidence draft feed site agent site git diagram evidence feed lint. Token markdown diagram review render flow render site schema render.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Schema theme primitive cache primitive schema yaml primitive build markdown source content agent token feed." unit="ms"}
- { label: a, value: 71 }
- { label: b, value: 29 }
- { label: c, value: 45 }
- { label: d, value: 44 }
:::

:::diagram{direction="lr" caption="Review token evidence schema markdown feed yaml diagram static git yaml git content evidence site schema."}
nodes:
  - { id: md, label: md chart }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate theme }
  - { id: transform, label: transform review }
  - { id: render, label: render diagram }
  - { id: emit, label: emit primitive }
  - { id: html, label: html spec }
  - { id: twin, label: twin flow }
edges:
  - { from: md, to: parse, label: feed }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: schema }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: token }
  - { from: validate, to: md }
:::

:::faq
### diagram review render theme?
Git publish git token site yaml agent cache flow evidence theme render diagram flow flow schema git.
### draft primitive diagram twin?
Flow review token token primitive git markdown twin evidence token yaml commit publish content commit cache schema yaml git.
:::

::cta{title="static feed publish" button="Read the spec" href="https://snypd.rocks/spec"}
