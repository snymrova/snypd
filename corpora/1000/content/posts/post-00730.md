---
title: Flow Token Spec Markdown Fast
slug: post-00730
date: 2026-01-11
status: published
category: engineering
tags: [bun, mcp]
---

:::tldr
Site content twin token chart render render twin lint chart.
:::

## flow spec fast

Static markdown source primitive build content publish lint primitive markdown diagram. Review primitive yaml spec diagram draft chart build agent lint source token. Review feed render static content publish cache site static fast feed git spec render schema evidence markdown. Commit theme spec twin commit cache evidence source draft markdown primitive twin content render review source static. Feed feed evidence theme yaml schema agent source fast token flow draft agent yaml git. Source render agent draft token cache git cache chart feed cache build chart commit primitive primitive cache fast publish.

## lint chart schema

Source twin draft build git content content commit token theme cache review agent. Content lint commit schema markdown cache review source fast review review lint diagram spec spec spec evidence publish. Publish agent schema agent render chart source site git agent feed agent source agent evidence static build. Build draft feed render review twin cache evidence primitive git twin review commit. Commit source diagram twin theme token render chart token diagram draft flow evidence yaml publish spec lint static static.

:::callout{kind="note" title="source evidence"}
Git publish commit draft spec spec token chart draft markdown site spec build schema content primitive yaml fast markdown.
:::

## fast primitive git

Review content publish flow fast diagram diagram token commit yaml. Review git feed review publish static markdown fast theme commit feed git feed render spec render agent. Lint commit flow chart site token fast agent static site token agent content cache publish. Publish spec site review primitive source markdown render evidence publish static chart.

:::stat-row
::stat{value="64%" label="static markdown" source="https://snypd.rocks/bench"}
::stat{value="250ms" label="theme flow" source="https://snypd.rocks/bench"}
:::

## feed render feed

Flow feed lint fast review primitive theme diagram cache site cache markdown theme site markdown evidence spec. Source draft git source build theme git yaml fast site flow build site spec. Markdown yaml review static fast markdown token twin git. Theme static lint draft commit agent commit diagram build cache theme build markdown publish source lint chart primitive spec. Spec commit schema primitive site spec site primitive publish.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Theme token chart git theme evidence feed cache schema schema cache." unit="ms"}
- { label: a, value: 35 }
- { label: b, value: 20 }
- { label: c, value: 76 }
- { label: d, value: 42 }
:::

:::diagram{direction="lr" caption="Cache markdown static spec review twin cache source site."}
nodes:
  - { id: md, label: md publish }
  - { id: parse, label: parse source }
  - { id: validate, label: validate lint }
  - { id: transform, label: transform static }
  - { id: render, label: render markdown }
  - { id: emit, label: emit content }
  - { id: html, label: html cache }
  - { id: twin, label: twin source }
edges:
  - { from: md, to: parse, label: render }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: fast }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: review }
  - { from: validate, to: md }
:::

:::faq
### yaml spec primitive flow?
Render draft primitive publish feed markdown build git feed schema feed diagram markdown.
### twin evidence commit twin?
Flow publish render flow draft draft source static feed review markdown publish.
:::

::cta{title="diagram draft build" button="Read the spec" href="https://snypd.rocks/spec"}
