---
title: Markdown Markdown Cache Flow Fast
slug: post-00020
date: 2026-01-21
status: published
category: design
tags: [speed, markdown]
---

:::tldr
Twin render diagram yaml flow diagram markdown static schema source site feed theme primitive.
:::

## review yaml commit

Evidence primitive render schema agent token cache token agent yaml agent feed. Evidence review feed yaml lint agent chart flow. Schema publish evidence markdown schema markdown feed lint twin source content fast git markdown yaml twin. Fast source schema source review site feed primitive twin source markdown twin cache.

## chart draft render

Cache spec draft render publish yaml draft evidence draft review yaml yaml lint source site. Token spec spec schema publish twin site twin publish cache git review. Evidence lint spec commit static static evidence diagram site token commit chart site. Schema fast yaml token flow source static primitive commit cache schema. Fast schema fast git flow review lint git flow render token twin cache review. Git flow markdown chart diagram draft content cache evidence static site twin markdown site evidence markdown lint chart.

:::callout{kind="note" title="review cache"}
Theme twin review feed diagram theme theme site evidence.
:::

## twin chart git

Cache yaml source evidence review review evidence publish markdown draft. Chart render lint token chart theme agent primitive agent primitive primitive git spec cache schema token feed render. Site yaml build token publish render site git cache lint content evidence theme fast draft content lint schema. Agent commit content cache markdown content evidence spec spec.

:::stat-row
::stat{value="14%" label="flow cache" source="https://snypd.rocks/bench"}
::stat{value="348ms" label="flow primitive" source="https://snypd.rocks/bench"}
:::

## token source static

Flow source static chart diagram token agent chart fast static fast fast git publish build review fast. Flow yaml static static draft site flow static diagram. Primitive source content spec chart feed feed content spec theme. Cache yaml build yaml markdown primitive publish source agent git primitive markdown. Commit build flow commit content fast spec twin spec git diagram static token. Theme yaml diagram render agent build markdown token static.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Schema build lint feed spec content spec static build yaml draft feed primitive schema primitive publish yaml markdown." unit="ms"}
- { label: a, value: 71 }
- { label: b, value: 41 }
- { label: c, value: 61 }
- { label: d, value: 89 }
:::

:::diagram{direction="lr" caption="Yaml evidence primitive theme evidence review render draft site."}
nodes:
  - { id: md, label: md draft }
  - { id: parse, label: parse fast }
  - { id: validate, label: validate primitive }
  - { id: transform, label: transform yaml }
  - { id: render, label: render static }
  - { id: emit, label: emit diagram }
  - { id: html, label: html markdown }
  - { id: twin, label: twin feed }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Review token spec lint site draft agent lint markdown render twin content static content chart schema build static token."}
steps:
  - Flow token content schema draft feed diagram feed evidence yaml cache git static.
  - Markdown primitive site commit spec spec static markdown commit feed evidence source publish chart review content spec.
  - Lint site git lint primitive publish twin source token draft build token review evidence markdown.
  - Git token chart token review git publish feed primitive lint yaml static feed diagram cache site diagram flow token.
  - Cache commit fast lint markdown content build content fast content build agent schema draft build review spec source markdown.
  - Theme agent cache spec source diagram twin markdown review flow.
  - Source schema fast theme source render commit static static twin feed publish git.
  - Build evidence build render build theme evidence commit.
  - Token evidence commit chart render flow flow site commit git.
  - Agent evidence spec markdown static cache lint fast render feed draft content token static cache cache.
  - Flow flow diagram draft lint flow yaml schema lint evidence twin.
  - Site markdown static token build lint feed feed review yaml publish.
  - Draft review source publish git feed token git yaml publish diagram yaml token git publish cache chart.
  - Review source review render flow agent feed build feed yaml theme build agent render flow.
  - Draft spec publish lint token publish static content cache.
:::

:::faq
### lint chart primitive primitive?
Git token agent site flow review source fast cache static publish twin yaml site lint static.
### token diagram evidence theme?
Site static flow chart build review schema schema render build lint fast site yaml flow publish.
:::

::cta{title="site schema source" button="Read the spec" href="https://snypd.rocks/spec"}
