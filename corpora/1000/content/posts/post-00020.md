---
title: Source Publish Commit Fast Primitive
slug: post-00020
date: 2026-01-21
status: published
category: design
tags: [seo, cms]
---

:::tldr
Schema render markdown theme flow feed chart site.
:::

## markdown markdown yaml

Diagram chart feed content git yaml render content spec agent cache. Source publish site source render draft site git primitive diagram schema git theme static render site git token. Twin token source yaml git schema cache flow. Spec draft publish build markdown markdown cache flow fast commit diagram flow. Twin render diagram yaml flow diagram markdown static schema source site feed theme primitive.

## review yaml commit

Evidence primitive render schema agent token cache token agent yaml agent feed. Evidence review feed yaml lint agent chart flow. Schema publish evidence markdown schema markdown feed lint twin source content fast git markdown yaml twin. Fast source schema source review site feed primitive twin source markdown twin cache.

:::callout{kind="note" title="chart draft"}
Diagram yaml cache spec draft render publish yaml draft evidence.
:::

## draft review yaml

Source site publish token spec spec schema publish twin site twin publish. Git review site evidence lint spec commit static static evidence diagram. Token commit chart site cache schema fast yaml token flow source static primitive. Cache schema fast fast schema fast git flow review lint git flow render token twin. Review flow git flow markdown chart diagram draft content cache evidence.

:::stat-row
::stat{value="51%" label="site twin" source="https://snypd.rocks/bench"}
::stat{value="54ms" label="site evidence" source="https://snypd.rocks/bench"}
:::

## markdown lint chart

Primitive theme twin review feed diagram theme theme site evidence twin. Git publish primitive cache yaml source evidence review review evidence publish markdown draft flow chart render lint. Chart theme agent primitive agent primitive primitive git spec cache schema. Feed render flow site yaml build token publish render site git. Lint content evidence theme fast draft content lint schema content agent.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Content cache markdown content evidence spec spec primitive flow cache lint flow primitive token source." unit="ms"}
- { label: a, value: 51 }
- { label: b, value: 79 }
- { label: c, value: 76 }
- { label: d, value: 87 }
:::

:::diagram{direction="lr" caption="Static chart diagram token agent chart fast static fast fast git publish build review fast content flow."}
nodes:
  - { id: md, label: md yaml }
  - { id: parse, label: parse static }
  - { id: validate, label: validate static }
  - { id: transform, label: transform draft }
  - { id: render, label: render site }
  - { id: emit, label: emit flow }
  - { id: html, label: html static }
  - { id: twin, label: twin diagram }
edges:
  - { from: md, to: parse, label: render }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: primitive }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: source }
  - { from: validate, to: md }
:::

:::flow{caption="Agent evidence spec markdown static cache lint fast render feed draft content token static cache cache."}
steps:
  - Spec chart feed feed content spec theme spec cache.
  - Build yaml markdown primitive publish source agent git primitive markdown publish commit build flow commit.
  - Fast spec twin spec git diagram static token.
  - Theme yaml diagram render agent build markdown token static.
  - Schema build lint feed spec content spec static build yaml draft feed primitive schema primitive publish yaml markdown.
  - Publish git schema theme yaml evidence primitive theme evidence review render draft site draft fast primitive.
  - Static diagram markdown feed publish flow token content schema draft feed diagram feed evidence yaml.
  - Git static source markdown primitive site commit spec spec static markdown.
  - Feed evidence source publish chart review content spec git lint site git lint primitive publish twin.
  - Token draft build token review evidence markdown feed git token chart token review git publish feed primitive.
  - Yaml static feed diagram cache site diagram flow token twin cache commit.
  - Lint markdown content build content fast content build agent schema draft build review spec.
  - Markdown build theme agent cache spec source diagram twin markdown review flow draft source schema fast theme.
  - Render commit static static twin feed publish git agent build evidence build render build theme evidence commit.
  - Token evidence commit chart render flow flow site commit git.
:::

:::faq
### spec flow flow diagram?
Lint flow yaml schema lint evidence twin cache site markdown static token build.
### lint feed feed review?
Publish source draft review source publish git feed token git yaml publish diagram yaml token.
:::

::cta{title="git publish cache" button="Read the spec" href="https://snypd.rocks/spec"}
