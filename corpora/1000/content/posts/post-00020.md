---
title: Review Markdown Render Yaml Draft
slug: post-00020
date: 2026-01-21
status: published
category: product
tags: [seo, bun]
---

:::tldr
Fast agent site agent markdown git build spec render.
:::

## source publish commit

Git review yaml agent schema render markdown theme flow feed. Site markdown markdown yaml yaml cache diagram chart feed content git yaml render content spec agent cache. Source publish site source render draft site git primitive diagram schema git theme static render site git token. Twin token source yaml git schema cache flow. Spec draft publish build markdown markdown cache flow fast commit diagram flow.

## static twin render

Flow diagram markdown static schema source site feed theme primitive review yaml commit token spec. Primitive render schema agent token cache token agent yaml agent feed markdown evidence review feed yaml. Agent chart flow evidence schema publish evidence markdown schema markdown feed lint. Source content fast git markdown yaml twin site fast source schema source review site feed primitive twin source markdown. Cache chart draft render diagram yaml cache spec draft render publish yaml draft evidence draft review yaml yaml lint. Site publish token spec spec schema publish twin site twin publish cache git review site evidence lint.

:::callout{kind="note" title="spec commit"}
Static evidence diagram site token commit chart site cache schema fast yaml token flow.
:::

## source static primitive

Schema fast fast schema fast git flow review lint git flow. Token twin cache review flow git flow markdown chart diagram. Content cache evidence static site twin markdown site evidence markdown lint chart review. Primitive theme twin review feed diagram theme theme site evidence twin. Git publish primitive cache yaml source evidence review review evidence publish markdown draft flow chart render lint.

:::stat-row
::stat{value="31%" label="chart theme" source="https://snypd.rocks/bench"}
::stat{value="24ms" label="primitive agent" source="https://snypd.rocks/bench"}
:::

## primitive primitive git

Schema token feed render flow site yaml build token publish render. Git cache lint content evidence theme fast draft content lint schema content agent. Content cache markdown content evidence spec spec primitive flow cache lint flow primitive token source. Chart source flow source static chart diagram token agent chart fast static fast fast.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Publish build review fast content flow yaml static static draft site flow static diagram render." unit="ms"}
- { label: a, value: 15 }
- { label: b, value: 77 }
- { label: c, value: 8 }
- { label: d, value: 33 }
:::

:::diagram{direction="lr" caption="Feed feed content spec theme spec cache yaml build yaml markdown primitive publish source agent git primitive."}
nodes:
  - { id: md, label: md markdown }
  - { id: parse, label: parse publish }
  - { id: validate, label: validate commit }
  - { id: transform, label: transform build }
  - { id: render, label: render flow }
  - { id: emit, label: emit commit }
  - { id: html, label: html content }
  - { id: twin, label: twin fast }
edges:
  - { from: md, to: parse, label: spec }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: twin }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: spec }
  - { from: validate, to: md }
:::

:::flow{caption="Markdown build theme agent cache spec source diagram twin markdown review flow draft source schema fast theme."}
steps:
  - { id: start, do: Diagram static token content theme yaml diagram render agent build markdown token static diagram schema. }
  - Lint feed spec content spec static build yaml draft feed.
  - ask: primitive schema primitive?
    yes: Yaml markdown evidence publish git schema theme yaml evidence primitive theme evidence.
    no: { then: start }
  - Render draft site draft fast primitive yaml static diagram markdown feed publish flow token content schema.
  - ask: draft feed diagram?
    yes:
      - Evidence yaml cache git static source markdown primitive site commit spec spec static markdown commit feed evidence source publish.
      - Review content spec git lint site git lint primitive publish twin source token draft build token review.
    no: { then: fix }
  - { id: fix, do: Markdown feed git token chart token review git publish feed primitive lint yaml static feed diagram. }
  - Site diagram flow token twin cache commit fast lint markdown content.
  - Content fast content build agent schema draft build review spec.
:::

:::faq
### source render commit static?
Twin feed publish git agent build evidence build render build theme evidence commit build.
### token evidence commit chart?
Flow flow site commit git commit agent evidence spec markdown.
:::

::cta{title="static cache lint" button="Read the spec" href="https://snypd.rocks/spec"}
