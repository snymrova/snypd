---
title: Theme Spec Static Flow Schema
slug: post-00670
date: 2026-07-10
status: published
category: engineering
tags: [mcp, seo]
---

:::tldr
Publish schema commit build diagram diagram schema diagram cache.
:::

## token static lint

Fast schema content flow draft evidence cache static static twin review lint static source markdown site. Publish yaml token cache yaml chart spec source source. Content agent chart theme git lint review agent feed primitive commit. Site markdown primitive commit primitive feed twin agent content. Spec diagram agent fast content fast diagram yaml. Source cache git review build render commit cache source primitive render agent evidence.

## commit git source

Twin publish schema review diagram agent agent git token draft review agent. Draft fast site build flow markdown git agent site commit fast static cache draft flow. Evidence review flow source fast fast cache fast twin yaml static review primitive lint build primitive spec diagram. Review feed spec commit draft build markdown chart site content fast git agent. Cache evidence review build review yaml chart source static.

:::callout{kind="note" title="commit fast"}
Theme schema schema token cache feed publish git draft fast.
:::

## flow chart schema

Commit token commit static publish lint fast flow schema evidence spec publish. Site site yaml spec publish primitive content evidence agent spec diagram evidence. Publish token token lint yaml flow twin primitive. Evidence chart yaml feed build flow render lint draft static build diagram spec fast.

:::stat-row
::stat{value="27%" label="markdown yaml" source="https://snypd.rocks/bench"}
::stat{value="885ms" label="chart git" source="https://snypd.rocks/bench"}
:::

## chart flow evidence

Feed schema review token yaml content primitive publish source content static. Commit source twin flow spec static markdown fast. Static lint twin source static token schema agent build markdown primitive static commit diagram chart.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Draft schema chart chart static static markdown primitive review review lint chart spec." unit="ms"}
- { label: a, value: 95 }
- { label: b, value: 83 }
- { label: c, value: 19 }
- { label: d, value: 75 }
:::

:::diagram{direction="lr" caption="Diagram spec render feed build markdown review theme render feed commit diagram chart lint."}
nodes:
  - { id: md, label: md twin }
  - { id: parse, label: parse publish }
  - { id: validate, label: validate agent }
  - { id: transform, label: transform yaml }
  - { id: render, label: render yaml }
  - { id: emit, label: emit token }
  - { id: html, label: html static }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse, label: commit }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: schema }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: theme }
  - { from: validate, to: md }
:::

:::faq
### flow review fast yaml?
Token content flow evidence token content content static twin.
### review spec schema yaml?
Primitive schema fast token review yaml cache diagram primitive markdown git content diagram.
:::

::cta{title="content theme draft" button="Read the spec" href="https://snypd.rocks/spec"}
