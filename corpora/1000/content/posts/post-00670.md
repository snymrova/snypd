---
title: Feed Draft Feed Theme Evidence
slug: post-00670
date: 2026-07-10
status: published
category: design
tags: [ai, ai]
---

:::tldr
Flow static spec chart markdown cache commit feed evidence content site commit draft publish static.
:::

## source fast source

Twin static markdown yaml primitive chart commit feed token evidence content. Markdown primitive flow git content flow fast content flow draft theme chart static. Content feed flow primitive chart static token spec fast primitive spec render draft. Flow spec render flow review chart schema render git build diagram git cache build chart spec agent.

## site diagram evidence

Agent evidence commit theme source cache static token site flow commit content schema render. Site chart site yaml token site build render draft agent lint commit static. Feed content chart evidence cache git spec feed markdown markdown static theme cache cache feed spec token static review. Git build theme markdown markdown fast token commit lint render chart spec cache. Diagram draft cache static commit build yaml feed.

:::callout{kind="note" title="cache diagram"}
Markdown flow review site yaml markdown feed git primitive markdown primitive feed markdown evidence.
:::

## static theme primitive

Review spec yaml build publish draft static source yaml cache build publish commit. Render source fast source fast flow lint render yaml render draft draft build. Flow content publish review publish content feed theme primitive chart fast review review token spec yaml fast.

:::stat-row
::stat{value="51%" label="primitive source" source="https://snypd.rocks/bench"}
::stat{value="96ms" label="flow lint" source="https://snypd.rocks/bench"}
:::

## markdown diagram lint

Commit static cache flow yaml markdown review site cache lint lint token schema feed publish spec commit review. Commit chart yaml static commit flow site static. Review diagram markdown schema site diagram markdown token chart publish chart markdown fast build yaml site lint. Evidence yaml build evidence agent review spec flow source content primitive theme diagram theme lint schema primitive diagram. Evidence build yaml source lint render fast theme schema site draft review markdown token.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Diagram token chart theme token build lint agent twin review schema review." unit="ms"}
- { label: a, value: 31 }
- { label: b, value: 2 }
- { label: c, value: 52 }
- { label: d, value: 90 }
:::

:::diagram{direction="lr" caption="Git review commit primitive static source render draft primitive schema publish primitive render chart."}
nodes:
  - { id: md, label: md site }
  - { id: parse, label: parse agent }
  - { id: validate, label: validate chart }
  - { id: transform, label: transform source }
  - { id: render, label: render yaml }
  - { id: emit, label: emit feed }
  - { id: html, label: html twin }
  - { id: twin, label: twin chart }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### fast static commit chart?
Primitive draft source schema cache lint feed chart agent.
### agent static diagram flow?
Evidence commit commit schema publish schema token source render.
:::

::cta{title="diagram theme review" button="Read the spec" href="https://snypd.rocks/spec"}
