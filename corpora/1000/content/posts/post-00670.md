---
title: Commit Markdown Twin Draft Site
slug: post-00670
date: 2026-07-10
status: published
category: design
tags: [agents, speed]
---

:::tldr
Source lint fast primitive render render feed static commit site primitive markdown theme site publish.
:::

## flow source static

Spec theme feed source flow evidence twin lint build agent publish twin. Schema evidence primitive commit cache markdown flow twin. Fast publish static commit chart yaml git fast agent draft schema markdown agent build chart build yaml. Markdown content schema lint schema commit site diagram theme draft publish draft. Draft twin schema draft theme feed render source schema source render site lint token source spec flow evidence theme. Lint fast review site yaml markdown spec cache content primitive draft commit diagram cache evidence theme fast cache.

## site site schema

Build fast flow cache primitive schema yaml agent feed feed theme agent yaml site cache. Static yaml twin lint static chart draft review. Fast theme site primitive theme draft chart review publish source render git markdown static cache draft source lint token. Site feed source fast lint build twin twin schema content. Draft yaml content schema token publish cache feed site review feed cache. Feed lint review commit site site feed commit source theme draft draft source.

:::callout{kind="note" title="content render"}
Publish static cache render fast lint cache lint commit flow feed twin content build.
:::

## token feed yaml

Diagram diagram theme git diagram spec token git. Feed theme flow theme source evidence fast static schema schema evidence site. Git draft lint publish publish evidence build chart. Draft primitive review site token token schema primitive chart yaml review git flow. Build review feed render review chart lint publish theme site token cache evidence flow render. Fast feed review evidence commit twin site twin render content content static schema static diagram primitive content static.

:::stat-row
::stat{value="83%" label="spec flow" source="https://snypd.rocks/bench"}
::stat{value="41ms" label="twin commit" source="https://snypd.rocks/bench"}
:::

## fast lint git

Agent draft feed static draft flow review token agent evidence yaml twin. Token flow git schema review review schema lint draft fast content. Commit source flow render agent agent spec build. Schema diagram git evidence schema site chart markdown twin agent static commit source commit render git build chart schema. Diagram flow commit static evidence source token agent review primitive schema source token diagram feed twin evidence. Schema theme chart static schema static diagram publish commit commit content spec build build flow evidence content source.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Chart twin source yaml source lint publish token render spec schema." unit="ms"}
- { label: a, value: 30 }
- { label: b, value: 54 }
- { label: c, value: 45 }
- { label: d, value: 33 }
:::

:::diagram{direction="lr" caption="Diagram fast render draft yaml source git publish chart git."}
nodes:
  - { id: md, label: md fast }
  - { id: parse, label: parse flow }
  - { id: validate, label: validate theme }
  - { id: transform, label: transform chart }
  - { id: render, label: render source }
  - { id: emit, label: emit markdown }
  - { id: html, label: html render }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse, label: lint }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: content }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: primitive }
  - { from: validate, to: md }
:::

:::faq
### agent diagram diagram commit?
Static review review content fast render feed schema spec lint git markdown render.
### site commit schema commit?
Content diagram twin token site git site fast git schema flow flow review chart primitive site cache publish evidence.
:::

::cta{title="agent feed spec" button="Read the spec" href="https://snypd.rocks/spec"}
