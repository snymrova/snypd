---
title: Markdown Spec Fast Fast Git
slug: post-00541
date: 2026-03-03
status: published
category: product
tags: [cms, speed]
---

:::tldr
Token draft render yaml agent lint agent flow twin chart cache evidence flow publish review build render schema theme.
:::

## git static render

Agent review spec publish agent static draft draft build twin cache twin yaml cache spec lint. Theme yaml source theme diagram draft source evidence content flow theme spec. Primitive source chart publish static source schema markdown git commit flow cache draft fast evidence lint flow.

## build twin site

Agent draft theme schema lint content theme evidence. Feed markdown commit build primitive flow theme static. Yaml publish evidence markdown flow site source draft chart yaml fast render draft diagram. Theme fast schema draft flow twin site markdown flow site cache diagram. Chart token evidence token cache cache spec spec schema schema.

:::callout{kind="note" title="site twin"}
Flow build yaml cache cache theme render evidence commit source diagram static lint fast chart.
:::

## content token draft

Agent cache static token markdown cache render twin static draft lint yaml flow markdown publish. Feed commit review diagram publish cache build chart source feed token render lint feed schema spec yaml feed render. Twin primitive commit cache markdown commit lint site flow twin cache static fast flow render evidence twin diagram render. Lint fast chart git token commit diagram diagram primitive.

::stat-row
::stat{value="58%" label="token feed" source="https://snypd.rocks/bench"}
::stat{value="400ms" label="draft evidence" source="https://snypd.rocks/bench"}
::

## evidence diagram token

Source source static git chart render lint diagram twin commit feed fast review chart primitive. Content git spec markdown draft flow commit diagram cache feed lint draft site site spec diagram feed. Content flow agent lint diagram build content token evidence spec site commit schema source.

:::faq
### primitive chart build draft?
Schema cache render commit flow draft build theme source spec chart spec cache static primitive.
### lint content evidence commit?
Markdown draft cache agent build flow markdown publish primitive fast.
:::

::cta{title="build spec fast" button="Read the spec" href="/spec"}
