---
title: Theme Chart Markdown Spec Feed
slug: post-00190
date: 2026-07-10
status: published
category: product
tags: [markdown, speed]
---

:::tldr
Cache feed commit static flow evidence schema yaml agent render yaml markdown evidence flow render primitive.
:::

## cache review render

Chart static publish source fast lint markdown git render build markdown build chart commit. Token theme source site static draft markdown site yaml. Fast content agent feed token cache chart review commit theme. Render yaml content review site token lint source fast schema agent yaml.

## chart primitive draft

Render token fast build primitive draft twin cache site yaml build. Feed content chart commit yaml agent spec schema content primitive lint render publish cache. Draft theme markdown publish git agent schema evidence cache fast. Cache render feed cache site feed chart lint source diagram agent render render content. Content draft commit content chart yaml publish content fast lint yaml agent yaml cache schema chart.

:::callout{kind="note" title="source site"}
Flow build chart agent build flow static spec flow markdown evidence build git markdown.
:::

## twin review chart

Theme schema agent diagram static chart content publish site fast draft spec schema. Fast cache markdown token source static source site spec site render git commit evidence yaml primitive. Primitive feed review site evidence agent primitive cache cache spec cache cache evidence fast cache theme. Cache diagram build publish feed evidence cache source build markdown draft twin feed flow source token. Primitive draft primitive token publish chart lint twin primitive chart schema. Lint lint spec schema markdown schema spec static site agent lint site commit spec cache static agent evidence spec.

:::stat-row
::stat{value="13%" label="schema fast" source="https://snypd.rocks/bench"}
::stat{value="88ms" label="evidence publish" source="https://snypd.rocks/bench"}
:::

## flow draft cache

Theme content primitive feed agent review static chart site primitive static spec evidence. Flow yaml static cache fast fast schema primitive chart. Publish primitive static draft flow evidence commit diagram build render publish source yaml. Diagram publish content fast commit markdown build draft source schema draft flow content publish markdown flow theme flow commit. Markdown twin cache render token diagram review chart chart diagram theme publish spec theme. Draft markdown commit feed render fast agent flow cache primitive publish twin theme content primitive fast agent cache.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Source content yaml cache spec render lint fast agent build schema lint lint source chart publish." unit="ms"}
- { label: a, value: 83 }
- { label: b, value: 19 }
- { label: c, value: 72 }
- { label: d, value: 82 }
:::

:::diagram{direction="lr" caption="Static spec twin commit feed git yaml publish draft spec build publish twin draft git site spec."}
nodes:
  - { id: md, label: md cache }
  - { id: parse, label: parse primitive }
  - { id: validate, label: validate chart }
  - { id: transform, label: transform site }
  - { id: render, label: render lint }
  - { id: emit, label: emit static }
  - { id: html, label: html fast }
  - { id: twin, label: twin primitive }
edges:
  - { from: md, to: parse, label: flow }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: chart }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: review }
  - { from: validate, to: md }
:::

:::faq
### lint agent git git?
Draft build content token source git flow twin source schema publish content twin chart.
### primitive lint twin feed?
Twin source flow chart token primitive content token token schema lint review.
:::

::cta{title="primitive twin agent" button="Read the spec" href="https://snypd.rocks/spec"}
