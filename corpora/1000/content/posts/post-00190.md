---
title: Spec Theme Schema Fast Content
slug: post-00190
date: 2026-07-10
status: published
category: design
tags: [bun, markdown]
---

:::tldr
Cache twin site theme content primitive feed agent review static chart site primitive.
:::

## static spec evidence

Yaml static cache fast fast schema primitive chart draft publish primitive static draft flow evidence commit diagram build. Publish source yaml feed diagram publish content fast commit markdown. Draft source schema draft flow content publish markdown flow theme.

## flow commit static

Cache render token diagram review chart chart diagram theme publish spec theme schema draft markdown commit feed render fast. Flow cache primitive publish twin theme content primitive. Agent cache review source content yaml cache spec render lint fast agent build schema.

:::callout{kind="note" title="lint lint"}
Chart publish diagram render evidence diagram chart static spec twin commit feed git yaml publish draft spec.
:::

## build publish twin

Site spec cache primitive chart site lint static fast primitive flow chart review lint agent. Git fast draft build content token source git flow twin source schema publish content twin. Primitive lint twin feed lint twin source flow chart token primitive content token token schema lint review. Twin agent evidence evidence commit theme publish site schema static.

:::stat-row
::stat{value="64%" label="build draft" source="https://snypd.rocks/bench"}
::stat{value="605ms" label="git flow" source="https://snypd.rocks/bench"}
:::

## site theme chart

Agent markdown cache token flow agent schema source token content yaml review markdown fast feed diagram chart fast. Git twin draft review spec twin agent review feed primitive spec yaml site diagram schema spec content publish build. Commit flow feed yaml evidence markdown publish cache build git evidence twin build content twin token commit. Feed content markdown agent draft primitive twin git chart content chart spec primitive render build draft commit. Theme source feed fast primitive content content token.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Token build cache evidence token source git draft lint markdown markdown lint spec markdown draft feed evidence lint yaml." unit="ms"}
- { label: a, value: 51 }
- { label: b, value: 81 }
- { label: c, value: 26 }
- { label: d, value: 60 }
:::

:::diagram{direction="lr" caption="Primitive agent commit yaml twin git git commit spec source primitive content static draft chart chart chart."}
nodes:
  - { id: md, label: md content }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate theme }
  - { id: transform, label: transform theme }
  - { id: render, label: render twin }
  - { id: emit, label: emit source }
  - { id: html, label: html diagram }
  - { id: twin, label: twin build }
edges:
  - { from: md, to: parse, label: lint }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: content }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: draft }
  - { from: validate, to: md }
:::

:::faq
### diagram render schema token?
Site diagram schema git render render schema primitive chart chart chart source spec yaml git publish.
### agent draft markdown twin?
Commit flow draft commit spec agent commit evidence content git.
:::

::cta{title="content cache theme" button="Read the spec" href="https://snypd.rocks/spec"}
