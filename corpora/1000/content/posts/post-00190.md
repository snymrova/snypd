---
title: Feed Git Yaml Publish Draft
slug: post-00190
date: 2026-07-10
status: published
category: product
tags: [agents, bun]
---

:::tldr
Draft git site spec cache primitive chart site lint static fast primitive flow chart review lint agent git git.
:::

## fast draft build

Source git flow twin source schema publish content twin chart primitive. Twin feed lint twin source flow chart token primitive content token token. Lint review primitive twin agent evidence evidence commit theme publish site schema static commit build draft commit git flow.

## site theme chart

Agent markdown cache token flow agent schema source token content yaml review markdown fast feed diagram chart fast. Git twin draft review spec twin agent review feed primitive spec yaml site diagram schema spec content publish build. Commit flow feed yaml evidence markdown publish cache build git evidence twin build content twin token commit. Feed content markdown agent draft primitive twin git chart content chart spec primitive render build draft commit. Theme source feed fast primitive content content token.

:::callout{kind="note" title="feed token"}
Cache evidence token source git draft lint markdown markdown lint.
:::

## spec markdown draft

Lint yaml static chart cache yaml chart primitive agent commit yaml twin git git commit spec. Primitive content static draft chart chart chart content yaml theme theme twin source diagram build lint content. Diagram render schema token review site diagram schema git render render schema primitive. Chart chart source spec yaml git publish agent draft markdown twin build commit flow draft commit spec. Commit evidence content git content cache theme diagram. Evidence theme flow evidence build review flow lint source.

:::stat-row
::stat{value="57%" label="publish flow" source="https://snypd.rocks/bench"}
::stat{value="141ms" label="build schema" source="https://snypd.rocks/bench"}
:::

## evidence git render

Diagram theme site spec chart agent render primitive agent token review site. Publish build markdown yaml feed yaml static spec spec build diagram theme theme git. Git theme review spec review token publish spec cache lint twin evidence markdown review flow render spec. Markdown primitive lint render twin token primitive build. Twin content source agent fast token diagram evidence draft schema agent.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Evidence render render static publish evidence draft spec evidence evidence feed markdown review fast token yaml token." unit="ms"}
- { label: a, value: 57 }
- { label: b, value: 42 }
- { label: c, value: 56 }
- { label: d, value: 29 }
:::

:::diagram{direction="lr" caption="Flow fast commit feed yaml render static git build."}
nodes:
  - { id: md, label: md lint }
  - { id: parse, label: parse agent }
  - { id: validate, label: validate diagram }
  - { id: transform, label: transform site }
  - { id: render, label: render build }
  - { id: emit, label: emit fast }
  - { id: html, label: html token }
  - { id: twin, label: twin markdown }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### diagram render feed render?
Agent review publish diagram diagram schema render token primitive review git token cache git.
### draft publish evidence render?
Markdown lint token agent build publish token render publish twin build build primitive flow draft source.
:::

::cta{title="flow content feed" button="Read the spec" href="https://snypd.rocks/spec"}
