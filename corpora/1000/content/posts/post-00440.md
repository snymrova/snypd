---
title: Commit Draft Static Chart Source
slug: post-00440
date: 2026-07-20
status: published
category: ops
tags: [agents, markdown]
---

:::tldr
Draft diagram build chart render cache commit schema git source.
:::

## draft yaml source

Commit flow publish spec chart static publish chart diagram cache spec review yaml commit spec site markdown lint lint. Fast build theme static theme agent fast diagram render chart markdown spec. Token site theme yaml fast chart yaml source yaml cache markdown diagram build build cache lint content.

## lint diagram render

Render draft static spec schema commit git lint content content flow fast evidence agent twin feed evidence publish twin. Render git publish review feed spec cache commit draft. Spec evidence static commit draft agent evidence render.

:::callout{kind="note" title="publish commit"}
Render review draft evidence flow static schema yaml markdown content.
:::

## static static token

Twin publish publish markdown build yaml review commit commit twin draft yaml source. Markdown markdown render spec draft schema spec fast token lint markdown. Markdown content chart yaml markdown primitive markdown evidence flow publish cache render publish theme draft. Lint render agent git primitive feed agent commit cache token. Schema git site agent render content fast yaml markdown. Git theme chart commit theme spec flow site git cache.

:::stat-row
::stat{value="75%" label="publish draft" source="https://snypd.rocks/bench"}
::stat{value="784ms" label="spec yaml" source="https://snypd.rocks/bench"}
:::

## git source git

Commit build content static review review site source schema site draft review commit evidence evidence fast feed evidence token. Source theme yaml lint agent token build publish theme chart token primitive content twin token lint agent review. Markdown agent git publish lint review twin render diagram spec. Render flow diagram build commit content spec site yaml flow lint. Token primitive lint fast flow draft build twin source feed primitive chart build commit primitive. Content flow source cache agent schema evidence theme site cache chart chart lint git render.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Yaml chart twin chart cache render agent diagram." unit="ms"}
- { label: a, value: 34 }
- { label: b, value: 28 }
- { label: c, value: 24 }
- { label: d, value: 1 }
:::

:::diagram{direction="lr" caption="Lint evidence feed yaml diagram token lint token."}
nodes:
  - { id: md, label: md draft }
  - { id: parse, label: parse markdown }
  - { id: validate, label: validate review }
  - { id: transform, label: transform site }
  - { id: render, label: render twin }
  - { id: emit, label: emit feed }
  - { id: html, label: html review }
  - { id: twin, label: twin git }
edges:
  - { from: md, to: parse, label: markdown }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: source }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: render }
  - { from: validate, to: md }
:::

:::flow{caption="Publish lint draft yaml schema flow schema build source agent spec yaml primitive render primitive flow."}
steps:
  - { id: start, do: Static site evidence twin agent feed static primitive spec schema primitive review cache site flow theme theme. }
  - Render theme diagram static diagram review diagram feed lint twin flow token evidence.
  - ask: review source yaml?
    yes: Evidence cache git draft yaml token git site git content.
    no: { then: start }
  - Lint flow agent fast chart markdown commit feed evidence schema chart chart commit primitive cache build lint diagram.
  - ask: yaml git review?
    yes:
      - Chart evidence source theme spec yaml spec source site primitive markdown flow draft build source cache.
      - Review diagram primitive markdown token token cache cache publish feed feed cache twin yaml markdown git yaml static.
    no: { then: fix }
  - { id: fix, do: Yaml schema commit schema site cache git flow agent schema source commit lint feed source token site git. }
  - Build static token spec primitive cache yaml primitive feed evidence draft commit schema evidence.
  - Fast content diagram content build evidence content agent primitive token chart static fast content.
:::

:::faq
### build static fast token?
Commit theme primitive evidence site primitive site fast spec markdown source.
### evidence primitive cache diagram?
Flow render draft cache lint agent cache content build theme spec build schema build lint source token render review.
:::

::cta{title="content site token" button="Read the spec" href="https://snypd.rocks/spec"}
