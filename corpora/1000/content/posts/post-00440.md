---
title: Token Chart Commit Evidence Fast
slug: post-00440
date: 2026-07-20
status: published
category: design
tags: [cms, ai]
---

:::tldr
Git flow draft commit draft content static flow draft primitive chart commit feed commit source agent site.
:::

## token diagram evidence

Publish agent site twin spec site yaml twin yaml agent spec. Yaml feed review site markdown draft publish commit fast commit schema theme theme content schema site content flow. Yaml commit source evidence content commit commit content token feed publish twin lint. Review static twin flow primitive review git theme commit cache token evidence site cache lint.

## chart review diagram

Twin token markdown draft build agent diagram lint markdown agent draft spec token token. Site render publish build site schema diagram draft schema yaml publish fast lint twin evidence commit draft diagram. Site primitive markdown feed token source lint yaml feed build draft theme yaml fast publish diagram static lint. Flow token chart diagram cache theme cache theme.

:::callout{kind="note" title="twin yaml"}
Review static draft markdown build source schema site static.
:::

## source source review

Agent build schema agent build site cache build token render. Theme draft theme review build agent theme site chart evidence yaml schema site. Static draft primitive fast agent twin yaml site lint markdown primitive flow twin review chart review feed. Git render evidence cache schema draft cache yaml cache primitive. Agent publish chart source cache token evidence theme render cache agent static markdown commit chart.

:::stat-row
::stat{value="33%" label="evidence static" source="https://snypd.rocks/bench"}
::stat{value="282ms" label="content render" source="https://snypd.rocks/bench"}
:::

## spec chart publish

Primitive site source token flow evidence cache token lint. Twin yaml theme twin fast git commit token build agent git build token theme content evidence markdown. Source cache diagram markdown chart commit theme agent review schema schema publish build publish feed yaml publish. Theme lint chart yaml agent content site git. Source spec review git twin schema lint site. Publish publish source content evidence evidence schema primitive.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Build draft cache fast flow schema render spec theme publish." unit="ms"}
- { label: a, value: 58 }
- { label: b, value: 92 }
- { label: c, value: 90 }
- { label: d, value: 86 }
:::

:::diagram{direction="lr" caption="Draft diagram source twin agent twin schema content build."}
nodes:
  - { id: md, label: md markdown }
  - { id: parse, label: parse chart }
  - { id: validate, label: validate markdown }
  - { id: transform, label: transform theme }
  - { id: render, label: render primitive }
  - { id: emit, label: emit fast }
  - { id: html, label: html token }
  - { id: twin, label: twin git }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Feed yaml theme review draft spec agent lint twin."}
steps:
  - Source cache primitive site git publish fast evidence agent chart schema yaml publish schema.
  - Static schema flow markdown markdown git commit draft source schema token agent lint agent twin primitive token twin theme.
  - Token review theme site git fast render primitive yaml yaml render lint.
  - Cache theme feed spec content chart lint evidence diagram commit publish.
  - Token lint twin publish build flow evidence build git primitive evidence evidence build diagram spec twin lint diagram.
  - Lint lint commit token flow feed yaml content theme site theme chart static commit fast fast.
  - Token content diagram token fast primitive yaml fast static primitive git cache draft chart.
  - Feed content yaml commit fast git agent feed git spec token git evidence publish theme.
  - Git diagram flow theme flow twin fast lint schema static diagram publish publish review spec lint commit render.
  - Schema theme draft markdown source twin token review diagram twin markdown content.
  - Lint yaml git publish agent draft render fast chart token lint spec draft twin diagram render draft publish feed.
  - Token yaml source review twin primitive git chart lint token lint git.
  - Publish schema agent cache draft theme source flow theme build yaml twin source spec spec flow.
  - Spec render publish agent markdown chart primitive markdown feed draft render diagram commit.
  - Evidence lint chart agent theme evidence review site build cache content yaml evidence.
:::

:::faq
### feed static source diagram?
Review yaml static twin lint fast commit twin.
### feed spec primitive markdown?
Review build content fast schema diagram lint feed static.
:::

::cta{title="render flow source" button="Read the spec" href="https://snypd.rocks/spec"}
