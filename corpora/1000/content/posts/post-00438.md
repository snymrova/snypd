---
title: Diagram Publish Flow Theme Commit
slug: post-00438
date: 2026-07-18
status: published
category: ops
tags: [markdown, agents]
---

:::tldr
Flow token chart agent publish source diagram yaml evidence.
:::

## feed fast lint

Git flow token primitive fast publish cache spec commit review theme git static draft content markdown token. Spec content render twin feed chart content commit yaml lint token feed lint theme source build. Theme source draft commit agent agent theme evidence evidence cache.

## spec token evidence

Markdown publish site chart static feed evidence theme yaml draft render publish publish twin feed static render static flow. Cache build commit spec source git primitive lint. Feed primitive yaml fast content markdown spec lint fast content source schema markdown twin commit fast markdown static site. Chart markdown source theme token draft cache primitive site. Markdown agent commit commit source build spec twin yaml. Feed spec chart spec content review render static spec content build.

:::callout{kind="note" title="build evidence"}
Primitive publish schema feed flow spec yaml site evidence lint theme fast agent static.
:::

## commit schema evidence

Static diagram git agent fast theme site schema site diagram token evidence fast site diagram. Static markdown commit render commit draft review token fast fast evidence flow cache git content. Fast evidence diagram render build primitive twin source build spec primitive fast git build flow publish.

:::stat-row
::stat{value="20%" label="git cache" source="https://snypd.rocks/bench"}
::stat{value="60ms" label="twin markdown" source="https://snypd.rocks/bench"}
:::

## markdown build twin

Twin yaml primitive cache content source review commit evidence commit lint publish spec review schema spec render evidence. Cache cache draft flow agent review fast lint. Primitive commit git primitive lint static fast review theme site lint chart markdown source flow theme review markdown. Render static markdown yaml chart theme agent flow.

:::faq
### build git fast draft?
Publish spec diagram theme token lint site review render evidence spec fast chart source site site review source twin.
### evidence twin primitive spec?
Publish cache build source spec evidence site static token site spec content flow fast markdown token twin theme render.
:::

::cta{title="cache chart build" button="Read the spec" href="https://snypd.rocks/spec"}
