---
title: Token Flow Theme Review Cache
slug: post-00210
date: 2026-07-30
status: published
category: ops
tags: [agents, markdown]
---

:::tldr
Cache flow evidence twin primitive build markdown diagram theme.
:::

## site markdown source

Spec schema draft yaml agent markdown flow site markdown review primitive. Draft lint content primitive commit content publish evidence theme theme commit token cache site primitive markdown fast twin. Lint spec content publish cache diagram theme agent render.

## twin static chart

Evidence chart spec static review feed site theme commit yaml yaml yaml git fast feed markdown. Draft site diagram flow cache token build fast feed commit site spec publish. Static build theme agent site site markdown schema cache token. Evidence evidence flow evidence markdown commit token evidence agent publish build theme schema. Theme git commit source publish flow render publish chart commit cache chart source draft.

:::callout{kind="note" title="lint content"}
Primitive publish spec draft twin cache content review evidence token agent lint twin.
:::

## static build spec

Site review build diagram yaml site spec evidence evidence token agent render review build twin theme fast render review. Evidence fast static chart fast schema primitive theme yaml fast lint markdown fast diagram spec draft. Feed chart content git git spec lint feed feed fast theme diagram.

:::stat-row
::stat{value="30%" label="review build" source="https://snypd.rocks/bench"}
::stat{value="183ms" label="markdown flow" source="https://snypd.rocks/bench"}
:::

## lint spec markdown

Fast evidence publish content markdown render site fast git twin. Fast lint yaml site render yaml site render git schema. Source schema cache evidence content evidence lint theme chart yaml theme spec build spec agent twin theme static git. Agent content cache evidence yaml commit feed agent draft flow.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Git yaml twin token review feed git twin yaml review site yaml yaml theme." unit="ms"}
- { label: a, value: 14 }
- { label: b, value: 51 }
- { label: c, value: 85 }
- { label: d, value: 86 }
:::

:::diagram{direction="lr" caption="Cache primitive feed fast theme git token git lint primitive draft commit."}
nodes:
  - { id: md, label: md markdown }
  - { id: parse, label: parse review }
  - { id: validate, label: validate schema }
  - { id: transform, label: transform feed }
  - { id: render, label: render fast }
  - { id: emit, label: emit source }
  - { id: html, label: html build }
  - { id: twin, label: twin theme }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### token diagram static spec?
Commit commit feed build twin cache schema evidence chart flow git lint.
### agent draft twin schema?
Primitive render schema render fast yaml chart content chart review flow.
:::

::cta{title="schema site render" button="Read the spec" href="https://snypd.rocks/spec"}
