---
title: Commit Twin Chart Feed Spec
slug: post-00210
date: 2026-07-30
status: published
category: ops
tags: [bun, cms]
---

:::tldr
Schema source flow theme git review fast static agent.
:::

## build publish commit

Draft chart agent lint token twin spec site cache token fast lint. Yaml flow git chart publish feed agent feed. Review markdown build spec review spec commit token primitive theme theme publish diagram flow fast schema draft draft diagram. Cache git token git token fast cache token yaml fast diagram.

## review yaml chart

Git site markdown primitive commit source static spec chart content content schema agent content twin spec markdown. Draft site source git twin build publish site content twin yaml. Flow primitive schema draft git yaml lint render lint theme site review. Feed theme fast token yaml yaml site review agent token review content spec twin spec chart chart feed. Publish spec fast site markdown token token theme render flow.

:::callout{kind="note" title="render flow"}
Feed fast flow spec fast static primitive content evidence static evidence diagram.
:::

## git fast primitive

Flow commit primitive lint feed theme render chart spec markdown content content diagram primitive diagram static fast agent. Diagram primitive render content agent feed schema fast twin spec publish twin review static. Twin diagram build diagram twin theme theme yaml yaml review publish commit agent source diagram git. Render flow fast markdown publish evidence twin render publish fast agent twin commit twin. Flow primitive source schema evidence flow review publish site render lint diagram publish chart markdown primitive static git diagram.

:::stat-row
::stat{value="23%" label="render render" source="https://snypd.rocks/bench"}
::stat{value="841ms" label="theme draft" source="https://snypd.rocks/bench"}
:::

## markdown site content

Cache static twin cache spec fast agent render spec cache site static evidence. Content static publish chart schema review yaml content twin cache agent agent content static chart build git cache static. Publish git cache primitive cache commit draft theme yaml flow.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Diagram build theme primitive render source source draft draft cache cache lint schema cache token schema publish evidence publish." unit="ms"}
- { label: a, value: 41 }
- { label: b, value: 89 }
- { label: c, value: 93 }
- { label: d, value: 11 }
:::

:::diagram{direction="lr" caption="Token theme draft spec flow commit git publish git twin git token diagram spec theme twin fast agent chart."}
nodes:
  - { id: md, label: md site }
  - { id: parse, label: parse review }
  - { id: validate, label: validate commit }
  - { id: transform, label: transform chart }
  - { id: render, label: render commit }
  - { id: emit, label: emit git }
  - { id: html, label: html flow }
  - { id: twin, label: twin theme }
edges:
  - { from: md, to: parse, label: commit }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: source }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: cache }
  - { from: validate, to: md }
:::

:::faq
### content chart lint spec?
Cache schema commit token yaml spec flow review static commit site lint fast content agent chart schema publish.
### agent commit content fast?
Twin git publish spec spec commit token diagram schema chart draft git spec publish content commit review.
:::

::cta{title="fast theme token" button="Read the spec" href="https://snypd.rocks/spec"}
