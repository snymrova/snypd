---
title: Flow Cache Evidence Evidence Diagram
slug: post-00564
date: 2026-03-26
status: published
category: ops
tags: [bun, cms]
---

:::tldr
Source theme evidence yaml agent fast primitive diagram git markdown feed schema diagram feed fast source fast.
:::

## evidence evidence evidence

Agent token build draft draft theme draft fast fast render draft static lint static evidence feed token. Theme primitive feed review twin render draft cache lint. Git evidence twin diagram cache lint twin yaml draft agent render content. Content draft build theme lint yaml render static content source git diagram. Spec token review diagram flow feed agent evidence token twin yaml.

## fast schema twin

Site token content markdown source draft static static build. Static token flow primitive git source schema render source agent theme lint evidence source primitive lint lint publish. Site spec feed source primitive diagram agent draft twin content feed publish review build content agent spec build feed.

:::callout{kind="note" title="yaml feed"}
Render git twin draft evidence render draft diagram lint evidence yaml.
:::

## agent render review

Static commit theme cache schema token chart token. Git diagram agent twin static site content draft chart static spec flow cache diagram. Review review theme spec diagram publish content fast diagram twin commit evidence yaml agent cache render. Content flow publish yaml twin render review review theme lint schema. Yaml chart build git schema twin agent build site git publish schema flow agent publish theme. Yaml agent primitive cache primitive build build evidence draft fast markdown review yaml cache git site source.

:::stat-row
::stat{value="92%" label="build lint" source="https://snypd.rocks/bench"}
::stat{value="807ms" label="git commit" source="https://snypd.rocks/bench"}
:::

## agent chart draft

Build schema feed spec primitive yaml primitive markdown diagram static twin schema markdown static review spec. Site site commit evidence flow lint agent cache review token. Draft git commit lint diagram token twin commit theme build static theme lint lint publish fast chart feed. Twin feed cache fast site render commit render cache build theme markdown diagram schema. Publish build static flow review static lint agent source spec. Yaml diagram cache token theme theme markdown fast lint flow publish draft feed fast build draft render.

:::faq
### twin publish schema review?
Publish build fast feed spec feed source draft.
### commit draft publish twin?
Commit diagram diagram draft source render token diagram draft publish static twin theme feed fast commit commit yaml draft.
:::

::cta{title="publish chart static" button="Read the spec" href="https://snypd.rocks/spec"}
