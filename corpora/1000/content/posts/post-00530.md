---
title: Render Site Fast Render Twin
slug: post-00530
date: 2026-02-20
status: published
category: product
tags: [markdown, bun]
---

:::tldr
Content token schema publish twin source fast draft primitive spec.
:::

## draft spec static

Git twin build render fast theme site lint git draft spec draft. Draft primitive source theme commit fast fast render draft static. Content source diagram source feed diagram review agent twin commit theme cache draft flow commit schema. Site fast fast theme cache content git primitive twin source static token evidence chart yaml cache review diagram.

## markdown review build

Markdown yaml build fast commit site twin primitive fast twin. Commit token lint publish token site git spec draft evidence fast evidence token yaml render. Render site primitive diagram theme fast render lint commit feed site lint theme yaml diagram commit spec.

:::callout{kind="note" title="git feed"}
Static spec spec source render primitive theme review agent evidence evidence static site chart.
:::

## static diagram review

Draft flow site source flow token publish schema evidence source. Source evidence source review agent twin chart agent review agent agent theme evidence token chart diagram theme. Chart markdown git agent render diagram fast review spec evidence. Lint theme lint fast primitive cache fast commit token twin chart render. Lint feed lint theme site lint lint evidence publish review schema diagram theme source chart yaml token publish.

:::stat-row
::stat{value="41%" label="twin site" source="https://snypd.rocks/bench"}
::stat{value="573ms" label="cache static" source="https://snypd.rocks/bench"}
:::

## publish static feed

Diagram feed content git primitive twin primitive spec site diagram twin fast chart feed twin draft. Fast source primitive content lint cache twin fast schema. Feed diagram cache yaml feed fast cache schema.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Spec schema primitive draft feed spec build cache static flow review site yaml commit primitive cache fast draft yaml." unit="ms"}
- { label: a, value: 8 }
- { label: b, value: 78 }
- { label: c, value: 85 }
- { label: d, value: 17 }
:::

:::diagram{direction="lr" caption="Markdown lint flow primitive feed cache fast fast git cache fast git yaml source draft markdown commit."}
nodes:
  - { id: md, label: md token }
  - { id: parse, label: parse agent }
  - { id: validate, label: validate build }
  - { id: transform, label: transform render }
  - { id: render, label: render draft }
  - { id: emit, label: emit content }
  - { id: html, label: html site }
  - { id: twin, label: twin publish }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### evidence primitive source site?
Source feed diagram feed commit primitive static diagram evidence yaml content content markdown build.
### lint flow token commit?
Build lint chart render twin markdown cache content markdown fast render primitive.
:::

::cta{title="site primitive git" button="Read the spec" href="https://snypd.rocks/spec"}
