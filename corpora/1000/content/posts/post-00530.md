---
title: Review Theme Static Theme Chart
slug: post-00530
date: 2026-02-20
status: published
category: design
tags: [mcp, cms]
---

:::tldr
Lint fast primitive primitive site chart flow render content primitive build content cache render token.
:::

## theme git diagram

Spec draft yaml schema build twin token spec site fast markdown cache publish chart render evidence. Feed agent token cache flow source draft build render fast primitive content build flow schema. Publish lint render token flow theme markdown schema chart build content.

## spec site diagram

Flow spec static draft theme schema draft evidence chart lint chart. Chart git markdown theme theme theme render static twin site diagram token lint build content build. Markdown spec theme review chart twin draft static flow commit build diagram flow commit markdown twin review site publish. Diagram schema cache agent primitive markdown review source evidence.

:::callout{kind="note" title="schema yaml"}
Draft render publish cache git primitive twin publish theme theme draft static markdown.
:::

## evidence lint agent

Agent draft review feed review yaml token content theme yaml source token schema site draft. Render diagram cache chart review markdown theme commit draft cache flow fast. Site theme publish fast flow git static build markdown evidence content.

:::stat-row
::stat{value="49%" label="flow token" source="https://snypd.rocks/bench"}
::stat{value="155ms" label="content primitive" source="https://snypd.rocks/bench"}
:::

## feed build content

Evidence diagram markdown cache agent git lint schema. Feed chart chart content site render static twin publish content build lint theme cache flow schema theme draft. Lint render theme content fast agent draft evidence primitive build flow content git primitive diagram commit lint agent.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Theme schema schema site spec theme feed site flow diagram schema feed source." unit="ms"}
- { label: a, value: 55 }
- { label: b, value: 12 }
- { label: c, value: 43 }
- { label: d, value: 80 }
:::

:::diagram{direction="lr" caption="Twin twin feed flow source feed chart yaml."}
nodes:
  - { id: md, label: md diagram }
  - { id: parse, label: parse build }
  - { id: validate, label: validate static }
  - { id: transform, label: transform evidence }
  - { id: render, label: render feed }
  - { id: emit, label: emit schema }
  - { id: html, label: html primitive }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse, label: schema }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: source }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: flow }
  - { from: validate, to: md }
:::

:::faq
### evidence site evidence static?
Token review spec commit schema content fast review lint content evidence draft.
### build chart commit markdown?
Token theme yaml schema content source review flow lint diagram review static.
:::

::cta{title="site site schema" button="Read the spec" href="https://snypd.rocks/spec"}
