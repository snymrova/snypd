---
title: Commit Static Commit Render Fast
slug: post-00530
date: 2026-02-20
status: published
category: design
tags: [seo, cms]
---

:::tldr
Theme cache static spec twin review feed cache spec review site review yaml build diagram.
:::

## lint content source

Cache diagram token lint static chart review spec review. Lint source git publish markdown publish fast render agent spec static feed diagram draft build. Lint draft lint yaml draft theme yaml draft token commit diagram cache chart evidence site evidence git cache. Site yaml draft render site fast render twin publish feed. Render content token schema publish twin source fast draft primitive spec draft.

## spec static token

Twin build render fast theme site lint git draft spec draft build draft primitive source. Commit fast fast render draft static review content source. Source feed diagram review agent twin commit theme cache draft flow commit schema diagram site fast fast. Cache content git primitive twin source static token evidence.

:::callout{kind="note" title="chart yaml"}
Review diagram markdown review build agent primitive markdown yaml build fast.
:::

## commit site twin

Twin git commit token lint publish token site git spec draft evidence fast evidence. Yaml render chart render site primitive diagram theme fast render lint. Feed site lint theme yaml diagram commit spec git feed static static spec spec source render.

:::stat-row
::stat{value="17%" label="theme review" source="https://snypd.rocks/bench"}
::stat{value="31ms" label="evidence evidence" source="https://snypd.rocks/bench"}
:::

## static site chart

Review static render draft flow site source flow token publish schema evidence source source source evidence source review. Twin chart agent review agent agent theme evidence. Chart diagram theme render chart markdown git agent render diagram fast. Spec evidence spec lint theme lint fast primitive cache fast commit token twin chart render schema. Feed lint theme site lint lint evidence publish review schema diagram theme.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Chart yaml token publish publish twin site git cache static publish static feed content evidence diagram feed." unit="ms"}
- { label: a, value: 8 }
- { label: b, value: 61 }
- { label: c, value: 15 }
- { label: d, value: 97 }
:::

:::diagram{direction="lr" caption="Spec site diagram twin fast chart feed twin draft theme."}
nodes:
  - { id: md, label: md fast }
  - { id: parse, label: parse source }
  - { id: validate, label: validate primitive }
  - { id: transform, label: transform content }
  - { id: render, label: render lint }
  - { id: emit, label: emit cache }
  - { id: html, label: html twin }
  - { id: twin, label: twin fast }
edges:
  - { from: md, to: parse, label: schema }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: agent }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: feed }
  - { from: validate, to: md }
:::

:::faq
### diagram cache yaml feed?
Cache schema feed spec schema primitive draft feed spec build cache static flow review.
### site yaml commit primitive?
Fast draft yaml content source diagram primitive source markdown lint flow.
:::

::cta{title="primitive feed cache" button="Read the spec" href="https://snypd.rocks/spec"}
