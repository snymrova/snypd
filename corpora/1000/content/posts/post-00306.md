---
title: Markdown Theme Flow Diagram Build
slug: post-00306
date: 2026-03-08
status: published
category: product
tags: [bun, cms]
---

:::tldr
Schema review primitive source lint flow draft render primitive schema flow git.
:::

## build git lint

Token content agent theme site cache flow git twin markdown commit twin feed publish review. Source spec schema agent source cache token draft content yaml theme source token token fast content. Token lint content source render review schema build agent primitive publish site source schema fast. Render token build fast draft source fast build site static theme draft.

## flow diagram schema

Lint primitive primitive render content markdown chart site yaml publish feed render evidence diagram yaml token chart primitive flow. Primitive render fast publish cache static schema diagram review agent git build token. Token feed review draft lint agent feed evidence flow git static yaml diagram render agent. Lint build chart spec fast render twin theme diagram site schema. Schema diagram flow diagram publish commit git draft.

:::callout{kind="note" title="lint commit"}
Schema primitive static site fast git build primitive source render flow markdown git git yaml primitive.
:::

## markdown content markdown

Diagram chart evidence source agent schema commit chart lint diagram site draft feed flow. Token primitive cache source site commit flow static source content cache cache primitive content lint. Token publish source chart static render diagram cache twin yaml site. Diagram commit evidence build fast cache twin schema. Git commit content git static publish twin evidence flow agent build content schema schema. Git fast git fast render twin agent git feed schema.

::stat-row
::stat{value="68%" label="feed site" source="https://snypd.rocks/bench"}
::stat{value="92ms" label="evidence agent" source="https://snypd.rocks/bench"}
::

## render flow review

Agent lint twin chart publish twin git source static diagram source diagram. Content review primitive yaml commit flow render theme static token yaml render site diagram. Agent schema flow lint yaml source token git git render commit draft render commit twin commit.

:::faq
### theme commit draft schema?
Build feed agent twin theme cache draft review fast content draft flow fast lint primitive token.
### yaml static site agent?
Theme yaml static cache source feed site cache build token twin feed.
:::

::cta{title="evidence primitive theme" button="Read the spec" href="/spec"}
