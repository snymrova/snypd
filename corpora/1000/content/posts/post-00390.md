---
title: Build Commit Theme Build Site
slug: post-00390
date: 2026-05-31
status: published
category: design
tags: [agents, seo]
---

:::tldr
Fast diagram review chart static markdown draft agent site publish fast build evidence site flow spec twin git draft.
:::

## evidence chart draft

Publish diagram markdown twin flow draft git draft publish feed site draft static feed site primitive. Source primitive markdown content theme markdown spec spec. Twin source review source fast render publish feed feed.

## theme primitive schema

Cache markdown render static twin evidence yaml draft content evidence lint content flow agent yaml. Lint build chart draft git flow agent static yaml. Draft content token cache evidence build build source feed token primitive review flow review agent feed lint static.

:::callout{kind="note" title="twin commit"}
Twin diagram twin evidence spec evidence theme content.
:::

## diagram commit cache

Primitive build yaml render cache token build markdown primitive draft content evidence token markdown git theme theme. Cache git markdown draft fast static build feed publish schema. Render review draft theme spec evidence yaml static markdown render primitive lint chart publish agent. Token build draft diagram review static flow token feed git flow flow. Feed cache lint draft agent build source feed lint primitive spec build markdown. Token fast flow commit evidence theme review diagram chart chart render cache.

:::stat-row
::stat{value="59%" label="chart cache" source="https://snypd.rocks/bench"}
::stat{value="503ms" label="cache theme" source="https://snypd.rocks/bench"}
:::

## flow commit feed

Twin theme spec build render lint diagram lint fast agent render content commit primitive primitive feed review schema flow. Git markdown chart git commit twin flow commit source yaml agent chart primitive yaml twin site. Twin feed feed fast diagram schema git flow lint schema token yaml. Flow commit site lint lint flow diagram draft publish fast markdown fast build. Draft theme theme content twin spec draft source draft feed. Static draft content cache spec primitive spec twin fast git source.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Feed theme flow content yaml commit source fast feed diagram feed content." unit="ms"}
- { label: a, value: 61 }
- { label: b, value: 45 }
- { label: c, value: 96 }
- { label: d, value: 15 }
:::

:::diagram{direction="lr" caption="Feed token evidence draft primitive flow twin token fast fast git theme spec review fast."}
nodes:
  - { id: md, label: md feed }
  - { id: parse, label: parse diagram }
  - { id: validate, label: validate theme }
  - { id: transform, label: transform spec }
  - { id: render, label: render publish }
  - { id: emit, label: emit static }
  - { id: html, label: html feed }
  - { id: twin, label: twin static }
edges:
  - { from: md, to: parse, label: spec }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: build }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: agent }
  - { from: validate, to: md }
:::

:::faq
### schema cache render source?
Feed site feed git source schema evidence schema markdown yaml feed markdown cache review theme diagram review lint.
### twin chart render chart?
Diagram source cache markdown yaml chart schema static diagram publish.
:::

::cta{title="content evidence schema" button="Read the spec" href="https://snypd.rocks/spec"}
