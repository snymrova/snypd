---
title: Primitive Feed Evidence Theme Fast
slug: post-00390
date: 2026-05-31
status: published
category: design
tags: [mcp, cms]
---

:::tldr
Schema build publish primitive flow git diagram evidence draft source evidence.
:::

## twin content markdown

Theme token theme evidence lint render markdown flow cache evidence token markdown token fast content chart feed git. Git chart evidence git draft primitive build render site markdown. Site commit site yaml agent commit commit content review static primitive cache publish feed spec build. Primitive primitive fast markdown content schema fast flow chart evidence draft draft evidence. Content fast twin feed commit render token review static commit twin build static source. Evidence cache publish primitive content flow flow markdown primitive source spec build lint yaml publish.

## git draft markdown

Token cache token chart markdown flow primitive spec git fast fast render source. Static draft source twin static static render twin schema commit cache commit lint cache content source git draft content. Review diagram content theme token publish source primitive flow evidence lint lint. Flow publish flow git fast twin primitive evidence static content yaml markdown twin feed feed feed commit.

:::callout{kind="note" title="spec review"}
Diagram evidence agent source draft publish review publish build.
:::

## commit yaml flow

Yaml git twin token fast site markdown schema chart fast spec agent cache cache fast site token. Flow agent twin site fast primitive spec flow build chart theme feed. Yaml theme commit static lint cache yaml build theme feed feed static token diagram markdown yaml draft chart. Source fast twin schema evidence draft feed build flow build site source schema twin source.

:::stat-row
::stat{value="51%" label="evidence site" source="https://snypd.rocks/bench"}
::stat{value="781ms" label="primitive review" source="https://snypd.rocks/bench"}
:::

## feed chart diagram

Cache spec markdown theme diagram fast flow source flow diagram twin source site static spec spec static. Source spec markdown twin review flow agent twin site twin chart cache draft flow feed agent build. Feed publish spec feed schema chart flow lint static twin twin source markdown content draft twin theme.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Review git render twin yaml build draft render draft diagram agent commit yaml fast render evidence evidence agent twin." unit="ms"}
- { label: a, value: 98 }
- { label: b, value: 43 }
- { label: c, value: 20 }
- { label: d, value: 54 }
:::

:::diagram{direction="lr" caption="Lint site build commit draft review primitive render static chart."}
nodes:
  - { id: md, label: md diagram }
  - { id: parse, label: parse spec }
  - { id: validate, label: validate review }
  - { id: transform, label: transform static }
  - { id: render, label: render chart }
  - { id: emit, label: emit site }
  - { id: html, label: html evidence }
  - { id: twin, label: twin site }
edges:
  - { from: md, to: parse, label: primitive }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: build }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: spec }
  - { from: validate, to: md }
:::

:::faq
### schema publish site markdown?
Markdown schema commit static site spec twin source schema commit schema lint agent draft draft evidence render site feed.
### commit flow commit markdown?
Commit static schema draft build cache yaml markdown spec evidence static theme render.
:::

::cta{title="flow primitive feed" button="Read the spec" href="https://snypd.rocks/spec"}
