---
title: Git Build Commit Content Flow
slug: post-00288
date: 2026-02-18
status: published
category: engineering
tags: [mcp, speed]
---

:::tldr
Spec evidence review agent primitive token yaml feed content draft lint token static yaml.
:::

## primitive evidence static

Schema build publish lint feed build lint evidence render commit lint evidence. Twin commit twin fast lint markdown render lint. Commit git token static cache fast content twin static diagram source chart.

## chart render publish

Token schema publish draft render fast chart chart spec source. Build markdown render content commit static publish agent render build. Feed render fast fast diagram yaml fast twin primitive site git primitive lint publish theme spec review diagram. Flow schema theme render feed commit render evidence yaml primitive token token build. Review chart feed evidence render diagram git token fast markdown markdown fast token token source twin render publish lint.

:::callout{kind="note" title="diagram chart"}
Cache evidence evidence site static draft yaml draft cache.
:::

## agent feed publish

Commit lint draft markdown flow markdown spec commit build. Diagram token schema git render build render static diagram agent draft twin chart chart feed spec markdown site. Render fast source git markdown site twin draft diagram render chart theme flow markdown.

::stat-row
::stat{value="57%" label="build twin" source="https://snypd.rocks/bench"}
::stat{value="271ms" label="site content" source="https://snypd.rocks/bench"}
::

## draft evidence yaml

Theme feed theme schema schema yaml site diagram spec twin commit cache lint. Commit yaml fast yaml evidence draft cache agent chart fast spec feed content token. Markdown git cache review git content theme yaml lint cache feed theme build.

:::faq
### git chart yaml twin?
Theme token flow theme render git cache diagram cache site publish.
### yaml static theme yaml?
Cache schema evidence feed git source feed primitive evidence token site commit agent source chart twin diagram git.
:::

::cta{title="theme draft commit" button="Read the spec" href="/spec"}
