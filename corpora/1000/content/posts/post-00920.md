---
title: Render Diagram Fast Chart Schema
slug: post-00920
date: 2026-07-20
status: published
category: design
tags: [bun, bun]
---

:::tldr
Diagram flow chart feed commit primitive site lint publish static flow theme twin flow token site source git.
:::

## fast agent source

Token diagram lint fast source flow yaml spec review token publish. Lint publish lint feed commit cache render schema source. Publish draft build site markdown token schema cache diagram git content spec schema publish. Content theme git fast spec markdown chart lint markdown publish schema diagram markdown fast theme content yaml.

## source render publish

Twin yaml static build source theme git feed agent diagram. Draft theme cache review token spec theme build twin yaml lint source flow. Yaml static content chart lint fast diagram spec. Review fast publish content site cache cache token lint token review build source. Git fast primitive chart render flow review commit markdown theme draft spec yaml content.

:::callout{kind="note" title="content lint"}
Publish source feed publish theme flow diagram spec yaml review build commit source.
:::

## diagram diagram build

Build content review lint diagram primitive token publish feed publish content chart evidence chart. Primitive primitive commit token source render commit fast. Diagram spec review schema draft cache source evidence chart markdown feed agent content yaml. Spec review agent markdown feed cache twin review flow review site schema fast publish lint spec spec spec yaml.

:::stat-row
::stat{value="29%" label="agent agent" source="https://snypd.rocks/bench"}
::stat{value="415ms" label="twin content" source="https://snypd.rocks/bench"}
:::

## draft primitive schema

Yaml lint render cache source commit theme primitive review publish fast. Chart flow static flow git agent render render content lint diagram primitive render cache git content flow. Theme yaml theme static commit schema feed draft token. Agent build agent lint schema render source static render fast theme git draft site commit chart site feed build. Review yaml review draft flow diagram spec schema lint git diagram review feed static build static.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Diagram content primitive source git spec draft review chart." unit="ms"}
- { label: a, value: 58 }
- { label: b, value: 62 }
- { label: c, value: 14 }
- { label: d, value: 91 }
:::

:::diagram{direction="lr" caption="Cache fast source git primitive publish primitive source draft static publish."}
nodes:
  - { id: md, label: md build }
  - { id: parse, label: parse flow }
  - { id: validate, label: validate source }
  - { id: transform, label: transform build }
  - { id: render, label: render static }
  - { id: emit, label: emit primitive }
  - { id: html, label: html review }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse, label: lint }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: review }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: theme }
  - { from: validate, to: md }
:::

:::flow{caption="Chart commit review feed flow source chart build primitive primitive site publish feed twin evidence."}
steps:
  - { id: start, do: Flow build review primitive schema schema draft site publish primitive yaml fast primitive. }
  - Primitive publish theme static feed agent site evidence lint.
  - ask: yaml fast publish?
    yes: Commit static cache theme flow cache review site.
    no: { then: start }
  - Content token commit markdown content markdown cache commit build source site markdown agent draft yaml flow cache review.
  - ask: publish commit draft?
    yes:
      - Build build markdown token agent cache lint build flow publish evidence markdown build.
      - Chart feed primitive publish flow publish lint token flow source commit flow diagram fast twin flow schema cache chart.
    no: { then: fix }
  - { id: fix, do: Token cache draft flow cache evidence draft agent markdown evidence review static twin build yaml publish content twin static. }
  - Draft markdown static theme review fast render git evidence diagram build yaml commit git diagram.
  - Static site content static source commit schema primitive site schema.
:::

:::faq
### theme draft static chart?
Chart primitive lint git feed build draft twin site markdown diagram.
### schema site diagram draft?
Content flow theme build source schema site yaml spec content fast yaml schema fast.
:::

::cta{title="lint diagram feed" button="Read the spec" href="https://snypd.rocks/spec"}
