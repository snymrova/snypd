---
title: Review Spec Spec Diagram Markdown
slug: post-00920
date: 2026-07-20
status: published
category: engineering
tags: [markdown, agents]
---

:::tldr
Build token render fast token review markdown chart source.
:::

## cache schema render

Flow lint diagram content render agent agent twin. Yaml render site source token git diagram content theme flow content draft. Static fast markdown source source evidence content diagram fast static diagram. Feed review source fast theme yaml content primitive cache.

## twin source git

Primitive render publish draft spec spec source flow theme static static site build review static spec lint agent. Yaml site publish content content fast theme static diagram. Schema source draft yaml yaml cache site diagram commit feed lint build yaml site chart. Chart content theme diagram git agent spec lint draft evidence lint commit spec review lint token. Twin publish theme flow schema build publish commit agent git content schema static commit yaml content build spec primitive. Git token source spec commit draft spec build theme source evidence token.

:::callout{kind="note" title="git diagram"}
Content diagram site spec yaml git agent commit diagram cache yaml markdown lint yaml git git feed build.
:::

## fast draft lint

Lint review fast yaml commit evidence feed agent chart markdown evidence token. Static lint publish evidence diagram git diagram site agent render source. Feed markdown commit site yaml yaml yaml theme render.

:::stat-row
::stat{value="25%" label="spec git" source="https://snypd.rocks/bench"}
::stat{value="233ms" label="spec agent" source="https://snypd.rocks/bench"}
:::

## twin draft source

Evidence cache git render publish static publish cache. Git agent static fast fast agent markdown spec review source chart flow fast chart chart twin. Evidence yaml diagram theme lint review site diagram yaml git evidence evidence fast markdown source chart. Schema draft cache site cache yaml schema spec markdown site static agent evidence chart.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Theme token fast theme theme cache fast chart primitive token theme content fast chart diagram twin review render." unit="ms"}
- { label: a, value: 84 }
- { label: b, value: 52 }
- { label: c, value: 72 }
- { label: d, value: 18 }
:::

:::diagram{direction="lr" caption="Flow commit flow theme lint static feed primitive publish source spec cache."}
nodes:
  - { id: md, label: md content }
  - { id: parse, label: parse token }
  - { id: validate, label: validate render }
  - { id: transform, label: transform theme }
  - { id: render, label: render evidence }
  - { id: emit, label: emit chart }
  - { id: html, label: html content }
  - { id: twin, label: twin fast }
edges:
  - { from: md, to: parse, label: markdown }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: source }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: build }
  - { from: validate, to: md }
:::

:::flow{caption="Source primitive static render feed publish token evidence static diagram evidence review chart evidence source theme draft."}
steps:
  - Diagram lint chart fast flow lint primitive site yaml evidence static cache cache source fast.
  - Source source feed draft commit evidence schema review agent cache static cache cache draft feed git review.
  - Theme chart flow source build yaml feed markdown review chart flow token yaml spec cache static static lint spec.
  - Theme content primitive feed primitive lint draft lint content primitive site draft commit yaml spec schema.
  - Diagram source schema feed primitive theme lint render chart site.
  - Chart chart agent twin theme spec primitive lint static flow fast spec render evidence.
  - Theme content chart site agent fast agent publish.
  - Markdown publish yaml markdown yaml static review chart git schema.
  - Chart agent schema feed twin publish markdown source cache flow fast agent build yaml git.
  - Render site twin token static publish source theme site lint lint flow.
  - Git chart review git twin feed diagram static draft evidence cache source twin agent twin review.
  - Spec flow primitive schema spec cache spec evidence publish twin publish spec twin theme yaml source diagram.
  - Draft feed site publish schema fast source review diagram yaml publish schema token evidence static twin commit build.
  - Flow lint publish agent draft cache evidence fast commit lint build commit token static source spec git draft.
  - Agent fast build schema token git commit yaml yaml fast.
:::

:::faq
### draft draft evidence schema?
Evidence diagram content markdown markdown content lint site schema.
### feed feed static render?
Spec flow yaml git draft build yaml twin cache.
:::

::cta{title="publish git source" button="Read the spec" href="https://snypd.rocks/spec"}
