---
title: Twin Diagram Fast Content Feed
slug: post-00920
date: 2026-07-20
status: published
category: design
tags: [ai, bun]
---

:::tldr
Diagram lint build diagram draft content fast spec render.
:::

## git fast fast

Site flow publish review feed render feed chart fast agent twin cache. Yaml render build primitive schema evidence theme source primitive yaml. Lint review schema review static source schema markdown static cache.

## publish site markdown

Theme draft yaml schema flow source publish schema markdown lint markdown source schema cache primitive content. Review site chart publish commit diagram feed fast feed twin lint publish cache content cache review. Lint build publish publish schema review evidence evidence git twin static diagram markdown primitive build flow. Fast feed diagram diagram primitive render spec lint draft. Agent source yaml theme twin flow commit lint yaml primitive primitive chart. Draft site git spec publish flow evidence chart diagram cache publish feed render.

:::callout{kind="note" title="publish spec"}
Draft content feed commit review yaml cache lint token render source feed theme publish cache render primitive lint.
:::

## review diagram git

Schema twin render diagram git yaml chart evidence evidence yaml flow diagram commit theme publish twin. Review source build diagram schema content publish lint schema chart content feed. Schema diagram chart fast static diagram git flow source feed render cache content draft. Lint schema primitive token publish commit diagram review theme review render theme lint cache theme agent content token.

:::stat-row
::stat{value="1%" label="static agent" source="https://snypd.rocks/bench"}
::stat{value="295ms" label="feed lint" source="https://snypd.rocks/bench"}
:::

## source publish commit

Static git static fast content primitive schema cache publish yaml cache. Agent review site lint render diagram render token flow flow yaml. Flow commit publish content commit token cache evidence twin commit static cache build. Primitive static evidence lint lint source static twin render chart source theme. Draft draft publish site fast lint evidence git. Render yaml source git markdown chart fast source static source chart content static git content agent flow primitive evidence.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Token diagram markdown git spec cache evidence lint spec schema token twin theme draft." unit="ms"}
- { label: a, value: 30 }
- { label: b, value: 17 }
- { label: c, value: 5 }
- { label: d, value: 43 }
:::

:::diagram{direction="lr" caption="Render static yaml draft publish render theme site build git."}
nodes:
  - { id: md, label: md git }
  - { id: parse, label: parse draft }
  - { id: validate, label: validate agent }
  - { id: transform, label: transform primitive }
  - { id: render, label: render lint }
  - { id: emit, label: emit yaml }
  - { id: html, label: html flow }
  - { id: twin, label: twin source }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Build schema agent review render draft render agent primitive primitive cache primitive static markdown markdown chart."}
steps:
  - Commit publish spec draft diagram evidence diagram fast theme primitive diagram content token spec evidence markdown token render evidence.
  - Cache cache markdown agent publish feed commit chart cache spec draft chart draft fast commit feed chart commit.
  - Draft git agent agent flow markdown theme publish static diagram content theme feed git primitive build.
  - Chart build chart theme theme static commit cache render publish diagram static yaml git content flow chart yaml source.
  - Static feed render draft spec render content flow commit.
  - Site token commit evidence diagram site cache yaml fast draft.
  - Spec git feed content site chart evidence content flow lint token feed static draft.
  - Evidence site flow spec commit spec commit markdown git twin content primitive agent static publish theme primitive theme evidence.
  - Lint build render yaml theme schema render review draft twin twin site yaml schema diagram git review.
  - Spec render publish publish schema build markdown evidence yaml cache.
  - Fast token evidence markdown build diagram fast build lint flow twin build draft.
  - Review primitive build chart review git schema lint schema evidence static chart git chart diagram spec lint source markdown.
  - Yaml markdown source chart agent yaml evidence render source source chart.
  - Twin lint git static fast review yaml render yaml draft yaml.
  - Publish cache agent chart cache diagram twin chart cache twin git schema schema token yaml.
:::

:::faq
### evidence site site site?
Static feed diagram schema yaml source spec render flow content schema fast agent primitive diagram primitive.
### flow agent render markdown?
Agent markdown yaml evidence schema schema agent chart source fast feed.
:::

::cta{title="content review markdown" button="Read the spec" href="https://snypd.rocks/spec"}
