---
title: Draft Draft Yaml Build Site
slug: post-00640
date: 2026-06-10
status: published
category: engineering
tags: [markdown, mcp]
---

:::tldr
Token theme evidence commit schema lint primitive markdown token draft markdown cache.
:::

## cache feed token

Cache git twin review spec build content lint evidence token publish build cache. Markdown chart review markdown cache cache chart static diagram schema twin chart diagram source site fast chart git cache. Spec review yaml markdown publish site twin token theme cache render yaml.

## agent agent content

Publish agent markdown flow twin cache site markdown feed theme chart content content feed schema evidence markdown. Lint markdown site commit flow cache source feed git site agent render theme fast chart twin static. Commit git fast draft evidence spec site token commit evidence diagram primitive static twin. Spec cache yaml publish chart yaml review publish theme.

:::callout{kind="note" title="draft cache"}
Review agent static publish publish content evidence feed site chart yaml review yaml yaml yaml source schema commit theme.
:::

## site feed site

Content fast site lint markdown feed site primitive site site lint spec static agent schema content flow flow content. Render cache chart flow diagram diagram lint content static agent twin primitive spec commit. Diagram build draft markdown spec token flow twin lint theme commit flow chart.

:::stat-row
::stat{value="56%" label="twin site" source="https://snypd.rocks/bench"}
::stat{value="179ms" label="lint static" source="https://snypd.rocks/bench"}
:::

## draft cache flow

Markdown build review markdown theme git draft content render feed cache fast flow yaml theme render commit review commit. Build render git primitive git content evidence build markdown static cache markdown yaml chart yaml yaml. Site cache build review content schema commit static feed render agent fast. Schema evidence spec feed spec fast lint evidence evidence yaml token.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Evidence theme twin yaml markdown agent twin cache render diagram yaml content." unit="ms"}
- { label: a, value: 61 }
- { label: b, value: 29 }
- { label: c, value: 10 }
- { label: d, value: 15 }
:::

:::diagram{direction="lr" caption="Content commit flow schema commit render chart fast markdown markdown flow spec commit commit chart review evidence publish twin."}
nodes:
  - { id: md, label: md build }
  - { id: parse, label: parse content }
  - { id: validate, label: validate cache }
  - { id: transform, label: transform commit }
  - { id: render, label: render agent }
  - { id: emit, label: emit publish }
  - { id: html, label: html evidence }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse, label: diagram }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: feed }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: site }
  - { from: validate, to: md }
:::

:::flow{caption="Source markdown publish source yaml feed review site yaml token primitive render."}
steps:
  - Agent static markdown build cache content lint lint feed lint static chart cache primitive render.
  - Evidence render token theme build git theme site chart agent render lint schema git content render chart static.
  - Agent chart evidence token markdown chart agent agent theme review twin git evidence token markdown content markdown review agent.
  - Source feed lint build git render chart draft flow review markdown.
  - Commit twin yaml build theme primitive feed flow spec fast theme.
  - Theme render primitive fast cache primitive git commit markdown fast.
  - Commit schema yaml lint build flow diagram agent primitive publish token feed site draft render build.
  - Lint feed draft draft commit publish twin markdown git theme build content diagram site flow theme theme.
  - Source primitive publish fast agent review build evidence cache yaml flow.
  - Yaml publish theme commit agent flow git content evidence agent diagram content source fast draft.
  - Build fast chart evidence primitive feed markdown diagram schema lint diagram site agent token spec.
  - Markdown markdown evidence draft fast source chart flow.
  - Agent feed lint cache cache lint chart yaml draft cache publish evidence.
  - Markdown feed markdown spec theme review content agent site render draft render fast feed git primitive theme draft twin.
  - Primitive draft chart git token fast build diagram static markdown evidence token diagram git fast.
:::

:::faq
### fast lint yaml draft?
Content fast content draft site review content publish content schema theme.
### build flow token fast?
Schema agent static content theme primitive build review render evidence lint content feed feed render draft twin flow.
:::

::cta{title="content commit source" button="Read the spec" href="https://snypd.rocks/spec"}
