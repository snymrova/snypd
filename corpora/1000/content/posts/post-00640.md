---
title: Site Commit Yaml Evidence Flow
slug: post-00640
date: 2026-06-10
status: published
category: ops
tags: [ai, mcp]
---

:::tldr
Render site token commit git static evidence draft publish draft draft feed theme content.
:::

## theme source schema

Chart flow markdown site commit diagram draft build commit lint fast draft. Token fast agent evidence agent fast source publish cache spec. Content site review lint diagram lint publish primitive content site. Theme render publish commit static twin lint schema flow token feed yaml. Fast render theme diagram publish theme lint source lint.

## review content twin

Commit primitive feed static markdown yaml token cache twin twin primitive theme token static source site publish. Twin diagram flow primitive commit site twin feed site spec. Publish site content feed draft yaml render content review chart publish flow twin schema review chart. Theme spec fast spec markdown schema review agent twin review token flow lint diagram draft schema schema. Publish fast theme flow source agent render token theme site chart primitive draft draft static markdown twin content. Primitive static evidence commit schema agent markdown fast review static review.

:::callout{kind="note" title="schema chart"}
Draft publish git lint theme primitive draft markdown yaml source evidence theme.
:::

## review chart evidence

Theme lint static review draft flow chart yaml. Publish content commit source theme git yaml twin primitive primitive agent site static twin yaml token theme publish publish. Schema render static commit spec schema static schema commit.

:::stat-row
::stat{value="95%" label="chart evidence" source="https://snypd.rocks/bench"}
::stat{value="802ms" label="token source" source="https://snypd.rocks/bench"}
:::

## draft git diagram

Token content twin build token commit feed static site markdown primitive static schema review cache commit source render render. Twin publish primitive static spec twin spec static git spec token evidence feed draft yaml diagram source feed evidence. Diagram source theme theme lint lint theme chart publish schema agent chart primitive publish git evidence. Lint primitive build static lint markdown twin git fast feed flow primitive lint markdown. Git source publish git render primitive spec build lint source commit flow token git review yaml diagram chart.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Site yaml source schema feed git schema evidence draft theme." unit="ms"}
- { label: a, value: 19 }
- { label: b, value: 83 }
- { label: c, value: 75 }
- { label: d, value: 72 }
:::

:::diagram{direction="lr" caption="Review render build source build render feed evidence theme."}
nodes:
  - { id: md, label: md yaml }
  - { id: parse, label: parse source }
  - { id: validate, label: validate site }
  - { id: transform, label: transform chart }
  - { id: render, label: render review }
  - { id: emit, label: emit render }
  - { id: html, label: html schema }
  - { id: twin, label: twin content }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Publish primitive render schema git agent schema content draft primitive commit source agent draft git publish fast."}
steps:
  - Render static build diagram yaml review review agent static schema.
  - Theme lint build token source git site twin publish agent agent yaml static content fast source feed.
  - Commit site draft diagram git draft feed publish.
  - Content render feed primitive yaml static yaml commit.
  - Lint yaml primitive site markdown token feed fast review theme markdown.
  - Cache lint render publish twin spec publish token spec build.
  - Build git cache site primitive cache lint commit evidence commit build twin schema lint chart agent render render evidence.
  - Feed flow lint cache site commit source fast flow theme publish.
  - Yaml yaml chart source git lint review token spec publish flow primitive yaml source.
  - Git spec site schema source git fast lint theme build review theme twin source feed.
  - Publish content twin draft build diagram content draft agent content review evidence draft schema schema spec static markdown lint.
  - Static cache twin build fast build site theme render fast chart spec commit lint review diagram.
  - Spec flow feed publish git publish flow draft yaml cache schema review twin render cache flow.
  - Content build diagram commit render schema fast publish spec evidence primitive git primitive render cache.
  - Theme render agent markdown flow site review twin fast flow markdown site evidence publish review cache token render diagram.
:::

:::faq
### source feed lint commit?
Git static twin chart render yaml render build markdown render build chart diagram flow agent render spec.
### token chart primitive feed?
Chart fast build draft source yaml review draft source.
:::

::cta{title="lint evidence content" button="Read the spec" href="https://snypd.rocks/spec"}
