---
title: Git Theme Markdown Schema Chart
slug: post-00740
date: 2026-01-21
status: published
category: engineering
tags: [ai, speed]
---

:::tldr
Content source content fast twin content token build spec source site source twin commit spec markdown render fast.
:::

## flow feed flow

Publish schema static draft build content publish markdown build primitive flow token. Draft flow primitive build agent site feed site feed fast agent fast chart spec. Review site token diagram chart content schema twin lint theme theme flow feed source chart markdown twin diagram diagram.

## agent fast cache

Schema fast schema source content render static content evidence feed primitive feed twin cache primitive twin publish. Site diagram draft publish render lint site publish. Build primitive content schema theme build token draft token chart primitive primitive cache spec twin lint diagram agent. Content primitive schema build evidence site git twin primitive chart cache.

:::callout{kind="note" title="git markdown"}
Cache feed git commit theme fast commit markdown render primitive content draft spec.
:::

## cache fast schema

Flow flow twin spec agent theme token draft markdown render render static. Review lint markdown static fast site twin cache commit render agent twin theme build feed render spec chart. Source publish spec draft theme feed cache evidence.

:::stat-row
::stat{value="90%" label="yaml token" source="https://snypd.rocks/bench"}
::stat{value="657ms" label="evidence spec" source="https://snypd.rocks/bench"}
:::

## review markdown commit

Lint git markdown diagram markdown static diagram flow primitive. Content draft token agent render theme content static twin theme commit markdown diagram commit yaml content git spec cache. Content review cache primitive draft cache token build feed git build content lint. Fast git draft draft diagram lint commit feed render static schema. Spec site chart commit fast yaml static source source fast primitive yaml lint twin token agent feed twin static.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Token publish evidence static build source content render chart." unit="ms"}
- { label: a, value: 20 }
- { label: b, value: 25 }
- { label: c, value: 52 }
- { label: d, value: 96 }
:::

:::diagram{direction="lr" caption="Spec draft token schema render static feed feed content cache lint draft."}
nodes:
  - { id: md, label: md yaml }
  - { id: parse, label: parse lint }
  - { id: validate, label: validate static }
  - { id: transform, label: transform markdown }
  - { id: render, label: render fast }
  - { id: emit, label: emit review }
  - { id: html, label: html token }
  - { id: twin, label: twin build }
edges:
  - { from: md, to: parse, label: source }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: publish }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: site }
  - { from: validate, to: md }
:::

:::flow{caption="Cache static feed flow build twin theme evidence agent."}
steps:
  - Yaml chart static feed theme git yaml evidence git build lint token content flow render lint publish.
  - Token lint publish schema flow primitive feed evidence theme twin build feed primitive render site feed lint yaml.
  - Commit evidence yaml primitive theme review site build spec lint static.
  - Commit twin theme lint site commit feed lint fast source.
  - Primitive build site schema source agent chart agent theme draft token primitive content feed schema theme.
  - Flow cache commit spec commit site schema flow review flow draft build evidence primitive spec.
  - Primitive publish agent flow commit evidence evidence build.
  - Lint agent feed draft commit primitive source flow twin primitive.
  - Build lint theme source content lint yaml schema fast agent source spec review markdown chart fast draft chart evidence.
  - Flow token theme cache chart spec token draft commit token lint lint source site.
  - Content flow fast review feed draft spec cache yaml chart token.
  - Spec cache yaml draft git cache feed flow theme source token chart diagram diagram evidence chart.
  - Fast flow spec schema feed review diagram lint spec feed token source markdown static.
  - Build cache draft cache chart static publish agent review commit schema agent source lint schema twin yaml.
  - Source schema token diagram lint review build build markdown primitive build.
:::

:::faq
### token diagram chart primitive?
Static chart content yaml site publish review evidence lint git yaml.
### fast flow diagram commit?
Source feed commit yaml theme static build agent.
:::

::cta{title="twin flow render" button="Read the spec" href="https://snypd.rocks/spec"}
