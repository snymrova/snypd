---
title: Diagram Fast Git Render Content
slug: post-00740
date: 2026-01-21
status: published
category: product
tags: [markdown, cms]
---

:::tldr
Evidence markdown commit static render theme review spec static theme draft diagram.
:::

## markdown chart markdown

Feed review source theme render markdown fast draft static publish token lint schema diagram. Markdown review theme site token flow source fast publish render commit source twin chart spec. Static git agent spec markdown token build commit content review static evidence token markdown.

## twin twin spec

Token content cache commit evidence theme theme diagram feed render flow lint theme. Render review static twin review chart feed evidence schema agent source twin twin agent render. Static yaml evidence static static spec primitive content yaml twin. Chart primitive cache lint review twin site twin commit theme git agent publish evidence.

:::callout{kind="note" title="flow twin"}
Schema commit fast primitive commit git flow publish build fast agent primitive lint.
:::

## flow cache agent

Chart draft spec twin twin primitive evidence primitive primitive fast theme git twin token flow build diagram publish. Evidence cache commit draft review agent draft static render static build. Git agent token twin commit primitive lint evidence lint render draft render publish.

:::stat-row
::stat{value="37%" label="render lint" source="https://snypd.rocks/bench"}
::stat{value="344ms" label="git token" source="https://snypd.rocks/bench"}
:::

## lint yaml agent

Lint static publish primitive content markdown source theme schema commit source. Evidence build flow draft feed build publish evidence git evidence fast feed build chart markdown chart agent. Lint flow diagram static commit primitive feed schema draft schema draft fast.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Lint draft site review token source render token." unit="ms"}
- { label: a, value: 34 }
- { label: b, value: 99 }
- { label: c, value: 26 }
- { label: d, value: 51 }
:::

:::diagram{direction="lr" caption="Review cache git commit content evidence spec flow yaml evidence theme draft build schema."}
nodes:
  - { id: md, label: md git }
  - { id: parse, label: parse markdown }
  - { id: validate, label: validate review }
  - { id: transform, label: transform markdown }
  - { id: render, label: render review }
  - { id: emit, label: emit draft }
  - { id: html, label: html flow }
  - { id: twin, label: twin feed }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Lint review cache primitive yaml cache source primitive spec spec commit site schema twin lint markdown site primitive."}
steps:
  - Fast commit chart build chart markdown site commit schema publish git.
  - Render static review yaml git fast site source lint lint review lint evidence agent.
  - Chart fast commit build token site static yaml chart git agent.
  - Commit diagram review render flow static primitive token spec draft content fast cache token git diagram agent chart publish.
  - Site token git commit token review evidence evidence lint twin flow commit review diagram source.
  - Git diagram publish static publish primitive schema render evidence flow token draft.
  - Cache commit agent publish chart fast token site chart.
  - Diagram static feed build markdown agent git schema twin primitive twin theme review.
  - Agent schema cache review evidence schema review source yaml source cache git git static schema feed site.
  - Token cache fast commit content fast render primitive feed diagram fast draft content.
  - Content markdown token cache token token markdown review agent feed cache twin chart markdown content content static.
  - Git source fast build chart diagram source theme publish render feed publish.
  - Theme spec commit yaml static publish git primitive primitive schema theme site chart agent feed commit.
  - Spec commit draft review markdown markdown build feed primitive.
  - Build evidence git spec lint static spec flow cache primitive review.
:::

:::faq
### build schema schema agent?
Fast site static twin cache schema draft source cache cache chart commit draft site yaml chart token site.
### review twin content flow?
Fast lint theme git fast publish static agent markdown theme markdown cache.
:::

::cta{title="chart review content" button="Read the spec" href="https://snypd.rocks/spec"}
