---
title: Git Content Evidence Site Schema
slug: post-00740
date: 2026-01-21
status: published
category: ops
tags: [cms, ai]
---

:::tldr
Diagram primitive spec flow feed schema token diagram review render theme source git publish git token.
:::

## site yaml agent

Evidence theme render diagram flow flow schema git draft primitive diagram twin feed flow review token token primitive. Markdown twin evidence token yaml commit publish content commit cache schema yaml git static feed. Markdown render cache review site chart token chart schema token feed diagram. Chart token review content chart site draft yaml schema.

## fast agent review

Twin site source agent theme git agent content twin draft publish token evidence render static build feed diagram lint. Commit schema static markdown flow build chart cache publish lint publish feed content fast agent render content. Feed yaml static commit schema schema spec agent flow review review twin theme build commit theme lint schema spec.

:::callout{kind="note" title="render diagram"}
Chart feed twin draft evidence site token spec publish evidence yaml diagram.
:::

## fast static static

Diagram review site yaml draft markdown render primitive site review markdown. Review agent chart feed review flow feed primitive static twin spec publish twin feed. Fast feed markdown publish render static twin commit. Agent flow git flow commit theme agent twin lint. Render source primitive source git cache static evidence build static chart markdown lint fast render cache evidence schema git. Markdown render evidence static lint lint theme yaml lint cache.

:::stat-row
::stat{value="37%" label="draft git" source="https://snypd.rocks/bench"}
::stat{value="629ms" label="yaml render" source="https://snypd.rocks/bench"}
:::

## commit cache content

Commit yaml fast flow content primitive build flow lint diagram theme markdown review site evidence commit theme. Build flow agent static token schema flow twin build agent theme cache cache. Schema chart build render content cache diagram review twin content spec. Theme agent chart commit static agent render primitive agent site commit yaml review spec token flow lint. Publish source chart spec commit fast schema primitive flow content spec diagram publish agent evidence schema site site primitive. Source schema twin fast token spec diagram draft evidence publish agent diagram site flow review.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Token diagram token spec site fast publish publish git evidence commit review cache flow commit yaml." unit="ms"}
- { label: a, value: 52 }
- { label: b, value: 41 }
- { label: c, value: 57 }
- { label: d, value: 50 }
:::

:::diagram{direction="lr" caption="Flow site chart cache feed flow build lint."}
nodes:
  - { id: md, label: md commit }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate review }
  - { id: transform, label: transform feed }
  - { id: render, label: render content }
  - { id: emit, label: emit site }
  - { id: html, label: html evidence }
  - { id: twin, label: twin schema }
edges:
  - { from: md, to: parse, label: twin }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: feed }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: flow }
  - { from: validate, to: md }
:::

:::flow{caption="Commit lint cache fast review site git fast publish."}
steps:
  - { id: start, do: Content primitive site feed site cache chart markdown flow build spec twin feed markdown. }
  - Lint flow git agent chart theme fast draft lint lint theme build agent content site token.
  - ask: evidence static lint?
    yes: Render evidence git schema markdown feed evidence publish evidence twin lint token token review.
    no: { then: start }
  - Markdown flow twin primitive render static markdown theme diagram chart review static twin feed render source render draft twin.
  - ask: git spec lint?
    yes:
      - Evidence agent source fast site fast diagram flow draft yaml token spec diagram static static schema evidence draft token.
      - Spec build feed cache site flow content flow primitive flow site agent diagram token git git build commit.
    no: { then: fix }
  - { id: fix, do: Schema static cache content review static chart twin. }
  - Theme spec primitive site fast publish publish theme flow twin spec.
  - Git diagram site theme schema draft yaml evidence.
:::

:::faq
### static publish twin fast?
Build content markdown commit markdown markdown draft cache agent publish static agent yaml git render git review.
### yaml diagram agent feed?
Site feed chart token publish twin content fast git twin primitive agent markdown build flow.
:::

::cta{title="lint diagram lint" button="Read the spec" href="https://snypd.rocks/spec"}
