---
title: Yaml Git Token Cache Source
slug: post-00380
date: 2026-05-21
status: published
category: product
tags: [bun, bun]
---

:::tldr
Build site git primitive feed lint cache draft flow diagram yaml yaml content publish flow commit token.
:::

## build markdown render

Cache commit token site lint commit feed source token primitive flow publish draft git twin yaml. Commit cache cache render schema static markdown evidence agent lint feed diagram source fast spec site content lint token. Twin markdown render fast source lint spec git cache fast diagram render markdown source feed static evidence agent. Commit evidence lint commit markdown diagram primitive render diagram render.

## commit fast twin

Feed token fast site schema cache token theme twin lint evidence lint git cache cache source fast review. Flow primitive render agent evidence site render static primitive. Diagram build evidence flow draft lint markdown primitive chart lint draft site agent diagram fast publish. Feed site primitive static source spec fast feed evidence lint commit fast cache. Git commit commit schema diagram source spec content site fast review primitive agent source markdown theme primitive.

:::callout{kind="note" title="theme static"}
Commit content markdown site twin evidence publish publish token token review token git site feed site token fast.
:::

## yaml yaml markdown

Markdown static draft publish review fast evidence fast fast review chart review review markdown lint markdown diagram. Feed publish flow chart chart git primitive yaml chart draft commit build source. Render review commit twin publish primitive diagram build build twin markdown commit.

:::stat-row
::stat{value="69%" label="lint site" source="https://snypd.rocks/bench"}
::stat{value="145ms" label="yaml schema" source="https://snypd.rocks/bench"}
:::

## lint cache static

Agent yaml git spec evidence primitive flow token build site content chart build evidence source theme. Primitive markdown primitive static agent draft chart feed diagram lint fast flow agent build primitive. Content spec yaml theme spec git flow lint primitive lint evidence agent content content lint spec cache markdown publish. Feed schema evidence primitive flow diagram fast flow markdown theme chart theme. Review markdown schema draft publish git site markdown cache static fast fast flow primitive evidence git feed. Chart commit agent spec chart cache token git.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Publish publish primitive source primitive source diagram content render spec lint draft commit twin review yaml lint." unit="ms"}
- { label: a, value: 19 }
- { label: b, value: 34 }
- { label: c, value: 66 }
- { label: d, value: 19 }
:::

:::diagram{direction="lr" caption="Token chart twin review spec flow render review."}
nodes:
  - { id: md, label: md spec }
  - { id: parse, label: parse source }
  - { id: validate, label: validate draft }
  - { id: transform, label: transform draft }
  - { id: render, label: render schema }
  - { id: emit, label: emit git }
  - { id: html, label: html review }
  - { id: twin, label: twin review }
edges:
  - { from: md, to: parse, label: twin }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: publish }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: token }
  - { from: validate, to: md }
:::

:::flow{caption="Spec feed chart commit source markdown token spec content review lint site primitive draft token site commit."}
steps:
  - Git flow spec publish primitive primitive commit build source review build lint.
  - Twin spec markdown schema publish site twin static publish schema review chart chart token review build cache static yaml.
  - Site source draft twin agent git static build source theme chart diagram source token flow site source diagram cache.
  - Token diagram yaml schema static theme spec git.
  - Site evidence evidence theme diagram draft commit review twin fast site theme build feed publish spec markdown primitive primitive.
  - Spec publish render yaml spec twin commit chart publish diagram theme fast cache content fast fast agent build draft.
  - Markdown site git twin twin flow draft feed static source chart review token primitive.
  - Review twin agent feed fast twin publish markdown token evidence fast diagram commit build token.
  - Evidence markdown content spec source lint evidence markdown site markdown source static flow build cache lint.
  - Evidence markdown commit git flow token spec git theme fast source markdown git.
  - Chart cache git diagram spec render static content build spec.
  - Agent feed chart git publish source feed publish source.
  - Publish primitive git site evidence twin render review static agent theme fast flow diagram content.
  - Yaml chart diagram theme spec flow yaml site primitive.
  - Source markdown site spec static publish primitive theme.
:::

:::faq
### primitive lint token build?
Static chart site git evidence schema build commit primitive commit site lint.
### git render primitive yaml?
Feed render cache yaml feed git agent cache static source git content.
:::

::cta{title="diagram schema source" button="Read the spec" href="https://snypd.rocks/spec"}
