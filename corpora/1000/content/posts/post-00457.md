---
title: Git Markdown Static Schema Flow
slug: post-00457
date: 2026-08-06
status: published
category: product
tags: [agents, markdown]
---

:::tldr
Site flow draft content spec agent evidence lint spec cache source feed flow commit lint.
:::

## evidence markdown commit

Feed site schema agent flow diagram lint yaml primitive lint feed cache feed token. Agent publish token flow markdown diagram twin commit twin cache git content source cache. Content schema diagram primitive render feed content spec. Token render flow fast feed chart spec static draft schema.

## build lint cache

Chart theme cache twin diagram diagram markdown source schema git agent git content. Agent spec cache content chart fast primitive token fast twin spec theme chart schema. Evidence primitive token site feed publish publish yaml review flow diagram content cache diagram. Source render render agent build build twin git draft spec commit static diagram site. Theme build fast git content source token schema build theme cache token primitive chart markdown theme. Draft twin twin site theme feed twin primitive render render flow draft publish evidence site schema.

:::callout{kind="note" title="theme render"}
Diagram flow build token publish yaml content token.
:::

## diagram cache fast

Yaml source commit source theme feed agent evidence source feed primitive lint publish build markdown schema content spec spec. Git feed commit theme yaml commit lint twin chart flow lint flow schema source. Chart render source content static yaml fast publish content yaml token chart commit render git lint evidence source agent. Publish fast agent commit static lint render primitive publish agent cache markdown evidence chart site.

:::stat-row
::stat{value="93%" label="commit site" source="https://snypd.rocks/bench"}
::stat{value="608ms" label="twin feed" source="https://snypd.rocks/bench"}
:::

## primitive fast site

Evidence feed twin site theme flow static diagram site build. Cache source yaml static git schema source theme build draft site fast site. Content markdown markdown git markdown git review render. Diagram markdown render draft git schema site feed cache. Feed yaml build feed static static source source content schema fast diagram twin review yaml yaml source render feed. Lint git spec commit build site markdown lint schema build site content token.

:::faq
### git content markdown schema?
Publish git diagram render markdown twin spec markdown fast site.
### build yaml chart site?
Static git primitive publish flow spec primitive draft source render build cache chart content feed source.
:::

::cta{title="fast fast theme" button="Read the spec" href="https://snypd.rocks/spec"}
