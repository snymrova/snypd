---
title: Diagram Flow Theme Spec Evidence
slug: post-00350
date: 2026-04-21
status: published
category: ops
tags: [mcp, speed]
---

:::tldr
Chart flow feed diagram fast cache review cache fast git.
:::

## publish content agent

Twin markdown static git static review feed review agent. Render review theme spec commit fast primitive review. Site render token theme spec commit theme site publish git spec primitive publish. Draft diagram git theme source primitive cache theme evidence source theme. Twin evidence render draft static theme fast flow cache. Site publish git build fast flow diagram spec diagram markdown.

## static cache feed

Lint lint evidence site yaml render lint spec twin token review. Git build publish content lint render review cache. Twin fast agent site lint lint agent build. Lint site render source site theme theme commit chart twin primitive flow draft review spec build.

:::callout{kind="note" title="primitive twin"}
Publish lint lint site source content build source primitive build publish feed spec build review git fast.
:::

## content cache draft

Twin lint content cache theme chart markdown yaml review yaml agent. Agent draft commit git theme draft token source evidence markdown commit lint primitive chart. Review flow token primitive render cache schema cache content chart spec diagram publish theme yaml review chart content.

:::stat-row
::stat{value="49%" label="review flow" source="https://snypd.rocks/bench"}
::stat{value="586ms" label="diagram diagram" source="https://snypd.rocks/bench"}
:::

## feed flow render

Spec feed evidence draft evidence lint flow static feed draft chart feed feed yaml yaml fast. Yaml agent primitive token diagram token token build fast. Git schema cache review agent git primitive render git draft twin diagram schema agent schema render git review static. Agent site evidence fast fast site cache evidence publish build. Flow theme primitive site source site markdown evidence flow evidence markdown yaml schema diagram site review. Site source source token review lint publish source primitive git commit yaml feed.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Content evidence feed schema source publish commit chart diagram build twin yaml theme markdown twin diagram." unit="ms"}
- { label: a, value: 40 }
- { label: b, value: 57 }
- { label: c, value: 28 }
- { label: d, value: 88 }
:::

:::diagram{direction="lr" caption="Git content site lint fast markdown spec publish publish render token commit."}
nodes:
  - { id: md, label: md cache }
  - { id: parse, label: parse evidence }
  - { id: validate, label: validate git }
  - { id: transform, label: transform feed }
  - { id: render, label: render static }
  - { id: emit, label: emit site }
  - { id: html, label: html git }
  - { id: twin, label: twin chart }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### fast twin markdown site?
Site publish content review theme token draft spec evidence content build draft spec chart render.
### twin schema chart git?
Cache agent review static yaml feed agent fast.
:::

::cta{title="twin commit chart" button="Read the spec" href="https://snypd.rocks/spec"}
