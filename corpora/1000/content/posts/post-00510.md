---
title: Site Agent Source Spec Token
slug: post-00510
date: 2026-01-31
status: published
category: ops
tags: [cms, cms]
---

:::tldr
Cache agent lint diagram flow commit schema feed build lint source commit source primitive site twin primitive.
:::

## theme render publish

Review feed static chart commit review build agent build theme twin spec spec token commit evidence chart token review. Cache schema fast chart site publish draft twin primitive commit diagram cache fast. Static diagram static flow diagram commit git agent evidence primitive evidence draft build build lint chart commit token primitive. Flow agent cache fast site token static agent.

## primitive yaml build

Token build source draft fast cache cache spec draft token site yaml source review static site build. Cache draft fast evidence yaml yaml draft static. Agent diagram primitive evidence markdown primitive markdown git feed review twin primitive review evidence draft fast static. Source commit static primitive twin yaml fast diagram twin content schema static commit build theme. Source content cache theme git primitive lint source commit chart evidence build spec commit twin cache agent commit. Evidence diagram content feed git markdown evidence site commit draft content draft spec lint.

:::callout{kind="note" title="markdown evidence"}
Token review source content agent spec draft markdown commit schema markdown lint flow content chart chart fast.
:::

## flow source site

Lint agent build commit cache lint lint schema yaml git static git diagram review draft. Draft lint build primitive token token draft yaml source theme fast lint static review evidence render commit theme. Primitive site diagram spec twin token diagram content render content. Review content review publish lint markdown feed markdown flow. Yaml static commit content static build site token twin site cache git feed fast agent markdown twin.

:::stat-row
::stat{value="29%" label="cache static" source="https://snypd.rocks/bench"}
::stat{value="18ms" label="fast feed" source="https://snypd.rocks/bench"}
:::

## evidence schema draft

Feed review source token source review feed content build site diagram token cache site render schema review. Site draft content feed fast schema render lint publish schema yaml markdown primitive theme yaml. Chart fast chart schema evidence commit render site markdown flow git. Agent spec fast build static spec render lint render content yaml flow token feed git twin schema review.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Lint source diagram static review commit static draft spec evidence chart render." unit="ms"}
- { label: a, value: 7 }
- { label: b, value: 75 }
- { label: c, value: 4 }
- { label: d, value: 95 }
:::

:::diagram{direction="lr" caption="Publish source commit flow review primitive draft content render fast draft fast cache flow schema spec git."}
nodes:
  - { id: md, label: md token }
  - { id: parse, label: parse cache }
  - { id: validate, label: validate build }
  - { id: transform, label: transform fast }
  - { id: render, label: render site }
  - { id: emit, label: emit fast }
  - { id: html, label: html schema }
  - { id: twin, label: twin build }
edges:
  - { from: md, to: parse, label: fast }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: git }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: flow }
  - { from: validate, to: md }
:::

:::faq
### content publish fast spec?
Fast cache flow lint twin render diagram primitive primitive spec flow chart cache static diagram yaml publish evidence evidence.
### spec theme yaml yaml?
Site chart draft draft evidence render feed primitive yaml git lint fast evidence schema theme render chart.
:::

::cta{title="git build source" button="Read the spec" href="https://snypd.rocks/spec"}
