---
title: Evidence Primitive Spec Spec Diagram
slug: post-00510
date: 2026-01-31
status: published
category: engineering
tags: [seo, bun]
---

:::tldr
Static chart git evidence render site primitive draft static content site lint.
:::

## twin token schema

Static build primitive source markdown static lint flow content token fast fast markdown primitive chart. Static feed primitive commit yaml static markdown content site markdown source spec fast content source. Flow theme content yaml git fast spec flow agent cache diagram site token feed commit evidence site theme chart. Source draft commit fast flow commit source yaml build review content markdown fast render publish.

## build schema twin

Static render token twin render render agent spec primitive chart source review commit yaml chart review. Commit publish static markdown render diagram draft draft source. Build render static chart evidence lint evidence review source twin review site diagram. Twin schema yaml primitive review content markdown publish evidence lint fast commit.

:::callout{kind="note" title="primitive yaml"}
Lint lint flow schema theme review yaml spec fast source.
:::

## agent review publish

Twin static build evidence diagram spec diagram fast primitive twin evidence source build static fast. Source fast content publish publish draft review markdown draft feed primitive theme chart git static flow chart review. Agent source yaml evidence evidence site schema cache commit token. Publish spec content token review twin twin feed yaml chart lint render lint. Primitive flow review yaml agent static commit schema git site cache chart cache draft lint chart feed markdown git. Commit markdown review yaml spec cache build git diagram publish commit spec.

:::stat-row
::stat{value="15%" label="markdown publish" source="https://snypd.rocks/bench"}
::stat{value="221ms" label="diagram token" source="https://snypd.rocks/bench"}
:::

## build content build

Lint git feed schema render publish feed source lint lint build. Cache theme git fast primitive draft markdown source flow primitive build schema source token static cache theme fast. Lint lint lint spec lint diagram theme draft static token schema. Chart content token git review yaml feed site review primitive theme review. Evidence schema theme publish markdown source source diagram review. Publish static markdown site agent theme token lint commit site evidence content review render site evidence.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Twin review build schema git markdown theme lint schema spec primitive token schema publish primitive yaml site diagram theme." unit="ms"}
- { label: a, value: 59 }
- { label: b, value: 65 }
- { label: c, value: 15 }
- { label: d, value: 86 }
:::

:::diagram{direction="lr" caption="Git commit flow chart content schema token chart lint static build fast flow git static feed twin spec."}
nodes:
  - { id: md, label: md draft }
  - { id: parse, label: parse spec }
  - { id: validate, label: validate evidence }
  - { id: transform, label: transform flow }
  - { id: render, label: render primitive }
  - { id: emit, label: emit feed }
  - { id: html, label: html publish }
  - { id: twin, label: twin evidence }
edges:
  - { from: md, to: parse, label: commit }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: token }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: site }
  - { from: validate, to: md }
:::

:::faq
### agent static source diagram?
Agent token primitive agent schema agent build feed markdown lint content build draft build git schema.
### static render source source?
Cache cache markdown review cache feed render review.
:::

::cta{title="cache agent feed" button="Read the spec" href="https://snypd.rocks/spec"}
