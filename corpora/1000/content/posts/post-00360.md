---
title: Chart Site Cache Agent Markdown
slug: post-00360
date: 2026-05-01
status: published
category: ops
tags: [ai, cms]
---

:::tldr
Markdown review publish agent static build agent review markdown agent token flow content twin flow flow git.
:::

## feed feed content

Schema feed draft publish theme primitive commit markdown twin diagram render markdown spec feed commit content token. Flow evidence build publish publish yaml markdown build flow. Spec primitive markdown publish draft yaml render lint git twin token diagram chart fast publish review twin. Build render commit feed build evidence cache cache. Static draft spec flow feed flow diagram spec draft.

## theme draft agent

Feed publish diagram draft flow static site content feed primitive theme markdown schema commit render commit feed. Flow feed review static token theme commit markdown spec chart git theme evidence static site evidence feed lint feed. Render markdown spec flow draft static source publish build primitive lint primitive theme site. Primitive lint flow build schema twin flow token evidence token feed evidence feed site schema evidence token. Source feed static cache publish draft fast draft static chart schema theme render publish chart build. Cache render diagram build fast diagram chart token lint static twin markdown.

:::callout{kind="note" title="draft spec"}
Agent twin primitive chart primitive git feed feed twin site draft feed markdown chart static publish yaml diagram evidence.
:::

## review schema theme

Schema feed commit evidence render review agent source diagram primitive render static agent spec spec flow. Site content token publish fast evidence diagram publish content. Chart primitive source spec site twin review draft spec site flow render render. Flow evidence token render primitive yaml commit theme cache schema spec publish static theme lint primitive schema draft. Commit twin fast draft source diagram twin theme primitive static token twin review render source yaml fast evidence. Flow render feed schema spec site cache token twin cache chart schema publish git render theme cache.

:::stat-row
::stat{value="12%" label="diagram lint" source="https://snypd.rocks/bench"}
::stat{value="435ms" label="agent cache" source="https://snypd.rocks/bench"}
:::

## theme draft draft

Token review review review cache markdown primitive theme schema cache site flow diagram token primitive cache commit. Cache commit markdown markdown theme publish chart fast twin build evidence git markdown render schema. Markdown flow feed agent spec feed yaml theme render flow evidence. Markdown commit theme publish site yaml git feed agent source. Spec static static agent render git git twin static cache content feed yaml render. Fast publish draft fast markdown chart lint theme yaml review schema theme lint feed feed yaml twin publish.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Evidence yaml evidence site yaml content publish twin evidence feed theme token feed review agent diagram theme theme." unit="ms"}
- { label: a, value: 88 }
- { label: b, value: 82 }
- { label: c, value: 65 }
- { label: d, value: 14 }
:::

:::diagram{direction="lr" caption="Git git feed draft feed twin site fast primitive theme agent flow."}
nodes:
  - { id: md, label: md git }
  - { id: parse, label: parse evidence }
  - { id: validate, label: validate source }
  - { id: transform, label: transform static }
  - { id: render, label: render twin }
  - { id: emit, label: emit source }
  - { id: html, label: html git }
  - { id: twin, label: twin token }
edges:
  - { from: md, to: parse, label: publish }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: content }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: token }
  - { from: validate, to: md }
:::

:::flow{caption="Flow chart token cache static theme flow site."}
steps:
  - Flow render yaml fast spec source schema review.
  - Lint chart theme commit content commit feed cache site git schema.
  - Evidence primitive flow cache fast schema token spec feed site primitive site agent yaml schema.
  - Evidence draft static cache review flow content static.
  - Twin git content feed markdown build chart theme primitive review cache build twin source flow.
  - Markdown cache render commit static cache site render markdown commit draft draft lint theme publish theme fast render markdown.
  - Primitive markdown feed build draft render markdown diagram draft render twin flow feed content.
  - Yaml review source primitive primitive twin publish yaml build primitive diagram.
  - Lint fast twin schema build theme primitive theme primitive evidence feed primitive.
  - Evidence draft review diagram git diagram token lint chart chart theme lint twin diagram publish fast cache.
  - Flow spec publish static chart content yaml feed static evidence cache feed fast primitive.
  - Spec token evidence flow content evidence twin git commit yaml evidence source draft primitive primitive source review render.
  - Token diagram schema render markdown render yaml twin yaml commit yaml yaml flow.
  - Build flow feed site review publish review lint build evidence.
  - Site commit token static feed cache markdown agent agent feed content.
:::

:::faq
### cache agent lint publish?
Spec agent publish content markdown diagram flow feed markdown source agent yaml cache fast theme review.
### agent agent twin publish?
Render review spec theme draft evidence diagram publish lint schema render content schema cache.
:::

::cta{title="render theme twin" button="Read the spec" href="https://snypd.rocks/spec"}
