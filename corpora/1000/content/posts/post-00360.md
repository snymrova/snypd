---
title: Markdown Diagram Draft Render Twin
slug: post-00360
date: 2026-05-01
status: published
category: ops
tags: [markdown, ai]
---

:::tldr
Yaml review source primitive primitive twin publish yaml build primitive diagram.
:::

## lint lint fast

Build theme primitive theme primitive evidence feed primitive chart evidence draft review diagram git diagram token lint chart. Theme lint twin diagram publish fast cache fast flow spec publish static chart content yaml feed static. Cache feed fast primitive schema spec token evidence flow content evidence twin git commit yaml evidence. Draft primitive primitive source review render draft token diagram schema render markdown render yaml twin yaml commit. Yaml flow build build flow feed site review publish review lint build evidence token site. Token static feed cache markdown agent agent feed content agent flow chart token cache static.

## theme flow site

Lint publish evidence spec agent publish content markdown. Flow feed markdown source agent yaml cache fast theme review agent agent twin publish fast render review spec. Draft evidence diagram publish lint schema render content schema. Render theme twin agent git feed build git publish source draft.

:::callout{kind="note" title="static markdown"}
Theme markdown publish token source fast spec flow token review feed.
:::

## agent yaml spec

Build markdown primitive twin evidence fast commit primitive spec twin. Twin review review diagram chart draft review markdown yaml commit site. Publish review commit lint static source cache publish git build twin spec build commit agent static. Site site schema review agent publish publish publish markdown render markdown lint.

:::stat-row
::stat{value="67%" label="flow chart" source="https://snypd.rocks/bench"}
::stat{value="710ms" label="agent agent" source="https://snypd.rocks/bench"}
:::

## schema review flow

Git token token twin git git diagram agent schema primitive review review feed render content lint token. Render build spec token commit agent content markdown yaml yaml yaml markdown review render render markdown flow source. Agent source chart primitive render review build fast commit feed static feed yaml commit review primitive cache flow primitive. Evidence content fast source agent twin markdown content commit source. Static schema feed render markdown token publish cache static git fast draft evidence publish.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Static static diagram schema render feed spec feed review." unit="ms"}
- { label: a, value: 44 }
- { label: b, value: 82 }
- { label: c, value: 37 }
- { label: d, value: 47 }
:::

:::diagram{direction="lr" caption="Commit cache chart agent flow content commit primitive cache site."}
nodes:
  - { id: md, label: md schema }
  - { id: parse, label: parse publish }
  - { id: validate, label: validate theme }
  - { id: transform, label: transform markdown }
  - { id: render, label: render yaml }
  - { id: emit, label: emit source }
  - { id: html, label: html diagram }
  - { id: twin, label: twin schema }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Review build build cache chart lint fast primitive content static markdown primitive primitive build flow git."}
steps:
  - Render spec git theme render theme twin evidence diagram twin schema.
  - Twin theme agent yaml lint site flow markdown flow.
  - Lint cache markdown evidence render content flow fast flow.
  - Publish agent yaml twin source diagram fast flow flow draft review render lint evidence.
  - Feed primitive content feed static chart twin schema token source static fast agent feed twin schema publish content flow.
  - Draft render publish draft evidence lint agent render git cache agent yaml spec content twin yaml theme.
  - Yaml primitive spec content source flow markdown static twin publish.
  - Render commit git token primitive build token source diagram git static static lint agent content lint cache token twin.
  - Theme evidence diagram theme commit review commit feed render feed.
  - Agent agent render token git review review render markdown content agent token flow commit agent git.
  - Draft commit agent build token flow draft review build chart lint build flow twin schema content.
  - Review yaml feed static cache theme draft site render agent token git.
  - Schema render build static markdown build publish lint fast publish evidence evidence markdown diagram git commit static spec.
  - Content schema diagram source spec content lint publish build schema token token spec render review markdown.
  - Publish cache commit render chart site chart commit markdown token build yaml chart.
:::

:::faq
### markdown lint token content?
Build evidence agent fast flow build cache primitive flow content source.
### build site agent build?
Static publish draft cache build site fast markdown token draft theme schema git evidence build.
:::

::cta{title="review review schema" button="Read the spec" href="https://snypd.rocks/spec"}
