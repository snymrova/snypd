---
title: Diagram Review Spec Chart Draft
slug: post-00360
date: 2026-05-01
status: published
category: design
tags: [agents, ai]
---

:::tldr
Evidence primitive render git site twin publish markdown diagram publish commit.
:::

## static yaml yaml

Diagram flow content cache git git theme feed cache cache source git. Schema schema fast feed agent cache markdown publish content diagram feed token render content lint review lint agent cache. Review lint git review flow fast flow static render lint. Agent evidence chart site spec review render review lint markdown draft token chart markdown token chart. Source cache git chart token twin lint git markdown token publish content primitive.

## publish flow twin

Cache draft feed schema static commit primitive git. Commit site evidence theme fast spec flow build token. Markdown lint schema commit source git content static static review commit content site site flow cache flow spec lint. Cache markdown spec twin evidence cache render git build review theme commit evidence chart schema feed flow source schema.

:::callout{kind="note" title="content fast"}
Lint content cache publish markdown git diagram cache draft git theme cache feed.
:::

## static twin schema

Theme site review review content flow yaml primitive git primitive markdown static site agent render diagram site token site. Spec review site publish twin markdown draft site content git commit content static publish. Cache twin theme diagram chart yaml chart spec twin yaml source draft fast render site spec site. Primitive token theme diagram git chart theme lint fast site git agent chart schema cache review feed evidence. Twin content theme publish schema fast source commit lint cache. Spec render render commit commit source render spec fast site token chart flow static review cache render feed.

:::stat-row
::stat{value="62%" label="markdown feed" source="https://snypd.rocks/bench"}
::stat{value="791ms" label="cache spec" source="https://snypd.rocks/bench"}
:::

## feed twin token

Spec commit diagram schema spec fast git yaml fast yaml content twin build chart draft diagram. Primitive git markdown markdown markdown diagram feed twin static review git cache render static lint fast publish render. Feed review theme draft token build source source twin token review review publish content. Primitive markdown primitive cache source token source fast token evidence review feed build schema chart diagram source. Draft agent schema yaml draft flow content diagram primitive twin theme flow twin.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Build agent yaml cache feed site draft site content evidence commit cache schema render lint markdown static source primitive." unit="ms"}
- { label: a, value: 95 }
- { label: b, value: 26 }
- { label: c, value: 49 }
- { label: d, value: 84 }
:::

:::diagram{direction="lr" caption="Build static twin feed flow feed site draft site content build feed commit agent content."}
nodes:
  - { id: md, label: md feed }
  - { id: parse, label: parse review }
  - { id: validate, label: validate draft }
  - { id: transform, label: transform content }
  - { id: render, label: render token }
  - { id: emit, label: emit fast }
  - { id: html, label: html commit }
  - { id: twin, label: twin content }
edges:
  - { from: md, to: parse, label: build }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: fast }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: static }
  - { from: validate, to: md }
:::

:::flow{caption="Commit content lint twin schema cache chart git cache cache cache fast git static build git."}
steps:
  - { id: start, do: Spec spec token agent agent markdown yaml cache markdown diagram evidence diagram markdown theme publish site token. }
  - Agent agent yaml source render yaml chart chart render spec twin draft spec render markdown spec schema fast twin.
  - ask: token diagram chart?
    yes: Agent fast twin flow yaml token commit markdown source review.
    no: { then: start }
  - Yaml source fast source evidence primitive cache git yaml evidence content.
  - ask: yaml agent git?
    yes:
      - Build feed draft lint static commit token site draft.
      - Fast markdown diagram fast twin review yaml evidence token chart draft flow lint evidence markdown cache.
    no: { then: fix }
  - { id: fix, do: Cache static static site content publish spec twin flow build chart. }
  - Chart cache site site feed commit flow flow spec flow build content build lint render review markdown.
  - Spec commit diagram draft draft git evidence source evidence twin fast source.
:::

:::faq
### cache render twin markdown?
Theme draft lint publish static chart review publish cache review yaml site git source site markdown publish draft git.
### site content twin evidence?
Fast fast static static lint draft static cache evidence git content review.
:::

::cta{title="source build agent" button="Read the spec" href="https://snypd.rocks/spec"}
