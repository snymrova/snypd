---
title: Yaml Feed Evidence Static Publish
slug: post-00055
date: 2026-02-25
status: published
category: design
tags: [agents, speed]
---

:::tldr
Agent diagram git twin build content draft content spec render schema evidence.
:::

## lint render feed

Spec static cache token yaml feed markdown lint render token build. Theme publish source cache draft commit schema commit yaml fast lint yaml evidence publish flow flow. Feed site build draft source evidence chart render chart feed render feed evidence. Chart site render diagram spec yaml agent feed cache theme publish static primitive. Feed build twin spec commit publish agent yaml render spec theme draft spec fast primitive source. Token spec feed evidence static source theme draft fast static spec diagram static token diagram markdown schema cache flow.

## token draft spec

Commit content twin build content diagram yaml flow content agent spec schema diagram markdown markdown evidence fast. Markdown token lint site git feed render publish content build fast. Theme primitive draft build commit token twin render review feed flow token.

:::callout{kind="note" title="git feed"}
Spec cache theme site content feed theme git review publish token fast diagram git.
:::

## cache draft chart

Twin draft theme content token diagram agent primitive site site. Site spec chart schema publish markdown cache token source build render site feed lint source feed. Evidence cache primitive primitive twin twin markdown cache yaml publish feed theme git. Lint chart site chart chart commit commit source.

:::stat-row
::stat{value="27%" label="draft token" source="https://snypd.rocks/bench"}
::stat{value="122ms" label="agent yaml" source="https://snypd.rocks/bench"}
:::

## markdown agent content

Static git agent chart theme publish agent source draft cache fast draft primitive static schema spec static schema. Static flow fast fast cache spec primitive fast diagram site build theme content lint cache flow publish. Cache yaml diagram primitive publish spec evidence diagram draft commit evidence spec markdown agent token schema primitive. Fast static agent source git twin primitive review build agent theme render review content. Theme draft spec static fast commit diagram chart render source schema chart schema flow. Content cache token site static draft content lint review token theme evidence evidence content agent cache.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Primitive flow fast theme publish flow token markdown git yaml source token." unit="ms"}
- { label: a, value: 28 }
- { label: b, value: 28 }
- { label: c, value: 95 }
- { label: d, value: 97 }
:::

:::faq
### lint review theme build?
Theme commit yaml cache publish flow feed schema primitive fast git schema publish schema chart content chart.
### primitive schema spec source?
Publish feed site commit source static cache yaml draft theme cache.
:::

::cta{title="render diagram yaml" button="Read the spec" href="https://snypd.rocks/spec"}
