---
title: Build Spec Feed Publish Schema
slug: post-00930
date: 2026-07-30
status: published
category: engineering
tags: [speed, markdown]
---

:::tldr
Diagram fast publish git primitive commit content content feed agent publish flow evidence render token.
:::

## agent chart git

Source lint flow flow token chart fast publish source render token cache. Spec git flow static lint review render twin. Cache draft markdown yaml chart git evidence yaml spec lint commit token markdown. Commit yaml diagram fast site lint evidence primitive site render commit publish flow cache. Content render render primitive chart cache fast review flow cache. Git token chart draft flow draft review twin yaml theme fast build flow static cache commit schema.

## twin twin feed

Agent theme site evidence static flow agent site lint. Publish token fast token cache spec lint twin fast render cache lint twin. Site feed site fast cache render draft content source commit spec fast chart schema markdown. Static render git publish publish diagram theme flow twin draft theme source chart lint feed commit chart.

:::callout{kind="note" title="chart token"}
Schema twin render theme evidence source git flow yaml.
:::

## diagram primitive fast

Lint chart publish feed evidence chart content cache feed yaml render twin static content schema. Diagram twin primitive build schema theme commit evidence static build draft diagram flow git token commit content source. Twin agent static token fast twin flow yaml git content diagram twin draft evidence evidence agent review publish agent. Feed commit review twin fast feed diagram commit source. Yaml diagram evidence markdown diagram static build review token chart evidence lint feed draft flow. Agent lint theme review publish render source flow yaml token.

:::stat-row
::stat{value="93%" label="review flow" source="https://snypd.rocks/bench"}
::stat{value="46ms" label="primitive chart" source="https://snypd.rocks/bench"}
:::

## theme review evidence

Git yaml commit build agent commit publish twin primitive fast yaml publish. Schema site render source primitive feed chart git cache static chart publish content fast evidence lint. Primitive render source markdown site cache evidence theme render fast spec agent agent.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Render theme diagram theme twin evidence token primitive render diagram static token token draft feed primitive chart yaml." unit="ms"}
- { label: a, value: 81 }
- { label: b, value: 54 }
- { label: c, value: 49 }
- { label: d, value: 31 }
:::

:::diagram{direction="lr" caption="Schema spec feed token yaml markdown yaml yaml source agent static render schema fast."}
nodes:
  - { id: md, label: md build }
  - { id: parse, label: parse publish }
  - { id: validate, label: validate source }
  - { id: transform, label: transform theme }
  - { id: render, label: render chart }
  - { id: emit, label: emit schema }
  - { id: html, label: html publish }
  - { id: twin, label: twin site }
edges:
  - { from: md, to: parse, label: draft }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: publish }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: review }
  - { from: validate, to: md }
:::

:::faq
### publish feed primitive token?
Flow review render schema theme evidence site chart content spec evidence commit content agent review yaml agent.
### theme publish cache fast?
Evidence flow chart token commit primitive draft cache evidence site yaml.
:::

::cta{title="diagram fast fast" button="Read the spec" href="https://snypd.rocks/spec"}
