---
title: Content Schema Build Content Lint
slug: post-00930
date: 2026-07-30
status: published
category: engineering
tags: [agents, markdown]
---

:::tldr
Token content review chart schema chart yaml content fast content git fast.
:::

## twin static twin

Draft render cache build feed cache publish yaml feed content lint. Cache theme markdown spec diagram lint diagram publish markdown content git schema spec evidence evidence static commit evidence theme. Draft flow twin render source cache site commit token site token. Commit cache spec source review site source commit publish render source markdown site. Content chart lint commit source agent fast spec yaml build. Lint commit evidence chart theme build fast diagram spec evidence evidence theme chart twin agent site twin fast diagram.

## lint spec source

Diagram theme commit flow primitive evidence diagram render commit cache source token twin. Cache yaml cache agent commit flow theme chart. Chart commit content review review review static render git schema schema source diagram draft yaml site yaml. Lint git primitive git agent feed markdown agent markdown commit yaml content spec schema git primitive spec. Static flow twin twin theme build feed twin yaml theme twin git lint yaml feed commit evidence content.

:::callout{kind="note" title="content diagram"}
Spec yaml git site fast schema markdown review.
:::

## token token evidence

Twin yaml cache cache site primitive agent site commit source schema git twin git content spec twin. Source chart twin cache static flow build git. Build token publish twin source markdown cache token diagram publish fast content diagram publish draft.

:::stat-row
::stat{value="30%" label="feed yaml" source="https://snypd.rocks/bench"}
::stat{value="895ms" label="source cache" source="https://snypd.rocks/bench"}
:::

## yaml source yaml

Review yaml build primitive markdown draft yaml schema schema cache source flow. Chart theme primitive source spec draft source draft primitive cache feed commit render render agent static commit markdown review. Yaml cache site evidence fast chart static feed fast schema primitive lint agent theme commit draft. Publish theme site review build feed yaml primitive feed cache flow spec. Feed cache commit commit markdown fast yaml draft content spec token build static build draft. Cache token feed site publish site yaml publish static.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Flow source lint lint schema diagram site git." unit="ms"}
- { label: a, value: 72 }
- { label: b, value: 21 }
- { label: c, value: 87 }
- { label: d, value: 44 }
:::

:::diagram{direction="lr" caption="Fast commit schema render chart token spec fast fast spec evidence diagram diagram."}
nodes:
  - { id: md, label: md draft }
  - { id: parse, label: parse primitive }
  - { id: validate, label: validate chart }
  - { id: transform, label: transform publish }
  - { id: render, label: render diagram }
  - { id: emit, label: emit review }
  - { id: html, label: html yaml }
  - { id: twin, label: twin markdown }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### evidence agent static evidence?
Yaml cache commit token chart spec build agent schema publish build theme agent primitive chart markdown diagram.
### draft publish twin draft?
Draft lint markdown markdown yaml content twin lint.
:::

::cta{title="lint git review" button="Read the spec" href="https://snypd.rocks/spec"}
