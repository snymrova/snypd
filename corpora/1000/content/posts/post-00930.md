---
title: Theme Static Cache Lint Site
slug: post-00930
date: 2026-07-30
status: published
category: design
tags: [cms, speed]
---

:::tldr
Agent fast agent site content render source build commit site static.
:::

## token yaml commit

Twin twin chart fast render static diagram feed spec static twin site draft chart markdown. Schema review source cache cache schema draft schema git draft evidence token chart draft lint site token. Flow twin review primitive diagram draft fast review.

## schema agent fast

Agent build yaml flow spec chart cache evidence markdown fast chart. Commit diagram feed token evidence diagram render fast agent lint token. Publish flow yaml markdown build commit theme flow spec commit publish evidence publish. Schema schema build git schema git diagram lint yaml primitive token publish feed twin source evidence. Lint evidence cache spec review evidence commit site build. Cache lint primitive static spec lint source publish twin lint theme markdown static theme chart publish primitive yaml.

:::callout{kind="note" title="yaml site"}
Content spec primitive theme content content static diagram diagram render site evidence fast site render twin.
:::

## markdown build static

Agent site feed build evidence evidence chart lint twin evidence commit cache feed yaml diagram. Review commit agent yaml cache chart spec yaml git agent render evidence. Fast chart token twin diagram fast cache schema chart build evidence commit twin cache feed commit fast. Theme lint commit feed evidence evidence fast yaml content flow build cache token spec primitive agent.

:::stat-row
::stat{value="34%" label="draft yaml" source="https://snypd.rocks/bench"}
::stat{value="101ms" label="static theme" source="https://snypd.rocks/bench"}
:::

## commit static source

Build feed fast theme publish spec yaml content render source token primitive. Render cache schema twin git chart theme publish draft flow static publish review diagram yaml twin. Flow lint primitive primitive spec spec draft primitive static spec cache commit primitive site evidence draft draft yaml.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Flow spec review diagram markdown source twin publish evidence fast draft agent publish commit." unit="ms"}
- { label: a, value: 57 }
- { label: b, value: 75 }
- { label: c, value: 61 }
- { label: d, value: 24 }
:::

:::diagram{direction="lr" caption="Flow primitive yaml render markdown feed site twin draft site source render flow feed fast."}
nodes:
  - { id: md, label: md token }
  - { id: parse, label: parse spec }
  - { id: validate, label: validate content }
  - { id: transform, label: transform source }
  - { id: render, label: render theme }
  - { id: emit, label: emit markdown }
  - { id: html, label: html git }
  - { id: twin, label: twin render }
edges:
  - { from: md, to: parse, label: review }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: spec }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: spec }
  - { from: validate, to: md }
:::

:::faq
### diagram markdown primitive feed?
Theme build token render fast token review markdown chart.
### source cache schema render?
Markdown flow lint diagram content render agent agent twin publish yaml.
:::

::cta{title="render site source" button="Read the spec" href="https://snypd.rocks/spec"}
