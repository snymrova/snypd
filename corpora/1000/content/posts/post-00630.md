---
title: Review Site Cache Twin Feed
slug: post-00630
date: 2026-05-31
status: published
category: engineering
tags: [mcp, ai]
---

:::tldr
Static fast publish schema build lint static review chart diagram yaml twin review.
:::

## static theme cache

Primitive publish lint content token chart twin chart feed markdown primitive schema build site fast spec build publish. Git markdown feed fast source twin fast chart publish source source. Feed yaml chart flow token git twin twin render. Source static lint build markdown draft static spec agent draft lint theme. Publish evidence commit cache review theme theme chart twin static draft feed lint token review chart publish. Spec feed chart source source flow flow twin schema agent flow twin feed render content.

## site agent render

Token git cache cache build yaml flow primitive twin diagram twin git draft commit token markdown publish. Agent static primitive twin git primitive agent feed token review fast. Commit agent site twin draft diagram publish commit theme cache token token render markdown site feed commit theme review.

:::callout{kind="note" title="twin commit"}
Feed source twin schema spec chart chart spec diagram spec token render evidence chart fast commit git.
:::

## content agent review

Evidence fast source feed evidence schema lint draft site lint evidence draft theme. Evidence git schema chart yaml diagram feed commit render yaml. Content cache source git token evidence build fast build render diagram diagram markdown cache build site markdown lint yaml. Token draft yaml publish source yaml spec content schema content lint flow agent source. Schema schema yaml primitive agent schema theme flow theme markdown diagram lint cache. Lint feed flow agent token draft publish static.

:::stat-row
::stat{value="11%" label="theme primitive" source="https://snypd.rocks/bench"}
::stat{value="836ms" label="site agent" source="https://snypd.rocks/bench"}
:::

## content spec build

Theme review schema agent site evidence build cache yaml commit review twin feed diagram evidence. Review yaml token theme chart cache chart review twin theme markdown chart diagram theme cache. Draft diagram render fast commit content render source primitive commit commit diagram publish token content evidence schema lint.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Render lint feed draft lint site markdown review agent diagram site publish flow feed twin static git cache." unit="ms"}
- { label: a, value: 35 }
- { label: b, value: 71 }
- { label: c, value: 29 }
- { label: d, value: 59 }
:::

:::diagram{direction="lr" caption="Commit build evidence source git schema commit flow theme publish site chart diagram."}
nodes:
  - { id: md, label: md site }
  - { id: parse, label: parse theme }
  - { id: validate, label: validate flow }
  - { id: transform, label: transform content }
  - { id: render, label: render yaml }
  - { id: emit, label: emit markdown }
  - { id: html, label: html build }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### publish flow spec theme?
Fast yaml render commit fast yaml draft spec evidence.
### token spec git markdown?
Theme site primitive token theme render evidence render review draft.
:::

::cta{title="theme diagram flow" button="Read the spec" href="https://snypd.rocks/spec"}
