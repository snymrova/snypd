---
title: Commit Build Review Theme Render
slug: post-00630
date: 2026-05-31
status: published
category: engineering
tags: [speed, cms]
---

:::tldr
Twin markdown token schema token review cache evidence lint token evidence agent.
:::

## diagram static twin

Twin publish source content draft source yaml review primitive content theme source twin chart. Agent draft git theme diagram git site publish. Markdown diagram review markdown diagram primitive git spec content. Diagram spec git flow site content yaml cache markdown yaml chart primitive agent cache source spec. Build draft theme static render yaml render flow static diagram spec publish evidence agent git publish flow. Commit primitive lint static agent primitive spec static yaml lint agent token primitive content git draft publish.

## token lint review

Agent build yaml review commit render twin commit spec lint flow git commit feed source cache diagram. Site source render git yaml review twin diagram content static flow theme build. Primitive token spec chart content lint diagram lint. Draft flow token spec cache draft cache static.

:::callout{kind="note" title="site publish"}
Review content publish token review render draft primitive site token.
:::

## git content agent

Draft schema review theme render schema flow twin. Render site build git agent site render render. Evidence cache site diagram spec draft build feed flow lint.

:::stat-row
::stat{value="64%" label="yaml review" source="https://snypd.rocks/bench"}
::stat{value="42ms" label="static markdown" source="https://snypd.rocks/bench"}
:::

## schema twin cache

Source flow evidence site evidence publish render primitive schema commit lint build yaml source diagram lint review agent primitive. Schema render draft diagram token content draft cache feed content cache render build spec theme. Fast diagram draft yaml evidence lint cache draft git review cache content source token.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Diagram yaml build markdown content spec yaml evidence render schema flow site cache evidence flow content." unit="ms"}
- { label: a, value: 89 }
- { label: b, value: 82 }
- { label: c, value: 98 }
- { label: d, value: 95 }
:::

:::diagram{direction="lr" caption="Theme git static diagram token source commit feed token lint site agent markdown primitive."}
nodes:
  - { id: md, label: md fast }
  - { id: parse, label: parse chart }
  - { id: validate, label: validate lint }
  - { id: transform, label: transform static }
  - { id: render, label: render flow }
  - { id: emit, label: emit source }
  - { id: html, label: html markdown }
  - { id: twin, label: twin draft }
edges:
  - { from: md, to: parse, label: git }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: feed }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: primitive }
  - { from: validate, to: md }
:::

:::faq
### primitive yaml twin content?
Theme cache agent feed diagram content diagram flow theme flow chart markdown token static theme.
### publish build chart token?
Content commit primitive flow render feed diagram render token feed review markdown diagram source token cache primitive publish.
:::

::cta{title="static lint yaml" button="Read the spec" href="https://snypd.rocks/spec"}
