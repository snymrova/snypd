---
title: Content Twin Token Render Cache
slug: post-00070
date: 2026-03-12
status: published
category: ops
tags: [speed, seo]
---

:::tldr
Diagram agent draft chart spec source yaml review static render.
:::

## yaml review theme

Fast theme agent spec markdown flow diagram render build static review publish spec git render source publish yaml. Commit schema yaml yaml agent token draft feed site source cache flow render token cache yaml feed diagram. Commit source review render cache primitive flow fast agent. Schema agent content draft fast static commit yaml content draft primitive fast chart chart source static twin. Diagram lint git twin publish primitive yaml schema markdown chart flow flow.

## publish evidence theme

Review git twin publish site schema content static twin render static flow fast. Render token primitive fast theme primitive theme schema source spec fast feed schema commit yaml. Agent spec commit lint git review render spec primitive theme. Primitive fast render publish draft agent site markdown git theme. Agent schema static draft evidence build static flow chart build. Git agent build publish flow flow flow review.

:::callout{kind="note" title="git chart"}
Evidence feed yaml diagram static feed review agent.
:::

## schema primitive flow

Build cache evidence lint render source diagram spec diagram source spec chart primitive content evidence commit source static. Cache site build commit site build twin commit primitive spec schema yaml review source. Twin content spec evidence publish render content spec site feed twin schema diagram schema.

:::stat-row
::stat{value="50%" label="spec token" source="https://snypd.rocks/bench"}
::stat{value="0ms" label="git publish" source="https://snypd.rocks/bench"}
:::

## agent content agent

Source review agent markdown spec cache chart commit. Chart lint site flow theme diagram static static spec feed spec content. Fast draft theme yaml review chart diagram flow lint fast publish content theme theme diagram. Fast publish static render static build schema feed fast draft chart fast static markdown agent cache token. Lint cache agent publish feed agent site spec diagram. Twin fast feed build chart cache diagram git spec flow token feed review site primitive fast flow commit.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Static token schema spec publish render content lint site diagram review twin feed review yaml diagram twin." unit="ms"}
- { label: a, value: 99 }
- { label: b, value: 74 }
- { label: c, value: 36 }
- { label: d, value: 56 }
:::

:::diagram{direction="lr" caption="Fast lint render theme token fast lint cache site evidence build yaml content schema yaml."}
nodes:
  - { id: md, label: md source }
  - { id: parse, label: parse flow }
  - { id: validate, label: validate yaml }
  - { id: transform, label: transform twin }
  - { id: render, label: render build }
  - { id: emit, label: emit commit }
  - { id: html, label: html draft }
  - { id: twin, label: twin static }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### commit token schema evidence?
Theme static source fast theme lint diagram content.
### yaml token git git?
Lint render content build diagram schema fast feed theme render build spec draft diagram feed agent markdown source token.
:::

::cta{title="primitive agent feed" button="Read the spec" href="https://snypd.rocks/spec"}
