---
title: Fast Theme Agent Spec Markdown
slug: post-00070
date: 2026-03-12
status: published
category: ops
tags: [speed, agents]
---

:::tldr
Static review publish spec git render source publish yaml flow.
:::

## commit schema yaml

Token draft feed site source cache flow render. Cache yaml feed diagram content commit source review render cache primitive. Fast agent source schema agent content draft fast static commit yaml content draft primitive fast chart chart source. Twin publish diagram lint git twin publish primitive yaml schema markdown chart flow flow. Evidence theme flow site review git twin publish site schema content static.

## twin render static

Git render token primitive fast theme primitive theme schema source spec fast feed schema. Yaml build agent spec commit lint git review render spec primitive theme primitive primitive fast render. Draft agent site markdown git theme primitive agent schema static draft evidence build. Flow chart build markdown git agent build publish flow flow flow review git chart. Evidence feed yaml diagram static feed review agent. Primitive flow render schema build cache evidence lint render source diagram spec diagram source spec chart primitive content.

:::callout{kind="note" title="evidence commit"}
Static static cache site build commit site build twin commit primitive spec schema yaml review source yaml.
:::

## twin content spec

Render content spec site feed twin schema diagram schema static spec token agent. Publish agent content agent schema agent source review agent markdown spec cache chart commit publish. Lint site flow theme diagram static static spec feed spec content commit fast draft theme yaml review. Diagram flow lint fast publish content theme theme diagram diagram fast publish static render static build schema. Fast draft chart fast static markdown agent cache token primitive lint cache agent publish feed agent site spec diagram.

:::stat-row
::stat{value="83%" label="twin fast" source="https://snypd.rocks/bench"}
::stat{value="841ms" label="build chart" source="https://snypd.rocks/bench"}
:::

## cache diagram git

Token feed review site primitive fast flow commit diagram static token schema spec publish render content lint site. Review twin feed review yaml diagram twin twin evidence lint fast git fast lint render theme token fast. Cache site evidence build yaml content schema yaml source flow yaml twin. Commit draft static commit token schema evidence content theme static.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Fast theme lint diagram content yaml token git git twin lint render content build diagram schema fast." unit="ms"}
- { label: a, value: 95 }
- { label: b, value: 11 }
- { label: c, value: 20 }
- { label: d, value: 24 }
:::

:::diagram{direction="lr" caption="Draft diagram feed agent markdown source token primitive agent feed flow."}
nodes:
  - { id: md, label: md twin }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate agent }
  - { id: transform, label: transform source }
  - { id: render, label: render git }
  - { id: emit, label: emit site }
  - { id: html, label: html spec }
  - { id: twin, label: twin site }
edges:
  - { from: md, to: parse, label: spec }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: evidence }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: draft }
  - { from: validate, to: md }
:::

:::faq
### source yaml theme markdown?
Draft lint review content token agent fast agent review.
### build review yaml primitive?
Render spec content source source render agent token token publish twin fast source evidence site diagram.
:::

::cta{title="yaml flow yaml" button="Read the spec" href="https://snypd.rocks/spec"}
