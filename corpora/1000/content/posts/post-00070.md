---
title: Markdown Theme Site Flow Diagram
slug: post-00070
date: 2026-03-12
status: published
category: product
tags: [ai, ai]
---

:::tldr
Chart primitive yaml render agent build static feed commit build lint source flow twin lint cache build.
:::

## cache fast draft

Fast content cache draft chart schema commit git review lint flow content schema content spec. Static commit static twin draft twin flow publish feed commit site site evidence build feed review static diagram. Draft flow content flow theme markdown build render twin content render diagram build build spec agent primitive primitive twin. Draft lint build feed markdown draft primitive source cache.

## fast review agent

Twin site commit twin render draft cache draft theme build build feed twin build. Cache content agent content agent feed source render build twin render cache content theme content static site git static. Lint build yaml site render review feed diagram review spec site git site site static flow. Chart build draft evidence agent site schema lint spec.

:::callout{kind="note" title="cache fast"}
Diagram source static spec spec evidence draft render.
:::

## review static token

Chart render theme markdown review render twin spec fast twin markdown evidence feed chart site draft agent evidence. Evidence content content schema yaml yaml agent site render site diagram theme cache schema feed git. Publish feed agent theme site token feed draft twin. Feed build source draft theme source primitive yaml static draft evidence source. Markdown build build primitive render render content evidence content primitive fast fast publish publish yaml site source commit evidence.

:::stat-row
::stat{value="56%" label="content diagram" source="https://snypd.rocks/bench"}
::stat{value="123ms" label="chart static" source="https://snypd.rocks/bench"}
:::

## feed render chart

Git git theme site build static yaml content twin token render cache schema flow commit build diagram agent draft. Spec source yaml review static render yaml review theme git flow fast theme agent spec markdown flow. Render build static review publish spec git render source publish yaml flow commit schema yaml yaml agent. Draft feed site source cache flow render token cache yaml feed.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Content commit source review render cache primitive flow fast agent source schema agent content draft fast static commit." unit="ms"}
- { label: a, value: 60 }
- { label: b, value: 8 }
- { label: c, value: 44 }
- { label: d, value: 16 }
:::

:::diagram{direction="lr" caption="Chart chart source static twin publish diagram lint git twin publish primitive yaml schema."}
nodes:
  - { id: md, label: md markdown }
  - { id: parse, label: parse chart }
  - { id: validate, label: validate flow }
  - { id: transform, label: transform flow }
  - { id: render, label: render publish }
  - { id: emit, label: emit evidence }
  - { id: html, label: html theme }
  - { id: twin, label: twin flow }
edges:
  - { from: md, to: parse, label: site }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: review }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: git }
  - { from: validate, to: md }
:::

:::faq
### twin publish site schema?
Static twin render static flow fast git render.
### token primitive fast theme?
Theme schema source spec fast feed schema commit yaml build.
:::

::cta{title="agent spec commit" button="Read the spec" href="https://snypd.rocks/spec"}
