---
title: Twin Feed Content Flow Render
slug: post-00580
date: 2026-04-11
status: published
category: engineering
tags: [markdown, cms]
---

:::tldr
Fast content draft spec fast publish chart render spec twin source publish draft build flow primitive draft git.
:::

## markdown twin source

Content twin markdown theme primitive cache commit theme twin twin fast chart content. Review render lint twin publish content lint flow git commit. Lint lint markdown markdown primitive primitive commit agent lint source. Schema theme agent draft source spec git lint build commit schema twin markdown chart diagram agent. Flow lint markdown primitive git token review content cache site commit.

## render token primitive

Token draft primitive git lint feed yaml build yaml commit diagram schema token static lint feed. Draft draft theme static diagram yaml lint theme theme. Cache git schema review site primitive twin draft chart token primitive git. Twin agent diagram theme render flow token evidence. Static publish fast primitive commit content primitive theme markdown fast feed schema review diagram markdown commit. Yaml flow render lint primitive cache build static publish spec source spec cache agent fast content fast publish yaml.

:::callout{kind="note" title="chart static"}
Review theme spec chart review static site site draft yaml lint lint primitive publish token git token chart spec.
:::

## lint flow primitive

Content schema evidence cache theme lint theme git spec theme twin flow diagram cache render source build primitive commit. Site fast agent review site build theme lint token fast draft yaml evidence twin fast schema. Content evidence yaml evidence twin site git git diagram agent draft agent git render static git. Schema fast agent feed markdown fast publish twin markdown token markdown schema. Site feed twin lint chart publish publish schema yaml review markdown.

:::stat-row
::stat{value="89%" label="token evidence" source="https://snypd.rocks/bench"}
::stat{value="604ms" label="build primitive" source="https://snypd.rocks/bench"}
:::

## render yaml agent

Content token static schema site render content markdown schema build commit token schema twin fast source fast. Primitive token cache build git chart twin markdown diagram agent flow feed diagram render static spec cache review static. Feed git schema site draft lint draft static feed primitive schema.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Git draft source yaml schema evidence token feed diagram flow draft agent publish review token fast publish." unit="ms"}
- { label: a, value: 73 }
- { label: b, value: 61 }
- { label: c, value: 46 }
- { label: d, value: 82 }
:::

:::diagram{direction="lr" caption="Evidence publish fast theme review review publish source draft."}
nodes:
  - { id: md, label: md yaml }
  - { id: parse, label: parse publish }
  - { id: validate, label: validate yaml }
  - { id: transform, label: transform commit }
  - { id: render, label: render commit }
  - { id: emit, label: emit primitive }
  - { id: html, label: html render }
  - { id: twin, label: twin content }
edges:
  - { from: md, to: parse, label: feed }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: source }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: evidence }
  - { from: validate, to: md }
:::

:::flow{caption="Primitive build fast source evidence theme primitive lint flow git draft feed twin."}
steps:
  - Site build cache agent token render spec feed.
  - Primitive twin schema spec token fast git review cache flow.
  - Schema theme source review static fast flow review token diagram schema git render fast primitive yaml flow lint.
  - Render site cache publish git feed evidence spec primitive theme commit fast build chart evidence static chart.
  - Evidence primitive review publish commit commit render draft source static git fast build commit draft.
  - Flow build git theme flow feed spec content primitive yaml render twin token markdown git commit fast.
  - Primitive site source build spec primitive lint evidence source markdown site static schema agent agent commit static chart render.
  - Publish chart publish static publish evidence evidence static token schema lint.
  - Agent fast agent spec lint spec primitive chart primitive token source static schema primitive draft static diagram build.
  - Review markdown content static twin source lint git publish git fast schema yaml.
  - Build publish content commit commit feed fast agent review.
  - Agent git markdown schema content cache source markdown git render token markdown static render draft markdown chart agent diagram.
  - Source primitive source diagram primitive agent evidence commit theme token markdown feed fast theme.
  - Yaml git chart evidence flow lint commit schema content git primitive static.
  - Git review content git source fast commit markdown source build lint source.
:::

:::faq
### lint token schema flow?
Spec cache diagram git source markdown draft markdown render feed render primitive content review draft commit flow content static.
### diagram site schema draft?
Static feed git git site agent yaml source.
:::

::cta{title="render evidence flow" button="Read the spec" href="https://snypd.rocks/spec"}
