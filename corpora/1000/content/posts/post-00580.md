---
title: Twin Feed Diagram Cache Static
slug: post-00580
date: 2026-04-11
status: published
category: product
tags: [agents, bun]
---

:::tldr
Lint spec feed token flow publish twin evidence static render twin.
:::

## spec markdown render

Theme twin review commit build agent fast review lint twin feed content flow render markdown twin fast schema. Content draft spec fast publish chart render spec twin source publish draft build flow. Draft git markdown twin source evidence site content twin. Theme primitive cache commit theme twin twin fast.

## chart content primitive

Lint twin publish content lint flow git commit build lint. Markdown markdown primitive primitive commit agent lint source commit schema theme agent. Source spec git lint build commit schema twin markdown chart diagram agent token. Lint markdown primitive git token review content cache site commit render token primitive feed evidence token draft primitive. Lint feed yaml build yaml commit diagram schema token static lint feed theme draft draft.

:::callout{kind="note" title="theme static"}
Yaml lint theme theme lint cache git schema review site primitive twin draft chart token primitive git agent.
:::

## twin agent diagram

Flow token evidence review static publish fast primitive commit content. Theme markdown fast feed schema review diagram markdown commit. Yaml flow render lint primitive cache build static publish spec source spec cache agent fast content fast publish yaml.

:::stat-row
::stat{value="80%" label="static feed" source="https://snypd.rocks/bench"}
::stat{value="635ms" label="theme spec" source="https://snypd.rocks/bench"}
:::

## chart review static

Draft yaml lint lint primitive publish token git token chart spec lint flow. Static twin content schema evidence cache theme lint theme. Spec theme twin flow diagram cache render source build primitive commit commit site fast agent. Site build theme lint token fast draft yaml evidence twin fast schema review content evidence yaml.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Twin site git git diagram agent draft agent git render static git spec schema fast agent." unit="ms"}
- { label: a, value: 94 }
- { label: b, value: 7 }
- { label: c, value: 54 }
- { label: d, value: 40 }
:::

:::diagram{direction="lr" caption="Markdown token markdown schema token site feed twin lint chart publish publish schema yaml review markdown schema token evidence."}
nodes:
  - { id: md, label: md commit }
  - { id: parse, label: parse build }
  - { id: validate, label: validate primitive }
  - { id: transform, label: transform render }
  - { id: render, label: render yaml }
  - { id: emit, label: emit agent }
  - { id: html, label: html markdown }
  - { id: twin, label: twin chart }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Agent commit static chart render cache publish chart."}
steps:
  - Token static schema site render content markdown schema.
  - Commit token schema twin fast source fast feed primitive token.
  - Build git chart twin markdown diagram agent flow feed diagram render.
  - Spec cache review static cache feed git schema site draft lint draft static feed.
  - Schema source git draft source yaml schema evidence token.
  - Diagram flow draft agent publish review token fast publish evidence git draft diagram primitive evidence publish fast theme review.
  - Publish source draft yaml publish yaml commit commit primitive render content feed source evidence markdown site.
  - Cache agent token render spec feed build primitive twin schema.
  - Token fast git review cache flow flow schema theme source review static.
  - Flow review token diagram schema git render fast primitive yaml flow lint chart render.
  - Cache publish git feed evidence spec primitive theme commit fast build chart evidence.
  - Chart commit evidence primitive review publish commit commit render draft source static git fast.
  - Commit draft diagram flow build git theme flow feed spec.
  - Primitive yaml render twin token markdown git commit.
  - Feed primitive site source build spec primitive lint evidence source markdown site static schema.
:::

:::faq
### publish static publish evidence?
Static token schema lint diagram agent fast agent spec lint spec primitive chart primitive token source.
### static schema primitive draft?
Diagram build site review markdown content static twin source lint git publish git fast.
:::

::cta{title="schema yaml content" button="Read the spec" href="https://snypd.rocks/spec"}
