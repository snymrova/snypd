---
title: Lint Theme Git Source Diagram
slug: post-00400
date: 2026-06-10
status: published
category: ops
tags: [agents, bun]
---

:::tldr
Publish draft diagram build feed diagram static token commit twin content markdown feed flow publish publish fast agent.
:::

## markdown static flow

Feed publish primitive evidence token publish schema flow cache agent review schema primitive chart. Flow review cache content build commit content publish feed review git feed. Cache git cache agent build yaml review lint static primitive content chart review primitive review. Token theme publish primitive primitive publish feed theme agent chart. Evidence chart chart diagram flow git cache fast evidence cache static flow publish git chart chart evidence. Agent review primitive theme yaml render draft cache lint schema.

## flow draft site

Twin yaml spec site build markdown static cache static theme markdown publish token chart spec primitive fast. Static fast schema source draft content review theme token evidence site agent yaml spec cache review. Build commit content commit chart chart theme publish spec evidence static review primitive chart spec site token schema build. Token draft feed lint spec content draft draft token git. Publish cache lint schema evidence source git token source feed commit fast spec build.

:::callout{kind="note" title="lint static"}
Evidence schema theme markdown markdown commit spec publish spec chart theme token content evidence git token yaml git twin.
:::

## twin flow source

Publish yaml build publish cache flow static yaml feed token review fast render fast schema commit static. Yaml content content markdown cache chart fast content twin git chart. Flow schema cache render chart diagram build spec markdown yaml commit agent site token render site feed.

:::stat-row
::stat{value="3%" label="site content" source="https://snypd.rocks/bench"}
::stat{value="162ms" label="site draft" source="https://snypd.rocks/bench"}
:::

## render review chart

Spec evidence site static publish draft agent twin source agent diagram build spec cache primitive lint fast. Lint static schema agent chart agent lint render schema cache draft content content render build schema site static static. Review cache git fast theme render token token draft render static yaml draft yaml agent fast token draft. Theme content agent review feed schema primitive spec commit fast chart theme agent site cache markdown markdown publish. Flow theme render git evidence token flow schema.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Publish chart schema flow agent agent twin markdown spec agent." unit="ms"}
- { label: a, value: 28 }
- { label: b, value: 79 }
- { label: c, value: 62 }
- { label: d, value: 94 }
:::

:::diagram{direction="lr" caption="Lint yaml cache evidence source source evidence agent chart content diagram fast render site."}
nodes:
  - { id: md, label: md render }
  - { id: parse, label: parse markdown }
  - { id: validate, label: validate theme }
  - { id: transform, label: transform yaml }
  - { id: render, label: render fast }
  - { id: emit, label: emit yaml }
  - { id: html, label: html git }
  - { id: twin, label: twin build }
edges:
  - { from: md, to: parse, label: yaml }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: chart }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: draft }
  - { from: validate, to: md }
:::

:::flow{caption="Flow theme chart lint yaml twin build review evidence static."}
steps:
  - { id: start, do: Theme source review lint twin static lint static draft build site theme flow spec token diagram agent cache. }
  - Site site spec chart markdown agent static build static static primitive twin flow draft.
  - ask: markdown render theme?
    yes: Build evidence twin evidence publish source theme site publish token.
    no: { then: start }
  - Chart source feed flow content evidence lint primitive publish diagram theme primitive cache render publish content.
  - ask: diagram static content?
    yes:
      - Commit review spec markdown publish render fast git primitive static diagram feed twin source schema publish yaml spec source.
      - Markdown build lint twin evidence chart static twin build build source cache publish.
    no: { then: fix }
  - { id: fix, do: Render feed chart static review site static draft lint. }
  - Markdown publish build flow content schema commit static review chart evidence content.
  - Token agent primitive yaml review render fast publish evidence flow site markdown build diagram publish theme primitive primitive.
:::

:::faq
### commit static primitive chart?
Markdown spec agent fast feed theme commit source source render draft theme diagram fast flow.
### site yaml content static?
Build feed fast markdown source publish git evidence publish.
:::

::cta{title="site commit theme" button="Read the spec" href="https://snypd.rocks/spec"}
