---
title: Flow Flow Evidence Publish Review
slug: post-00400
date: 2026-06-10
status: published
category: ops
tags: [seo, seo]
---

:::tldr
Feed fast twin publish primitive chart spec render token render chart agent feed source token commit agent content.
:::

## schema chart commit

Git render build render chart diagram yaml lint markdown render publish draft spec evidence token feed. Content schema feed commit git markdown diagram schema diagram token feed. Build twin markdown static commit source diagram build markdown markdown theme lint agent evidence.

## lint evidence review

Cache build publish chart source evidence publish publish token spec review flow primitive. Site primitive cache primitive fast git markdown fast yaml cache render git content static. Evidence static cache static fast flow draft static build. Schema static site site spec markdown chart schema diagram schema. Theme draft fast lint commit review commit render draft review static publish markdown commit.

:::callout{kind="note" title="fast token"}
Content review commit cache static evidence evidence schema site commit source build yaml evidence evidence content build.
:::

## lint token theme

Twin theme theme content twin theme cache flow agent render schema commit primitive diagram site spec. Build markdown static draft flow lint commit cache draft source site agent static diagram fast review agent git flow. Render content git cache site token static agent markdown site feed build static publish cache.

:::stat-row
::stat{value="77%" label="cache site" source="https://snypd.rocks/bench"}
::stat{value="875ms" label="commit review" source="https://snypd.rocks/bench"}
:::

## diagram schema cache

Source commit render evidence lint evidence render publish token. Publish cache twin draft commit spec build yaml. Site render source diagram primitive source flow markdown git git cache source git git. Git spec schema schema site static theme agent markdown lint markdown. Fast schema evidence agent static markdown twin twin flow site feed theme chart fast token draft. Primitive flow static yaml agent commit evidence twin static site markdown agent markdown source build render agent.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Evidence token render source commit draft cache twin agent commit." unit="ms"}
- { label: a, value: 70 }
- { label: b, value: 79 }
- { label: c, value: 23 }
- { label: d, value: 88 }
:::

:::diagram{direction="lr" caption="Feed diagram agent lint source agent twin commit evidence markdown primitive."}
nodes:
  - { id: md, label: md source }
  - { id: parse, label: parse static }
  - { id: validate, label: validate token }
  - { id: transform, label: transform spec }
  - { id: render, label: render theme }
  - { id: emit, label: emit build }
  - { id: html, label: html render }
  - { id: twin, label: twin review }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Yaml theme static theme diagram spec token primitive diagram publish markdown draft lint diagram theme twin commit."}
steps:
  - Git source theme publish site source source chart render markdown.
  - Git content content cache agent build static agent token yaml diagram git evidence site diagram chart review chart feed.
  - Content feed token yaml review cache build feed fast chart yaml cache review source spec.
  - Feed publish draft fast chart draft yaml cache commit build fast static.
  - Token static flow feed content draft diagram render schema yaml schema fast primitive static markdown commit.
  - Source review content review agent render primitive draft token site git schema flow draft diagram.
  - Build fast fast publish cache schema lint diagram chart draft site build flow feed publish publish source token commit.
  - Source flow publish agent token schema feed theme fast agent agent chart agent schema cache yaml.
  - Cache render static evidence feed commit yaml build publish lint markdown token evidence content theme.
  - Lint render schema token schema feed site review flow flow yaml cache.
  - Spec chart fast theme yaml source git flow source feed review git.
  - Draft markdown commit cache site diagram publish publish content schema feed yaml schema primitive twin publish flow publish.
  - Agent site commit site feed markdown render schema markdown cache flow chart publish spec yaml draft fast draft fast.
  - Diagram theme theme commit schema review publish theme schema fast.
  - Publish schema git theme content draft source commit yaml spec flow yaml theme lint schema feed publish.
:::

:::faq
### fast flow flow twin?
Flow agent flow publish token draft primitive diagram feed token.
### build feed render git?
Theme feed review agent site source theme commit commit draft markdown git primitive render source commit.
:::

::cta{title="publish cache fast" button="Read the spec" href="https://snypd.rocks/spec"}
