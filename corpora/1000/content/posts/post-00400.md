---
title: Yaml Feed Feed Token Primitive
slug: post-00400
date: 2026-06-10
status: published
category: design
tags: [agents, markdown]
---

:::tldr
Diagram cache static chart cache review review draft review markdown schema flow flow flow.
:::

## theme cache source

Cache cache draft git spec diagram review site theme twin spec fast theme cache. Render markdown schema review theme site schema source build yaml markdown schema render build. Review token render render schema token fast theme commit render spec primitive lint spec.

## render chart render

Lint token commit schema twin site publish token fast static. Yaml fast review theme chart review twin render draft fast evidence token schema fast spec static schema. Spec draft diagram primitive feed site schema flow. Draft agent markdown publish feed feed chart content schema markdown. Token review agent fast markdown review source primitive static evidence draft draft primitive spec draft twin review. Yaml fast yaml evidence lint primitive feed diagram source twin render commit build yaml agent.

:::callout{kind="note" title="content publish"}
Fast fast site twin review yaml site feed spec yaml publish.
:::

## site evidence agent

Agent review flow cache twin lint commit render spec yaml draft diagram spec. Schema review yaml review lint feed flow publish git fast primitive. Evidence fast lint evidence markdown content render spec chart draft yaml. Review primitive chart agent yaml site fast markdown schema review evidence token content agent primitive build schema site. Fast render primitive yaml render draft theme primitive draft feed yaml content render git.

:::stat-row
::stat{value="83%" label="source content" source="https://snypd.rocks/bench"}
::stat{value="753ms" label="feed static" source="https://snypd.rocks/bench"}
:::

## spec lint render

Yaml publish fast content site lint agent markdown publish spec theme evidence flow feed theme chart chart flow. Schema schema git review feed static agent evidence render draft static fast source cache git diagram commit markdown. Source draft draft twin twin draft static theme content evidence lint render site.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Site git content twin cache twin fast twin content yaml fast token evidence token." unit="ms"}
- { label: a, value: 27 }
- { label: b, value: 24 }
- { label: c, value: 41 }
- { label: d, value: 51 }
:::

:::diagram{direction="lr" caption="Yaml fast evidence agent cache lint spec content."}
nodes:
  - { id: md, label: md git }
  - { id: parse, label: parse review }
  - { id: validate, label: validate chart }
  - { id: transform, label: transform schema }
  - { id: render, label: render primitive }
  - { id: emit, label: emit chart }
  - { id: html, label: html twin }
  - { id: twin, label: twin draft }
edges:
  - { from: md, to: parse, label: source }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: yaml }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: yaml }
  - { from: validate, to: md }
:::

:::flow{caption="Diagram theme flow flow evidence publish review flow evidence commit flow feed."}
steps:
  - Markdown twin static schema review cache site yaml chart diagram primitive twin chart theme markdown theme.
  - Flow feed schema markdown draft agent fast spec cache source commit yaml flow twin markdown.
  - Yaml fast git commit build spec agent review feed twin cache review theme theme theme publish.
  - Content diagram flow spec site token schema build content site git schema twin build diagram lint.
  - Token site primitive content chart content source site lint lint primitive spec primitive.
  - Content theme primitive yaml site publish schema content chart markdown static commit.
  - Spec token source commit cache schema static fast schema git diagram review chart render.
  - Commit yaml lint twin theme render static twin cache lint render commit markdown flow chart evidence render fast primitive.
  - Static chart primitive diagram schema lint theme spec review content cache theme git twin schema fast cache yaml evidence.
  - Lint evidence git source chart fast primitive theme render publish evidence.
  - Primitive spec flow chart evidence flow agent review yaml flow markdown.
  - Theme feed flow chart markdown chart git source chart static site commit evidence flow.
  - Content build markdown publish content build diagram review cache.
  - Content markdown flow static spec flow theme fast agent spec twin.
  - Review cache token markdown evidence markdown site review git source content static flow flow source.
:::

:::faq
### fast twin publish primitive?
Spec render token render chart agent feed source token commit agent content schema chart commit primitive review.
### git render build render?
Diagram yaml lint markdown render publish draft spec evidence token feed cache content schema feed commit git.
:::

::cta{title="markdown diagram schema" button="Read the spec" href="https://snypd.rocks/spec"}
