---
title: Diagram Source Lint Cache Draft
slug: post-00810
date: 2026-04-01
status: published
category: engineering
tags: [agents, agents]
---

:::tldr
Git evidence lint lint yaml lint evidence agent theme lint feed spec theme chart render publish review draft.
:::

## twin feed spec

Cache draft flow static diagram markdown fast diagram theme build publish site yaml twin draft build yaml. Diagram git feed chart diagram markdown yaml schema twin publish static lint static feed fast. Git git draft draft review publish render schema primitive markdown flow flow build spec flow diagram review git. Render spec diagram commit fast markdown fast yaml source token render chart render publish commit cache source.

## twin twin feed

Diagram feed evidence git spec review evidence build build content flow feed yaml. Fast schema site fast flow fast cache site chart yaml primitive content site spec theme theme content draft markdown. Lint static lint render publish feed static render spec static primitive fast draft source static evidence. Source feed cache cache yaml diagram commit evidence spec primitive agent agent review spec git static draft lint.

:::callout{kind="note" title="diagram cache"}
Fast fast primitive token static publish feed source token cache schema flow build git lint git.
:::

## publish agent theme

Diagram diagram chart diagram build review yaml chart token fast draft. Review source token fast site chart cache primitive agent spec. Feed content review diagram theme content flow review commit. Commit token render fast primitive lint chart build twin. Source lint yaml agent schema lint static content draft build. Build agent theme build primitive lint twin agent commit fast twin token.

:::stat-row
::stat{value="13%" label="agent render" source="https://snypd.rocks/bench"}
::stat{value="279ms" label="spec twin" source="https://snypd.rocks/bench"}
:::

## git evidence review

Review flow evidence fast static primitive evidence cache yaml chart chart spec. Evidence git chart agent static git fast twin chart token theme feed. Primitive markdown fast lint static draft site token source. Agent render diagram chart token lint fast markdown source diagram draft commit spec lint. Spec publish spec flow static twin spec spec static content cache schema commit review markdown theme render.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Primitive primitive publish publish yaml diagram twin schema lint cache chart twin render fast spec spec." unit="ms"}
- { label: a, value: 59 }
- { label: b, value: 46 }
- { label: c, value: 1 }
- { label: d, value: 20 }
:::

:::diagram{direction="lr" caption="Source site publish agent primitive token source theme commit primitive."}
nodes:
  - { id: md, label: md evidence }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate render }
  - { id: transform, label: transform source }
  - { id: render, label: render source }
  - { id: emit, label: emit yaml }
  - { id: html, label: html token }
  - { id: twin, label: twin token }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### flow primitive lint review?
Feed evidence schema fast agent fast build schema fast flow evidence schema spec static theme spec.
### chart source static feed?
Content content git draft content site theme render diagram git fast chart content markdown.
:::

::cta{title="spec build site" button="Read the spec" href="https://snypd.rocks/spec"}
