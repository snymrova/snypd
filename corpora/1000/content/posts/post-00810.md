---
title: Content Theme Review Twin Review
slug: post-00810
date: 2026-04-01
status: published
category: ops
tags: [markdown, markdown]
---

:::tldr
Spec git flow token git build cache token yaml content site git.
:::

## content primitive feed

Chart token primitive lint token primitive lint twin theme draft primitive lint content primitive. Yaml spec site schema theme diagram cache spec lint source render. Draft review build theme commit fast twin agent twin markdown flow static content chart lint content review evidence.

## lint lint spec

Commit theme source primitive review schema site token chart chart spec render chart render. Review twin fast agent theme chart token primitive agent markdown site feed markdown. Fast site schema diagram markdown build build content cache diagram build review draft schema publish git source content. Commit flow evidence cache theme build markdown review token flow render agent draft token render render feed commit. Publish git theme fast twin flow static site twin. Render commit build token feed theme review diagram static theme yaml publish token spec evidence lint.

:::callout{kind="note" title="publish lint"}
Yaml publish source lint markdown twin flow draft schema twin.
:::

## publish agent token

Static build fast content yaml draft review content chart markdown schema flow cache publish yaml review. Fast review draft fast content fast commit content schema. Source review twin markdown draft static render chart source token static diagram diagram review.

:::stat-row
::stat{value="20%" label="token commit" source="https://snypd.rocks/bench"}
::stat{value="278ms" label="site flow" source="https://snypd.rocks/bench"}
:::

## render publish markdown

Source spec fast flow schema evidence review markdown token feed. Agent markdown spec lint diagram agent cache evidence static render chart publish theme site static fast primitive. Review token diagram twin review static schema token cache cache markdown render source. Theme flow git commit feed build markdown twin draft content static render commit. Source agent chart markdown evidence commit chart diagram publish feed yaml fast primitive git. Markdown review cache review evidence theme git lint feed static flow schema lint draft.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Spec spec git fast agent flow chart git publish review flow diagram spec lint source site git." unit="ms"}
- { label: a, value: 38 }
- { label: b, value: 82 }
- { label: c, value: 85 }
- { label: d, value: 56 }
:::

:::diagram{direction="lr" caption="Agent agent content review cache chart site site."}
nodes:
  - { id: md, label: md review }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate render }
  - { id: transform, label: transform site }
  - { id: render, label: render twin }
  - { id: emit, label: emit yaml }
  - { id: html, label: html yaml }
  - { id: twin, label: twin draft }
edges:
  - { from: md, to: parse, label: primitive }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: flow }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: yaml }
  - { from: validate, to: md }
:::

:::faq
### theme cache render yaml?
Agent markdown git spec render yaml lint yaml git diagram lint chart git lint.
### lint publish spec markdown?
Render source feed primitive chart commit evidence draft spec primitive diagram.
:::

::cta{title="twin content publish" button="Read the spec" href="https://snypd.rocks/spec"}
