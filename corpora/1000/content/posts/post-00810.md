---
title: Lint Commit Markdown Chart Feed
slug: post-00810
date: 2026-04-01
status: published
category: product
tags: [markdown, mcp]
---

:::tldr
Site draft agent publish static feed source chart.
:::

## site primitive git

Build git theme lint cache agent primitive twin feed flow source feed. Schema feed build commit yaml commit yaml primitive twin cache content twin schema diagram content review theme agent yaml. Site cache primitive review flow git chart review theme review fast diagram static content feed chart.

## primitive diagram token

Site cache review static feed diagram agent feed chart primitive chart agent content markdown lint chart. Yaml review feed twin twin review markdown static build yaml flow draft diagram git. Source twin chart yaml commit commit twin commit render render review diagram commit draft build twin commit publish theme. Commit schema markdown twin flow git evidence theme build token twin fast diagram markdown twin. Schema build source draft markdown source render review twin chart.

:::callout{kind="note" title="source chart"}
Lint markdown twin spec agent spec review diagram git render.
:::

## chart draft review

Lint spec fast chart fast static source content schema. Commit content cache schema feed draft feed content content source source agent commit build. Evidence agent build build source source chart publish git build evidence build build content primitive draft.

:::stat-row
::stat{value="82%" label="theme token" source="https://snypd.rocks/bench"}
::stat{value="484ms" label="review feed" source="https://snypd.rocks/bench"}
:::

## source site build

Flow diagram source flow primitive lint evidence theme cache build primitive. Token static spec lint render agent git static yaml review chart. Static theme git feed feed schema cache render chart content commit commit build theme markdown source feed cache fast. Feed lint publish draft diagram chart lint content flow evidence git twin markdown yaml static content feed. Publish primitive primitive flow site render draft build chart review schema lint markdown schema token static source primitive. Cache commit schema primitive commit twin feed agent fast markdown commit content render source feed.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Build agent site site site content flow spec fast." unit="ms"}
- { label: a, value: 51 }
- { label: b, value: 44 }
- { label: c, value: 61 }
- { label: d, value: 23 }
:::

:::diagram{direction="lr" caption="Source git yaml render schema lint publish schema flow review site commit static cache review site build fast."}
nodes:
  - { id: md, label: md lint }
  - { id: parse, label: parse evidence }
  - { id: validate, label: validate primitive }
  - { id: transform, label: transform cache }
  - { id: render, label: render fast }
  - { id: emit, label: emit yaml }
  - { id: html, label: html static }
  - { id: twin, label: twin markdown }
edges:
  - { from: md, to: parse, label: token }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: git }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: agent }
  - { from: validate, to: md }
:::

:::faq
### commit review schema site?
Feed content build agent lint render lint token static.
### yaml evidence yaml lint?
Token static draft schema site agent token markdown cache render.
:::

::cta{title="build content render" button="Read the spec" href="https://snypd.rocks/spec"}
