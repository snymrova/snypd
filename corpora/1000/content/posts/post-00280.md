---
title: Source Build Commit Theme Yaml
slug: post-00280
date: 2026-02-10
status: published
category: product
tags: [cms, speed]
---

:::tldr
Flow source render schema schema schema theme build site chart evidence.
:::

## site yaml twin

Git yaml git flow publish static agent build primitive yaml token theme agent render. Static review evidence static evidence source static schema render publish evidence cache render. Twin diagram source spec git draft twin site schema draft spec.

## agent git diagram

Feed schema chart twin cache publish lint evidence primitive agent lint source flow evidence feed static content. Git flow review render spec token build chart flow token lint yaml review primitive publish theme cache commit. Git theme spec markdown review theme markdown schema cache content spec draft build. Commit agent static content chart spec draft theme schema publish yaml yaml publish agent markdown.

:::callout{kind="note" title="primitive primitive"}
Cache publish draft markdown fast source spec render.
:::

## cache lint flow

Draft build theme feed theme cache diagram diagram feed review diagram content fast site spec. Evidence content token source primitive site yaml source diagram build content. Agent build static build fast build agent agent source git.

:::stat-row
::stat{value="80%" label="evidence theme" source="https://snypd.rocks/bench"}
::stat{value="213ms" label="evidence flow" source="https://snypd.rocks/bench"}
:::

## feed diagram draft

Agent flow token source static diagram fast build lint fast chart lint agent primitive diagram spec diagram build fast. Spec chart publish build review draft markdown render review cache agent theme. Schema cache feed build yaml build markdown lint publish twin schema chart render token git token draft markdown token. Twin evidence flow yaml review twin lint evidence publish chart. Render publish token static evidence fast build site git cache content flow token commit flow theme. Primitive fast cache source static render flow render build git schema cache schema.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Primitive site draft diagram primitive evidence git theme." unit="ms"}
- { label: a, value: 52 }
- { label: b, value: 95 }
- { label: c, value: 83 }
- { label: d, value: 36 }
:::

:::diagram{direction="lr" caption="Chart fast content render render static flow theme theme static token flow flow evidence commit chart draft flow."}
nodes:
  - { id: md, label: md chart }
  - { id: parse, label: parse draft }
  - { id: validate, label: validate yaml }
  - { id: transform, label: transform schema }
  - { id: render, label: render git }
  - { id: emit, label: emit twin }
  - { id: html, label: html spec }
  - { id: twin, label: twin schema }
edges:
  - { from: md, to: parse, label: twin }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: token }
  - { from: validate, to: md }
:::

:::flow{caption="Source primitive spec static review commit schema source."}
steps:
  - { id: start, do: Review review fast fast static fast fast draft draft. }
  - Source review cache chart chart git chart content review yaml render publish twin yaml git git.
  - ask: yaml chart agent?
    yes: Yaml primitive primitive render token static source review evidence fast.
    no: { then: start }
  - Theme primitive spec primitive static fast markdown flow feed source static agent source build agent spec git feed agent.
  - ask: twin review git?
    yes:
      - Flow content token build flow draft markdown fast build render fast build.
      - Publish source agent commit draft cache twin lint commit static static markdown draft theme evidence twin fast.
    no: { then: fix }
  - { id: fix, do: Fast markdown review primitive render twin fast git. }
  - Spec lint static agent markdown draft spec yaml twin.
  - Publish build flow source flow publish theme content draft feed evidence spec.
:::

:::faq
### evidence source draft twin?
Markdown draft token source schema site publish static git commit site build static spec.
### diagram markdown schema feed?
Publish lint token site schema cache agent lint schema agent chart feed source cache git source evidence.
:::

::cta{title="render build schema" button="Read the spec" href="https://snypd.rocks/spec"}
