---
title: Chart Agent Flow Lint Schema
slug: post-00280
date: 2026-02-10
status: published
category: product
tags: [mcp, markdown]
---

:::tldr
Build token theme diagram twin build content markdown spec fast diagram draft review feed.
:::

## lint chart theme

Feed commit flow fast evidence cache token token diagram feed flow content site diagram agent flow schema. Flow chart source feed yaml source chart render git fast git yaml flow agent. Build evidence chart fast site git fast draft. Build theme fast token markdown twin publish flow cache cache schema commit evidence feed. Feed evidence twin git schema lint spec source.

## git commit primitive

Feed content flow twin git diagram token primitive diagram publish. Agent token diagram diagram evidence static fast publish schema diagram theme cache agent content publish static. Draft commit content fast spec fast chart schema site draft git chart cache. Diagram source theme token yaml spec publish schema token git flow spec theme review agent static diagram. Git lint schema publish commit git publish source chart render schema agent chart render schema fast diagram. Evidence spec lint spec content markdown lint source fast token.

:::callout{kind="note" title="lint spec"}
Feed draft fast draft commit fast yaml schema fast draft git source evidence.
:::

## draft primitive chart

Yaml publish fast content theme schema twin site cache git feed diagram flow schema chart twin cache commit. Review chart render twin cache diagram primitive publish build feed feed static flow git git schema. Build feed spec chart review cache twin agent evidence. Chart flow commit evidence twin git build primitive review git content site diagram evidence source primitive spec theme chart. Feed theme agent review cache build cache primitive token content. Site static build diagram review build site draft spec chart.

:::stat-row
::stat{value="71%" label="twin primitive" source="https://snypd.rocks/bench"}
::stat{value="483ms" label="git evidence" source="https://snypd.rocks/bench"}
:::

## chart twin publish

Spec site draft content chart content site chart review evidence content draft review yaml. Spec chart publish spec fast review token fast token markdown primitive lint feed token evidence primitive feed schema. Diagram source primitive review theme spec fast build build content build publish lint.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Render review publish yaml draft content twin fast site content source draft yaml build spec diagram schema twin content." unit="ms"}
- { label: a, value: 7 }
- { label: b, value: 16 }
- { label: c, value: 35 }
- { label: d, value: 22 }
:::

:::diagram{direction="lr" caption="Flow flow build diagram content lint spec source source commit."}
nodes:
  - { id: md, label: md lint }
  - { id: parse, label: parse git }
  - { id: validate, label: validate lint }
  - { id: transform, label: transform fast }
  - { id: render, label: render git }
  - { id: emit, label: emit source }
  - { id: html, label: html static }
  - { id: twin, label: twin flow }
edges:
  - { from: md, to: parse, label: schema }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: feed }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: yaml }
  - { from: validate, to: md }
:::

:::flow{caption="Markdown twin commit content feed commit feed static."}
steps:
  - Markdown chart review agent content git cache draft flow schema fast review content token commit.
  - Cache spec evidence source lint content render cache.
  - Token site spec cache feed agent static commit cache schema spec content.
  - Draft spec chart theme content flow content fast feed spec.
  - Evidence draft site yaml review agent yaml theme publish primitive cache primitive render chart source schema render markdown commit.
  - Site source feed git draft primitive cache lint primitive twin diagram.
  - Lint token agent cache yaml source twin feed diagram site lint chart draft evidence markdown.
  - Content spec static agent agent static lint review evidence chart.
  - Fast markdown markdown review content evidence render schema review build review token spec render lint.
  - Render build cache flow token schema spec feed source flow fast chart site evidence diagram git publish git spec.
  - Twin yaml source commit draft lint render markdown git yaml.
  - Markdown diagram cache theme evidence flow cache flow lint spec static feed twin lint diagram.
  - Draft feed site theme feed lint source flow lint draft content static render review git token fast.
  - Token spec git commit diagram diagram chart markdown evidence.
  - Build build feed schema fast evidence chart render flow markdown cache lint lint schema spec flow.
:::

:::faq
### static flow static feed?
Commit commit static feed publish spec fast evidence primitive.
### theme static static diagram?
Commit commit render flow yaml commit token yaml review fast review yaml evidence diagram publish primitive token cache markdown.
:::

::cta{title="cache commit schema" button="Read the spec" href="https://snypd.rocks/spec"}
