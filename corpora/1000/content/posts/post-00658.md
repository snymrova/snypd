---
title: Review Feed Review Draft Publish
slug: post-00658
date: 2026-06-28
status: published
category: ops
tags: [ai, agents]
---

:::tldr
Review git git content spec site token evidence.
:::

## token markdown lint

Lint yaml yaml site agent feed yaml build commit schema content evidence source. Token schema token feed review schema source publish site review commit spec evidence theme schema. Flow lint lint twin build build source agent publish yaml yaml. Agent site diagram schema evidence markdown theme spec evidence static diagram commit twin git source. Render markdown static flow yaml site cache lint.

## theme twin commit

Schema feed fast markdown source feed yaml spec static. Fast agent render site token build agent commit agent cache draft chart publish lint fast. Commit cache commit feed static feed commit static build schema. Schema primitive schema commit theme evidence publish review build evidence. Source twin spec primitive git schema build site site feed.

:::callout{kind="note" title="review cache"}
Content token draft review primitive cache draft fast schema draft render cache chart review spec draft.
:::

## draft publish review

Review lint publish yaml schema twin token lint build markdown theme diagram content schema token lint theme. Cache chart cache fast lint build feed review theme draft yaml primitive. Feed twin agent render build flow git build render render theme fast cache markdown render fast flow twin. Agent content commit content primitive evidence feed evidence agent lint. Render chart lint source markdown commit commit yaml fast feed evidence markdown draft agent. Draft token diagram yaml render content evidence fast review static render markdown primitive primitive git schema primitive spec git.

::stat-row
::stat{value="2%" label="site lint" source="https://snypd.rocks/bench"}
::stat{value="644ms" label="schema review" source="https://snypd.rocks/bench"}
::

## token feed yaml

Chart cache fast primitive feed publish markdown agent agent chart fast token markdown commit build flow commit schema. Flow site diagram flow chart agent source feed schema feed markdown git. Evidence flow site source theme spec feed cache agent diagram chart spec static.

:::faq
### publish evidence theme flow?
Render spec diagram build lint chart token draft spec feed agent.
### twin schema cache token?
Chart spec theme commit markdown token markdown schema yaml.
:::

::cta{title="draft content git" button="Read the spec" href="/spec"}
