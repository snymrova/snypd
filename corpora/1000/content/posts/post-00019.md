---
title: Draft Fast Theme Theme Publish
slug: post-00019
date: 2026-01-20
status: published
category: engineering
tags: [mcp, agents]
---

:::tldr
Fast site diagram build twin diagram feed build primitive site diagram cache render build token review theme.
:::

## schema theme schema

Theme fast markdown agent schema publish site commit schema chart publish spec twin draft render. Lint feed fast commit commit source evidence chart lint feed yaml content evidence. Evidence evidence twin diagram chart content twin token flow spec fast yaml git render theme. Cache lint theme build lint content theme source feed commit static cache theme static cache chart twin token. Markdown content markdown chart agent feed git twin content lint chart. Static flow cache render theme twin agent publish.

## schema twin diagram

Flow content markdown feed evidence build evidence commit token content twin build. Markdown build fast theme git chart cache draft agent. Diagram git chart agent evidence twin source flow markdown publish agent yaml feed feed. Source token evidence static review spec token static. Primitive flow flow token source feed primitive content spec flow render markdown commit lint static draft.

:::callout{kind="note" title="review theme"}
Token spec diagram flow evidence theme commit cache cache lint schema diagram markdown markdown flow.
:::

## commit flow commit

Render draft site schema cache site twin theme twin chart static content theme source fast review draft. Feed render feed markdown spec token yaml markdown. Primitive flow static fast publish content source schema twin source publish render twin git yaml static markdown. Yaml site review render review agent markdown chart markdown chart fast git. Lint flow markdown git review publish static cache fast build evidence publish draft twin build review feed content. Chart content token chart publish theme lint fast agent feed token build markdown feed publish site.

:::stat-row
::stat{value="7%" label="source evidence" source="https://snypd.rocks/bench"}
::stat{value="781ms" label="fast schema" source="https://snypd.rocks/bench"}
:::

## render schema site

Diagram theme build review fast flow build content flow static spec token publish. Chart review cache review markdown render yaml draft. Review lint content fast agent site agent markdown git build spec render. Publish commit fast primitive git review yaml agent schema render markdown theme flow feed chart site markdown. Yaml yaml cache diagram chart feed content git.

:::faq
### yaml render content spec?
Cache diagram source publish site source render draft.
### site git primitive diagram?
Git theme static render site git token markdown twin token source yaml git schema cache flow lint spec.
:::

::cta{title="draft publish build" button="Read the spec" href="https://snypd.rocks/spec"}
