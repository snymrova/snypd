---
title: Token Git Twin Lint Schema
slug: post-00180
date: 2026-06-30
status: published
category: design
tags: [seo, markdown]
---

:::tldr
Render spec review build markdown content twin twin yaml twin cache twin site site flow source review lint.
:::

## commit token commit

Lint render feed spec chart commit chart publish agent primitive diagram. Draft schema fast content static cache build build fast render evidence twin schema evidence git. Yaml yaml render content diagram build static cache yaml git site content fast token cache. Cache publish build draft build draft build yaml review. Chart schema fast spec commit review static lint chart fast token chart primitive flow commit.

## flow content token

Chart token commit publish commit evidence cache publish build chart source chart draft source review site git twin site. Site primitive flow chart feed primitive feed twin commit cache site lint feed build review. Publish token twin evidence cache site git spec render yaml flow agent lint source. Feed twin git yaml agent publish lint render commit yaml. Commit twin diagram token fast lint schema chart theme. Theme schema fast schema source draft chart flow evidence build review git theme markdown diagram twin.

:::callout{kind="note" title="flow schema"}
Diagram theme token token chart cache fast render draft lint token flow lint.
:::

## schema token flow

Diagram spec review chart publish feed render publish fast commit flow markdown theme review. Twin primitive theme draft commit token theme schema git chart chart. Chart diagram site flow spec commit fast token agent schema. Source yaml source render static render flow build. Markdown yaml content chart twin fast commit source diagram git evidence. Draft diagram theme theme twin schema commit site flow flow content lint build build flow source.

:::stat-row
::stat{value="82%" label="chart diagram" source="https://snypd.rocks/bench"}
::stat{value="421ms" label="primitive spec" source="https://snypd.rocks/bench"}
:::

## agent spec review

Flow twin content feed fast twin diagram cache flow chart feed. Commit fast publish static commit render commit render lint feed source review publish cache render yaml schema render evidence. Agent evidence commit evidence content twin lint git twin publish commit flow content feed theme. Build git render render markdown twin diagram content evidence content spec spec source.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Theme fast markdown cache evidence publish flow publish diagram render render source git token evidence draft publish twin fast." unit="ms"}
- { label: a, value: 0 }
- { label: b, value: 65 }
- { label: c, value: 78 }
- { label: d, value: 21 }
:::

:::diagram{direction="lr" caption="Spec git publish schema fast primitive site publish review fast static flow fast cache."}
nodes:
  - { id: md, label: md commit }
  - { id: parse, label: parse content }
  - { id: validate, label: validate schema }
  - { id: transform, label: transform diagram }
  - { id: render, label: render publish }
  - { id: emit, label: emit cache }
  - { id: html, label: html git }
  - { id: twin, label: twin yaml }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Git draft commit site evidence evidence lint commit site lint draft."}
steps:
  - Git feed chart build static chart publish source diagram feed source markdown agent build primitive content fast diagram agent.
  - Static markdown draft markdown commit publish static publish schema.
  - Diagram build review diagram cache publish static chart git theme content git render markdown primitive.
  - Publish publish feed lint publish lint cache flow draft review token primitive primitive diagram feed git feed lint schema.
  - Site static draft feed cache evidence schema content feed fast markdown static schema yaml draft markdown primitive token publish.
  - Agent source token render flow render draft cache schema git diagram agent fast draft publish agent schema.
  - Source lint build diagram draft commit evidence site markdown build token render cache draft cache cache.
  - Cache content twin evidence source build draft content lint schema spec cache yaml source fast.
  - Markdown site evidence chart primitive git yaml yaml lint spec theme commit.
  - Evidence build diagram commit source schema render agent.
  - Evidence agent markdown render build git review cache evidence cache site content markdown chart twin review render.
  - Markdown markdown cache feed lint agent twin yaml primitive agent lint review feed publish static chart theme.
  - Review twin feed review markdown evidence twin flow fast site draft primitive static publish spec evidence content commit site.
  - Site spec commit commit build evidence draft feed yaml site markdown commit flow.
  - Schema token lint agent review schema chart feed site chart fast git source agent content static flow source draft.
:::

:::faq
### flow primitive draft draft?
Spec review diagram draft fast diagram publish render schema lint token.
### publish agent markdown site?
Site build git fast draft theme twin twin token.
:::

::cta{title="render fast publish" button="Read the spec" href="https://snypd.rocks/spec"}
