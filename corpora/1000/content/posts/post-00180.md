---
title: Primitive Feed Twin Commit Cache
slug: post-00180
date: 2026-06-30
status: published
category: product
tags: [bun, markdown]
---

:::tldr
Review fast publish token twin evidence cache site git spec.
:::

## render yaml flow

Source build feed twin git yaml agent publish lint render commit yaml. Commit twin diagram token fast lint schema chart theme. Theme schema fast schema source draft chart flow evidence build review git theme markdown diagram twin.

## flow schema draft

Token token chart cache fast render draft lint token. Lint schema token flow diagram fast diagram spec review chart publish feed render publish fast commit flow markdown. Review token twin primitive theme draft commit token theme. Git chart chart build chart diagram site flow spec commit fast token agent schema markdown source yaml source. Static render flow build token markdown yaml content chart twin. Commit source diagram git evidence evidence draft diagram theme theme twin schema commit site.

:::callout{kind="note" title="flow flow"}
Lint build build flow source diagram chart diagram.
:::

## site primitive spec

Review token cache flow twin content feed fast twin diagram cache. Chart feed feed commit fast publish static commit render commit render lint feed source review publish cache render. Schema render evidence commit agent evidence commit evidence content twin lint git twin publish commit.

:::stat-row
::stat{value="86%" label="content feed" source="https://snypd.rocks/bench"}
::stat{value="126ms" label="draft build" source="https://snypd.rocks/bench"}
:::

## git render render

Diagram content evidence content spec spec source schema theme fast markdown cache evidence publish flow publish diagram render render. Git token evidence draft publish twin fast agent commit source build fast spec git publish schema fast. Site publish review fast static flow fast cache commit.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Schema diagram publish cache git yaml feed git feed." unit="ms"}
- { label: a, value: 80 }
- { label: b, value: 24 }
- { label: c, value: 50 }
- { label: d, value: 79 }
:::

:::diagram{direction="lr" caption="Source diagram feed source markdown agent build primitive content fast diagram agent."}
nodes:
  - { id: md, label: md theme }
  - { id: parse, label: parse static }
  - { id: validate, label: validate markdown }
  - { id: transform, label: transform draft }
  - { id: render, label: render markdown }
  - { id: emit, label: emit commit }
  - { id: html, label: html publish }
  - { id: twin, label: twin static }
edges:
  - { from: md, to: parse, label: publish }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: schema }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: commit }
  - { from: validate, to: md }
:::

:::flow{caption="Primitive draft draft token spec review diagram draft fast diagram publish render schema lint token publish agent markdown."}
steps:
  - Build review diagram cache publish static chart git theme content git render markdown primitive feed publish publish feed.
  - Publish lint cache flow draft review token primitive primitive diagram feed git.
  - Lint schema twin site static draft feed cache evidence schema content feed fast markdown static schema yaml draft markdown.
  - Token publish source agent source token render flow render.
  - Cache schema git diagram agent fast draft publish agent schema review source lint.
  - Diagram draft commit evidence site markdown build token render cache.
  - Cache cache yaml cache content twin evidence source build draft content lint schema.
  - Cache yaml source fast publish markdown site evidence chart primitive git.
  - Yaml lint spec theme commit markdown evidence build diagram commit source schema render agent chart.
  - Agent markdown render build git review cache evidence cache site content markdown chart twin review render.
  - Markdown markdown cache feed lint agent twin yaml primitive agent lint review feed publish static chart theme.
  - Review twin feed review markdown evidence twin flow fast site draft primitive static publish spec evidence content commit site.
  - Site spec commit commit build evidence draft feed yaml site markdown commit flow.
  - Schema token lint agent review schema chart feed site chart fast git source agent content static flow source draft.
  - Git draft commit site evidence evidence lint commit site lint draft.
:::

:::faq
### site theme site build?
Fast draft theme twin twin token render fast publish publish git render render chart primitive.
### lint fast lint spec?
Fast cache content render evidence source twin primitive content token flow cache review static cache.
:::

::cta{title="agent site static" button="Read the spec" href="https://snypd.rocks/spec"}
