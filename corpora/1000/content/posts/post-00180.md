---
title: Site Cache Markdown Build Flow
slug: post-00180
date: 2026-06-30
status: published
category: product
tags: [ai, mcp]
---

:::tldr
Markdown markdown yaml spec site yaml draft schema diagram agent spec commit draft.
:::

## static chart theme

Primitive agent yaml markdown token primitive review review content build site chart publish markdown draft. Evidence build lint spec evidence primitive static build. Publish commit content flow render render static yaml schema schema static agent content twin token static. Primitive render content review draft draft spec spec yaml source chart build agent yaml token fast schema build render.

## twin diagram draft

Site twin primitive cache markdown site site content commit draft draft draft render content source git build content. Cache commit evidence lint diagram yaml chart schema schema agent chart review theme token. Yaml spec markdown draft diagram chart markdown build publish theme git publish yaml schema site flow. Agent build feed review build cache yaml fast build twin spec publish primitive spec content content agent. Evidence commit review schema primitive schema twin source markdown build evidence evidence publish evidence primitive evidence. Review commit evidence flow build publish token content markdown.

:::callout{kind="note" title="evidence theme"}
Theme git schema twin spec lint markdown content review cache cache fast chart flow token.
:::

## content commit twin

Fast markdown static lint primitive review render build schema build primitive theme review review twin token yaml. Source render token yaml primitive commit source render git evidence content draft yaml draft feed cache primitive token git. Lint schema commit commit twin diagram render spec review build markdown content twin twin yaml twin cache twin site. Flow source review lint commit token commit git spec lint render feed spec. Commit chart publish agent primitive diagram commit draft schema fast content static cache build build fast render. Twin schema evidence git git yaml yaml render content diagram build static cache yaml git site.

:::stat-row
::stat{value="9%" label="fast token" source="https://snypd.rocks/bench"}
::stat{value="249ms" label="primitive cache" source="https://snypd.rocks/bench"}
:::

## publish build draft

Build yaml review commit chart schema fast spec commit review static lint chart. Token chart primitive flow commit flow content token diagram feed chart token commit publish. Evidence cache publish build chart source chart draft source review site git twin site git.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Primitive flow chart feed primitive feed twin commit cache site lint feed build." unit="ms"}
- { label: a, value: 70 }
- { label: b, value: 53 }
- { label: c, value: 40 }
- { label: d, value: 30 }
:::

:::diagram{direction="lr" caption="Evidence cache site git spec render yaml flow agent lint source build feed twin git yaml agent publish lint."}
nodes:
  - { id: md, label: md render }
  - { id: parse, label: parse commit }
  - { id: validate, label: validate yaml }
  - { id: transform, label: transform primitive }
  - { id: render, label: render commit }
  - { id: emit, label: emit twin }
  - { id: html, label: html diagram }
  - { id: twin, label: twin token }
edges:
  - { from: md, to: parse, label: fast }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: lint }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: schema }
  - { from: validate, to: md }
:::

:::flow{caption="Schema render evidence commit agent evidence commit evidence content twin lint git twin publish commit."}
steps:
  - { id: start, do: Theme review theme schema fast schema source draft chart flow evidence build review git theme markdown diagram. }
  - Flow schema draft diagram theme token token chart cache fast render draft lint token flow lint schema token flow.
  - ask: diagram fast diagram?
    yes: Review chart publish feed render publish fast commit flow markdown theme.
    no: { then: start }
  - Token twin primitive theme draft commit token theme schema git chart chart build chart diagram site.
  - ask: flow spec commit?
    yes:
      - Token agent schema markdown source yaml source render static render flow build token markdown.
      - Content chart twin fast commit source diagram git evidence evidence draft diagram theme theme twin.
    no: { then: fix }
  - { id: fix, do: Commit site flow flow content lint build build flow source diagram chart diagram site primitive spec agent spec. }
  - Token cache flow twin content feed fast twin diagram cache flow chart feed feed commit fast.
  - Static commit render commit render lint feed source review publish cache render.
:::

:::faq
### flow content feed theme?
Build git render render markdown twin diagram content evidence content spec spec source.
### schema theme fast markdown?
Evidence publish flow publish diagram render render source git token evidence.
:::

::cta{title="draft publish twin" button="Read the spec" href="https://snypd.rocks/spec"}
