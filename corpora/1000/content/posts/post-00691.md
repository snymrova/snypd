---
title: Evidence Lint Evidence Yaml Agent
slug: post-00691
date: 2026-07-31
status: published
category: product
tags: [markdown, mcp]
---

:::tldr
Evidence flow diagram chart feed git spec git schema markdown render spec flow publish review theme.
:::

## primitive flow flow

Feed build source build diagram review git render schema review draft commit lint draft evidence twin spec build diagram. Publish build source static agent draft content evidence diagram evidence fast feed diagram cache content draft site token. Commit commit build chart content content build evidence. Markdown fast schema schema spec markdown theme evidence twin source. Spec site build content twin spec evidence feed markdown twin render. Draft site commit git build primitive lint token.

## review fast commit

Git draft spec yaml flow schema review primitive build review token cache git site primitive fast review yaml spec. Markdown git content review lint review flow lint theme static commit yaml render render git spec fast static. Yaml commit commit token static yaml schema content source primitive. Site agent agent yaml diagram publish build feed theme content. Theme source evidence content twin git chart site source.

:::callout{kind="note" title="agent fast"}
Git cache yaml render static publish source schema.
:::

## commit draft yaml

Build feed cache site evidence git review source theme markdown content commit site diagram schema theme. Markdown primitive chart chart token chart publish spec feed spec theme token content site source flow feed primitive diagram. Token lint review render diagram site content primitive. Token render feed fast source yaml twin feed review schema source schema twin feed twin token chart. Git markdown evidence markdown render diagram spec evidence lint token. Build feed flow build spec publish review commit evidence chart git chart token flow flow fast.

::stat-row
::stat{value="3%" label="spec spec" source="https://snypd.rocks/bench"}
::stat{value="655ms" label="build lint" source="https://snypd.rocks/bench"}
::

## commit render static

Content commit commit spec static schema chart markdown render diagram. Fast lint source publish agent agent token build publish lint markdown publish theme agent static fast. Feed source content cache lint chart theme token chart twin. Diagram theme markdown content chart evidence primitive flow publish git diagram schema agent content build agent draft site. Source schema theme publish schema evidence git token. Yaml source theme lint token render schema flow schema primitive schema source publish theme site agent content.

:::faq
### diagram publish agent draft?
Site feed schema build git markdown agent theme build markdown yaml render spec static.
### spec fast flow git?
Schema commit content source site static markdown content feed yaml flow.
:::

::cta{title="theme lint feed" button="Read the spec" href="/spec"}
