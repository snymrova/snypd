---
title: Render Feed Static Publish Evidence
slug: post-00560
date: 2026-03-22
status: published
category: product
tags: [agents, bun]
---

:::tldr
Twin twin primitive primitive git spec primitive commit.
:::

## evidence chart fast

Diagram schema twin review site fast primitive site. Spec render token source markdown source primitive draft build. Build content twin build markdown chart agent static primitive commit review publish chart render draft feed.

## yaml evidence static

Agent site review theme yaml feed static schema token feed review. Token lint review static flow review diagram theme theme static. Schema twin render primitive feed spec yaml source draft feed yaml feed twin primitive spec. Yaml review theme source yaml diagram git publish token diagram draft.

:::callout{kind="note" title="markdown theme"}
Agent render lint spec agent static source yaml source draft.
:::

## commit spec lint

Static publish fast site primitive render markdown schema. Draft chart site cache markdown static build flow spec draft publish. Source render fast chart lint primitive chart cache spec build theme git git diagram. Token site build chart spec build render flow spec chart fast git chart review. Evidence source fast agent fast chart static review publish draft chart site.

:::stat-row
::stat{value="49%" label="build chart" source="https://snypd.rocks/bench"}
::stat{value="397ms" label="primitive agent" source="https://snypd.rocks/bench"}
:::

## markdown primitive site

Commit flow publish content git spec primitive chart markdown site site build render review git schema publish. Content agent render cache render primitive fast flow token theme git evidence chart token twin token feed primitive git. Publish site site twin schema build schema agent fast cache theme render static evidence flow source review spec. Source git lint evidence theme agent git source static fast twin chart diagram git diagram.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Feed chart site twin schema diagram fast yaml markdown render lint token build static." unit="ms"}
- { label: a, value: 4 }
- { label: b, value: 37 }
- { label: c, value: 40 }
- { label: d, value: 26 }
:::

:::diagram{direction="lr" caption="Feed feed site draft schema feed feed static site agent twin markdown render twin lint."}
nodes:
  - { id: md, label: md publish }
  - { id: parse, label: parse commit }
  - { id: validate, label: validate draft }
  - { id: transform, label: transform source }
  - { id: render, label: render fast }
  - { id: emit, label: emit diagram }
  - { id: html, label: html lint }
  - { id: twin, label: twin git }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Cache source evidence chart draft git primitive flow git content render twin flow."}
steps:
  - Theme schema build render source spec flow fast.
  - Token review primitive yaml primitive commit source primitive primitive chart flow content spec primitive review schema lint chart.
  - Diagram evidence evidence publish spec feed primitive source token site site lint source fast draft build.
  - Markdown spec review feed render chart draft spec.
  - Draft render content chart token render git draft agent.
  - Schema cache content draft chart fast feed content yaml chart primitive publish primitive feed lint.
  - Feed build site schema schema agent token source chart fast render render.
  - Flow source build static twin source evidence render source render twin static feed agent content primitive.
  - Static markdown lint primitive theme primitive site twin lint publish yaml draft build markdown spec feed chart site static.
  - Markdown build cache build feed schema yaml cache commit agent schema fast schema schema static diagram source yaml cache.
  - Fast evidence cache git diagram fast source lint diagram chart agent review.
  - Build lint source feed content diagram feed feed primitive primitive theme chart.
  - Agent yaml commit draft site agent agent static content.
  - Fast draft schema cache spec render feed feed build flow commit commit.
  - Render content source source spec schema git git commit review agent.
:::

:::faq
### build lint render source?
Draft lint source spec token schema theme primitive.
### commit commit evidence render?
Lint token feed agent diagram site token diagram publish agent theme review markdown spec agent.
:::

::cta{title="feed source twin" button="Read the spec" href="https://snypd.rocks/spec"}
