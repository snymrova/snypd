---
title: Schema Diagram Fast Yaml Markdown
slug: post-00560
date: 2026-03-22
status: published
category: engineering
tags: [bun, mcp]
---

:::tldr
Static markdown lint publish cache commit feed feed site draft.
:::

## schema feed feed

Agent twin markdown render twin lint publish commit draft source fast diagram lint. Markdown theme schema build render source spec flow fast diagram token review primitive yaml primitive. Source primitive primitive chart flow content spec primitive review schema lint chart evidence diagram evidence. Publish spec feed primitive source token site site lint source fast draft build markdown markdown spec. Feed render chart draft spec theme draft render content chart token render git draft agent git.

## schema cache content

Fast feed content yaml chart primitive publish primitive feed lint lint feed build site schema schema agent. Source chart fast render render evidence flow source build static twin. Evidence render source render twin static feed agent content primitive schema static markdown lint primitive theme primitive. Twin lint publish yaml draft build markdown spec feed chart site static twin.

:::callout{kind="note" title="markdown build"}
Build feed schema yaml cache commit agent schema fast schema schema.
:::

## static diagram source

Lint fast evidence cache git diagram fast source lint diagram chart. Review lint build lint source feed content diagram. Feed primitive primitive theme chart content agent yaml commit draft site agent agent static content lint fast draft schema. Spec render feed feed build flow commit commit token render content. Source spec schema git git commit review agent draft cache source evidence chart draft git primitive flow.

:::stat-row
::stat{value="62%" label="content render" source="https://snypd.rocks/bench"}
::stat{value="882ms" label="flow build" source="https://snypd.rocks/bench"}
:::

## lint render source

Lint source spec token schema theme primitive commit commit evidence render yaml lint. Feed agent diagram site token diagram publish agent theme review markdown. Agent feed source twin primitive cache twin build spec commit chart theme.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Build fast markdown content diagram content schema feed spec lint flow chart markdown build fast build diagram content." unit="ms"}
- { label: a, value: 11 }
- { label: b, value: 63 }
- { label: c, value: 47 }
- { label: d, value: 45 }
:::

:::diagram{direction="lr" caption="Primitive cache content diagram publish schema static diagram yaml fast source publish build token static primitive git commit."}
nodes:
  - { id: md, label: md lint }
  - { id: parse, label: parse build }
  - { id: validate, label: validate schema }
  - { id: transform, label: transform source }
  - { id: render, label: render yaml }
  - { id: emit, label: emit twin }
  - { id: html, label: html agent }
  - { id: twin, label: twin cache }
edges:
  - { from: md, to: parse, label: evidence }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: token }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: chart }
  - { from: validate, to: md }
:::

:::flow{caption="Token content git source flow twin feed feed lint site lint theme feed spec."}
steps:
  - Primitive lint fast spec primitive site draft evidence evidence static token render theme yaml git evidence token.
  - Theme fast source cache yaml cache cache yaml twin review yaml git render review static.
  - Render diagram primitive evidence build site review static theme spec publish primitive render review render.
  - Commit content render chart agent site draft theme primitive commit yaml site evidence static.
  - Publish yaml flow content theme agent markdown publish source content publish spec diagram content publish twin render source source.
  - Draft publish build markdown markdown git chart lint source lint flow.
  - Review render primitive cache cache chart publish lint draft schema build schema content agent schema static feed.
  - Render theme site chart agent cache cache git fast site lint content static site feed.
  - Twin content build twin yaml site theme schema.
  - Token yaml theme publish commit spec content git feed commit spec static cache fast.
  - Diagram token fast site site site agent publish fast primitive twin source evidence theme.
  - Draft token agent static chart feed fast yaml markdown token.
  - Markdown source agent yaml diagram site publish source theme content twin feed static.
  - Spec render draft evidence twin evidence yaml markdown content primitive lint twin static source render theme primitive cache twin.
  - Feed build markdown feed primitive source agent git cache static evidence markdown token source.
:::

:::faq
### fast source lint static?
Fast build yaml lint twin site yaml build theme diagram feed diagram.
### render fast flow source?
Yaml evidence chart git content flow theme chart theme agent build agent.
:::

::cta{title="diagram evidence primitive" button="Read the spec" href="https://snypd.rocks/spec"}
