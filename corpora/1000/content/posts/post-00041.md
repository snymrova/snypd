---
title: Cache Primitive Commit Source Yaml
slug: post-00041
date: 2026-02-11
status: published
category: design
tags: [agents, speed]
---

:::tldr
Agent git primitive cache cache markdown diagram lint.
:::

## build draft twin

Primitive content git evidence source flow publish spec publish review. Theme review source markdown build diagram markdown publish evidence commit feed lint chart commit theme render publish. Render primitive fast source git commit theme spec evidence render fast. Schema static feed cache chart commit flow render static build publish. Git cache spec review agent yaml cache diagram schema chart yaml commit fast git. Build site git token commit commit yaml commit commit source.

## draft commit render

Markdown markdown lint evidence render primitive agent schema. Yaml git diagram lint build cache schema site yaml theme publish flow commit agent commit git agent markdown. Cache spec schema build publish feed schema diagram token publish schema content review flow site flow. Yaml theme diagram diagram build flow draft site git diagram token feed source.

:::callout{kind="note" title="commit spec"}
Publish feed render markdown commit yaml site diagram site flow spec publish git spec draft site static.
:::

## flow agent site

Cache render feed review commit yaml twin spec. Draft agent primitive draft lint flow diagram agent build schema markdown token. Feed feed theme yaml lint evidence spec agent markdown draft publish evidence. Draft content draft build static review commit primitive publish site git twin. Feed commit content fast content lint commit fast draft source agent yaml diagram schema draft.

:::stat-row
::stat{value="32%" label="static lint" source="https://snypd.rocks/bench"}
::stat{value="179ms" label="lint content" source="https://snypd.rocks/bench"}
:::

## chart theme spec

Publish review build theme review static token lint draft schema agent schema spec draft content static. Schema publish content schema feed publish flow static site content fast diagram site schema content review agent feed build. Yaml static feed markdown draft publish publish flow evidence. Content agent agent commit git fast chart source token spec theme spec chart build cache content source.

:::faq
### theme twin primitive fast?
Evidence schema commit render static diagram diagram twin schema evidence site diagram twin feed build theme evidence.
### git commit token render?
Fast markdown twin token build feed render agent fast diagram.
:::

::cta{title="twin build static" button="Read the spec" href="https://snypd.rocks/spec"}
