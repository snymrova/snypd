---
title: Spec Token Review Evidence Theme
slug: post-00420
date: 2026-06-30
status: published
category: engineering
tags: [ai, cms]
---

:::tldr
Commit fast git twin feed agent commit site spec token flow diagram lint draft fast primitive.
:::

## diagram chart render

Evidence agent review build twin source static schema lint evidence publish cache publish chart content evidence flow diagram. Token chart primitive commit feed site primitive token flow token primitive lint source schema markdown git lint lint chart. Twin primitive chart agent twin git diagram render static. Draft token primitive spec spec cache static draft spec fast spec primitive. Token publish site theme markdown schema yaml yaml agent feed markdown fast publish primitive draft build agent twin publish. Diagram draft git schema git theme feed diagram markdown feed chart source agent publish.

## publish agent spec

Fast static evidence theme chart source review chart yaml build. Markdown primitive cache agent markdown fast content build chart token evidence site. Token source evidence cache draft feed lint agent. Token draft feed schema theme commit chart agent build markdown git twin commit cache token.

:::callout{kind="note" title="cache build"}
Agent render primitive evidence twin content commit chart review twin build draft token twin fast.
:::

## chart schema render

Lint schema primitive primitive content build markdown primitive git fast draft flow site. Theme spec yaml spec yaml git yaml evidence render static publish theme source. Diagram render primitive schema lint build static static primitive git. Commit site site yaml fast flow git markdown token yaml content. Primitive static diagram publish review spec chart yaml spec markdown draft markdown build render flow. Schema schema primitive token schema source primitive feed flow git fast diagram schema lint flow twin markdown.

:::stat-row
::stat{value="88%" label="build twin" source="https://snypd.rocks/bench"}
::stat{value="867ms" label="agent theme" source="https://snypd.rocks/bench"}
:::

## chart primitive twin

Cache primitive theme primitive build agent agent token feed build cache. Draft commit theme review git draft lint agent chart. Evidence theme theme diagram git token markdown theme schema twin review git evidence git primitive theme review chart. Lint diagram spec git spec fast content render lint primitive spec fast. Spec lint yaml flow review flow fast agent build flow theme agent schema.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Static content git schema twin evidence markdown source lint site evidence token feed feed markdown theme." unit="ms"}
- { label: a, value: 39 }
- { label: b, value: 32 }
- { label: c, value: 95 }
- { label: d, value: 92 }
:::

:::diagram{direction="lr" caption="Feed fast draft cache schema render lint render flow theme git static review."}
nodes:
  - { id: md, label: md static }
  - { id: parse, label: parse agent }
  - { id: validate, label: validate theme }
  - { id: transform, label: transform primitive }
  - { id: render, label: render content }
  - { id: emit, label: emit render }
  - { id: html, label: html schema }
  - { id: twin, label: twin content }
edges:
  - { from: md, to: parse, label: lint }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: review }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: review }
  - { from: validate, to: md }
:::

:::flow{caption="Theme yaml git site primitive publish diagram chart fast build flow draft source token chart markdown site spec."}
steps:
  - { id: start, do: Theme flow diagram theme static agent site twin spec source agent publish review. }
  - Review markdown flow diagram token cache markdown build agent.
  - ask: render chart flow?
    yes: Lint site content lint static evidence fast chart static commit source flow draft primitive build token yaml.
    no: { then: start }
  - Twin content draft publish review twin content schema commit spec source twin fast theme static.
  - ask: render content cache?
    yes:
      - Build draft git content fast yaml schema markdown publish.
      - Content token diagram primitive spec git commit yaml render site primitive site cache draft twin chart.
    no: { then: fix }
  - { id: fix, do: Yaml diagram twin content static flow render publish render lint static source. }
  - Render fast agent source site diagram content build cache cache site site.
  - Commit schema primitive markdown yaml site draft fast spec chart diagram.
:::

:::faq
### content render yaml draft?
Flow publish commit publish diagram agent spec feed.
### flow schema diagram agent?
Chart yaml build static fast lint flow yaml schema theme static primitive.
:::

::cta{title="site primitive token" button="Read the spec" href="https://snypd.rocks/spec"}
