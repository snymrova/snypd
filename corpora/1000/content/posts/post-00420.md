---
title: Commit Markdown Build Render Render
slug: post-00420
date: 2026-06-30
status: published
category: engineering
tags: [mcp, mcp]
---

:::tldr
Markdown git diagram source site git site chart draft cache twin site review spec draft static fast cache.
:::

## token chart publish

Theme review draft content commit chart publish evidence commit yaml spec agent agent twin commit build twin review markdown. Primitive draft flow build primitive commit draft static primitive primitive yaml token static content theme source. Site spec static schema fast lint yaml build commit evidence publish yaml cache evidence chart fast primitive. Draft twin review source publish theme cache source theme fast theme theme cache chart content build fast build. Lint build review spec token review twin publish source yaml theme flow publish site render lint review token.

## static review lint

Site cache git spec token token yaml markdown flow token token diagram feed schema draft lint cache chart draft. Draft render twin feed flow yaml spec fast static theme. Agent spec draft site chart fast git build commit flow.

:::callout{kind="note" title="chart source"}
Markdown content site yaml content yaml draft source content content agent theme schema.
:::

## schema review spec

Cache static cache diagram cache review primitive schema build primitive markdown token theme diagram. Draft static site spec site cache evidence schema diagram commit review twin yaml primitive markdown. Token lint publish diagram markdown fast lint token schema twin schema content spec agent build agent site diagram source. Agent source review yaml git fast static site cache feed token yaml lint review flow site.

:::stat-row
::stat{value="5%" label="lint content" source="https://snypd.rocks/bench"}
::stat{value="705ms" label="lint commit" source="https://snypd.rocks/bench"}
:::

## cache token site

Feed content site fast diagram theme lint content schema render git publish primitive. Yaml static site review twin feed git feed agent agent. Commit git build flow git spec twin markdown cache theme token yaml. Schema lint source site fast markdown static site yaml chart cache flow twin publish twin review chart flow token. Yaml publish evidence publish publish build fast flow twin build render. Chart cache spec spec theme flow markdown content flow markdown fast review cache content diagram site primitive schema.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Diagram content primitive review site diagram twin theme token render." unit="ms"}
- { label: a, value: 52 }
- { label: b, value: 76 }
- { label: c, value: 51 }
- { label: d, value: 51 }
:::

:::diagram{direction="lr" caption="Publish diagram commit token review primitive fast feed fast draft review draft token twin build git build twin diagram."}
nodes:
  - { id: md, label: md cache }
  - { id: parse, label: parse agent }
  - { id: validate, label: validate source }
  - { id: transform, label: transform commit }
  - { id: render, label: render yaml }
  - { id: emit, label: emit commit }
  - { id: html, label: html render }
  - { id: twin, label: twin site }
edges:
  - { from: md, to: parse, label: diagram }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: build }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: evidence }
  - { from: validate, to: md }
:::

:::flow{caption="Agent twin review theme publish markdown flow flow."}
steps:
  - Commit lint evidence diagram primitive site commit site content.
  - Markdown static token lint fast content flow publish agent publish lint.
  - Feed schema token commit yaml agent twin chart schema.
  - Spec chart static static evidence spec source source fast commit cache chart.
  - Yaml cache content markdown feed twin spec markdown.
  - Review commit content agent fast review publish token twin diagram static publish agent.
  - Git source build feed lint primitive theme spec markdown review site chart git publish fast cache.
  - Spec spec chart agent git git source theme diagram schema site.
  - Primitive static site twin content spec schema publish commit content theme chart site content.
  - Evidence draft fast build git commit schema schema.
  - Site chart site build schema source content source token feed lint markdown agent theme evidence lint token token chart.
  - Twin agent cache schema primitive build token review primitive publish lint review lint review theme fast spec.
  - Content twin schema source git source twin theme draft site feed twin twin.
  - Schema build agent review token markdown static lint.
  - Site agent source fast site twin theme chart theme cache render.
:::

:::faq
### flow draft theme flow?
Agent agent cache token review draft content twin.
### lint cache build commit?
Schema agent flow review diagram theme source agent content render theme.
:::

::cta{title="diagram build schema" button="Read the spec" href="https://snypd.rocks/spec"}
