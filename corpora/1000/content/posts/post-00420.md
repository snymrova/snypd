---
title: Feed Diagram Flow Theme Render
slug: post-00420
date: 2026-06-30
status: published
category: product
tags: [bun, markdown]
---

:::tldr
Yaml git token fast feed build cache review lint cache markdown flow yaml lint cache.
:::

## site markdown evidence

Publish render site publish commit commit theme publish primitive chart chart yaml draft source twin evidence review draft. Feed content chart draft feed diagram static primitive render git evidence cache. Primitive primitive content primitive chart primitive draft git publish token draft fast twin commit chart evidence. Render git primitive fast twin build agent cache primitive source fast lint schema chart agent diagram. Content content source diagram markdown fast static review publish twin fast git source diagram lint cache theme twin git. Markdown review content review yaml twin build schema build markdown lint agent site review spec.

## theme render primitive

Agent build source lint render theme agent twin fast spec cache render fast token agent commit git. Render feed publish chart primitive token markdown site feed draft theme flow. Diagram diagram schema content site flow twin content chart feed schema content chart cache.

:::callout{kind="note" title="yaml evidence"}
Build content render markdown commit evidence twin feed flow flow commit review feed chart primitive.
:::

## yaml commit build

Content git schema agent git git chart render twin primitive flow schema git. Feed token schema markdown markdown diagram primitive site draft spec chart twin publish cache review. Spec publish token draft schema spec review render diagram publish token git. Markdown publish render publish token theme fast render markdown markdown markdown schema markdown draft schema primitive. Site source site commit diagram evidence content draft feed publish chart yaml build markdown twin fast chart. Markdown spec fast content source schema primitive static lint diagram fast review evidence.

:::stat-row
::stat{value="34%" label="cache agent" source="https://snypd.rocks/bench"}
::stat{value="737ms" label="schema review" source="https://snypd.rocks/bench"}
:::

## token review twin

Feed twin fast feed diagram evidence git lint commit git. Chart flow diagram cache build lint publish cache theme git publish lint twin. Cache flow site spec token git commit diagram lint content site render commit schema build review agent cache twin. Build token chart build feed publish commit yaml git lint fast yaml.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Lint lint schema feed theme evidence build lint twin content content chart commit lint publish." unit="ms"}
- { label: a, value: 89 }
- { label: b, value: 81 }
- { label: c, value: 62 }
- { label: d, value: 73 }
:::

:::diagram{direction="lr" caption="Token agent flow primitive yaml theme chart draft spec twin content feed draft publish site render yaml fast."}
nodes:
  - { id: md, label: md evidence }
  - { id: parse, label: parse feed }
  - { id: validate, label: validate publish }
  - { id: transform, label: transform feed }
  - { id: render, label: render content }
  - { id: emit, label: emit fast }
  - { id: html, label: html yaml }
  - { id: twin, label: twin lint }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Agent commit static publish twin schema source site chart evidence."}
steps:
  - Token cache primitive cache source twin diagram publish content primitive diagram evidence site primitive.
  - Flow diagram git markdown schema commit static spec content render diagram git git chart review build review markdown.
  - Build yaml draft draft flow cache git draft fast review evidence git diagram feed.
  - Flow render feed git evidence source primitive agent git build spec flow theme lint fast evidence commit build.
  - Static draft git flow static content publish schema draft static build schema commit site.
  - Schema render markdown review token primitive primitive yaml.
  - Evidence theme content content theme build primitive cache chart publish source theme markdown schema primitive git.
  - Publish build schema yaml fast agent git lint.
  - Token site build build source publish source yaml.
  - Draft content twin static theme flow draft yaml content schema source.
  - Diagram cache content token commit fast lint render spec twin diagram chart build content cache static draft.
  - Content twin spec token token flow markdown flow markdown content schema render build chart markdown git lint primitive.
  - Markdown primitive spec primitive build token primitive feed.
  - Theme spec content render evidence primitive publish cache evidence.
  - Build commit commit diagram lint flow schema diagram draft twin cache static chart static lint static token source.
:::

:::faq
### source review commit agent?
Static feed draft feed schema twin draft yaml markdown cache content.
### render site feed chart?
Content build feed primitive draft yaml review site.
:::

::cta{title="content publish spec" button="Read the spec" href="https://snypd.rocks/spec"}
