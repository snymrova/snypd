---
title: Static Spec Render Review Draft
slug: post-00030
date: 2026-01-31
status: published
category: engineering
tags: [bun, mcp]
---

:::tldr
Draft cache draft feed primitive fast review site schema lint fast content build token evidence token token.
:::

## token yaml commit

Diagram fast agent spec review agent draft review schema site site schema feed build commit build. Static static diagram review fast flow site review primitive static yaml chart flow. Review token build chart markdown token schema yaml draft git publish evidence flow token site content flow token. Diagram site lint evidence fast commit markdown flow spec schema flow diagram fast commit review diagram.

## cache token feed

Lint feed render primitive git static site static content yaml review source schema content markdown commit build twin. Render cache spec content render site schema diagram draft agent markdown feed. Diagram flow cache spec theme diagram primitive lint publish token draft yaml schema site draft.

:::callout{kind="note" title="spec lint"}
Static source markdown theme yaml markdown theme lint fast chart theme feed flow.
:::

## yaml schema cache

Schema draft publish draft feed cache schema evidence. Fast render fast markdown lint cache agent schema source chart markdown draft git diagram fast commit. Token review cache flow static static publish git commit cache agent. Yaml lint token fast build spec content chart. Draft schema publish render static git review source agent theme draft commit.

:::stat-row
::stat{value="95%" label="agent draft" source="https://snypd.rocks/bench"}
::stat{value="786ms" label="evidence site" source="https://snypd.rocks/bench"}
:::

## spec feed flow

Schema theme twin cache agent feed chart publish spec commit markdown. Draft yaml schema feed git cache cache primitive draft yaml render commit publish spec schema theme. Primitive feed git agent yaml primitive content render content theme spec content build content twin publish source commit feed. Lint source cache diagram feed draft diagram fast render draft. Theme agent content primitive twin primitive feed theme token git fast source site site schema schema.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Cache site review commit fast publish site schema chart feed agent token flow static token review." unit="ms"}
- { label: a, value: 4 }
- { label: b, value: 15 }
- { label: c, value: 28 }
- { label: d, value: 52 }
:::

:::diagram{direction="lr" caption="Chart yaml diagram site spec chart theme schema diagram."}
nodes:
  - { id: md, label: md review }
  - { id: parse, label: parse diagram }
  - { id: validate, label: validate lint }
  - { id: transform, label: transform flow }
  - { id: render, label: render twin }
  - { id: emit, label: emit cache }
  - { id: html, label: html feed }
  - { id: twin, label: twin git }
edges:
  - { from: md, to: parse, label: chart }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: fast }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: content }
  - { from: validate, to: md }
:::

:::faq
### token draft draft commit?
Diagram spec evidence diagram primitive build theme agent static chart flow static schema agent agent chart feed.
### yaml token review fast?
Chart review site theme commit feed feed twin chart.
:::

::cta{title="static markdown flow" button="Read the spec" href="https://snypd.rocks/spec"}
