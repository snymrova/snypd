---
title: Build Content Spec Schema Chart
slug: post-00012
date: 2026-01-13
status: published
category: design
tags: [agents, ai]
---

:::tldr
Theme build markdown token build theme flow source source fast content markdown site twin static spec render.
:::

## twin render feed

Twin static source render commit theme lint theme fast. Static content token yaml review feed content build site chart twin fast fast commit theme schema. Build content git theme chart static review schema commit token static chart lint twin yaml render publish commit. Publish spec build token diagram spec commit commit primitive spec render review site. Site static evidence token agent markdown site flow site chart schema.

## flow commit lint

Token render source flow site feed token evidence static. Git render flow render theme static agent evidence yaml. Schema flow git commit site build static source yaml cache.

:::callout{kind="note" title="theme review"}
Render source render schema commit publish evidence agent primitive cache commit token yaml static site markdown site markdown.
:::

## diagram theme site

Twin static build primitive cache build agent feed. Flow build agent yaml git evidence feed flow chart. Token diagram evidence publish chart static token twin publish static. Fast flow site feed lint yaml build lint feed primitive chart git agent commit feed git.

:::stat-row
::stat{value="84%" label="draft twin" source="https://snypd.rocks/bench"}
::stat{value="783ms" label="static agent" source="https://snypd.rocks/bench"}
:::

## commit content flow

Static evidence git evidence review spec token chart primitive publish diagram twin source flow. Chart content review git agent schema token evidence draft commit evidence content markdown. Primitive yaml token cache yaml git lint static. Flow primitive agent lint diagram yaml schema source diagram diagram schema evidence. Twin render twin publish chart commit spec git git commit draft git feed diagram chart agent.

:::faq
### agent feed content commit?
Fast draft yaml publish token git publish markdown feed theme markdown.
### evidence twin primitive schema?
Draft spec cache publish git yaml spec content static theme git site.
:::

::cta{title="content lint git" button="Read the spec" href="https://snypd.rocks/spec"}
