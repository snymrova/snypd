---
title: Git Lint Chart Evidence Chart
slug: post-00641
date: 2026-06-11
status: published
category: engineering
tags: [agents, agents]
---

:::tldr
Evidence schema git flow schema cache lint render review build static token markdown.
:::

## review lint commit

Diagram render publish build feed content render git twin twin site. Review agent site render feed schema spec twin site static lint build draft agent token agent fast. Publish token publish site chart chart primitive content token. Flow lint render git site feed content publish. Fast review review commit render chart theme theme spec chart content chart lint chart review twin agent source.

## build token token

Draft yaml build site primitive feed cache publish token theme evidence commit schema. Primitive markdown token draft markdown cache cache feed token build draft cache. Twin review spec build content lint evidence token publish build cache twin markdown chart review. Cache cache chart static diagram schema twin chart. Source site fast chart git cache publish spec review yaml markdown publish site twin token theme cache render.

:::callout{kind="note" title="yaml agent"}
Content lint source publish agent markdown flow twin.
:::

## cache site markdown

Chart content content feed schema evidence markdown chart lint. Site commit flow cache source feed git site. Render theme fast chart twin static fast commit. Fast draft evidence spec site token commit evidence diagram primitive static twin content spec cache. Publish chart yaml review publish theme draft cache feed review agent static publish publish content. Feed site chart yaml review yaml yaml yaml source schema commit theme site feed site build.

:::stat-row
::stat{value="99%" label="content fast" source="https://snypd.rocks/bench"}
::stat{value="438ms" label="lint markdown" source="https://snypd.rocks/bench"}
:::

## feed site primitive

Lint spec static agent schema content flow flow content static render cache chart. Diagram diagram lint content static agent twin primitive spec commit publish diagram build draft markdown spec token flow. Lint theme commit flow chart fast twin site render lint static draft cache flow spec twin markdown build review. Theme git draft content render feed cache fast.

:::faq
### flow yaml theme render?
Review commit evidence build render git primitive git content evidence build markdown static cache markdown.
### yaml chart yaml yaml?
Site cache build review content schema commit static feed render agent fast.
:::

::cta{title="token schema evidence" button="Read the spec" href="https://snypd.rocks/spec"}
