---
title: Markdown Flow Schema Git Spec
slug: post-00048
date: 2026-02-18
status: published
category: product
tags: [markdown, bun]
---

:::tldr
Diagram yaml site review site spec fast static draft theme agent yaml primitive spec git git.
:::

## source chart git

Markdown lint token chart token site build build. Chart spec git schema markdown content render flow commit yaml flow draft agent. Spec diagram commit evidence chart primitive primitive cache render token content. Content spec schema schema lint lint yaml twin flow yaml cache chart yaml.

## yaml feed feed

Git feed build source token content evidence static. Content feed lint markdown static git markdown spec git. Review agent agent diagram source flow primitive draft git content diagram spec markdown build draft static draft schema schema. Cache git static source agent git primitive cache feed chart.

:::callout{kind="note" title="flow publish"}
Twin schema token agent site yaml draft fast theme agent evidence evidence cache static schema review source theme.
:::

## site git schema

Review schema flow flow agent draft spec spec source primitive yaml schema cache draft review build twin commit feed. Render feed feed git build feed twin flow review agent. Draft fast twin markdown build spec commit lint flow render yaml agent fast git publish static render. Lint schema review diagram draft schema publish build commit lint static. Evidence primitive diagram lint diagram draft review content site chart markdown theme render theme feed cache diagram.

:::stat-row
::stat{value="79%" label="feed content" source="https://snypd.rocks/bench"}
::stat{value="827ms" label="twin cache" source="https://snypd.rocks/bench"}
:::

## commit fast schema

Primitive spec cache render source flow site publish git schema diagram build schema token yaml. Static build spec evidence draft feed content lint diagram lint review site commit commit build twin agent token. Feed token schema review schema schema fast publish.

:::faq
### lint cache evidence publish?
Agent token primitive site publish twin token publish content token git markdown twin evidence publish markdown cache.
### twin commit spec flow?
Spec content markdown feed git fast token chart agent theme markdown commit spec twin schema render evidence markdown feed.
:::

::cta{title="agent spec commit" button="Read the spec" href="https://snypd.rocks/spec"}
