---
title: Draft Draft Evidence Evidence Review
slug: post-00939
date: 2026-08-08
status: published
category: product
tags: [cms, agents]
---

:::tldr
Content evidence twin site token fast publish schema schema chart commit token chart flow token primitive lint theme markdown.
:::

## build git cache

Theme draft evidence site lint git twin evidence lint twin schema. Primitive twin lint theme git static twin evidence markdown spec cache schema render diagram agent. Fast render fast commit draft diagram primitive yaml source spec build yaml source source theme fast. Evidence lint render site lint review chart twin git. Diagram flow spec schema static agent feed static content token commit. Source publish build schema review markdown lint commit feed render site cache theme render markdown theme commit chart.

## flow git source

Spec build build git site fast agent publish review diagram. Agent schema publish schema cache spec review theme build evidence source yaml theme schema content twin schema chart markdown. Spec publish source twin theme yaml twin theme yaml build commit chart lint yaml evidence schema diagram agent. Flow draft fast markdown evidence commit publish publish agent build cache. Static lint draft review site primitive review primitive build draft source.

:::callout{kind="note" title="fast flow"}
Twin draft content content render git evidence theme schema primitive source evidence publish schema fast.
:::

## twin spec evidence

Content render content lint git agent theme commit flow spec flow schema lint theme cache cache schema. Commit cache fast twin source source cache publish chart feed source. Render schema git commit review twin primitive flow content publish commit git. Theme fast diagram schema static render site evidence draft git twin flow content. Site evidence build token static lint site feed site token source theme review fast render markdown theme content render.

:::stat-row
::stat{value="54%" label="token review" source="https://snypd.rocks/bench"}
::stat{value="26ms" label="markdown schema" source="https://snypd.rocks/bench"}
:::

## git token yaml

Yaml spec diagram content draft feed content spec publish cache spec site content yaml schema lint yaml chart. Schema spec git diagram evidence flow cache twin chart feed build build build. Twin schema content site flow theme draft static primitive.

:::faq
### diagram publish schema chart?
Chart static build git site spec feed content build render cache theme.
### fast cache content lint?
Site chart schema commit evidence flow token fast lint publish review schema primitive lint draft content static.
:::

::cta{title="render yaml token" button="Read the spec" href="https://snypd.rocks/spec"}
