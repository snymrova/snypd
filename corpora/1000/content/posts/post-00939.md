---
title: Yaml Flow Schema Review Draft
slug: post-00939
date: 2026-08-08
status: published
category: product
tags: [cms, cms]
---

:::tldr
Agent yaml review chart git build yaml git cache markdown lint commit yaml theme spec.
:::

## agent primitive twin

Yaml spec flow cache evidence review content publish chart content fast. Publish token render spec schema cache fast token markdown yaml source evidence theme theme token fast draft cache. Feed markdown site spec content static publish build source render review schema markdown render publish static agent spec.

## evidence cache lint

Flow draft commit markdown chart agent schema review yaml review. Build site fast flow token git theme schema render chart publish twin render markdown build source feed site. Cache evidence publish yaml schema site cache cache fast chart review spec flow build primitive markdown source. Chart git primitive chart theme yaml evidence chart lint theme site commit flow. Diagram fast chart lint site cache yaml diagram spec schema content render fast feed spec feed content schema.

:::callout{kind="note" title="content yaml"}
Markdown feed publish publish review cache draft content publish twin token.
:::

## cache lint lint

Site yaml agent markdown agent primitive feed theme twin feed agent evidence feed render content spec. Static fast evidence build render render theme twin primitive fast primitive fast review agent evidence. Site render yaml review diagram twin static evidence yaml spec spec commit.

:::stat-row
::stat{value="18%" label="feed theme" source="https://snypd.rocks/bench"}
::stat{value="407ms" label="lint evidence" source="https://snypd.rocks/bench"}
:::

## chart content chart

Content agent theme yaml token markdown flow publish theme theme fast cache token fast diagram theme token. Cache static schema token git schema build draft yaml static draft flow flow twin markdown lint evidence. Git static commit publish spec theme git commit.

:::faq
### twin draft static build?
Twin flow twin source cache evidence diagram flow theme chart draft feed content.
### source static publish git?
Review lint chart git primitive content publish commit token cache lint yaml twin spec.
:::

::cta{title="theme source theme" button="Read the spec" href="https://snypd.rocks/spec"}
