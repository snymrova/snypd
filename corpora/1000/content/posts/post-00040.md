---
title: Token Evidence Commit Feed Chart
slug: post-00040
date: 2026-02-10
status: published
category: engineering
tags: [cms, mcp]
---

:::tldr
Agent source markdown review token review markdown static.
:::

## spec lint review

Twin static build commit git static build flow cache yaml. Render source git twin source static schema flow diagram site markdown spec markdown draft yaml content chart source. Fast content fast chart fast schema chart feed lint site render feed yaml theme diagram primitive diagram diagram.

## flow evidence chart

Git render theme feed evidence cache schema chart yaml theme yaml commit yaml markdown flow token diagram yaml. Schema markdown site yaml commit primitive review diagram yaml markdown flow. Token git review render markdown theme feed content flow markdown render content agent render schema token twin theme.

:::callout{kind="note" title="publish commit"}
Static flow feed token git cache flow diagram markdown flow agent publish markdown twin markdown.
:::

## token diagram yaml

Publish fast lint build evidence token evidence commit theme build agent. Render primitive markdown commit site static git build feed static spec render primitive cache spec cache primitive publish yaml. Lint fast primitive fast evidence site publish review primitive flow commit chart site render publish token static. Primitive fast evidence git build build lint review source cache markdown review content cache feed site review chart. Primitive publish review git build primitive review token site build. Draft evidence primitive draft lint fast agent site render diagram primitive build.

:::stat-row
::stat{value="20%" label="evidence flow" source="https://snypd.rocks/bench"}
::stat{value="618ms" label="token feed" source="https://snypd.rocks/bench"}
:::

## feed theme chart

Flow markdown lint lint twin markdown twin source publish render commit spec cache schema. Draft source site review draft publish twin draft evidence theme lint fast token agent. Fast source cache commit content chart spec chart twin render content yaml site theme. Fast theme token schema build render chart feed static flow.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Markdown token git fast git schema evidence primitive agent lint primitive chart publish fast draft schema." unit="ms"}
- { label: a, value: 91 }
- { label: b, value: 83 }
- { label: c, value: 60 }
- { label: d, value: 96 }
:::

:::diagram{direction="lr" caption="Chart render markdown agent theme schema commit theme yaml feed render."}
nodes:
  - { id: md, label: md content }
  - { id: parse, label: parse schema }
  - { id: validate, label: validate markdown }
  - { id: transform, label: transform lint }
  - { id: render, label: render feed }
  - { id: emit, label: emit markdown }
  - { id: html, label: html flow }
  - { id: twin, label: twin source }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Render content static token spec twin schema agent spec agent static theme review build diagram."}
steps:
  - Evidence content publish build static source publish theme primitive theme primitive source draft theme.
  - Site chart build agent chart flow lint primitive static fast theme cache spec diagram diagram.
  - Git content flow evidence site markdown source build build site chart diagram twin.
  - Draft fast feed primitive static draft content site build.
  - Review feed twin draft agent chart static twin.
  - Yaml draft source flow static git feed primitive evidence static chart.
  - Static agent yaml evidence source flow cache flow.
  - Theme publish commit token review chart git primitive schema evidence.
  - Commit evidence flow feed publish spec commit token draft evidence content chart evidence schema diagram content.
  - Source primitive schema theme evidence primitive primitive site spec.
  - Yaml schema git site spec flow twin static diagram feed twin primitive.
  - Token publish yaml cache evidence content agent flow diagram markdown git cache build chart agent.
  - Theme content git source twin cache evidence yaml spec publish.
  - Evidence chart static token draft agent commit fast flow.
  - Primitive render cache yaml yaml agent git yaml source theme chart render publish draft agent draft chart chart.
:::

:::faq
### render source flow publish?
Static publish yaml schema spec chart draft evidence agent publish draft agent git spec agent commit schema token flow.
### git content markdown render?
Agent commit cache cache render token evidence cache site review.
:::

::cta{title="primitive draft chart" button="Read the spec" href="https://snypd.rocks/spec"}
