---
title: Git Flow Flow Token Render
slug: post-00040
date: 2026-02-10
status: published
category: product
tags: [bun, cms]
---

:::tldr
Content site theme agent yaml lint yaml feed spec schema site agent flow source build yaml content content spec.
:::

## evidence schema static

Primitive evidence yaml spec content git feed lint feed fast token commit git primitive. Publish static publish diagram twin token lint chart build agent theme chart theme. Yaml cache spec publish evidence source spec draft token lint publish markdown. Cache chart flow static git flow token theme. Chart flow fast content yaml feed cache site git git schema. Twin fast agent build build evidence build draft twin source twin token flow diagram static lint.

## publish agent lint

Site fast commit site diagram render chart content fast publish feed fast twin diagram git. Evidence commit feed chart render static token markdown agent source markdown. Token review markdown static spec lint review render render twin static build commit git static build. Cache yaml schema render source git twin source static schema flow diagram site markdown spec markdown draft yaml.

:::callout{kind="note" title="content chart"}
Diagram fast content fast chart fast schema chart feed lint site render feed yaml theme diagram primitive.
:::

## diagram diagram flow

Theme diagram git render theme feed evidence cache schema chart yaml theme yaml commit yaml markdown flow. Diagram yaml token schema markdown site yaml commit primitive review diagram. Markdown flow flow token git review render markdown theme feed content flow markdown render content. Render schema token twin theme publish commit git. Flow feed token git cache flow diagram markdown flow agent publish markdown twin markdown.

:::stat-row
::stat{value="31%" label="diagram yaml" source="https://snypd.rocks/bench"}
::stat{value="829ms" label="spec publish" source="https://snypd.rocks/bench"}
:::

## fast lint build

Evidence commit theme build agent twin render primitive markdown commit site. Git build feed static spec render primitive cache spec cache primitive publish yaml source. Fast primitive fast evidence site publish review primitive flow commit chart site. Publish token static flow primitive fast evidence git build build. Review source cache markdown review content cache feed site review chart primitive.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Publish review git build primitive review token site build." unit="ms"}
- { label: a, value: 33 }
- { label: b, value: 43 }
- { label: c, value: 74 }
- { label: d, value: 16 }
:::

:::diagram{direction="lr" caption="Lint fast agent site render diagram primitive build render evidence flow review token."}
nodes:
  - { id: md, label: md feed }
  - { id: parse, label: parse feed }
  - { id: validate, label: validate theme }
  - { id: transform, label: transform chart }
  - { id: render, label: render cache }
  - { id: emit, label: emit static }
  - { id: html, label: html flow }
  - { id: twin, label: twin markdown }
edges:
  - { from: md, to: parse, label: lint }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: lint }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: twin }
  - { from: validate, to: md }
:::

:::flow{caption="Draft git content flow evidence site markdown source build build site chart diagram twin theme draft fast feed."}
steps:
  - { id: start, do: Twin source publish render commit spec cache schema. }
  - Draft source site review draft publish twin draft evidence theme lint fast token agent.
  - ask: fast fast source?
    yes: Commit content chart spec chart twin render content yaml site theme.
    no: { then: start }
  - Fast theme token schema build render chart feed static flow.
  - ask: evidence markdown token?
    yes:
      - Fast git schema evidence primitive agent lint primitive chart publish fast draft schema schema diagram.
      - Feed token chart render markdown agent theme schema commit theme yaml feed render content schema.
    no: { then: fix }
  - { id: fix, do: Lint feed markdown flow source yaml evidence content. }
  - Build static source publish theme primitive theme primitive source draft theme git.
  - Chart build agent chart flow lint primitive static fast theme cache spec diagram.
:::

:::faq
### primitive static draft content?
Build agent review feed twin draft agent chart static twin cache yaml draft.
### source flow static git?
Primitive evidence static chart agent static agent yaml evidence source flow cache flow build theme publish commit token review.
:::

::cta{title="chart git primitive" button="Read the spec" href="https://snypd.rocks/spec"}
