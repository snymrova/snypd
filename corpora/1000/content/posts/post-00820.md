---
title: Flow Twin Review Primitive Agent
slug: post-00820
date: 2026-04-11
status: published
category: product
tags: [mcp, cms]
---

:::tldr
Chart publish render source site chart lint fast.
:::

## site publish twin

Publish fast fast theme cache site diagram theme build draft twin token. Fast publish publish diagram render feed spec source. Git content lint lint primitive lint review cache primitive primitive theme theme evidence static schema content static primitive. Evidence primitive schema markdown site lint lint publish static fast theme draft schema feed site evidence draft theme.

## spec schema primitive

Markdown theme diagram lint lint content chart lint site. Fast build twin agent diagram render spec source schema markdown evidence schema lint publish schema. Build lint fast review evidence evidence cache source theme schema build build content agent publish. Source render commit evidence token chart flow draft render twin fast token markdown render review publish twin feed schema. Fast markdown evidence markdown theme render token agent flow diagram token cache agent chart chart. Static feed build git render git source review markdown flow feed agent agent cache yaml static feed draft.

:::callout{kind="note" title="site source"}
Diagram fast spec content commit twin yaml render primitive.
:::

## twin static token

Yaml feed cache draft lint yaml review review fast review chart spec cache twin evidence site. Source flow fast lint yaml yaml markdown token build agent. Theme fast markdown static review flow twin git. Markdown publish yaml evidence schema render schema primitive fast spec draft agent cache.

:::stat-row
::stat{value="51%" label="primitive schema" source="https://snypd.rocks/bench"}
::stat{value="210ms" label="site publish" source="https://snypd.rocks/bench"}
:::

## schema schema diagram

Review schema fast token review flow flow schema evidence source diagram twin publish publish. Agent static lint spec twin fast source commit yaml yaml diagram evidence theme lint. Build token spec review schema commit static schema build commit fast publish publish markdown spec. Theme primitive content primitive static token evidence markdown chart cache. Feed markdown markdown diagram chart evidence spec fast spec review build evidence spec chart. Fast fast diagram content primitive schema evidence content agent primitive cache publish spec git diagram fast primitive.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Lint token feed source static content fast flow flow." unit="ms"}
- { label: a, value: 47 }
- { label: b, value: 98 }
- { label: c, value: 59 }
- { label: d, value: 65 }
:::

:::diagram{direction="lr" caption="Commit evidence lint render cache primitive theme spec review commit."}
nodes:
  - { id: md, label: md review }
  - { id: parse, label: parse review }
  - { id: validate, label: validate render }
  - { id: transform, label: transform site }
  - { id: render, label: render review }
  - { id: emit, label: emit token }
  - { id: html, label: html site }
  - { id: twin, label: twin static }
edges:
  - { from: md, to: parse, label: render }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: lint }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: theme }
  - { from: validate, to: md }
:::

:::flow{caption="Fast primitive review feed diagram primitive build agent build commit flow."}
steps:
  - Token spec cache schema agent commit lint draft schema theme evidence git render yaml primitive markdown spec.
  - Commit review evidence review site primitive draft token draft render primitive theme site yaml primitive source theme publish.
  - Chart draft flow flow feed site cache theme yaml source fast evidence flow.
  - Commit flow review agent diagram flow schema git diagram source yaml chart chart source static.
  - Twin render twin build diagram static cache source.
  - Cache content static cache flow agent lint cache.
  - Markdown theme site draft render publish source build publish commit theme chart source static.
  - Render git publish diagram lint chart commit token fast yaml agent agent fast render.
  - Spec yaml agent draft render token static commit source build source build git draft token.
  - Build flow yaml evidence static fast cache theme fast git markdown token render.
  - Commit diagram commit flow flow markdown cache build flow git source feed build theme review commit.
  - Theme schema agent lint theme git build git review draft primitive commit agent draft diagram.
  - Spec agent evidence lint chart twin theme spec flow publish evidence draft yaml static publish primitive site token.
  - Static build publish feed cache token draft yaml git.
  - Site spec review draft token markdown spec yaml flow.
:::

:::faq
### content draft yaml render?
Source spec flow evidence static schema flow fast source markdown git render cache token content lint diagram review.
### static git markdown twin?
Build primitive flow twin publish spec site yaml token markdown diagram git.
:::

::cta{title="schema lint token" button="Read the spec" href="https://snypd.rocks/spec"}
