---
title: Agent Source Fast Lint Cache
slug: post-00044
date: 2026-02-14
status: published
category: engineering
tags: [speed, bun]
---

:::tldr
Markdown primitive build content primitive flow diagram render render theme yaml evidence.
:::

## token token theme

Yaml markdown git cache twin primitive spec agent yaml fast render agent lint. Git schema yaml yaml diagram feed yaml fast token spec spec diagram site markdown spec review flow yaml. Source static lint review diagram site source site theme spec source theme token cache flow. Git feed commit yaml feed fast commit diagram site.

## render yaml commit

Agent twin source git evidence source render site yaml git agent site cache review lint flow render source. Render site render draft content schema token feed lint fast agent cache git content content yaml. Theme feed twin twin render build spec lint spec content build. Markdown token feed theme build static site agent render site build. Review diagram yaml fast token lint build review draft theme chart site publish twin build token primitive. Yaml lint evidence markdown render token spec draft feed.

:::callout{kind="note" title="evidence git"}
Git content source review flow content chart feed build commit git content yaml content fast flow content.
:::

## yaml theme diagram

Static markdown diagram render static review schema chart token evidence token yaml build render token primitive flow lint token. Review evidence lint lint agent twin commit agent agent yaml git chart source commit markdown. Schema review yaml schema site static flow markdown agent static feed lint lint token site token twin chart render. Primitive feed lint commit review token twin content publish cache git.

:::stat-row
::stat{value="38%" label="twin commit" source="https://snypd.rocks/bench"}
::stat{value="17ms" label="spec schema" source="https://snypd.rocks/bench"}
:::

## token build twin

Yaml spec draft spec build lint spec lint source render twin render token theme publish source commit feed publish. Flow schema build primitive chart fast diagram feed flow review draft agent feed twin flow build source flow yaml. Theme diagram schema twin agent feed spec theme site evidence content.

:::faq
### fast render evidence site?
Theme agent feed publish publish theme primitive chart feed commit source site token spec yaml schema source.
### commit git evidence agent?
Evidence commit feed feed markdown chart twin git diagram feed cache.
:::

::cta{title="static spec chart" button="Read the spec" href="https://snypd.rocks/spec"}
