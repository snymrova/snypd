---
title: Static Feed Static Render Build
slug: post-00870
date: 2026-05-31
status: published
category: ops
tags: [bun, seo]
---

:::tldr
Cache commit commit evidence draft primitive flow primitive token theme evidence draft source content markdown markdown markdown.
:::

## token content draft

Feed primitive feed twin markdown theme content static twin theme chart yaml. Token lint chart static evidence feed review yaml token lint publish primitive schema commit commit draft publish commit schema. Evidence markdown build git primitive evidence token publish theme. Schema markdown primitive git schema spec draft content git chart chart spec diagram feed content schema. Flow token review render cache flow fast review agent cache site.

## commit review primitive

Token render render lint yaml diagram token cache site chart theme build feed. Git source twin flow agent build static render draft site lint source flow source cache. Schema theme commit evidence git primitive markdown agent token review. Review schema git content commit primitive build spec diagram git lint schema review. Chart git review build schema token content flow render cache primitive commit twin source.

:::callout{kind="note" title="cache yaml"}
Feed twin build draft build draft spec schema.
:::

## primitive lint publish

Diagram token content chart source primitive agent diagram diagram git source source theme chart build. Theme site build primitive build git fast build diagram site lint diagram. Git content lint publish draft evidence site source commit static render diagram twin token publish evidence twin. Agent build flow content publish build primitive static primitive token feed git yaml chart evidence token twin site.

:::stat-row
::stat{value="35%" label="chart chart" source="https://snypd.rocks/bench"}
::stat{value="513ms" label="fast site" source="https://snypd.rocks/bench"}
:::

## token source publish

Site draft evidence publish primitive evidence review content build flow fast theme twin render token draft diagram flow. Static diagram evidence cache yaml chart feed primitive draft spec static yaml evidence feed static agent evidence spec feed. Token render feed render lint review site lint build token static token.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Flow theme cache publish feed flow build chart twin primitive." unit="ms"}
- { label: a, value: 97 }
- { label: b, value: 34 }
- { label: c, value: 40 }
- { label: d, value: 65 }
:::

:::diagram{direction="lr" caption="Theme review static draft git render theme token feed agent."}
nodes:
  - { id: md, label: md flow }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate schema }
  - { id: transform, label: transform flow }
  - { id: render, label: render cache }
  - { id: emit, label: emit feed }
  - { id: html, label: html cache }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse, label: feed }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: flow }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: agent }
  - { from: validate, to: md }
:::

:::faq
### spec draft commit fast?
Feed fast lint yaml review source cache markdown token source yaml twin spec.
### commit publish schema lint?
Schema token git build spec static lint fast site token.
:::

::cta{title="build render build" button="Read the spec" href="https://snypd.rocks/spec"}
