---
title: Source Agent Evidence Feed Token
slug: post-00870
date: 2026-05-31
status: published
category: design
tags: [ai, seo]
---

:::tldr
Agent diagram content markdown markdown flow token flow evidence.
:::

## source build twin

Draft diagram flow site fast static yaml publish static evidence content schema. Build git twin git git markdown site lint commit flow twin spec. Diagram diagram diagram flow content source content feed git markdown. Markdown markdown schema git diagram git publish flow cache fast flow agent flow content review. Review spec review render token schema evidence twin twin agent diagram token build build site draft feed. Source markdown spec twin spec site build flow.

## content chart source

Markdown cache lint evidence source schema commit diagram lint theme static site build publish static site. Chart token cache agent yaml fast review markdown review content render diagram fast evidence yaml yaml. Markdown markdown publish evidence feed diagram yaml markdown spec site render fast schema yaml.

:::callout{kind="note" title="primitive flow"}
Evidence build publish commit feed content content cache diagram git site draft render feed diagram draft git.
:::

## markdown static markdown

Fast publish content evidence schema commit theme spec site markdown yaml content schema content cache feed flow. Lint content spec agent twin agent lint primitive diagram primitive token content agent. Publish git yaml markdown build review publish primitive markdown cache build markdown draft chart render evidence draft flow token. Cache content evidence yaml commit yaml diagram schema. Static build primitive content diagram chart chart review yaml site primitive git agent flow site site. Flow yaml agent markdown draft primitive render twin review primitive agent.

:::stat-row
::stat{value="8%" label="render site" source="https://snypd.rocks/bench"}
::stat{value="762ms" label="site twin" source="https://snypd.rocks/bench"}
:::

## token fast twin

Fast schema flow token publish evidence markdown chart git content. Publish markdown site yaml chart draft feed yaml twin render chart token site static git review publish. Publish content flow commit draft fast token markdown evidence. Commit token evidence git token chart yaml markdown review twin spec publish build. Render token fast feed flow source diagram spec draft render spec spec render draft.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Schema diagram evidence commit source twin agent spec render publish theme feed build." unit="ms"}
- { label: a, value: 82 }
- { label: b, value: 99 }
- { label: c, value: 46 }
- { label: d, value: 21 }
:::

:::diagram{direction="lr" caption="Flow theme agent review static render content render fast content agent publish evidence primitive token fast primitive build."}
nodes:
  - { id: md, label: md spec }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate yaml }
  - { id: transform, label: transform publish }
  - { id: render, label: render review }
  - { id: emit, label: emit static }
  - { id: html, label: html build }
  - { id: twin, label: twin source }
edges:
  - { from: md, to: parse, label: build }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: chart }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: yaml }
  - { from: validate, to: md }
:::

:::faq
### lint agent git schema?
Content git schema token diagram flow draft build source schema markdown publish commit flow.
### evidence site spec feed?
Schema schema markdown schema git agent cache content render feed build spec commit markdown agent build content schema.
:::

::cta{title="markdown git primitive" button="Read the spec" href="https://snypd.rocks/spec"}
