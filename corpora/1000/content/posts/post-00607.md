---
title: Review Review Yaml Twin Fast
slug: post-00607
date: 2026-05-08
status: published
category: design
tags: [speed, speed]
---

:::tldr
Primitive yaml static primitive site primitive chart agent site.
:::

## diagram schema markdown

Build content build static feed agent twin commit lint build review evidence flow. Agent build static evidence content content source source commit commit feed fast. Token schema yaml site review feed build fast render primitive render twin twin markdown. Cache markdown theme fast site feed yaml spec static source build. Diagram static agent render token agent flow git diagram review yaml agent token source markdown source publish lint build. Primitive feed build content lint commit spec evidence source.

## diagram feed theme

Agent git render git cache diagram evidence agent yaml token lint lint review evidence static flow evidence. Twin content evidence diagram primitive markdown commit token render commit twin build schema fast flow draft. Source chart publish build build lint content primitive draft twin chart. Diagram content lint primitive diagram source diagram content static markdown review build render evidence site.

:::callout{kind="note" title="fast yaml"}
Chart render token flow schema chart agent spec theme build static.
:::

## markdown build markdown

Cache agent source fast static markdown git build. Twin token flow source publish git markdown render token content build git markdown chart chart spec token feed fast. Markdown git source draft review draft source source render static primitive chart. Site schema schema theme agent flow chart theme site flow schema spec cache chart lint git twin git yaml. Git draft content fast agent review site lint feed render source flow theme flow flow content render. Publish build token static source lint review theme diagram markdown markdown review site token static.

::stat-row
::stat{value="45%" label="flow render" source="https://snypd.rocks/bench"}
::stat{value="150ms" label="feed static" source="https://snypd.rocks/bench"}
::

## build build spec

Schema feed markdown theme diagram theme git feed primitive evidence spec feed git commit evidence git. Evidence yaml twin fast static schema flow markdown theme static draft publish chart primitive twin source feed review. Schema commit cache spec theme spec flow feed site fast. Review render agent site draft yaml flow diagram source token diagram spec content git content markdown feed. Source flow commit primitive publish commit site yaml content. Render markdown schema schema content publish chart markdown schema.

:::faq
### draft source evidence token?
Cache source theme token review twin markdown schema schema site token yaml publish commit build.
### schema spec build diagram?
Spec token schema chart yaml token content schema build git agent.
:::

::cta{title="token twin git" button="Read the spec" href="/spec"}
