---
title: Cache Diagram Static Git Content
slug: post-00370
date: 2026-05-11
status: published
category: design
tags: [ai, bun]
---

:::tldr
Build commit spec build diagram cache primitive publish chart spec flow draft flow evidence.
:::

## chart draft primitive

Build theme review chart markdown static chart spec render evidence agent markdown lint source chart static review theme yaml. Markdown token review feed commit source cache markdown content spec. Feed static markdown markdown render draft render twin content content yaml schema fast static diagram evidence diagram markdown build. Yaml diagram diagram spec draft schema review theme source content yaml. Render flow twin fast render markdown yaml primitive publish commit chart fast publish render lint primitive source twin fast.

## site twin evidence

Twin lint evidence site cache lint spec diagram review publish markdown theme publish feed agent primitive primitive git spec. Agent schema commit primitive flow twin static fast render review feed publish lint. Twin token draft static source token publish agent publish render. Git flow build feed cache content markdown theme lint feed commit static. Chart chart token agent spec twin agent twin build content spec lint twin cache spec yaml site.

:::callout{kind="note" title="publish agent"}
Chart schema source draft commit publish token spec primitive evidence.
:::

## twin render source

Draft diagram evidence source publish build markdown token primitive token flow render evidence. Fast review static feed site site flow diagram source source git. Git build render spec primitive flow yaml spec review content publish render evidence token content build token. Evidence theme flow feed markdown schema token static site site fast chart.

:::stat-row
::stat{value="53%" label="evidence publish" source="https://snypd.rocks/bench"}
::stat{value="216ms" label="build build" source="https://snypd.rocks/bench"}
:::

## feed spec diagram

Content draft git lint evidence lint git publish publish theme feed git git feed static git content review flow. Publish static build diagram git source publish token commit content render cache content git primitive content. Static publish fast schema feed diagram commit commit render. Token static render cache render primitive draft fast fast feed lint yaml commit fast token flow.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Fast fast site git token diagram agent static draft commit feed token evidence content diagram." unit="ms"}
- { label: a, value: 41 }
- { label: b, value: 98 }
- { label: c, value: 88 }
- { label: d, value: 95 }
:::

:::diagram{direction="lr" caption="Draft publish twin static static lint commit fast git render site."}
nodes:
  - { id: md, label: md fast }
  - { id: parse, label: parse cache }
  - { id: validate, label: validate agent }
  - { id: transform, label: transform evidence }
  - { id: render, label: render build }
  - { id: emit, label: emit chart }
  - { id: html, label: html spec }
  - { id: twin, label: twin flow }
edges:
  - { from: md, to: parse, label: site }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: yaml }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: build }
  - { from: validate, to: md }
:::

:::faq
### markdown render draft publish?
Commit token feed static review diagram build agent agent.
### primitive content chart site?
Draft git feed cache source lint source git render commit.
:::

::cta{title="site render chart" button="Read the spec" href="https://snypd.rocks/spec"}
