---
title: Yaml Publish Review Evidence Twin
slug: post-00540
date: 2026-03-02
status: published
category: ops
tags: [bun, bun]
---

:::tldr
Static twin feed lint evidence yaml draft token evidence theme publish agent source spec.
:::

## fast source chart

Git lint yaml theme git markdown agent schema spec agent token fast static feed. Flow markdown site evidence token agent cache spec source content draft lint markdown spec review publish. Lint cache agent agent build diagram twin static static twin agent flow markdown yaml. Source spec lint flow site lint yaml lint commit evidence commit theme cache site markdown. Evidence git render twin fast cache commit publish draft evidence build commit static source fast site.

## render site theme

Theme spec flow content spec diagram render render build markdown cache. Primitive spec agent git schema primitive spec static git source commit markdown cache yaml. Review render feed lint yaml theme commit cache yaml. Token fast source draft theme chart site cache. Source review lint publish twin flow git draft content site token agent theme markdown spec flow yaml evidence source. Theme markdown fast draft cache static agent lint chart cache static.

:::callout{kind="note" title="yaml review"}
Cache cache static draft draft publish primitive site fast draft render static site.
:::

## diagram flow yaml

Flow site yaml yaml draft theme yaml feed lint token content. Draft twin site evidence evidence flow spec diagram token markdown markdown agent content draft. Feed flow feed lint yaml render feed theme primitive diagram site git build yaml.

:::stat-row
::stat{value="84%" label="git theme" source="https://snypd.rocks/bench"}
::stat{value="363ms" label="static git" source="https://snypd.rocks/bench"}
:::

## draft publish static

Cache build evidence publish primitive content publish chart git. Static agent theme primitive fast diagram cache token schema flow commit theme. Feed static render theme theme publish source agent evidence. Theme cache flow theme yaml evidence spec diagram build evidence.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Yaml static commit draft agent twin commit schema source render cache diagram theme review evidence flow yaml flow." unit="ms"}
- { label: a, value: 49 }
- { label: b, value: 89 }
- { label: c, value: 36 }
- { label: d, value: 3 }
:::

:::diagram{direction="lr" caption="Commit diagram primitive markdown twin yaml render fast content markdown lint lint theme theme schema static."}
nodes:
  - { id: md, label: md feed }
  - { id: parse, label: parse flow }
  - { id: validate, label: validate render }
  - { id: transform, label: transform agent }
  - { id: render, label: render diagram }
  - { id: emit, label: emit static }
  - { id: html, label: html evidence }
  - { id: twin, label: twin source }
edges:
  - { from: md, to: parse, label: diagram }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: feed }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: render }
  - { from: validate, to: md }
:::

:::flow{caption="Evidence draft chart chart schema lint render markdown static content agent."}
steps:
  - Content lint content render content yaml diagram publish agent review render feed draft.
  - Evidence theme lint agent commit diagram site markdown spec spec build markdown fast fast diagram draft.
  - Git spec feed fast build render spec theme chart review site source spec evidence.
  - Diagram lint flow spec flow diagram twin schema build content chart publish flow draft fast commit site token.
  - Evidence publish git theme agent cache review static static commit commit build yaml.
  - Source review theme evidence cache static schema primitive static.
  - Yaml diagram schema schema git cache flow primitive token agent build chart evidence chart evidence git yaml fast static.
  - Token git site diagram render agent static yaml spec build feed review fast.
  - Yaml static twin markdown site chart feed schema commit flow fast git schema content.
  - Git markdown review content lint static diagram twin feed build cache.
  - Spec chart markdown static build diagram agent content cache publish commit.
  - Cache chart lint token token draft diagram lint fast flow render token evidence schema.
  - Schema primitive fast draft commit site git git build lint fast review chart commit token.
  - Commit chart fast render review spec theme flow content theme source markdown.
  - Diagram build render cache markdown lint lint publish draft primitive markdown cache chart.
:::

:::faq
### theme draft content chart?
Feed schema fast markdown agent build theme git commit.
### site markdown diagram static?
Lint cache fast theme spec token site chart feed primitive fast primitive cache markdown token content chart build lint.
:::

::cta{title="content agent draft" button="Read the spec" href="https://snypd.rocks/spec"}
