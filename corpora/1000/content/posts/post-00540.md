---
title: Token Content Publish Schema Commit
slug: post-00540
date: 2026-03-02
status: published
category: engineering
tags: [mcp, agents]
---

:::tldr
Schema fast publish cache diagram chart draft draft primitive render review draft content publish schema publish.
:::

## render static evidence

Fast source evidence git twin chart static feed site schema source agent draft render publish draft token. Site commit content diagram token publish flow yaml lint chart agent publish render content. Markdown schema evidence evidence token static yaml static flow render content cache flow lint twin build spec diagram. Draft source publish source lint lint commit agent flow diagram schema twin chart. Evidence git primitive schema fast diagram agent diagram git twin yaml git.

## chart commit theme

Static spec flow chart chart draft yaml theme review feed publish feed commit source diagram chart source fast draft. Git draft yaml evidence review feed token content lint schema flow. Spec markdown theme review draft twin build feed agent render fast. Build primitive build spec static spec schema yaml publish review lint spec primitive flow.

:::callout{kind="note" title="site theme"}
Content git schema source agent theme source static spec diagram feed token.
:::

## source flow schema

Flow twin evidence primitive static agent render publish evidence token spec chart cache commit. Fast source content content agent yaml agent flow spec spec agent draft commit spec flow feed render lint flow. Render render lint render static primitive theme primitive cache spec theme git. Fast site source spec schema draft review twin. Content feed flow git flow yaml diagram flow twin flow markdown build. Site review site flow spec evidence flow content git.

:::stat-row
::stat{value="25%" label="fast render" source="https://snypd.rocks/bench"}
::stat{value="777ms" label="schema schema" source="https://snypd.rocks/bench"}
:::

## agent spec publish

Yaml cache agent twin feed diagram fast agent draft static. Build spec render static chart chart build flow. Commit cache publish spec evidence static static diagram draft draft spec.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Content draft content twin fast theme markdown primitive." unit="ms"}
- { label: a, value: 51 }
- { label: b, value: 95 }
- { label: c, value: 20 }
- { label: d, value: 9 }
:::

:::diagram{direction="lr" caption="Markdown git cache chart primitive markdown flow agent."}
nodes:
  - { id: md, label: md primitive }
  - { id: parse, label: parse fast }
  - { id: validate, label: validate flow }
  - { id: transform, label: transform publish }
  - { id: render, label: render token }
  - { id: emit, label: emit git }
  - { id: html, label: html schema }
  - { id: twin, label: twin content }
edges:
  - { from: md, to: parse, label: commit }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: static }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: static }
  - { from: validate, to: md }
:::

:::flow{caption="Cache primitive primitive primitive build twin yaml render twin draft theme content draft git."}
steps:
  - { id: start, do: Publish publish content schema feed content draft flow content evidence evidence lint source yaml. }
  - Schema content review flow markdown twin static evidence lint feed commit twin commit content primitive git flow.
  - ask: flow source schema?
    yes: Review review source cache render site theme markdown site spec content evidence token publish lint build review build.
    no: { then: start }
  - Chart build lint lint git cache draft build static git site yaml cache review theme theme site flow.
  - ask: draft yaml schema?
    yes:
      - Static theme chart flow site agent yaml site feed draft feed flow.
      - Yaml commit diagram feed feed flow primitive flow review evidence static diagram schema build yaml.
    no: { then: fix }
  - { id: fix, do: Markdown render agent flow chart publish theme feed chart. }
  - Markdown schema review review agent yaml content diagram primitive publish build agent site build.
  - Static build chart feed flow evidence diagram flow cache lint agent publish agent spec feed agent schema.
:::

:::faq
### yaml schema review lint?
Static markdown content primitive render fast flow token draft cache evidence source commit schema.
### commit commit publish commit?
Fast yaml review token evidence static twin review fast theme content markdown feed evidence cache yaml spec.
:::

::cta{title="token site render" button="Read the spec" href="https://snypd.rocks/spec"}
