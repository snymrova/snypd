---
title: Fast Chart Markdown Chart Site
slug: post-00540
date: 2026-03-02
status: published
category: design
tags: [mcp, bun]
---

:::tldr
Chart evidence chart agent feed render commit flow lint.
:::

## build review content

Fast source cache review content feed static site render. Feed content primitive render flow static feed publish feed yaml fast chart feed. Publish markdown fast schema feed diagram chart evidence spec markdown token static schema. Publish content spec spec cache flow twin fast fast chart diagram static chart diagram feed feed. Theme commit build feed markdown theme yaml agent markdown token.

## review fast render

Twin source commit draft site feed theme yaml feed fast chart primitive static static. Diagram site diagram diagram yaml publish review evidence twin feed draft publish fast static twin feed. Evidence yaml draft token evidence theme publish agent source spec fast source. Yaml fast git lint yaml theme git markdown agent schema spec agent token fast static feed evidence. Markdown site evidence token agent cache spec source content draft lint markdown spec review publish fast lint cache. Agent build diagram twin static static twin agent.

:::callout{kind="note" title="flow markdown"}
Git source spec lint flow site lint yaml lint commit evidence commit theme cache site.
:::

## markdown evidence evidence

Twin fast cache commit publish draft evidence build commit static. Fast site render site theme feed cache theme spec flow content spec diagram render render build markdown. Fast primitive spec agent git schema primitive spec static git source. Markdown cache yaml theme review render feed lint yaml theme commit cache yaml agent token. Source draft theme chart site cache twin source review lint publish twin flow git.

:::stat-row
::stat{value="43%" label="content site" source="https://snypd.rocks/bench"}
::stat{value="287ms" label="agent theme" source="https://snypd.rocks/bench"}
:::

## markdown spec flow

Source cache theme markdown fast draft cache static agent lint chart cache static yaml review site. Cache static draft draft publish primitive site fast draft render static. Diagram flow yaml build token flow site yaml yaml draft theme yaml feed. Token content fast draft twin site evidence evidence flow spec diagram token. Markdown agent content draft static feed flow feed.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Yaml render feed theme primitive diagram site git build yaml diagram git." unit="ms"}
- { label: a, value: 12 }
- { label: b, value: 40 }
- { label: c, value: 52 }
- { label: d, value: 62 }
:::

:::diagram{direction="lr" caption="Publish static site primitive cache build evidence publish primitive content publish chart git."}
nodes:
  - { id: md, label: md lint }
  - { id: parse, label: parse static }
  - { id: validate, label: validate agent }
  - { id: transform, label: transform theme }
  - { id: render, label: render primitive }
  - { id: emit, label: emit fast }
  - { id: html, label: html diagram }
  - { id: twin, label: twin cache }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Spec chart markdown static build diagram agent content cache publish commit."}
steps:
  - Schema flow commit theme theme feed static render theme theme publish.
  - Agent evidence primitive theme cache flow theme yaml evidence spec diagram build evidence diagram yaml static commit.
  - Agent twin commit schema source render cache diagram theme review evidence flow yaml.
  - Site schema lint agent review commit diagram primitive markdown twin yaml render fast content markdown lint lint theme.
  - Schema static feed flow render agent diagram static evidence.
  - Diagram feed render site content lint content render content yaml diagram publish agent review render feed draft.
  - Evidence theme lint agent commit diagram site markdown spec spec build markdown fast fast diagram draft.
  - Git spec feed fast build render spec theme chart review site source spec evidence.
  - Diagram lint flow spec flow diagram twin schema build content chart publish flow draft fast commit site token.
  - Evidence publish git theme agent cache review static static commit commit build yaml.
  - Source review theme evidence cache static schema primitive static.
  - Yaml diagram schema schema git cache flow primitive token agent build chart evidence chart evidence git yaml fast static.
  - Token git site diagram render agent static yaml spec build feed review fast.
  - Yaml static twin markdown site chart feed schema commit flow fast git schema content.
  - Git markdown review content lint static diagram twin feed build cache.
:::

:::faq
### static cache chart lint?
Token draft diagram lint fast flow render token evidence schema git.
### schema primitive fast draft?
Site git git build lint fast review chart commit token spec commit chart fast render review.
:::

::cta{title="spec theme flow" button="Read the spec" href="https://snypd.rocks/spec"}
