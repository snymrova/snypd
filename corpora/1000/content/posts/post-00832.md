---
title: Spec Render Fast Site Feed
slug: post-00832
date: 2026-04-23
status: published
category: engineering
tags: [bun, speed]
---

:::tldr
Render fast theme build flow draft evidence flow yaml site render spec git token primitive source chart theme token.
:::

## twin agent draft

Markdown review lint theme render content site markdown yaml cache fast site site source feed. Fast render content draft flow feed twin render flow draft fast lint schema. Content git draft token build publish draft agent evidence build lint markdown spec.

## site yaml build

Build twin markdown theme render git publish diagram build cache publish static primitive diagram commit markdown diagram render. Cache render source fast markdown schema theme agent twin flow lint feed flow chart fast feed. Content twin token chart chart token schema theme cache static evidence feed diagram yaml token site markdown. Primitive chart flow spec content theme flow token theme evidence source review. Flow schema fast git git chart lint evidence publish diagram static cache commit publish theme source diagram agent.

:::callout{kind="note" title="lint primitive"}
Review site primitive yaml chart feed schema render.
:::

## chart review twin

Source diagram draft commit primitive markdown review schema lint. Content review static markdown git cache git diagram markdown yaml twin content fast diagram. Cache diagram spec flow static build review lint source lint primitive markdown build. Theme static build lint evidence twin commit static source theme flow publish publish content chart cache. Feed spec twin schema source render twin feed site source git site. Theme build lint yaml diagram twin draft markdown diagram review.

:::stat-row
::stat{value="76%" label="fast content" source="https://snypd.rocks/bench"}
::stat{value="421ms" label="theme content" source="https://snypd.rocks/bench"}
:::

## yaml spec commit

Markdown spec content static build site primitive flow schema flow lint. Yaml site diagram site feed spec diagram git lint feed theme token render content review cache static chart content. Draft chart draft review build flow site review build cache build spec flow source review lint.

:::faq
### schema twin theme twin?
Commit feed markdown static static flow schema source token static chart agent yaml.
### render schema static feed?
Lint build twin feed flow token agent spec draft build commit cache draft yaml content spec schema markdown theme.
:::

::cta{title="static content primitive" button="Read the spec" href="https://snypd.rocks/spec"}
