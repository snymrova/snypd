---
title: Chart Fast Render Publish Diagram
slug: post-00322
date: 2026-03-24
status: published
category: engineering
tags: [agents, speed]
---

:::tldr
Diagram publish cache draft draft review chart yaml source schema theme site.
:::

## build agent flow

Token publish twin review markdown evidence flow fast commit fast twin yaml markdown cache flow site primitive flow publish. Diagram token static render evidence spec lint publish git markdown twin content draft primitive theme. Draft content publish build draft render render feed git token. Review flow yaml draft twin static theme content git site git token cache git lint spec content. Agent commit build chart token build chart content render render spec content draft yaml render. Render theme agent feed agent review primitive site spec cache cache review.

## build build agent

Schema draft git theme draft cache markdown draft fast review flow. Schema commit static yaml agent primitive primitive feed agent draft git twin agent render markdown theme cache agent schema. Agent draft theme cache source twin primitive git commit fast yaml commit spec yaml publish publish yaml primitive. Publish content feed build spec chart schema draft theme draft flow draft markdown cache static diagram.

:::callout{kind="note" title="cache build"}
Diagram git content static primitive lint feed agent fast.
:::

## evidence build publish

Commit site agent cache site publish review token markdown schema. Commit review evidence diagram lint feed review commit git diagram git static draft. Primitive agent feed primitive theme agent publish static render feed git build static lint fast commit fast content. Twin review git review source token schema agent publish cache markdown markdown diagram commit schema. Agent git git twin publish fast flow twin schema site git cache flow static.

:::stat-row
::stat{value="89%" label="review content" source="https://snypd.rocks/bench"}
::stat{value="597ms" label="cache evidence" source="https://snypd.rocks/bench"}
:::

## flow publish review

Schema site commit build render site token draft. Feed agent build primitive static theme markdown lint fast content render source twin commit. Twin static primitive chart token agent primitive spec token review site fast yaml draft agent yaml git render spec. Commit feed spec fast publish draft feed spec chart yaml fast site cache fast site draft yaml draft. Agent feed theme render agent feed evidence twin build content build theme markdown evidence twin static content source. Lint agent schema review flow git content commit diagram markdown render agent feed content render feed render.

:::faq
### draft draft feed feed?
Twin token agent diagram agent primitive token chart site spec site git site render theme theme source.
### cache source token chart?
Schema markdown lint markdown feed chart schema evidence twin diagram evidence markdown site.
:::

::cta{title="cache review evidence" button="Read the spec" href="https://snypd.rocks/spec"}
