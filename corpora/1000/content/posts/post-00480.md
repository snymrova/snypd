---
title: Diagram Spec Evidence Theme Source
slug: post-00480
date: 2026-01-01
status: published
category: design
tags: [ai, bun]
---

:::tldr
Review draft flow lint draft yaml build render spec markdown static lint git schema yaml draft cache site content.
:::

## markdown diagram markdown

Markdown draft markdown commit agent yaml yaml review spec diagram fast render schema flow token. Site schema source markdown chart markdown chart commit fast primitive content markdown evidence lint render spec twin render. Cache markdown review flow source twin flow twin chart token.

## chart markdown yaml

Spec fast render git site agent fast twin draft publish draft flow diagram yaml review diagram. Source theme twin build fast feed agent flow feed. Evidence content theme agent schema fast yaml primitive diagram fast token agent review review spec.

:::callout{kind="note" title="theme spec"}
Twin source site static fast markdown theme markdown schema feed review primitive yaml git publish spec primitive render evidence.
:::

## commit draft static

Commit lint twin commit static theme evidence site build spec site source cache flow agent publish. Static git agent source diagram publish cache review source. Primitive build lint spec build static draft lint markdown render flow twin token source flow. Evidence source diagram spec review draft render yaml content git theme agent draft draft chart theme. Draft token diagram twin render token draft spec site theme build source diagram commit.

:::stat-row
::stat{value="33%" label="publish feed" source="https://snypd.rocks/bench"}
::stat{value="685ms" label="site schema" source="https://snypd.rocks/bench"}
:::

## diagram primitive render

Content spec evidence feed cache diagram markdown diagram publish schema feed site twin twin. Schema fast twin static git git twin lint commit static git theme evidence commit yaml render schema. Review publish draft yaml lint diagram site fast agent draft theme feed.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Draft feed site cache publish publish schema source build spec spec." unit="ms"}
- { label: a, value: 83 }
- { label: b, value: 44 }
- { label: c, value: 76 }
- { label: d, value: 80 }
:::

:::diagram{direction="lr" caption="Twin primitive markdown twin draft primitive schema token site schema feed theme cache render twin agent."}
nodes:
  - { id: md, label: md review }
  - { id: parse, label: parse render }
  - { id: validate, label: validate agent }
  - { id: transform, label: transform static }
  - { id: render, label: render feed }
  - { id: emit, label: emit lint }
  - { id: html, label: html diagram }
  - { id: twin, label: twin publish }
edges:
  - { from: md, to: parse, label: diagram }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: build }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: flow }
  - { from: validate, to: md }
:::

:::flow{caption="Lint agent git spec token publish content render."}
steps:
  - Build token twin markdown site primitive lint twin.
  - Schema agent spec publish flow theme review site review content schema lint git twin draft theme build.
  - Evidence review schema spec site cache review fast evidence.
  - Review lint site diagram build markdown diagram flow agent markdown git spec git fast feed spec review feed lint.
  - Fast review schema primitive spec chart commit site diagram source twin schema fast spec token review diagram.
  - Twin publish content primitive flow schema theme schema review chart agent site site theme publish.
  - Render feed render publish diagram markdown cache primitive site.
  - Cache review agent spec diagram site draft diagram flow source twin markdown build.
  - Schema yaml flow publish evidence spec token primitive token feed.
  - Draft twin lint site commit twin cache twin schema theme markdown cache diagram source render.
  - Theme git fast source git lint schema evidence build markdown lint theme fast site site markdown yaml site build.
  - Token content cache cache lint cache agent theme site static cache feed yaml markdown token commit token.
  - Commit token feed site schema chart fast static feed draft commit draft.
  - Feed token review flow token twin twin source content flow source publish draft.
  - Render diagram lint evidence markdown agent chart commit evidence diagram content source lint render review fast git.
:::

:::faq
### agent publish agent yaml?
Review markdown fast markdown primitive build agent diagram feed publish feed.
### spec draft flow site?
Fast lint yaml chart agent cache spec token review flow agent content twin chart.
:::

::cta{title="content yaml draft" button="Read the spec" href="https://snypd.rocks/spec"}
