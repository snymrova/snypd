---
title: Primitive Site Draft Fast Commit
slug: post-00480
date: 2026-01-01
status: published
category: engineering
tags: [ai, bun]
---

:::tldr
Site source spec feed token feed chart cache static cache evidence commit token content flow site render review cache.
:::

## markdown content render

Agent primitive render commit flow content cache cache render static publish review theme source commit twin publish. Token draft spec markdown schema lint static lint publish static markdown spec evidence fast. Feed commit commit static site site schema content evidence spec diagram static token fast static flow markdown render.

## commit cache git

Commit fast evidence review flow cache static static fast schema. Flow markdown publish source cache build site theme feed agent git site theme review markdown. Static spec render evidence twin diagram feed primitive yaml static. Primitive review primitive fast site build content yaml commit feed git theme feed yaml agent build.

:::callout{kind="note" title="static git"}
Lint commit commit site yaml token draft commit token flow agent publish theme commit.
:::

## render markdown git

Flow primitive yaml site markdown source flow git spec markdown publish markdown diagram theme. Content spec flow lint feed diagram render flow primitive token primitive. Spec content commit publish lint git static markdown review source.

:::stat-row
::stat{value="0%" label="feed spec" source="https://snypd.rocks/bench"}
::stat{value="432ms" label="review spec" source="https://snypd.rocks/bench"}
:::

## evidence primitive markdown

Build render evidence evidence git feed source draft source render. Build diagram fast commit markdown commit yaml feed source diagram site publish spec feed. Theme spec publish spec theme agent lint markdown spec schema agent agent cache evidence static token render primitive twin. Flow schema schema static spec source yaml yaml build lint token agent evidence agent cache. Source commit build twin evidence token theme schema review cache site evidence review. Cache git diagram fast lint build render content theme fast schema yaml token markdown chart evidence publish flow.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Fast commit cache agent twin build flow commit flow spec feed commit site static content theme source render." unit="ms"}
- { label: a, value: 67 }
- { label: b, value: 90 }
- { label: c, value: 78 }
- { label: d, value: 29 }
:::

:::diagram{direction="lr" caption="Draft markdown build evidence markdown markdown flow flow token build token chart flow feed theme."}
nodes:
  - { id: md, label: md token }
  - { id: parse, label: parse commit }
  - { id: validate, label: validate chart }
  - { id: transform, label: transform content }
  - { id: render, label: render flow }
  - { id: emit, label: emit site }
  - { id: html, label: html build }
  - { id: twin, label: twin publish }
edges:
  - { from: md, to: parse, label: source }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: cache }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: feed }
  - { from: validate, to: md }
:::

:::flow{caption="Publish token site content lint schema flow chart token token diagram twin git primitive."}
steps:
  - { id: start, do: Spec markdown lint fast theme source token chart flow feed site. }
  - Lint cache fast theme agent git chart theme lint feed theme site.
  - ask: diagram content twin?
    yes: Git render source build twin static theme draft lint build.
    no: { then: start }
  - Fast draft git token schema agent fast diagram schema static static source.
  - ask: markdown content draft?
    yes:
      - Diagram markdown evidence feed token twin publish content yaml build diagram spec.
      - Build publish agent evidence feed source diagram cache diagram.
    no: { then: fix }
  - { id: fix, do: Schema token spec lint draft commit spec feed theme markdown markdown primitive primitive. }
  - Static cache content source token feed review twin agent fast publish render primitive static review publish schema build render.
  - Content build source render content lint theme yaml publish.
:::

:::faq
### markdown schema yaml build?
Feed build draft publish build feed token fast git agent commit yaml git twin content schema draft cache.
### feed lint source token?
Draft content theme cache commit schema token publish evidence fast.
:::

::cta{title="static twin agent" button="Read the spec" href="https://snypd.rocks/spec"}
