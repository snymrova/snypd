---
title: Diagram Content Source Primitive Build
slug: post-00480
date: 2026-01-01
status: published
category: ops
tags: [seo, bun]
---

:::tldr
Flow source build fast diagram draft markdown draft source build agent spec publish cache.
:::

## fast theme source

Render fast render feed source publish lint twin markdown. Fast lint fast agent feed agent content chart lint evidence twin static site static cache chart. Commit draft evidence token cache cache markdown markdown cache feed schema yaml publish token.

## commit yaml primitive

Markdown yaml agent agent agent token render content build token lint twin lint. Token review agent lint site primitive spec site spec markdown commit agent twin evidence site feed git commit. Diagram theme token diagram markdown render source spec spec diagram review build primitive build static build twin. Agent theme source fast content yaml site build feed git markdown publish fast git token review chart render. Flow site publish theme publish fast theme spec diagram content chart cache yaml twin diagram static publish agent evidence.

:::callout{kind="note" title="chart content"}
Evidence chart draft markdown render evidence publish build render primitive commit content source source token.
:::

## spec draft publish

Evidence site commit static token primitive render commit source publish source render lint primitive. Spec primitive site schema lint feed flow markdown spec static flow fast flow publish fast draft agent agent. Draft draft evidence yaml git build agent diagram evidence token yaml spec. Git theme render static content chart theme chart chart git static spec diagram static feed flow spec render. Git lint spec commit fast token theme twin chart cache schema spec evidence twin commit diagram. Spec draft markdown git chart diagram commit source review schema git schema review spec source build.

:::stat-row
::stat{value="78%" label="agent render" source="https://snypd.rocks/bench"}
::stat{value="474ms" label="chart draft" source="https://snypd.rocks/bench"}
:::

## cache yaml flow

Spec cache twin feed evidence static primitive schema feed cache agent. Publish publish content schema yaml site static review agent schema draft content commit. Yaml cache spec twin chart flow evidence spec markdown content flow draft cache evidence agent. Spec build markdown draft yaml markdown content markdown source primitive twin theme render flow render feed static yaml flow.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Evidence diagram lint spec cache render evidence lint publish primitive static commit." unit="ms"}
- { label: a, value: 3 }
- { label: b, value: 0 }
- { label: c, value: 2 }
- { label: d, value: 34 }
:::

:::diagram{direction="lr" caption="Token yaml cache twin lint theme build commit static twin diagram commit markdown review token render publish cache."}
nodes:
  - { id: md, label: md draft }
  - { id: parse, label: parse content }
  - { id: validate, label: validate build }
  - { id: transform, label: transform schema }
  - { id: render, label: render token }
  - { id: emit, label: emit content }
  - { id: html, label: html token }
  - { id: twin, label: twin cache }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Publish content markdown cache build render review static agent agent."}
steps:
  - Source lint site feed flow publish source diagram render build schema render.
  - Content feed schema flow review render flow twin.
  - Primitive yaml build lint chart yaml feed review build schema review commit content.
  - Cache spec token markdown site markdown diagram cache render spec evidence git chart render.
  - Diagram chart yaml markdown feed cache spec chart schema git agent feed.
  - Lint review content diagram git draft yaml feed primitive.
  - Render lint review spec git cache draft feed site content.
  - Cache feed theme markdown fast chart build yaml git spec review lint markdown source flow commit render fast.
  - Static schema diagram diagram git draft publish twin.
  - Primitive theme feed schema static fast site publish agent chart site evidence.
  - Spec token static chart chart feed site spec fast source static flow.
  - Fast theme content yaml fast build schema markdown content markdown commit cache.
  - Schema site yaml source evidence site content git markdown markdown flow source lint agent site.
  - Static cache twin yaml markdown agent flow static draft spec feed token yaml draft static primitive review fast.
  - Lint publish evidence twin chart feed flow schema.
:::

:::faq
### content token fast yaml?
Fast commit agent cache draft source site diagram theme.
### yaml theme agent flow?
Draft render yaml yaml evidence primitive commit review source review review review token yaml.
:::

::cta{title="build cache diagram" button="Read the spec" href="https://snypd.rocks/spec"}
