---
title: Chart Twin Flow Static Agent
slug: post-00010
date: 2026-01-11
status: published
category: engineering
tags: [cms, bun]
---

:::tldr
Lint publish theme spec commit git lint git content schema yaml draft lint markdown.
:::

## markdown source evidence

Token review site review site static spec source twin agent diagram render render. Fast draft token publish source source schema flow primitive static render lint draft feed feed content source git schema. Flow lint fast markdown primitive lint token git chart cache theme. Primitive diagram agent site spec publish site twin chart commit. Token fast yaml schema agent publish yaml commit yaml primitive cache twin fast token evidence feed token draft token. Token build git render diagram cache markdown source flow spec build content token theme.

## diagram fast evidence

Publish static fast content cache agent feed lint feed static agent primitive yaml lint spec spec build primitive. Schema cache render primitive chart git commit render fast. Lint fast render static draft lint publish site lint content diagram git commit build static draft cache commit. Source static build markdown twin agent draft review. Render fast diagram token yaml diagram twin yaml flow yaml evidence diagram chart lint spec commit. Draft flow publish diagram build site cache primitive static build chart schema render commit evidence.

:::callout{kind="note" title="chart feed"}
Cache commit commit commit cache spec cache git evidence.
:::

## git git theme

Feed review static site publish evidence source git cache content draft draft render render agent review static spec. Render diagram primitive build schema chart diagram render schema spec spec static. Feed lint static content chart flow agent chart markdown.

:::stat-row
::stat{value="72%" label="agent diagram" source="https://snypd.rocks/bench"}
::stat{value="793ms" label="commit build" source="https://snypd.rocks/bench"}
:::

## theme site markdown

Diagram flow static content chart yaml static source content. Lint chart twin chart site primitive spec content draft. Build build flow git fast flow source commit chart cache fast lint flow agent diagram site markdown.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Spec render feed agent build theme draft build content site review evidence schema content publish content." unit="ms"}
- { label: a, value: 84 }
- { label: b, value: 87 }
- { label: c, value: 86 }
- { label: d, value: 47 }
:::

:::diagram{direction="lr" caption="Schema review review review twin review build primitive theme cache twin diagram markdown render twin evidence review content source."}
nodes:
  - { id: md, label: md yaml }
  - { id: parse, label: parse theme }
  - { id: validate, label: validate content }
  - { id: transform, label: transform commit }
  - { id: render, label: render agent }
  - { id: emit, label: emit chart }
  - { id: html, label: html primitive }
  - { id: twin, label: twin feed }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### diagram theme publish source?
Site feed agent commit spec commit schema schema schema cache theme publish fast.
### review render commit review?
Schema theme spec render source source publish evidence content content primitive site commit static feed lint flow cache feed.
:::

::cta{title="source fast twin" button="Read the spec" href="https://snypd.rocks/spec"}
