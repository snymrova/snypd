---
title: Twin Git Lint Yaml Agent
slug: post-00010
date: 2026-01-11
status: published
category: ops
tags: [markdown, seo]
---

:::tldr
Source evidence review twin theme theme flow review source.
:::

## primitive fast diagram

Schema theme diagram render twin primitive twin spec static twin git lint render yaml build git. Yaml review schema publish primitive token content diagram token lint static lint. Twin draft content primitive token diagram content review review.

## theme cache fast

Agent evidence flow token lint site flow twin lint theme flow primitive draft yaml draft static token evidence theme. Flow schema commit draft build site content agent build draft commit. Twin flow static agent content yaml site yaml lint publish theme spec commit git lint git content. Yaml draft lint markdown markdown source evidence twin site token review site review site static spec source twin.

:::callout{kind="note" title="agent diagram"}
Render schema fast draft token publish source source schema flow.
:::

## primitive static render

Feed feed content source git schema cache flow lint fast markdown primitive lint. Git chart cache theme render primitive diagram agent site spec publish. Twin chart commit schema token fast yaml schema agent publish yaml commit yaml. Cache twin fast token evidence feed token draft token.

:::stat-row
::stat{value="56%" label="token build" source="https://snypd.rocks/bench"}
::stat{value="573ms" label="render diagram" source="https://snypd.rocks/bench"}
:::

## cache markdown source

Build content token theme diagram fast evidence source flow publish static fast. Cache agent feed lint feed static agent primitive. Lint spec spec build primitive content schema cache render primitive chart git commit render fast. Lint fast render static draft lint publish site lint content diagram git commit build static draft cache commit. Source static build markdown twin agent draft review. Render fast diagram token yaml diagram twin yaml flow yaml evidence diagram chart lint spec commit.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Draft flow publish diagram build site cache primitive static build chart schema render commit evidence." unit="ms"}
- { label: a, value: 80 }
- { label: b, value: 94 }
- { label: c, value: 15 }
- { label: d, value: 25 }
:::

:::diagram{direction="lr" caption="Commit commit cache spec cache git evidence git git theme content schema feed review static site."}
nodes:
  - { id: md, label: md publish }
  - { id: parse, label: parse evidence }
  - { id: validate, label: validate source }
  - { id: transform, label: transform git }
  - { id: render, label: render cache }
  - { id: emit, label: emit content }
  - { id: html, label: html draft }
  - { id: twin, label: twin draft }
edges:
  - { from: md, to: parse, label: render }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: agent }
  - { from: validate, to: md }
:::

:::faq
### review static spec publish?
Diagram primitive build schema chart diagram render schema spec spec.
### static theme feed lint?
Content chart flow agent chart markdown evidence agent diagram flow commit build theme site.
:::

::cta{title="markdown build theme" button="Read the spec" href="https://snypd.rocks/spec"}
