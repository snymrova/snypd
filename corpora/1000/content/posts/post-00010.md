---
title: Token Diagram Content Review Review
slug: post-00010
date: 2026-01-11
status: published
category: engineering
tags: [mcp, cms]
---

:::tldr
Twin agent evidence flow token lint site flow twin lint theme flow.
:::

## primitive draft yaml

Token evidence theme cache flow schema commit draft build site content agent build draft. Chart twin flow static agent content yaml site yaml lint publish theme spec commit git lint. Content schema yaml draft lint markdown markdown source evidence twin site token review site review. Static spec source twin agent diagram render render schema fast draft token publish.

## source source schema

Static render lint draft feed feed content source git schema. Flow lint fast markdown primitive lint token git chart cache theme. Primitive diagram agent site spec publish site twin chart commit. Token fast yaml schema agent publish yaml commit yaml primitive cache twin fast token evidence feed token draft token. Token build git render diagram cache markdown source flow spec build content token theme. Fast evidence source flow publish static fast content cache agent feed lint feed static agent primitive yaml lint.

:::callout{kind="note" title="spec spec"}
Primitive content schema cache render primitive chart git commit render.
:::

## fast schema lint

Static draft lint publish site lint content diagram git commit. Static draft cache commit markdown source static build markdown twin. Draft review evidence render fast diagram token yaml. Twin yaml flow yaml evidence diagram chart lint spec commit git draft flow publish diagram build site cache. Static build chart schema render commit evidence chart feed.

:::stat-row
::stat{value="15%" label="cache commit" source="https://snypd.rocks/bench"}
::stat{value="585ms" label="commit cache" source="https://snypd.rocks/bench"}
:::

## spec cache git

Git theme content schema feed review static site publish evidence source git cache content draft. Render render agent review static spec publish render diagram primitive build schema chart. Render schema spec spec static theme feed lint static content chart flow agent chart markdown evidence agent. Flow commit build theme site markdown build theme diagram flow static content chart yaml static source content content. Chart twin chart site primitive spec content draft diagram build build flow.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Fast flow source commit chart cache fast lint flow agent diagram site markdown evidence spec." unit="ms"}
- { label: a, value: 18 }
- { label: b, value: 96 }
- { label: c, value: 3 }
- { label: d, value: 24 }
:::

:::diagram{direction="lr" caption="Draft build content site review evidence schema content publish."}
nodes:
  - { id: md, label: md content }
  - { id: parse, label: parse diagram }
  - { id: validate, label: validate flow }
  - { id: transform, label: transform flow }
  - { id: render, label: render site }
  - { id: emit, label: emit schema }
  - { id: html, label: html schema }
  - { id: twin, label: twin review }
edges:
  - { from: md, to: parse, label: review }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: review }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: twin }
  - { from: validate, to: md }
:::

:::faq
### review build primitive theme?
Twin diagram markdown render twin evidence review content source yaml theme.
### content commit agent chart?
Feed diagram theme publish source site site feed agent.
:::

::cta{title="commit spec commit" button="Read the spec" href="https://snypd.rocks/spec"}
