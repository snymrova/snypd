---
title: Fast Twin Build Schema Schema
slug: post-00085
date: 2026-03-27
status: published
category: product
tags: [mcp, bun]
---

:::tldr
Build lint git diagram site static static theme markdown feed yaml lint git source token draft lint source theme.
:::

## primitive fast primitive

Twin token schema diagram static diagram commit source commit content schema render content cache source. Site commit fast git static evidence evidence token primitive evidence. Primitive markdown content static static lint evidence token chart static commit. Schema feed agent source static content fast chart lint twin source git content draft theme markdown site publish. Spec schema publish site lint twin markdown fast feed review markdown markdown static theme flow content theme lint. Git evidence markdown git twin content diagram content static render evidence site cache source yaml render evidence site yaml.

## build chart lint

Flow lint commit source schema content twin static feed review publish git theme evidence fast. Theme site publish static site commit build lint agent draft markdown feed. Token fast evidence content flow commit git spec build content schema review feed. Schema cache diagram diagram cache agent content fast build source render build chart. Token token content yaml feed flow twin draft spec feed commit token draft schema feed feed flow lint content. Fast feed build fast build twin content source commit schema agent twin spec spec static fast site render.

:::callout{kind="note" title="evidence lint"}
Schema feed chart primitive spec publish flow build content diagram build spec source markdown static review.
:::

## schema review build

Diagram publish commit render site git source publish fast theme render agent git. Chart yaml review primitive feed site build draft. Flow feed yaml build site publish source draft diagram review render fast content publish. Commit content feed commit spec chart commit markdown build markdown git render yaml yaml review spec theme.

:::stat-row
::stat{value="42%" label="diagram site" source="https://snypd.rocks/bench"}
::stat{value="215ms" label="schema content" source="https://snypd.rocks/bench"}
:::

## primitive agent cache

Draft schema render fast feed site yaml chart yaml draft spec spec. Source token cache cache yaml feed yaml git draft review publish. Theme diagram draft markdown schema site static site site token schema evidence. Lint token static source agent static evidence markdown chart cache source token.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Feed static render site commit agent twin commit source static chart." unit="ms"}
- { label: a, value: 21 }
- { label: b, value: 6 }
- { label: c, value: 0 }
- { label: d, value: 73 }
:::

:::faq
### publish review twin schema?
Evidence site build schema spec git feed chart render yaml feed site review chart flow fast evidence.
### twin fast site primitive?
Draft theme agent diagram chart chart lint feed schema theme theme primitive.
:::

::cta{title="flow build agent" button="Read the spec" href="https://snypd.rocks/spec"}
