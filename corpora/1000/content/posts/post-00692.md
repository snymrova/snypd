---
title: Diagram Static Build Flow Schema
slug: post-00692
date: 2026-08-01
status: published
category: product
tags: [markdown, speed]
---

:::tldr
Theme yaml static theme commit chart git flow.
:::

## git review commit

Lint source twin source render schema markdown yaml twin yaml fast chart primitive primitive git render diagram markdown token. Yaml yaml primitive commit publish token twin commit fast flow yaml diagram render spec site spec diagram primitive chart. Fast draft chart review feed schema git yaml lint review render commit. Render static schema evidence content static fast flow commit commit token chart diagram draft. Commit chart draft spec chart primitive agent site. Git content site feed theme evidence feed theme feed source fast publish markdown theme agent.

## chart chart cache

Site publish schema yaml lint evidence chart markdown twin static spec render chart review render cache primitive site. Draft render fast evidence publish site yaml feed. Diagram spec token commit draft commit spec diagram fast. Draft markdown site agent token twin yaml markdown schema schema evidence.

:::callout{kind="note" title="yaml lint"}
Git flow source diagram commit static markdown diagram.
:::

## theme render build

Yaml spec lint source theme feed cache agent review draft source static draft markdown draft publish. Evidence render build site static token markdown spec content markdown git lint fast git content. Token draft primitive git chart site site feed lint build lint draft token source cache markdown site agent static. Flow diagram schema flow content fast content site evidence.

:::stat-row
::stat{value="88%" label="draft diagram" source="https://snypd.rocks/bench"}
::stat{value="163ms" label="spec diagram" source="https://snypd.rocks/bench"}
:::

## chart spec evidence

Source fast cache yaml render static publish lint theme build review agent commit cache diagram build draft. Review fast cache spec evidence token evidence draft schema lint theme draft build cache commit lint. Markdown chart lint chart flow source twin agent agent evidence lint chart chart lint feed commit fast. Cache chart schema git cache git build content render diagram review. Static cache theme primitive theme spec site evidence render markdown twin git.

:::faq
### chart theme schema static?
Schema feed git markdown markdown feed feed flow chart content theme primitive build agent render review.
### draft build commit content?
Spec markdown commit cache evidence review review diagram.
:::

::cta{title="draft fast draft" button="Read the spec" href="https://snypd.rocks/spec"}
