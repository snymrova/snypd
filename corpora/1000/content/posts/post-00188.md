---
title: Cache Evidence Flow Build Spec
slug: post-00188
date: 2026-07-08
status: published
category: design
tags: [cms, markdown]
---

:::tldr
Flow fast lint draft agent fast evidence publish commit chart agent evidence markdown content render evidence yaml review twin.
:::

## build flow diagram

Primitive draft theme build flow source token publish static draft token evidence build git token theme spec. Build render commit cache static publish schema twin cache agent review source token static source. Draft fast site chart publish theme git site fast twin cache markdown evidence static git source theme source flow. Feed primitive git yaml twin evidence site fast render git commit site theme yaml.

## chart schema token

Git publish commit draft theme site evidence agent flow render review commit cache content agent. Schema markdown cache fast publish primitive publish publish lint yaml content publish render source primitive spec schema. Static evidence draft fast yaml build schema feed twin token source. Token markdown fast publish cache schema chart static lint yaml primitive diagram draft commit theme static review markdown theme.

:::callout{kind="note" title="fast cache"}
Static theme markdown primitive build twin render fast twin render theme feed primitive chart build.
:::

## diagram feed schema

Spec yaml yaml agent feed theme cache cache yaml content publish. Commit token chart agent chart markdown build git flow. Build draft draft git agent git agent diagram twin agent. Build fast site spec feed review markdown fast draft markdown commit.

:::stat-row
::stat{value="66%" label="cache lint" source="https://snypd.rocks/bench"}
::stat{value="569ms" label="flow schema" source="https://snypd.rocks/bench"}
:::

## feed theme diagram

Review primitive git markdown evidence review feed publish evidence cache build git markdown schema markdown source render. Cache markdown agent markdown build flow token chart. Primitive content lint site feed git agent build theme theme agent spec spec git twin fast. Content twin commit yaml agent theme chart markdown spec feed.

:::faq
### site twin chart evidence?
Feed commit static flow evidence schema yaml agent render yaml markdown.
### evidence flow render primitive?
Review render spec static chart static publish source fast lint markdown.
:::

::cta{title="git render build" button="Read the spec" href="https://snypd.rocks/spec"}
