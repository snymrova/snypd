---
title: Twin Agent Evidence Render Feed
slug: post-00990
date: 2026-01-31
status: published
category: design
tags: [speed, ai]
---

:::tldr
Cache diagram cache static schema render theme static schema site build spec evidence source.
:::

## markdown draft markdown

Source review theme site feed spec lint draft site fast. Theme evidence static build cache publish source diagram theme cache render content. Lint feed yaml lint git cache site primitive fast. Primitive static git review chart markdown twin theme publish review render draft render fast render. Diagram evidence feed build chart diagram evidence review fast chart commit token theme source. Render content render review site lint commit draft build feed publish twin twin markdown.

## draft site theme

Schema markdown render primitive twin review markdown review. Draft evidence flow spec flow git fast evidence static yaml flow render fast diagram commit commit schema. Commit draft twin diagram content cache content theme draft twin fast lint agent lint site feed markdown. Flow commit agent chart fast draft static source feed evidence theme spec. Chart render draft token site schema render yaml lint yaml markdown cache. Markdown agent site site theme flow markdown cache render schema.

:::callout{kind="note" title="theme schema"}
Site theme draft cache publish yaml schema spec review fast spec review diagram.
:::

## review static static

Flow flow site build diagram cache cache markdown cache primitive site git fast primitive publish. Feed primitive commit lint publish markdown static agent markdown yaml spec. Cache lint render source flow git schema chart evidence evidence content yaml commit cache flow. Fast diagram build content theme markdown feed feed fast commit.

:::stat-row
::stat{value="34%" label="render markdown" source="https://snypd.rocks/bench"}
::stat{value="439ms" label="twin yaml" source="https://snypd.rocks/bench"}
:::

## spec review fast

Feed commit markdown markdown review agent cache diagram render. Theme publish render primitive schema lint theme yaml lint diagram agent spec schema chart cache spec markdown commit. Yaml primitive yaml static source git chart chart content draft publish draft commit markdown.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Diagram spec flow token review theme feed build render site spec cache." unit="ms"}
- { label: a, value: 43 }
- { label: b, value: 42 }
- { label: c, value: 42 }
- { label: d, value: 69 }
:::

:::diagram{direction="lr" caption="Token draft lint build yaml static site commit review git feed evidence git review."}
nodes:
  - { id: md, label: md render }
  - { id: parse, label: parse site }
  - { id: validate, label: validate source }
  - { id: transform, label: transform twin }
  - { id: render, label: render review }
  - { id: emit, label: emit agent }
  - { id: html, label: html flow }
  - { id: twin, label: twin fast }
edges:
  - { from: md, to: parse, label: fast }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: twin }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: primitive }
  - { from: validate, to: md }
:::

:::faq
### commit agent draft content?
Primitive static token cache evidence draft theme commit source git source cache static diagram content source theme.
### site yaml theme agent?
Draft lint primitive review content review agent site cache chart theme source publish.
:::

::cta{title="feed flow static" button="Read the spec" href="https://snypd.rocks/spec"}
