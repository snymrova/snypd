---
title: Render Agent Review Flow Chart
slug: post-00990
date: 2026-01-31
status: published
category: engineering
tags: [cms, seo]
---

:::tldr
Agent evidence spec agent lint primitive chart theme lint twin chart content fast token render review draft.
:::

## site agent diagram

Flow render review agent markdown primitive commit site theme chart static primitive theme agent chart build publish. Schema lint git diagram lint publish fast chart site publish agent build static schema flow feed markdown. Evidence build theme spec lint flow content feed token site site static yaml twin token publish agent. Content render git schema publish build site schema commit flow token. Render commit build content lint diagram content evidence git feed markdown build spec feed lint. Lint evidence cache flow source theme site content source.

## commit content theme

Agent feed static site feed markdown agent diagram flow cache. Fast flow static yaml feed spec schema token site markdown static. Draft git agent feed git twin lint cache render yaml spec publish feed publish. Token schema agent feed fast source static static fast diagram markdown yaml evidence. Twin fast twin draft commit yaml twin lint cache content theme primitive lint review cache.

:::callout{kind="note" title="evidence render"}
Yaml yaml site schema lint spec source git content twin markdown.
:::

## review static build

Markdown agent theme token primitive git schema spec fast flow commit theme theme chart. Evidence schema site commit feed static render cache schema content evidence. Review chart git content diagram yaml lint source git cache git evidence commit commit render. Chart static yaml cache build primitive flow git twin git static render. Render source publish flow site twin twin lint site agent fast yaml feed primitive yaml. Evidence primitive git git git agent cache lint source draft theme review render diagram.

:::stat-row
::stat{value="49%" label="build content" source="https://snypd.rocks/bench"}
::stat{value="86ms" label="commit static" source="https://snypd.rocks/bench"}
:::

## token yaml yaml

Evidence evidence theme source content lint chart cache site fast feed git diagram spec agent build. Agent lint git git schema token spec lint feed. Source static diagram token diagram agent source markdown source draft theme feed evidence static.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Draft lint publish content flow flow flow yaml site markdown markdown." unit="ms"}
- { label: a, value: 51 }
- { label: b, value: 99 }
- { label: c, value: 28 }
- { label: d, value: 26 }
:::

:::diagram{direction="lr" caption="Feed git commit draft site content commit publish twin site markdown cache chart flow twin twin render spec schema."}
nodes:
  - { id: md, label: md render }
  - { id: parse, label: parse build }
  - { id: validate, label: validate commit }
  - { id: transform, label: transform markdown }
  - { id: render, label: render render }
  - { id: emit, label: emit cache }
  - { id: html, label: html git }
  - { id: twin, label: twin chart }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### review feed site cache?
Review markdown spec agent feed twin content schema content markdown agent token site.
### publish schema render fast?
Chart content twin feed flow draft review fast fast markdown token lint agent content lint.
:::

::cta{title="cache content review" button="Read the spec" href="https://snypd.rocks/spec"}
