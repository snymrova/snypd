---
title: Flow Lint Flow Twin Chart
slug: post-00990
date: 2026-01-31
status: published
category: design
tags: [bun, markdown]
---

:::tldr
Build evidence theme static build spec build diagram build static fast site diagram static.
:::

## git site commit

Site primitive schema content content fast git flow git build render. Yaml content spec lint theme markdown evidence build site build git. Theme lint lint primitive review twin commit twin lint chart lint static.

## git commit evidence

Spec content git schema content yaml render yaml schema twin build theme feed lint render build token review. Git flow content render flow primitive flow twin content fast cache git cache. Agent agent evidence fast static agent flow flow publish git feed. Source feed feed flow twin chart source lint fast content token static review primitive review markdown lint token yaml. Feed publish static flow commit source publish lint flow static review diagram diagram.

:::callout{kind="note" title="twin twin"}
Build flow feed feed publish content chart review source chart git diagram cache source lint source fast git.
:::

## yaml build evidence

Twin feed flow lint fast draft token agent commit agent commit fast yaml evidence render static. Build primitive feed static static primitive git commit yaml chart. Diagram git schema primitive primitive primitive flow fast twin site twin schema. Review token evidence diagram feed review primitive content fast evidence token.

:::stat-row
::stat{value="55%" label="site publish" source="https://snypd.rocks/bench"}
::stat{value="179ms" label="feed review" source="https://snypd.rocks/bench"}
:::

## flow token primitive

Token flow chart evidence twin publish yaml review build primitive commit. Draft theme evidence token review token schema yaml flow. Evidence feed twin content commit markdown flow content yaml render publish. Yaml cache spec site site site spec cache chart lint commit primitive schema draft twin yaml theme.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Markdown theme primitive fast schema markdown git schema." unit="ms"}
- { label: a, value: 43 }
- { label: b, value: 55 }
- { label: c, value: 95 }
- { label: d, value: 75 }
:::

:::diagram{direction="lr" caption="Markdown lint fast site evidence static draft content primitive markdown diagram schema evidence primitive."}
nodes:
  - { id: md, label: md token }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate flow }
  - { id: transform, label: transform theme }
  - { id: render, label: render flow }
  - { id: emit, label: emit draft }
  - { id: html, label: html schema }
  - { id: twin, label: twin primitive }
edges:
  - { from: md, to: parse, label: cache }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: spec }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: yaml }
  - { from: validate, to: md }
:::

:::faq
### publish content content commit?
Evidence token content commit commit chart markdown build static diagram theme theme yaml site.
### primitive schema source theme?
Static markdown twin twin lint flow draft git site site yaml agent twin twin.
:::

::cta{title="render fast diagram" button="Read the spec" href="https://snypd.rocks/spec"}
