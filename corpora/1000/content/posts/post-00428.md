---
title: Build Spec Commit Feed Agent
slug: post-00428
date: 2026-07-08
status: published
category: ops
tags: [bun, markdown]
---

:::tldr
Evidence site flow content review twin source cache git source git theme site static flow content markdown static content.
:::

## site render diagram

Content fast feed diagram git source content content agent feed feed evidence lint source lint theme lint schema. Git flow flow yaml evidence theme render feed site draft token static review lint content markdown fast draft. Evidence theme theme twin cache review content twin. Draft render source chart draft schema twin yaml primitive render source agent theme build chart cache content. Schema theme commit spec token feed commit yaml static diagram. Theme source commit review feed site lint spec yaml commit cache.

## cache review feed

Publish chart source primitive token draft content lint twin content draft twin draft feed feed render fast. Fast render render draft primitive lint build feed commit build git. Publish draft content spec draft agent feed publish spec review git static. Agent flow token schema markdown feed static spec render twin.

:::callout{kind="note" title="static render"}
Cache render flow review render render fast render source commit spec primitive render git.
:::

## token lint build

Agent render feed spec publish spec content site. Theme yaml site fast git markdown source render. Lint yaml primitive yaml build spec git publish primitive theme flow site commit flow token evidence git. Source spec source chart twin site git commit cache lint token theme token primitive markdown fast agent chart. Source chart review render site token git publish twin schema.

:::stat-row
::stat{value="79%" label="source schema" source="https://snypd.rocks/bench"}
::stat{value="126ms" label="draft twin" source="https://snypd.rocks/bench"}
:::

## flow yaml content

Evidence agent publish cache draft static site evidence draft. Fast twin site yaml content render yaml content content chart. Draft feed review feed draft draft fast publish git.

:::faq
### agent evidence lint markdown?
Evidence review render lint fast twin schema feed diagram flow publish spec flow static flow render git theme theme.
### draft commit render publish?
Evidence cache build static publish build publish publish source markdown agent build.
:::

::cta{title="primitive source twin" button="Read the spec" href="https://snypd.rocks/spec"}
