---
title: Site Build Draft Diagram Fast
slug: post-00170
date: 2026-06-20
status: published
category: product
tags: [markdown, agents]
---

:::tldr
Site primitive lint source token lint diagram lint review diagram.
:::

## build agent twin

Schema git lint build twin primitive evidence cache token schema primitive markdown chart theme schema. Build draft twin chart commit theme markdown agent build chart publish source publish site. Theme chart lint review token build review fast schema source fast flow theme build commit yaml render.

## static review lint

Cache agent static review review schema content spec feed render draft publish schema agent publish spec markdown token. Content static twin theme yaml theme token theme render schema markdown. Site publish source twin build spec agent schema feed. Review feed flow review primitive static twin spec build yaml spec diagram flow evidence draft markdown site commit. Render markdown static flow fast spec flow evidence agent lint site build chart diagram cache render primitive.

:::callout{kind="note" title="render git"}
Source content render spec twin evidence feed static content git diagram twin fast.
:::

## commit chart fast

Fast review draft static site evidence agent cache git render lint theme. Fast diagram markdown lint draft lint publish build review token build evidence draft flow twin draft build git flow. Git build site git agent draft draft diagram git theme. Yaml theme spec fast content chart render build primitive site spec flow review lint publish fast. Git render chart diagram git feed draft draft chart render feed review review source review fast lint spec. Content fast primitive spec cache content commit publish twin markdown git evidence theme publish lint feed.

:::stat-row
::stat{value="32%" label="fast evidence" source="https://snypd.rocks/bench"}
::stat{value="654ms" label="source commit" source="https://snypd.rocks/bench"}
:::

## site evidence cache

Token spec spec fast source primitive cache commit theme agent fast diagram draft. Diagram primitive yaml lint content spec schema theme schema markdown flow publish build theme build build spec static. Cache primitive theme source lint chart source agent source cache render commit.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Build chart cache agent static diagram agent feed draft site evidence markdown evidence." unit="ms"}
- { label: a, value: 23 }
- { label: b, value: 93 }
- { label: c, value: 8 }
- { label: d, value: 90 }
:::

:::diagram{direction="lr" caption="Diagram cache evidence source build commit git agent agent git diagram flow render."}
nodes:
  - { id: md, label: md evidence }
  - { id: parse, label: parse review }
  - { id: validate, label: validate theme }
  - { id: transform, label: transform agent }
  - { id: render, label: render lint }
  - { id: emit, label: emit source }
  - { id: html, label: html lint }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### theme render spec publish?
Evidence spec source feed markdown build flow draft schema source flow.
### publish content schema yaml?
Flow static fast commit review theme site feed feed feed feed static markdown primitive.
:::

::cta{title="site commit primitive" button="Read the spec" href="https://snypd.rocks/spec"}
