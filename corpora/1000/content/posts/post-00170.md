---
title: Schema Primitive Chart Diagram Token
slug: post-00170
date: 2026-06-20
status: published
category: product
tags: [speed, cms]
---

:::tldr
Schema evidence spec markdown theme git static token lint primitive schema feed primitive.
:::

## render content fast

Site primitive render commit agent lint fast agent commit chart diagram review publish yaml commit twin twin render. Cache markdown git render publish fast source flow token review build markdown draft git markdown. Markdown fast commit spec cache site static spec lint draft spec token cache cache content. Render content commit feed theme theme render evidence commit commit. Chart render agent agent twin review yaml source yaml review.

## build theme agent

Token flow publish fast commit theme chart build lint source publish chart render git fast agent schema git. Diagram yaml review feed cache primitive source build lint site git primitive evidence publish source source token. Content markdown git theme site draft draft primitive site theme lint flow fast publish commit content build. Fast evidence feed evidence build git review spec draft source commit cache git diagram draft theme build. Commit build git diagram publish primitive token source agent twin source lint. Agent draft feed primitive agent theme fast yaml commit theme primitive.

:::callout{kind="note" title="site feed"}
Agent feed source git source site site diagram chart render.
:::

## content evidence publish

Source lint twin twin commit chart schema schema site publish draft token chart fast fast commit markdown publish source. Diagram fast commit build yaml theme feed schema lint static agent spec agent token markdown markdown. Site build draft diagram fast publish feed build build site primitive. Source token lint diagram lint review diagram build agent twin agent git. Git lint build twin primitive evidence cache token schema primitive markdown chart theme schema yaml build draft twin chart.

:::stat-row
::stat{value="66%" label="theme markdown" source="https://snypd.rocks/bench"}
::stat{value="22ms" label="build chart" source="https://snypd.rocks/bench"}
:::

## publish source publish

Theme chart lint review token build review fast schema source fast flow theme build commit yaml render. Review lint git flow cache agent static review review schema content spec feed render. Publish schema agent publish spec markdown token token content static twin theme yaml. Token theme render schema markdown content site publish source.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Build spec agent schema feed schema review feed flow review primitive static twin spec build yaml spec diagram flow." unit="ms"}
- { label: a, value: 72 }
- { label: b, value: 43 }
- { label: c, value: 4 }
- { label: d, value: 47 }
:::

:::diagram{direction="lr" caption="Chart render markdown static flow fast spec flow evidence agent lint site build chart diagram."}
nodes:
  - { id: md, label: md cache }
  - { id: parse, label: parse render }
  - { id: validate, label: validate primitive }
  - { id: transform, label: transform render }
  - { id: render, label: render git }
  - { id: emit, label: emit site }
  - { id: html, label: html source }
  - { id: twin, label: twin content }
edges:
  - { from: md, to: parse, label: render }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: spec }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: twin }
  - { from: validate, to: md }
:::

:::faq
### evidence feed static content?
Diagram twin fast commit chart fast feed publish fast review draft static site evidence agent.
### cache git render lint?
Feed fast diagram markdown lint draft lint publish build.
:::

::cta{title="review token build" button="Read the spec" href="https://snypd.rocks/spec"}
