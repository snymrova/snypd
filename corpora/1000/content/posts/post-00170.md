---
title: Spec Fast Source Primitive Cache
slug: post-00170
date: 2026-06-20
status: published
category: design
tags: [agents, ai]
---

:::tldr
Diagram draft schema diagram primitive yaml lint content spec schema theme schema markdown flow.
:::

## publish build theme

Spec static lint cache primitive theme source lint chart source. Source cache render commit draft build chart cache. Static diagram agent feed draft site evidence markdown.

## evidence build feed

Draft diagram cache evidence source build commit git agent agent git diagram flow render evidence review theme agent. Source lint agent theme render spec publish cache evidence spec source feed. Build flow draft schema source flow publish content.

:::callout{kind="note" title="schema yaml"}
Flow static fast commit review theme site feed feed feed feed static markdown primitive.
:::

## site commit primitive

Token yaml theme token primitive static flow chart. Twin agent git commit git flow spec commit. Render lint commit chart lint static static theme flow cache. Spec lint chart content evidence twin lint publish. Git twin fast static publish theme schema render flow fast fast diagram fast. Cache token static feed primitive cache token theme git yaml render agent render schema twin build git review agent.

:::stat-row
::stat{value="8%" label="evidence fast" source="https://snypd.rocks/bench"}
::stat{value="333ms" label="build evidence" source="https://snypd.rocks/bench"}
:::

## chart source build

Markdown publish lint twin schema primitive schema markdown. Feed cache build markdown draft agent flow source primitive commit content twin token git static commit flow chart. Theme site diagram static evidence build git build site twin. Draft schema static draft git cache lint render diagram content feed static spec schema publish chart git source agent. Schema build render flow source publish evidence fast twin content site. Twin commit yaml static review static feed agent flow flow flow build markdown draft agent evidence primitive.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Fast build cache twin chart markdown review review diagram." unit="ms"}
- { label: a, value: 45 }
- { label: b, value: 73 }
- { label: c, value: 89 }
- { label: d, value: 68 }
:::

:::diagram{direction="lr" caption="Draft source chart chart draft token lint review content cache primitive agent cache."}
nodes:
  - { id: md, label: md chart }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate spec }
  - { id: transform, label: transform chart }
  - { id: render, label: render twin }
  - { id: emit, label: emit review }
  - { id: html, label: html flow }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse, label: publish }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: theme }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: site }
  - { from: validate, to: md }
:::

:::faq
### markdown diagram draft content?
Draft content git site spec flow agent render theme feed source evidence commit feed spec diagram yaml feed.
### twin spec source evidence?
Source build token evidence fast agent twin draft build.
:::

::cta{title="token build schema" button="Read the spec" href="https://snypd.rocks/spec"}
