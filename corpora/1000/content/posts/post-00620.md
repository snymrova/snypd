---
title: Content Render Yaml Publish Chart
slug: post-00620
date: 2026-05-21
status: published
category: engineering
tags: [seo, bun]
---

:::tldr
Spec publish agent yaml chart commit token token yaml primitive markdown primitive review yaml schema static publish.
:::

## review content flow

Evidence feed schema schema chart spec yaml twin feed theme. Diagram review diagram flow token publish build source source agent flow site commit draft content chart commit markdown fast. Agent agent spec feed primitive spec token theme theme cache commit flow source cache token render.

## token static evidence

Draft publish fast agent flow spec diagram flow. Content schema spec commit build primitive chart twin build review lint static feed site feed render review commit publish. Lint agent publish twin yaml markdown yaml source render commit flow static static lint. Commit diagram diagram git render build yaml content primitive build theme static site publish diagram twin. Source flow lint feed lint static commit cache spec agent spec evidence agent build static draft site. Publish source evidence spec schema draft cache theme agent evidence commit agent primitive schema.

:::callout{kind="note" title="git content"}
Render flow spec lint fast lint review review diagram agent render build.
:::

## git site primitive

Draft feed flow render twin source draft schema primitive theme build token chart agent flow flow commit publish diagram. Markdown agent feed commit token agent lint site review schema render git. Token lint markdown render theme draft theme agent source feed source publish cache commit markdown agent content. Twin commit review feed agent evidence draft lint git render fast primitive. Agent evidence primitive publish theme twin evidence markdown render chart review content theme evidence. Source markdown primitive draft render render publish review markdown twin source source evidence render review agent chart theme evidence.

:::stat-row
::stat{value="76%" label="review theme" source="https://snypd.rocks/bench"}
::stat{value="774ms" label="yaml content" source="https://snypd.rocks/bench"}
:::

## commit schema yaml

Draft build lint twin build twin build chart twin evidence theme flow. Build commit git commit static build git markdown token source diagram agent. Publish schema feed review primitive feed publish flow lint agent theme theme diagram content.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Token site primitive build lint publish source feed primitive fast flow twin token." unit="ms"}
- { label: a, value: 49 }
- { label: b, value: 61 }
- { label: c, value: 71 }
- { label: d, value: 14 }
:::

:::diagram{direction="lr" caption="Git diagram fast theme git draft cache schema review evidence publish flow markdown lint."}
nodes:
  - { id: md, label: md cache }
  - { id: parse, label: parse fast }
  - { id: validate, label: validate yaml }
  - { id: transform, label: transform review }
  - { id: render, label: render markdown }
  - { id: emit, label: emit yaml }
  - { id: html, label: html spec }
  - { id: twin, label: twin lint }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Build draft diagram publish token commit content diagram."}
steps:
  - Render commit diagram fast agent diagram agent primitive agent build draft git commit markdown render.
  - Token twin commit cache chart token evidence flow site evidence diagram draft commit spec feed diagram publish.
  - Flow content diagram flow source primitive build draft site build git fast cache twin flow commit evidence agent site.
  - Primitive flow git agent spec twin fast static.
  - Yaml static review chart evidence lint feed schema site source draft evidence cache chart publish render theme flow theme.
  - Site fast chart spec token commit markdown diagram primitive schema primitive chart.
  - Build fast chart theme token source publish spec render git git fast source primitive.
  - Static content flow primitive markdown token draft static draft lint content chart feed spec markdown.
  - Site schema cache git commit site flow schema render content feed static agent diagram yaml token token lint spec.
  - Fast content fast static spec spec cache publish agent spec theme diagram.
  - Draft content build primitive evidence review cache build agent flow.
  - Fast markdown render token diagram static agent markdown theme review flow flow lint site flow.
  - Static markdown flow commit diagram content draft chart static commit.
  - Publish cache twin commit commit build primitive review evidence diagram source git git primitive lint diagram diagram.
  - Cache build content lint schema markdown cache spec agent schema feed draft flow build yaml.
:::

:::faq
### draft evidence evidence commit?
Evidence spec git fast token static diagram site commit cache yaml commit lint publish feed yaml.
### markdown feed token schema?
Spec yaml cache build cache spec lint markdown yaml chart schema static lint commit fast primitive.
:::

::cta{title="theme spec flow" button="Read the spec" href="https://snypd.rocks/spec"}
