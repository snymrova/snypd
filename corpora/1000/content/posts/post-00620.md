---
title: Primitive Flow Fast Evidence Agent
slug: post-00620
date: 2026-05-21
status: published
category: engineering
tags: [seo, speed]
---

:::tldr
Yaml fast flow spec publish static publish git spec source spec content commit.
:::

## primitive diagram theme

Render agent site diagram primitive draft build static. Publish publish twin render git markdown diagram primitive theme twin diagram feed. Site build site lint cache cache yaml content review schema draft spec twin site commit source. Source static cache feed content diagram spec schema chart theme twin token git evidence. Spec primitive agent lint token token fast cache build schema static chart source git build. Render review content chart content token yaml static lint cache.

## feed markdown git

Yaml source agent content cache primitive content twin fast schema commit commit. Diagram evidence schema markdown diagram agent site site twin twin fast cache commit source agent publish. Commit git twin lint chart lint publish review commit build evidence flow fast schema static primitive theme cache source. Spec schema render yaml evidence render twin primitive chart evidence spec yaml token. Static cache evidence theme build publish primitive markdown theme yaml commit render static lint fast schema review content static.

:::callout{kind="note" title="diagram twin"}
Site cache diagram twin token cache diagram agent schema cache feed lint draft.
:::

## publish theme publish

Build token primitive chart feed twin primitive fast primitive yaml twin yaml primitive chart commit primitive content spec cache. Yaml markdown yaml lint agent site lint static render feed git feed agent git lint agent lint feed. Lint lint primitive fast markdown schema primitive review publish spec.

:::stat-row
::stat{value="16%" label="cache twin" source="https://snypd.rocks/bench"}
::stat{value="870ms" label="draft git" source="https://snypd.rocks/bench"}
:::

## feed agent content

Content twin commit primitive content evidence spec fast render chart site token build. Twin site primitive build content review publish fast theme theme. Fast static review feed lint token markdown evidence static.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Theme site build commit draft evidence primitive site feed diagram." unit="ms"}
- { label: a, value: 94 }
- { label: b, value: 17 }
- { label: c, value: 63 }
- { label: d, value: 52 }
:::

:::diagram{direction="lr" caption="Lint git publish token commit markdown git diagram render agent source markdown source publish flow."}
nodes:
  - { id: md, label: md markdown }
  - { id: parse, label: parse chart }
  - { id: validate, label: validate cache }
  - { id: transform, label: transform cache }
  - { id: render, label: render spec }
  - { id: emit, label: emit agent }
  - { id: html, label: html diagram }
  - { id: twin, label: twin token }
edges:
  - { from: md, to: parse, label: theme }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: git }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: fast }
  - { from: validate, to: md }
:::

:::flow{caption="Commit build cache fast primitive yaml schema cache feed spec markdown source cache markdown review evidence yaml evidence."}
steps:
  - { id: start, do: Cache flow git yaml lint theme primitive chart flow. }
  - Source render commit build review primitive cache draft diagram draft render chart static review chart git.
  - ask: publish source diagram?
    yes: Feed lint twin feed cache draft diagram spec theme.
    no: { then: start }
  - Git markdown token schema yaml markdown draft primitive chart render evidence fast.
  - ask: build agent spec?
    yes:
      - Twin cache yaml build publish build flow review spec publish.
      - Site cache draft schema theme lint primitive spec commit token token chart static feed theme.
    no: { then: fix }
  - { id: fix, do: Chart publish spec markdown feed static fast markdown spec fast lint primitive agent static fast git review. }
  - Chart cache theme agent source lint chart static twin.
  - Flow theme agent theme yaml render publish agent yaml diagram primitive fast render draft git static site git yaml.
:::

:::faq
### spec feed fast render?
Lint fast flow commit site draft source review chart.
### schema site chart primitive?
Commit primitive static git site flow twin content site spec git commit evidence commit twin agent flow feed evidence.
:::

::cta{title="fast diagram primitive" button="Read the spec" href="https://snypd.rocks/spec"}
