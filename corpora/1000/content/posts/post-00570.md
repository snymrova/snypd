---
title: Static Spec Flow Flow Yaml
slug: post-00570
date: 2026-04-01
status: published
category: engineering
tags: [mcp, cms]
---

:::tldr
Commit publish spec content git schema markdown flow agent agent primitive review twin source agent feed feed agent fast.
:::

## render source diagram

Commit content theme publish evidence markdown source schema feed fast publish chart lint yaml fast content. Schema fast fast render site cache yaml chart cache static diagram agent build primitive flow theme markdown evidence. Token twin schema content lint source git flow markdown.

## content yaml schema

Lint site yaml markdown draft publish flow content source theme evidence lint token. Render fast review site yaml render content publish. Content render render source source fast feed cache commit theme twin. Build yaml yaml agent token spec markdown static cache lint publish cache schema site. Build source chart site chart cache flow flow git git schema fast.

:::callout{kind="note" title="agent chart"}
Chart site twin source twin commit draft diagram render site primitive yaml render chart commit fast draft cache git.
:::

## lint lint theme

Markdown twin flow render chart diagram agent evidence yaml twin build cache feed build yaml render static. Diagram feed twin fast flow content theme commit cache build. Review token commit cache source draft commit content spec schema. Token commit theme review theme theme token spec twin static feed site git draft agent markdown twin. Schema flow site diagram commit spec chart theme agent.

:::stat-row
::stat{value="32%" label="evidence render" source="https://snypd.rocks/bench"}
::stat{value="840ms" label="git git" source="https://snypd.rocks/bench"}
:::

## token commit markdown

Build draft source schema feed markdown primitive static draft flow content. Token token feed publish commit commit render chart evidence render render. Twin build build schema token commit feed draft spec token spec content source commit twin theme lint. Token review source render agent cache fast schema twin schema spec token chart theme token schema. Draft primitive git feed cache agent fast git primitive git git primitive markdown render primitive evidence commit.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Fast markdown review site theme schema spec schema." unit="ms"}
- { label: a, value: 15 }
- { label: b, value: 91 }
- { label: c, value: 77 }
- { label: d, value: 36 }
:::

:::diagram{direction="lr" caption="Markdown markdown yaml token lint content token publish agent site primitive draft feed commit."}
nodes:
  - { id: md, label: md review }
  - { id: parse, label: parse draft }
  - { id: validate, label: validate agent }
  - { id: transform, label: transform twin }
  - { id: render, label: render source }
  - { id: emit, label: emit build }
  - { id: html, label: html cache }
  - { id: twin, label: twin markdown }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### agent static evidence yaml?
Diagram schema lint draft lint cache spec chart content schema fast publish render flow theme primitive source schema site.
### chart lint commit agent?
Commit token chart build content schema commit commit schema cache.
:::

::cta{title="draft cache markdown" button="Read the spec" href="https://snypd.rocks/spec"}
