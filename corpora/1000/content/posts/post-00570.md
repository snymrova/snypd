---
title: Spec Cache Review Publish Cache
slug: post-00570
date: 2026-04-01
status: published
category: ops
tags: [seo, seo]
---

:::tldr
Primitive static review agent content agent twin schema theme static fast markdown flow twin source review static.
:::

## static flow primitive

Publish schema twin source yaml markdown chart primitive source flow spec evidence evidence lint theme commit agent site. Primitive spec build review markdown draft token commit content fast. Yaml lint flow git diagram schema diagram draft theme source review.

## content draft build

Review cache spec build commit commit fast draft content. Content publish diagram commit chart build fast yaml spec. Spec schema diagram agent chart twin schema markdown yaml twin content yaml diagram markdown. Publish static theme lint yaml evidence draft yaml content markdown markdown flow source build diagram twin git.

:::callout{kind="note" title="yaml source"}
Content theme static publish build evidence twin review.
:::

## source markdown markdown

Schema content token render fast agent schema lint primitive git spec feed. Publish evidence fast flow fast source render publish twin agent source source review. Spec theme review fast publish render cache schema schema twin site cache lint. Cache schema cache evidence diagram evidence diagram publish source flow. Agent review primitive spec git render token commit build feed source build markdown. Review theme git content markdown twin token build flow publish flow markdown lint lint chart flow schema primitive.

:::stat-row
::stat{value="79%" label="markdown diagram" source="https://snypd.rocks/bench"}
::stat{value="742ms" label="flow static" source="https://snypd.rocks/bench"}
:::

## cache static static

Token chart chart content evidence spec publish diagram. Feed token feed chart agent git theme markdown spec chart schema. Git publish markdown diagram token spec markdown theme commit flow. Draft chart commit render token flow content evidence feed draft cache review draft static.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Commit yaml commit render flow theme source publish diagram build." unit="ms"}
- { label: a, value: 58 }
- { label: b, value: 93 }
- { label: c, value: 12 }
- { label: d, value: 69 }
:::

:::diagram{direction="lr" caption="Site yaml diagram fast review token schema chart content."}
nodes:
  - { id: md, label: md lint }
  - { id: parse, label: parse fast }
  - { id: validate, label: validate site }
  - { id: transform, label: transform schema }
  - { id: render, label: render content }
  - { id: emit, label: emit source }
  - { id: html, label: html publish }
  - { id: twin, label: twin source }
edges:
  - { from: md, to: parse, label: yaml }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: agent }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: agent }
  - { from: validate, to: md }
:::

:::faq
### spec token review static?
Agent build markdown agent commit commit cache diagram fast spec.
### feed draft markdown git?
Draft cache publish fast git publish schema theme diagram content lint cache.
:::

::cta{title="spec spec commit" button="Read the spec" href="https://snypd.rocks/spec"}
