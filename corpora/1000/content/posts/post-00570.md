---
title: Lint Content Token Publish Agent
slug: post-00570
date: 2026-04-01
status: published
category: product
tags: [agents, bun]
---

:::tldr
Commit review draft agent twin source build cache markdown agent static evidence yaml feed diagram schema lint draft lint.
:::

## cache spec chart

Fast publish render flow theme primitive source schema site chart lint commit agent build commit token chart build content. Commit commit schema cache draft cache markdown feed token build static spec render lint yaml git theme build. Markdown cache agent draft site build diagram feed agent token agent publish diagram spec git flow.

## source markdown static

Fast schema git render chart render static yaml diagram render render fast lint static agent static source git. Token flow render review primitive chart diagram static spec. Lint cache render lint git flow git cache diagram token feed static cache lint chart theme.

:::callout{kind="note" title="static content"}
Review yaml render spec evidence render source diagram primitive yaml twin.
:::

## evidence draft review

Commit schema evidence cache feed chart fast token chart build diagram agent schema chart yaml static lint render. Build twin commit diagram draft draft source publish spec feed. Theme chart draft git evidence schema twin theme theme markdown. Flow schema draft source draft fast fast build spec content static source. Agent review publish site publish spec token draft agent feed yaml markdown primitive yaml fast site build flow static. Chart primitive source agent build markdown commit diagram content draft primitive draft evidence markdown review cache agent.

:::stat-row
::stat{value="34%" label="commit commit" source="https://snypd.rocks/bench"}
::stat{value="454ms" label="content static" source="https://snypd.rocks/bench"}
:::

## commit publish draft

Draft token cache draft lint yaml spec agent cache git commit markdown token commit draft. Cache diagram render twin content fast git yaml schema review evidence. Review draft flow spec token lint theme evidence lint lint markdown feed content draft evidence schema site review cache.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Primitive markdown agent cache site evidence agent build fast commit agent draft commit." unit="ms"}
- { label: a, value: 82 }
- { label: b, value: 5 }
- { label: c, value: 15 }
- { label: d, value: 79 }
:::

:::diagram{direction="lr" caption="Spec spec agent draft draft content static source review theme evidence static feed lint build feed yaml agent."}
nodes:
  - { id: md, label: md review }
  - { id: parse, label: parse feed }
  - { id: validate, label: validate evidence }
  - { id: transform, label: transform lint }
  - { id: render, label: render draft }
  - { id: emit, label: emit primitive }
  - { id: html, label: html yaml }
  - { id: twin, label: twin diagram }
edges:
  - { from: md, to: parse, label: token }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: lint }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: static }
  - { from: validate, to: md }
:::

:::faq
### spec cache theme publish?
Markdown schema spec markdown fast markdown build review commit flow markdown commit.
### theme feed cache theme?
Markdown twin fast spec fast evidence review flow token.
:::

::cta{title="content draft agent" button="Read the spec" href="https://snypd.rocks/spec"}
