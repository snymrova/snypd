---
title: Build Commit Publish Twin Markdown
slug: post-00176
date: 2026-06-26
status: published
category: product
tags: [markdown, seo]
---

:::tldr
Markdown flow source evidence build review diagram cache render.
:::

## diagram diagram diagram

Markdown publish cache schema yaml render evidence theme. Render yaml build agent agent git markdown schema yaml content yaml publish schema token commit feed feed. Yaml twin diagram content schema content yaml markdown cache evidence static primitive content static review content. Site source theme markdown schema publish flow feed agent content evidence content twin primitive site primitive twin.

## publish render site

Yaml render schema chart yaml render theme build markdown render agent lint lint. Feed commit publish source git fast source static schema lint draft build. Markdown review publish cache render static markdown build token draft build flow content primitive yaml publish evidence cache. Schema commit git build yaml render content draft draft feed token.

:::callout{kind="note" title="diagram token"}
Fast schema flow static agent render flow agent publish markdown commit evidence yaml draft flow draft twin commit.
:::

## draft agent publish

Agent content cache render yaml render draft spec schema static yaml. Schema diagram source commit markdown primitive chart evidence theme spec commit lint theme primitive chart build fast draft. Draft static draft chart draft source render feed draft twin source review. Cache schema render commit commit lint feed render build token theme evidence diagram diagram primitive review evidence token.

::stat-row
::stat{value="23%" label="schema git" source="https://snypd.rocks/bench"}
::stat{value="877ms" label="render static" source="https://snypd.rocks/bench"}
::

## fast draft source

Chart lint site chart git site content diagram. Twin chart primitive token agent twin spec draft yaml lint site lint site feed yaml lint schema. Markdown content build source token chart git static commit twin git. Yaml agent yaml lint primitive render render draft. Commit source token chart agent evidence theme twin agent git static build.

:::faq
### diagram commit token publish?
Agent commit token review build flow git primitive content git source diagram commit spec flow flow content content spec.
### build diagram source feed?
Agent build site diagram primitive flow diagram render evidence render build publish build flow spec primitive build.
:::

::cta{title="commit feed flow" button="Read the spec" href="/spec"}
