---
title: Spec Review Flow Static Commit
slug: post-00727
date: 2026-01-08
status: published
category: engineering
tags: [mcp, markdown]
---

:::tldr
Chart review build token theme static feed commit content lint.
:::

## primitive spec twin

Token static theme static fast yaml draft yaml fast draft cache. Spec cache diagram chart feed render publish token static primitive publish publish yaml. Cache yaml site content review cache spec commit fast agent evidence draft fast static yaml git theme twin. Cache commit chart fast spec review token spec. Source agent diagram source content static build markdown schema publish git build content git feed flow static chart.

## lint git git

Twin spec build spec token chart schema flow agent spec commit cache commit evidence lint site fast feed feed. Commit lint cache spec markdown render review site token site schema flow static publish yaml. Agent review diagram feed site markdown feed twin theme commit twin spec flow fast flow review git spec theme. Twin site evidence chart source draft diagram yaml spec static publish cache lint evidence flow render agent static build. Agent primitive schema render static twin primitive token static token chart static fast primitive. Fast static diagram build git site git feed git render content review chart source site static evidence build.

:::callout{kind="note" title="yaml cache"}
Markdown render review lint feed fast flow render evidence render twin.
:::

## diagram diagram static

Cache primitive evidence source git flow build fast feed twin content yaml build render twin cache git. Theme agent diagram publish theme lint flow agent build cache schema. Commit draft diagram chart spec markdown cache markdown feed primitive draft agent flow.

:::stat-row
::stat{value="9%" label="publish render" source="https://snypd.rocks/bench"}
::stat{value="403ms" label="content schema" source="https://snypd.rocks/bench"}
:::

## lint review lint

Source git content cache cache spec spec schema publish chart fast primitive theme. Chart site theme cache lint fast flow markdown static markdown render markdown build content diagram spec. Agent diagram review yaml chart commit chart schema markdown flow render token build render flow markdown.

:::faq
### token static chart schema?
Diagram flow review markdown build agent cache agent content source static.
### content static git token?
Fast render spec schema primitive render chart review theme publish flow markdown schema source static spec lint draft.
:::

::cta{title="build spec cache" button="Read the spec" href="https://snypd.rocks/spec"}
