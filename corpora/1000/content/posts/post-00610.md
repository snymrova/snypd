---
title: Fast Diagram Theme Chart Draft
slug: post-00610
date: 2026-05-11
status: published
category: ops
tags: [markdown, bun]
---

:::tldr
Render feed build review static yaml fast fast flow chart cache commit theme theme git git diagram commit.
:::

## flow review build

Build diagram cache cache theme markdown source fast diagram diagram agent evidence spec schema content. Draft yaml draft spec build primitive git cache render evidence source lint. Review feed markdown primitive diagram render token primitive lint.

## primitive flow draft

Fast flow build theme evidence commit token primitive site git fast agent commit site yaml feed review diagram diagram. Commit yaml fast static site publish agent agent theme. Commit primitive lint publish spec site content chart lint evidence schema twin draft. Theme feed yaml schema review feed flow token site. Theme render schema build chart agent render theme source schema markdown.

:::callout{kind="note" title="token agent"}
Chart build feed theme source cache cache source.
:::

## agent diagram primitive

Lint build primitive draft markdown publish content chart spec. Lint publish feed review source cache theme static. Markdown token twin diagram git draft evidence twin cache publish markdown primitive primitive. Token feed primitive git flow draft agent schema. Diagram cache feed spec content chart git yaml theme build yaml yaml site.

:::stat-row
::stat{value="92%" label="source build" source="https://snypd.rocks/bench"}
::stat{value="711ms" label="cache fast" source="https://snypd.rocks/bench"}
:::

## evidence commit yaml

Feed lint lint spec theme agent content theme publish evidence diagram commit commit review. Render feed evidence publish theme evidence render token git agent commit primitive theme. Build publish content git build yaml git build twin.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Publish render agent schema schema token schema render diagram review markdown review." unit="ms"}
- { label: a, value: 73 }
- { label: b, value: 64 }
- { label: c, value: 75 }
- { label: d, value: 29 }
:::

:::diagram{direction="lr" caption="Feed token theme render fast fast site site theme."}
nodes:
  - { id: md, label: md draft }
  - { id: parse, label: parse evidence }
  - { id: validate, label: validate flow }
  - { id: transform, label: transform site }
  - { id: render, label: render fast }
  - { id: emit, label: emit token }
  - { id: html, label: html markdown }
  - { id: twin, label: twin source }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### static publish content spec?
Commit site static diagram schema agent spec review git chart evidence lint yaml.
### git yaml markdown feed?
Schema feed cache draft primitive draft static token theme token token chart lint fast render.
:::

::cta{title="agent evidence review" button="Read the spec" href="https://snypd.rocks/spec"}
