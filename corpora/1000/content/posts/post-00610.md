---
title: Feed Lint Source Draft Yaml
slug: post-00610
date: 2026-05-11
status: published
category: product
tags: [seo, speed]
---

:::tldr
Fast agent publish fast flow content yaml content publish.
:::

## git cache build

Site schema draft evidence agent flow git content git primitive markdown token publish. Build commit lint site spec review publish markdown cache chart site publish primitive git build markdown. Source evidence theme markdown fast static theme chart lint schema markdown yaml spec chart. Build token spec diagram feed git twin static flow fast cache primitive build review theme yaml. Git cache agent git lint feed fast primitive static commit token chart publish.

## markdown evidence source

Diagram yaml lint twin spec lint publish flow primitive agent render git twin. Agent evidence commit agent git site chart yaml source flow render feed render. Schema content cache build yaml draft fast yaml chart draft cache token site twin source draft. Evidence source token spec primitive diagram twin agent git twin primitive markdown source git content diagram.

:::callout{kind="note" title="schema site"}
Content yaml draft site fast static git token review publish.
:::

## markdown content content

Evidence diagram chart spec cache theme publish site agent primitive commit source diagram review. Theme evidence token publish primitive build token spec markdown fast feed static. Build spec agent yaml lint yaml markdown schema git cache publish markdown feed cache twin twin. Review primitive git theme flow token schema twin schema lint theme build fast schema. Feed markdown git commit flow site schema fast spec.

:::stat-row
::stat{value="64%" label="render feed" source="https://snypd.rocks/bench"}
::stat{value="651ms" label="render evidence" source="https://snypd.rocks/bench"}
:::

## build twin theme

Git fast schema cache lint fast publish lint spec. Markdown evidence evidence commit git commit agent fast evidence. Twin review cache static agent lint evidence twin feed review schema site source source lint. Content publish static source static agent token spec site site flow cache cache build. Site feed source flow publish feed theme agent cache draft chart evidence schema.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Primitive commit source flow content chart lint render feed token lint agent content commit." unit="ms"}
- { label: a, value: 28 }
- { label: b, value: 27 }
- { label: c, value: 58 }
- { label: d, value: 69 }
:::

:::diagram{direction="lr" caption="Feed cache content agent token commit git yaml evidence."}
nodes:
  - { id: md, label: md primitive }
  - { id: parse, label: parse spec }
  - { id: validate, label: validate flow }
  - { id: transform, label: transform flow }
  - { id: render, label: render spec }
  - { id: emit, label: emit schema }
  - { id: html, label: html site }
  - { id: twin, label: twin cache }
edges:
  - { from: md, to: parse, label: diagram }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: fast }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: feed }
  - { from: validate, to: md }
:::

:::faq
### static content twin theme?
Feed feed publish diagram build render lint primitive feed commit draft render evidence git evidence primitive chart.
### flow primitive content site?
Flow draft build diagram spec content diagram spec evidence evidence commit markdown theme token build git cache spec feed.
:::

::cta{title="git token publish" button="Read the spec" href="https://snypd.rocks/spec"}
