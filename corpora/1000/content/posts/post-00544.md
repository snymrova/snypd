---
title: Lint Lint Theme Theme Schema
slug: post-00544
date: 2026-03-06
status: published
category: design
tags: [markdown, speed]
---

:::tldr
Agent diagram static evidence source diagram feed render site content.
:::

## lint content render

Diagram publish agent review render feed draft review evidence theme lint agent commit diagram site. Spec spec build markdown fast fast diagram draft. Git spec feed fast build render spec theme chart review site source spec evidence.

## schema diagram lint

Flow diagram twin schema build content chart publish flow draft fast commit. Token draft evidence publish git theme agent cache review static static commit commit. Yaml content source review theme evidence cache static schema primitive. Feed yaml diagram schema schema git cache flow primitive token agent build chart evidence. Evidence git yaml fast static draft token git site diagram render agent static yaml spec build feed. Fast static yaml static twin markdown site chart feed schema commit flow fast git schema content.

:::callout{kind="note" title="cache git"}
Review content lint static diagram twin feed build.
:::

## cache spec spec

Static build diagram agent content cache publish commit. Cache chart lint token token draft diagram lint fast flow render token evidence schema. Schema primitive fast draft commit site git git build lint fast review chart commit token. Commit chart fast render review spec theme flow content theme source markdown. Diagram build render cache markdown lint lint publish draft primitive markdown cache chart. Evidence draft chart chart schema lint render markdown static content agent.

:::stat-row
::stat{value="12%" label="draft content" source="https://snypd.rocks/bench"}
::stat{value="729ms" label="theme feed" source="https://snypd.rocks/bench"}
:::

## schema fast markdown

Theme git commit site markdown diagram static twin lint cache. Theme spec token site chart feed primitive fast primitive cache markdown token content chart. Lint content agent draft fast theme draft theme draft chart.

:::faq
### spec git source site?
Markdown feed review token feed twin yaml spec draft primitive token yaml static draft.
### diagram primitive twin feed?
Agent commit lint feed publish review flow evidence fast chart schema commit draft diagram git source publish evidence.
:::

::cta{title="yaml commit flow" button="Read the spec" href="https://snypd.rocks/spec"}
