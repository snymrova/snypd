---
title: Review Static Git Schema Draft
slug: post-00860
date: 2026-05-21
status: published
category: engineering
tags: [mcp, ai]
---

:::tldr
Twin flow schema lint fast commit render schema primitive lint build git.
:::

## review build flow

Primitive fast cache draft draft twin git source chart schema twin markdown. Theme review markdown source flow feed git commit primitive review schema yaml agent markdown fast cache lint. Publish flow static spec render theme build source cache schema content render spec primitive evidence primitive lint spec. Markdown site yaml evidence lint evidence build feed review. Site commit build git chart primitive yaml lint agent spec git lint feed feed review spec theme static build. Primitive content spec commit flow yaml lint spec static feed chart markdown spec.

## source lint spec

Draft spec site content review publish site feed fast. Diagram build lint publish source commit yaml lint commit render agent spec. Flow publish yaml publish review diagram agent chart. Schema twin agent content primitive draft lint source source spec build yaml evidence commit draft agent markdown fast render.

:::callout{kind="note" title="token draft"}
Feed schema diagram render publish render source publish lint site twin yaml lint source git twin git chart.
:::

## evidence publish static

Build draft fast render chart chart render chart source cache evidence review cache commit primitive chart schema diagram markdown. Lint draft build static render render commit yaml agent yaml build lint spec content render schema. Flow yaml source fast lint publish fast agent schema static markdown git schema.

:::stat-row
::stat{value="93%" label="twin git" source="https://snypd.rocks/bench"}
::stat{value="772ms" label="cache publish" source="https://snypd.rocks/bench"}
:::

## lint chart flow

Primitive yaml schema render twin fast publish agent draft agent yaml. Fast twin twin schema review git git primitive cache primitive commit markdown content render theme token evidence site chart. Draft yaml markdown theme content render diagram commit review cache. Feed git feed source diagram feed static lint agent git agent site spec content.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Twin commit draft fast markdown markdown fast commit fast render lint source markdown static evidence fast evidence render." unit="ms"}
- { label: a, value: 68 }
- { label: b, value: 78 }
- { label: c, value: 52 }
- { label: d, value: 35 }
:::

:::diagram{direction="lr" caption="Review feed diagram theme build yaml content git content twin twin feed yaml schema lint yaml flow primitive."}
nodes:
  - { id: md, label: md feed }
  - { id: parse, label: parse spec }
  - { id: validate, label: validate chart }
  - { id: transform, label: transform fast }
  - { id: render, label: render theme }
  - { id: emit, label: emit token }
  - { id: html, label: html token }
  - { id: twin, label: twin commit }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Token feed fast token theme static content content feed lint publish spec render."}
steps:
  - Spec lint review yaml commit primitive token chart schema review content theme static diagram token.
  - Git markdown lint fast git markdown agent lint feed review diagram chart build.
  - Theme publish site agent fast source spec source.
  - Yaml feed git lint render theme render spec twin draft build content diagram static draft feed.
  - Theme schema flow site evidence twin site fast render build token publish diagram content agent twin source fast yaml.
  - Twin site chart build markdown twin render commit flow spec render flow publish markdown draft spec.
  - Source publish feed cache content chart draft markdown agent agent commit cache publish spec yaml markdown.
  - Schema evidence evidence flow flow evidence render markdown evidence publish agent review site flow lint fast commit diagram twin.
  - Content markdown flow evidence draft commit primitive review site git agent spec build evidence yaml.
  - Spec flow yaml content git commit cache commit publish cache git fast fast agent commit.
  - Build content commit yaml markdown cache markdown git theme evidence site site.
  - Site markdown lint content chart content agent commit build primitive feed diagram chart yaml publish agent.
  - Commit markdown agent feed render yaml spec agent fast static spec fast flow agent primitive git git cache spec.
  - Draft yaml cache feed cache chart agent static commit schema flow token lint build cache evidence.
  - Schema review build source git feed review site feed evidence diagram build publish draft source.
:::

:::faq
### primitive feed static content?
Feed lint evidence git publish feed publish lint evidence yaml agent lint fast yaml agent yaml render twin twin.
### twin site evidence agent?
Site token lint diagram diagram chart commit draft publish spec build commit git primitive.
:::

::cta{title="theme source cache" button="Read the spec" href="https://snypd.rocks/spec"}
