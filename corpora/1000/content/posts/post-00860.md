---
title: Evidence Spec Feed Publish Token
slug: post-00860
date: 2026-05-21
status: published
category: engineering
tags: [markdown, agents]
---

:::tldr
Review site lint build token static token render flow theme cache publish.
:::

## feed flow build

Primitive twin spec publish commit render theme review static draft git render theme token feed agent flow yaml schema. Cache feed cache spec feed flow agent spec draft commit fast site feed fast lint yaml review source. Markdown token source yaml twin spec commit publish schema lint render. Token git build spec static lint fast site token build render build evidence spec yaml flow content diagram. Yaml static flow publish markdown schema static spec spec spec flow. Source fast commit static site build draft commit render site agent.

## feed chart fast

Evidence static source review theme yaml chart git primitive content static markdown markdown. Site static fast publish static markdown evidence feed static. Yaml evidence diagram chart schema theme flow build git. Build flow spec site build agent commit token markdown agent render publish markdown feed source. Lint markdown draft feed flow content theme primitive build.

:::callout{kind="note" title="build spec"}
Evidence review git diagram fast markdown publish content.
:::

## evidence diagram feed

Flow evidence site agent site theme cache diagram schema spec site git. Theme evidence build evidence cache agent lint theme evidence flow build. Theme evidence cache theme chart markdown schema feed git site render. Token build agent draft static cache chart commit source flow cache publish primitive. Content primitive render feed build token draft fast chart source primitive. Token cache yaml lint schema flow cache lint fast commit render agent feed review twin content site.

:::stat-row
::stat{value="22%" label="static token" source="https://snypd.rocks/bench"}
::stat{value="892ms" label="markdown build" source="https://snypd.rocks/bench"}
:::

## evidence static twin

Draft content fast static schema diagram build token commit source. Agent content site site chart evidence yaml static cache static static flow render. Publish fast commit lint static schema diagram flow fast source primitive. Content spec schema token primitive render lint chart fast twin draft flow evidence agent git. Yaml fast content review git token schema evidence fast render lint. Feed source render publish lint flow yaml cache.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Build spec markdown yaml feed agent review review publish token source." unit="ms"}
- { label: a, value: 2 }
- { label: b, value: 66 }
- { label: c, value: 66 }
- { label: d, value: 39 }
:::

:::diagram{direction="lr" caption="Markdown yaml theme schema schema render spec yaml static."}
nodes:
  - { id: md, label: md twin }
  - { id: parse, label: parse agent }
  - { id: validate, label: validate cache }
  - { id: transform, label: transform content }
  - { id: render, label: render cache }
  - { id: emit, label: emit yaml }
  - { id: html, label: html markdown }
  - { id: twin, label: twin site }
edges:
  - { from: md, to: parse, label: agent }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: feed }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: render }
  - { from: validate, to: md }
:::

:::flow{caption="Schema agent static build markdown primitive twin source cache render theme diagram theme commit render feed render."}
steps:
  - Evidence draft primitive chart spec render draft site publish.
  - Source publish static markdown render fast theme markdown publish primitive diagram yaml theme markdown theme.
  - Token render token commit yaml yaml lint lint review schema schema twin review flow fast static fast.
  - Token primitive spec fast build yaml content flow commit render primitive chart twin site content.
  - Git build diagram flow source schema draft markdown.
  - Chart agent diagram draft feed fast review spec source draft review render token site.
  - Lint content git chart commit primitive token spec site build schema schema render evidence diagram git fast.
  - Static theme source lint twin spec feed content spec git flow publish token render fast draft.
  - Feed git markdown markdown spec diagram draft publish publish content source.
  - Token build git twin feed review commit feed fast yaml token feed flow source build.
  - Site twin token commit evidence static agent build git content primitive source static theme evidence commit markdown.
  - Flow spec render build source commit site static site feed markdown static evidence theme markdown primitive commit chart.
  - Render source draft spec markdown draft diagram diagram draft draft lint build diagram twin review content.
  - Git cache fast schema static review theme twin draft.
  - Build feed theme publish feed content fast markdown commit commit publish static spec feed markdown primitive lint lint.
:::

:::faq
### chart feed markdown content?
Evidence yaml evidence lint schema build review primitive chart twin evidence feed render draft spec agent fast primitive.
### fast commit markdown source?
Yaml spec site cache cache diagram static build build diagram yaml chart yaml.
:::

::cta{title="diagram evidence spec" button="Read the spec" href="https://snypd.rocks/spec"}
