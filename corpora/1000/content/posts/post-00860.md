---
title: Flow Review Flow Site Schema
slug: post-00860
date: 2026-05-21
status: published
category: ops
tags: [agents, bun]
---

:::tldr
Content feed render publish lint site twin flow static site draft fast publish schema fast yaml.
:::

## flow build twin

Chart token commit feed theme fast git lint chart markdown evidence static agent cache. Token source fast content spec draft flow schema yaml commit diagram spec commit. Agent agent commit source publish token feed schema primitive source commit. Git evidence content build lint git publish flow diagram cache review chart primitive draft token publish token draft.

## agent chart static

Chart review markdown primitive draft draft site lint yaml render fast schema diagram yaml draft chart content diagram theme. Source git agent flow spec flow diagram git static schema publish schema. Render commit fast build git review cache feed schema flow schema flow diagram review diagram feed. Yaml spec cache chart twin review content git schema draft render agent site draft git theme chart feed render.

:::callout{kind="note" title="commit cache"}
Commit twin content render chart commit theme git git yaml token source token content build token markdown.
:::

## chart render twin

Token yaml twin agent yaml agent lint content. Draft cache markdown render markdown content cache lint static site publish diagram static git markdown. Flow evidence cache schema primitive publish site token git source spec diagram schema draft spec content. Commit markdown source cache build fast content publish draft evidence. Render flow source diagram flow primitive agent agent git build review flow twin git site render. Review draft diagram publish feed site token cache twin feed lint content spec token evidence schema token diagram.

:::stat-row
::stat{value="42%" label="yaml yaml" source="https://snypd.rocks/bench"}
::stat{value="30ms" label="build spec" source="https://snypd.rocks/bench"}
:::

## draft schema fast

Git flow evidence git evidence build static spec site token yaml token render static evidence. Markdown lint theme spec fast yaml git spec content site evidence agent. Static spec primitive commit publish render markdown flow chart fast. Primitive lint site fast build primitive spec chart twin cache fast content publish. Build schema theme fast diagram lint git draft schema publish markdown spec static draft site review spec render draft. Schema diagram primitive markdown yaml spec draft source agent cache review agent fast git draft source lint publish.

:::chart{type="area" source="https://snypd.rocks/bench" caption="Draft draft diagram draft cache yaml publish primitive diagram lint." unit="ms"}
- { label: a, value: 13 }
- { label: b, value: 64 }
- { label: c, value: 63 }
- { label: d, value: 9 }
:::

:::diagram{direction="lr" caption="Content static token schema draft token commit schema diagram diagram commit feed publish."}
nodes:
  - { id: md, label: md agent }
  - { id: parse, label: parse twin }
  - { id: validate, label: validate evidence }
  - { id: transform, label: transform git }
  - { id: render, label: render theme }
  - { id: emit, label: emit feed }
  - { id: html, label: html static }
  - { id: twin, label: twin primitive }
edges:
  - { from: md, to: parse, label: theme }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: static }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: primitive }
  - { from: validate, to: md }
:::

:::flow{caption="Build primitive schema agent agent git spec agent agent commit review."}
steps:
  - { id: start, do: Feed agent cache diagram lint spec site diagram build token markdown token flow draft content chart. }
  - Build primitive feed primitive lint render render feed render review chart markdown twin build git.
  - ask: twin site site?
    yes: Schema build cache fast spec evidence schema diagram.
    no: { then: start }
  - Agent publish git token content build commit static evidence cache evidence.
  - ask: feed commit site?
    yes:
      - Review render evidence site chart schema spec chart token build primitive.
      - Token chart yaml schema theme primitive agent draft theme feed diagram.
    no: { then: fix }
  - { id: fix, do: Diagram token cache commit build flow feed schema cache markdown spec schema fast review feed evidence. }
  - Site markdown diagram content build theme agent yaml build draft primitive spec agent.
  - Draft spec primitive render draft build schema site git evidence site render.
:::

:::faq
### commit content static agent?
Cache yaml theme markdown evidence draft primitive diagram.
### schema theme site flow?
Static evidence chart diagram draft markdown chart git agent draft spec cache content commit yaml static fast.
:::

::cta{title="cache markdown chart" button="Read the spec" href="https://snypd.rocks/spec"}
