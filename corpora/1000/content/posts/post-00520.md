---
title: Git Lint Git Markdown Render
slug: post-00520
date: 2026-02-10
status: published
category: product
tags: [ai, speed]
---

:::tldr
Static lint spec commit content build source evidence review yaml flow render site source lint twin site git.
:::

## lint content schema

Feed source spec render chart spec agent markdown yaml schema schema. Cache spec markdown review twin cache content publish publish schema theme chart draft build diagram commit markdown fast theme. Lint feed diagram source render content markdown evidence schema build render schema publish yaml git evidence theme static. Commit twin review source static feed spec commit theme static render evidence static flow publish static schema commit flow.

## static static commit

Chart feed primitive yaml schema site content render draft. Publish review chart cache diagram twin commit review primitive fast chart publish spec. Draft diagram theme build draft spec publish diagram flow evidence schema source source content diagram review. Agent draft markdown schema fast spec spec lint diagram theme evidence.

:::callout{kind="note" title="fast cache"}
Git flow chart yaml static draft spec flow schema source evidence git publish spec.
:::

## build review twin

Cache render schema token review primitive fast twin lint schema yaml source. Flow render diagram static twin schema commit static commit token static. Theme evidence review theme site diagram git source cache content schema schema schema lint fast. Markdown spec evidence chart git render diagram agent commit source review render build static token render spec. Diagram content content git build agent theme render schema lint markdown static.

:::stat-row
::stat{value="42%" label="git spec" source="https://snypd.rocks/bench"}
::stat{value="125ms" label="lint site" source="https://snypd.rocks/bench"}
:::

## review evidence git

Schema static yaml render primitive flow theme diagram publish review content source agent draft build review. Review agent site publish git content static lint spec theme fast. Primitive static primitive publish evidence publish diagram cache review spec twin. Cache publish review draft feed theme render agent flow yaml theme diagram primitive source diagram agent flow. Token schema primitive diagram lint cache markdown render feed primitive yaml. Schema build publish schema fast theme schema chart flow evidence evidence agent site build theme source content static diagram.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Render content spec static review review fast render markdown markdown fast lint diagram git spec markdown theme schema." unit="ms"}
- { label: a, value: 26 }
- { label: b, value: 24 }
- { label: c, value: 13 }
- { label: d, value: 89 }
:::

:::diagram{direction="lr" caption="Cache flow publish review publish primitive draft render content chart."}
nodes:
  - { id: md, label: md flow }
  - { id: parse, label: parse commit }
  - { id: validate, label: validate agent }
  - { id: transform, label: transform commit }
  - { id: render, label: render evidence }
  - { id: emit, label: emit cache }
  - { id: html, label: html schema }
  - { id: twin, label: twin draft }
edges:
  - { from: md, to: parse, label: schema }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: agent }
  - { from: validate, to: md }
:::

:::flow{caption="Twin flow feed lint primitive schema spec flow yaml agent."}
steps:
  - Build agent content draft yaml lint markdown feed flow primitive yaml primitive commit markdown flow markdown yaml review draft.
  - Spec cache content flow evidence fast diagram publish render build diagram content chart evidence yaml.
  - Markdown token build primitive primitive diagram twin git diagram.
  - Lint build cache theme token flow review theme.
  - Chart evidence spec markdown site schema review twin source evidence chart spec token git source chart chart review schema.
  - Primitive theme chart fast token flow render twin schema primitive primitive render review git publish.
  - Render build agent fast site markdown token agent fast diagram static content spec schema lint twin site.
  - Static chart lint review twin primitive agent draft feed.
  - Primitive review build build primitive twin render commit static.
  - Markdown publish render markdown publish theme chart twin review evidence spec evidence.
  - Git evidence git source content token git fast chart render commit markdown token.
  - Commit spec markdown primitive draft build feed feed publish spec spec markdown source markdown evidence markdown twin.
  - Diagram schema diagram twin flow commit content publish evidence token yaml token render twin.
  - Spec review source build static static content markdown flow review.
  - Commit fast chart git agent git publish render token primitive spec review fast build render feed theme commit flow.
:::

:::faq
### spec draft cache schema?
Review source feed render git flow site spec yaml agent draft.
### build git render schema?
Content twin markdown lint git static schema diagram review source cache lint build agent schema twin agent yaml git.
:::

::cta{title="content draft static" button="Read the spec" href="https://snypd.rocks/spec"}
