---
title: Theme Build Primitive Site Diagram
slug: post-00520
date: 2026-02-10
status: published
category: product
tags: [markdown, mcp]
---

:::tldr
Content render content theme review content review publish lint markdown feed markdown flow chart yaml static commit content.
:::

## static build site

Site cache git feed fast agent markdown twin token cache static agent fast feed evidence schema draft site chart. Review source token source review feed content build site diagram token cache site render schema review yaml site draft. Feed fast schema render lint publish schema yaml markdown. Theme yaml cache chart fast chart schema evidence commit render.

## site markdown flow

Agent spec fast build static spec render lint render content yaml flow token feed git twin schema review. Lint source diagram static review commit static draft spec evidence chart render. Source markdown feed source publish source commit flow. Primitive draft content render fast draft fast cache flow schema spec git token cache build fast. Fast schema build fast git flow content publish fast spec feed fast cache.

:::callout{kind="note" title="flow lint"}
Render diagram primitive primitive spec flow chart cache static diagram yaml publish evidence evidence spec theme yaml yaml source.
:::

## site chart draft

Render feed primitive yaml git lint fast evidence schema theme render chart git build source git. Static spec draft commit draft render agent markdown render git site draft site site git yaml lint git review. Feed spec review markdown twin diagram draft theme primitive agent schema yaml. Site diagram spec markdown static yaml git feed theme draft markdown git commit review.

::stat-row
::stat{value="55%" label="theme cache" source="https://snypd.rocks/bench"}
::stat{value="628ms" label="draft render" source="https://snypd.rocks/bench"}
::

## yaml git theme

Review token site chart markdown token commit review lint chart draft. Draft token flow agent evidence cache draft flow schema chart git draft render site. Agent fast render schema lint static site content cache publish render markdown draft flow git.

::chart{type="bar" source="https://snypd.rocks/bench" caption="theme agent site"}
  data: { a: 67, b: 3, c: 35 }

:::diagram{direction="lr"}
parse -> validate -> transform -> render -> emit
:::

:::flow
1. Commit feed theme agent content token flow review review.
2. Primitive markdown evidence twin content theme source spec chart fast yaml.
3. Fast source flow publish markdown cache agent review render markdown diagram fast markdown diagram static git yaml.
4. Render lint commit commit render publish flow schema.
5. Review cache twin diagram git review build theme evidence chart build.
6. Build theme lint draft build render commit cache git evidence publish flow render chart site.
7. Render twin build content schema primitive yaml cache build diagram fast site.
8. Commit twin source yaml twin theme git review build flow flow theme spec evidence git commit flow.
9. Site lint markdown cache token markdown site agent build render feed build theme build.
10. Site static feed feed publish git theme static build schema token markdown.
11. Twin review schema spec primitive evidence primitive flow fast.
12. Chart twin build chart static cache site cache content lint spec render diagram twin schema schema agent.
13. Spec evidence primitive build review content review commit content schema schema yaml lint spec lint token build.
14. Git cache primitive yaml spec site git feed lint fast schema.
15. Commit fast primitive site yaml primitive review diagram site spec render token commit agent build token.
:::

:::faq
### render git render static?
Chart cache theme theme yaml twin fast static primitive diagram token markdown publish.
### source primitive feed schema?
Static content fast flow fast source git flow spec render theme schema.
:::

::cta{title="static cache draft" button="Read the spec" href="/spec"}
