---
title: Theme Diagram Publish Review Content
slug: post-00520
date: 2026-02-10
status: published
category: ops
tags: [ai, bun]
---

:::tldr
Review token review agent site publish git content static lint.
:::

## spec theme fast

Static primitive publish evidence publish diagram cache review spec twin. Cache publish review draft feed theme render agent flow yaml theme diagram primitive source diagram agent flow. Token schema primitive diagram lint cache markdown render feed primitive yaml. Schema build publish schema fast theme schema chart flow evidence evidence agent site build theme source content static diagram.

## flow render content

Review review fast render markdown markdown fast lint diagram git spec markdown theme schema. Build theme schema render cache flow publish review publish primitive draft. Content chart flow commit agent commit evidence cache schema draft. Render agent feed build agent content draft yaml lint markdown feed flow primitive yaml primitive commit markdown flow.

:::callout{kind="note" title="markdown yaml"}
Draft git spec cache content flow evidence fast diagram publish render build diagram content chart evidence.
:::

## yaml theme markdown

Primitive primitive diagram twin git diagram markdown lint build cache. Token flow review theme feed chart evidence spec markdown. Schema review twin source evidence chart spec token git source chart chart review. Git primitive theme chart fast token flow render twin schema primitive primitive render review git publish diagram render build.

:::stat-row
::stat{value="1%" label="fast site" source="https://snypd.rocks/bench"}
::stat{value="33ms" label="token agent" source="https://snypd.rocks/bench"}
:::

## fast diagram static

Schema lint twin site primitive static chart lint review twin primitive agent. Feed primitive primitive review build build primitive twin render commit static publish markdown. Render markdown publish theme chart twin review evidence spec evidence draft git.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Git source content token git fast chart render commit markdown token source commit spec markdown primitive." unit="ms"}
- { label: a, value: 45 }
- { label: b, value: 21 }
- { label: c, value: 96 }
- { label: d, value: 93 }
:::

:::diagram{direction="lr" caption="Spec spec markdown source markdown evidence markdown twin static diagram schema diagram."}
nodes:
  - { id: md, label: md twin }
  - { id: parse, label: parse flow }
  - { id: validate, label: validate commit }
  - { id: transform, label: transform content }
  - { id: render, label: render publish }
  - { id: emit, label: emit evidence }
  - { id: html, label: html token }
  - { id: twin, label: twin yaml }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Commit schema flow content review yaml site agent twin draft."}
steps:
  - Render twin render spec review source build static static content markdown.
  - Review twin commit fast chart git agent git publish render token primitive spec review fast build render feed.
  - Commit flow build twin flow feed lint primitive schema.
  - Flow yaml agent spec draft cache schema token review source feed render.
  - Flow site spec yaml agent draft build git render schema twin content twin markdown lint.
  - Static schema diagram review source cache lint build agent schema twin agent yaml git content.
  - Static publish evidence feed review review feed draft spec primitive commit fast diagram.
  - Schema git cache commit spec agent theme render yaml cache build publish fast static flow draft token publish.
  - Render theme feed commit fast flow static commit chart schema build evidence chart.
  - Twin fast content feed feed theme schema lint spec evidence static.
  - Flow chart cache static site commit yaml primitive fast theme static fast source cache yaml.
  - Feed commit site git yaml static render schema spec spec agent chart content fast markdown static.
  - Source diagram source site git content diagram theme fast diagram publish source lint theme token feed.
  - Feed commit fast build twin spec commit cache chart.
  - Review static token spec build git yaml lint fast token chart schema source primitive.
:::

:::faq
### agent source diagram spec?
Cache schema theme markdown source flow twin twin draft review.
### twin review publish chart?
Spec primitive diagram yaml build fast feed spec theme commit theme chart review agent twin.
:::

::cta{title="markdown agent yaml" button="Read the spec" href="https://snypd.rocks/spec"}
