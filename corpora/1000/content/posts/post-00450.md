---
title: Diagram Static Lint Agent Flow
slug: post-00450
date: 2026-07-30
status: published
category: product
tags: [speed, speed]
---

:::tldr
Theme cache theme twin yaml content review static draft markdown build.
:::

## source schema site

Source review review render agent build schema agent build site cache build token render draft theme draft. Review build agent theme site chart evidence yaml schema. Chart static draft primitive fast agent twin yaml site lint markdown primitive flow. Review chart review feed render git render evidence cache schema draft cache yaml cache primitive yaml agent publish chart. Cache token evidence theme render cache agent static markdown commit chart spec evidence static token content render.

## spec chart publish

Primitive site source token flow evidence cache token lint. Twin yaml theme twin fast git commit token build agent git build token theme content evidence markdown. Source cache diagram markdown chart commit theme agent review schema schema publish build publish feed yaml publish. Theme lint chart yaml agent content site git. Source spec review git twin schema lint site. Publish publish source content evidence evidence schema primitive.

:::callout{kind="note" title="render build"}
Cache fast flow schema render spec theme publish yaml schema schema flow content.
:::

## draft diagram source

Twin schema content build markdown chart markdown theme. Fast token git static source cache primitive site git. Fast evidence agent chart schema yaml publish schema twin static schema flow. Markdown git commit draft source schema token agent. Agent twin primitive token twin theme publish token review theme site git. Render primitive yaml yaml render lint spec cache theme feed spec content chart lint.

::stat-row
::stat{value="74%" label="diagram commit" source="https://snypd.rocks/bench"}
::stat{value="361ms" label="schema token" source="https://snypd.rocks/bench"}
::

## lint twin publish

Evidence build git primitive evidence evidence build diagram spec twin lint diagram commit lint lint commit token flow. Yaml content theme site theme chart static commit fast fast fast token content diagram token fast primitive yaml fast. Primitive git cache draft chart git feed content yaml commit fast git agent feed.

::chart{type="bar" source="https://snypd.rocks/bench" caption="git spec token"}
  data: { a: 61, b: 72, c: 41 }

:::diagram{direction="lr"}
parse -> validate -> transform -> render -> emit
:::

:::faq
### theme schema git diagram?
Theme flow twin fast lint schema static diagram publish publish review spec lint commit render lint schema theme.
### draft markdown source twin?
Review diagram twin markdown content twin lint yaml git publish agent.
:::

::cta{title="draft render fast" button="Read the spec" href="/spec"}
