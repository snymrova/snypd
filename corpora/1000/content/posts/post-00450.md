---
title: Draft Evidence Flow Review Site
slug: post-00450
date: 2026-07-30
status: published
category: ops
tags: [markdown, bun]
---

:::tldr
Chart publish twin diagram spec draft feed static review render yaml yaml markdown lint build markdown fast schema.
:::

## evidence spec markdown

Commit fast chart site token primitive site source build lint review. Draft static publish draft build commit flow theme content content render chart evidence agent site static flow lint. Review content fast chart schema cache yaml lint lint git agent.

## build publish chart

Build feed render feed token spec commit cache token flow twin theme cache diagram site. Feed cache publish render static primitive markdown diagram publish markdown publish fast. Agent lint site evidence lint lint twin twin twin commit agent commit spec markdown build draft schema markdown. Schema diagram lint source fast evidence chart cache theme fast. Token fast schema fast commit diagram lint twin token render token theme review yaml.

:::callout{kind="note" title="agent schema"}
Source static commit spec twin build flow static render.
:::

## twin static flow

Theme publish twin git chart markdown cache render theme review site primitive. Flow agent render git schema site yaml schema primitive source build render commit lint evidence diagram draft render. Agent source theme diagram draft draft commit yaml draft render. Publish theme chart static lint chart git site lint agent schema feed token source draft lint token git git. Commit evidence site agent publish yaml evidence token yaml git site.

:::stat-row
::stat{value="36%" label="evidence primitive" source="https://snypd.rocks/bench"}
::stat{value="197ms" label="publish flow" source="https://snypd.rocks/bench"}
:::

## source flow fast

Spec diagram render theme schema spec publish schema feed publish. Spec draft feed fast flow cache site schema markdown markdown review review render render schema content schema. Cache static flow commit source content theme yaml. Lint markdown content fast schema build review static theme yaml publish git. Site diagram git markdown publish review markdown commit review draft review content cache build theme flow twin. Token primitive cache review draft diagram feed commit.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Flow theme schema yaml content flow build agent primitive schema cache draft." unit="ms"}
- { label: a, value: 22 }
- { label: b, value: 83 }
- { label: c, value: 51 }
- { label: d, value: 76 }
:::

:::diagram{direction="lr" caption="Token schema site review build render spec build twin cache evidence diagram evidence commit git."}
nodes:
  - { id: md, label: md chart }
  - { id: parse, label: parse evidence }
  - { id: validate, label: validate source }
  - { id: transform, label: transform primitive }
  - { id: render, label: render flow }
  - { id: emit, label: emit flow }
  - { id: html, label: html cache }
  - { id: twin, label: twin chart }
edges:
  - { from: md, to: parse, label: content }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: schema }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: feed }
  - { from: validate, to: md }
:::

:::faq
### build token lint draft?
Commit publish source build fast build evidence cache review.
### content theme chart lint?
Primitive token agent cache spec token spec yaml cache diagram token markdown theme token token.
:::

::cta{title="git content theme" button="Read the spec" href="https://snypd.rocks/spec"}
