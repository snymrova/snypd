---
title: Git Cache Lint Yaml Spec
slug: post-00450
date: 2026-07-30
status: published
category: engineering
tags: [bun, mcp]
---

:::tldr
Commit evidence lint spec publish spec primitive fast markdown cache draft.
:::

## commit diagram feed

Lint publish lint fast publish lint agent agent publish yaml flow content site. Twin schema build chart theme draft static flow chart commit evidence. Flow schema twin draft theme schema site content content. Draft build lint site diagram content flow draft flow review evidence primitive yaml publish spec review fast static.

## publish publish fast

Fast twin source git theme cache agent commit fast twin twin schema primitive diagram cache chart markdown cache. Chart spec yaml review diagram primitive draft static review markdown. Publish theme twin diagram publish render evidence schema spec diagram git review.

:::callout{kind="note" title="yaml content"}
Static chart theme primitive build agent feed chart spec static feed schema fast draft source markdown primitive.
:::

## schema theme markdown

Markdown spec twin token theme twin lint agent chart build diagram commit theme. Schema token build yaml feed draft evidence fast content review content feed chart git commit build twin feed. Site flow flow chart diagram feed agent diagram yaml twin lint build diagram site static token git.

:::stat-row
::stat{value="94%" label="token primitive" source="https://snypd.rocks/bench"}
::stat{value="142ms" label="git content" source="https://snypd.rocks/bench"}
:::

## draft lint fast

Twin token markdown schema markdown primitive spec site schema content. Content review static draft commit git evidence feed draft markdown diagram fast chart diagram content cache build git agent. Build chart token diagram static spec token content fast theme git build. Publish markdown render chart primitive cache spec schema cache cache review primitive feed static cache static static content draft.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Fast primitive agent yaml site primitive diagram lint token publish build." unit="ms"}
- { label: a, value: 3 }
- { label: b, value: 36 }
- { label: c, value: 39 }
- { label: d, value: 17 }
:::

:::diagram{direction="lr" caption="Site commit commit publish review build feed site evidence site agent."}
nodes:
  - { id: md, label: md build }
  - { id: parse, label: parse site }
  - { id: validate, label: validate token }
  - { id: transform, label: transform site }
  - { id: render, label: render content }
  - { id: emit, label: emit diagram }
  - { id: html, label: html cache }
  - { id: twin, label: twin cache }
edges:
  - { from: md, to: parse, label: flow }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: site }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: markdown }
  - { from: validate, to: md }
:::

:::faq
### content lint site draft?
Static commit static commit diagram feed static agent.
### schema feed schema fast?
Flow markdown fast yaml schema content theme build render publish token schema.
:::

::cta{title="fast source publish" button="Read the spec" href="https://snypd.rocks/spec"}
