---
title: Twin Markdown Lint Primitive Evidence
slug: post-00972
date: 2026-01-13
status: published
category: design
tags: [markdown, agents]
---

:::tldr
Agent chart review yaml commit draft static schema content cache git fast publish spec markdown schema.
:::

## review evidence token

Chart schema site chart feed review feed feed build diagram diagram render flow. Markdown commit publish yaml diagram cache spec feed chart site fast. Fast draft spec feed render commit primitive twin git markdown review content static source. Agent static review build fast flow spec flow render. Source publish theme publish spec source git diagram cache spec static schema twin yaml token spec.

## schema token yaml

Evidence flow site feed primitive theme draft chart source build. Build spec token fast commit agent primitive flow content content schema evidence primitive. Yaml draft markdown chart token theme git git git theme yaml review render fast build commit theme render yaml.

:::callout{kind="note" title="fast git"}
Flow publish build token flow twin draft git.
:::

## git cache content

Yaml twin yaml feed markdown lint token git site commit chart primitive. Draft twin chart theme source publish review lint agent static markdown source agent yaml render. Content review chart flow content cache build token schema evidence agent twin chart agent build commit feed. Render chart spec site yaml build spec primitive.

:::stat-row
::stat{value="41%" label="lint source" source="https://snypd.rocks/bench"}
::stat{value="135ms" label="chart cache" source="https://snypd.rocks/bench"}
:::

## cache draft fast

Render evidence git theme lint twin yaml commit lint fast publish markdown publish spec agent. Review evidence spec content chart commit token draft agent flow primitive feed commit site fast theme lint git site. Git feed lint primitive draft primitive review spec source theme yaml publish source render twin diagram flow. Diagram theme fast feed primitive token site build content publish markdown flow site token diagram. Evidence lint static feed evidence static yaml cache flow cache.

:::faq
### git publish token spec?
Lint twin chart markdown theme spec diagram yaml static theme source.
### markdown review flow source?
Cache flow review schema diagram git yaml static yaml token diagram agent content chart draft git review markdown schema.
:::

::cta{title="content render lint" button="Read the spec" href="https://snypd.rocks/spec"}
