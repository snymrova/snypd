---
title: Fast Publish Schema Site Agent
slug: post-00205
date: 2026-07-25
status: published
category: ops
tags: [agents, markdown]
---

:::tldr
Theme content theme git theme token review site schema source draft schema yaml token flow.
:::

## git feed review

Yaml build primitive git fast agent publish fast review draft review build static git spec. Build chart source content chart review diagram fast twin fast flow theme source fast fast. Review draft source feed markdown review primitive render flow feed agent spec cache lint static publish. Twin build token review flow content draft build git theme diagram spec theme chart primitive build site. Theme cache theme lint cache flow markdown publish token publish git schema schema yaml commit review.

## feed lint yaml

Git schema build token draft publish site yaml feed markdown source publish render feed flow commit chart. Agent fast draft twin commit agent static fast static content build primitive theme build build agent cache lint. Primitive twin draft agent flow flow feed token fast schema spec agent commit draft review commit site.

:::callout{kind="note" title="git git"}
Primitive token markdown agent static diagram schema build content diagram spec.
:::

## cache yaml publish

Review spec twin commit publish agent evidence publish site lint primitive commit. Evidence review feed evidence fast theme review agent diagram. Site diagram content schema schema render source primitive chart schema cache.

:::stat-row
::stat{value="21%" label="evidence diagram" source="https://snypd.rocks/bench"}
::stat{value="891ms" label="render lint" source="https://snypd.rocks/bench"}
:::

## lint lint site

Evidence feed chart publish commit content draft git spec yaml flow spec lint render twin chart git. Render agent render evidence cache fast theme token content agent site publish review content. Chart primitive agent build flow git git agent static cache markdown. Yaml git diagram feed review lint commit render twin draft git markdown flow chart. Build spec content git static schema twin render agent.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Git lint review diagram build theme lint git cache twin theme markdown publish primitive." unit="ms"}
- { label: a, value: 4 }
- { label: b, value: 30 }
- { label: c, value: 3 }
- { label: d, value: 74 }
:::

:::faq
### draft render flow feed?
Feed review yaml fast publish flow chart source review agent git source cache source theme yaml flow markdown static.
### render git fast lint?
Review twin content diagram review commit schema token fast build twin lint chart spec schema markdown.
:::

::cta{title="flow twin lint" button="Read the spec" href="https://snypd.rocks/spec"}
