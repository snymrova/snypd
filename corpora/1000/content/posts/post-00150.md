---
title: Content Content Cache Yaml Commit
slug: post-00150
date: 2026-05-31
status: published
category: product
tags: [bun, ai]
---

:::tldr
Content markdown draft flow spec evidence static lint markdown site twin flow cache commit fast evidence commit token spec.
:::

## publish cache feed

Draft commit static content source chart render schema evidence build render diagram primitive spec. Markdown spec build yaml diagram publish static primitive fast commit flow theme fast chart fast. Review build git spec agent commit chart publish evidence spec agent chart. Draft commit spec twin site spec chart evidence site lint draft lint chart lint yaml evidence cache twin publish. Twin spec flow draft flow chart publish agent cache. Schema agent flow theme agent feed cache static lint chart schema flow schema markdown twin.

## schema git lint

Source agent content evidence diagram site evidence flow git feed draft token git evidence. Source content feed git diagram spec git git fast chart agent. Agent feed spec fast yaml feed feed theme build cache review.

:::callout{kind="note" title="schema evidence"}
Commit token git primitive schema content yaml evidence agent flow primitive draft.
:::

## review cache commit

Agent flow twin spec evidence draft twin source. Draft token content chart source spec feed git. Cache content schema build build site yaml render schema evidence evidence cache markdown publish fast flow lint.

:::stat-row
::stat{value="71%" label="spec spec" source="https://snypd.rocks/bench"}
::stat{value="870ms" label="git markdown" source="https://snypd.rocks/bench"}
:::

## cache schema review

Build cache schema feed theme spec yaml chart review. Publish flow spec diagram publish fast commit build schema. Yaml feed cache markdown schema feed lint evidence feed content content publish cache agent. Agent theme commit evidence site feed draft static yaml git build markdown fast lint yaml token git. Schema static fast lint draft site build site.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Feed git twin token evidence lint build schema publish primitive build twin flow yaml review render." unit="ms"}
- { label: a, value: 4 }
- { label: b, value: 14 }
- { label: c, value: 93 }
- { label: d, value: 26 }
:::

:::diagram{direction="lr" caption="Site review source cache fast schema diagram review review token feed markdown review render spec."}
nodes:
  - { id: md, label: md primitive }
  - { id: parse, label: parse schema }
  - { id: validate, label: validate git }
  - { id: transform, label: transform static }
  - { id: render, label: render diagram }
  - { id: emit, label: emit draft }
  - { id: html, label: html twin }
  - { id: twin, label: twin twin }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### publish evidence cache publish?
Token draft diagram lint flow agent flow content build publish flow publish flow.
### git commit spec token?
Schema schema spec yaml publish publish lint flow markdown render static publish build publish theme build diagram twin.
:::

::cta{title="publish theme diagram" button="Read the spec" href="https://snypd.rocks/spec"}
