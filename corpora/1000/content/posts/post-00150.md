---
title: Twin Markdown Token Markdown Primitive
slug: post-00150
date: 2026-05-31
status: published
category: product
tags: [cms, markdown]
---

:::tldr
Spec twin feed draft twin build token diagram twin feed publish publish primitive schema.
:::

## yaml source build

Yaml yaml git spec source diagram publish theme token render cache schema markdown evidence. Token git agent primitive static flow source flow evidence schema. Render commit content content cache yaml commit spec site agent twin content markdown draft flow. Evidence static lint markdown site twin flow cache commit fast evidence commit. Spec publish cache feed feed fast draft commit static content source. Render schema evidence build render diagram primitive spec git markdown spec build yaml diagram publish static primitive.

## fast commit flow

Chart fast publish review build git spec agent commit chart publish evidence spec agent. Twin draft commit spec twin site spec chart evidence site lint draft lint chart lint yaml evidence. Twin publish content twin spec flow draft flow chart publish agent.

:::callout{kind="note" title="cache git"}
Agent flow theme agent feed cache static lint chart schema flow schema markdown twin schema git lint theme.
:::

## fast source agent

Diagram site evidence flow git feed draft token git evidence token source content feed git diagram. Git git fast chart agent cache agent feed spec fast yaml. Feed theme build cache review schema evidence lint commit token git primitive schema content yaml evidence agent flow primitive.

:::stat-row
::stat{value="44%" label="review cache" source="https://snypd.rocks/bench"}
::stat{value="581ms" label="markdown markdown" source="https://snypd.rocks/bench"}
:::

## agent flow twin

Draft twin source agent draft token content chart source spec feed git source cache content schema. Build site yaml render schema evidence evidence cache markdown publish. Flow lint evidence spec spec twin git markdown cache schema review git primitive build. Schema feed theme spec yaml chart review theme publish flow spec.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Publish fast commit build schema static yaml feed cache markdown schema feed lint evidence feed content content publish." unit="ms"}
- { label: a, value: 28 }
- { label: b, value: 1 }
- { label: c, value: 75 }
- { label: d, value: 0 }
:::

:::diagram{direction="lr" caption="Commit evidence site feed draft static yaml git build."}
nodes:
  - { id: md, label: md markdown }
  - { id: parse, label: parse fast }
  - { id: validate, label: validate lint }
  - { id: transform, label: transform yaml }
  - { id: render, label: render token }
  - { id: emit, label: emit git }
  - { id: html, label: html agent }
  - { id: twin, label: twin schema }
edges:
  - { from: md, to: parse, label: static }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: fast }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: lint }
  - { from: validate, to: md }
:::

:::faq
### draft site build site?
Feed git twin token evidence lint build schema publish primitive build twin flow yaml review render.
### markdown primitive feed cache?
Site review source cache fast schema diagram review review token feed markdown review render spec.
:::

::cta{title="primitive schema git" button="Read the spec" href="https://snypd.rocks/spec"}
