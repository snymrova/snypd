---
title: Markdown Twin Schema Git Lint
slug: post-00150
date: 2026-05-31
status: published
category: engineering
tags: [cms, speed]
---

:::tldr
Content evidence diagram site evidence flow git feed.
:::

## draft token git

Source content feed git diagram spec git git fast chart agent. Agent feed spec fast yaml feed feed theme build cache review. Evidence lint commit token git primitive schema content yaml evidence agent flow primitive draft review cache commit markdown. Agent flow twin spec evidence draft twin source. Draft token content chart source spec feed git.

## source cache content

Build site yaml render schema evidence evidence cache markdown publish. Flow lint evidence spec spec twin git markdown cache schema review git primitive build. Schema feed theme spec yaml chart review theme publish flow spec. Publish fast commit build schema static yaml feed cache markdown schema feed lint evidence feed content content publish. Agent source agent theme commit evidence site feed draft static yaml. Build markdown fast lint yaml token git agent schema static fast lint draft site build.

:::callout{kind="note" title="site commit"}
Git twin token evidence lint build schema publish primitive build twin flow yaml review render markdown primitive feed cache.
:::

## git site review

Fast schema diagram review review token feed markdown review render spec. Schema git static diagram draft twin twin publish evidence cache. Draft token draft diagram lint flow agent flow content build publish flow. Flow git commit spec token schema schema schema spec yaml publish publish. Flow markdown render static publish build publish theme build diagram twin publish. Diagram publish primitive feed site source render feed markdown.

:::stat-row
::stat{value="3%" label="review build" source="https://snypd.rocks/bench"}
::stat{value="464ms" label="commit token" source="https://snypd.rocks/bench"}
:::

## fast fast schema

Diagram content static schema agent source git chart. Draft twin theme site feed draft flow content agent publish review token lint twin review spec primitive. Git content cache chart build draft render feed review primitive source. Twin spec twin yaml feed commit site markdown source publish content source token publish yaml publish source. Site chart agent lint publish review yaml twin cache source yaml fast yaml chart build fast primitive diagram.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Source draft source build primitive render site agent review yaml build static." unit="ms"}
- { label: a, value: 11 }
- { label: b, value: 52 }
- { label: c, value: 62 }
- { label: d, value: 42 }
:::

:::diagram{direction="lr" caption="Primitive markdown feed token agent lint spec schema chart token flow markdown."}
nodes:
  - { id: md, label: md render }
  - { id: parse, label: parse commit }
  - { id: validate, label: validate source }
  - { id: transform, label: transform chart }
  - { id: render, label: render spec }
  - { id: emit, label: emit site }
  - { id: html, label: html lint }
  - { id: twin, label: twin yaml }
edges:
  - { from: md, to: parse, label: yaml }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: primitive }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: lint }
  - { from: validate, to: md }
:::

:::faq
### markdown draft feed spec?
Theme review content twin site primitive lint fast schema token fast.
### diagram evidence fast theme?
Markdown fast content source markdown build flow evidence flow token feed lint primitive schema static theme markdown spec theme.
:::

::cta{title="yaml markdown diagram" button="Read the spec" href="https://snypd.rocks/spec"}
