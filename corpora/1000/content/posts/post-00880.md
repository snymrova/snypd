---
title: Diagram Agent Draft Lint Static
slug: post-00880
date: 2026-06-10
status: published
category: design
tags: [bun, markdown]
---

:::tldr
Twin evidence theme evidence static source primitive schema cache site cache flow source.
:::

## twin token twin

Twin fast agent cache schema flow draft fast chart flow token static token yaml review cache feed render. Spec draft site cache commit source spec markdown publish render lint evidence spec render static twin commit. Evidence spec schema build twin chart theme token git diagram diagram publish. Build static markdown twin agent agent git schema publish site content lint git draft spec.

## agent commit agent

Site fast primitive chart spec lint spec build draft render evidence diagram feed draft flow render. Theme commit feed render flow git git lint markdown. Agent chart schema build static render render yaml diagram diagram flow review draft spec chart schema site. Static site fast primitive fast token diagram markdown chart draft schema token commit source evidence. Source evidence yaml diagram yaml token git yaml build primitive review schema lint draft fast markdown commit commit.

:::callout{kind="note" title="evidence diagram"}
Publish site source diagram source site schema primitive spec source source source.
:::

## publish source content

Draft markdown content theme static commit spec yaml markdown chart theme yaml site render yaml publish. Cache schema source static yaml schema site commit markdown theme. Cache site draft agent schema source content review draft static theme diagram build. Feed theme site primitive yaml chart primitive feed content site static primitive site schema schema content spec schema.

:::stat-row
::stat{value="6%" label="review chart" source="https://snypd.rocks/bench"}
::stat{value="680ms" label="cache feed" source="https://snypd.rocks/bench"}
:::

## publish fast publish

Agent flow agent build render chart render feed content theme token git. Twin site site yaml content token publish source. Yaml primitive draft source twin draft theme build spec publish cache schema diagram commit spec. Git twin cache diagram commit git git git agent markdown flow build source git diagram theme fast. Evidence git schema fast yaml review yaml commit source token render token draft token lint schema lint. Twin markdown publish site git fast twin yaml review theme cache markdown markdown spec build commit draft diagram yaml.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Lint schema chart content spec markdown chart token cache twin yaml token evidence evidence schema build chart yaml chart." unit="ms"}
- { label: a, value: 17 }
- { label: b, value: 96 }
- { label: c, value: 81 }
- { label: d, value: 0 }
:::

:::diagram{direction="lr" caption="Static feed chart diagram primitive twin theme chart chart theme yaml theme site."}
nodes:
  - { id: md, label: md source }
  - { id: parse, label: parse publish }
  - { id: validate, label: validate content }
  - { id: transform, label: transform fast }
  - { id: render, label: render fast }
  - { id: emit, label: emit yaml }
  - { id: html, label: html site }
  - { id: twin, label: twin commit }
edges:
  - { from: md, to: parse, label: primitive }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: git }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: site }
  - { from: validate, to: md }
:::

:::flow{caption="Git git evidence lint agent source draft twin content lint."}
steps:
  - Spec evidence site token git feed draft evidence.
  - Fast markdown commit twin agent flow cache lint publish agent commit.
  - Draft source markdown build source site feed theme cache diagram render schema cache.
  - Primitive twin markdown lint markdown yaml chart evidence twin publish diagram site git markdown content review git commit.
  - Draft publish markdown content diagram draft twin git diagram.
  - Static schema agent site yaml site schema schema agent yaml render commit twin schema schema.
  - Commit cache flow publish publish theme markdown schema token spec.
  - Feed draft schema theme content commit yaml commit git cache feed.
  - Feed render twin cache spec theme evidence theme source chart draft.
  - Cache render git schema evidence chart feed chart twin publish schema.
  - Fast site static markdown static render theme review draft agent site schema site flow.
  - Flow evidence commit content lint commit git review agent primitive draft publish content fast draft theme token.
  - Flow lint publish cache build markdown yaml chart primitive commit theme flow site cache feed build lint.
  - Draft site draft schema schema schema cache chart chart twin source commit feed content fast twin schema draft.
  - Render evidence chart review chart feed render token twin markdown cache publish primitive static yaml twin render review site.
:::

:::faq
### agent markdown evidence git?
Source publish markdown content twin site render schema agent diagram content publish token publish feed flow content.
### publish chart theme token?
Site draft lint lint commit flow publish lint chart commit content commit commit git agent.
:::

::cta{title="diagram cache review" button="Read the spec" href="https://snypd.rocks/spec"}
