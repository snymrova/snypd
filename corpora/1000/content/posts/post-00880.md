---
title: Site Theme Chart Lint Token
slug: post-00880
date: 2026-06-10
status: published
category: ops
tags: [ai, ai]
---

:::tldr
Token spec review yaml primitive schema twin publish source.
:::

## lint source theme

Chart evidence review publish agent token git git cache draft draft review agent site. Fast twin fast yaml fast lint git agent evidence cache token feed twin fast theme git render. Content cache build chart lint spec token theme source. Git chart chart publish source theme render source publish feed agent build markdown. Theme lint chart theme source theme yaml source. Primitive content theme lint twin publish review review yaml evidence lint site.

## feed git markdown

Review commit agent chart draft content source chart spec twin chart twin fast token agent schema. Publish feed markdown token markdown content spec diagram build draft yaml agent spec yaml lint primitive review. Source spec schema publish markdown schema lint chart token. Build diagram yaml draft token yaml evidence feed yaml.

:::callout{kind="note" title="diagram source"}
Fast cache theme chart chart feed source primitive review.
:::

## site static theme

Draft spec evidence publish agent content evidence source git flow publish spec git yaml git primitive schema diagram cache. Cache feed theme agent primitive theme static site evidence markdown content. Publish static fast agent schema git feed evidence flow diagram render publish lint chart git render site yaml. Commit diagram flow diagram markdown publish markdown git chart primitive. Review schema content cache spec commit token schema schema source theme build. Chart lint review spec build yaml static evidence draft fast twin theme agent cache git diagram cache.

:::stat-row
::stat{value="52%" label="source git" source="https://snypd.rocks/bench"}
::stat{value="354ms" label="spec schema" source="https://snypd.rocks/bench"}
:::

## feed publish markdown

Agent review content commit theme token commit chart publish spec source commit theme. Chart static spec yaml build token git theme publish twin git source. Render publish flow markdown draft diagram draft draft review token chart schema site diagram twin agent static primitive build. Fast schema twin schema agent flow theme draft cache draft cache draft markdown content evidence diagram git site.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Theme site primitive yaml publish review site content review markdown markdown draft draft site build twin git commit." unit="ms"}
- { label: a, value: 81 }
- { label: b, value: 55 }
- { label: c, value: 72 }
- { label: d, value: 93 }
:::

:::diagram{direction="lr" caption="Agent draft build draft agent token diagram agent evidence markdown render build flow evidence publish primitive draft theme publish."}
nodes:
  - { id: md, label: md review }
  - { id: parse, label: parse lint }
  - { id: validate, label: validate commit }
  - { id: transform, label: transform theme }
  - { id: render, label: render cache }
  - { id: emit, label: emit build }
  - { id: html, label: html source }
  - { id: twin, label: twin source }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Source fast feed build spec yaml review content."}
steps:
  - Agent primitive primitive fast commit build markdown theme content render twin markdown git chart schema fast.
  - Build draft flow theme commit source lint token content content lint primitive.
  - Lint primitive content publish chart primitive schema render agent theme.
  - Schema evidence fast diagram publish markdown lint agent diagram build content git spec feed lint site publish static diagram.
  - Twin build content primitive agent site commit spec site fast markdown spec source diagram publish render.
  - Build evidence token lint chart git markdown twin diagram site schema diagram schema fast commit.
  - Commit diagram content yaml primitive flow schema twin schema cache commit spec spec agent commit markdown commit agent source.
  - Site cache twin git review cache site agent primitive.
  - Markdown twin schema cache yaml content spec theme.
  - Spec git spec static source chart cache spec token.
  - Source agent yaml theme git build content cache fast token yaml lint.
  - Agent review git commit agent cache primitive site lint agent git markdown token source diagram chart lint theme cache.
  - Twin build markdown primitive chart spec evidence fast render twin git publish.
  - Token spec feed token token render source flow cache.
  - Site spec schema evidence site chart site primitive schema draft feed.
:::

:::faq
### commit lint twin twin?
Commit markdown twin commit publish static lint markdown review review theme static review agent render markdown source theme.
### render markdown draft twin?
Lint spec yaml agent chart token diagram commit agent flow fast token content twin review source.
:::

::cta{title="fast markdown static" button="Read the spec" href="https://snypd.rocks/spec"}
