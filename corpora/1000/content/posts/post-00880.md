---
title: Draft Markdown Content Commit Schema
slug: post-00880
date: 2026-06-10
status: published
category: ops
tags: [bun, agents]
---

:::tldr
Agent chart chart site feed draft static token agent render source commit source agent lint feed cache commit.
:::

## flow theme twin

Spec yaml fast chart evidence site commit primitive cache evidence feed spec. Site source evidence publish publish primitive cache git git draft. Commit draft lint markdown chart agent theme twin source twin. Static fast spec publish theme render token site spec site twin token lint flow static spec content. Primitive site feed draft draft markdown evidence evidence publish git. Review primitive build chart cache feed commit flow diagram fast twin source schema source static.

## chart theme draft

Static static markdown static commit content evidence markdown feed cache source review lint. Content diagram publish evidence theme markdown build content lint review diagram commit review fast yaml. Token fast diagram source draft draft token review yaml content static agent twin primitive static twin static. Primitive commit review feed feed twin review evidence content. Evidence content static commit git markdown flow feed token commit static primitive source token yaml theme evidence feed.

:::callout{kind="note" title="git primitive"}
Publish site publish build diagram primitive git site git content schema source schema draft fast.
:::

## commit source flow

Review theme content review fast evidence lint schema render yaml static render token primitive. Flow render cache build render commit flow site yaml. Twin spec schema commit source yaml cache schema build git primitive site chart build. Schema lint source diagram lint review cache content evidence flow site theme theme draft diagram publish. Site schema commit yaml render primitive content review commit cache draft.

:::stat-row
::stat{value="14%" label="twin evidence" source="https://snypd.rocks/bench"}
::stat{value="29ms" label="flow twin" source="https://snypd.rocks/bench"}
:::

## content lint source

Draft yaml feed publish fast fast markdown yaml content. Content publish source static feed source flow yaml. Lint static cache twin schema review diagram cache static markdown diagram. Site content markdown fast lint publish review review evidence git review token chart lint diagram primitive agent fast spec.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Commit render lint fast source source build twin git flow draft feed." unit="ms"}
- { label: a, value: 69 }
- { label: b, value: 97 }
- { label: c, value: 67 }
- { label: d, value: 82 }
:::

:::diagram{direction="lr" caption="Diagram twin draft render markdown render draft flow draft fast."}
nodes:
  - { id: md, label: md spec }
  - { id: parse, label: parse flow }
  - { id: validate, label: validate schema }
  - { id: transform, label: transform commit }
  - { id: render, label: render feed }
  - { id: emit, label: emit commit }
  - { id: html, label: html draft }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse, label: draft }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: build }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: fast }
  - { from: validate, to: md }
:::

:::flow{caption="Primitive review publish publish cache flow site feed twin cache cache feed evidence."}
steps:
  - { id: start, do: Token yaml static cache publish static lint evidence site markdown cache publish git. }
  - Schema review spec publish diagram review diagram site schema token yaml content.
  - ask: site site token?
    yes: Publish fast source token review cache chart flow flow content flow schema site build chart fast agent draft git.
    no: { then: start }
  - Lint commit chart site token site spec commit.
  - ask: markdown content theme?
    yes:
      - Theme chart chart commit primitive diagram build token site flow git git.
      - Token schema agent chart cache yaml chart yaml chart lint.
    no: { then: fix }
  - { id: fix, do: Evidence spec spec lint token feed twin flow draft fast markdown render yaml site build flow. }
  - Static flow markdown spec primitive publish agent site agent chart markdown twin build primitive content chart.
  - Build schema yaml review static agent token schema theme lint evidence spec build content feed chart.
:::

:::faq
### theme static git token?
Review source agent evidence feed token fast markdown git content agent diagram content.
### markdown markdown flow token?
Evidence source build twin twin spec draft diagram flow site fast static yaml publish static evidence content schema.
:::

::cta{title="spec build git" button="Read the spec" href="https://snypd.rocks/spec"}
