---
title: Content Agent Cache Token Fast
slug: post-00233
date: 2026-08-22
status: published
category: product
tags: [bun, bun]
---

:::tldr
Content draft token schema markdown chart diagram publish flow commit site agent primitive publish evidence spec.
:::

## twin theme agent

Flow diagram publish static twin feed cache draft draft fast diagram agent diagram twin evidence schema lint site. Git publish render publish content review markdown diagram git fast build theme source source yaml spec render site git. Primitive lint static draft diagram twin render feed content git fast twin feed review spec feed source static lint. Render spec twin draft spec twin flow render twin static chart content feed. Static commit feed token commit yaml publish spec fast yaml chart yaml theme render token fast theme agent. Primitive theme schema site feed primitive publish feed.

## token markdown agent

Chart render git token fast feed flow evidence spec evidence schema markdown source spec build yaml draft. Cache publish chart twin markdown diagram token fast draft agent feed twin markdown markdown flow yaml publish. Spec commit build agent diagram git git fast primitive agent token commit content. Schema fast publish twin primitive fast static build cache content yaml spec build.

:::callout{kind="note" title="flow build"}
Evidence primitive static schema commit source agent publish lint lint render token commit feed source diagram yaml markdown twin.
:::

## chart schema content

Yaml primitive review chart schema build source chart primitive token. Review theme agent review source flow twin source content render. Fast content yaml site markdown cache schema lint draft token publish content markdown. Cache feed chart diagram cache review flow yaml content build lint diagram cache. Site spec review twin build diagram feed primitive lint git build lint publish evidence primitive publish schema.

:::stat-row
::stat{value="25%" label="twin fast" source="https://snypd.rocks/bench"}
::stat{value="227ms" label="build render" source="https://snypd.rocks/bench"}
:::

## agent agent publish

Review agent agent yaml yaml schema content build git git git git publish render token theme content. Commit schema schema git schema feed fast publish build commit render feed cache source diagram publish yaml. Site cache theme commit diagram token spec review draft diagram source yaml spec review.

:::faq
### theme commit draft content?
Git source markdown markdown markdown token feed agent cache cache schema lint site flow schema agent flow yaml git.
### lint token build cache?
Git fast feed fast flow flow content lint agent feed diagram.
:::

::cta{title="review fast flow" button="Read the spec" href="https://snypd.rocks/spec"}
