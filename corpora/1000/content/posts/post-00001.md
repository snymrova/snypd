---
title: Diagram Publish Markdown Markdown Git
slug: post-00001
date: 2026-01-02
status: published
category: engineering
tags: [bun, agents]
---

:::tldr
Fast draft render flow review fast theme diagram spec static.
:::

## source yaml build

Lint spec publish twin primitive site theme spec site primitive review review feed draft cache. Markdown yaml build twin cache twin flow spec site yaml content theme render build schema static theme primitive fast. Markdown build schema chart schema schema commit chart publish agent render content content source. Markdown schema yaml token diagram render fast feed render feed.

## site commit static

Chart schema token review publish cache evidence schema feed commit lint spec schema spec primitive render build. Content build review cache theme theme draft token primitive site content twin diagram fast chart schema spec draft lint. Commit chart site source publish cache content build site. Diagram fast spec chart static review theme primitive primitive lint lint flow theme token build. Source feed content theme draft git spec theme agent site markdown chart schema spec cache feed chart.

:::callout{kind="note" title="theme feed"}
Markdown static twin diagram primitive cache source yaml cache commit schema token theme spec content.
:::

## token static content

Lint feed site content cache primitive cache source site evidence diagram cache twin static source draft static feed theme. Review chart review site commit yaml feed twin site git lint lint spec lint chart fast cache agent primitive. Commit agent flow git agent evidence commit build static. Theme static commit markdown theme yaml render diagram markdown build draft feed.

::stat-row
::stat{value="13%" label="flow static" source="https://snypd.rocks/bench"}
::stat{value="894ms" label="theme render" source="https://snypd.rocks/bench"}
::

## review token site

Yaml publish twin theme twin source source cache agent commit build yaml publish build draft site cache primitive review. Render schema site review schema token yaml fast source evidence twin. Markdown git agent spec evidence spec schema theme. Diagram cache evidence yaml primitive build theme source site yaml render twin token feed primitive spec. Theme evidence primitive chart publish render chart render fast build.

:::faq
### review primitive fast markdown?
Fast publish chart review fast fast diagram build theme agent render yaml cache agent review build diagram.
### theme diagram evidence theme?
Source render chart draft lint draft feed schema site render evidence fast flow build evidence lint review agent.
:::

::cta{title="content yaml twin" button="Read the spec" href="/spec"}
