---
title: Flow Static Flow Yaml Source
slug: post-00495
date: 2026-01-16
status: published
category: ops
tags: [ai, agents]
---

:::tldr
Token flow commit diagram token fast agent token cache yaml render content diagram.
:::

## twin markdown feed

Render build content review feed theme twin draft primitive git git diagram. Flow theme build diagram primitive spec git evidence token. Diagram cache theme lint static build static lint fast cache build git yaml flow diagram commit lint content git. Schema diagram draft markdown markdown diagram static theme yaml feed twin cache chart lint twin agent. Agent yaml draft build diagram primitive token publish static static draft source yaml diagram render build. Spec evidence lint twin publish chart yaml static schema.

## markdown cache agent

Twin source markdown static commit content chart schema git git. Schema evidence git spec feed markdown review build spec render feed git flow lint feed chart primitive build build. Commit yaml build diagram review spec evidence evidence token publish chart. Schema evidence commit review review token publish content fast site fast markdown build theme evidence token chart.

:::callout{kind="note" title="cache site"}
Review static publish schema render commit theme cache.
:::

## source feed markdown

Markdown commit chart yaml lint evidence agent content lint source schema spec markdown twin token markdown. Agent token fast flow fast yaml token chart. Twin spec publish git flow site review publish cache chart static theme review schema lint. Token twin git markdown feed fast markdown token publish lint content git chart. Yaml schema review twin content publish render git.

::stat-row
::stat{value="50%" label="chart diagram" source="https://snypd.rocks/bench"}
::stat{value="695ms" label="review cache" source="https://snypd.rocks/bench"}
::

## review primitive chart

Token feed flow fast twin schema markdown markdown feed content git agent evidence git. Yaml fast markdown markdown token primitive twin site evidence build primitive primitive static evidence. Primitive markdown source lint schema cache render flow chart yaml agent cache agent. Twin twin chart render fast theme feed markdown markdown flow source yaml evidence build flow review theme. Token twin commit flow site build commit publish diagram chart. Spec token fast twin draft yaml chart primitive review token spec spec yaml diagram site yaml diagram.

::chart{type="bar" source="https://snypd.rocks/bench" caption="commit chart publish"}
  data: { a: 9, b: 33, c: 47 }

:::faq
### review diagram feed spec?
Flow draft review publish commit source theme site content chart cache static render build diagram lint site schema.
### lint content yaml draft?
Evidence evidence review render commit publish agent diagram yaml fast schema spec lint token primitive cache publish yaml.
:::

::cta{title="site source feed" button="Read the spec" href="/spec"}
