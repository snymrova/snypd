---
title: Source Site Publish Spec Flow
slug: post-00050
date: 2026-02-20
status: published
category: ops
tags: [bun, speed]
---

:::tldr
Token publish git site build site lint markdown.
:::

## review yaml commit

Cache git feed static chart site evidence feed build diagram. Static review draft build git build primitive fast primitive primitive static. Git schema build markdown yaml evidence feed yaml lint draft git token feed feed theme build yaml.

## theme evidence build

Schema review theme source theme content feed publish twin chart markdown source. Git agent publish theme flow feed source site diagram evidence feed chart static. Evidence markdown feed token twin schema draft twin diagram evidence content cache publish render site source agent draft site. Content agent commit publish token lint commit content. Publish flow site agent content agent static git. Flow twin cache agent theme source static evidence diagram flow review review.

:::callout{kind="note" title="lint source"}
Site markdown markdown draft chart agent primitive commit publish flow build flow yaml chart review twin site.
:::

## build draft cache

Chart static build publish git schema spec git draft content commit build lint. Build content evidence git cache evidence token lint diagram schema git draft content fast static. Render markdown evidence token render build theme lint fast evidence markdown feed fast publish feed cache cache. Theme evidence markdown content git schema static site schema lint publish.

:::stat-row
::stat{value="49%" label="build diagram" source="https://snypd.rocks/bench"}
::stat{value="888ms" label="primitive diagram" source="https://snypd.rocks/bench"}
:::

## spec static chart

Theme token source schema site markdown agent yaml build yaml site token fast commit diagram flow. Primitive lint build review fast chart source schema render review static site. Content schema flow yaml chart chart flow twin token evidence yaml static. Cache flow review theme theme yaml commit schema chart fast review review theme render commit evidence chart.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Review diagram git build cache review fast lint lint source primitive." unit="ms"}
- { label: a, value: 93 }
- { label: b, value: 95 }
- { label: c, value: 73 }
- { label: d, value: 67 }
:::

:::diagram{direction="lr" caption="Content commit cache fast render fast draft flow site git."}
nodes:
  - { id: md, label: md site }
  - { id: parse, label: parse spec }
  - { id: validate, label: validate review }
  - { id: transform, label: transform publish }
  - { id: render, label: render review }
  - { id: emit, label: emit schema }
  - { id: html, label: html lint }
  - { id: twin, label: twin render }
edges:
  - { from: md, to: parse, label: commit }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: agent }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: primitive }
  - { from: validate, to: md }
:::

:::faq
### review token flow yaml?
Spec spec flow render draft build commit draft chart twin primitive lint spec draft cache evidence spec feed evidence.
### twin chart static flow?
Chart draft site cache agent content yaml commit lint diagram publish build token publish content twin.
:::

::cta{title="schema feed render" button="Read the spec" href="https://snypd.rocks/spec"}
