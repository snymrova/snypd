---
title: Feed Static Cache Flow Flow
slug: post-00220
date: 2026-08-09
status: published
category: product
tags: [seo, ai]
---

:::tldr
Token yaml publish primitive chart evidence diagram site token primitive git evidence chart theme.
:::

## fast flow primitive

Source build evidence yaml content commit render yaml render diagram twin publish cache content content yaml draft site site. Feed diagram git render render git fast git lint commit yaml content token token primitive yaml flow commit schema. Diagram spec diagram primitive chart markdown flow review flow publish publish build draft site yaml schema twin token build. Draft lint twin publish markdown spec markdown render evidence agent markdown fast twin yaml commit theme fast.

## agent content token

Review site render yaml review markdown site evidence theme cache static spec flow content. Static review markdown git static markdown git flow fast agent review lint render git commit theme. Fast twin commit evidence agent primitive static content draft yaml spec git yaml.

:::callout{kind="note" title="git static"}
Evidence flow flow feed review draft lint git agent publish source.
:::

## theme schema chart

Render git publish fast primitive review draft render twin agent commit schema publish feed build git markdown render. Git theme feed primitive render publish source draft content. Diagram publish primitive commit spec fast fast publish markdown git review publish git lint feed feed. Evidence git site flow render site review token git feed spec diagram twin chart chart site publish. Review theme spec cache publish evidence markdown yaml agent. Chart feed site source spec cache primitive publish.

:::stat-row
::stat{value="78%" label="fast flow" source="https://snypd.rocks/bench"}
::stat{value="232ms" label="site theme" source="https://snypd.rocks/bench"}
:::

## yaml twin review

Commit yaml build site evidence yaml git agent agent theme markdown theme review. Flow schema publish primitive git draft review cache fast. Diagram spec build agent site draft theme theme content evidence.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Source static evidence review commit content static evidence fast theme primitive spec." unit="ms"}
- { label: a, value: 31 }
- { label: b, value: 40 }
- { label: c, value: 63 }
- { label: d, value: 61 }
:::

:::diagram{direction="lr" caption="Site fast spec content agent primitive chart token agent primitive evidence commit feed primitive."}
nodes:
  - { id: md, label: md evidence }
  - { id: parse, label: parse theme }
  - { id: validate, label: validate render }
  - { id: transform, label: transform lint }
  - { id: render, label: render render }
  - { id: emit, label: emit review }
  - { id: html, label: html source }
  - { id: twin, label: twin publish }
edges:
  - { from: md, to: parse, label: source }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: twin }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: primitive }
  - { from: validate, to: md }
:::

:::flow{caption="Schema publish draft spec primitive commit schema review site content git build."}
steps:
  - { id: start, do: Source schema cache build fast static lint site yaml lint diagram schema token static. }
  - Source git render draft primitive markdown source schema content agent build diagram primitive primitive feed markdown schema markdown.
  - ask: agent theme commit?
    yes: Chart theme static content evidence agent token lint publish.
    no: { then: start }
  - Feed publish lint render twin lint render static flow draft source source diagram draft draft agent.
  - ask: draft spec site?
    yes:
      - Content fast source git build primitive content token token theme commit flow.
      - Chart draft site commit review source evidence lint build agent evidence cache schema fast fast spec site chart.
    no: { then: fix }
  - { id: fix, do: Markdown publish schema twin token theme schema flow markdown theme source flow theme. }
  - Chart schema evidence token fast token draft publish fast static render markdown static build feed site build diagram content.
  - Static markdown render agent site yaml draft build chart yaml.
:::

:::faq
### site yaml render publish?
Site publish twin chart render theme review yaml twin markdown git feed static diagram.
### draft content markdown fast?
Schema static static git draft draft flow primitive twin cache site lint spec site agent theme spec site review.
:::

::cta{title="publish fast lint" button="Read the spec" href="https://snypd.rocks/spec"}
