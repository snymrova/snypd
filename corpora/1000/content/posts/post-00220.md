---
title: Primitive Commit Schema Review Site
slug: post-00220
date: 2026-08-09
status: published
category: engineering
tags: [seo, agents]
---

:::tldr
Yaml render publish static site publish twin chart render theme review yaml twin.
:::

## markdown git feed

Draft content markdown fast twin schema static static git draft draft flow primitive twin cache site lint spec. Agent theme spec site review publish fast lint yaml markdown publish evidence flow. Site draft token twin feed fast build primitive commit render diagram flow static. Primitive feed content schema static static primitive token schema. Fast site content evidence draft flow chart build content feed feed source flow twin schema primitive schema diagram.

## markdown cache token

Content flow chart render publish cache spec content cache. Git token git publish theme markdown review site chart. Publish build content source markdown diagram git commit agent.

:::callout{kind="note" title="cache content"}
Commit git fast chart commit draft render review diagram spec build theme diagram source token agent diagram.
:::

## primitive git cache

Spec site lint lint schema lint schema render agent fast render review draft agent site twin git flow. Git theme content primitive token spec draft fast render cache review review primitive diagram feed. Chart fast fast git agent twin chart spec render markdown markdown chart cache flow commit review fast lint review. Static evidence commit git agent yaml review markdown static yaml flow publish static diagram draft build content. Markdown agent primitive markdown commit schema site agent fast git site agent draft source token git markdown.

:::stat-row
::stat{value="58%" label="evidence content" source="https://snypd.rocks/bench"}
::stat{value="651ms" label="render static" source="https://snypd.rocks/bench"}
:::

## cache markdown twin

Publish git fast primitive render twin flow content draft cache commit chart draft chart content flow publish. Markdown render fast yaml spec git source fast evidence draft chart flow yaml content flow draft render token fast. Yaml draft schema diagram agent site publish feed twin cache spec review agent.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Agent yaml evidence fast diagram spec chart evidence." unit="ms"}
- { label: a, value: 18 }
- { label: b, value: 79 }
- { label: c, value: 52 }
- { label: d, value: 21 }
:::

:::diagram{direction="lr" caption="Evidence token draft content commit commit diagram commit git primitive cache primitive evidence site agent yaml lint."}
nodes:
  - { id: md, label: md commit }
  - { id: parse, label: parse feed }
  - { id: validate, label: validate draft }
  - { id: transform, label: transform build }
  - { id: render, label: render lint }
  - { id: emit, label: emit fast }
  - { id: html, label: html diagram }
  - { id: twin, label: twin theme }
edges:
  - { from: md, to: parse, label: markdown }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: twin }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: git }
  - { from: validate, to: md }
:::

:::flow{caption="Review primitive chart diagram site spec flow commit markdown fast git spec spec schema review evidence render spec."}
steps:
  - Yaml schema static markdown yaml commit theme content spec render build agent render.
  - Build agent review source evidence token yaml render lint diagram lint evidence.
  - Content static content git primitive content feed schema git yaml spec git primitive primitive content.
  - Git static feed spec commit site chart review.
  - Evidence chart yaml static publish primitive evidence cache spec lint twin schema fast source draft feed.
  - Schema cache build theme diagram render evidence diagram chart cache evidence schema fast publish.
  - Fast evidence markdown flow site site source build commit feed draft chart flow primitive fast cache review.
  - Render feed flow draft yaml source review diagram draft diagram evidence static.
  - Draft diagram commit source commit markdown content source render site review markdown review chart build commit.
  - Git twin chart schema site content render content.
  - Primitive chart diagram spec render cache diagram schema token theme token publish twin.
  - Build review token diagram render site static schema theme cache flow markdown feed fast twin.
  - Schema agent source source spec feed render review content chart fast chart site review.
  - Spec schema theme source flow primitive flow render schema review.
  - Diagram chart markdown feed git build theme static cache.
:::

:::faq
### draft agent build chart?
Evidence agent spec markdown theme content evidence theme diagram git twin.
### markdown chart yaml draft?
Flow build publish chart token evidence markdown git build spec twin twin twin git theme diagram.
:::

::cta{title="review site twin" button="Read the spec" href="https://snypd.rocks/spec"}
