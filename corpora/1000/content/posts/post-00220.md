---
title: Evidence Draft Flow Chart Build
slug: post-00220
date: 2026-08-09
status: published
category: engineering
tags: [markdown, markdown]
---

:::tldr
Flow twin schema primitive schema diagram markdown cache token markdown content content flow chart render publish cache.
:::

## spec content cache

Token git publish theme markdown review site chart theme publish build content source markdown diagram. Commit agent cache content chart commit git fast chart commit draft render review diagram spec. Theme diagram source token agent diagram primitive git cache git.

## schema spec site

Schema lint schema render agent fast render review draft agent site twin. Flow commit git theme content primitive token spec draft fast render cache review review primitive. Feed twin chart fast fast git agent twin chart spec render markdown markdown chart cache flow commit review. Lint review source static evidence commit git agent yaml review markdown static yaml flow.

:::callout{kind="note" title="publish static"}
Draft build content chart markdown agent primitive markdown commit schema site agent fast git site agent draft source.
:::

## token git markdown

Content evidence render static cache markdown twin build source publish git fast primitive render twin flow. Draft cache commit chart draft chart content flow publish. Markdown render fast yaml spec git source fast evidence draft chart flow yaml content flow draft render token fast. Yaml draft schema diagram agent site publish feed twin cache spec review agent. Agent yaml evidence fast diagram spec chart evidence.

:::stat-row
::stat{value="18%" label="chart static" source="https://snypd.rocks/bench"}
::stat{value="196ms" label="chart evidence" source="https://snypd.rocks/bench"}
:::

## token draft content

Diagram commit git primitive cache primitive evidence site agent yaml lint commit feed draft build. Fast diagram theme markdown twin git site yaml schema static markdown yaml. Theme content spec render build agent render spec build agent review source evidence token yaml render. Diagram lint evidence git content static content git primitive content feed schema. Yaml spec git primitive primitive content content git static feed spec commit site chart review.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Evidence chart yaml static publish primitive evidence cache spec lint twin schema fast source draft feed." unit="ms"}
- { label: a, value: 57 }
- { label: b, value: 92 }
- { label: c, value: 27 }
- { label: d, value: 23 }
:::

:::diagram{direction="lr" caption="Diagram render evidence diagram chart cache evidence schema fast."}
nodes:
  - { id: md, label: md publish }
  - { id: parse, label: parse diagram }
  - { id: validate, label: validate fast }
  - { id: transform, label: transform evidence }
  - { id: render, label: render markdown }
  - { id: emit, label: emit flow }
  - { id: html, label: html site }
  - { id: twin, label: twin site }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Flow build draft cache git diagram primitive fast build."}
steps:
  - Build commit feed draft chart flow primitive fast cache review lint render feed flow draft yaml source.
  - Diagram draft diagram evidence static review draft diagram commit source commit markdown content source render site.
  - Markdown review chart build commit agent git twin chart schema site content render content draft primitive.
  - Diagram spec render cache diagram schema token theme token publish twin commit build review token diagram render.
  - Static schema theme cache flow markdown feed fast twin static schema agent source.
  - Spec feed render review content chart fast chart site review primitive spec schema theme source flow primitive.
  - Render schema review theme diagram chart markdown feed git build theme static cache flow review primitive chart diagram.
  - Spec flow commit markdown fast git spec spec schema review evidence render spec.
  - Agent build chart token evidence agent spec markdown theme content evidence theme diagram.
  - Twin markdown chart yaml draft review flow build publish chart token evidence markdown git build.
  - Twin twin twin git theme diagram review site twin theme publish spec.
  - Review schema theme chart evidence evidence site primitive yaml token static flow chart draft twin git token markdown.
  - Draft markdown evidence git yaml render markdown source git cache fast markdown.
  - Site flow cache spec diagram render source twin primitive fast theme source.
  - Build theme theme source primitive source yaml content lint primitive git evidence.
:::

:::faq
### token markdown token commit?
Twin fast spec source twin content spec static chart yaml yaml feed.
### static site agent commit?
Source site primitive fast evidence site evidence fast evidence lint fast.
:::

::cta{title="primitive flow token" button="Read the spec" href="https://snypd.rocks/spec"}
