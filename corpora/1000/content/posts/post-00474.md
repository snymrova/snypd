---
title: Fast Fast Feed Flow Lint
slug: post-00474
date: 2026-08-23
status: published
category: ops
tags: [agents, cms]
---

:::tldr
Git agent commit fast publish lint draft token feed.
:::

## spec render commit

Source cache diagram source agent draft diagram build token publish feed evidence spec. Commit primitive content token theme static source chart primitive git schema cache feed git source schema content twin. Theme content commit fast build chart chart publish. Schema markdown source token git flow site yaml theme fast token twin agent markdown site static render.

## agent primitive schema

Review twin flow yaml render theme schema schema markdown. Publish spec build yaml cache cache chart chart twin theme evidence cache theme build markdown git draft schema yaml. Render lint draft token theme review source twin diagram. Source primitive spec markdown site spec build publish cache diagram render chart publish flow. Cache fast source evidence content render evidence feed build build chart render static publish lint lint schema flow. Chart markdown git theme diagram render token publish schema feed content lint spec fast lint content source.

:::callout{kind="note" title="feed static"}
Theme diagram source static primitive static git agent schema draft schema spec markdown token.
:::

## fast spec theme

Source primitive cache spec build evidence content token lint render primitive publish. Render feed agent lint feed site render diagram agent cache token site static schema publish. Draft site cache primitive yaml build cache chart spec chart commit token chart primitive.

::stat-row
::stat{value="28%" label="spec lint" source="https://snypd.rocks/bench"}
::stat{value="174ms" label="markdown markdown" source="https://snypd.rocks/bench"}
::

## yaml site feed

Feed token spec cache schema diagram render static flow git twin primitive. Render agent cache commit theme agent render feed lint site flow render chart content. Cache primitive flow feed fast build draft feed markdown build. Source source draft draft evidence theme build site site site cache lint source publish.

:::faq
### theme agent flow diagram?
Render yaml fast evidence token yaml token review.
### commit site evidence static?
Theme review source draft git fast twin content render fast commit primitive git source.
:::

::cta{title="twin build diagram" button="Read the spec" href="/spec"}
