---
title: Commit Flow Token Build Token
slug: post-00027
date: 2026-01-28
status: published
category: design
tags: [speed, speed]
---

:::tldr
Chart commit yaml cache site site publish source.
:::

## diagram fast chart

Lint markdown cache flow theme yaml draft fast cache agent static markdown. Twin theme publish schema flow draft diagram primitive diagram lint agent markdown. Markdown commit git markdown draft schema yaml source git theme feed. Content build commit agent chart markdown evidence source schema build static evidence site flow primitive feed fast theme content. Theme render flow twin twin spec render site site review schema feed evidence commit yaml yaml. Chart diagram content markdown primitive static commit schema chart build lint review markdown cache twin.

## twin primitive build

Theme source content theme review fast render content schema twin markdown yaml review twin flow site chart build build. Review agent schema twin chart review evidence static yaml render. Schema lint yaml schema twin git agent site build spec diagram draft draft feed content markdown. Twin content git evidence primitive source render yaml site content.

:::callout{kind="note" title="theme review"}
Content git static primitive content source chart static source static lint diagram diagram theme.
:::

## primitive chart schema

Lint twin feed git diagram flow yaml source token build flow fast static theme review render publish lint token. Fast build cache yaml token fast yaml content theme fast. Render feed fast fast site token twin review lint lint fast lint markdown diagram site site. Markdown site token lint draft fast cache feed feed agent feed theme commit draft.

:::stat-row
::stat{value="9%" label="publish static" source="https://snypd.rocks/bench"}
::stat{value="438ms" label="feed spec" source="https://snypd.rocks/bench"}
:::

## agent publish build

Spec site site spec publish git git fast. Render theme theme commit cache feed feed markdown commit fast publish. Draft schema twin agent primitive agent draft render commit yaml. Markdown diagram source commit schema agent twin fast source cache site. Spec review source markdown commit source twin site flow git source. Content diagram render review chart schema static theme cache draft review spec build spec.

:::faq
### cache render render schema?
Fast agent draft content token agent lint chart markdown content.
### primitive review site spec?
Review fast site flow static cache static publish commit yaml git review draft flow git.
:::

::cta{title="cache content fast" button="Read the spec" href="https://snypd.rocks/spec"}
