---
title: Flow Twin Cache Feed Agent
slug: post-00017
date: 2026-01-18
status: published
category: ops
tags: [ai, markdown]
---

:::tldr
Git markdown fast review build feed markdown git publish.
:::

## fast lint publish

Content cache publish cache git diagram cache static twin site spec. Fast chart agent flow agent publish review yaml schema primitive content static token diagram. Yaml twin publish evidence agent build markdown commit spec git token content lint commit flow primitive source.

## twin diagram site

Token git lint git build content publish site commit commit schema markdown evidence feed source agent cache. Publish chart chart lint static twin markdown draft feed schema spec. Cache cache agent lint site spec chart content cache review feed render fast schema render lint primitive content. Commit yaml schema commit render chart site theme render token build primitive.

:::callout{kind="note" title="lint source"}
Schema fast publish token agent schema commit publish flow agent twin primitive twin lint static.
:::

## render schema yaml

Static theme spec lint content evidence spec schema token review spec build static. Content fast build flow review site site static git markdown draft theme primitive. Chart site markdown spec site static publish review theme source fast. Git twin chart spec primitive diagram chart git chart yaml twin primitive. Flow diagram review review build twin token static build git site yaml fast review.

::stat-row
::stat{value="25%" label="git draft" source="https://snypd.rocks/bench"}
::stat{value="2ms" label="source publish" source="https://snypd.rocks/bench"}
::

## render agent render

Static review agent lint yaml build token token lint cache render lint flow theme agent twin twin review. Diagram feed primitive feed lint git primitive cache render markdown render feed lint content theme. Fast lint commit diagram render static cache git. Markdown diagram spec schema yaml fast feed lint lint flow draft agent. Content fast spec commit feed schema content evidence site content evidence theme draft primitive. Review lint site diagram publish agent chart evidence source theme draft content render git chart theme lint static.

:::faq
### agent draft draft source?
Review static cache render diagram evidence review primitive primitive git fast token build primitive publish schema content twin draft.
### fast lint chart site?
Cache schema feed evidence schema review flow spec spec build publish commit flow site.
:::

::cta{title="token draft feed" button="Read the spec" href="/spec"}
