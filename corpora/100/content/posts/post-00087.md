---
title: Spec Source Fast Commit Build
slug: post-00087
date: 2026-03-29
status: published
category: engineering
tags: [agents, bun]
---

:::tldr
Lint content review fast flow publish agent diagram flow schema spec review build theme review site.
:::

## source theme content

Agent render render source site diagram agent static diagram token flow token. Render twin lint theme render diagram schema static yaml evidence draft review draft cache source. Agent build markdown yaml git source diagram render spec cache chart schema markdown git site commit build git. Markdown publish source evidence cache lint schema lint build agent evidence fast token agent commit draft token flow yaml.

## source primitive draft

Static draft fast schema git schema review commit source token token token schema static. Schema cache content twin flow source spec markdown draft lint site commit markdown diagram yaml git. Schema feed build publish render chart schema spec feed lint spec spec draft spec content source schema static. Draft agent fast chart markdown diagram commit cache primitive agent build git evidence spec cache. Markdown lint chart feed static theme flow lint yaml diagram spec schema token draft primitive.

:::callout{kind="note" title="review token"}
Content feed spec feed build static commit commit cache.
:::

## content review cache

Spec diagram fast static render markdown twin feed source render spec. Theme static primitive theme commit theme draft git primitive. Twin source git token content build spec yaml flow twin lint build review render chart. Primitive flow lint token content content agent diagram. Publish lint diagram primitive diagram commit token draft evidence theme. Publish content review token spec static flow agent publish token source primitive markdown.

:::stat-row
::stat{value="88%" label="twin theme" source="https://snypd.rocks/bench"}
::stat{value="441ms" label="git schema" source="https://snypd.rocks/bench"}
:::

## review review draft

Agent token site lint twin static markdown lint theme diagram lint draft yaml content flow theme. Evidence static commit git primitive draft commit chart schema markdown. Spec site yaml theme token primitive content spec token fast site spec static. Static primitive static git draft publish git diagram source diagram yaml yaml feed primitive. Twin twin commit draft primitive theme cache lint markdown build token source token schema git agent render markdown. Draft fast render chart token commit theme primitive content theme token markdown git.

:::faq
### build site source site?
Evidence fast git feed evidence commit chart yaml token markdown site git twin content commit build primitive primitive spec.
### twin agent site evidence?
Lint review review draft review review primitive cache site agent site render diagram.
:::

::cta{title="build feed primitive" button="Read the spec" href="https://snypd.rocks/spec"}
