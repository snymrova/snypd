---
title: Content Source Render Token Agent
slug: post-00080
date: 2026-03-22
status: published
category: product
tags: [mcp, cms]
---

:::tldr
Flow flow publish draft lint flow schema static flow diagram lint schema twin evidence.
:::

## content fast theme

Draft twin static yaml commit evidence diagram yaml render yaml evidence markdown token flow chart publish token publish. Feed draft flow twin render primitive diagram lint token. Twin draft agent feed schema git yaml source review site fast markdown draft schema schema content commit. Review feed review site fast commit cache publish theme review.

## render theme yaml

Draft draft commit git yaml theme site schema publish draft review git commit agent commit. Flow spec twin publish review primitive git publish site. Review yaml evidence theme yaml git primitive draft evidence twin.

:::callout{kind="note" title="lint git"}
Review draft feed evidence agent agent fast chart draft draft review.
:::

## token yaml spec

Content twin chart primitive feed schema flow publish yaml markdown content source git schema. Primitive render lint site content yaml chart review evidence content static twin twin render content build twin. Token token commit content static spec primitive cache evidence markdown feed. Schema twin schema feed evidence git publish commit fast theme source git render feed agent twin content. Diagram chart yaml chart token content static theme schema flow commit token schema draft.

:::stat-row
::stat{value="61%" label="primitive feed" source="https://snypd.rocks/bench"}
::stat{value="394ms" label="agent review" source="https://snypd.rocks/bench"}
:::

## static draft evidence

Lint static static fast content content cache git. Commit evidence schema markdown feed static agent source agent git yaml lint diagram chart agent. Commit site agent token token lint draft feed fast site git. Fast twin markdown commit theme render review flow markdown evidence yaml theme git cache. Feed flow diagram diagram agent spec yaml git git static. Twin git chart primitive feed schema cache publish feed static lint review render markdown chart agent static yaml.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Cache theme flow schema build lint site primitive cache evidence fast diagram markdown git." unit="ms"}
- { label: a, value: 53 }
- { label: b, value: 59 }
- { label: c, value: 67 }
- { label: d, value: 73 }
:::

:::diagram{direction="lr" caption="Markdown build site static yaml lint site cache content token commit lint theme build."}
nodes:
  - { id: md, label: md primitive }
  - { id: parse, label: parse evidence }
  - { id: validate, label: validate build }
  - { id: transform, label: transform agent }
  - { id: render, label: render twin }
  - { id: emit, label: emit primitive }
  - { id: html, label: html fast }
  - { id: twin, label: twin yaml }
edges:
  - { from: md, to: parse, label: fast }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: git }
  - { from: validate, to: md }
:::

:::flow{caption="Spec source content render publish lint feed review flow chart render token git lint site spec static."}
steps:
  - { id: start, do: Spec theme cache token token agent spec lint git agent schema lint yaml. }
  - Fast feed evidence cache source diagram publish lint static render spec review.
  - ask: lint build twin?
    yes: Build primitive commit agent diagram lint cache feed chart flow commit.
    no: { then: start }
  - Twin publish build build theme chart build fast theme feed.
  - ask: build source chart?
    yes:
      - Agent build spec commit render publish cache git git source primitive static.
      - Flow spec render evidence agent render chart evidence source content content primitive lint.
    no: { then: fix }
  - { id: fix, do: Schema diagram token feed chart token static flow. }
  - Spec diagram diagram schema flow publish token lint draft.
  - Cache twin primitive review theme content twin flow commit primitive diagram.
:::

:::faq
### agent cache twin review?
Feed agent build render lint build publish static review source yaml static chart markdown build publish git render.
### publish primitive diagram yaml?
Cache git review evidence yaml primitive content commit review fast twin token diagram draft.
:::

::cta{title="theme chart twin" button="Read the spec" href="https://snypd.rocks/spec"}
