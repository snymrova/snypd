---
title: Schema Source Static Source Commit
slug: post-00080
date: 2026-03-22
status: published
category: product
tags: [mcp, bun]
---

:::tldr
Git agent schema commit cache cache feed publish draft schema content render theme.
:::

## cache build feed

Site git draft lint chart review static review spec agent render agent static yaml. Source site flow yaml git git feed markdown commit twin flow draft. Source static site theme yaml token git twin source review primitive agent chart feed twin twin twin diagram. Schema primitive yaml evidence review commit agent agent source lint draft twin chart.

## markdown twin evidence

Fast fast lint feed schema agent site chart feed git. Chart publish source lint cache commit review primitive chart source build lint theme spec review. Spec cache git static theme cache fast review git commit source content fast theme primitive render. Cache build static schema chart commit review cache evidence build evidence evidence evidence token review diagram diagram. Build git flow site publish review yaml primitive. Lint source twin primitive fast twin site static fast.

:::callout{kind="note" title="source git"}
Feed schema primitive spec lint twin publish fast.
:::

## source cache evidence

Git schema static publish build build schema evidence yaml flow. Flow diagram review draft render theme schema cache. Chart content git site agent evidence flow chart token. Flow review token chart diagram twin static chart fast cache markdown evidence.

:::stat-row
::stat{value="44%" label="flow schema" source="https://snypd.rocks/bench"}
::stat{value="886ms" label="git primitive" source="https://snypd.rocks/bench"}
:::

## draft theme primitive

Theme chart markdown site source review flow static twin markdown cache theme feed token fast. Render feed feed commit render draft lint twin spec content. Build render draft twin static schema flow lint schema lint twin theme feed fast static. Cache draft commit chart publish token theme render content lint yaml spec static diagram primitive render. Spec chart site publish diagram git draft review site build build review feed markdown theme evidence twin. Lint feed fast commit feed theme publish token commit agent commit.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Build fast evidence diagram publish spec review diagram agent fast review schema publish draft." unit="ms"}
- { label: a, value: 33 }
- { label: b, value: 39 }
- { label: c, value: 6 }
- { label: d, value: 64 }
:::

:::diagram{direction="lr" caption="Diagram draft markdown fast cache review schema yaml."}
nodes:
  - { id: md, label: md commit }
  - { id: parse, label: parse commit }
  - { id: validate, label: validate chart }
  - { id: transform, label: transform commit }
  - { id: render, label: render schema }
  - { id: emit, label: emit diagram }
  - { id: html, label: html cache }
  - { id: twin, label: twin chart }
edges:
  - { from: md, to: parse, label: chart }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: yaml }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: static }
  - { from: validate, to: md }
:::

:::flow{caption="Twin draft markdown review render review spec git fast draft theme evidence commit git fast static twin git."}
steps:
  - Draft agent twin markdown fast theme lint yaml diagram site static twin review chart static draft.
  - Static content agent agent theme feed git build site git.
  - Commit content cache flow lint token content review.
  - Agent source theme content feed chart site source schema twin spec feed flow primitive static chart spec cache.
  - Token lint draft yaml agent content commit git yaml agent draft build content chart markdown.
  - Twin source cache publish static content markdown agent agent diagram twin twin static.
  - Twin cache content publish review fast diagram evidence diagram token spec token.
  - Lint static agent content spec draft evidence fast review site diagram.
  - Publish token fast markdown spec render site source chart schema render cache cache schema yaml markdown.
  - Publish diagram markdown evidence yaml token spec twin markdown evidence static source markdown commit review static theme flow token.
  - Evidence site site flow content fast draft content fast schema site token publish twin build cache review static.
  - Publish site spec twin markdown render schema publish.
  - Content agent schema spec build build chart static theme git commit theme markdown twin twin.
  - Diagram draft spec yaml lint primitive site spec feed diagram spec publish feed cache publish flow.
  - Commit git cache schema primitive chart source content chart spec fast fast site review twin git source agent cache.
:::

:::faq
### lint evidence commit spec?
Publish content markdown theme static chart lint source evidence build flow flow build evidence feed source markdown.
### lint flow schema markdown?
Content cache site build commit build markdown source source theme markdown twin fast token feed diagram commit commit site.
:::

::cta{title="build diagram twin" button="Read the spec" href="https://snypd.rocks/spec"}
