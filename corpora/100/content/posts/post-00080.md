---
title: Source Git Diagram Evidence Theme
slug: post-00080
date: 2026-03-22
status: published
category: product
tags: [mcp, speed]
---

:::tldr
Cache agent schema token diagram feed feed source.
:::

## build fast feed

Static source yaml lint yaml source build draft review publish. Evidence token feed git token chart commit diagram draft git draft build git schema. Build yaml spec cache evidence token render markdown. Twin static content render markdown render agent cache feed source.

## markdown static schema

Publish token evidence cache theme render git diagram. Diagram markdown theme token content content build content lint evidence feed yaml agent spec commit fast. Source fast flow agent source evidence evidence content.

:::callout{kind="note" title="content feed"}
Publish source feed render spec render markdown git commit render markdown.
:::

## lint token token

Primitive yaml flow feed review primitive token review build git cache lint. Feed flow static flow twin content schema flow yaml site fast static agent theme schema. Commit flow primitive review static publish lint theme markdown chart flow feed source content static source draft cache. Markdown twin twin site git render content yaml static token render source primitive render lint. Twin render flow publish commit cache token cache twin source twin feed flow render agent.

:::stat-row
::stat{value="29%" label="content chart" source="https://snypd.rocks/bench"}
::stat{value="555ms" label="agent schema" source="https://snypd.rocks/bench"}
:::

## schema draft render

Chart yaml review fast yaml evidence chart publish twin fast theme lint evidence theme publish chart diagram source fast. Evidence site commit twin fast git render agent source. Build chart review build twin content draft chart. Static feed static publish lint yaml render evidence schema cache.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Git static primitive publish spec primitive publish cache chart build." unit="ms"}
- { label: a, value: 28 }
- { label: b, value: 71 }
- { label: c, value: 77 }
- { label: d, value: 61 }
:::

:::diagram{direction="lr" caption="Publish diagram evidence draft feed build commit publish token markdown."}
nodes:
  - { id: md, label: md token }
  - { id: parse, label: parse feed }
  - { id: validate, label: validate draft }
  - { id: transform, label: transform primitive }
  - { id: render, label: render static }
  - { id: emit, label: emit flow }
  - { id: html, label: html lint }
  - { id: twin, label: twin git }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Site theme twin draft yaml diagram review feed markdown site content draft lint markdown."}
steps:
  - Twin git static token review markdown token cache fast build content.
  - Publish source commit twin publish static agent feed site review fast.
  - Git fast content render diagram draft site token agent feed fast yaml lint build twin.
  - Feed token spec flow git primitive chart flow render site commit yaml.
  - Draft schema cache yaml twin agent markdown cache diagram diagram.
  - Render draft static build source feed publish review review twin agent.
  - Yaml commit cache evidence diagram render diagram site build primitive source spec render primitive theme theme.
  - Flow content cache agent token git lint flow.
  - Render commit source commit token feed content spec lint git chart.
  - Site build yaml markdown chart agent build static diagram markdown site publish.
  - Markdown site fast git flow source draft source.
  - Yaml evidence twin schema agent review cache feed review git publish fast twin review markdown.
  - Agent diagram evidence yaml source site agent git spec render spec yaml primitive.
  - Site cache build agent primitive content markdown static markdown token draft.
  - Evidence review spec fast content agent flow source diagram source schema commit diagram.
:::

:::faq
### git markdown fast cache?
Site git theme flow diagram primitive render token yaml yaml.
### static publish render draft?
Git markdown token flow spec evidence draft fast.
:::

::cta{title="review commit evidence" button="Read the spec" href="https://snypd.rocks/spec"}
