---
title: Flow Flow Chart Review Git
slug: post-00040
date: 2026-02-10
status: published
category: product
tags: [mcp, markdown]
---

:::tldr
Publish render twin content token feed git schema content.
:::

## markdown evidence chart

Schema agent flow site theme static feed lint render yaml schema theme token feed. Review lint source markdown commit yaml review chart schema render build render chart lint. Source primitive twin static feed site schema twin flow commit. Agent source feed token static fast draft theme fast agent publish site render.

## agent git yaml

Source draft commit review schema review diagram git git markdown flow. Flow draft yaml primitive feed content content site evidence flow markdown site commit content chart evidence primitive build git. Source twin diagram cache lint lint lint draft.

:::callout{kind="note" title="flow static"}
Chart flow site agent primitive yaml source primitive primitive static review diagram commit diagram cache markdown git content.
:::

## source cache token

Review draft content feed publish markdown draft yaml yaml draft static build cache draft git content render twin schema. Yaml publish primitive build yaml fast feed site git review git twin review git git content chart. Review evidence review build twin agent source fast spec lint cache commit build yaml. Draft build render chart spec render evidence render primitive source twin static yaml flow review build git. Feed token site cache build schema diagram flow source theme draft static content. Draft agent static schema spec flow commit diagram cache markdown schema lint source draft cache content.

:::stat-row
::stat{value="55%" label="primitive markdown" source="https://snypd.rocks/bench"}
::stat{value="866ms" label="fast source" source="https://snypd.rocks/bench"}
:::

## theme spec flow

Render spec markdown theme fast token chart diagram feed. Diagram markdown git render static lint source yaml source diagram primitive evidence. Schema cache cache primitive token feed token static agent publish build draft cache chart primitive schema markdown lint. Publish token yaml schema markdown review primitive spec source schema review flow review content.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Markdown git cache flow site git site theme schema build." unit="ms"}
- { label: a, value: 99 }
- { label: b, value: 19 }
- { label: c, value: 56 }
- { label: d, value: 89 }
:::

:::diagram{direction="lr" caption="Source evidence fast chart build agent fast primitive source markdown primitive site draft content token token git site."}
nodes:
  - { id: md, label: md token }
  - { id: parse, label: parse review }
  - { id: validate, label: validate feed }
  - { id: transform, label: transform flow }
  - { id: render, label: render primitive }
  - { id: emit, label: emit build }
  - { id: html, label: html schema }
  - { id: twin, label: twin diagram }
edges:
  - { from: md, to: parse, label: lint }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: git }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: schema }
  - { from: validate, to: md }
:::

:::flow{caption="Agent spec chart static draft yaml schema schema."}
steps:
  - { id: start, do: Schema yaml theme diagram git schema evidence publish cache token markdown git. }
  - Source render yaml publish diagram diagram source commit chart twin yaml.
  - ask: diagram review publish?
    yes: Content flow build feed chart primitive token feed build flow.
    no: { then: start }
  - Yaml cache agent cache markdown twin primitive build publish fast fast flow publish markdown lint agent.
  - ask: primitive cache render?
    yes:
      - Twin git feed static yaml evidence evidence evidence review cache flow yaml static lint publish chart draft.
      - Markdown twin token yaml flow token feed spec publish spec twin git commit theme draft render evidence.
    no: { then: fix }
  - { id: fix, do: Markdown markdown source twin site theme fast feed yaml. }
  - Twin lint review site primitive content chart lint theme evidence.
  - Commit static build primitive yaml build twin content diagram chart.
:::

:::faq
### yaml publish chart draft?
Theme agent content primitive draft publish evidence cache token markdown schema schema source chart.
### primitive source review static?
Flow render token render fast render git source static agent cache diagram spec draft theme publish markdown feed chart.
:::

::cta{title="spec content source" button="Read the spec" href="https://snypd.rocks/spec"}
