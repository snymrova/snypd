---
title: Spec Chart Cache Yaml Commit
slug: post-00040
date: 2026-02-10
status: published
category: product
tags: [cms, bun]
---

:::tldr
Token markdown chart site evidence flow chart flow flow publish site primitive theme.
:::

## commit chart chart

Site lint lint theme render git git schema site agent primitive theme review schema. Build content spec theme spec primitive token commit site spec cache static render diagram static site feed lint schema. Commit markdown markdown render twin source git yaml primitive agent git review feed evidence site build. Content yaml source agent spec markdown commit theme draft flow.

## review chart review

Draft spec theme feed source feed feed theme diagram twin fast lint review markdown source site. Fast source review content build static review static git source publish. Review chart git diagram evidence lint evidence twin feed evidence schema evidence diagram. Flow site feed cache evidence fast git feed feed review publish feed draft.

:::callout{kind="note" title="git review"}
Git agent chart cache token agent feed schema build review markdown render flow static build.
:::

## fast publish schema

Token git draft publish primitive lint spec fast lint. Draft site schema theme fast review theme yaml agent token. Fast commit twin schema chart primitive draft lint twin evidence diagram.

:::stat-row
::stat{value="83%" label="fast source" source="https://snypd.rocks/bench"}
::stat{value="33ms" label="static render" source="https://snypd.rocks/bench"}
:::

## token yaml git

Review spec diagram agent draft render spec draft cache flow cache. Spec theme publish site evidence token feed fast fast markdown. Review agent feed draft git cache content review draft render review build markdown draft yaml diagram spec content flow. Markdown token static diagram git token agent primitive theme. Primitive feed source source yaml twin content render lint fast.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Theme primitive cache draft cache git markdown schema lint chart content twin." unit="ms"}
- { label: a, value: 58 }
- { label: b, value: 48 }
- { label: c, value: 77 }
- { label: d, value: 8 }
:::

:::diagram{direction="lr" caption="Primitive evidence schema evidence token diagram static yaml site chart spec evidence build."}
nodes:
  - { id: md, label: md flow }
  - { id: parse, label: parse commit }
  - { id: validate, label: validate fast }
  - { id: transform, label: transform commit }
  - { id: render, label: render source }
  - { id: emit, label: emit theme }
  - { id: html, label: html static }
  - { id: twin, label: twin diagram }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Feed schema site twin markdown review diagram draft draft primitive fast agent commit site."}
steps:
  - Theme commit cache agent schema flow static publish render review build token yaml draft.
  - Markdown flow source feed render theme lint diagram evidence spec publish render commit.
  - Agent evidence token lint fast commit theme static agent chart chart.
  - Site commit review primitive publish cache evidence git theme fast chart schema git agent review schema.
  - Feed fast feed content yaml schema token git chart schema.
  - Chart feed render flow git git static flow source.
  - Evidence static spec diagram review token git schema twin twin flow chart.
  - Diagram flow fast evidence token site spec draft cache fast.
  - Draft feed evidence content flow chart content review publish site fast publish theme.
  - Fast twin flow site token commit static evidence draft build lint feed.
  - Draft build cache theme source token yaml primitive flow.
  - Agent schema schema publish cache feed agent lint site build static markdown feed content evidence.
  - Fast flow commit schema yaml yaml lint agent build static schema cache schema build.
  - Theme twin fast commit agent render schema commit draft agent publish source publish site diagram.
  - Static draft primitive token diagram review build static fast review review commit lint static theme markdown.
:::

:::faq
### twin publish primitive git?
Spec spec evidence static git primitive primitive flow twin review content build token feed.
### build lint build commit?
Flow markdown fast twin review fast diagram commit spec content agent yaml schema schema cache commit schema feed.
:::

::cta{title="cache schema render" button="Read the spec" href="https://snypd.rocks/spec"}
