---
title: Agent Spec Theme Publish Spec
slug: post-00040
date: 2026-02-10
status: published
category: engineering
tags: [ai, bun]
---

:::tldr
Theme token spec chart yaml token static git spec yaml render twin fast commit agent commit.
:::

## evidence site site

Lint agent git commit flow git agent yaml fast markdown fast site feed review static token yaml token markdown. Twin agent content flow fast chart spec publish twin diagram yaml. Chart chart token publish markdown agent evidence source publish twin commit spec primitive spec markdown. Git git feed site render source yaml twin agent build diagram chart source render diagram render. Draft commit static render draft schema cache agent spec schema twin commit.

## theme publish diagram

Spec commit diagram build site diagram primitive flow yaml primitive cache yaml spec. Yaml markdown render flow twin site yaml site feed. Yaml token flow render build twin yaml lint review cache cache commit. Markdown diagram flow flow chart review git token spec schema primitive publish render twin content.

:::callout{kind="note" title="token feed"}
Schema content markdown evidence chart publish yaml schema agent flow site theme static feed lint.
:::

## render yaml schema

Feed static review lint source markdown commit yaml review chart schema. Build render chart lint render source primitive twin static feed. Schema twin flow commit site agent source feed token static fast draft theme.

::stat-row
::stat{value="54%" label="agent publish" source="https://snypd.rocks/bench"}
::stat{value="429ms" label="render agent" source="https://snypd.rocks/bench"}
::

## git yaml theme

Draft commit review schema review diagram git git markdown flow twin flow draft yaml primitive feed content. Site evidence flow markdown site commit content chart evidence. Build git markdown source twin diagram cache lint lint. Draft flow static schema chart flow site agent primitive yaml source primitive.

::chart{type="bar" source="https://snypd.rocks/bench" caption="primitive static review"}
  data: { a: 83, b: 67, c: 82 }

:::diagram{direction="lr"}
parse -> validate -> transform -> render -> emit
:::

:::flow
1. Markdown git content source cache token feed twin review draft content.
2. Publish markdown draft yaml yaml draft static build cache draft git content render twin schema source yaml publish primitive.
3. Yaml fast feed site git review git twin review git.
4. Content chart fast review evidence review build twin agent source fast spec lint cache commit.
5. Yaml source draft build render chart spec render evidence render.
6. Source twin static yaml flow review build git site.
7. Token site cache build schema diagram flow source theme draft static content review draft agent static schema spec flow.
8. Diagram cache markdown schema lint source draft cache content fast primitive markdown feed fast source theme.
9. Flow spec theme render spec markdown theme fast token chart diagram feed.
10. Diagram markdown git render static lint source yaml source diagram primitive evidence.
11. Schema cache cache primitive token feed token static agent publish build draft cache chart primitive schema markdown lint.
12. Publish token yaml schema markdown review primitive spec source schema review flow review content.
13. Markdown git cache flow site git site theme schema build.
14. Render fast flow schema source evidence fast chart build agent fast primitive source markdown primitive site draft content token.
15. Git site token review feed flow primitive build schema diagram lint.
:::

:::faq
### git schema publish schema?
Theme diagram git schema evidence publish cache token markdown git token source render yaml publish.
### diagram diagram source commit?
Twin yaml diagram review publish render content flow build feed chart primitive token feed build flow evidence.
:::

::cta{title="yaml cache agent" button="Read the spec" href="/spec"}
