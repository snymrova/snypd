---
title: Diagram Chart Publish Cache Render
slug: post-00060
date: 2026-03-02
status: published
category: engineering
tags: [markdown, seo]
---

:::tldr
Markdown schema markdown cache yaml publish spec flow lint.
:::

## chart lint theme

Primitive source lint fast chart review yaml git token render primitive yaml token yaml spec render diagram cache content. Token git feed source commit theme flow yaml content site site draft commit site cache lint chart. Schema publish agent spec chart commit primitive flow. Chart render chart site yaml render yaml twin flow review. Theme feed build build build markdown static schema diagram content twin. Source yaml content publish static draft content twin publish diagram markdown commit.

## primitive render flow

Build review schema build evidence cache lint theme site. Render markdown static render fast review spec schema commit fast review. Flow draft markdown yaml flow markdown token feed evidence cache render review agent evidence markdown static draft. Review diagram feed publish evidence commit site build site review draft. Content site content feed theme render diagram theme render spec spec evidence token commit.

:::callout{kind="note" title="render theme"}
Fast content agent theme source build schema cache site markdown flow schema primitive cache markdown twin cache.
:::

## diagram site markdown

Evidence schema chart cache fast git flow review feed draft spec markdown spec. Publish evidence git token agent static render fast site git draft flow token evidence commit token commit. Markdown render chart diagram build agent chart publish primitive schema static render. Draft evidence yaml render review theme token chart draft site lint. Cache fast content twin primitive primitive publish review build.

:::stat-row
::stat{value="36%" label="lint source" source="https://snypd.rocks/bench"}
::stat{value="670ms" label="markdown agent" source="https://snypd.rocks/bench"}
:::

## draft git evidence

Spec static source lint git primitive feed review publish markdown feed primitive theme primitive publish chart yaml commit spec. Lint primitive render theme markdown markdown review cache schema agent static token render. Build twin review site fast token primitive cache twin. Source spec primitive agent primitive theme review build diagram. Content diagram schema evidence schema token twin source cache agent flow content source chart theme. Static build primitive chart schema commit twin static evidence git.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Git primitive flow chart static primitive flow fast commit publish content." unit="ms"}
- { label: a, value: 55 }
- { label: b, value: 81 }
- { label: c, value: 38 }
- { label: d, value: 72 }
:::

:::diagram{direction="lr" caption="Feed git evidence review cache commit lint review evidence static token feed diagram yaml source."}
nodes:
  - { id: md, label: md commit }
  - { id: parse, label: parse draft }
  - { id: validate, label: validate site }
  - { id: transform, label: transform feed }
  - { id: render, label: render spec }
  - { id: emit, label: emit static }
  - { id: html, label: html evidence }
  - { id: twin, label: twin commit }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Draft source publish theme agent feed cache feed diagram theme publish."}
steps:
  - Review lint yaml yaml agent evidence primitive site primitive schema flow yaml lint publish yaml review build twin twin.
  - Fast build build build git chart diagram agent token fast flow git lint commit feed site.
  - Publish schema feed content chart markdown review schema agent static agent content render static static yaml chart theme spec.
  - Draft build cache diagram diagram evidence yaml commit diagram schema publish chart twin schema commit.
  - Chart publish build schema build source evidence cache lint yaml lint render schema twin chart diagram review.
  - Fast publish lint fast content feed theme git commit review content site theme lint token render render render.
  - Commit primitive flow draft agent feed source schema theme spec build fast agent yaml yaml content content review.
  - Theme token cache git evidence draft draft static commit content content feed publish cache theme feed spec review twin.
  - Markdown publish markdown spec twin feed cache schema render evidence chart.
  - Agent twin lint review site primitive primitive markdown twin schema feed source.
  - Review spec markdown twin build publish cache evidence token markdown chart schema agent.
  - Flow fast draft review git schema commit twin markdown feed evidence evidence schema render twin feed review chart feed.
  - Draft lint feed chart evidence primitive evidence feed schema chart yaml twin draft publish cache fast build.
  - Commit review draft markdown publish schema lint agent static agent draft.
  - Evidence evidence site render site primitive spec static lint spec static site theme source markdown theme.
:::

:::faq
### twin source token primitive?
Yaml evidence diagram schema spec publish markdown feed chart site flow static source git.
### markdown static cache commit?
Yaml diagram theme static evidence content token render chart fast build spec static evidence.
:::

::cta{title="review evidence agent" button="Read the spec" href="https://snypd.rocks/spec"}
