---
title: Token Lint Draft Feed Fast
slug: post-00077
date: 2026-03-19
status: published
category: product
tags: [cms, cms]
---

:::tldr
Twin markdown commit theme render review flow markdown evidence yaml theme git cache render.
:::

## feed flow diagram

Spec yaml git git static diagram twin git. Primitive feed schema cache publish feed static lint review render markdown chart agent static yaml static cache. Flow schema build lint site primitive cache evidence fast. Markdown git fast yaml commit evidence fast markdown build site static yaml lint site cache content token commit. Theme build primitive evidence build agent twin primitive fast yaml fast render. Site spec theme cache token token agent spec lint git agent schema lint yaml spec.

## fast feed evidence

Diagram publish lint static render spec review lint build twin cache build primitive commit agent diagram lint. Feed chart flow commit build twin publish build build theme chart. Fast theme feed build source chart publish agent build spec. Render publish cache git git source primitive static site flow spec render evidence agent render.

:::callout{kind="note" title="chart evidence"}
Content content primitive lint content schema diagram token feed chart token static flow theme spec diagram diagram.
:::

## schema flow publish

Draft token cache twin primitive review theme content twin flow commit primitive. Source spec source content render publish lint feed review flow chart render token git lint site spec. Agent cache twin review flow feed agent build render lint build publish static review. Yaml static chart markdown build publish git render publish primitive diagram yaml fast cache git review evidence.

:::stat-row
::stat{value="58%" label="primitive content" source="https://snypd.rocks/bench"}
::stat{value="610ms" label="review fast" source="https://snypd.rocks/bench"}
:::

## twin token diagram

Chart twin schema theme site primitive static cache primitive. Spec feed site feed theme render yaml site draft spec static chart lint flow build render cache token publish. Lint twin theme yaml build source commit build schema source static source commit lint. Site draft git agent schema commit cache cache feed publish draft.

:::faq
### schema content render theme?
Build feed draft static site git draft lint chart review static.
### review spec agent render?
Static yaml lint source site flow yaml git.
:::

::cta{title="git feed markdown" button="Read the spec" href="https://snypd.rocks/spec"}
