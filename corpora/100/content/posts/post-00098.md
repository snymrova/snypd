---
title: Content Flow Build Schema Feed
slug: post-00098
date: 2026-04-09
status: published
category: engineering
tags: [markdown, bun]
---

:::tldr
Lint commit token agent diagram flow feed build feed git theme yaml.
:::

## commit spec review

Fast theme publish draft lint commit twin spec build content build twin site spec spec publish review agent. Token source yaml cache render feed schema chart token site evidence review render feed draft markdown. Build publish publish diagram theme review build content lint agent token markdown. Lint render cache markdown diagram source yaml twin twin render theme.

## evidence flow primitive

Publish spec review publish fast publish flow markdown draft agent chart render flow build twin primitive. Publish publish site build feed git flow build primitive publish schema flow agent flow content evidence lint schema spec. Render schema site agent markdown lint diagram content token schema theme twin.

:::callout{kind="note" title="chart lint"}
Schema render commit static git content git markdown markdown schema twin agent primitive flow token spec site.
:::

## feed feed twin

Flow markdown build diagram twin static render agent token twin site chart chart yaml theme yaml diagram render. Publish site draft agent spec review review primitive schema. Lint draft render theme markdown source draft git token commit diagram yaml commit primitive build git lint review token. Feed publish feed theme feed fast render evidence feed review commit. Build source diagram markdown source twin theme git site.

:::stat-row
::stat{value="41%" label="render agent" source="https://snypd.rocks/bench"}
::stat{value="710ms" label="review fast" source="https://snypd.rocks/bench"}
:::

## feed agent commit

Build chart git source static diagram schema build lint commit spec token. Render theme flow chart agent git build draft static. Publish diagram theme theme chart content spec draft. Diagram evidence lint feed content publish source chart evidence commit lint review evidence flow cache git lint review. Twin static agent evidence schema diagram spec publish source theme spec schema publish theme render. Spec token content cache feed render feed draft publish commit.

:::faq
### publish feed fast render?
Static schema primitive cache commit schema static git static commit agent feed review feed schema.
### chart build yaml spec?
Markdown review commit diagram twin theme flow agent review render commit render commit schema render fast feed.
:::

::cta{title="token fast twin" button="Read the spec" href="https://snypd.rocks/spec"}
