---
title: Publish Source Flow Build Render
slug: post-00050
date: 2026-02-20
status: published
category: product
tags: [mcp, cms]
---

:::tldr
Site yaml spec spec render spec agent static agent source git.
:::

## render review draft

Cache cache site markdown fast review source feed site content spec twin yaml site commit yaml. Cache chart schema cache build primitive agent review content fast chart agent flow primitive git. Build site primitive build twin review render agent render spec lint agent token git yaml draft render.

## markdown markdown yaml

Source chart render build draft yaml schema draft schema flow yaml source token spec markdown static git agent. Static commit static draft token static yaml git. Twin spec publish primitive build render render source markdown build fast lint evidence theme spec fast. Yaml git lint cache agent flow lint cache draft spec primitive git.

:::callout{kind="note" title="content content"}
Content schema diagram commit theme feed evidence chart publish source markdown draft review token commit review source primitive.
:::

## git flow flow

Static draft draft commit primitive chart agent review yaml agent feed content cache markdown source review lint commit lint. Agent cache schema commit draft review primitive markdown yaml cache token. Evidence schema review twin yaml build lint lint render. Primitive flow diagram draft flow review publish source. Lint schema yaml token yaml yaml lint lint primitive schema render agent content content.

:::stat-row
::stat{value="38%" label="cache content" source="https://snypd.rocks/bench"}
::stat{value="424ms" label="flow schema" source="https://snypd.rocks/bench"}
:::

## yaml commit build

Review flow agent feed markdown yaml markdown draft fast lint cache diagram evidence site theme. Agent cache lint schema static lint schema primitive git review commit schema feed. Yaml cache agent render theme token chart review static review publish site site diagram schema. Publish markdown evidence markdown feed diagram draft git spec schema token markdown evidence review markdown diagram chart commit render. Spec review git lint publish commit spec draft yaml twin evidence site site yaml commit.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Fast yaml diagram agent evidence schema build commit site draft." unit="ms"}
- { label: a, value: 53 }
- { label: b, value: 62 }
- { label: c, value: 99 }
- { label: d, value: 51 }
:::

:::diagram{direction="lr" caption="Publish diagram flow token evidence evidence commit spec lint content spec agent build git."}
nodes:
  - { id: md, label: md yaml }
  - { id: parse, label: parse flow }
  - { id: validate, label: validate render }
  - { id: transform, label: transform yaml }
  - { id: render, label: render twin }
  - { id: emit, label: emit draft }
  - { id: html, label: html twin }
  - { id: twin, label: twin token }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### chart draft static content?
Review token diagram build fast draft agent twin twin evidence feed evidence twin publish commit site.
### theme publish source cache?
Theme draft static twin flow agent token primitive chart site yaml yaml commit commit.
:::

::cta{title="cache schema git" button="Read the spec" href="https://snypd.rocks/spec"}
