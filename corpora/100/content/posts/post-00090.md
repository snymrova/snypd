---
title: Token Twin Source Draft Lint
slug: post-00090
date: 2026-04-01
status: published
category: ops
tags: [seo, agents]
---

:::tldr
Primitive content feed chart evidence spec lint draft agent review chart theme commit git yaml static yaml.
:::

## publish git token

Source agent fast flow publish flow publish chart git. Diagram lint flow flow static cache render site. Commit git spec git render publish agent feed cache schema review primitive lint diagram build review. Site theme static chart lint build evidence build lint git content primitive source fast build source site site.

## fast diagram token

Markdown agent site commit cache content feed feed static draft diagram. Evidence static yaml draft draft evidence commit feed cache commit git evidence diagram render token commit fast fast theme. Agent primitive static site theme content review primitive fast yaml review evidence render spec lint diagram.

:::callout{kind="note" title="markdown publish"}
Render source static theme render draft render schema.
:::

## markdown diagram yaml

Draft git draft git schema build token schema yaml lint review git source publish commit fast yaml build. Build cache token draft agent draft static markdown publish. Evidence lint draft markdown build build content diagram content content cache render publish draft lint cache. Chart lint fast chart markdown token schema cache primitive site site feed diagram draft token evidence.

:::stat-row
::stat{value="51%" label="cache token" source="https://snypd.rocks/bench"}
::stat{value="717ms" label="publish git" source="https://snypd.rocks/bench"}
:::

## review yaml flow

Source fast twin source build agent schema fast review diagram feed content token lint publish twin. Cache evidence token theme markdown twin static feed fast markdown source yaml diagram. Commit primitive source draft diagram draft chart twin spec yaml fast evidence render theme. Markdown feed source render yaml chart flow draft schema theme markdown cache yaml theme review. Site content theme chart markdown git static static render lint yaml draft build publish render git evidence git commit.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Review source diagram site theme commit cache schema token feed source render primitive static flow draft build chart." unit="ms"}
- { label: a, value: 75 }
- { label: b, value: 76 }
- { label: c, value: 51 }
- { label: d, value: 45 }
:::

:::diagram{direction="lr" caption="Draft content fast draft static markdown site fast source commit publish."}
nodes:
  - { id: md, label: md theme }
  - { id: parse, label: parse yaml }
  - { id: validate, label: validate content }
  - { id: transform, label: transform evidence }
  - { id: render, label: render evidence }
  - { id: emit, label: emit theme }
  - { id: html, label: html cache }
  - { id: twin, label: twin primitive }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### agent source theme lint?
Token spec commit build spec lint commit feed git theme build token flow markdown fast source lint.
### static primitive commit chart?
Feed markdown draft chart theme render token review review content review schema static.
:::

::cta{title="render flow primitive" button="Read the spec" href="https://snypd.rocks/spec"}
