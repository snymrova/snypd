---
title: Twin Static Source Static Source
slug: post-00090
date: 2026-04-01
status: published
category: ops
tags: [seo, mcp]
---

:::tldr
Render commit flow build agent twin chart review review site flow publish review markdown.
:::

## yaml commit cache

Build publish cache flow static agent commit theme feed primitive theme feed review diagram yaml static primitive. Review primitive schema publish schema twin yaml publish schema render flow lint yaml spec source fast commit build agent. Publish evidence lint content review fast flow publish agent diagram. Schema spec review build theme review site source theme content draft spec agent render render source site diagram. Static diagram token flow token git render twin.

## lint theme render

Static yaml evidence draft review draft cache source flow agent build markdown yaml git source diagram render spec. Chart schema markdown git site commit build git twin markdown publish. Evidence cache lint schema lint build agent evidence fast token agent commit draft token flow yaml source. Draft fast fast static draft fast schema git schema review. Source token token token schema static review schema cache content twin flow source spec markdown. Lint site commit markdown diagram yaml git flow schema feed build publish render.

:::callout{kind="note" title="chart schema"}
Feed lint spec spec draft spec content source schema static commit draft.
:::

## agent fast chart

Commit cache primitive agent build git evidence spec cache yaml markdown lint chart feed static theme flow lint. Diagram spec schema token draft primitive review token theme content feed spec feed build static. Commit cache content review cache source token spec diagram fast static render markdown twin feed source.

:::stat-row
::stat{value="17%" label="spec theme" source="https://snypd.rocks/bench"}
::stat{value="108ms" label="static primitive" source="https://snypd.rocks/bench"}
:::

## theme commit theme

Primitive yaml twin source git token content build spec yaml flow twin lint build review. Chart markdown primitive flow lint token content content agent diagram. Publish lint diagram primitive diagram commit token draft evidence theme. Publish content review token spec static flow agent publish token source primitive markdown.

:::chart{type="donut" source="https://snypd.rocks/bench" caption="Twin theme site git schema review review draft schema evidence agent token site lint twin static markdown lint." unit="ms"}
- { label: a, value: 13 }
- { label: b, value: 82 }
- { label: c, value: 38 }
- { label: d, value: 45 }
:::

:::diagram{direction="lr" caption="Content flow theme build evidence static commit git primitive draft commit chart schema markdown draft."}
nodes:
  - { id: md, label: md spec }
  - { id: parse, label: parse site }
  - { id: validate, label: validate yaml }
  - { id: transform, label: transform theme }
  - { id: render, label: render token }
  - { id: emit, label: emit primitive }
  - { id: html, label: html content }
  - { id: twin, label: twin spec }
edges:
  - { from: md, to: parse, label: token }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: fast }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: site }
  - { from: validate, to: md }
:::

:::faq
### spec static static static?
Static git draft publish git diagram source diagram yaml.
### yaml feed primitive diagram?
Twin commit draft primitive theme cache lint markdown build token source token schema git agent render markdown draft draft.
:::

::cta{title="fast render chart" button="Read the spec" href="https://snypd.rocks/spec"}
