---
title: Git Render Token Commit Static
slug: post-00020
date: 2026-01-21
status: published
category: ops
tags: [cms, ai]
---

:::tldr
Schema yaml token cache site agent theme source spec token.
:::

## evidence git review

Source site git yaml token static static chart draft lint commit feed chart. Evidence primitive cache theme spec twin source chart flow lint diagram. Fast review diagram feed commit evidence git schema review build render. Content fast twin spec lint twin build render agent twin.

## commit fast build

Yaml static evidence build markdown content cache build cache feed site primitive fast. Schema twin schema diagram lint git chart content token primitive flow. Chart primitive commit feed fast draft schema cache render static content render build static markdown twin review. Build spec source content diagram cache source spec publish yaml twin commit cache. Markdown token commit draft feed fast markdown source theme spec primitive.

:::callout{kind="note" title="diagram publish"}
Primitive evidence chart review agent yaml static lint render yaml content.
:::

## source theme static

Render schema site schema flow feed chart static token render agent spec schema evidence lint. Twin site review build agent draft review git source build render cache. Token lint evidence token flow publish content markdown commit agent flow publish. Site markdown fast commit agent schema feed review build feed fast fast spec theme draft publish.

:::stat-row
::stat{value="54%" label="site theme" source="https://snypd.rocks/bench"}
::stat{value="240ms" label="chart markdown" source="https://snypd.rocks/bench"}
:::

## cache primitive evidence

Build build git source flow source token git source git site diagram content diagram evidence agent static twin. Publish evidence twin diagram yaml cache review token review. Markdown evidence theme source draft review lint twin git lint markdown content theme schema publish source spec. Review markdown cache theme feed cache site review flow schema cache feed yaml.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Static twin build render source spec publish fast build review commit site evidence build." unit="ms"}
- { label: a, value: 90 }
- { label: b, value: 98 }
- { label: c, value: 17 }
- { label: d, value: 38 }
:::

:::diagram{direction="lr" caption="Theme git twin source agent publish cache commit content agent primitive feed chart evidence flow."}
nodes:
  - { id: md, label: md markdown }
  - { id: parse, label: parse render }
  - { id: validate, label: validate build }
  - { id: transform, label: transform content }
  - { id: render, label: render source }
  - { id: emit, label: emit evidence }
  - { id: html, label: html content }
  - { id: twin, label: twin agent }
edges:
  - { from: md, to: parse, label: draft }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: chart }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: chart }
  - { from: validate, to: md }
:::

:::flow{caption="Theme token build token lint content twin diagram cache site schema render site."}
steps:
  - { id: start, do: Schema publish primitive fast static evidence token content agent lint primitive markdown. }
  - Token build flow lint schema source markdown spec schema schema token draft publish yaml lint markdown token.
  - ask: yaml agent lint?
    yes: Diagram git evidence primitive static lint markdown diagram.
    no: { then: start }
  - Theme draft publish twin flow chart review evidence theme static.
  - ask: lint build git?
    yes:
      - Twin static schema draft review cache schema flow.
      - Theme flow static build markdown commit review feed build commit publish.
    no: { then: fix }
  - { id: fix, do: Source feed agent source agent token content cache flow markdown yaml draft evidence primitive agent feed source. }
  - Review twin chart agent feed markdown review schema spec build markdown site render agent schema content render evidence.
  - Yaml yaml diagram git source commit git build flow yaml chart markdown diagram source twin twin theme.
:::

:::faq
### diagram flow token chart?
Build theme twin schema build theme git cache agent markdown twin schema publish yaml build git.
### theme primitive yaml flow?
Publish draft schema site commit static token theme git.
:::

::cta{title="publish agent cache" button="Read the spec" href="https://snypd.rocks/spec"}
