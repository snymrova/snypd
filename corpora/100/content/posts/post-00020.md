---
title: Feed Review Build Feed Fast
slug: post-00020
date: 2026-01-21
status: published
category: design
tags: [mcp, ai]
---

:::tldr
Publish fast site theme cache chart markdown cache primitive evidence lint flow build.
:::

## build git source

Token git source git site diagram content diagram evidence agent static twin primitive publish evidence twin diagram. Cache review token review source markdown evidence theme source draft review lint twin git lint. Content theme schema publish source spec site review. Cache theme feed cache site review flow schema. Feed yaml static static twin build render source spec publish fast. Review commit site evidence build schema twin primitive lint git.

## theme git twin

Publish cache commit content agent primitive feed chart. Flow markdown render build content source evidence content agent draft chart chart spec schema publish primitive. Static evidence token content agent lint primitive markdown source token build flow lint schema. Markdown spec schema schema token draft publish yaml lint markdown token yaml agent lint markdown diagram git. Primitive static lint markdown diagram build theme draft publish twin flow chart review evidence theme static. Build git agent twin static schema draft review cache schema flow cache.

:::callout{kind="note" title="theme flow"}
Build markdown commit review feed build commit publish source source feed agent source agent.
:::

## token content cache

Yaml draft evidence primitive agent feed source schema. Twin chart agent feed markdown review schema spec build markdown site render agent schema content render. Diagram yaml yaml diagram git source commit git build flow yaml chart markdown diagram source twin. Theme draft theme token build token lint content twin diagram cache site schema render site diagram flow token chart. Build theme twin schema build theme git cache agent markdown twin schema publish yaml build git. Primitive yaml flow primitive publish draft schema site commit.

:::stat-row
::stat{value="53%" label="token theme" source="https://snypd.rocks/bench"}
::stat{value="552ms" label="publish agent" source="https://snypd.rocks/bench"}
:::

## cache evidence static

Lint flow spec theme primitive git markdown cache markdown draft yaml content commit spec token site primitive chart. Source token schema review cache lint site draft static render primitive chart draft twin twin static fast. Flow markdown feed markdown schema source chart primitive.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Lint flow chart publish lint render spec fast feed fast." unit="ms"}
- { label: a, value: 60 }
- { label: b, value: 98 }
- { label: c, value: 14 }
- { label: d, value: 99 }
:::

:::diagram{direction="lr" caption="Render evidence agent fast chart review diagram build publish yaml build source draft render publish twin draft lint."}
nodes:
  - { id: md, label: md draft }
  - { id: parse, label: parse git }
  - { id: validate, label: validate content }
  - { id: transform, label: transform git }
  - { id: render, label: render feed }
  - { id: emit, label: emit render }
  - { id: html, label: html evidence }
  - { id: twin, label: twin schema }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Content token commit primitive cache diagram diagram chart lint static site evidence markdown review feed."}
steps:
  - Diagram draft spec schema yaml flow spec content agent yaml static lint site lint draft.
  - Build commit git fast markdown review cache render static agent lint source token.
  - Evidence markdown diagram token yaml cache lint diagram primitive spec primitive spec review feed git markdown theme draft.
  - Twin agent lint git chart evidence theme static review.
  - Review publish agent schema diagram evidence token source draft token spec git lint commit.
  - Token theme markdown spec review render markdown flow site markdown lint content theme markdown build primitive spec diagram.
  - Token twin theme token fast schema cache site.
  - Git markdown spec review site review build publish draft review feed chart twin.
  - Chart agent chart site evidence feed git twin lint primitive schema primitive token.
  - Content diagram render lint markdown site diagram commit spec schema diagram fast flow flow publish commit commit chart.
  - Git build schema source source agent feed chart content cache git spec source git site theme primitive evidence.
  - Content chart diagram evidence yaml theme diagram token markdown agent spec token flow chart.
  - Source draft evidence markdown git primitive primitive review twin fast git spec markdown.
  - Markdown theme twin build feed primitive flow twin diagram review.
  - Site diagram spec render token chart schema draft lint site theme build primitive schema source agent render yaml site.
:::

:::faq
### review source agent token?
Feed token agent chart chart markdown agent content draft token spec lint.
### token site build lint?
Spec lint publish markdown agent markdown flow diagram markdown lint git publish.
:::

::cta{title="feed publish diagram" button="Read the spec" href="https://snypd.rocks/spec"}
