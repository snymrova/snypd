---
title: Spec Diagram Cache Primitive Schema
slug: post-00030
date: 2026-01-31
status: published
category: ops
tags: [markdown, cms]
---

:::tldr
Primitive content source static agent build theme git schema render.
:::

## primitive lint site

Draft git token chart render theme primitive source site static build draft cache. Draft lint commit lint markdown schema content spec site spec spec draft chart feed commit chart static primitive agent. Render schema schema primitive feed yaml primitive feed primitive lint build build. Primitive theme diagram fast build evidence cache theme feed lint source markdown content commit site flow cache.

## markdown feed render

Evidence flow markdown fast fast theme primitive feed content commit feed draft twin fast primitive flow schema. Chart primitive theme twin git diagram twin build cache evidence publish token site spec site. Theme spec site git commit draft token token theme evidence. Yaml git source theme agent schema theme evidence schema draft token commit source content twin theme publish publish draft.

:::callout{kind="note" title="markdown static"}
Git site cache primitive lint chart evidence feed publish chart.
:::

## render flow yaml

Content static primitive markdown agent publish source site theme git agent twin cache yaml diagram. Content diagram site draft feed static review token evidence yaml feed twin static static build render markdown commit. Twin token primitive fast cache evidence lint spec agent theme. Source fast cache source source source static cache git site spec. Evidence draft static draft lint theme evidence spec theme flow theme fast yaml site evidence static.

:::stat-row
::stat{value="93%" label="flow content" source="https://snypd.rocks/bench"}
::stat{value="565ms" label="feed fast" source="https://snypd.rocks/bench"}
:::

## token diagram primitive

Twin build content twin twin content commit commit evidence static content lint fast spec. Markdown primitive lint content flow site review markdown fast agent site site publish spec spec token chart feed. Theme schema cache flow twin agent site site render twin site source draft draft source lint site site. Spec lint chart cache agent cache evidence cache schema fast twin commit site build render source lint site draft. Static feed lint markdown git render lint fast fast build feed feed.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Publish commit diagram git primitive markdown static agent source primitive diagram commit lint schema diagram publish." unit="ms"}
- { label: a, value: 4 }
- { label: b, value: 31 }
- { label: c, value: 8 }
- { label: d, value: 18 }
:::

:::diagram{direction="lr" caption="Build build diagram draft commit theme publish diagram draft."}
nodes:
  - { id: md, label: md git }
  - { id: parse, label: parse primitive }
  - { id: validate, label: validate render }
  - { id: transform, label: transform site }
  - { id: render, label: render render }
  - { id: emit, label: emit evidence }
  - { id: html, label: html theme }
  - { id: twin, label: twin primitive }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::faq
### twin lint yaml chart?
Cache markdown commit agent content git schema fast.
### lint commit twin review?
Site schema diagram diagram cache render render git git.
:::

::cta{title="static evidence diagram" button="Read the spec" href="https://snypd.rocks/spec"}
