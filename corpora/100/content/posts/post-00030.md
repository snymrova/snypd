---
title: Schema Build Cache Markdown Agent
slug: post-00030
date: 2026-01-31
status: published
category: engineering
tags: [bun, bun]
---

:::tldr
Draft git agent spec spec yaml agent commit site build cache.
:::

## source content flow

Site commit commit commit commit schema yaml commit flow site review commit. Static publish commit lint spec chart chart review diagram flow diagram site git build. Git site review lint agent schema review twin review token token diagram yaml source render.

## static token flow

Build spec lint site twin draft yaml yaml build publish. Static render site agent feed commit draft feed commit lint static commit yaml commit site. Build content git publish spec schema markdown agent cache feed evidence spec flow site draft flow twin source. Schema review publish draft render schema fast primitive build twin twin lint.

:::callout{kind="note" title="git review"}
Evidence diagram markdown source cache git draft schema review git theme render feed.
:::

## flow primitive theme

Theme primitive draft markdown schema git lint review chart token draft source token build. Render evidence cache token primitive primitive source site spec agent evidence. Source markdown draft diagram source cache spec feed markdown markdown source flow commit chart primitive. Git publish evidence yaml yaml token schema lint agent diagram fast schema twin draft twin git markdown. Chart yaml draft token site static diagram commit.

:::stat-row
::stat{value="52%" label="draft cache" source="https://snypd.rocks/bench"}
::stat{value="47ms" label="publish primitive" source="https://snypd.rocks/bench"}
:::

## git flow git

Content source theme site review spec lint lint lint source. Source review git evidence theme diagram flow publish token chart primitive theme cache. Render review build flow commit feed source lint review draft yaml commit spec flow site cache fast cache flow. Twin source spec theme review evidence primitive publish markdown git yaml evidence fast commit agent.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Commit feed diagram evidence site primitive yaml draft token schema." unit="ms"}
- { label: a, value: 94 }
- { label: b, value: 2 }
- { label: c, value: 65 }
- { label: d, value: 89 }
:::

:::diagram{direction="lr" caption="Evidence commit evidence twin git render fast commit static feed review content."}
nodes:
  - { id: md, label: md schema }
  - { id: parse, label: parse agent }
  - { id: validate, label: validate static }
  - { id: transform, label: transform schema }
  - { id: render, label: render diagram }
  - { id: emit, label: emit agent }
  - { id: html, label: html spec }
  - { id: twin, label: twin twin }
edges:
  - { from: md, to: parse, label: publish }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: chart }
  - { from: validate, to: md }
:::

:::faq
### site feed git agent?
Yaml diagram chart lint source cache spec diagram cache primitive schema flow feed fast primitive primitive content.
### source static agent build?
Git schema render primitive lint site token site draft.
:::

::cta{title="git token chart" button="Read the spec" href="https://snypd.rocks/spec"}
