---
title: Feed Evidence Spec Flow Site
slug: post-00030
date: 2026-01-31
status: published
category: product
tags: [speed, markdown]
---

:::tldr
Publish schema review publish draft render schema fast primitive build twin twin lint git review publish evidence.
:::

## diagram markdown source

Draft schema review git theme render feed flow primitive theme commit static theme primitive draft. Schema git lint review chart token draft source. Build cache render evidence cache token primitive primitive source site spec. Evidence commit source markdown draft diagram source cache.

## spec feed markdown

Flow commit chart primitive source git publish evidence yaml yaml token schema lint agent diagram fast schema. Draft twin git markdown markdown chart yaml draft token site static diagram commit static draft cache markdown publish primitive. Flow git draft build content source theme site review spec lint lint lint source draft.

:::callout{kind="note" title="source review"}
Evidence theme diagram flow publish token chart primitive theme cache feed render review build flow.
:::

## commit feed source

Draft yaml commit spec flow site cache fast cache flow yaml twin source spec theme review. Primitive publish markdown git yaml evidence fast commit agent render commit feed diagram evidence site primitive. Draft token schema feed agent commit flow publish evidence commit evidence twin git render fast. Static feed review content schema agent static schema diagram agent spec twin publish render chart.

:::stat-row
::stat{value="49%" label="feed git" source="https://snypd.rocks/bench"}
::stat{value="30ms" label="chart yaml" source="https://snypd.rocks/bench"}
:::

## diagram chart lint

Spec diagram cache primitive schema flow feed fast primitive primitive content. Static agent build theme git schema render primitive lint site token site draft git token chart render. Primitive source site static build draft cache feed draft. Commit lint markdown schema content spec site spec spec draft chart feed. Chart static primitive agent lint render schema schema primitive feed yaml primitive feed primitive lint. Build source primitive theme diagram fast build evidence cache theme.

:::chart{type="line" source="https://snypd.rocks/bench" caption="Lint source markdown content commit site flow cache markdown feed render site chart evidence flow markdown fast fast theme." unit="ms"}
- { label: a, value: 15 }
- { label: b, value: 94 }
- { label: c, value: 8 }
- { label: d, value: 66 }
:::

:::diagram{direction="lr" caption="Draft twin fast primitive flow schema commit chart primitive theme twin git diagram twin build cache evidence publish token."}
nodes:
  - { id: md, label: md site }
  - { id: parse, label: parse spec }
  - { id: validate, label: validate site }
  - { id: transform, label: transform render }
  - { id: render, label: render theme }
  - { id: emit, label: emit spec }
  - { id: html, label: html site }
  - { id: twin, label: twin git }
edges:
  - { from: md, to: parse, label: commit }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: draft }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: token }
  - { from: validate, to: md }
:::

:::faq
### token theme evidence feed?
Git source theme agent schema theme evidence schema draft token commit source content twin theme.
### publish publish draft markdown?
Render git site cache primitive lint chart evidence feed publish chart render flow yaml.
:::

::cta{title="review yaml content" button="Read the spec" href="https://snypd.rocks/spec"}
