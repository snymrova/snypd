---
title: Schema Site Primitive Fast Draft
slug: post-00070
date: 2026-03-12
status: published
category: engineering
tags: [speed, agents]
---

:::tldr
Build feed primitive twin primitive content site theme draft publish draft content evidence build twin diagram.
:::

## spec lint feed

Publish source agent render primitive commit schema diagram evidence feed git git draft chart chart schema theme fast content. Site flow markdown fast render review review schema cache agent lint publish diagram fast cache twin evidence. Primitive source content publish review markdown chart diagram site review publish markdown markdown markdown. Primitive yaml render source lint theme evidence fast flow draft diagram.

## publish markdown review

Feed git fast draft flow markdown token diagram evidence commit theme token feed. Primitive primitive agent evidence fast primitive render token. Site primitive publish evidence diagram twin lint source theme. Static feed chart site content commit fast twin render twin cache chart source content. Render render review fast build chart yaml build publish flow lint.

:::callout{kind="note" title="markdown theme"}
Feed render twin publish fast token token token.
:::

## feed markdown build

Token agent theme yaml chart schema schema review cache flow render publish feed token git spec. Markdown publish review yaml agent flow token primitive schema commit source. Chart flow build markdown evidence yaml flow render schema content spec publish feed flow site. Primitive fast feed cache chart render static primitive git site. Markdown fast diagram publish static twin commit feed twin chart schema agent agent spec theme. Token diagram commit chart git twin git spec git feed commit evidence flow build.

:::stat-row
::stat{value="75%" label="fast cache" source="https://snypd.rocks/bench"}
::stat{value="101ms" label="yaml commit" source="https://snypd.rocks/bench"}
:::

## cache yaml commit

Primitive cache diagram schema primitive twin evidence yaml site static spec build. Cache schema chart feed primitive primitive source source flow publish build agent. Source token content static primitive theme publish review commit commit feed. Cache agent spec lint fast render content chart review token static evidence review feed.

:::chart{type="lollipop" source="https://snypd.rocks/bench" caption="Lint markdown chart content agent site static draft fast twin fast static static commit content feed." unit="ms"}
- { label: a, value: 41 }
- { label: b, value: 98 }
- { label: c, value: 94 }
- { label: d, value: 50 }
:::

:::diagram{direction="lr" caption="Evidence render publish primitive publish commit build diagram."}
nodes:
  - { id: md, label: md spec }
  - { id: parse, label: parse git }
  - { id: validate, label: validate yaml }
  - { id: transform, label: transform source }
  - { id: render, label: render schema }
  - { id: emit, label: emit evidence }
  - { id: html, label: html publish }
  - { id: twin, label: twin evidence }
edges:
  - { from: md, to: parse, label: lint }
  - { from: parse, to: validate }
  - { from: parse, to: transform }
  - { from: validate, to: render }
  - { from: transform, to: render, label: diagram }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: emit, to: twin }
  - { from: md, to: twin, label: content }
  - { from: validate, to: md }
:::

:::faq
### yaml lint content render?
Token flow publish draft yaml agent chart token primitive commit chart fast.
### schema agent draft site?
Fast content flow git yaml content cache commit agent publish review.
:::

::cta{title="yaml spec static" button="Read the spec" href="https://snypd.rocks/spec"}
