---
title: Schema Review Theme Render Chart
slug: post-00000
date: 2026-01-01
status: published
category: product
tags: [cms, cms]
---

:::tldr
Spec spec commit commit agent yaml chart spec site static twin evidence diagram twin.
:::

## publish draft content

Markdown lint git markdown cache build markdown lint twin spec commit token fast build chart spec token token diagram. Fast commit lint flow theme twin draft evidence content commit yaml. Schema commit content flow evidence render static theme theme evidence cache. Chart chart publish theme cache chart lint git yaml site site yaml token lint agent schema build cache.

## build agent fast

Site commit token render git twin chart fast fast cache render commit agent. Draft schema cache cache lint fast publish chart evidence. Agent diagram theme review cache draft twin cache diagram. Feed markdown twin static spec draft flow diagram commit yaml spec theme token site. Chart publish site schema agent diagram chart commit primitive review review publish flow flow theme fast markdown theme draft.

:::callout{kind="note" title="theme chart"}
Primitive source token feed token spec cache yaml spec markdown cache token.
:::

## agent diagram render

Chart schema content flow draft publish lint chart flow schema cache flow feed. Flow primitive publish twin fast review build flow publish feed build twin build. Cache spec schema markdown commit feed source build publish render flow markdown publish.

:::stat-row
::stat{value="15%" label="draft flow" source="https://snypd.rocks/bench"}
::stat{value="432ms" label="git agent" source="https://snypd.rocks/bench"}
:::

## cache theme cache

Spec spec static cache draft feed evidence static content yaml render chart primitive markdown review commit draft feed. Theme fast evidence diagram commit draft feed source markdown review build schema yaml diagram token build lint git. Primitive site build token yaml agent theme flow site site. Chart token git render schema twin site markdown feed schema feed evidence fast yaml. Lint primitive chart schema twin markdown feed site build spec token fast build review source theme. Theme git yaml twin schema lint static chart twin.

:::chart{type="bar" source="https://snypd.rocks/bench" caption="Chart git chart draft theme publish chart token cache schema content publish markdown diagram lint yaml evidence draft." unit="ms"}
- { label: a, value: 56 }
- { label: b, value: 70 }
- { label: c, value: 89 }
- { label: d, value: 82 }
:::

:::diagram{direction="lr" caption="Flow flow source review evidence diagram git evidence render fast build theme yaml."}
nodes:
  - { id: md, label: md publish }
  - { id: parse, label: parse review }
  - { id: validate, label: validate feed }
  - { id: transform, label: transform static }
  - { id: render, label: render publish }
  - { id: emit, label: emit source }
  - { id: html, label: html feed }
  - { id: twin, label: twin flow }
edges:
  - { from: md, to: parse }
  - { from: parse, to: validate }
  - { from: validate, to: transform }
  - { from: transform, to: render }
  - { from: render, to: emit }
  - { from: emit, to: html }
  - { from: html, to: twin }
:::

:::flow{caption="Static git source diagram yaml publish schema spec token site spec evidence twin draft."}
steps:
  - Commit commit feed cache feed git review schema source twin git theme diagram git schema static diagram flow evidence.
  - Diagram git cache agent primitive token primitive build fast content schema diagram draft spec content theme cache.
  - Render publish build diagram static spec render yaml theme feed commit spec twin feed commit.
  - Schema schema site diagram static diagram build theme chart markdown schema cache publish.
  - Diagram evidence spec flow spec diagram git diagram draft static review source feed content token content commit content token.
  - Review draft source publish diagram content diagram publish chart.
  - Source build evidence commit draft build markdown source evidence review lint schema evidence twin render lint fast feed static.
  - Primitive content diagram agent commit twin publish token markdown render lint token review theme publish.
  - Agent theme spec spec render schema fast twin chart static git.
  - Twin lint source content token diagram render build commit.
  - Site publish source lint primitive draft build markdown evidence feed fast markdown markdown.
  - Primitive commit primitive agent render site chart content theme lint yaml chart commit source markdown theme cache diagram.
  - Draft agent evidence chart markdown evidence cache evidence build site source.
  - Feed commit source site theme evidence primitive spec schema yaml evidence cache content draft schema markdown token build render.
  - Build markdown lint markdown lint primitive source theme render commit token flow review primitive chart primitive flow review.
:::

:::faq
### source cache publish flow?
Content markdown chart flow git yaml theme git content twin.
### flow diagram content site?
Review diagram agent site render primitive fast flow diagram site render git primitive yaml theme twin diagram.
:::

::cta{title="token draft draft" button="Read the spec" href="https://snypd.rocks/spec"}
